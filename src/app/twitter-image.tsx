/**
 * Twitter card image — reuses the OpenGraph image design.
 * Twitter supports summary_large_image cards at the same 1200×630 size.
 */
export { default, alt, size, contentType } from './opengraph-image'
