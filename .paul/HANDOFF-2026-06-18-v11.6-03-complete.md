# PAUL Handoff

**Date:** 2026-06-18
**Status:** paused — clean (all work committed + pushed; phase v11.6-03 closed)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (centralreform.live + brotherslazaroff.live) — multi-tenant worship-band setlist/chart app (CRC + Brothers Lazaroff, both live).
**Core value:** the band gets the right charts on their iPads each week; Daniel authors setlists via Claude + MCP (the browser app is the band/consumer surface — Perform mode on 11" iPads).

---

## Current State

**Milestone:** v11.6 — Airtight (Weekend Stress & Usability). **3 of 4 phases complete.**
**Phase:** v11.6-03 (Off-site resilience, flaky wifi) — **✅ COMPLETE 4/4 + transitioned.**
**Next phase:** v11.6-04 (Authoring + publish round-trip) — the FINAL milestone phase, **not started**.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY        [phase v11.6-03 CLOSED; loop IDLE]
  ✓        ✓        ✓
```

Latest pushed commit: **`c6f75080a3`** (phase v11.6-03 close). Branch `master`, in sync with origin (this pause commit will follow).

---

## What Was Done (this session)

1. **Resumed** at v11.6-03-02 shipped; archived the consumed handoff.
2. **Shipped WS-10 (`df0bd6b433`)** — v11.6-03-03: `SmartScoreViewer` now resolves MusicXML bytes **IDB-first by `fileId`** (`offline-idb getFile` → Blob API read, no `fetch(blob:)` round-trip) → network-fallback `/api/drive/file/<id>`, mirroring Text/Audio/PDF. PDFOverlay musicxml branch passes `fileId` (Image branch `fileUrl` untouched). tsc 0 · smart-score-viewer 32/32 (3 new) · `next build --webpack` 0. (6 failing perform/music tests baseline-confirmed PRE-EXISTING.)
3. **Closed phase v11.6-03 (`c6f75080a3`)** — v11.6-03-04: WS-29 + WS-30 **ACCEPTED as residuals (Decision A, no production code)**. Verify-first code-trace established a full offline reload of an already-visited `/perform` URL **already recovers** (networkFirst HTML + cacheFirst chunks); the only residual is soft-nav to an UNVISITED setlist offline. WS-29 = AudioViewer best-effort IDB + accepted iPad-WebKit `<audio src=blob:>` rejection. SW left untouched (cycle-9 recovery-loop history). Recorded in STATE Decisions + Deferred Issues; UAT items added.
4. **Transition:** PROJECT.md + ROADMAP.md evolved (phase ✅), STATE/paul.json → v11.6-04.

(Phase-03 full record: 01 verify+decide→C · 02 WS-12/13 `7ac307812f` · 03 WS-10 `df0bd6b433` · 04 WS-29/30 accepted. P0 gig-packet text-chart fix `a48382929a` shipped earlier in the milestone, prod-verified 16/16.)

---

## What's Next

**Immediate:** `/paul:plan` for **Phase v11.6-04 — Authoring + publish round-trip** (FINAL phase; closing it completes the v11.6 milestone).
- Finalize the 3 Camp Sabra sets via MCP: keys/BPM/order/bonds airtight, `verify_setlist_charts` clean.
- Airtight publish-&-deliver: `publish_setlist` recipient model + QR sign-in + push/in-app notify.
- **STOP-gate:** the live publish/notify SEND notifies real band members → plan `autonomous: false` for the send. Use `preview_publish` / `dryRun:true` to test; real send only on Daniel's explicit confirmation; record real-send confirmation → `.paul/UAT-PENDING.md`.
- `/ui-ux-pro-max` if any UI is touched.

**After that:** v11.6 milestone close (`/paul:complete-milestone`) → version bump + tag.

---

## Anchor fixtures (the 3 weekend sets, all chart-healthy, none published)
- Shir Shabbat — Juneteenth (Fri Jun 19) `a84f8cce-176e-4b5e-9653-4df71db6f5ba` — all PDF.
- Camp Sabra — Havdalah & Kids Singalong (Sat Jun 20) `7e005452-7c42-4cdc-b27d-ff0c78b6667b` — mostly text/plain.
- Camp Sabra — Staff Concert (Late Night) (Sat Jun 20) `7c640a8a-358e-48ee-8523-6b8a0eca9d05` — all text/plain.
  (Note: there is also a text-chart set `c2075393-d994-4643-ade3-432a6864d87f` "Camp Sabra — Staff Concert — June 20" referenced in the gig-packet UAT.)

---

## Open UAT (real iPad / live MCP, post-deploy, non-blocking) — `.paul/UAT-PENDING.md`
- **v11.6-03-03 (WS-10):** with a MusicXML chart cached, go offline → it renders from IDB (forward-risk: no MusicXML in the 3 sets).
- **v11.6-03-04 (WS-29/30):** WS-30 hard-reload of a visited /perform URL offline recovers (accepted residual: soft-nav to an unvisited set offline fails); WS-29 audio offline fails gracefully on iPad WebKit.
- Earlier: v11.6-02 reading items, v11.6-02.1 gig-packet re-print (David), v11.2/v11.1 carry-overs.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live state — Session Continuity = exact next command |
| `.paul/ROADMAP.md` | § Phase v11.6-04 (the next phase to plan) |
| `.paul/UAT-PENDING.md` | Non-blocking field-confirm items |
| `.paul/phases/v11.6-03-offsite-resilience/v11.6-03-04-SUMMARY.md` | Last slice (WS-29/30 accepted) |
| `.paul/research/v11-6-03-04-sw-offline-reload-verify-2026-06-18.md` | The SW offline-reload code-trace (WS-30 verdict) |
| `public/perform-shell-sw.js` | The load-bearing Perform SW (do not churn — cycle-9 history) |

---

## Working Notes / Posture
- **Autonomy:** auto-commit + push per plan/phase to prod `master`; bake decisions into PLANs; Daniel paces with "go" per PAUL step. STOP at phase boundaries / STOP-gates (v11.6-04 live send) / real decisions.
- **Quality floor:** tsc 0 + tests green + AC proof; `SKIP_ENV_VALIDATION=1 npx next build --webpack` before declaring deployable; `/ui-ux-pro-max` BLOCKING on UI.
- **Git:** master (prod). `git pull` first next session (multi-computer); push `origin master` (NOT `master:main`).
- **Operational carry:** David STAYS admin (settled). F4 BL key badges still await authoring (18 BL charts). INFRA (not app code): `/perform` cold·mobile CLS 0.250 + Vercel TTFB keep-warm.
- **v11.7 follow-up noted:** unify `TextScoreViewer`'s local chord-token detection onto the shared `text-chart-pdf.ts` module (duplicated during the P0).

---

## Resume Instructions
1. `git pull` (multi-computer).
2. Read `.paul/STATE.md` (Session Continuity = exact next command).
3. Run `/paul:resume` (or `/paul:plan` directly for v11.6-04 — load `/ui-ux-pro-max` if it touches UI; remember the live-send STOP-gate).

---
*Handoff created: 2026-06-18*
