# PAUL Handoff

**Date:** 2026-04-26 (after v50-05-01 close)
**Status:** paused — clean checkpoint between plans within v50-05 (context-budget pause before tackling the cutover plan)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — worship band setlist/chart app.
**Core value:** Musicians can instantly access setlists, transpose charts, and control monitor mixes — live on any device, no paper or prep needed.

---

## Current State

**Milestone:** v5.0 — Bulletproof Editor (Local-First Rewrite) — **4 of 7 phases complete (~62%)**
**Phase:** v50-05 of 7 — Spreadsheet editor UI cutover — **1 of 3 plans complete**
**Plan to write next:** v50-05-02 — Cutover

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     [Loop reset — ready for v50-05-02 PLAN]

v50-01 ✓✓✓   v50-02 ✓✓✓   v50-03 ✓✓✓   v50-04 ✓✓✓   v50-05-01 ✓✓✓
v50-05-02: ○ ──▶ ○ ──▶ ○     [Cutover plan — next]
v50-05-03: ○ ──▶ ○ ──▶ ○     [Polish plan — after 02]
```

**Working tree:** clean. **Branch:** master, up to date with origin/master.

---

## What Was Done (this session)

Shipped phase **v50-05 Plan 01** (build SetlistGrid + boot sync engine) end-to-end (PLAN → APPLY → UNIFY). 5 commits on `origin/master`:

- `d72b6b5` — chore(paul): archive prior handoff
- `96428b9` — Task 1: grid scaffold + ProductionFirestoreAdapter + SyncEngineBoot
- `ef5c99d` — Task 2: cell-edit interactions (TextCell + DropdownCell + KeyCell/LeadCell/TypeCell + ChartCell + use-grid-keyboard)
- `f29c46c` — Task 3: drag-reorder + AddRowPlaceholder + Backspace-delete + computeReorderUpdates pure helper
- `1b4ebac` — UNIFY: SUMMARY + STATE + ROADMAP for v50-05-01

**Net delivery:** 20 new files (~3,855 LOC) + 4 modified. 29 new vitest cases, all green. Full suite **1374/1374**. tsc clean. next build clean (77 routes — SetlistGrid intentionally unmounted in prod; legacy editor still serves the route).

**Where the new editor lives:**
- Component tree: `src/components/setlist/grid/*` — exported via `src/components/setlist/grid/index.ts`
- Engine boot: `src/lib/sync/init.ts` (mounted via `LazyClientComponents` → `next/dynamic ssr:false`)
- Hook: `src/hooks/use-grid-keyboard.ts`

**Full plan summary:** `.paul/phases/v50-05-spreadsheet-editor/v50-05-01-SUMMARY.md` (270 lines, includes deviations + decisions + next-phase readiness).

---

## What's In Progress

Nothing — v50-05-01 fully closed at a clean checkpoint. Working tree clean. origin/master in sync.

---

## What's Next: Plan v50-05-02 (Cutover)

**This is mostly subtraction + a one-line route change.** Estimated net **−8,400 LOC** of legacy editor surface (`use-setlist-logic.ts` 818 LOC + supporting modules + `SetlistEditorV2.tsx` + sub-components + tests + `/api/setlist/flush` route + broadcast-channel merge code) replaced by the SetlistGrid component already written in v50-05-01.

### What needs to happen

**1. Route swap (`src/app/(main)/setlists/[id]/page.tsx`):**
- Currently mounts `<SetlistEditorV2 ...>` with ~10 `initial*` props serialized from a server-side Firestore `getDoc`.
- Swap to `<SetlistGrid setlistId={id} name={existingSetlist?.name} eventDateLabel={...} />`.
- The new editor reads tracks from Dexie via live query — tracks need to be hydrated in Dexie before the grid mounts. Two options to choose between (this is the main design decision for v50-05-02):
  - **Option A** — Server-render the setlist data into Dexie via a hydration component. Pass `initialServerData` to SetlistGrid; on first mount, write to Dexie if not already present.
  - **Option B** — Server fetch returns the setlist + tracks; pass them to a `<SetlistGridHydrator>` client component that calls `applyEdit('set', ...)` for each track once on mount, marked idempotent via Dexie ID lookup.
- Pick A or B as a `paul:plan` checkpoint:decision before writing tasks.

**2. Legacy deletion (verify zero non-editor callers before each delete):**

| Path | LOC | Safe to delete? | Notes |
|------|-----|-----------------|-------|
| `src/hooks/use-setlist-logic.ts` | 818 | ✅ | Only consumer is SetlistEditorV2 + its tests |
| `src/lib/setlist-flush.ts` | 73 | ✅ | Only consumer is use-setlist-logic |
| `src/lib/setlist-draft.ts` | 74 | ✅ | Only consumer is use-setlist-logic |
| `src/components/setlist/v2/SetlistEditorV2.tsx` + sub-components | ~1500 | ✅ after route swap | All editor modals here |
| `src/components/setlist/v2/__tests__/setlist-editor-v2.test.tsx` | ~400 | ✅ | |
| `src/hooks/__tests__/use-setlist-logic.test.ts` | ~700 | ✅ | |
| `src/lib/setlist-flush.test.ts` | ~100 | ✅ | |
| `src/app/api/setlist/flush/route.ts` + tests | ~200 | ✅ | Only the editor unload-flush calls this |
| `src/lib/setlist-firebase.ts` editor-only exports (`StaleWriteError`, `updateSetlistWithVersion`, swap helpers) | ~80 | ⚠️ NARROW | Type `Setlist` and `createSetlistService` (list/get/create/duplicate/delete/cloneForNextWeek/saveAsTemplate) are used by ~22 non-editor files — KEEP. Run grep before deleting any specific export |

**3. ChartCell click → match-modal flow:**
- `src/components/setlist/grid/cells/ChartCell.tsx` currently has a no-op `onClick` placeholder.
- Wire it to whatever the existing library/match-modal flow is (search src for `MatchModal` or similar — pattern existed before v50-05).
- Plumb the handler through SetlistGrid props or via TanStack Table `meta` channel (existing convention).

**4. Verification (mandatory):**
- `npm run test` — full suite green (currently 1374; some tests will be removed as legacy tests delete; net regression count must be zero)
- `npx tsc --noEmit` — clean
- `npm run build` — clean (route count drops from 77 → 76 once `/api/setlist/flush` is deleted)
- `pnpm rg -l 'use-setlist-logic|setlist-flush|setlist-draft|SetlistEditorV2' src/` — zero src/ matches outside the deleted-files set
- Manual smoke after deploy to Vercel: open a real setlist in prod, edit a row, verify Saving → Saved indicator + Firestore write lands.

### Required skill

**`/ui-ux-pro-max` is BLOCKING for APPLY** per `.paul/SPECIAL-FLOWS.md`. Load before APPLY (not before PLAN).

### Cutover constraint

App is **intentionally broken-for-band during this phase** — band is not in production right now (waiting on dependability), so a hard cutover with no parallel scaffolding is the strategy. This is the phase the user signed up for.

### After v50-05-02

- v50-05-03 — Touch/iPad + mobile + a11y + multi-select + AlertDialog + ContextMenu + undo
- v50-06 — Concurrent-edit safety + offline + cross-tab (reconciliation modal lands here, plus `expectedUpdatedAt` tracking, plus the cross-tab-lock test flake fix)
- v50-07 — Migration, kitchen-sink, cutover (run `scripts/migrate-v50.ts` apply on prod for real, Playwright kitchen-sink suite, ship to band)

---

## Key Files (for v50-05-02 to read)

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | v5.0 phase overview (4/7 phases; v50-05 1/3 plans) |
| `.paul/PROJECT.md` | Project facts + decisions table |
| `.paul/SPECIAL-FLOWS.md` | Required-skill registry (`/ui-ux-pro-max`) |
| `.paul/phases/v50-01-architecture/ARCHITECTURE.md` | Binding architecture (§6 = spreadsheet editor spec) |
| `.paul/phases/v50-05-spreadsheet-editor/v50-05-01-SUMMARY.md` | What just shipped — read this for the cell/hook contracts |
| `.paul/phases/v50-05-spreadsheet-editor/v50-05-01-PLAN.md` | The plan that just closed |
| `src/components/setlist/grid/index.ts` | Barrel export — what v50-05-02 imports |
| `src/components/setlist/grid/SetlistGrid.tsx` | Top-level grid component to mount at the route |
| `src/lib/sync/init.ts` | ProductionFirestoreAdapter + SyncEngineBoot (already wired into LazyClientComponents) |
| `src/app/(main)/setlists/[id]/page.tsx` | The route that needs the mount swap |
| `src/lib/setlist-firebase.ts` | The file to NARROW (don't delete — non-editor exports stay) |
| `src/hooks/use-setlist-logic.ts` | LEGACY (818 LOC) — to delete |
| `src/components/setlist/v2/SetlistEditorV2.tsx` | LEGACY — to delete after route swap |

---

## Key Context (don't relearn)

- **Band is NOT in production right now.** Broken-for-band periods during the rewrite are acceptable. No parallel-editor scaffolding, no feature flags, hard cutover. App can be intentionally broken during v50-05.
- **Push to `origin master`** (not `master:main`). Deploy straight to production on Vercel; no preview branches.
- **User works from multiple computers** — pull before starting any session.
- **Test pattern locked in:** Dexie-touching tests use `import 'fake-indexeddb/auto'` + `getDb` + `resetDbForTests`. Use `FakeClock` injection (NOT `vi.useFakeTimers`) for any timer-driven Dexie test.
- **cmdk-using tests need stubs:** `ResizeObserver` and `Element.scrollIntoView` must be stubbed at module-eval time of test file. Pattern is in `src/components/setlist/grid/__tests__/SetlistGrid.edit.test.tsx` (lines ~6–18). Reuse this for any v50-05-02 test that mounts cmdk components.
- **Track field aliasing happens at the cell layer.** Editor maps `leadMusician` ↔ helper `lead`; helpers stay generic.
- **`expectedUpdatedAt` is uniformly undefined on track updates** in v50-05. Honest tracking lands in v50-06 alongside the reconciliation modal. Don't add it in v50-05-02.
- **Drag-end testing:** use the exported pure function `computeReorderUpdates` from `SetlistGrid.tsx`. Don't try to simulate pointer/keyboard drag in jsdom.
- **vitest.config.ts testTimeout is 10s** (bumped from 5s in v50-05-01 because parallel pressure tipped engine.test.ts AC-4 over the default). Don't lower.
- **vitest sometimes flakes on `cross-tab-lock.test.ts > exactly one of two instances acquires the lock`** — pre-existing, deferred to v50-06. Don't fix in v50-05-02.

---

## Outstanding (carryover, not blocking v50-05-02)

- **Cross-tab-lock test flake** — fold into v50-06.
- **Production `migrate-v50.ts` apply** — deferred to v50-07.
- **`openai` npm dep + `template-parser.ts`** orphans from v50-02 — safe to delete in a future dep-cleanup pass.
- **Sentry deprecation** — `sentry.client.config.ts` → `instrumentation-client.ts` rename. Cosmetic.
- **`expectedUpdatedAt` tracking + reconciliation modal §6.9** — v50-06.

---

## Resume Instructions

1. `git pull origin master` (multi-computer workflow).
2. `/paul:resume` — will load STATE, archive this handoff, and route to `/paul:plan` for v50-05-02.
3. **Before writing the plan:** read `.paul/phases/v50-05-spreadsheet-editor/v50-05-01-SUMMARY.md` to understand the cell/hook contracts the cutover consumes.
4. **Before APPLY:** invoke `/ui-ux-pro-max` per SPECIAL-FLOWS.md.
5. **Read `src/app/(main)/setlists/[id]/page.tsx` first** — the route swap is one line but you need to know what `initial*` props the legacy editor consumed and decide how to hydrate Dexie (Option A vs B above).
6. **Inventory the legacy editor surface** before declaring delete scope — grep `use-setlist-logic|setlist-flush|setlist-draft|SetlistEditorV2|/api/setlist/flush` for any non-editor caller you missed.
7. **Plan v50-05-02 as 3 tasks max:** (a) route swap + Dexie hydration, (b) legacy deletion, (c) ChartCell binding flow + verification. Vertical slices preferred per plan-format.md.

---

*Handoff created: 2026-04-26 (after v50-05-01 close)*
*Pause reason: context-budget — v50-05-02 deserves a fresh session*
