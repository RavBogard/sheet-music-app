# Handoff — W-04 Plan 01 shipped; Plan 02 + Plan 03 ahead

**Date:** 2026-05-16, end of day
**Branch:** master @ `ec9b33aee` (also feat/mcp-server)
**Author:** Claude (W-04 implementation session)
**Context budget:** This handoff written at ~50-58% context cap per Daniel's instruction.

---

## What's shipped today (both sessions)

Two Claude Code sessions worked concurrently. Together we shipped:

**Morning (the other session — tactical bugstomp triage):**
- `845bee9d4` L-003 search tokenizer normalization
- `f88c8b6c7` A-001/B-002/B-003 chart verify family + publish_setlist pre-flight
- `e4bea186c` L-001 orphan marking via verify_setlist_charts
- `7eee79927` B-001 chunked upload (request_chart_upload_url + finalize_chart_upload)
- `b3f78850a` swap_chart + V4-NOTE-1 chart filter completeness
- `5507059d7` autonomous bug-stomp stress-test prompt

**Afternoon (this session — W-02 + W-04 Plan 01 strategic ship):**
- `525165c37` **W-02 trust calibration** — titleSpecificity scorer + bondCorrectionHistory + sibling cascade + search_library ranking bias + 4 PAUL phase plans (mcp-w01..04) + W-plans research docs
- `9ea59cb34` F-01 (the other session): update_track validates songId before write
- `c8d559ebb` F-05 (the other session): publish_setlist dryRun never refuses on chart-health
- `ec9b33aee` F-04 (the other session) — **with my W-04 Plan 01 piggy-backed in by accident**

## ⚠️ Commit-history caveat

`ec9b33aee` is mislabeled. Its description only mentions F-04 (verify_setlist_charts phantom-bonds split). It ALSO contains all of W-04 Plan 01:
- `+52` lines on `src/lib/mcp/server-tracks-write.ts` (version stamping on every write)
- `+14` lines on `src/lib/setlist-write.ts` (initial version on create + update)
- `+20` lines on `src/lib/mcp/tools/setlists.ts` (envelope surfaces version + lastModifiedAt per setlist + track)
- New `src/lib/mcp/error-envelopes.ts` (177 lines — staleVersionEnvelope + trackNotFoundEnvelope + zodErrorToEnvelope (F-02 absorbed) + stampVersionWrite helpers)
- New `src/lib/mcp/tools/wait-for-setlist-change.ts` (282 lines — long-poll MCP tool, max 60s, immediate-return-on-already-advanced + listener path + timeout path)
- New `src/lib/mcp/__tests__/mcp-wait-for-setlist-change.emulator.test.ts` (237 lines, 7 tests)
- Tool registration in `src/lib/mcp/tools/index.ts`

**Root cause:** shared-worktree race. Both sessions were editing files in `sheet-music-app/` via absolute paths (Edit tool uses absolute paths regardless of `cd`). The other session's `git add -A` then swept everything in the working tree — including my staged W-04 Plan 01 work — into their F-04 commit.

**New standing rule** (committed to project_mcp_status.md memory): in shared-worktree mode, always `git add <explicit/file/list>`, never `git add -A` or `git add .`.

## State of W-04

Plan 01 (Foundation) is **shipped** inside `ec9b33aee`:
- ✅ Every existing MCP write path stamps `version: FieldValue.increment(1) + lastModifiedAt`
- ✅ New setlists + tracks start at `version: 1`
- ✅ `get_setlist` returns `{setlist: {..., version, lastModifiedAt}, tracks: [{..., version, lastModifiedAt}]}`
- ✅ `list_setlists` returns `version` per row
- ✅ `wait_for_setlist_change(setlistId, sinceVersion, timeoutSec?, includeFullState?)` long-poll MCP tool live (timeoutSec 1..60s, default 30s)
- ✅ error-envelopes.ts module ready for Plan 02 consumers
- ✅ 244 emulator tests pass (was 196 pre-W-02; +48 across W-02 + W-04 Plan 01)
- ✅ `next build` clean

Plan 02 (Single-row gating) is **next**. See `.paul/phases/mcp-w04-bidirectional-sync/mcp-w04-01-PLAN.md` for the original full-plan acceptance criteria; Plan 02 + Plan 03 will be split out from there. Estimated ~2 days of focused work.

Plan 03 (Bulk + publish) is **after that**. ~1.5 days.

## What's the NEXT next-best action?

Pick one of these in the next session. Sequenced by impact:

### Option A — W-04 Plan 02 (single-row write gating)

Add optional `lastSeenVersion` arg to:
- `update_track` (reject with `staleVersionEnvelope` on mismatch)
- `update_setlist`
- `remove_track`
- `reorder_setlist`
- `delete_setlist`

Plus E-002 envelope polish: `update_track` on missing trackId returns `trackNotFoundEnvelope` (includes setlistVersion + lastModifiedAt + hint).

Refactor required: every write path needs to convert from `db.batch()` → `db.runTransaction()` so the version check is atomic with the write. The pattern is already present in `bulkUpdateTracks` (line ~719 of `server-tracks-write.ts`) — copy that shape for the others.

The error-envelopes helpers are ready: `staleVersionEnvelope({resource, currentVersion, lastSeenVersion, ...})` and `trackNotFoundEnvelope({trackId, setlistId, setlistVersion, setlistLastModifiedAt})`.

Estimated effort: ~2 days. Plan 01's foundation makes this mostly mechanical.

### Option B — W-01 (propose-then-confirm + learning loop)

5 new MCP tools: `propose_setlist_changes`, `commit_staged_changes`, `preview_publish`, `flag_bond`, `review_flagged_bonds`, `record_bond_correction`. Plus `.paul/AGENT-GUIDE.md`. The inline aggregation of `titleContextHints` lives here (deferred from W-02 §Task 5 since the repo has no Functions infrastructure).

W-01 depends on W-02 (✅ shipped) and is most useful AFTER W-04 (since `commit_staged_changes` should pass `lastSeenVersion`). Doable now in a "race-tolerant" mode if needed.

Estimated effort: ~5 days. Plan in `.paul/phases/mcp-w01-agentic-ux-shape/`.

### Option C — Backfill the W-02 specificity to existing library_index rows

`npx tsx scripts/backfill-title-specificity.ts --apply` against prod. Idempotent. Dry-run first: `npx tsx scripts/backfill-title-specificity.ts` (no flag = dry-run). Existing rows (~500) currently have NO specificity — search_library ranking only kicks in for NEW uploads until this runs.

Daniel approval pending. Recommended before W-01 ships so the learning-loop's ranking bias has data to work with on existing entries.

### Option D — Address F-02 + F-03 follow-ups from the bugstomp report

- **F-02 (Zod envelope normalization):** my error-envelopes.ts already has `zodErrorToEnvelope` ready. Plug it into the MCP tool wrapper layer (likely the `jsonResult` helper in `tools/index.ts`, or a higher-order tool wrapper). Without this, Zod validation failures still throw -32602 instead of returning the envelope.
- **F-03 (order-numbering invariant + W-05):** post-write contiguity check on remove_track / update_track(position) / reorder_setlist. Filed as W-05 by the parallel session. ~0.5 day work.

### Option E — Daniel's call on the chart-bytes public policy

Already settled (open by policy) but the recent verify-charts work surfaces `phantomBonds` separately from `orphanedMarked` now — see F-04's commit. Worth a quick read.

## Coordination notes for the next session

- **PULL FIRST.** Always. Use `git pull --ff-only origin master`. If FF fails, the parallel session moved ahead — investigate before doing anything.
- **STAGE EXPLICITLY.** Per the new memory rule, `git add path/to/file path/to/other` — never `git add -A` or `git add .` in shared-worktree mode.
- **READ project_mcp_status.md FIRST** to know what shipped between sessions.
- **The two worktrees:**
  - `sheet-music-app/` on `master` (production-bound).
  - `sheet-music-app-mcp/` on `feat/mcp-server` (ff-merged from master).
  - They share `.git`. Both branches stay equal; ff-merge `feat/mcp-server` to master after every master push.
- **HANDOFF.md** at repo root: read it; it's the other session's hand-off note. May contain newer context than this file.
- **firebase.bugstomp.json**: untracked config the other session used for an alternate emulator port (8090 instead of 8080) so two emulator instances could run simultaneously. Leave it alone unless you need a second emulator.

## Memory updates made this session

- `project_mcp_status.md` — added W-02 wave entry + W-04 Plan 01 wave entry + new "shared-worktree git add discipline" standing rule.
- `feedback_learning_self_healing.md` (created) — Daniel's recurring product principle that agent-facing features with human-correction steps should self-heal via deterministic counter aggregation, never ML.

## Resume prompt (paste this into the next Claude Code session)

> Resume the W-04 work on centralreform.live MCP server.
>
> Read `.paul/HANDOFF-2026-05-16-w04-plan-01.md` first — it's the full state-of-the-world handoff.
>
> Master is at `ec9b33aee`. W-04 Plan 01 (Foundation) is shipped. Pick the next-best action from Option A–E in the handoff. Default recommendation: **Option A (W-04 Plan 02 — single-row write gating)** since it's the natural continuation and unblocks W-01.
>
> Pull master FIRST before any work. Stage commits with EXPLICIT file paths only (never `git add -A`). The parallel session may have shipped more.
>
> Read `~/.claude/projects/C--Users-dsbog-centralreform-live/memory/project_mcp_status.md` for the up-to-date MCP wave history.
