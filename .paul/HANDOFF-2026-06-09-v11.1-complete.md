# PAUL Handoff

**Date:** 2026-06-09 (session 7)
**Status:** paused — clean (v11.1 milestone CLOSED; tree clean, in sync with origin/master)

---

## READ THIS FIRST

You have no prior context. This document + STATE.md tell you everything.

**Project:** sheet-music-app (centralreform.live) — band charts/recordings on iPads; setlists authored via Claude + MCP. MULTI-TENANT (2nd live tenant: Brothers Lazaroff on brotherslazaroff.live).
**Core value:** The band gets the right charts + recordings on their iPads each week, and Daniel authors setlists conversationally via Claude + MCP.

---

## Current State

**Version:** `package.json` **11.1.0** · tag **`v11.1.0`** (pushed) · origin/master tip `50d4666b49`
**Milestone:** v11.1 Brothers Lazaroff Post-Launch Fixes — ✅ **COMPLETE 2026-06-09** (4/4 phases, archived)
**Phase/Plan:** None active — loop idle, no milestone open.

```
PLAN ──▶ APPLY ──▶ UNIFY      [v11.1 COMPLETE — idle, ready for next milestone]
  ○        ○        ○
```

---

## What Was Done (this session)

Took v11.1 from "1 of 4" to **closed**, fully autonomous per the v11.x directive — three PAUL loops back-to-back, each plan→apply→unify→transition→commit→push, quality floor held every phase (tsc clean · vitest 3339/0 · `SKIP_ENV_VALIDATION=1 next build` clean · /ui-ux-pro-max on UI phases) · CRC byte-identical throughout:

1. **v11.1-01 Org-aware authed branding** (`72f1cdb66e`) — `getOrgBranding.logoUrl`; `(main)/layout.tsx` resolves host org from `x-org-id` → `serverOrgShortName`+`serverLogoUrl` to both nav headers (flash-free SSR); new `OrgLogo` (img, or navy "BL" initials monogram when `logoUrl` empty).
2. **v11.1-03 Library generic-tab visibility** (`3d7471679e`) — `getServerLibrary(orgId)` + `/api/library/list` host-org `rowOrg` filter (default) + admin `allSites` opt-out (`getServerUser().isAdmin`); filtering at the fetch layer → Library tab + bind-picker + header search all inherit isolation; org-neutral broslaz tab labels + admin "All sites" toggle. Display-only (serving routes untouched → err-public).
3. **v11.1-04 broslaz liturgical vocab sweep** (`4490abe53c`) — `vocab.ts` +3 keys (`upcomingSection`/`createNewSetlistHeading`/`matrixTitle`); `SetlistDashboard` headers + `SetlistMatrixView` title now via `label(useOrg(), key)`. broslaz reads "Upcoming Shows"/"Create New Set"/"Set Matrix".
4. **Diagnosed the broslaz MCP "authorization failed"** — proved server-side OAuth chain + authorizedDomains healthy; root cause was the apex-vs-www URL (apex 308 strips the auth header). **Daniel confirmed MCP now works** with the www URL.
5. **`/paul:complete-milestone`** (`29d9a96878`) — MILESTONES.md § v11.1 + archive `.paul/milestones/v11.1-ROADMAP.md`; PROJECT.md evolved; ROADMAP collapsed (+ Next-Milestone placeholder); version 11.0.0→11.1.0; tag `v11.1.0`.

---

## What's In Progress

Nothing. Milestone closed; tree clean; all pushed.

---

## What's Next

**Immediate:** define the next milestone — `/paul:discuss-milestone` (explore scope) or `/paul:milestone` (create directly). Nothing is required; v11.1 is done.

**Backlog to consider when scoping:**
- **recordings-collection org-scoping** — `/api/recordings/upload:107` hardcodes `orgId: DEFAULT_ORG_ID`; fix the stamp (host `x-org-id`), THEN host-filter `subscribeRecordingsForSong`. (Deferred from v11.1-03.)
- **SERVICE_TYPE_LABELS → vocab table** — only if a non-synagogue tenant needs service-type categories (gated-away for broslaz today). (Deferred from v11.1-04.)
- v7.0 fold-forward re-triage (`.paul/MILESTONES.md` § v7.0).

**Open user item (not blocking):** the session account `crcmusic@centralreform.org` is `role: member`, `orgIds: ["crc"]` — it can't author broslaz setlists (would fall back to CRC). For broslaz authoring use David's band_leader account, or have an admin grant `crcmusic@` band_leader + Brothers Lazaroff Band-access (claim+doc) via `/manage → People`.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live state — v11.1 COMPLETE, loop idle |
| `.paul/MILESTONES.md` | § v11.1 entry (this milestone's record) |
| `.paul/ROADMAP.md` | Next-Milestone placeholder + collapsed v11.1 |
| `.paul/milestones/v11.1-ROADMAP.md` | Archived v11.1 roadmap snapshot |
| `src/lib/org/{branding,vocab,membership,registry}.ts` | org branding/vocab/membership/host-resolution layer |
| `src/components/nav/OrgLogo.tsx` | img-or-monogram org logo |

---

## Reusable gotchas (carry forward)

- **Canonical tenant MCP URLs use the `www.` host** — apex 308/307-redirects and strips the `Authorization` header → "authorization failed". broslaz: `https://www.brotherslazaroff.live/api/mcp`; CRC: `https://www.centralreform.live/api/mcp`.
- **Run `SKIP_ENV_VALIDATION=1 npx next build` before declaring any shared-lib/client phase deployable** — tsc + vitest miss client/server bundle-boundary breaks.
- `git pull` first next session (multi-computer); push `origin master` (NOT `master:main`).

---

## Resume Instructions

1. `git pull` (multi-computer).
2. Read `.paul/STATE.md` (v11.1 COMPLETE; loop idle).
3. `/paul:resume` (or directly `/paul:discuss-milestone` to scope the next milestone).

---
*Handoff created: 2026-06-09 (session 7 — v11.1 milestone complete + closed)*
