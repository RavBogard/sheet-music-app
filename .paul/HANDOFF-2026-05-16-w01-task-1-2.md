# Handoff — W-04 complete; W-01 ~40% shipped (Tasks 1+2)

**Date:** 2026-05-16, end of session
**Master + feat/mcp-server:** ff-equal at `c2e3f36d3`
**Author:** Claude (W-04 close-out + W-01 propose-then-confirm slice)
**Context:** Written at session end — substantial work shipped, resume in
fresh session for the remaining W-01 tasks.

---

## What shipped this session (in order)

1. **`5de7b5517` — W-04 Plan 02**: lastSeenVersion gating on the 5
   single-row write paths (`update_track`, `update_setlist`,
   `remove_track`, `reorder_setlist`, `delete_setlist`). 5 paths
   refactored from `db.batch()` to `db.runTransaction()` with optional
   `lastSeenVersion` reject + `track_not_found` envelope polish.
   Emulator suite: 244 → 257.

2. **`ab0bc1255` — F-03 invariant-on-write half**: test-only commit
   pinning the contiguous-order invariant on remove_track / position-
   move / reorder_setlist. The re-pack loop is already in place in Plan
   02's transactional refactor; tests seed legacy non-contiguous data
   ([0, 2, 5] and [0, 1, 1] race-shape) and assert the heal. Emulator
   suite: 260 → 265.

3. **`2f710aef8` — W-04 Plan 03**: bulk_update_tracks atomic pre-flight
   with per-row `lastSeenVersion` + `staleRows[]` envelope; best-effort
   skip-and-report; publish_setlist optional setlist-level gate (fires
   BEFORE chart-health pre-flight + recipient resolution, including in
   dryRun). publish_setlist now also bumps the setlist's version +
   lastModifiedBy/At so wait_for_setlist_change observers wake on
   publish. Emulator suite: 265 → 273. **W-04 surface complete
   end-to-end.**

4. **`c2e3f36d3` — W-01 Tasks 1+2**: propose_setlist_changes +
   commit_staged_changes — chat-native propose → confirm lifecycle.
   Two new MCP tools (count 36 → 38), new `proposal_stages` Firestore
   collection with rules. Emulator suite: 273 → 282.

Plus folded in from the parallel "bugstomp" session via clean rebases:
F-13 (`d7861735e`), F-02 (`51f529f3f`), F-17 (`e44c12212`), F-15
(`da84be4ab`), bug-stomp v6 docs prompt (`fbdc6ba84`).

## Cross-session coordination notes

- Pattern: sibling-file notes in `sheet-music-app-mcp/`.
  - Their status to me: `OTHER-CLAUDE-NOTE.md` (latest overwrites prior).
  - My replies to them: `OTHER-CLAUDE-NOTE-REPLY.md`.
- Decided AGAINST same-file co-channel — Write tool overwrites, races
  would clobber. The sibling pattern has been clean.
- Other Claude is idle as of `da84be4ab`/`fbdc6ba84` ship. Bugstomp queue
  is functionally cleared; remaining items (F-06, F-12, F-16) are
  Daniel-decision, no code.

## W-01 — what's left in the phase

W-01 plan is in `.paul/phases/mcp-w01-agentic-ux-shape/mcp-w01-01-PLAN.md`.
Tasks 1+2 of 7 shipped this session. **Resume from Task 3 next session.**

### Remaining tasks

**Task 3 — `preview_publish` wrapper** (~0.5 day)
- Thin wrapper over `publish_setlist({dryRun: true})`. Reformats the
  envelope as `{chartHealth, audience, snapshotDiff, flaggedBonds,
  recommendation}` where:
  - `chartHealth` reuses the dryRun's `chartHealth` field.
  - `audience` is `{count, breakdown: {admin, band_leader, musician,
    member}}` derived from the recipient list.
  - `snapshotDiff` compares the current snapshot vs.
    `publishedSnapshot` from the setlist doc (added / removed /
    modified tracks).
  - `flaggedBonds` queries the `bond_flags` collection from Task 4.
  - `recommendation` is `"hard_block"` if any chart status is
    `"missing"`, `"review_first"` if `flaggedBonds > 0`, else
    `"publish"`.
- File: `src/lib/mcp/tools/preview-publish.ts` (NEW).
- Schema in `tools/index.ts`. Hook into existing publishSetlist.
- AC-5 in the plan.

**Task 4 — `flag_bond` + `review_flagged_bonds`** (~0.5 day)
- `flag_bond(setlistId, trackId, reason)` upserts a doc in
  `bond_flags/{setlistId}_{trackId}` (composite id — natural unique
  key per row).
- `review_flagged_bonds(setlistId)` returns every flag on the setlist
  joined with current track state + 5 alternative songIds from
  search_library ranked by W-02 signals.
- File: `src/lib/mcp/tools/bond-corrections.ts` (NEW — Task 5 lives
  here too).
- Firestore rules for `bond_flags`.
- AC-6 (first half) in the plan.

**Task 5 — `record_bond_correction`** (~0.5 day)
- Writes `bond_corrections/{id}` with `{setlistId, trackId, fromSongId,
  toSongId, reason, contextKey from setlist.templateType, correctedAt,
  correctedBy}`.
- Same transaction: increments `library_index.{fromSongId}.bond
  CorrectionHistory.correctedAwayFrom` and `library_index.{toSongId}.
  bondCorrectionHistory.correctedTo`.
- Deletes `bond_flags/{setlistId}_{trackId}` on success.
- **IMPORTANT** (deferred from W-02): inline aggregation into
  `titleContextHints/{stem}_{contextKey}` after N=3 consistent picks.
  W-02 originally specified a Cloud Function for this but the project
  has no Functions infrastructure — so the same transaction also
  performs the read-modify-write on the hint doc when correctedTo
  count hits 3. See W-02's `library-upload.ts` for the schema field
  shape and the `library.ts` search-side hint lookup contract (W-02
  ships ranking bias + hint boost reads; W-01 ships the write side).
- File: `src/lib/mcp/tools/bond-corrections.ts` (same file as Task 4).
- Firestore rules for `bond_corrections` + `titleContextHints` write
  permission for the server.
- AC-6 (second half) in the plan.

**Task 6 — `.paul/AGENT-GUIDE.md` + MCP server `instructions` injection**
(~0.5 day)
- Write `.paul/AGENT-GUIDE.md` covering:
  - When to ask vs. proceed: `specificity < 0.5` AND `siblingsInCatalog
    > 1` → ask; otherwise commit-and-flag.
  - How to format a proposal in chat: tabular summary with confidence
    column, total flagged count.
  - Batch review pattern: call `review_flagged_bonds` at end of
    authoring, walk through each, call `record_bond_correction`.
  - The propose → commit loop with concrete examples (reference the
    2026-05-16 Bar Mitzvah session as the canonical failure mode).
- Modify `src/lib/mcp/server.ts`: read AGENT-GUIDE.md content at
  startup, inject into the MCP server's `instructions` field. (Verify
  the SDK actually surfaces this on the discovery endpoint.)
- AC-7 in the plan.

**Task 7 — memory + ship**
- Update `project_mcp_status.md` with the W-01 closing entry (tools
  count 38 → 41 after Tasks 4+5 add 3 tools).
- Push master + ff feat/mcp-server.

### Estimated total for remaining tasks

~2 days of focused work. Tasks 3 + 4 + 5 are independent and could ship
as separate commits if context budget is tight. Task 6 (AGENT-GUIDE) is
docs-only + a small server.ts wiring change.

## Coordination notes for next session

- **Pull master first** every time: `git pull --ff-only origin master`.
  If FF fails, the parallel session moved ahead — investigate before
  doing anything.
- **Specific-path `git add`** only (no `-A`) — shared-worktree rule
  still applies even with the other session idle.
- **Branch shape**: master and feat/mcp-server stay ff-equal. After
  each master push: `cd sheet-music-app-mcp && git fetch origin && git
  rebase origin/master && git push --force-with-lease origin
  feat/mcp-server`.
- **W-02 confidence surface**: I derive `confidence` from
  `library_index.{songId}.titleSpecificity`. If you change the
  threshold (`STOP_AND_ASK_THRESHOLD` in `src/lib/mcp/title-specificity
  .ts`), the propose-changes envelope's "high/medium/low" buckets shift
  with it — same single source of truth.
- **Stage TTL cleanup**: stale stages older than their TTL just sit in
  Firestore until commit returns `stage_expired` (which best-effort-
  deletes the doc). The W-01 plan mentions a Cloud Function sweep for
  >24h stages; deferred since the repo has no Functions infrastructure.
  Add to follow-up list if Firestore growth becomes an issue.
- **Other Claude is idle** at session end. Bugstomp queue is cleared
  modulo the 3 Daniel-decision items. They have no W-01 work on their
  side — entire W-01 phase is mine.

## Memory updates this session

- `project_mcp_status.md` — three entries: W-04 Plan 02, F-03 invariant
  pin, W-04 Plan 03, W-01 Tasks 1+2. Tool count 36 → 38. Branch state
  at each push.
- No new memory files this session.

## Resume prompt (paste this into the next Claude Code session)

> Resume the W-01 work on centralreform.live MCP server.
>
> Read `.paul/HANDOFF-2026-05-16-w01-task-1-2.md` first — it's the full
> state-of-the-world handoff including the remaining-tasks breakdown.
>
> Master and feat/mcp-server are ff-equal at `c2e3f36d3`. W-04 surface
> is complete; W-01 Tasks 1+2 (propose_setlist_changes +
> commit_staged_changes) shipped. **Next: pick up at W-01 Task 3
> (preview_publish wrapper)** and continue through Tasks 4–6 + memory
> update.
>
> Pull master FIRST. Stage commits with EXPLICIT file paths only (never
> `git add -A`). Other Claude may have shipped more between sessions —
> check `sheet-music-app-mcp/OTHER-CLAUDE-NOTE.md` for any status update.
>
> Read `~/.claude/projects/C--Users-dsbog-centralreform-live/memory/project_mcp_status.md` for the up-to-date MCP wave history.
>
> The W-01 phase plan is at `.paul/phases/mcp-w01-agentic-ux-shape/mcp-w01-01-PLAN.md` with full AC for every task.

