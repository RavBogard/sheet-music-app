# PAUL Handoff

**Date:** 2026-04-27 (after v50-07-02 close)
**Status:** paused — clean checkpoint at PLAN-committed-but-APPLY-pending boundary
**Pause reason:** context budget for v50-07-03 APPLY + v50-07-04 Playwright + v50-07-05 Sentry was tight; cleared session to resume with full headroom

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — worship band setlist/chart app
**Milestone:** v5.0 Bulletproof Editor — 6 of 7 phases complete
**Phase:** v50-07 (final) — Migration + kitchen-sink Playwright + cutover
**Currently in:** v50-07-03 PLAN committed; APPLY pending

---

## What's already shipped in v50-07

### v50-07-01 ✓ (commit `a82affb`)
Read-only production audit via new `scripts/audit-v50.ts`. Findings:
- 29 setlists; 24 with embedded tracks (650 total)
- 0 distinct songIds in embedded tracks (legacy uses `id`/`fileId` not `songId`)
- `songs/*` empty; top-level `tracks/*` empty (v5.0 editor unused in prod)
- 10 setlists carry orphan `liveState` (chats/songGroups already clean)
- 🐛 Pre-existing bug in `migrate-v50.ts` MARKER_PATH (3-seg = collection)

User selected **Option C Hybrid Lazy Hydration**: setlists migrate forward on first edit-open via SetlistGridHydrator; perf-view dual-reads. Best balance for band-not-onboarded reality.

### v50-07-02 ✓ (commit `db00d61`)
- Patched `migrate-v50.ts` MARKER_PATH → `system/v50Migration` (2-seg doc); 13 tests still pass
- New `scripts/scrub-livestate.ts` (~250 LOC) + 14 unit tests; dry-run by default; rollback snapshots in `migrations/livestate-scrub/snapshot/{id}`
- Production scrub applied: 10 setlists' `liveState` removed; re-audit confirms count = 0; setlists/29 + tracks/650 unchanged
- Suite 1456/1456; tsc + next build clean

---

## What's next: v50-07-03 APPLY

PLAN already committed at `.paul/phases/v50-07-migration-cutover/v50-07-03-PLAN.md` (commit `3ff74bc`). 3 tasks, 7 ACs, autonomous.

### Task 1: Lazy hydration in `SetlistGridHydrator`
- Add `hydrated?: boolean` to `LocalSetlist` (`src/lib/local/types.ts`)
- New effect after existing Dexie priming: gate on `initialSetlist.hydrated !== true && initialTracks.length > 0`
- Fan out `applyEdit({op:'set', collection:'tracks', doc:t}, {withoutUndo:true})` for each legacy track
- After fan-out: `applyEdit({op:'update', collection:'setlists', docId:setlistId, patch:{hydrated:true}, expectedUpdatedAt:initialSetlist.updatedAt}, {withoutUndo:true})`
- Guard ref to fire-once-per-mount; errors warn-logged + setlist retries on next mount
- Test-seam: pass `applyEdit` as a prop with default = real export

### Task 2: Dual-read in `useSetlistPerformance`
- New `useState<SetlistTrack[]>` for top-level tracks
- New `useEffect` subscribing to `onSnapshot(query(collection(db,'tracks'), where('setlistId','==',setlistId)))`
- Sort by `order` ascending; on snapshot → setState; on cleanup → unsubscribe
- Replace `tracks` derivation: `topLevelTracks.length > 0 ? topLevelTracks : (setlistData?.tracks ?? [])`

### Task 3: Tests
- SetlistGridHydrator: 5 new cases (fires/skips conditions; failure path; fire-once)
- useSetlistPerformance: 4 new cases (top-level present/empty; live update; cleanup)
- Mock `firebase/firestore` at module scope

### Mandatory before APPLY
**`/ui-ux-pro-max` BLOCKING per SPECIAL-FLOWS.md.** Even though v50-07-03 changes are data-correctness (no new pixels), the SPECIAL-FLOWS rule applies because frontend rendering paths are touched. Load the skill at APPLY entry briefly.

### After v50-07-03 closes
- v50-07-04: Playwright kitchen-sink (≥100 runs random edits + airplane-mode + force-quits + cross-tab)
- v50-07-05: Sentry alarms on save-path failures + manual UAT prep + ship-to-band
- Then: bug sweep + lint + push + milestone audit (`/paul:audit-milestone`) + v5.0 milestone close

---

## Key files (read these for v50-07-03)

| File | Purpose |
|------|---------|
| `.paul/phases/v50-07-migration-cutover/v50-07-03-PLAN.md` | THE plan — read first |
| `.paul/phases/v50-07-migration-cutover/v50-07-02-SUMMARY.md` | Substrate context (MARKER_PATH + scrub) |
| `.paul/phases/v50-07-migration-cutover/v50-07-01-DRY-RUN-REPORT.md` | Audit findings (29 setlists, 0 songIds, etc.) |
| `src/components/setlist/grid/SetlistGridHydrator.tsx` | Touchpoint #1 (104 LOC) |
| `src/hooks/use-setlist-performance.ts` | Touchpoint #2 (90 LOC) |
| `src/lib/local/types.ts` | LocalSetlist + LocalTrack types |
| `src/lib/local/write.ts` | applyEdit signature reference (lines 63-130) |
| `src/lib/sync/init.ts` | ProductionFirestoreAdapter — confirms 'set' writes top-level tracks/{id} |
| `src/lib/sync/snapshot-listener.ts` | Pattern for onSnapshot wiring (already used in hydrator) |

---

## Resume instructions

1. `git pull origin master` (multi-computer workflow).
2. `/paul:resume` — should auto-route to `/paul:apply .paul/phases/v50-07-migration-cutover/v50-07-03-PLAN.md`.
3. Load `/ui-ux-pro-max` at APPLY entry per SPECIAL-FLOWS.md.
4. Execute Tasks 1-3 from v50-07-03-PLAN.md; verify; commit + push.
5. Then plan v50-07-04 (Playwright kitchen-sink) — own session if scope warrants.

---

## Production state at handoff

- `git log --oneline -3`:
  - `3ff74bc` chore(paul): v50-07-03 PLAN
  - `db00d61` feat(v50-07-02): MARKER_PATH patch + liveState scrub
  - `a82affb` feat(v50-07-01): production audit + dry-run report

- Working tree: clean.
- Suite: 1456/1456; tsc + next build clean.
- Production Firestore:
  - 29 setlists; 0 with `liveState` field (scrubbed); 24 with legacy `tracks[]` arrays
  - 0 top-level `tracks/{id}` docs (no v5.0 editor activity yet)
  - `system/v50Migration` marker absent (v50-04 backfill never applied; deferred)
  - `system/livestateScrub` marker present (v50-07-02 applied)
  - `migrations/livestate-scrub/snapshot/*` — 10 rollback snapshots in place

---

*Handoff created: 2026-04-27 (after v50-07-02 close at commit `db00d61`)*
*Next: v50-07-03 APPLY — lazy hydration + perf-view dual-read*
