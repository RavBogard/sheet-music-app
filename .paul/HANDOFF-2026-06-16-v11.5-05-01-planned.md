# PAUL Handoff

**Date:** 2026-06-16
**Status:** PAUSED mid-phase — Phase v11.5-05 in PLAN (Plan 01 authored, awaiting APPLY). Plan/STATE/ROADMAP edits may be uncommitted (see WIP note).

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — multi-tenant worship/band sheet-music app. Browser = band/consumer surface (Perform mode on iPads); Daniel authors via Claude + MCP. 2 live tenants: CRC + Brothers Lazaroff (brotherslazaroff.live).
**Core value:** the band gets the right charts + recordings on their iPads each week; the web app's job is performance + quick edits.

---

## Current State

**Milestone:** 🚧 v11.5 — Bulletproof Performance. Oracle `docs/ACCESS-POLICY.md` v0.4. 3 of 4 active phases complete (v11.5-03 photo-import DROPPED → v11.6).
**Phase:** v11.5-05 (Consumer polish quick wins) — **the LAST v11.5 phase.** In PLAN.
**Plan:** **v11.5-05-01 created, awaiting APPLY** — `.paul/phases/v11.5-05-consumer-polish/v11.5-05-01-PLAN.md` (autonomous; Q6+Q3; 2 tasks; /ui-ux-pro-max BLOCKING).

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY     [v11.5-05-01 PLAN created, awaiting APPLY]
  ✓        ○        ○
```

---

## What Was Done (this session, 2026-06-16)

1. `/paul:resume` from a clean v11.5-04 phase boundary; archived the consumed `HANDOFF-2026-06-16-v11.5-04-complete.md`.
2. **Verify-first mapping of all 5 v11.5-05 items** (Q3/Q4/Q5/Q6/F4) against DEPLOYED code (Explore agent + direct reads of `vocab.ts`, `PublicSetlistListing.tsx`, `SetlistRow.tsx`, `SetlistCards.tsx`, `qr/[code]/page.tsx`, `.paul/research/DESIGN-AUDIT-2026-06.md`). Two findings corrected the audit's framing:
   - **F4 is NOT a display gate.** `SetlistRow` renders `displayKey` for ALL orgs whenever `track.key` exists — no org conditional. So "broslaz rows lack key badges" (audit `bl-setlist-detail-1180.png`) is almost certainly **missing `track.key` DATA on BL tracks OR a key-resolution gap**, not a CRC-only render. → written into Plan 02 as a VERIFY-FIRST probe (resolve root cause before coding; if data-only it may become an authoring/backfill note, not a component change).
   - **Q4/Q5 confirmed real gaps:** `/setlists` has NO junk/test filter (only `/perform` via `splitPublicSetlists`); no display-time filename-extension strip exists (relies on an upload-time *fallback* only).
3. **Decomposed the phase into 3 plans** and authored Plan 01.
4. Updated STATE.md (Current Position / Loop / Session Continuity) + ROADMAP.md (phase → 🚧 Planning, plans listed).

## Phase decomposition (3 plans)

- **Plan 01 (authored, awaiting APPLY) — Q6 + Q3** (pure copy/vocab, CRC byte-identical):
  - Q6: new `publicListingSubtitle` vocab key → BL "Public sets", CRC "Public setlists" (BASE verbatim). `PublicSetlistListing.tsx:146` is the hardcoded site; the component already uses `label(org, …)`.
  - Q3: `qr/[code]/page.tsx` expired/used branch (both 410 + 404 map to `state="expired"`) → org-branded (`useOrg`+`getOrgBranding(org).shortName`) + reworded "expired or was already used — ask for a fresh QR". Auth/PUT flow untouched.
- **Plan 02 (not authored) — Q5 + F4:** Q5 = display-time extension-strip helper applied to consumer surfaces (`SetlistRow` title, public listing, bind picker); F4 = VERIFY-FIRST BL key-badge root cause (data vs resolution) then fix. Both center on `SetlistRow.tsx`.
- **Plan 03 (not authored) — Q4:** anon `/setlists` (→ `SetlistDashboard`) — write controls already gate on `user && isMember`/`!!user` (verify holds); REAL work = suppress junk/test drafts from the anon archive (mirror `splitPublicSetlists` isTest/test-uid filter; decide "junk draft" = isTest and/or zero-track).

## What's Next

**Immediate:** `/paul:apply .paul/phases/v11.5-05-consumer-polish/v11.5-05-01-PLAN.md` — load **/ui-ux-pro-max** (BLOCKING) first.
**After that:** UNIFY 01 → author+apply Plan 02 (Q5+F4) → Plan 03 (Q4) → phase close = `/paul:complete-milestone` for v11.5.

## Flagged / Backlog (not blocking)

- **INFRA (not app code):** `/perform` cold·mobile CLS 0.250; Vercel TTFB keep-warm.
- **v11.6 candidates:** library folder/audio non-chart clutter; photo-of-paper-chart import (dropped); recordings org-scoping + `finalize_chart_upload` signed-URL org-stamp; anon chord-cache org-scoping.
- **Operational:** David Lazaroff STAYS admin (settled — do NOT suggest reverting to band_leader).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (v11.5-05-01 awaiting APPLY) |
| `.paul/phases/v11.5-05-consumer-polish/v11.5-05-01-PLAN.md` | The plan to apply |
| `.paul/ROADMAP.md` | § Phase v11.5-05 = full item scope (Q3/Q4/Q5/Q6/F4) + constraints |
| `.paul/research/DESIGN-AUDIT-2026-06.md` | Source of Q3/Q4/Q5/Q6/F4 (lines 108, 133-134 = F4/Q5 on BL detail) |

## Resume Instructions

1. `git pull` first (multi-computer). master is prod; push `origin master` (NOT master:main).
2. `/paul:resume` → it routes to `/paul:apply` for v11.5-05-01.
3. Autonomy posture binding: auto-commit+push per plan to master; quality floor held (tsc + tests + `next build --webpack` exit 0); /ui-ux-pro-max BLOCKING on this UI phase; Daniel paces with "go"/"1" per PAUL step.

---
*Handoff created: 2026-06-16 (pause mid-PLAN, v11.5-05-01 authored)*
