# PAUL Handoff

**Date:** 2026-06-09 (session 2 — mid-phase pause, context-limit)
**Status:** paused (clean — v11-05 phase IN PROGRESS, 2 of 5 slices loop-complete + committed LOCAL, nothing pushed)

---

## READ THIS FIRST

You have no prior context. This document + STATE.md tell you everything.

**Project:** sheet-music-app (CentralReform.live) — band charts/recordings on iPads; setlists authored via Claude + MCP.
**Core value:** The band gets the right charts + recordings on their iPads each week, and Daniel authors setlists conversationally via Claude + MCP.
**Current milestone:** v11.0 Brothers Lazaroff Multi-Tenant — give David Lazaroff an org-scoped instance on **brotherslazaroff.live**.

---

## Current State

**Milestone:** v11.0 — 6 phases: v11-01..04 ✅ · **v11-05 IN PROGRESS** · v11-06 (isolation audit) pending.
**Phase v11-05** (cross-tenant collection scoping) — **5 vertical slices** (roster split into users+assignments):
```
PLAN ──▶ APPLY ──▶ UNIFY        [v11-05-01 ✅ · v11-05-02 ✅ — next: PLAN v11-05-03]
  ✓        ✓        ✓     (2 of 5 slices done; all committed LOCAL, unpushed)
```
**App:** package.json `10.1.0`. Production branch `master`; push `origin master` (NOT `master:main`).

**Local commits this session (UNPUSHED — origin/master..HEAD):**
- `e166be2928` feat(v11-05-01) — templates (setlistTemplates wall + liturgical doc-id namespacing)
- `87483e29b9` feat(v11-05-02) — users roster (multi-org membership, CRC-safe-by-default)

---

## What Was Done (this session)

1. `/paul:resume` → `git pull` (up to date).
2. `/paul:discuss v11-05` — 3 decisions: vertical per-collection slices · audit+backfill before each filter flip · vocab in-scope. Wrote CONTEXT.md.
3. Read-only collection inventory (Explore agent) — see STATE Loop Position "COLLECTION INVENTORY".
4. **v11-05-01 (Templates)** full loop: `setlistTemplates` MCP reads/writes org-walled (list filter + not-found walls on get/update/delete/clone + source-setlist wall + create-stamp); client `templates` liturgical overrides namespaced by doc-id (CRC bare-key = zero migration); backfill extended to `setlistTemplates`. tsc 0 · suite 3310/0 · eslint 0 · emulator 7/7 + backfill 5/5 + unit 6/6. Committed local.
5. **2 Daniel decision rounds:** deploy = **batch at phase close**; roster = **multi-org** (David = `['crc','brotherslazaroff']`) via **doc.orgIds mirrored from claims**.
6. **v11-05-02 (Roster/users)** full loop: roster reads (`list_musicians`/`suggest_musicians`/`suggest_band` + `/api/scheduling/suggest-band`) org-scoped; `orgIds` claim mirrored to user doc at sync-claims. **DEVIATION:** filtered **in-memory** via `rowOrgIds(doc.orgIds)` (missing→`['crc']`) instead of `array-contains` — CRC-safe with NO backfill, no composite index, no breakage of the existing roster suite. tsc 0 · suite 3311/0 · eslint 0 · mcp-roster emulator 46/46 (incl. 2 org cases) · sync-claims+membership unit 16/16. Committed local.

---

## What's In Progress

Nothing mid-edit. Both slices fully closed + committed locally. Tree clean.

---

## What's Next

**Immediate:** `/paul:plan v11-05-03` — **scheduling_assignments** (most mechanical remaining slice). Org derives from each assignment's already-org-stamped **setlist**. Scope the cross-tenant reads: `subscribeToAllUpcomingAssignments` (`scheduling-firebase.ts:80`), `suggestBand` play-count read (`roster.ts`), `/api/scheduling/{suggest,suggest-band,history,remind}` + `cron/scheduling-reminder`; stamp `orgId` on assignment create in `assignment-service.ts`. Emulator-backed.

**After that:** `04` congregation singleton (`config/congregation` → per-org refactor, architectural) → `05` CreationWizard setlist-create orgId-stamp + de-synagogue vocab (UI → **/ui-ux-pro-max BLOCKING**) → **phase-close deploy** → **v11-06** isolation audit.

---

## PHASE-CLOSE DEPLOY GATE (batch — Daniel chose batch-at-close)

Run scripts (dry-run→inspect→`--apply`) THEN push the whole phase once:
- **v11-05-01 (HARD gate):** `node scripts/backfill-orgid-v11.mjs --apply` — the `list_templates` equality filter makes the 3 CRC templates vanish unless stamped first.
- **v11-05-02 (SOFT — CRC already safe):** `node scripts/backfill-user-orgids.mjs --apply` (tag BL members) + `node scripts/fix-david-orgids-claim.mjs --uid=<david> --apply` (David → `['crc','brotherslazaroff']`; he then re-auths / hits sync-claims to mirror onto his doc).
- Prod-script auth on this box: firebase-CLI refresh-token → temp ADC (see STATE Blockers/Concerns).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live state — Loop Position has the COLLECTION INVENTORY + DEPLOY GATE |
| `.paul/phases/v11-05-collection-scoping/CONTEXT.md` | full slice rationale + the 3 phase decisions |
| `.paul/phases/v11-05-collection-scoping/v11-05-0{1,2}-SUMMARY.md` | what shipped per slice |
| `src/lib/org/membership.ts` | `getOrgIdsFromClaims` / `rowOrgIds` / `orgIdsEqual` |
| `src/lib/mcp/org-context.ts` | `orgFrom` / `rowOrg` (MCP caller-org seam) |
| `scripts/backfill-*.mjs`, `scripts/fix-david-orgids-claim.mjs` | phase-close prod scripts (dry-run first) |

---

## Reusable gotchas (carry forward)

- Commit with `git commit -F <file>` — the PowerShell-style `@'...'@` here-string leaks `@` into the subject when run via the Bash tool (had to amend v11-05-01).
- Standalone emulator test files can hit a vitest app/instance split (`getFirestore(app)` ≠ the tool's `getFirestore()` → tool sees an empty project). Add org/collection cases INTO an existing working `*.emulator.test.ts` harness instead of a fresh file.
- Behavior changes to shared routes/normalizers retroactively break pre-existing test fixtures — audit + update them (did so for sync-claims + the array-contains-vs-in-memory pivot).
- `array-contains` can't match a missing field and was flaky/empty in the emulator — prefer in-memory membership filtering with a CRC default for tenant scoping unless a real index is warranted.

---

## Resume Instructions

1. `git pull` (multi-computer — other boxes may push v7.1 `.coord/` work; rebase the 2 local v11-05 commits on top).
2. Read `.paul/STATE.md` (Loop Position + Session Continuity).
3. `/paul:resume` → routes to `/paul:plan v11-05-03`.

---
*Handoff created: 2026-06-09 (session 2)*
