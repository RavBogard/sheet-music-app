# PAUL Handoff

**Date:** 2026-06-09 (end of session — v11-04 phase complete)
**Status:** paused (clean — v11-04 PHASE COMPLETE, all 3 plans shipped + live-verified; v11-05 not started)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — band charts/recordings on iPads; setlists authored via Claude + MCP.
**Core value:** The band gets the right charts + recordings on their iPads each week, and Daniel authors setlists conversationally via Claude + MCP.
**Current milestone:** v11.0 Brothers Lazaroff Multi-Tenant — give David Lazaroff his own org-scoped instance on **brotherslazaroff.live** (LIVE, branded, sign-in working, and now tenant-isolated on ALL reads).

---

## Current State

**Milestone:** v11.0 — **6 phases**: v11-01 ✅ · v11-02 ✅ · v11-02b ✅ · v11-03 ✅ · **v11-04 ✅ COMPLETE** · v11-05 (collection scoping) · v11-06 (isolation audit). ~67% done (4 of 6).
**Phase:** v11-04 BL consumer surface + onboarding — **COMPLETE (3/3, 2026-06-09)**.
**App:** package.json `10.1.0`. Production branch `master`; push `origin master` (NOT `master:main`). origin/master IN SYNC, tree clean.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY        [v11-04 PHASE COMPLETE — next: PLAN v11-05]
  ✓        ✓        ✓
```

---

## What Was Done (this session, 2026-06-09)

Started from `/paul:resume` (which routed to apply v11-04-02) and completed the **entire v11-04 phase**:

1. **v11-04-02 — org-aware consumer branding/metadata** (`feat(v11-04-02)` `f50060e387`). Data-driven `src/lib/org/branding.ts` metadata fields (CRC byte-identical) → root `layout.tsx` static metadata converted to `generateMetadata()` (org title/og/twitter/appleWebApp/metadataBase/manifest) + new `public/manifest-brotherslazaroff.json` + org-aware `/perform` wordmark/aria-label (`PublicSetlistListing`) + de-synagogued listing title (`vocab.ts` `publicListingTitle`). **LIVE-VERIFIED:** BL /perform tab title "Upcoming Shows & Sets | Brothers Lazaroff" + "Brothers Lazaroff" wordmark; CRC byte-identical.
2. **v11-04-03 — authed-dashboard read scoping** (`feat(v11-04-03)` `6b1ba7f189`). Opt-in `org` on `getSetlistsPage`/`getUpcomingSetlists`/`getRecentSetlists` (mirrors `getAllSetlists`' `.where('orgId','==',org)` + the deployed `(orgId,date)` index); both `getSetlistsPage` callers (`(main)/setlists/page.tsx` SSR + `/api/setlists/page` route) thread the host org from `x-org-id` via `coerceOrgId`; the 4 client `subscribeToAllSetlists` callers (`DashboardClient`, `SetlistDrawer`, `use-add-to-setlist`, `use-setlist-dashboard`) pass `useOrg()`. **LIVE-VERIFIED:** BL `/api/setlists/page` → `items:[]` (zero CRC leak); CRC returns its setlists unchanged.
3. **Phase close** — STATE.md + ROADMAP.md updated, phase-close `docs(v11-04)` committed.

**Commits this session (all pushed to origin master):** `feat(v11-04-02)` `f50060e387` · `docs(v11-04-02)` `67425d3fda` · `docs(v11-04-03)` plan `317f2d58a7` · `feat(v11-04-03)` `6b1ba7f189` · `docs(v11-04)` phase-close `823cbc7ad9` · (this handoff).

**Gates (every plan):** tsc clean · full suite **3304 passed / 0 failed** · eslint 0 errors · `/ui-ux-pro-max` loaded (BLOCKING on both UI plans). The 2 recurring `onTaskUpdate` vitest worker-RPC timeouts are the known parallel-load flake, not failures.

---

## What's In Progress

Nothing mid-flight. v11-04 fully closed; v11-05 not started.

---

## What's Next

**Immediate:** Start **v11-05 — cross-tenant collection scoping**. Likely run `/paul:discuss-phase v11-05` first (spans many subsystems), then `/paul:plan` decomposed into vertical slices. Scope:
- Org-scope the still-cross-tenant collections for READ + WRITE: **templates** (read/list), **roster/musicians** (`users`, `scheduling_assignments`, `musician_availability`), **congregation**, **service-personnel**.
- The **in-app CreationWizard setlist-create orgId stamp** (flagged by v11-04-03): an in-app-created setlist currently carries no orgId, so it would be **invisible in the now-org-scoped dashboard**. MCP create already stamps (v11-02-03). Low-risk today (Daniel/David author via MCP) but must close here.
- De-synagogue the now-unblocked **CreationWizard / perform-view / display-card vocab** (deferred from v11-03-03 / v11-04 — depends on org-scoped congregation+templates).
- Nature: data-isolation work like v11-02 → use the **Firebase emulator** for rules/scoping coverage (mandatory per the autonomy directive's quality floor).

**After that:** **v11-06** — cross-tenant isolation security audit (close gate). Use `scripts/e2e-bl-tenant-probe.mjs` (DAVID_BEARER + CRC_BEARER env) against `https://www.centralreform.live/api/mcp`.

---

## UAT pending (verify at milestone end — `.paul/UAT-PENDING.md`)

- **v11-04-03 authed dashboard:** sign in on brotherslazaroff.live → `/setlists` shows ONLY BL setlists (currently the empty-library state, NOT CRC services); sign in on centralreform.live → CRC dashboard unchanged. (Server-side proven by unit tests + the public `/api/setlists/page` probe; this is the human authed-session confirmation — no-local-dev means prod is the only place it shows.)
- **v11-02-04 / v11-02b:** David self-onboards via login (BL-scoped token) OR manual bearer tokenId `93JMXhT1OspFsWDMmb9V`; confirms he sees only BL data.

---

## Operating rules (v11.0 autonomy directive — Daniel 2026-06-08)

Run the milestone autonomously: waive approval/continuation gates, auto-proceed plan→plan & phase→phase, auto-commit per phase + push to prod `master`, bake decisions into PLANs, Firebase deploys/backfills are AUTO tasks. **Quality floor HELD:** tsc clean + tests green + AC proof every task; emulator-backed rules tests + the v11-06 isolation audit are mandatory/blocking; `/ui-ux-pro-max` BLOCKING on UI phases; backfills get dry-run + idempotency + rollback. **STOP only for:** product ambiguity, unresolvable quality-gate failure, or a discovered cross-tenant LEAK / CRC lock-out risk.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (Session Continuity has the resume pointer) |
| `.paul/ROADMAP.md` | Phase overview (v11-01..04 ✅ · v11-05/06 pending) |
| `.paul/UAT-PENDING.md` | Deferred human/authed-session UAT checks |
| `.paul/phases/v11-04-consumer-surface-onboarding/v11-04-0{1,2,3}-SUMMARY.md` | What shipped per plan |
| `src/lib/org/{branding,vocab,registry,org-context}.ts` | org branding/vocab/coerce/useOrg seams |
| `src/lib/server-setlists.ts` | `getAllSetlists`/`getSetlistsPage`/getUpcoming/getRecent opt-in `org` filter |
| `src/proxy.ts` | Edge sets `x-org-id` on ALL routes incl. /api (line ~150) from host |
| `scripts/e2e-bl-tenant-probe.mjs` | reusable MCP tenant-isolation probe (for v11-06) |

---

## Reusable gotchas (carry into v11-05/06)

- **Deployed-surface probe is mandatory for the host→org seam** — no-local-dev + CRC-default masks BL misresolution (the v11-03 `coerceOrgId` hotfix lesson). The public `/api/setlists/page` endpoint lets you probe tenant read-scoping WITHOUT auth (hit it on each host, compare).
- **`coerceOrgId(x-org-id)`** validates an already-resolved org id; do NOT pass it through `resolveOrgIdByDomain` (expects a host → returns crc; that was the v11-03-01 bug).
- **New tenant host must be added to Firebase Auth authorizedDomains** or web sign-in silently fails (`auth/unauthorized-domain`) — use `scripts/add-auth-domains.mjs`.
- **Prod-script auth on this box:** no SA creds/gcloud → convert firebase-CLI refresh token → temp ADC json, point `GOOGLE_APPLICATION_CREDENTIALS` at it, delete after. `firebase deploy` (rules/indexes) uses the CLI directly.
- **Local `next build` fails** on `/api/cron/aggregate-corrections` (missing CRON_SECRET, Vercel-injected) — not a code issue; rely on the Vercel build.

---

## Resume Instructions

1. `git pull` (multi-computer — other boxes may push v7.1 `.coord/` work).
2. Read `.paul/STATE.md` (Session Continuity).
3. Run `/paul:resume` → it routes to v11-05 (start with `/paul:discuss-phase v11-05`, then plan).

---
*Handoff created: 2026-06-09*
