# PAUL Handoff

**Date:** 2026-03-11
**Status:** context-limit

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** CentralReform.live — Sheet music app for worship band
**Core value:** Musicians can instantly access setlists, transpose charts, and control monitor mixes — live on any device, no paper or prep needed.

---

## Current State

**Version:** v1.7 Critical Bug Fixes
**Phase:** 3 of 5 — Print Pipeline & Gig Packet Overhaul
**Plan:** 03-01 — APPLY complete, needs UNIFY

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ○
```

---

## What Was Done This Session

- **Phase 1 UNIFY completed** — Closed loop on mobile sign-in fix (commit 88a483d)
- **Phase 2 PLAN + APPLY + UNIFY completed** — Two quick fixes (commit f6b00e9):
  - Added `googleProvider.addScope('profile')` at all 3 GoogleAuthProvider sites (fixes avatar)
  - Replaced changelog redirect("/manage") with static version history page
- **Phase 3 PLAN + APPLY completed** — Print pipeline overhaul (commit ecae2dd):
  - Replaced Inngest async PDF generation with direct synchronous generation in API route
  - Simplified PrintModal: direct blob fetch, no Firestore onSnapshot polling
  - Lightened modal overlay from bg-black/90 to bg-black/60, added shadow-2xl
  - Root cause of 500 error: Inngest not configured on Vercel
- **Roadmap restructured** from 4 to 5 phases (added monitor buses investigation)

---

## What's In Progress

- Phase 3: Code committed but needs UNIFY (SUMMARY + transition + state update)
- Tests not yet run for Phase 3 (TypeScript compiles clean, no Firestore/Inngest imports remain)
- Need to push all commits to main for Vercel deploy

---

## What's Next

**Immediate:** `/paul:unify .paul/phases/03-print-pipeline-overhaul/03-01-PLAN.md`

**After that:**
1. Push to main (user always deploys to main, no preview branches)
2. Plan Phase 4 — Key Signature Position (move key from left to right of song title)
3. Plan Phase 5 — Monitor Buses Investigation (user sees 4 buses, expected 5)

---

## Key Decisions This Session

- Avatar root cause: GoogleAuthProvider missing `profile` OAuth scope → user.photoURL always null
- Print 500 root cause: Inngest not configured on Vercel → inngest.send() fails
- Solution: Bypass Inngest entirely, generate PDFs synchronously (matches working personal/public routes)
- Changelog: Simple static page with hardcoded version data (no database)
- User confirmed: ALL issues important, print pipeline is "essential, essential"

---

## User Requests Still Pending

- Avatar: Users need to sign out and back in to get photoURL populated (scope change requires re-auth)
- SW banner: User hasn't confirmed if Phase 1 fix resolved false "update available" banners
- Monitor buses: 4 visible instead of expected 5 (Phase 5)

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview (5 phases, 2 complete) |
| `.paul/phases/03-print-pipeline-overhaul/03-01-PLAN.md` | Print pipeline plan (needs UNIFY) |
| `src/app/api/setlist/print/route.ts` | Rewritten: direct sync PDF generation |
| `src/components/setlist/PrintModal.tsx` | Simplified: direct blob fetch, cleaner UI |
| `src/lib/firebase.ts` | Added profile scope to GoogleAuthProvider |

---

## Git State

```
Last commit: ecae2dd (fix(phase-3): replace Inngest async print with direct sync generation)
Branch: master
Unpushed: 3 commits (88a483d, f6b00e9, ecae2dd)
```

---

## Resume Instructions

1. Run `/paul:resume`
2. It will suggest: `/paul:unify .paul/phases/03-print-pipeline-overhaul/03-01-PLAN.md`
3. After unify + transition, push to main
4. Then plan Phase 4

---

*Handoff created: 2026-03-11*
