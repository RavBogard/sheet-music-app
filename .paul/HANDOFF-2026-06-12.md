# PAUL Handoff

**Date:** 2026-06-12 19:38 UTC
**Status:** paused (clean stopping point — two loops closed + shipped)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — multi-tenant worship/band sheet-music app. Browser = band/consumer surface (Perform mode on iPads); Daniel authors via Claude + MCP. 2 live tenants: CRC (centralreform.live) + Brothers Lazaroff (brotherslazaroff.live).
**Core value:** The band gets the right charts + recordings on their iPads each week; the web app's job is **performance + quick edits** (doctrine: bulletproof > novel). MCP is the **authoring** surface.

---

## Current State

**Milestone:** v11.5 — Bulletproof Performance (🚧 5 phases; oracle `docs/ACCESS-POLICY.md` v0.4).
**Phase:** v11.5-01 of 5 — Tenancy + anon correctness — **2 of 3 plans complete.**
**Plans:** 01 = H4 ✅ · 02 = H5 (UNPLANNED — next) · 03 = H9 ✅.

**Loop Position:** (idle — last loop closed)
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     (Plan 03 / H9 closed + shipped)
```

---

## What Was Done (this session)

- **H4 (Plan 01) shipped** `180c9b666e` — `perform/setlist/layout.tsx` client→async-server; resolves `x-org-id`→`getOrgBranding`, threads branding into `<AppNavigation>`. Fixed the CRC-branding leak on broslaz `/perform/setlist/[id]` (+ track sub-route). CRC byte-identical. Regression test + stress cell B4. (SUMMARY: `.../v11.5-01-01-SUMMARY.md`.)
- **H9 (Plan 03) shipped** `d7cbb1a4e0` — band_leader library-edit lockout fix (field-reported by David). `editEnrichment` gate `assertAdmin`→`assertLibraryEditor` (admin|band_leader) + admin-only `collection` guard (`forbidden_field`) + cross-tenant `orgWall`→`row_not_found` (admin byte-identical); both handlers thread `orgFrom(extra)`; descriptions corrected. Emulator authz test 5/5. (SUMMARY: `.../v11.5-01-03-SUMMARY.md`.) Planned as a STOP-gate, Daniel approved.
- **H9 registered** in ROADMAP/STATE (`3e068c8829`); docs commits `046c983dcf` (H4), `88dff486a2` (H9).
- All pushed to `origin master` (prod). Gates every plan: tsc 0 · tests green · `next build --webpack` clean.

---

## What's In Progress

- Nothing mid-flight. Clean stop. Phase v11.5-01 has **one plan left: H5 (Plan 02)** — not yet authored.

---

## What's Next

**Immediate:** `/paul:plan` for **Plan v11.5-01-02 (H5)** — anon chord-cache write 401 → recompute every load + console noise (incl. run-3 B-10's PATCH 401). Server route only (NOT behind a UI gate). Align oracle D-Q2 (anon transpose stays open + rate-limited; don't regress authed; don't double-punish vs the existing cold-load 429s). **VERIFY-FIRST** the 401 vector (which cache-write endpoint, why anon is rejected).

**This is the LAST plan in phase v11.5-01** → its UNIFY triggers the **phase transition** (PROJECT/ROADMAP evolve + phase-close commit + route to next phase v11.5-02).

**⚠️ OPERATIONAL (not code) — do this:** **Revert David admin → band_leader.** Daniel temp-bumped him to admin 2026-06-12 18:35 UTC to unblock June-13 re-tagging. H9 is now live, so the proper band_leader path works — drop him back via the orgIds-preserving `/api/admin/set-role` (NEVER `scripts/set-role.js` — it wipes orgIds), then live-confirm the band_leader edit path. Tracked in `.paul/UAT-PENDING.md`.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (Session Continuity is current) |
| `.paul/ROADMAP.md` | v11.5 milestone + phase v11.5-01 (H4✅/H5/H9✅) |
| `.paul/phases/v11.5-01-tenancy-anon-correctness/v11.5-01-03-SUMMARY.md` | H9 result |
| `.paul/phases/v11.5-01-tenancy-anon-correctness/v11.5-01-01-SUMMARY.md` | H4 result |
| `.paul/UAT-PENDING.md` | Non-blocking live UATs + the revert-David reminder |
| `src/lib/mcp/tools/library-review.ts` | H9 fix (editEnrichment gate + orgWall) — reference for H5-adjacent work |
| `.paul/research/MILESTONE-BRIEF-v11.5-bulletproof-performance.md` | Ratified v11.5 spec |

---

## Resume Instructions

1. Read `.paul/STATE.md` (Session Continuity) for latest position.
2. `git pull` first (multi-computer box); push `origin master` (NOT master:main).
3. Run `/paul:resume` (or go straight to `/paul:plan` for H5).
4. Autonomy posture binding: run autonomously, auto-commit+push per phase to prod `master`, quality floor held (tsc + tests + `next build --webpack` before deployable). H5 is server-only — likely `autonomous: true` (NOT a STOP-gate unless it changes auth/permissions). Daniel paces with "go" per PAUL step.
5. Don't forget the **revert-David-to-band_leader** operational step.

---

*Handoff created: 2026-06-12 19:38 UTC*
