# PAUL Handoff

**Date:** 2026-06-10
**Status:** paused (clean — phase boundary, committed + pushed to prod `master`)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (centralreform.live) — multi-tenant worship-band app (CRC + Brothers Lazaroff). Daniel authors setlists via Claude + MCP; the browser app is the band/consumer surface (Perform mode on iPads). Next.js + Firebase (`crcmusiccharts`); git repo at the `sheet-music-app/` subdirectory; `.paul/` is inside it.
**Core value:** The band gets the right charts + recordings on their iPads each week; Daniel authors conversationally via Claude + MCP.

---

## Current State

**Milestone:** 🚧 v11.3 — Worthiness & Access (5 phases). Oracle: `docs/ACCESS-POLICY.md` v0.3 (a finding is a bug only if it contradicts a cell; err-public prime directive).
**Phase:** 3 of 5 COMPLETE → **next is Phase v11.3-04 (/perform performance, BUG-2, P2)**. Plans: none active.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY     [v11.3-03 ✅ COMPLETE + committed + pushed — ready to PLAN v11.3-04]
  ○        ○        ○
```

**Git:** `master`, in sync with `origin/master`, tip **`4fe1748318`** (v11.3-03 phase commit, Vercel auto-deploying). Push `origin master` (NOT `master:main`). Multi-computer → `git pull` before next session. NOTE: one tiny uncommitted STATE.md doc-edit may ride the next commit (Git-State hash-line lag, normal).

---

## What Was Done (this session)

- **`/paul:resume`** → reconciled state (verified the +1 docs commit `d940af34a6` was docs-only), archived the consumed handoff to `.paul/handoffs/archive/HANDOFF-2026-06-11-v11.3-02-complete.md`.
- **Phase v11.3-03 Harness & hygiene — COMPLETE (2 plans, both PLAN→APPLY→UNIFY)**, shipped as one phase commit `4fe1748318` `feat(v11.3-03): harness & hygiene`:
  - **v11.3-03-01 (BUG-9):** `/test-login` added to `src/proxy.ts` `publicExactRoutes` (EXACT match, not prefix — `/test-login-*` siblings stay gated) so the headless test-account sign-in harness is reachable unauth and can consume its `?code`. +3 regression cases in `src/__tests__/proxy-auth.test.ts`.
  - **v11.3-03-01 (BUG-7):** `GET /api/auth/qr` validates `code` against `/^[A-Z0-9]{6}$/` → 400 BEFORE `initAdmin()`/Firestore (was 500 on the odd-segment `.doc(code)` throw). New `src/app/api/auth/qr/__tests__/route.test.ts` (malformed→400, missing→400, valid→404).
  - **v11.3-03-02 (BUG-1):** `sweepOrphanTestDataCore` (`src/lib/mcp/tools/test-tokens.ts`) now also sweeps orphan `library_index` rows (owner-absent test-shape `uploadedBy`) + best-effort Storage purge (reuses `bestEffortStorageDelete`); extended result/arg types + tool description; +2 emulator cases in `mcp-test-tokens.emulator.test.ts`.
  - **BUG-1 verify-first finding:** the two `[role-*] tiny` CRC rows from run-1 (`upload-0d872e08-…`, `upload-cef9ddf9-…`) were ALREADY cleaned out-of-band. New probe `scripts/v11-3-03-library-orphan-sweep.mjs` (dry-run + `--diagnose` + `--apply`, admin-SDK via firebase-CLI refresh-token ADC) found 0 orphans across 713 rows, confirmed by id-prefix + title + filter. **No prod delete performed** — the durable orphan-sweep coverage is the deliverable.
  - Gates green every loop: tsc clean · vitest 33/33 + emulator 36/36 · `SKIP_ENV_VALIDATION=1 next build`. CRC byte-identical.

---

## What's In Progress

- Nothing mid-flight. Clean phase boundary. v11.3-03 shipped to prod.

---

## What's Next

**Immediate:** `/paul:plan` for **Phase v11.3-04 — /perform performance** (BUG-2, P2):
- Highest-traffic route regression: p75 **LCP 2600 / FCP ~3100 / TTFB ~1450ms**, and **CLS regressed 0.15 → 0.2** between the two stress runs.
- **VERIFY-FIRST:** characterize cold-load vs steady-state composition before fixing; suspect **chart-image reflow** for the CLS regression. Healthy comparator: `/setlists` LCP 1.1s / CLS 0.02.
- Each fix gets a regression cite. This is a UI-touching route → **/ui-ux-pro-max is BLOCKING** during APPLY (SPECIAL-FLOWS gate).

**After that:** v11.3-05 (P3 polish — BUG-6 broslaz PWA manifest app-shell + F-6 cold-load 429 self-heal). D8 publish/notify is v11.4 (NOT this milestone).

---

## Binding Context (do not re-litigate)

- **Oracle:** `docs/ACCESS-POLICY.md` v0.3 is the bug arbiter; err-public prime directive.
- **Scope walls:** D8 → v11.4; BUG-3 (RUM)/BUG-8 (member library)/Policy-Q1 closed by policy (no code); F-4 dup setlist + Cowork sandbox proxy out; D2 anon **recordings** stays the ⚠️ veto cell.
- **Autonomy posture (binding):** run autonomously, auto-commit + push per phase to `master`, bake decisions into PLANs. Quality floor: tsc + tests + AC proof per task; `SKIP_ENV_VALIDATION=1 npx next build` before declaring deployable; emulator tests where rules/queries change; **/ui-ux-pro-max BLOCKING on UI-touching phases (v11.3-04 qualifies).** STOP only for ambiguity / unresolvable gate failure / cross-tenant leak. Daniel paces with "go" per PAUL step.
- **Tooling gotchas:** Bash tool is bash — use `<<'EOF'` heredocs for commit messages. Admin-SDK prod scripts: no SA creds on this box → mint temp `authorized_user` ADC from the firebase-CLI configstore refresh token (logged in as daniel@centralreform.org); see `scripts/v11-3-03-library-orphan-sweep.mjs` for the working pattern. `next build` needed for client/server bundle-boundary breaks (tsc/vitest miss them).

---

## Reusable from this phase

- `scripts/v11-3-03-library-orphan-sweep.mjs` — prod library_index orphan probe/sweep (dry-run / `--diagnose` / `--apply`). Kept for future test-data hygiene.
- `sweep_orphan_test_data` MCP tool now covers `library_index` orphans (in addition to setlists/templates).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (read first) |
| `.paul/ROADMAP.md` | v11.3 milestone + phase detail (§ Phase v11.3-04) |
| `docs/ACCESS-POLICY.md` | The oracle (v0.3) |
| `.paul/phases/v11.3-03-harness-hygiene/` | Completed phase 3 (PLAN + SUMMARY ×2) |
| `.paul/research/STRESS-TEST-REPORT-2026-06-10.md` | run-1 stress report (BUG-2 detail) |
| `.paul/UAT-PENDING.md` | Non-blocking live checks |

---

## Resume Instructions

1. `git pull` (multi-computer).
2. Read `.paul/STATE.md` for latest position.
3. Run `/paul:resume` (or `/paul:plan` directly for Phase v11.3-04).

---

*Handoff created: 2026-06-10 — paused at the v11.3-03 → v11.3-04 phase boundary (v11.3-03 shipped to prod, tip `4fe1748318`).*
