# PAUL Handoff

**Date:** 2026-06-18
**Status:** paused — clean (all work committed + pushed; deploy live)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (centralreform.live + brotherslazaroff.live) — multi-tenant worship-band setlist/chart app (CRC + Brothers Lazaroff, both live).
**Core value:** the band gets the right charts on their iPads each week; Daniel authors setlists via Claude + MCP (the browser app is the band/consumer surface — Perform mode on 11" iPads).

---

## Current State

**Milestone:** v11.6 — Airtight (Weekend Stress & Usability). 2 of 4 phases complete.
**Phase:** 3 of 4 — **v11.6-03 Off-site resilience (flaky wifi) — IN PROGRESS.**
**Decision locked:** close-scope = **C (full sweep)** — fix all 5 residual offline gaps.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [v11.6-03-02 closed; next: PLAN v11.6-03-03]
```

Phase 03 slices: 01 verify+decide ✅ · **02 WS-12/13 ✅ `7ac307812f`** · 03 WS-10 (next) · 04 WS-29/30.
Latest pushed commit: `86c5e835b6` (v11.6-03-02 close-loop docs). Branch `master`, in sync with origin.

---

## What Was Done (this session — a long one)

1. **Resumed** at v11.6-02-07 (drawer-nav), **applied + unified** it (WS-08 jumpToSong-in-queue + WS-28 SwipeOverlay delete) → `6dee681893`.
2. **Closed phase v11.6-02** (Perform reading airtight, 7/7) — full transition (PROJECT/ROADMAP/STATE) `234b433206`.
3. **Opened + scoped phase v11.6-03**: planned v11.6-03-01 (verify-first), **verified the 5 residual offline gaps against deployed code** (`.paul/research/v11-6-03-offline-gap-verify-2026-06-18.md`); reframed — **offline reading already WORKS** (SW+IDB re-introduced 2026-05-28), so 03 = close gaps not re-introduce.
4. **P0 INTERRUPT — gig-packet text-chart fix (insert v11.6-02.1, `a48382929a`):** David reported gig packets failing on centralreform.live. Data-confirmed root cause (`verify_setlist_charts`): the website Full Packet only `PDFDocument.load`s PDF/image charts; scraped **text/plain** charts threw → caught → silently dropped, so the all-text Camp Sabra sets printed a chartless cover. Fix = new shared `src/lib/pdf/text-chart-pdf.ts` renderer (Courier + WinAnsi sanitizer + column-preserving transpose) wired into web `generatePrintPdf` + MCP `generate_gig_packet`. **Prod-verified:** MCP generate_gig_packet on the live Staff Concert set rendered **16/16 charts, 0 missing**. 14 tests + no-regression; tsc/build clean. **David can re-print Full Packets now.**
5. **Resolved v11.6-03-01 decision → C**, unified it.
6. **Shipped v11.6-03-02 (WS-12 + WS-13, `7ac307812f`):** WS-12 — an already-open Perform set survives a transient offline `onSnapshot` error (pure `shouldShowFatalSetlistError` gate; full-screen error only when `tracks.length===0`). WS-13 — `SaveOfflineButton` all-ready label now "Saved N/N" (was bare "Saved"); verify-first found WS-13 largely already built, so this was the minimal clarity delta, not a new gate.

---

## What's Next

**Immediate:** `/paul:plan` v11.6-03-03 — **WS-10**: `SmartScoreViewer.tsx:251-253` does `fetch(contentToLoad)` for the MusicXML score (NOT IDB-first like the PDF/text/audio viewers) → offline MusicXML can fail. Rebuild to resolve bytes IDB-first (`offline-idb getFile(fileId)` → Blob) before the network fetch, mirroring `AudioViewer.tsx:62-93`. Forward-risk (no MusicXML in this weekend's 3 sets) but MusicXML is the strategic preferred format. `/ui-ux-pro-max` if any UI.

**After that:** v11.6-03-04 — WS-29 (audio offline: already best-effort; document/accept the WebKit `blob:` residual) + WS-30 (widen SW online-session caching so offline full-reload is reliable). That closes phase 03 → transition → phase v11.6-04 (Authoring + publish round-trip; **live send = STOP-gate**).

---

## Open UAT (real iPad, post-deploy, non-blocking) — `.paul/UAT-PENDING.md`
- **v11.6-02.1 (P0):** David re-prints a Full Packet for a text-chart set on centralreform.live → charts present (not a chartless cover).
- **v11.6-03-02:** WS-12 (drop wifi with a set open → set stays, no blank); WS-13 ("Saved N/N" when all cached).
- Earlier v11.6-02 items (text-fit/PDF-render/controls/image/drawer) + v11.2/v11.1 carry-overs.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live state — Session Continuity = exact next command |
| `.paul/phases/v11.6-03-offsite-resilience/v11.6-03-02-SUMMARY.md` | Last shipped slice |
| `.paul/research/v11-6-03-offline-gap-verify-2026-06-18.md` | The 5-gap verify (WS-10 row drives plan 03) |
| `.paul/phases/v11.6-02.1-gig-packet-text-charts/` | P0 fix PLAN+SUMMARY |
| `src/lib/pdf/text-chart-pdf.ts` | Shared text-chart→PDF renderer (web + MCP) |
| `src/components/music/SmartScoreViewer.tsx` | WS-10 target (line 251-253) |

---

## Working Notes / Posture
- **Autonomy:** auto-commit + push per plan to prod `master`; bake decisions into PLANs; Daniel paces with "go" per PAUL step. STOP only at phase boundaries / STOP-gates (phase 04 live send) / real decisions.
- **Quality floor:** tsc 0 + tests green + AC proof; `SKIP_ENV_VALIDATION=1 npx next build --webpack` before declaring deployable; `/ui-ux-pro-max` BLOCKING on UI.
- **Git:** master (prod). `git pull` first next session (multi-computer); push `origin master` (NOT `master:main`).
- **v11.7 follow-up noted:** unify `TextScoreViewer`'s local `isChordToken`/`isChordLine` onto the shared `text-chart-pdf.ts` module (duplicated during the P0 to avoid churning the live viewer).
- **Windows quirk:** the CARL `carl_log_decision` MCP errored (missing `~/.carl/decisions/development.json`) — decisions are recorded in PLAN/SUMMARY/STATE instead; don't block on CARL infra.

---

## Resume Instructions
1. `git pull` (multi-computer).
2. Read `.paul/STATE.md` (Session Continuity = exact next command).
3. Run `/paul:resume` (or `/paul:plan` directly for v11.6-03-03 — load `/ui-ux-pro-max` if it touches UI).

---
*Handoff created: 2026-06-18*
