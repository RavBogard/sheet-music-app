# PAUL Handoff

**Date:** 2026-06-17
**Status:** paused — clean loop boundary (v11.6-02-04 closed; phase 02 at 4 of 6 plans)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — multi-tenant worship-band setlist/chart app. CRC + Brothers Lazaroff (brotherslazaroff.live) both live.
**Core value:** the band gets the right charts on their iPads each week; Daniel authors setlists via Claude + MCP (the browser app is the band/consumer surface — Perform mode on 11" iPads).

---

## Current State

**Milestone:** v11.6 — Airtight (Weekend Stress & Usability). Narrow hardening anchored to the 3 Camp Sabra weekend sets.
**Phase:** 2 of 4 — **v11.6-02 Perform reading airtight — IN PROGRESS (4 of 6 plans done).**
**Last commit:** `d6e6593b71` (close-loop docs for Plan 04). All work pushed to `origin master` (Vercel auto-deploys).

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [v11.6-02-04 closed]
```

---

## What Was Done (this session)

Resumed from the morning handoff (Plan 01 was already planned), then ran **FOUR full PLAN→APPLY→UNIFY loops**, each committed + pushed to prod `master`:

1. **v11.6-02-01 Nav & hydration airtight** `49dbe460e7` — WS-01 (P0: bonded prayer/reading rows opened their chart instead of bouncing to song 1; `performQueueMembers`/`resolveQueueStart` helpers in `PDFOverlay.tsx`), WS-09 (follower not yanked on live reorder), WS-02 (CSP `img-src` += `https://www.google.com` for Firestore long-poll hydration).
2. **v11.6-02-02 Text Fit-mode reading airtight** `34a39fba30` — WS-03 (true-width auto-fit + 11px legibility floor + `overflow-x-auto`; new pure `text-score-layout.ts`), WS-04 (lyric-governed column → transpose-stable chords), WS-20 (44px controls + zoom aria-labels). `TextScoreViewer.tsx`.
3. **v11.6-02-03 WS-25 slash-bass spelling** `8f7f1a5bd7` — transposed slash-bass spells Bb/Eb not A#/D# (`music-math.ts`); in-key context preserved.
4. **v11.6-02-04 PDF render reliability** `ea4dcd7ec9` — WS-05 (render watchdog → Retry not infinite spinner; width>0 page guard), WS-07 ("Page X of N" indicator), WS-16 (rotate retry-reset). Pure `pdf-viewer-state.ts` + `PDFViewer.tsx`.

Every plan: tsc 0 · tests green · `next build --webpack` 0. Real-iPad confirmations appended to `.paul/UAT-PENDING.md` (non-blocking).

**Phase 02 was re-decomposed twice** as scope clarified: text split into P1-core (02) vs nits (03), and the original "PDF/toolbar/drawer" plan split into 04 (PDF-render, done) / 05 (toolbar+image) / 06 (drawer-nav) — a 6-plan phase.

**Deferred to v11.7 (recorded in STATE Deferred Issues):** WS-19 (chord-line regex — ambiguous), WS-23 (preferFlats — misfire risk), WS-24 (section-gap — no repro).

---

## What's Next

**Immediate:** `/paul:plan` for **Plan 05 (toolbar + image viewer)**:
- **WS-18** — zoom-% readout is `hidden md:…` → shows a bare `/` on iPad (`PerformanceToolbar.tsx:181-183). Hits the live sets.
- **WS-22** — transpose trigger click timed out on iPad-WebKit in the sweep (PDF + text). Verify reachability (likely toolbar overflow/obscured at narrow width). Hits the live sets — **highest live-weight item in this plan.**
- **WS-26** — "Fit to width" actually does `setZoom(1)` (`PerformanceToolbar.tsx:174`); correct only via the width==container invariant.
- **WS-14** — landscape fit-to-width scales portrait PDFs up → overflow below fold (`PDFViewer.tsx`).
- **WS-06** — `ImageScoreViewer` ignores store `zoom` (toolbar Zoom inert on image charts).
- **WS-15** — image-chart load error has no Retry.
- **NOTE:** the 3 weekend sets contain NO image charts → WS-06/15 are lower live-weight; consider sub-scoping (e.g. 05 = toolbar WS-18/22/26/14, defer image WS-06/15 if they bloat the plan).

**After that:** Plan 06 (drawer-nav: WS-08 P1 drawer→single-chart route kills Next/Prev · WS-17 dead current-position highlight · WS-28 SwipeOverlay stale promise — implement or delete) → **phase-02 close (transition: PROJECT/ROADMAP evolve + phase-close commit).**

Then phase 03 (off-site resilience — reframed to VERIFY offline-works + close WS-10/WS-12) and phase 04 (authoring + publish round-trip; live send = STOP-gate).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live state (read first) — Session Continuity has the Plan 05 punch list |
| `.paul/research/v11-6-01-stress-triage-REPORT-2026-06-17.md` | The 31-finding triage report (WS-NN source/line cites) |
| `.paul/phases/v11.6-02-perform-reading-airtight/v11.6-02-04-SUMMARY.md` | Most recent loop outcome |
| `src/components/performance/PerformanceToolbar.tsx` | Plan 05 primary surface (WS-18/22/26) |
| `src/components/music/PDFViewer.tsx` | WS-14 (landscape) |
| `src/components/music/ImageScoreViewer.tsx` | WS-06/15 |
| `e2e/v11-6-perform-stress.spec.ts` | Re-runnable live Perform sweep (the WS-22 transpose-timeout cell lives here) |

---

## Working Notes / Posture

- **Autonomy:** auto-commit + push per phase to prod `master`; bake decisions into PLANs; Daniel paces with "go" per PAUL step. STOP only at phase boundaries / STOP-gates (phase 04 live send) / real 2+-option decisions.
- **Quality floor:** tsc 0 + tests green + AC proof; `SKIP_ENV_VALIDATION=1 npx next build --webpack` before declaring deployable; /ui-ux-pro-max BLOCKING on UI phases (Plan 05 touches UI).
- **Testing reality:** react-pdf does NOT render in jsdom — extract decisions to pure helpers + structure-test + real-iPad UAT (the Plan 04 pattern: `pdf-viewer-state.ts`).
- **Git:** master (prod). `git pull` first next session (multi-computer); push `origin master` (NOT `master:main`).
- **Method:** live Playwright on prod from THIS Windows box (NOT cowork — can't launch WebKit). Manual Daniel UAT → `.paul/UAT-PENDING.md`.

---

## Resume Instructions

1. `git pull` (multi-computer).
2. Read `.paul/STATE.md` (Session Continuity = Plan 05 punch list).
3. Skim `.paul/research/v11-6-01-stress-triage-REPORT-2026-06-17.md` for the WS-18/22/26/14/06/15 line cites.
4. Run `/paul:resume` (or `/paul:plan` directly for Plan 05).

---
*Handoff created: 2026-06-17*
