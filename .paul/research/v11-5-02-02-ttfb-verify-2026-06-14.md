# H7 — /perform cold-open TTFB verify-first (v11.5-02-02 Task 1)

**Date:** 2026-06-14 · **Probe:** `node scripts/v11-3-04-webvitals-slice.mjs --since 7`
(read-only; 1232 observations since 2026-06-07; firebase-CLI ADC, cleaned up).

## /perform cold(navigate) p75 — measured vs baseline

| metric | baseline (2026-06-10) | now — cold·desktop | now — cold·mobile | aggregate p75 |
|--------|----------------------|--------------------|--------------------|---------------|
| TTFB   | ~1633ms              | **1990ms**         | **2330ms**         | 1823ms        |
| FCP    | ~3551ms              | 3797ms             | 6064ms             | 3797ms        |
| LCP    | —                    | 3247ms             | 4092ms             | 3012ms        |
| CLS    | (BUG-2 lineage)      | 0.040              | **0.250** (n=8)    | 0.040         |

cold·tablet(iPad): n=1 (346ms) — too low to read. CWV "good" thresholds: TTFB≤800, FCP≤1800, LCP≤2500, CLS≤0.1.

## VERDICT — INFRA-BOUND. No /perform app churn.

TTFB did **not** improve and is, if anything, marginally higher (1823–1990ms vs ~1633ms — same
order of magnitude, within RUM noise + cold-start variance). The v11.3-04-03 fix already took the
Firestore query OFF the first-byte path (Suspense streaming — recon-confirmed at
`src/app/perform/page.tsx:51-65`; the shell/skeleton flushes before the listing fetch resolves).
There is **no new server-side blocking fetch** on the critical path. The residual cold TTFB is
dominated by Vercel serverless cold-start + Edge→Firestore-region geo latency — an INFRA lever,
NOT app code. Per the standing deferred-issue guidance, do **not** re-churn `/perform` app code.

**Action:** Vercel infra follow-up logged to STATE `### Deferred Issues` (fluid-compute / keep-warm
/ region). H7 closes with zero app changes.

## Side-flags (out of H7/TTFB scope — recorded for awareness, NOT actioned here)

- **/perform cold·mobile CLS 0.250** (n=8) vs `/setlists` 0.03. CLS axis (prior BUG-2 lineage,
  whose sign-in-card reservation fix was desktop/iPad-framed). Low sample; candidate for a future
  CLS-focused look, NOT this plan. **Constraint honored:** the F1 "tonight's set" hero (Tasks 2-3)
  renders in the SSR first paint (seeded from `initialSetlists`) so it does NOT add post-hydration
  shift / worsen this.
- Per-track `/perform/setlist/[id]/track/<id>` rows show CLS 0.40–0.44 on a few tracks — chart-image
  reflow on the chart view (known), distinct from the index. Not in scope.
- `/qr/<code>` rows show very high FCP/TTFB on a few codes (n≈0 LCP) — sparse single-hit QR
  redirects, not a surface regression.
