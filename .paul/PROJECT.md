# Project: sheet-music-app (CentralReform.live)

> **⚠️ 2026-05-15 AUTHORING-MODEL PIVOT — read before acting**
>
> As of 2026-05-15, the MCP server (`/api/mcp`) is Daniel's PRIMARY author surface. He builds weekly setlists, edits charts, uploads new charts, and adjusts monitor mixes via Claude Desktop / claude.ai — NOT via the in-app library UI. The browser app is now the band/consumer surface (Perform mode on iPads, chart-bind picker, gig-packet print, setlist viewing).
>
> Any references below to "v7.0 ✅ COMPLETE" / "awaiting next milestone" / "MCP runs as a parallel workstream" reflect the pre-pivot framing. The MCP workstream is now the active primary workstream. See:
> - `~/.claude/projects/.../memory/user_mcp_is_primary_author_workflow.md` (the pivot)
> - `~/.claude/projects/.../memory/project_mcp_status.md` (27 tools live, latest f650d94f0, remaining gaps: generate_gig_packet → publish_setlist → list_library)
> - `.paul/research/mcp-stress-test-2026-05-15-mcp-first-PROMPT.md` (the NEXT stress test — the older `.paul/research/mcp-stress-test-2026-05-15-followup-PROMPT.md` is SUPERSEDED)
>
> When ranking work, prioritize (a) Daniel's MCP authoring flow and (b) the band's iPad consumer flow. The in-app library admin UI is no longer load-bearing for Daniel's daily use.

## What This Is

A web app for Central Reform Congregation's worship band that musicians load on tablets, iPads, and phones to manage setlists, view and live-transpose charts, and adjust monitor mixes on the fly. Used during live services, rehearsals, and community jam events where anyone can pull up the setlist and charts on their own device.

## Core Value

Musicians can instantly access setlists, transpose charts to their instrument, and control their monitor mix — all live on any device, no paper or prep needed.

## Current State

| Attribute | Value |
|-----------|-------|
| Version | **v11.2 MCP Stress-Test Fixes — ✅ COMPLETE 5/5 (2026-06-11; `package.json` `11.2.0`, tag `v11.2.0`, tip `f27ae7bc5f`)** — all 9 Brothers Lazaroff MCP/Perform stress-test bugs fixed (BUG-1 resolver 404 · BUG-9 publish-audience org scope · BUG-2/3 error contract · BUG-4/5 publish+test-data hygiene · BUG-6/7/8 P3 polish: dashboard-hero brand, chord-wrap, MCP timestamp ISO), CRC byte-identical throughout. Prior: **v11.1 Brothers Lazaroff Post-Launch Fixes — ✅ COMPLETE 4/4 (2026-06-09; `package.json` `11.1.0`, tag `v11.1.0`; MCP authoring verified live);** **v11.1-01 org-aware authed branding ✅** (nav wordmark+logo host-resolved; broslaz "BL" monogram) + **v11.1-02 multi-org membership + authoring ✅** (host-derived authoring bearer + admin org-membership toggle) + **v11.1-03 library generic-tab visibility ✅** (host-isolate chart reads + admin All-sites toggle) + **v11.1-04 broslaz liturgical vocab sweep ✅** (dashboard headers + matrix title via vocab layer); CRC byte-identical throughout. Prior: **v11.0.0 — Brothers Lazaroff Multi-Tenant ✅ COMPLETE 2026-06-09** (`package.json` `11.0.0`, tag `v11.0.0`). Multi-tenant platform; 2nd live tenant brotherslazaroff.live fully isolated; close-gate AUDIT.md GO; live probe 19/19. — Parallel: **v7.1 Production Hardening & MCP Authoring Surface** continues via the bongo `.coord/` cowork cycles 1–13 (PAUL-tracking label for the post-v7.0 hardening campaign: MCP surface → 108 tools, iPad-WebKit/Perform hardening, MusicXML capo/transpose, studio-bridge v10.x, drive-sync/library-index backfills, storage-backup alarms, a11y/WCAG, ops observability, test-harness). Prior PAUL milestone: v7.0 ✅ 2026-05-14. Full detail: `.paul/MILESTONES.md` §§ v11.0, v7.1. |
| Status | Production. On `master`, in sync with `origin/master` (Vercel auto-deploy). package.json `11.2.0`, tag `v11.2.0`. MCP server is Daniel's primary authoring surface; broslaz MCP URL `https://www.brotherslazaroff.live/api/mcp` (verified live 2026-06-09). v7.1 cycle-13 continues via `.coord/`. |
| Last Updated | 2026-06-10 (v11.3 Worthiness & Access OPEN; Phase v11.3-01 anon access correctness ✅ — BUG-5 + BUG-4) |

**Production URLs:**
- CentralReform.live: Main application

## Requirements

### Validated (Shipped)

- [x] Setlist management with drag-and-drop ordering, sections, BPM/key metadata
- [x] Sheet music library synced from Google Drive with Firebase Storage CDN
- [x] Performance mode full-screen PDF viewer with smart prefetch and swipe nav
- [x] AI chord detection via Gemini OCR with transposed chord overlays
- [x] Auto-transposition with musician profiles and instrument presets
- [x] Print gig packets with per-musician transpositions
- [x] Light/dark theme with auto dark mode for performance
- [x] ~~PWA offline support with 30-day Workbox caching~~ — Removed: SW caused production issues, venue has wifi
- [x] ~~AI chat assistant for natural language setlist management~~ — Removed in v50-02 (2026-04-26): user did not use the feature; deleted as dead weight before v5.0 editor rewrite
- [x] v1.3 Bugsweep: codebase audit, security fixes, backend hardening, frontend robustness — Phase 1-4
- [x] v1.3.1 Regression fixes: cache-busted PDF worker, iPad monitor connection stability
- [x] v1.4 Phase 1: Library management — rename songs, unlink charts, archive restore
- [x] Auto-key detection on all chart assignment paths (fixed in c6375f4)
- [x] v1.4 Phase 2: Prominent key display + 5 monitor buses
- [x] v1.4 Phase 3: Print gig packet fixes (black screen, date, auth)
- [x] v1.4 Phase 4: PDF health scanner false positives fixed
- [x] v1.5 Phase 1: Critical bug fixes — AI slot safety, clearSaveTimer, transposition race, initAdmin, Firestore sanitization
- [x] v1.5 Phase 2: Security & API consistency — npm audit, CSP/HSTS, error sanitization, 18 routes migrated to createApiHandler
- [x] v1.5 Phase 3: Architecture cleanup — SetlistEditorV2, MusicianPicker, TransposerMenu, SongChartsLibrary split into sub-components
- [x] v1.5 Phase 4: Quality & deps — vitest jsdom default, ESLint exhaustive-deps re-enabled, deps/fonts/indexes already current
- [x] v1.5 Phase 5: UI/UX polish — skip-to-main-content link, mobile zoom indicator, tablet landscape sidebar toolbar
- [x] v1.5 Phase 6: Performance & monitoring — bundle analyzer, Sentry source maps + Web Vitals, component tests for PerformanceToolbar + SetlistEditorV2
- [x] v1.6 Phase 1: Auth & CSP hardening — signInWithPopup finalized, CSP audit, static PWA icon fallbacks
- [x] v1.6 Phase 2: Firebase-only file serving — removed Drive fallback, Storage is sole source of truth
- [x] v1.6 Phase 3: Performance view overhaul — key-left setlist redesign, removed tablet sidebar toolbar
- [x] v1.6 Phase 4: Regression sweep — route-auth test fix, 2 withAuth migrations, 23 ESLint suppress comments, CI green

### Active (In Progress)

**v7.0 — Document-Driven Setlist Creation (opened 2026-05-13).** Detailed plan in `.paul/ROADMAP.md` § Current Milestone — 8 phases in 6 waves. Daniel feeds service-outline documents (May 15 Shir Shabbat .docx is the v70-05 canary) and the system produces a complete setlist: charts bound, recordings linked, and gaps surfaced via a structured form for Daniel to fill before commit. Wave 0 = image-chart support (v70-01 PNG/JPEG/HEIC; prereq for v70-05 doc-extraction canary because the May 15 doc references `dodi li (sher).png`). Wave 1 = recordings data model (v70-02 NEW `recordings/{id}` collection with foreign key `songId?` + `notes` field for attribution). Wave 2 = parallel (v70-03 per-track media affordances [chart click-through + recording-bind UI] / v70-04 doc upload + text extraction via mammoth/pdfjs). Wave 3 = sequential (v70-05 Gemini structured extraction with Zod validation / v70-06 resolve + missing-chart + recording-match with fuzzy confidence scoring). Wave 4 = v70-07 interview form (NOT chat) + setlist preview + commit via existing createSetlistService. Wave 5 = v70-08 best-practice audit + remediation (5 dimensions: security / accessibility / performance / code quality + data integrity / UX consistency; reuses v5.4 architectural-audit pattern; BLOCKS milestone close). 12 constraints locked at /paul:discuss-milestone (Firebase Storage / .docx+.pdf+.txt only with image OCR DEFERRED to v7.1 / structured form / `notes` field for attribution / NEW recordings collection / Gemini+Zod / fuzzy match with confidence / reuse /api/library/upload for missing charts / service date REQUIRED / service type auto-inferred / image charts disable AI chord+transpose / audit blocks close). Daniel-directive "do it right" — no time pressure; quality > speed.

**v5.0 — Bulletproof Editor — UAT pending (2026-04-27).** All 7 phases shipped; awaiting Rabbi Daniel + one band member to execute the weekly worship cycle UAT before milestone close via `/paul:audit-milestone`. v5.1 + v5.2 + v5.3 are the prerequisite polish + hardening to make Daniel's full v5.0 UAT comfortable on iPad with the band onboard. After UAT succeeds + first-week smoke, v5.0 milestone closes.

_(v6.0 moved to Validated (Shipped) — closed 2026-05-13 via /paul:complete-milestone with PENDING-UAT marker per v51-04 codified pattern; full milestone log at .paul/MILESTONES.md § v6.0; archive snapshot at .paul/milestones/v6.0-ROADMAP.md.)_

_(Original v6.0 detail block from 2026-05-12 milestone open, preserved here for archive reference only:)_

> **v6.0 — Tracks Single-Source-of-Truth (plan locked 2026-05-12).** Detailed plan in `.paul/MILESTONE-CONTEXT.md` — 10 phases in 4 waves; finishes the v50-05 tracks migration (top-level `tracks/{id}` becomes single source; every reader and writer routes through one helper). Wave 1 = orthogonal UX fixes (v60-01 SyncIndicator conflict click rewire + v60-02 pagehide blur mid-edit text protection). Wave 2 = HFG closure (v60-03 Java JDK 21 + emulator canary green; BLOCKS Wave 3). Wave 3 = migration spine (v60-04 server reads → v60-05 editor/perform reads → v60-06 dashboard + 15-setlist backfill → v60-07 writer removal + immediate strip → v60-08 cleanup). Wave 4 = folded-in v5.4 deferrals (v60-09 cross-device library sync / v60-10 Mobile AddBar variant). 12 decisions locked (no re-litigation): Option A finish migration / 15-setlist backfill / silent last-write-wins / immediate strip / browser-smoke mandatory / Java install pre-Wave-3 / library sync + Mobile AddBar fold INTO v6.0 / major-version bump v6.0. Outcome: 12 phases LOOP COMPLETE (2 emergent close-gates added 2026-05-13 — v60-11 shortcut-aware mirror + v60-12 public tracks visibility). 24 plans / 25 commits / HFG counter 0/3 held throughout. Closed 2026-05-13.

**v5.0 — Bulletproof Editor — UAT pending (2026-04-27).** All 7 phases shipped; awaiting Rabbi Daniel + one band member to execute the weekly worship cycle UAT before milestone close via `/paul:audit-milestone`. v5.1 + v5.2 + v5.3 are the prerequisite polish + hardening to make Daniel's full v5.0 UAT comfortable on iPad with the band onboard. After UAT succeeds + first-week smoke, v5.0 milestone closes.

_(v5.0 entry consolidated above into the v5.2-shipped close-path note; original v50-07 production-deploy details remain in MILESTONES.md.)_

**v4.5 — Superseded by v5.0** (2026-04-26): 2 of 8 phases shipped (v45-01 save-path observability, v45-07 library cache invalidation). 6 phases cancelled — they targeted save-path machinery v5.0 deletes wholesale.

### Validated (Shipped this cycle)

**🚧 v11.3 Worthiness & Access — Phase v11.3-01 Anon access correctness ✅ COMPLETE 2/2 (2026-06-10)** — First phase of v11.3 (oracle: `docs/ACCESS-POLICY.md` v0.3). Closes the two anon-read prime-directive violations from the 2026-06-10 stress runs. **v11.3-01-01 (BUG-5):** `/api/library/file/[id]` was a default-auth, `db-*`-only route → anon 401 `missing_bearer` for every id; reworked into a public chart proxy mirroring `/api/drive/file/[fileId]` (`requireAuth:false` + `isTrusted` Sec-Fetch/bearer gate + `chart` tier + `fetchFileById` for `upload-*`/Storage; `db-*` digitized_charts now anon-public). **Verify-first refined BUG-5 → P2:** the Perform render path uses `/api/drive/file` (anon-OK), so the "blank cold-device render" P1 escalation was not real; the genuine fix was anon "Open chart in new tab" on legacy `db-*` charts + the oracle contradiction. No client edit (`parseFileId` already correct). **v11.3-01-02 (BUG-4, D-Q2):** anon transpose dead-ended on "Waiting for scan…" — `/api/library/chord-cache` GET/POST + `/api/ai/transposer/scan` POST were default-auth → 401. Opened GET+POST (anon read + persist scan results to cut total Gemini spend) and scan (POST) to anon with an **anon-only** `ai`-tier rate-limit (authed unchanged — it had no limiter before → zero regression); PATCH/DELETE stay role-gated. Quality floor held both plans (tsc · route tests 13 total green · `next build` clean). **UAT-PENDING (live, non-blocking):** anon db-* "open in new tab" + cold-device `upload-*` Perform render; anon transpose renders chords; authed transpose unchanged. Phases 02–05 (agent chart-upload, hygiene, /perform perf, P3 polish) remain. (2026-06-10)

**🎉 v11.2 MCP Stress-Test Fixes — ✅ COMPLETE 5/5 (2026-06-11; tag `v11.2.0`, tip `f27ae7bc5f`)** — All 9 findings from the Brothers Lazaroff MCP + Perform stress-test (2026-06-09) closed, BL tenant only, CRC byte-identical throughout. **v11.2-01 BUG-1** propose/commit 404 → shared `getSetlistById` resolver (stage→confirm→commit restored). **v11.2-02 BUG-9** publish-audience org-scoped (no cross-tenant roster leak; verify-first + STOP-gate honored). **v11.2-03 BUG-2+3** MCP error contract — deterministic HTTP-class codes (500→404/400/409) + uniform single/bulk `RichErrorBody`. **v11.2-04 BUG-4+5** publish/test-data hygiene — `preview_publish` flags unbonded `type:song` rows (`review_first`); `cleanup_all_test_data` sweeps owner-real `isTest` setlists (full-sweep mode, prefix-isolation preserved) + authed dashboard hides isTest via shared `isNonTestSetlist` (reused from `/perform`). **v11.2-05 BUG-6+7+8** P3 polish — dashboard hero host-resolves brand (no "CRC MUSIC" on broslaz, mirrors v11.1-01 nav seam); `TextScoreViewer` keeps lyric words intact (word-atomic wrap); single `serializeTimestamps()` pass in `jsonResult` renders every MCP tool's timestamps ISO. **Scope correction:** `library_index`/`songs` have no `isTest` field (owner-cascaded) → that half of BUG-5 dropped as unimplementable. Quality floor held every loop (tsc · emulator/unit suites green · `next build` clean). **UAT-PENDING (non-blocking, live/safe):** BL-connector reconnect → BUG-1 + BUG-9 retests. (2026-06-11)

**🎉 v11.1 Brothers Lazaroff Post-Launch Fixes — ✅ CONTENT COMPLETE 4/4 (2026-06-09; awaiting `/paul:complete-milestone` after live-tenant UAT)** — Closing the gaps a real second tenant surfaced after launch. **Two-tier tenancy model locked:** consumers (musicians/members) are host-derived (access both sites; experience set by the landing-page host); band leaders carry explicit `orgIds` membership (CRC / broslaz / both). **Phase v11.1-02 Multi-org membership + authoring ✅ COMPLETE 2026-06-09 (2 plans):** **v11.1-02-01** host-derived MCP authoring — the OAuth + self-service mint paths stamp the bearer's org from the connection DOMAIN's `x-org-id` (validated ∈ caller's orgIds, else primary-org fallback) via new `resolveMintOrg`; a multi-org leader authors for a tenant by pointing Claude Desktop at that tenant's MCP URL (`https://www.brotherslazaroff.live/api/mcp`). Fixes the live bug where Daniel's MCP setlist landed `orgId:'crc'`. Org pinned at mint by host, NOT a tool arg → v11-06-02 no-arg-injection invariant preserved (test unchanged). emulator 9/9; pushed `941e6856d1`. **v11.1-02-02** admin org-membership toggle — a tri-state "Band access" control (CRC / BL / Both) on band_leader/admin rows in `/manage` → People (admin-only), writing `orgIds` to BOTH the Auth claim and the user doc (set-role txn, claim+doc lockstep) + a membership badge; reuses shadcn Select/AlertDialog (/ui-ux-pro-max). unit 4/4; pushed `d466160601`. Full suite 3327/0; next build clean. **Phase v11.1-01 Org-aware authed branding ✅ COMPLETE 2026-06-09 (1 plan):** the authed top-nav (DesktopHeader + MobileHeader) now resolves wordmark + logo from the host org — `getOrgBranding` gained a `logoUrl` field (crc `/logo.jpg`, broslaz `""`); `(main)/layout.tsx` resolves the host org from `x-org-id` (root-layout seam) and passes `serverOrgShortName`+`serverLogoUrl` to both headers (flash-free SSR); new `OrgLogo` renders the logo image or, when `logoUrl` is empty, a navy `bg-brand`/`text-brand-foreground` "BL" monogram (Daniel decision — auto-upgrades to `<img>` if a path is ever set). Congregation store kept as the live-update fallback. CRC byte-identical (1 invisible a11y micro-improvement: mobile alt "Logo"→"Central Reform Congregation logo"). Closes the last consumer-visible CRC-branding leak on broslaz. tsc clean · suite 3332/0 · `SKIP_ENV_VALIDATION=1 next build` clean; /ui-ux-pro-max BLOCKING satisfied. **Phase v11.1-03 Library generic-tab visibility ✅ COMPLETE 2026-06-09 (1 plan):** the in-app chart library now host-isolates per tenant — `getServerLibrary(orgId)` (SSR) + `/api/library/list` (default host-org filter via `rowOrg`) scope `library_index` charts to the landing-page org; because the Library tab, chart-bind picker, and header search all read the shared `useLibraryStore`, filtering at the fetch layer isolates all three from one point. An admin-only **"All sites"** toggle (gated by `getServerUser().isAdmin`) reveals the full cross-tenant pool for authoring/binding; display-only — the file-serving routes are untouched so direct chart links always work (err-public). broslaz gets org-neutral tab labels ("Charts / Uploads / Audio", no Shireinu); CRC byte-identical. **DECISIONS (Daniel):** Shared flag DEFERRED (libraries disjoint today); **recordings-collection scoping DEFERRED** (separate per-song surface + `/api/recordings/upload` hardcodes orgId=crc → fix stamp first). tsc clean · suite 3339/0 · next build clean; /ui-ux-pro-max satisfied. **Phase v11.1-04 broslaz liturgical vocab sweep ✅ COMPLETE 2026-06-09 (1 plan):** the last hardcoded synagogue strings that bypassed the org vocab layer — `SetlistDashboard` section headers ("Upcoming Services", "Create New Setlist") + `SetlistMatrixView` title ("Liturgical Matrix") — now resolve via `label(useOrg(), key)`; added 3 vocab keys (crc base byte-identical + broslaz "Upcoming Shows"/"Create New Set"/"Set Matrix"). "Plan Service" was already vocab'd (v11-05-05). DEFERRED: SERVICE_TYPE_LABELS vocab-table refactor (gated-away for broslaz). tsc clean · suite 3339/0 · next build clean. **v11.1 CONTENT COMPLETE 4/4** → awaiting `/paul:complete-milestone`. **UAT-PENDING (live tenant, before formal close):** broslaz authed nav ("Brothers Lazaroff" + BL monogram, desktop + iPad-WebKit); /library (broslaz-only charts in tab + add-songs picker + header search; admin All-sites reveals full pool); dashboard ("Upcoming Shows"/"Create New Set") + matrix ("Set Matrix"); MCP authoring (connect Claude Desktop to the broslaz MCP URL → author a test setlist → lands broslaz); admin sets a leader's membership in /manage; CRC unchanged throughout. (2026-06-09)

**🎉🎉 v11.0 Brothers Lazaroff Multi-Tenant — MILESTONE COMPLETE 2026-06-09 (6/6 phases + v11-02b; 23 plans; ~2 days)** — The app is now multi-tenant: a second live tenant, **Brothers Lazaroff** (`brotherslazaroff.live`), is fully isolated from CRC across reads, writes, all collections (setlists/tracks/songs/recordings/templates/roster/scheduling/congregation), branding, vocab, and MCP authoring — within the same app + Firebase (`crcmusiccharts`), CRC behavior-neutral. **v11-03** domain + navy/dark branding + de-synagogued vocab · **v11-04** BL consumer surface (perform/print) + org-aware metadata + authed-dashboard read scoping · **v11-05** cross-tenant collection scoping (templates/roster/assignments/congregation R+W) + in-app create orgId-stamp + CreationWizard vocab (`f3448a8395`) · **v11-06 close-gate isolation audit — VERDICT GO**: all three adversarial axes closed (Firestore-rules leakage — scheduling_assignments/_history hardened via `orgReadOk` + per-org congregation wildcard, rules deployed; MCP org-scope escape — no-arg-injection invariant CI-locked; host-spoof — `verifyBearer` bearer-authoritative, ignores forged `x-org-id`); leadHistory tenant-scoped; **live prod probe 19/19** (BL-isolated + CRC-intact); sign-off at `.paul/phases/v11-06-isolation-audit/AUDIT.md`. Pushed to prod tip `2a8441d6e5`, Vercel deployed, firestore.rules deployed. **Residuals (accepted, low-risk, defense-in-depth):** setlistTemplates app-only (no client path); scheduling_history orgId-absent rows; users claim-based (no orgId field). Ran fully autonomously per the v11.0 autonomy directive; quality floor held throughout (tsc 0, emulator 941/941, non-emulator 3323/0). **UAT-PENDING:** David's hands-on UX confirmation on brotherslazaroff.live. (2026-06-09)

**🎉 v11.0 Brothers Lazaroff Multi-Tenant — Phase v11-02b Org-aware token minting ✅ COMPLETE 2026-06-08 (1 plan / LOOP COMPLETE)**
- [x] v11-02b Org-aware token minting — closes the gap Daniel surfaced 2026-06-08: the self-service mint paths (`/api/mcp/tokens` + `/api/mcp/oauth/token`) called `createMcpToken(uid,label)` with no org arg, hard-defaulting `orgId` to crc — so a non-CRC member self-minting (or via Claude Desktop's OAuth flow) would have landed in CRC's tenant. **v11-02b-01** added `getPrimaryOrgForMinting(uid)` (reuses v11-01-01's `getUserOrgIds`; first-of, default crc) and threaded it into both mint routes; `mint_admin_bearer` (inherits caller org) + the test-token mint (default crc) left untouched. Now tenant members onboard self-service via plain login → a correctly org-scoped bearer, no manual raw-token handoff; David's experience matches Daniel's. CRC (claimless) stays crc. unit 9/9 + emulator 3/3 (claim→resolver→mint→token-doc orgId→verifyBearer); tsc clean; Vercel prod build READY (feat(v11-02b) `2db15f36d9`); prod-verify getUserOrgIds(DavidUid)===["brotherslazaroff"]. **Decision:** fix now (Daniel) over deferring to v11-04. **Caveat:** first-org-wins on multi-org claims (single-org today). (2026-06-08)

**🎉 v11.0 Brothers Lazaroff Multi-Tenant — Phase v11-02 MCP org-scoping ✅ COMPLETE 2026-06-08 (4 plans / all LOOP COMPLETE) — FIRST LIVE SECOND TENANT**
- [x] v11-02 MCP org-scoping — the MCP tenant wall, deployed + proven live. **v11-02-01** caller-org resolution foundation: `createMcpToken` stamps `orgId` at all 4 mint sites, `verifyBearer` resolves caller org from the `mcpTokens` doc (default crc) → `AuthInfo.extra` → `orgFrom(extra)` seam; prod token backfill (117 stamped). Behavior-neutral. **v11-02-02** READS org-scoped (6 tools): list/search filter to caller org; get_setlist/get_song → cross-tenant not-found wall; search_chart_text metadata+chords parent-org drop; emulator 7/7. **v11-02-03** WRITES walled: `loadEditableSetlist(db,id,uid,org)` chokepoint scopes 8 setlist-write tools + per-tool guards on delete/recompute/clone/clone-from-template/update_song/delete_chart; every create stamps caller org (incl. clone + template raw-batch writers + `stampOrg` on the 3 chart-create tools); not-found wall (NOT a leaky cross_tenant_denied code); emulator 8/8. **v11-02-04** issued David Lazaroff's `brotherslazaroff` bearer (`scripts/issue-bl-bearer.mjs`, orgId on the token doc) + `orgIds:['brotherslazaroff']` claim by MERGE (role preserved) on his existing band_leader account; shipped `feat(v11-02)` (`c7da31ac2a`) to prod; **live e2e 12/12** against www.centralreform.live/api/mcp (David reads BL-only, create stamps BL, cannot get/update/delete a CRC setlist, CRC unaffected). **Key decisions:** caller org from the token doc (not Auth claims); org param OPTIONAL default crc on every tool (prod passes explicit orgFrom); admin-SDK writes bypass Firestore rules so the app layer is the control; not-found wall to avoid existence-leak; claim by MERGE. **DEFERRED to v11-04:** templates READ/LIST, roster/musicians, congregation, service-personnel still cross-tenant. **UAT-PENDING:** Daniel securely hands David the raw bearer (tokenId `93JMXhT1OspFsWDMmb9V`) + David's UX confirmation. Foundation for v11-03 (host routing/branding). (2026-06-08)

**🎉 v11.0 Brothers Lazaroff Multi-Tenant — Phase v11-01 Tenant foundation ✅ COMPLETE 2026-06-08 (4 plans / all LOOP COMPLETE)**
- [x] v11-01 Tenant foundation — the multi-tenant spine. **v11-01-01** org model + tenant registry (`src/lib/org/` — DEFAULT_ORG_ID, ORGS, Org type, membership claims with backward-compat default-crc). **v11-01-02** write-path orgId stamping at all 5 server create sites (setlist+seeded tracks, addTrack, bulkAddTracks, library_index+songs, recording); tracks inherit parent setlist's orgId; default "crc" everywhere (CRC behavior unchanged). **v11-01-03** CRC backfill — `src/lib/org/backfill-orgid.ts` + emulator test + prod `.mjs` runner/RUNBOOK; ran prod migration stamping **2105 docs** orgId="crc" + seeded orgs/{crc,brotherslazaroff}, idempotency verified. **v11-01-04** org-scoped Firestore rules (write-ISOLATION not write-requirement — reads unchanged/err-public, cross-tenant writes denied, orgId immutable across tenants) + 42 blocking emulator rules tests (which **caught + fixed a CRC lock-out bug** — bare `token.orgIds` access throws for claimless users) + deployed to prod. **Key decisions:** write-isolation over write-requirement (client write paths omit orgId — hard-require would lock out CRC); reads stay public (cross-tenant read filtering deferred to app query layer, v11-03); `.get('orgIds',[])` default-crc in rules. **Verification:** tsc clean throughout; emulator suites green (orgid-stamping + backfill-orgid + firestore-rules-orgscope); prod backfill + rules deploy both verified live. Foundation for v11-02 (MCP org-scoping) → v11-03 (host routing). (2026-06-08)

**v7.0 Document-Driven Setlist Creation — Phase v70-07 ✅ COMPLETE 2026-05-14 (Wave 4 — interview form + setlist preview + commit; 3 plans / all LOOP COMPLETE)**
- [x] v70-07 Interview form + setlist preview + commit — The commit stage that turns v7.0's "feed a doc, get a setlist" pipeline into a real, end-to-end action. Split into 3 plans (Daniel-confirmed 2026-05-14). **v70-07-01** extracted the server-side setlist-write logic that lived inline in `import/execute` into `src/lib/setlist-write.ts` — `createSetlistServerSide` (parent `setlists/{id}` doc + batched top-level `tracks/{id}` seed) + `updateSetlistServerSide` (metadata-patch-only) — the ONE server-side write path, consumed by `import/execute`, v70-07's doc-commit, and (per the parallel MCP-workstream handoff) the MCP write tools; signature Daniel-approved; emulator-tested; interim-committed + pushed so the MCP tools could consume it. **v70-07-02** added the doc-import UI to the existing `ImporterModal`: a third "Upload Document" input option (.docx/.pdf/.txt) that chains the v70-04→06 routes client-side (`extract-document` → `extract-structure` → `resolve`), then walks a structured **interview form** (setlist name, REQUIRED service date auto-suggested from the filename, service type auto-inferred from document keywords, optional rabbi — `src/lib/setlist-import/interview-defaults.ts` owns the pure helpers) and a read-only **setlist preview** grouped by section with per-track chart-match / missing-chart / recording-candidate status. **v70-07-03** closed the loop: `src/lib/setlist-import/commit.ts` (`commitDocumentSetlist` + `flattenResolvedStructure` — flattens the resolved `{ sections, tracks }` into `createSetlistServerSide` input with section headers interleaved before their songs, `libraryMatch` → bound chart `fileId`, `recordingCandidates` ignored), the band_leader-gated `POST /api/setlists/import/commit-document` route, and the wiring of the ImporterModal preview "Create Setlist" button. **Verification:** `next build` ✓ (all 5 import routes registered); `tsc` clean for all changed files; emulator suite 56/56 (HFG coverage — `setlist-write.emulator.test.ts` + `commit.emulator.test.ts`, no clause-(b) waiver); `setlist-import` suite 39/39; HFG counter held at 0/3. /ui-ux-pro-max BLOCKING gate satisfied on plans 02 + 03 (backend-only plan 01 N/A). Deviations: 1 minor (v70-07-02 service-type inference simplified to keyword-only — the date-based fallback was dropped because `getServiceContext`'s `ServiceType` enum doesn't map cleanly onto `templateType`, and the user confirms the value anyway). **Recording binding DEFERRED entirely for v70-07** (Daniel decision — `recordingCandidates` surfaced in the preview but not bound on commit). The doc-driven pipeline is now feature-complete: upload → extract → AI structure → resolve → interview → preview → a created, chart-bound setlist. **Carry-forwards:** v70-07-02 + v70-07-03 UI human-verify checklists in `.paul/UAT-PENDING.md` (v51-04 pattern). (2026-05-14)

**v7.0 Document-Driven Setlist Creation — Phase v70-06 ✅ COMPLETE 2026-05-14 (Wave 3 — resolve + missing-chart + recording-match; single-plan phase / LOOP COMPLETE)**
- [x] v70-06 Resolve + missing-chart + recording-match — The resolution stage of v7.0's "feed a doc, get a setlist" pipeline. **v70-06-01** added `src/lib/setlist-import/resolve.ts` — `resolveSetlistStructure(structure, opts?)` takes v70-05's Zod-validated `{ sections, tracks }` and annotates each track: a fuzzy chart match (`libraryMatch` = `{ fileId, name, confidence }`, best `application/pdf` / `image/*` library entry above the 0.82 similarity threshold), a `missingChart` boolean (true when no confident chart match — v70-07 routes these to `/api/library/upload`), and `recordingCandidates` (all `audio/*` library entries above threshold, sorted best-first). The match algorithm is MIRRORED from `import/parse/route.ts` (normalize → `levenshteinDistance` → `1 - d/maxLen` → 0.82 threshold, exact-match-only for <3-char normalized strings) — import/parse is NOT refactored. The library is read once via `getServerLibrary()` and partitioned by mimeType prefix. Returns a discriminated never-throws `ResolveResult`. **PROPOSE-ONLY — zero Firestore writes** (Daniel-confirmed 2026-05-14; supersedes the ROADMAP's "pre-creates recordings/* docs" wording — avoids orphaned docs, v70-07's commit owns persistence). Behind it, `POST /api/setlists/import/resolve` — a JSON route (`createApiHandler`, default auth, `checkRateLimit('upload')`, `maxDuration=60`, body validated by the reused `SetlistStructureSchema`); maps `ok` → 200 `{ success, sections, tracks }`, `library_unavailable` → 502, malformed → 400. Backend slice — NO UI (v70-07), NO new dependencies. **Verification:** `next build` ✓ (`ƒ /api/setlists/import/resolve` registered); `resolve.test.ts` 8/8 (chart match PDF + image, missingChart, recording candidates sorted + empty, sections passthrough + order preservation, empty library, short-title exact-match); full `src/lib/setlist-import/` suite 26/26 (no regressions). HFG N/A (zero Firestore writes). /ui-ux-pro-max gate N/A (backend-only slice). Zero deviations. The doc → text → structure → resolve chain is now complete; **v70-07 consumes the resolved `{ sections, tracks }`** for its interview form + commit. (2026-05-14)

**v7.0 Document-Driven Setlist Creation — Phase v70-09 ✅ COMPLETE 2026-05-14 (out-of-sequence polish jump — setlist metadata editor; single-plan phase / LOOP COMPLETE)**
- [x] v70-09 Setlist metadata editor — Closes long-standing Issue 2 ("no way to modify the name / date / etc. of a setlist that has already been created"). Daniel pulled this forward 2026-05-14, ahead of v70-06. **v70-09-01** added `src/components/setlist/grid/SetlistMetaEditSheet.tsx` — a `Sheet`-based form (name `Input` / event date `Calendar`-in-`Popover` / service type `Select` over the 6 `templateType` enum values / rabbi `Input`) opened by a new pencil edit-icon button (`onEditMeta`) in `SetlistGridTopBar` beside the setlist name. Save builds a CHANGED-FIELDS-ONLY patch and writes via the existing v6.0 `applyEdit('update','setlists',…)` outbox — no engine touch, no new API route, no new deps. `eventDate` is written as an ISO string (firestoreTimestampSchema accepts Date/{seconds,nanoseconds}/string, NOT plain numbers). `SetlistGrid` gained a `useLiveQuery` on the setlist doc so the top bar reflects name/date edits immediately and seeds the Sheet; the static `name`/`eventDateLabel` props are kept as a fallback. Cancel / Escape / an unchanged Save are non-destructive (no write enqueued). **Verification:** `next build` ✓ (`/setlists/[id]` builds); `tsc` clean for all 5 files; new tests `SetlistMetaEditSheet.test.tsx` 6/6 + `SetlistGridTopBar.test.tsx` 4/4; grid suite **41 failed/150 passed** vs pre-change baseline **41 failed/140 passed** — +10 passing, ZERO new regressions (the 41 are the pre-existing dead-TanStack-table baseline, proven by a stash-revert comparison). /ui-ux-pro-max consulted during planning (SPECIAL-FLOWS.md gate + Daniel-explicit). Zero deviations. Daniel iPad UAT entry appended to `.paul/UAT-PENDING.md`. (2026-05-14)

**v7.0 Document-Driven Setlist Creation — Phase v70-05 ✅ COMPLETE 2026-05-14 (Wave 3 — Gemini structured extraction; single-plan phase / LOOP COMPLETE)**
- [x] v70-05 Gemini structured extraction — The AI structure pass for v7.0's "feed a doc, get a setlist" pipeline. **v70-05-01** added `src/lib/setlist-import/extract-structure.ts` — `extractSetlistStructure(text)` sends raw document text to Gemini via `geminiFlash()` (the chord-detection model setup, `gemini-3-flash-preview`, reused unchanged), strips markdown ```json fences, `JSON.parse`s, and validates against `SetlistStructureSchema` (Zod) — a `{ sections: {name,order}[], tracks: {title, key?, vocalLead?, sectionName?, referenceLink?, notes?, order}[] }` shape. Returns a discriminated `StructureResult` (`{ ok: true, sections, tracks }` | `{ ok: false, reason: 'malformed'|'empty'|'gemini_error', message, raw? }`) and NEVER throws — empty input, non-JSON output, schema-fail, and Gemini/API errors are all typed results; `malformed`/`empty` carry the raw model output so a human can review what Gemini produced. Optional fields use Zod `.nullish()` defensively (Gemini emits `null` despite "omit" instructions), normalized to absent keys after parse. Behind it, `POST /api/setlists/import/extract-structure` — a JSON route (`createApiHandler`, default auth, `checkRateLimit('upload')`, `maxDuration=60`) that is a sibling of `import/extract-document` + `import/parse` + `import/execute`; maps `gemini_error` → 502, `malformed`/`empty` → 422 `{ error, raw }`, success → 200 `{ success, sections, tracks }`. **Backend slice:** NO UI (the ImporterModal "Document" option is v70-07), NO library/recording resolution (v70-06), NO persistence. No new dependencies (`@google/generative-ai` + `zod` already present). **Verification:** `next build` ✓ (`ƒ /api/setlists/import/extract-structure` registered); `extract-structure.test.ts` 9/9 (well-formed / fenced / null-fields / non-JSON / schema-fail / empty-input / gemini-reject / flash-throw / never-throws — Gemini mocked via `vi.mock('@/lib/gemini')` + hoisted state); full `src/lib/setlist-import/` suite 18/18 (no regressions). HFG counter held at 0/3 (no engine / Firestore-data-layer touches). /ui-ux-pro-max gate N/A (backend-only slice). The doc → text → structure chain (`extract-document` → `extract-structure`) is now in place; **v70-06 consumes the `tracks` output** for library + recording matching. (2026-05-14)

**v7.0 Document-Driven Setlist Creation — Phase v70-04 ✅ COMPLETE 2026-05-14 (Wave 2 — doc upload + text extraction; single-plan phase / LOOP COMPLETE)**
- [x] v70-04 Doc upload + text extraction — The document text-extraction foundation for v7.0's "feed a doc, get a setlist" pipeline. **v70-04-01** added `src/lib/setlist-import/extract-document.ts` — `detectDocumentFormat` (mimeType-first, extension-fallback) + `extractDocumentText(buffer, opts)` dispatching `.docx` → `mammoth.extractRawText`, `.pdf` → the shared server-side pdfjs loader (`getPdfjs`, now exported from `pdf-chord-extractor.ts` — legacy build, worker disabled), `.txt` → UTF-8; returns a discriminated `ExtractResult` (`{ ok: true, text, format, charCount }` | `{ ok: false, reason: 'unsupported_format'|'extraction_failed'|'empty' }`) and NEVER throws. Behind it, `POST /api/setlists/import/extract-document` — a multipart route (25MB cap, 400/422 error mapping) that is a sibling of the existing `import/parse` + `import/execute` routes. **Foundation slice (Daniel-approved scoping):** NO UI, NO Gemini, NO persistence — the doc-import UI extends the existing `ImporterModal` (the live "Import Setlist AI" modal) in v70-05/v70-07, NOT a CreationWizard option as the ROADMAP text originally said. One new dependency: `mammoth@^1.12.0`. **Verification:** `next build` ✓ (route registered); `extract-document.test.ts` 9/9 (format detection, real `.docx` via jszip-built fixture, `.pdf` via mocked pdfjs loader — real pdfjs can't run under jsdom, the sibling `pdf-chord-extractor.test.ts` mocks it identically; unsupported/empty/corrupt all clean no-throw); `pdf-chord-extractor.test.ts` 16/16 (regression check on the one-line `export` change). HFG counter held at 0/3 (no engine / Firestore-data-layer touches). /ui-ux-pro-max gate N/A (backend-only foundation slice). **Unblocks v70-05** — Gemini structured extraction consumes this route's output text. (2026-05-14)

**v7.0 Document-Driven Setlist Creation — Phase v70-03 ✅ COMPLETE 2026-05-14 (Wave 2 — per-track media affordances; 2 plans / both LOOP COMPLETE)**
- [x] v70-03 Per-track media affordances — The setlist row card (`MobileRowCard`) now exposes both bound media types. **v70-03-01** (chart click-through): the chart indicator becomes an `<a target="_blank">` to the chart serving URL when a chart is bound — reuses the existing `/api/drive/file/[fileId]` + `/api/library/file/[id]` routes (no new route, no rules change); `onClick` `stopPropagation` isolates the link from the card's tap-to-edit; unbound rows keep the plain non-interactive icon. **v70-03-02** (recording-bind UI): a new `AudioLines` affordance beside the chart icon opens `RecordingBindPopover` — lists a song's reference recordings newest-first (subscribed via the v70-02 `songId+createdAt` index), plays each inline via native `<audio preload="none">`, and (for band-leaders/admins) uploads new ones. Backed by two new App-Router routes — `POST /api/recordings/upload` (band-leader/admin gated, audio-mime + 25MB validation, writes Storage + `recordings/{id}` doc) and `GET /api/recordings/file/[id]` (admin-side audio serving, mirrors `/api/drive/file`) — plus `recordings-client.ts` (subscribe/upload/playback-URL) and `uploadRecordingToStorage`/`downloadFromStoragePath` helpers in `firebase-storage.ts`. **Key finding:** `storage.rules` has no `recordings/` block and the client never imports `firebase/storage` — so recording playback flows through the admin-side serving route (the route IS the access control), needing no `storage.rules` change. **Re-spec deviation:** v70-03-01's original PLAN (and v70-03-02's) targeted `SetlistGrid`'s TanStack `columns`/`ChartCell`/`ChartBindPopover` — all dead code (`SortableRow` has zero call sites post-0ec6773c; `MobileCardList → MobileRowCard` is the sole live render path). Caught at Task 1 QUALIFY on v70-03-01, classified as a Spec issue, both plans re-spec'd in place to target `MobileRowCard`; the dead `SetlistGrid` table block remains as flagged tech debt for a future cleanup phase. **Verification:** `next build` ✓ across all tasks; `MobileRowCard.test.tsx` + `RecordingBindPopover.test.tsx` + `MobileCardList.test.tsx` green (live-path grid files; the ~41 dead-table grid-test failures are the pre-existing 52-failure baseline). HFG counter held at 0/3 (no engine / Firestore-data-layer touches — v70-02 already shipped + emulator-tested the recordings rules). /ui-ux-pro-max BLOCKING gate satisfied on both plans. **New standing pattern:** `checkpoint:human-verify` tasks no longer block APPLY — they accumulate in `.paul/UAT-PENDING.md` and Daniel verifies the whole list against the deployed build at milestone end (formalizes the v51-04 PENDING-UAT pattern; saved as `feedback-uat-checklist` memory). **Carry-forwards:** v70-03-01 + v70-03-02 human-verify checklists in `.paul/UAT-PENDING.md`. (2026-05-14)

**v7.0 Document-Driven Setlist Creation — Phase v70-02 ✅ COMPLETE 2026-05-14 (Wave 1 foundation — recordings data model; single-plan phase / LOOP COMPLETE)**
- [x] v70-02 Recordings data model — The v7.0 recordings domain foundation, deployed to production: NEW `recordings/{id}` Firestore collection. **v70-02-01** added the `Recording` TypeScript interface to `models.ts` (`id`, `songId?` optional FK to `songs/{id}`, `title`, `fileName?`, `mimeType?`, `storagePath`, `durationSeconds?`, `notes?` free-form attribution, `createdAt`, `createdBy`); an exported `getRecordingStoragePath(recordingId, ext)` helper in `firebase-storage.ts` establishing the `recordings/{id}.{ext}` Storage convention (own top-level prefix, parallel to `library/{fileId}`); a `match /recordings/{recordingId}` Firestore rules block mirroring `songs/{songId}` (read `isMember`, write band-leader/admin — recordings are band-internal, NOT public-perform-view content); and a `recordings: songId ASC, createdAt DESC` composite index for the recording-by-song chronological query. Rules + index deployed via `firebase deploy --project crcmusiccharts` (rules compiled + released, index accepted). **Emulator-backed rules coverage:** new `firestore-rules-recordings.emulator.test.ts` (10-scenario read × write × auth-context matrix) — `npm run test:emulator` 10/10 green. **HFG counter held at 0/3** — real-Firestore emulator coverage, no clause-(b) waiver. `next build` ✓; main suite 1650/52 (52-failure baseline held, zero new regressions). Boundary-clean additive slice — zero existing code paths touched. **Scope discipline:** NO upload route, NO UI, NO playback, NO `recordings` lib helper — those land in v70-03 (recording-bind UI) / v70-06 (recording-match) by design, against this model. (2026-05-14)

**v7.0 Document-Driven Setlist Creation — Phase v70-01 ✅ COMPLETE 2026-05-14 (Wave 0 foundation — image-chart support; 2 plans / both LOOP COMPLETE)**
- [x] v70-01 Image-chart support — Image charts (PNG/JPEG/HEIC) are now a first-class chart type, end-to-end: **v70-01-01** (upload + view + toolbar) extended `/api/library/upload` ALLOWED_TYPES (HEIC server-converted to JPEG via `heic-convert`), added `ImageScoreViewer` wired into PDFOverlay's type-branch, disabled the transposer + AI-chord controls for image charts with an explanatory tooltip, and three-layer type detection (mimeType → extension → useLibraryStore live-backstop) covering picker-bound + legacy tracks. **v70-01-02** (print embed) replaced the print-pipeline image SKIP guard with `embedImageTrack()` — each image chart embeds as its own letter-size page (18pt margin per /ui-ux-pro-max, aspect-preserved `scaleToFit`, centered, no caption); the `/api/setlist/print/personal` mapper now propagates `fileName`+`mimeType` so personal packets embed images too; the obsolete PrintModal deferral banner was removed; `computeContentHash` `cacheVersion` bumped 2→3 so stale skip-era cached PDFs don't serve. **Enterprise-audit-hardened** (v70-01-02 ran `/paul:audit`, 3 must-have + 4 strongly-recommended upgrades applied before APPLY): `embedImageTrack` wraps fetch+decode+embed in one try/catch — `embedJpg`/`embedPng` decode throws return `false` with a structured `logger.warn` (observable, never a silent drop); a corrupt/missing/oversized image degrades gracefully while the rest of the packet prints; image bytes capped at 25MB by the upload route's `MAX_FILE_SIZE`. **Verification:** `next build` ✓ Compiled successfully; print-pipeline 26/26 + print-modal 23/23; full suite 1649 pass / 52 fail (52-failure baseline matches v60-12 exactly — zero new regressions, +5 passing tests). HFG counter held at 0/3 (no engine / Firestore-data-layer touches; pdf-lib embed is pure). /ui-ux-pro-max BLOCKING gate satisfied on both plans. **Unblocks v70-05** — the May 15 Shir Shabbat canary's `dodi li (sher).png` is now uploadable, viewable, AND printable. **Carry-forwards:** EXIF orientation (`embedJpg` ignores EXIF — phone photos may embed rotated; routes to a follow-up if Daniel reports it); low-res upscaling softness (accepted trade-off for stage legibility); shadcn Tooltip debt inherited from v70-01-01; AC-3/AC-4 (v70-01-01) + the v70-01-02 print human-verify checkpoint are PENDING-UAT carry-forward against deployed commits per v51-04 codified pattern (8th use this milestone). (2026-05-14)

**🎉 v6.0 — Tracks Single-Source-of-Truth ✅ COMPLETE 2026-05-13** *(closed via /paul:complete-milestone with PENDING-UAT marker per v51-04 codified pattern; 5th consecutive; 12 phases / 24 plans / 25 commits / ~2 days)* — Top-level `tracks/{id}` is now the SOLE source for "the live track list" across the entire codebase. Every reader routes through one of three helpers (`getTracksForSetlist` server / `getTracksForSetlistClient` client / `fetchTracksForSetlistClient` Web-SDK direct) — no more embedded-array fallback, no more half-migrated dual-source state, no more dominant regression class. **12 phases:** Wave 1 orthogonal UX (v60-01 conflict-pill silent LWW / v60-02 pagehide blur protection). Wave 2 HFG reset (v60-03 Java + emulator canary; counter 1/3 → 0/3 held through close). Wave 3 migration spine (v60-04 server-readers / v60-05 editor+perf-readers / v60-06 dashboard+backfill 8 plans / v60-07 writer-removal 4 plans / v60-08 cleanup with 22-consumer-site Daniel-approved mid-APPLY expansion). Wave 4 v5.4 fold-forwards (v60-09 cross-device library sync via subscribeSongsLibrary continuous listener / v60-10 Mobile AddBar coarse-pointer sticky-bottom). Wave 5 emergent v60-11 shortcut-aware mirror (131-doc backfill: songs 364 → 495; "Lechu Goldman" surfaces). Wave 6 emergent v60-12 public tracks visibility (firestore.rules deployed + @firebase/rules-unit-testing dev dep + 8-scenario rules emulator test). **Patterns established:** pre-APPLY manual architectural audit (since /paul:audit broken in this repo per feedback_no_paul_audit; caught 5 spec issues on v60-11 + 5 concerns on v60-12 before code touched) / emulator-backed coverage as HFG-preserving alternative to clause-(b) waivers / parallel batch pairs via Promise.allSettled / status-field ownership boundary / sibling-listener resilience contract / spec-issue diagnostic at mid-APPLY when estimates off by 10x / emergent close-gate phases inside milestone close path / Firebase CLI as automated task (NOT human-action checkpoint per feedback_firebase_cli). Full milestone entry: `.paul/MILESTONES.md § v6.0`; archive: `.paul/milestones/v6.0-ROADMAP.md`. PENDING-UAT carry-forwards remain (AC-4 v60-11 picker / AC-4 v60-12 incognito-perform / v60-09 two-device / v60-10 iPad sticky / Issue 2 setlist-missing cascade / v60-01..v60-08 accumulated smokes) against worship cycle Fri PM + Sat AM. Master HEAD at close: `04499a4` feat(v60-12-01). (2026-05-13)

**v6.0 Tracks Single-Source-of-Truth — Phase v60-12 ✅ LOOP COMPLETE + DEPLOYED 2026-05-13 (single-plan emergent close-gate phase — public tracks visibility)**
- [x] v60-12 Public tracks visibility — Single plan v60-12-01 closes Daniel-reported UAT-class bug: incognito-browser visitors to centralreform.live could click "Perform" on the upcoming setlist but the perform view rendered "No tracks yet" because `tracks/{trackId}` required `isMember()` to read AND `useSetlistPerformance` skipped the snapshot listener for unauthenticated users (citing a stale comment that lied about the page rendering an error for public users — which it never did). **Mechanism:** `firestore.rules` `match /tracks/{trackId}` opened to `allow read: if true` (writes still band-leader/admin-gated; songs/{id} STAYS member-only since public perform view doesn't subscribe to songs/*); `src/hooks/use-setlist-performance.ts` effect guard simplified from `if (!setlistId || !user) return` → `if (!setlistId) return` + stale comment rewritten to cite v60-12-01 contract; `@firebase/rules-unit-testing@^5.0.1` dev dep added + new `src/lib/songs/__tests__/firestore-rules-tracks.emulator.test.ts` (FIRST emulator-backed rules test in the project — reusable 8-scenario template covering read × write × auth-context matrix); existing hook test reversed from "does NOT mount for public" → "DOES mount for public" with contract-reversal comment block. **Decisions:** Option A simple public-read over Option B parent-doc visibility check (setlists already publicly readable at firestore.rules:86; tracks are the natural extension; Option B would require adding `public: true` flag with backfill + dashboard logic — out of scope). Songs/{id} rule STAYS member-only (confirmed via snapshot-listener.ts inspection — only subscribes to setlists + tracks; subscribeSongsLibrary lives in editor-side SetlistGridHydrator that public users don't hit). Add `@firebase/rules-unit-testing` dev dep rather than skip rules testing (rules edits are high-blast-radius; emulator safety net non-negotiable). Effect deps trimmed `[setlistId, user, startSnapshotListener]` → `[setlistId, startSnapshotListener]` (listener doesn't care about auth identity once mounted). Reverse existing test rather than delete (preserves intent + documents contract change in test history). **Pre-APPLY architectural audit cleared 5 concerns** before any code touched: songs/{id} truly not needed (confirmed); PDFOverlay path uses `/api/drive/file/[fileId]` with `hasBrowserFetchMetadata` browser-fetch fallback (public users can load PDFs); v60-09 archive flow uses admin SDK (no rule conflict); v60-03 emulator infra works; auth-claim staleness not a concern (only relaxing reads). **Verification:** 8/8 rules emulator test scenarios GREEN under firebase emulators:exec (Scenario A unauth single-doc read SUCCEEDS / B unauth query SUCCEEDS returning 3 docs / C-E unauth writes REJECTED / F member-only write REJECTED / G band-leader write SUCCEEDS / H admin write SUCCEEDS). tsc EXIT=0; next build ✓ Compiled in 11.5s; vitest 1636/52 (52-failure baseline matches v60-11 exactly). `firebase deploy --only firestore:rules --project crcmusiccharts` COMPLETE; rules compiled clean and released to cloud.firestore. HFG counter held at 0/3 via the new rules emulator test. /ui-ux-pro-max gate N/A (data-layer + auth-rules only). **Patterns established:** Firestore rules emulator testing via @firebase/rules-unit-testing (readFileSync the production firestore.rules; 8-scenario read × write × auth-context matrix template); public-read-by-default for entities that are derived projections of already-public parents (setlists public → tracks/* extension); contract-reversal test rewrites cite the prior contract + reason for change in a comment block. **AC-4 PENDING-UAT** for Daniel-loop incognito perform smoke post-Vercel-deploy of `04499a4`. (2026-05-13)

**v6.0 Tracks Single-Source-of-Truth — Phase v60-11 ✅ LOOP COMPLETE 2026-05-13 (single-plan phase — shortcut-aware songs mirror + subscribe.ts self-heal; v6.0 Wave 5 milestone-close-gate done; 11 of 11 phases LOOP COMPLETE at this point)**
- [x] v60-11 Shortcut-aware songs mirror + subscribe.ts self-heal — Single plan v60-11-01 closes the 134-doc library_index/songs gap that v60-09 surfaced via UAT Issue 1 ("Lechu Goldman" and ~133 similar Drive-shortcut songs invisible in chart-binder picker). **Mechanism:** new helper `buildSongsMirrorPayload` in `src/lib/sync-engine.ts` builds the songs/* payload `{ id, title, normalizedTitle, fileId, createdAt? }` with title sourced VERBATIM from `library_index.name` (no `.pdf` strip — matches bootstrap-songs.ts:142 pattern so the 364 existing bootstrap docs and the new mirrored docs share shape); cron-sync's chunk loop now commits a parallel `songsBatch` alongside the existing library_index batch via `Promise.allSettled([batch.commit(), songsBatch.commit()])` — songs failure is non-fatal (logged warn), library_index commit independent. NO `status` field on the cron mirror payload — status is owned by `/api/library/archive` (the sole writer); `.set({ merge: true })` preserves existing status on every tick, so the hourly cron cannot clobber user-driven archive flips. Single mirror site at line 200 batch.set only — lines 249/267 (storageCopiedAt / storageFailed) are storage metadata, do NOT trigger mirror. Empty-name files skip the songs/* write but still hit library_index (matches subscribe.ts:85 empty-title guard). New `scripts/backfill-shortcuts-songs.ts` (234 LOC) does append-only one-off seeding of the historical 134 missing docs: reads both collections, computes `missing = libDocs.filter(lib => !songsIds.has(lib.id))`, writes only missing entries with the same buildSongsMirrorPayload-shape payload; idempotent via dedicated marker `system/v60-11-backfill` (distinct from v54SongsBootstrap); --dry-run / --apply / --force / --help flags. Folded in latent Issue 3: `src/lib/songs/subscribe.ts:101-103` onError handler gains a `recoverFromFirestoreShutdown(err)` call (bare, no `void` — matches 5 sibling listeners alert-store / firestore-monitor-client / use-monitor-connection / use-safe-firestore-sync / notification-store); after a service-worker deploy, the songs listener now auto-recovers from "Firestore shutting down" instead of silently dying. Uncommitted production diagnostic `scripts/diag-lechu-goldman.ts` relocated to `scripts/diag/diag-lechu-goldman.ts` with a header note recording the 134-doc gap measurement (498 library_index vs 364 songs/*). **Decisions:** Drop MIME filter at songs/* mirror site (Option 2 from 2026-05-13 handoff) rather than resolve-shortcuts-at-sync (Option 1) or hybrid (Option 3) — smallest surface; v60-09's `status !== 'archived'` Dexie filter is now the picker gate; Storage already resolves shortcut PDFs via the `storageCopiedAt` step (2026-05-12). Cron mirrors title-only (no status) per status-field ownership boundary — archive route is sole writer. Title verbatim (no `.pdf` strip) to match the 364 bootstrap-written docs. Single mirror site (line 200 only) — storage metadata updates don't change name. Parallel songsBatch + Promise.allSettled — songs failure non-fatal. Combined single-commit per phase at transition (per memory feedback_paul_phase_commits). Backfill scope is "all missing" but script named "backfill-shortcuts-songs" to preserve Daniel's UAT framing (~41 shortcuts + ~93 non-charts of various MIME). **Pre-APPLY architectural audit caught 5 spec issues** before any code touched: A1 BLOCKING (title strip would cause picker-shape inconsistency between existing PDF docs "Adon Olam.pdf" and new shortcut docs "Lechu Goldman"); A2 BLOCKING (cron writing `status: 'active'` every hour would clobber user-driven archives — silent v60-09 regression); B1 MEDIUM (per-doc Promise.allSettled inside `db.batch()` is incompatible — need parallel songsBatch instead); B2 MEDIUM (script-name vs actual-scope divergence — "shortcuts" name but contents mixed); C2 LOW (unnecessary `void` prefix on recoverFromFirestoreShutdown call — function is sync void-return). All 5 patched INTO PLAN.md before APPLY started; no APPLY-time auto-fixes or DRIFT/GAP at qualify. Per feedback_no_paul_audit memory: `/paul:audit` exists in skill registry + config.md says enabled but doesn't actually work in this repo; performed manual architectural audit inline against source files. **Verification:** tsc EXIT=0; next build ✓ Compiled successfully in 8.6s; vitest 1636 pass / 52 fail / 40 skip (+21 passing vs v60-09 baseline 1615/52 incl. 2 new subscribe-error tests + 7 new sync-engine-songs-mirror tests + 12 unrelated drift; 52-failure baseline matches v60-09 EXACTLY). subscribe-error.test.ts unit-level coverage with adapter test-seam: 2/2 GREEN across Error / code-object / string / null error shapes; listener handle preserved post-error. sync-engine-songs-mirror.test.ts 7-scenario emulator-adjacent: 7/7 GREEN — Scenario 1 shortcut mirror with verbatim name; Scenario 2 PDF with extension preserved; Scenario 3 modified file omits createdAt (merge preserves prior); Scenario 4 empty-name skips songs/* but writes library_index; Scenario 5 folder/audio/doc all mirror (no MIME filter); Scenario 6 separate batch IDs prove parallel commit; Scenario 7 contract assertion — NO songs payload across ANY scenario carries a status field. **HFG counter held at 0/3** via emulator-adjacent coverage of both new code paths — no clause-(b) waiver. **LOC delta:** ~+59 LOC production (subscribe.ts +2 + sync-engine.ts +57) + ~+324 LOC test + 234 LOC backfill script. /ui-ux-pro-max gate N/A (data-layer only); SPECIAL-FLOWS skill audit PASS. **Patterns established:** parallel batch pair (library_index + songs) committed via Promise.allSettled — non-fatal songs failure; status field ownership boundary (archive route is sole writer; other writers respect via merge:true with no status field in their payload); sibling-listener resilience contract (every Firestore onSnapshot error handler calls recoverFromFirestoreShutdown to recover after SW deploys); pre-APPLY manual architectural audit pattern (read PLAN + cross-check against source files + classify findings as BLOCKING/MEDIUM/LOW + patch PLAN before APPLY) — proven again to catch spec issues that would otherwise surface as APPLY-time defects or production regressions. **PENDING-UAT signals to watch:** AC-3 — Daniel runs `npx tsx scripts/backfill-shortcuts-songs.ts --dry-run` then `--apply` against production .env.local credentials, expecting gap count ~134 ± a few + sample IDs including `1jgs72zwhfEvqsqeeCFMw8Th7Zsk0mVJj` "Lechu Goldman"; AC-4 — Daniel opens picker, types "Lechu Goldman", song appears within ~1s, binds to chart, /perform/{id} renders the resolved PDF; verify on 2+ other previously-missing shortcut songs. **Scope NOT in v60-11:** Issue 2 (setlist-missing cascade after deploys) — separate phase pending Daniel clearing site data + reporting persistence. Title-shape divergence reconciliation across writer surfaces (upload route strips extensions, rename trims, cron/bootstrap verbatim) — pre-existing v60-09 inconsistency, out of scope here. Delete-mirror (Drive file deleted → songs/* doc deleted) — phase isn't claiming delete-sync. (2026-05-13)

**v6.0 Tracks Single-Source-of-Truth (in progress) — Phase v60-10 ✅ LOOP COMPLETE 2026-05-13 (single-plan phase — Mobile AddBar variant; v6.0 Wave 4 closed from this session's perspective)**
- [x] v60-10 Mobile AddBar variant — Single plan v60-10-01 closes the third and last v5.4 fold-forward into v6.0 (after v60-03 emulator canary + v60-09 cross-device library sync — parallel session). Resolves v53-03 deferred CONTEXT Q1 ~11 days after the original deferral. **Mechanism:** AddBar.tsx outer wrapper gains coarse-pointer-only sticky-bottom positioning via Tailwind arbitrary-variant CSS-driven classes `[@media(pointer:coarse)]:fixed bottom-0 left-0 right-0 z-40 bg-background pb-[env(safe-area-inset-bottom)] shadow-[0_-4px_12px_rgba(0,0,0,0.18)]` — chosen over JS-driven `isCoarse && 'fixed'` to eliminate the first-paint flash on iPad (Daniel's primary surface; useEffect runs after paint). New `useVirtualKeyboardOpen()` hook (35 LOC) listens on `window.visualViewport.resize` with SSR + JSDOM feature-detect guards + 150px threshold (tolerates browser-chrome reveal/hide ~80px, catches real iOS Safari keyboard ≥250-320px). `hideForKeyboard = isCoarse && keyboardOpen` toggles Tailwind `hidden` (display:none) — removes bar from a11y tree when keyboard pops so it cannot steal focus behind the keyboard or overlap an inline cell editor. v53-03 split-button + 5-tile popover shape preserved byte-for-byte; all 7 onContextMenu preventDefault disambiguation surfaces untouched. **Decisions:** Option A (`position: fixed`) locked at Task 2 checkpoint:decision after /ui-ux-pro-max consultation — rejected Option B (sticky-in-scroll-container — requires SetlistGrid restructure into height-bounded overflow shell, high regression risk against drag-and-drop autoscroll + modal positioning + EmptyState centering) and Option C (FAB — abandons v53-03 split-button shape, violates Track A old-AddBar precedent + Daniel muscle memory, increases tap-to-add cost). z-40 per html-tailwind stack guideline + ui-ux-pro-max Z-Index Management HIGH rule (below Radix Dialog z-50 so ChartBindDialog overlays cleanly). `env(safe-area-inset-bottom)` for iOS home-indicator clearance on phones. SetlistGrid.tsx **unmodified** — existing `pb-32` (128px) on row-list container line 1625 is the "measured equivalent" per plan's explicit "or measured equivalent" authorization clause; adding the literal `[@media(pointer:coarse)]:pb-20` would have shrunk the coarse-pointer spacer to 80px via Tailwind selector specificity (DRIFT documented). **Verification:** vitest 24/24 on touched files (12 v53-03 baseline preserved + 5 new v60-10 variant + 7 new hook); tsc EXIT=0; next build ✓ Compiled successfully in 15.0s; HFG counter 0/3 held throughout (no engine/cells/sync/library touch); zero file overlap with v60-09 parallel session (v60-09 owns AddRowPlaceholder.tsx + ChartBindPopover.tsx + library routes + new subscribe.ts; v60-10 owns AddBar.tsx + new useVirtualKeyboardOpen hook). **Patterns established:** CSS-driven media-query positioning + JS-driven visibility composition (static layout via CSS, dynamic visibility via JS hooks); visualViewport-based virtual-keyboard detection (SSR + JSDOM guarded, 150px threshold); hide-with-display:none over animate-out-of-view when user attention is on the keyboard (no motion needed; bonus a11y benefit); `env(safe-area-inset-bottom)` reflexive pairing with `position: fixed bottom-0`; "measured equivalent" authorization in plan text → re-measure existing layout before adding to avoid regressions like Tailwind selector specificity inversions. **AC-6 PENDING-UAT carry-forward** for Daniel-loop iPad Safari (landscape) + iPhone Safari (portrait) + desktop Chrome trackpad browser-smoke against deployed commit per v51-04 codified pattern + Daniel-memory "never use local dev server — always push to production on Vercel". /ui-ux-pro-max gate satisfied (3 focused domain searches: ux sticky-bottom + html-tailwind safe-area + ux animation/reduced-motion); SPECIAL-FLOWS skill audit PASS. (2026-05-13)

**v6.0 Tracks Single-Source-of-Truth (in progress) — Phase v60-09 ✅ LOOP COMPLETE 2026-05-13 (single-plan phase — cross-device library sync; second v5.4 fold-forward closed)**
- [x] v60-09 Cross-device library sync — Single plan v60-09-01 closes the v54-03 fold-forward gap: `library_index/{fileId}` and `songs/{fileId}` now stay continuously consistent across devices, so a rename / archive / upload from Daniel's iPad propagates to the picker on his Mac without a manual reload. **Mechanism:** new `src/lib/songs/subscribe.ts` (`subscribeSongsLibrary`) wraps `onSnapshot(collection(db, 'songs'))` and mirrors docChanges to Dexie via direct `db.songs.put`/`db.songs.delete` (NOT applyEdit — server-authoritative reads must not enqueue circular outbox writes). SetlistGridHydrator flips its single-mount anchor from `primeSongsLibrary()` one-shot to `subscribeSongsLibrary()` continuous; `songsSubscribeRef` sentinel prevents double-subscribe across re-mounts; effect cleanup unsubscribes. ChartBindDialog's per-open re-prime + REPRIME_MIN_INTERVAL_MS deleted (continuous listener supersedes the throttled refresh). Write-side parity: rename / archive / upload routes now mirror title / normalizedTitle / status to `songs/{fileId}` via parallel `Promise.allSettled` writes (songs failure logs warn but does NOT fail the user-facing API response; self-heals via next snapshot tick). `setlistSchema` was untouched (v60-08 closure); only `LocalSong.status?: 'active' \| 'archived'` field added to types. Picker filter `status !== 'archived'` applied at the Dexie query layer in ChartBindPopover + AddRowPlaceholder useLiveQuery — keeps the predicate inside the IndexedDB worker; archived songs hidden from both Library AND Recent groups uniformly; missing status reads as active for backward compat with the 364 v54-01-01 bootstrap docs (no backfill migration required). **Decisions:** Filter at Dexie query layer (NOT JSX render) per /ui-ux-pro-max consult — smaller rendered list = faster cmdk filter on iPad / mid-session archive cascades through useLiveQuery cleanly. Hide archived from BOTH Library and Recent per Daniel's mental model "archived = gone from picker entirely" (sticky memory via recent[] + defaults survives in the songs doc, restored on un-archive). Silent removal on cross-device archive (no greyed-out '(archived)' affordance) — matches Library archive metaphor. Admin-SDK `onSnapshot` in the emulator test (NOT firebase/firestore client SDK) — jsdom lacks streaming APIs the client SDK needs; admin SDK provides identical Firestore snapshot semantics via the same emulator; SubscribeAdapter abstraction makes this an implementation detail. Promise.allSettled (not Promise.all) for parallel library_index + songs writes — songs mirror failure is non-fatal. Kept primeSongsLibrary export deprecated-but-functional — saves rewriting the v53-02-01 test suite. **Verification:** tsc EXIT=0; next build ✓ Compiled successfully in 12.3s; vitest 1615 pass / 52 fail / 40 skip (+3 new mirror tests vs v60-08 baseline 1612/52; failure count matches exactly); emulator suite 5/5 GREEN (subscribe.emulator.test.ts: initial / modify / archive / delete / unsubscribe). **HFG counter held at 0/3** via real-Firestore emulator coverage — no clause-(b) waiver consumed (second engine-adjacent phase post-v60-03 reset to ship emulator-backed coverage instead of a waiver). **LOC delta:** ~+170 LOC production (subscribe.ts new file ≈110 LOC + route/component edits ≈60 LOC); AC-8 LOC DRIFT documented and approved-in-plan. /ui-ux-pro-max gate satisfied (Task 1 entry consult: 3 verdicts on filter placement / Recent group treatment / mid-session disappearance). **Patterns established:** snapshot-listener helper with provider-agnostic DocChange[] shape (adapter swap allows admin/client SDK interchange in tests vs production); single-mount anchor pattern (SetlistGridHydrator owns one boot for the songs subscription, ref-guarded against re-mount churn); real-Firestore emulator coverage as HFG-preserving alternative to clause-(b) waivers for engine-adjacent listener phases. **PENDING-UAT signals to watch:** two-device live propagation (Mac↔iPad rename / archive / upload within ~1s, no reload); archived songs disappear from picker on both devices; un-archive restores; sticky memory (Recent group entries) survives the archive cycle. **Scope NOT in v60-09:** cron Drive→library_index sync route — if a song lands via the nightly poll (not the upload route), the songs/* mirror won't exist until somebody touches it via rename/archive/upload. Acceptable for Daniel's upload-driven workflow; if it surfaces during UAT, follow-up plan adds songs/* mirroring to the cron route. **Parallel-session coordination:** v60-10 (Mobile AddBar) shipped concurrently in another Claude session; zero file overlap by plan-time boundary contract; sequential commits via Option 1 (v60-10 committed first with PROJECT.md + its own bundle; v60-09 rebased + committed second with STATE.md + ROADMAP.md capturing both sessions' metadata). (2026-05-13)

**v6.0 Tracks Single-Source-of-Truth (in progress) — Phase v60-08 ✅ LOOP COMPLETE 2026-05-13 (single-plan phase — migration cleanup; Wave 3 fully closed)**
- [x] v60-08 Migration cleanup — Single plan v60-08-01 closes the v50-05 → v6.0 tracks migration story end-to-end. **Mechanism:** dropped the embedded-tracks fallback from both `getTracksForSetlist` (server-tracks.ts) and `getTracksForSetlistClient` + `fetchTracksForSetlistClient` (client-tracks.ts) — readers now always query top-level `tracks/{id}`. Dropped `tracks: z.array(setlistTrackSchema)` from `setlistSchema` AND `tracks: SetlistTrack[]` from the hand-defined `Setlist` interface, propagating breakage to **22 consumer sites across 10 production files** which were migrated to the v60-06 denorms (`trackCount`, `fileIds`, `songCount`). `setlist-firebase.ts` `duplicateSetlist`/`cloneSetlist`/`saveAsTemplate` refactored to fetch source tracks via `fetchTracksForSetlistClient` before cloning. `matrix/route.ts` widened to a local `SetlistWithTracks = Setlist & { tracks: ... }` type. Track-title search predicate in `useSetlistDashboard` deleted (re-introducible via the v60-06-04 Dexie-bulk pattern if needed). **Backfill execution:** Claude-invoked `npx tsx scripts/backfill-tracks-v60.ts --apply` against production `crcmusiccharts` post-checkpoint-clearance — 5 migrated / 5 skip-hydrated / 5 skip-empty / 0 errors; idempotency marker `system/v60TracksBackfill` set; rollback snapshots written. **Decisions:** Daniel-approved mid-APPLY spec-issue expansion (option 2: migrate all 22 consumer sites in this commit rather than defer to v60-08-02) when the plan's "≤2 consumer sites" estimate proved off by an order of magnitude; drop the `Setlist` interface field (not just the schema) to force end-to-end type-level migration; keep `_setlistData` param on client helpers for ABI stability (callers continue to pass it; void-cast inside). **Verification:** tsc EXIT=0; next build `Compiled successfully in 6.8s`; vitest 1612 pass / 52 fail / 40 skip — failure count matches v60-07 baseline exactly (-4 passing from deliberate test removal: 3 unhydrated-fallback cases + 1 track-title-search test for dropped behavior). **LOC delta:** production -80 net (111 ins / 191 del across 17 files); total -110 net (229 ins / 339 del across 26 files). Cleanup plan — net-negative LOC is the success signal. AC-6 LOC ceiling "DRIFT" documented and Daniel-approved at checkpoint. /ui-ux-pro-max gate N/A (data-layer only); SPECIAL-FLOWS skill audit PASS. **Patterns established:** helper-fetch indirection for clone/duplicate/save-template paths (rather than caller-passes-tracks signature); ABI-stable param retention pattern (`_setlistData` void-cast); spec-issue diagnostic at mid-APPLY (pause, present scope reality, let user choose expand vs defer). **PENDING-UAT signals to watch:** publish/print/email/perf-view still render tracks on active setlists (post-backfill, hydrated:true); pre-backfill archived setlists (pre-2026-03-28) now intentionally read empty per Daniel's 15-setlist scope decision; duplicate/clone/save-as-template flows hit one extra Firestore fetch latency-wise. **v60-08 phase mandate COMPLETE.** Wave 3 (v60-04..v60-08) fully closed. **v60-09 (cross-device library sync) + v60-10 (Mobile AddBar) — Wave 4 parallel candidates — UNBLOCKED.** Bundled commit + push paths include the entire `.paul/phases/v60-08-migration-cleanup/` dir per feedback_paul_phase_commits. (2026-05-13)

**v6.0 Tracks Single-Source-of-Truth (in progress) — Phase v60-07 ✅ LOOP COMPLETE 2026-05-13 (embedded-array writer removal + immediate strip)**
- [x] v60-07 already documented elsewhere in this section; see ROADMAP.md Phase Structure row for the full decision log.

**v6.0 Tracks Single-Source-of-Truth (in progress) — Phase v60-06 ✅ LOOP COMPLETE 2026-05-12 → 2026-05-13 (Wave 3 reader-migration spine fully delivered)**
- [x] v60-06 Dashboard reader migration + 15-setlist backfill — 8 plans across 2 days. **v60-06-01** HeroCard + CompactSetlistRow read `setlist.trackCount` with embedded fallback. **v60-06-02** Setlist type + reconciler extended with songCount + fileIds; cascade seed bundles 4 fields atomically; 3 dashboard surfaces migrated (HeroCard fileIds, NextServiceCard songCount, PublicSetlistListing songCount). **v60-06-03** SetlistCards offline-cache + use-upcoming-prep migrated to denormalized fileIds/songCount. **v60-06-04** bulk Dexie hook `useDexieTracksForSetlists` + title-aware dashboard reader migration (UpcomingTimeline + PrepRecommendations + NextServiceCard). **v60-06-05** SetlistDrawer click-time public-setlist read migrated to one-shot Dexie + getTracksForSetlistClient. **v60-06-06** new `fetchTracksForSetlistClient` Web-SDK 2-branch direct-fetch helper + TemplatesSection admin Import-from-Setlist migration — completes the client reader inventory (Dexie-aware 3-branch + bulk Dexie subscription + Web-SDK direct-fetch). **v60-06-07** matrix endpoint (/api/setlists/matrix) migrated to getTracksForSetlist with Promise.all parallel fanout — completes the server reader spine (page.tsx publish + print/public + print/personal + email-packets + resend-email + matrix all now route through one helper). **v60-06-08** new `scripts/backfill-tracks-v60.ts` migration tool (apply/dry-run/rollback triad reusing migrate-v50.ts MigrationFirestore abstraction; 22-test fake-only suite, no firebase-admin imports; rollback collection `migration_snapshots/{setlistId}`; idempotency marker `system/v60TracksBackfill`); production dry-run captured 5 MIGRATE candidates (late Mar–Apr 2026; track counts 16-45) + 5 SKIP-HYDRATED + 5 SKIP-EMPTY. Suite 1591 → 1613 (+22 from backfill tests). HFG counter 0/3 held throughout. Aggregate v60-06: +263 LOC source + 49 hook + 205 test + 1041 LOC migration script & tests. Patterns established: 3-tier client reader inventory (Dexie-aware single + Dexie bulk + Web-SDK direct-fetch); bulk-snapshot mapper via `await Promise.all(snapshot.docs.map(async ...))` for per-doc helper invocation; spread-with-explicit-override narrowing variants documented across 3 contexts (Record array via `Record<string, unknown>[]` annotation / single object via `as unknown as T` / typed-record cast variant); migration script triad formalized across 3 production migrations (migrate-v50 / bootstrap-songs / backfill-tracks-v60). /ui-ux-pro-max gate satisfied for client-touching plans (v60-06-01..06); not required for server-side plans (v60-06-07/08). **v60-07 embedded-array writer strip UNBLOCKED** — every reader surface (server spine + client inventory + dashboard denorm + historical backfill tool) closed. PENDING-UAT carry: Daniel runs `--apply` for v60-06-08 during a safe Mon–Wed worship-cycle window. (2026-05-12 → 2026-05-13)

**v6.0 Tracks Single-Source-of-Truth (in progress) — Phase v60-05 ✅ LOOP COMPLETE 2026-05-12 (editor + perform-view reader migration)**
- [x] v60-05 Editor + perform-view reader migration — Single-plan phase (v60-05-01). Extracted new `getTracksForSetlistClient` 3-branch helper to `src/lib/client-tracks.ts` (hydrated+Dexie / hydrated+no-Dexie / unhydrated fallback) + 7 unit tests + `use-setlist-performance.ts` refactor to consume the helper. Editor side proved already Dexie-routed (no refactor needed beyond helper extraction). +21 LOC net; HFG 0/3 held; /ui-ux-pro-max gate satisfied. Deferred: matrix/route.ts → v60-06-07. (2026-05-12)

**v6.0 Tracks Single-Source-of-Truth (in progress) — Phase v60-04 ✅ LOOP COMPLETE 2026-05-12 (server-side reader migration; Wave 3 server spine complete)**
- [x] v60-04 Server-side reader migration — 3 plans. **v60-04-01** new `getTracksForSetlist(db, setlistId, setlistData)` Admin-SDK helper at `src/lib/server-tracks.ts` + page.tsx publish path migrated + emulator-backed coverage test. **v60-04-02** print/public/[id]/page.tsx + print/personal/[id]/page.tsx migrated. **v60-04-03** email-packets/route.ts + resend-email/route.ts migrated; latent songs[] type fix bundled. Aggregate +39 LOC net production across 7 files; 3 new emulator-backed tests; HFG counter 0/3 held; Wave 3 server spine complete; v60-07 writer strip path ready. (2026-05-12)

**v6.0 Tracks Single-Source-of-Truth (in progress) — Phase v60-03 ✅ LOOP COMPLETE 2026-05-12 (Wave 2 closed; Harness Fidelity Gate counter RESET 1/3 → 0/3; Wave 3 UNBLOCKED)**
- [x] v60-03 H-SL-7 emulator canary + HFG counter reset — Single-session PLAN→APPLY→UNIFY closing the Harness Fidelity Gate that has gated v6.0 since milestone open. Ships the v54-02-02 deferred-from-2026-05-08 H-SL-7 regression canary now that Java JDK 21 is locally available (`openjdk 21.0.11` via Microsoft OpenJDK, already installed). **Mechanism:** new `describe` block in `src/lib/sync/__tests__/engine.emulator.test.ts` (+245 LOC test only, no production code changed) boots a real `SyncEngine` + a test-only `EmulatorAdapter` (firebase-admin under the hood, mirroring `ProductionFirestoreAdapter`'s set/update/delete contract with T1.3 precondition semantics) + `CrossTabLock` (with `FakeChannelHub`) + `Dexie` (fake-indexeddb). Seeds `setlists/{id}` with `serverTimestamp`; enqueues two rapid `applyEdit('update', ...)` calls with the same `expectedUpdatedAt`; calls `engine.start()`; polls until FSM idle. With v5h3-01-03's `engine.ts:282-321` writeback active: both edits drain in ~352ms, no `VersionMismatchError`, both patches merged on remote. **Counter-reset *proof* (locked decision Q6):** working-tree-only revert of engine.ts:282-321 → canary FAILS (engine never reaches idle within 10s polling deadline; `expect(drained).toBe(true)` fails at 10244ms); restore → canary back to GREEN. `git diff --stat src/lib/sync/engine.ts` is empty at task close — no permanent revert ever landed. Failure output captured verbatim in `v60-03-01-SUMMARY.md`. **Decisions:** local-revert-restore proof mode (vs feature-flag-test or branch-demo-then-discard) — smallest blast radius; no production-code instrumentation / polling-timeout assertion (vs direct VersionMismatchError event-log capture) — robust against v60-01 silent-LWW retry interleavings; rooted in observable user symptom / RTL editor↔perf-view propagation pair (broader v54-02-02 Plan 02 scope) intentionally NOT shipped — Gap B remains tracked; counter mechanism stays in place to catch any future propagation-class regression / engine.emulator.test.ts extended (not split into new file) for `*.emulator.test.ts` glob + CI cache coherence. **Verification:** `npm run test:emulator` exit 0 with 3/3 (existing 2 + new canary) in ~3.98s; `tsc --noEmit` exit 0; `next build` exit 0; PROJECT.md + v5h3-01 postmortem reset audit-trail intact. **HFG counter: 0 of 3 ✅ RESET 2026-05-12** (was 1/3 since v53-02). **v53-02 clause-(b) waiver RESOLVED.** **v5h3-01 postmortem Action #2 ✅ CLOSED 2026-05-12 by v60-03-01.** Wave 3 engine-touching phases (v60-04..v60-08) unblocked. No /ui-ux-pro-max gate (infra/test phase, no UI surface). Pre-existing 52 main-suite test failures (SetlistGrid.contextmenu/undo/sync-engine/etc) confirmed orthogonal to v60-03 — they're a separate test-infra cleanup target, not addressed here. Patterns established: full-stack engine canary via firebase-admin → real Firestore emulator / counter-reset *proof* protocol (working-tree revert + capture + restore) / EmulatorAdapter pattern reusable for future engine-adjacent canaries (snapshot-listener round-trip, cross-tab handoff). (2026-05-12)

**v6.0 Tracks Single-Source-of-Truth (in progress) — Phase v60-02 ✅ LOOP COMPLETE — PENDING-UAT 2026-05-12 (Wave 1 behaviorally complete)**
- [x] v60-02 pagehide / visibilitychange blur — mid-edit text protection — Single-session PLAN→APPLY→UNIFY. Closes the iPad-Safari swipe-away save-loss class implicated in v5h3-01 by installing window-level "blur" via `pagehide` + `visibilitychange:hidden` on both edit surfaces (TextCell.tsx and MobileRowCard.tsx) and giving the sync engine a 2-second drain window before the tab is suspended. **Mechanism (3 changes, ~75 LOC):** (1) `TextCell.tsx` gains a `useRef(commit)` + gated `useEffect([editing])` that registers `window.addEventListener('pagehide', ...)` + `document.addEventListener('visibilitychange', ...)` (handler checks `document.visibilityState === 'hidden'`) — both call the existing `commit()` via the ref-stabilized closure; `commit()`'s existing `draft !== asString(value)` guard handles dedup against onBlur. (2) `MobileRowCard.tsx` mirrors the pattern with a `flushRef` that fans out to `commitTitle/Lead/Bpm/Notes` (key commits synchronously onChange so no flush needed); gated on `isEditing` to avoid 30+ listeners per setlist. (3) `src/lib/sync/init.ts` gains a new exported `installPagehideDrainHook()` — idempotent (module-level boolean flag) one-shot `window.addEventListener('pagehide', () => void whenEngineIdle(2000))` wired into `bootEngineOnce()` after `engine.start()`. **Decisions:** silent auto-commit confirmed (no UI feedback — user is leaving the tab; matches locked v6.0 decision #4 silent-LWW posture + existing blur=commit contract) / dual-listen on pagehide AND visibilitychange:hidden (visibilitychange fires first on iOS tab-swipe; both can fire — commit guard dedupes) / 2000ms drain budget pinned (iOS Safari grants ~5s before suspension; 2s headroom for one outbox flush without risking BFCache re-show timing; promise `void`-discarded — best-effort) / ref-stabilized commit closures over deps-in-effect (avoids listener churn on every keystroke) / MobileRowCard listener gated on `isEditing` (perf-aware default) / delta-count assertions for window event-listener tests (robust against transitive-import listeners from firebase.ts recovery wiring) / source-pinning regex test for the 2000ms literal (locks the constant cheaply when same-module export makes runtime spying invasive) / new direct-render MobileRowCard.test.tsx over extending the obsoleted MobileCardList.test.tsx (cleaner isolation, no Sheet/Dexie integration baggage). **Auto-fixes:** (1) JSX closing-tag typo in MobileRowCard.test.tsx harness (`</DndContext></DndContext>` → `</SortableContext></DndContext>`) — one qualify loop. (2) init-pagehide subscribe-spy test needed `setTimeout(50ms)` macrotask wait (microtask flush insufficient because `whenEngineIdle` does `await import('./store')`). **Tests:** +4 in TextCell.test.tsx (pagehide commit / visibilitychange commit / gated-on-editing / unchanged-guard) + new MobileRowCard.test.tsx with 4 (title flush / lead flush / isEditing gate / no-change guard) + new init-pagehide.test.ts with 5 (listener registration delta / idempotency / subscribe-path / no-throw on idle / source pinning of 2000ms). All 13 new tests GREEN. `tsc --noEmit` exit 0; `next build` exit 0. **Pre-existing test infra regressions** (52 unrelated failures across SetlistGrid.contextmenu/undo/sync-engine/upload-musescore/smart-score-viewer) confirmed orthogonal to v60-02 via git-stash baseline diff (same failures at master HEAD `6dc44f3` without v60-02 changes) — carry-forward to Wave 2 (v60-03 HFG closure resets counter to 0/3 + addresses harness fidelity). /ui-ux-pro-max gate satisfied (consult logged before frontend touch; UX consult answered silent-vs-feedback / iPad-Safari dual-listen / accessibility-of-implicit-commit verdicts — all aligned with plan boundaries). **Harness Fidelity Gate counter unchanged at 1 of 3** — orthogonal UX fix; native event listeners only; no engine-internals touch (engine.ts boundary-locked); installPagehideDrainHook is a top-level lifecycle wire-in, not an engine path. Daniel iPad UAT closes AC-5 PENDING-UAT against deployed commit (5th consecutive v51-04 codified pattern). **Side-task:** `vercel env pull .env.local` per Daniel's autonomous mandate this session — `development` environment now available locally (GOOGLE_PRIVATE_KEY / VERCEL_OIDC_TOKEN / BRIDGE_ALERT_EMAIL / SESSION_ROLE_SECRET); verified env presence does NOT affect pre-existing test failures. Patterns established: ref-stabilized event-listener pattern for fresh-closure-without-re-register / idempotent module-level lifecycle hook via boolean flag / delta-count assertions for window event-listener spying / source-content regex test for load-bearing constants when runtime spying is invasive. (2026-05-12)

**v6.0 Tracks Single-Source-of-Truth (in progress) — Phase v60-01 ✅ LOOP COMPLETE — PENDING-UAT 2026-05-12**
- [x] v60-01 SyncIndicator conflict click rewire + silent last-write-wins-on-retry — Single combined commit closes Daniel's #1 fatigue surface: "Conflict — review" pill no longer latches when the force-disabled reconciliation modal can't open. **Mechanism:** new `OutboxRow.forceLwwOnConflict?: boolean` field (additive, optional; no Dexie schema bump); `retryFailedOutboxRows` sets the flag on reset when the user clicks the conflict pill (manual retry IS intent to overwrite per locked decision #4, sole-admin app); engine.ts gains a new `VersionMismatchError` branch BEFORE the existing legacy-stamp self-heal that, when `forceLwwOnConflict === true`, strips `expectedUpdatedAt`, clears the flag, increments attempts to 1, schedules immediate retry, captures to Sentry as `feature: 'conflict-resolution' / site: 'silent-lww-on-retry'` (warning level — NOT in ERROR_LEVEL_FEATURES), and dispatches `DRAIN_RETRY_PENDING`; on the next drain the adapter applies the write unconditionally (LWW), the row is deleted, and the FSM dispatches `DRAIN_OK` (state → 'idle'). `SyncIndicator.tsx` onClick selection simplified to `retryFailedHandler` for both `'failed'` and `'conflict'` states; `onResolveConflict` prop + `useReconciliationModalOptional` hook kept as void-discarded references for backward compat (v60-08 cleanup phase removes them along with the reconciliation modal). **Decisions:** per-row policy flag over engine FSM change (state-machine.ts boundary-locked) / silent-LWW branch runs BEFORE legacy-stamp self-heal so manual retries of legacy docs take this path / `'conflict-resolution'` Sentry feature at warning level for observability without paging / `onResolveConflict` prop retained as void-discard for clean v60-08 deletion / /ui-ux-pro-max gate OPTIONAL with documented rationale (behavior change only, no visual delta — v54-01-03 precedent). **Auto-fixes:** (1) Added `'conflict-resolution'` to `SyncFailureFeature` union in sentry-capture.ts (essential type compile — v54-02-01 AuditAction precedent); (2) Added `status: 'pending'` reset to the new VersionMismatch branch's outbox update payload (caught at Qualify — without it the row stays 'sending' after the branch runs, scheduleNextPump skips it, and the retry never fires). **Latent bug surfaced (deferred to v60-01-02 if signal):** the adjacent legacy-stamp self-heal branch has the same missing `status: 'pending'` reset — when triggered, the legacy row stays 'sending' and the auto-retry never fires; depends on a subsequent user edit to create a NEW outbox row that carries the now-acknowledged stamp. Tests: +3 cleanup (retryFailedOutboxRows sets flag / discardFailedOutboxRows does NOT set flag / no-op on empty) + 2 engine (silent-LWW retries to idle / no-flag path still routes to Conflict — AC-4 contract preserved) + 2 SyncIndicator (conflict click invokes retryFailedHandler not resolveConflictHandler / fallback to retryFailedOutboxRows when no prop). Cleanup 7/7, engine 15/15, SyncIndicator 14/14 — 36/36 across v60-01-touched files. `npm run check:types` exit 0. Full-suite has 52 pre-existing failures in 10 files (incl. `SetlistGrid.undo.test.tsx` 5/5) — confirmed via stash-pop regression test against master HEAD `9914c17`, NOT v60-01-introduced; surfaces for v6.0 milestone-level triage. **Harness Fidelity Gate counter unchanged at 1 of 3** — additive policy branch INSIDE existing v5h3-01-03 + 2026-05-12 self-heal scope; clause-(b) waiver from v53-02 covers it. /ui-ux-pro-max gate satisfied OPTIONAL per documented rationale (no visual surface delta). Browser-smoke checklist appended to PLAN.md for Daniel UAT against deployed commit (AC-4 PENDING-UAT close per v51-04 codified pattern, 5th consecutive milestone). Patterns established: per-row policy flag for one-shot engine behavior overrides (alternative to mutating state-machine for user-initiated semantics) / silent-resolution policy for sole-admin apps (Sentry capture + DRAIN_OK instead of conflict-state + modal) / type-system boundary auto-fix bundling counts as essential. (2026-05-12)

**v5.4 Hotfix + Harness Fidelity — ✅ MILESTONE COMPLETE 2026-05-12 (PENDING-UAT carry-forward; deferrals fold into v6.0)**
- [x] v54-02 Harness Fidelity Emulator + RTL — Plan 01 shipped (`firebase.json` emulators block + `vitest.emulator.config.ts` + `npm run test:emulator(:ci)` + CI emulator-tests job with `actions/setup-java@v4` temurin/21 + canary `engine.emulator.test.ts` proving real-Firestore round-trip). Side-fix bundled: `package.json` version `0.2.6` → `5.4.0` eliminates regression baseline; `scripts/update-build-info.js` skip-bump on shallow clones (preserves `pkg.version` when `git describe` fails, breaking the regression loop). Closes deferred "Pre-existing local-only state cleanup" sub-phase. Plan 02 (H-SL-7 emulator canary) DEFERRED — `winget install Microsoft.OpenJDK.21 --scope user` failed locally; iterating against CI alone was deemed too error-prone for sync-engine-adjacent tests. **Fold-forward to v6.0 as v60-03** (Java install + canary green; HFG counter resets to 0/3 before Wave 3 engine phases). HFG counter held at 1/3. /ui-ux-pro-max gate n/a (infra phase, no UI surface). (2026-05-08)
- [x] v54-01 Picker bootstrap + thead hotfix — End-to-end across 3 plans. **v54-01-01 (`a693d23`)** new `scripts/bootstrap-songs.ts` (firebase-admin + MigrationFirestore reuse + 2-segment marker doc + snapshot collection for sticky-memory-safe rollback; `--dry-run`/`--apply`/`--rollback`/`--force`/`--no-backstitch` CLI) populated 364 production chart docs in `songs/*` (94 CRC + 272 Shireinu; MIME-type filter excluded 89 non-chart entries — folders/audio/docs/spreadsheets — from library_index's 455 active rows) + back-stitched 385 setlist tracks with `songId` via `track.fileId === songs/{id}` mapping. Closes deferred v50-07-02b sub-phase. Spreadsheet thead overlap fixed (removed v53-02-01's `overflow-x-auto` wrapper, bumped `top-[3.25rem]` → `top-[3.75rem]` per v51-02-01-DESIGN-CONTRACT.md:18 lockstep, opaque header bg). /ui-ux-pro-max BLOCKING at checkpoint:decision locked path-a (sticky on th/td directly) over path-b (display:grid). Suite 1597 → 1615 (+18 from bootstrap-songs.test.ts). **v54-01-02 (`6735f48`)** perform-view click regression fix — handlePickSong + handleBindChart now write `track.fileId = song.id` alongside `track.songId` so newly-picked tracks pass SetlistRow.tsx:47 `hasFile` gate (UAT-routed via v51-04 same-phase pattern; +2 regression tests). **v54-01-03** trackCount reconciler in SetlistGridHydrator (live Dexie tracks count via `useLiveQuery` + debounced patch to `setlists/{id}.trackCount` on drift; 800ms debounce; ref-tracked last-written prevents listener-echo write loop). Fixes dashboard "0 songs" on legacy setlists automatically on next open. HFG counter unchanged at 1/3 (admin script bypasses sync engine; thead path-a touched no engine seams; reconciler is priming-adjacent under existing v53-02 waiver). (2026-05-08 → 2026-05-09)
- [x] v5.4 P0 sync/UX patches (2026-05-12 morning) — 8 commits in response to production reports (deleted tracks resurrecting, library-added tracks invisible, aggressive merge popups). `6cd2c4e` stops clearing tombstones on delete-commit success (engine.ts). `c9e92a5` makes SSR read top-level `tracks/{id}` when `setlists/{S}.hydrated === true` (page.tsx). `ed63efc` filters reconciliation modal to version-mismatch rows only (later superseded). `5601726` folds `trackCount` into the cascade's `hydrated:true` setlist update. `b0e7033` engine silent-self-heal of legacy unstamped docs (auto-retries VersionMismatchError when `remote=undefined && attempts==0`). `7421c51` test expectations updated. `a0c61cc` force-disables reconciliation modal (`hasConflict = false` unconditional — pragmatic stop-gap until v60-01 properly rewires SyncIndicator click). `4ee6e70` library Add-to-setlist mirrors writes into top-level `tracks/{id}` (use-add-to-setlist.ts — start of the dual-write bridge v6.0 will decommission). All shipped to master; PENDING-UAT against deployed commits over the upcoming worship cycle. (2026-05-12)
- [x] Dead-code sweep — `146b40b` deleted unused `isMobile` + `useMediaQuery` import in SetlistGrid.tsx (T2.6.a). `6b83ec4` dropped deprecated `clearFailedOutboxRows` alias in `src/lib/sync/cleanup.ts` (T2.6.d). (2026-05-12)
- [x] Architectural audit + v6.0 reconciliation — User halted band-aid patching mid-session: *"stop with the bandaids. I want a real plan and a real fix. with real research."* Four parallel scope-narrowed audit agents + one synthesizer produced `RESEARCH/audit-writes.md` (27 production writers), `RESEARCH/audit-reads.md` (26 read sites + 5 subs), `RESEARCH/audit-sync.md` (sync mechanics + 4 hot-path traces), `RESEARCH/audit-hotpaths.md` (6 end-to-end user scenarios), and the synthesized `RESEARCH/TRACKS-MIGRATION-AUDIT-2026-05-12.md` design doc recommending Option A (finish v50-05 migration). `/paul:discuss-milestone` reconciled v5.4 deferrals against migration scope; user locked 12 decisions; `.paul/MILESTONE-CONTEXT.md` defines v6.0 — Tracks Single-Source-of-Truth (10 phases in 4 waves) with v54-02-02 / v54-03 / Mobile AddBar folded forward as v60-03 / v60-09 / v60-10. Patterns established: "scope-narrowed parallel audit agents + synthesizer" as antidote to band-aid drift; "fold-forward labels on milestone reconciliation" so deferred-but-relevant phases get explicit reassignment in the new milestone. (2026-05-12)
- [x] Deferred to v6.0 (fold-forward, NOT cancelled): v54-02-02 H-SL-7 emulator canary → **v60-03** (Java install + canary green; resets HFG counter to 0/3 before Wave 3); v54-03 cross-device library_index↔songs/* continuous sync → **v60-09**; Mobile parallel-render AddBar variant → **v60-10**. (2026-05-12)

**v5.4 Hotfix + Harness Fidelity (legacy entry preserved during in-progress phase — superseded by milestone-close entry above)**
- [x] v54-01 Picker bootstrap + thead hotfix — Closed two regressions surfaced on v5.3 PENDING-UAT during Daniel-loop UAT 2026-05-08. **Bug 1 (picker empty since v50-05 cutover):** new `scripts/bootstrap-songs.ts` (modeled on `migrate-v50.ts` — `--dry-run`/`--apply`/`--rollback`/`--force`/`--no-backstitch` CLI; marker doc `system/v54SongsBootstrap`; snapshot collection `migrations/v54-bootstrap/snapshot/{songId}` for sticky-memory-safe rollback) populated 364 chart docs in production `songs/*` (94 CRC + 272 Shireinu charts; matches Daniel's expected count within ±2). MIME-type filter (PDF + MusicXML only) added mid-phase when production dry-run revealed library_index's 455 active rows include 19 folders + 57 audio + 8 docs + 4 octet-stream + 2 spreadsheets + 1 wav + 1 m4a = 89 non-chart entries; filter brought write count to 364. Back-stitch loop set `songId` on 385 setlist tracks where `track.fileId` matched (higher than v50-07-01 audit's 351 — setlists added since). Closes the deferred v50-07-02b sub-phase (DRY-RUN-REPORT.md:189-207) that v53-02-01 SUMMARY §212 explicitly flagged. **Bug 2 (spreadsheet thead overlap):** removed `<div className="overflow-x-auto">` wrapper at SetlistGrid.tsx:1610 that v53-02-01 added for sticky-right ChartCell — per CSS spec the wrapper was a scroll container, so `<thead>` `position: sticky` pinned against IT instead of the viewport. Sticky-right ChartCell still works because chart `<th>` (`sticky right-0 z-20`) and `<td>` (`sticky right-0 z-[5]`) pin against the page's natural horizontal scrollport; SetlistGrid is tablet+desktop only (mobile flow uses MobileCardList) so column-sum ~704-784px fits iPad portrait + landscape without page-level horizontal scroll. Bumped thead `top-[3.25rem]` → `top-[3.75rem]` to match real topbar height (back button h-11 + py-2 = 60px = 3.75rem) per v51-02-01-DESIGN-CONTRACT.md:18 lockstep rule (v52-03 violated it by growing SyncIndicator without updating offsets); bumped header bg to fully opaque so any browser without `backdrop-filter` support still masks body row text. **Decisions:** songs/{libId} = library_index doc id directly (back-stitch trivializes; v50-04's "distinct songId" was theoretical, never shipped) / back-stitch ON by default (--no-backstitch flag opts out) / free-text Custom create does NOT auto-promote to songs/* (preserves escape hatch; avoids typo-pollution) / path-a (drop overflow-x-auto, sticky on th/td directly) chosen over path-b (display:grid) per /ui-ux-pro-max checkpoint:decision — smallest-fix bias for hotfix; preserves cell boundary locks; ~10 LOC vs 150-300 / literal `top-[3.75rem]` (no CSS custom property) — JS ResizeObserver wiring would be over-engineering for hotfix; comment + DESIGN-CONTRACT pointer enforces lockstep / extensions kept in displayed titles (matches /library tab + pre-v50-05 AddSongsModal precedent). **Auto-fix (mid-phase):** MIME-type filter when production dry-run count (455) didn't match Daniel's expected (366); inspector script identified non-chart MIME breakdown; added `CHART_MIME_TYPES` Set + 2 new test cases to bootstrap-songs.test.ts (skips non-chart mimeTypes / accepts MusicXML). 18 new tests in bootstrap-songs.test.ts (FakeFirestore mirror of migrate-v50 pattern; covers AC-1..AC-5 + idempotency + rollback). Suite 1597 → 1615 (+18); grid suite 171/171 serial; a11y 13/13; ChartBindPopover 9/9; next build clean. **Harness Fidelity Gate counter unchanged at 1 of 3** — path-a touched no engine seams; bootstrap admin script bypasses sync engine deliberately (firebase-admin direct writes; mirrors migrate-v50 / scrub-livestate precedent). /ui-ux-pro-max BLOCKING gate satisfied at checkpoint:decision (Task 3); recommended path-a + literal top + extensions kept; produced design-system reasoning matrix weighing smallest-fix bias vs Safari quirks vs HFG exposure vs iPad portrait viewport reality. Daniel approved with "go" override at checkpoint:human-verify (PENDING-UAT marker; 4th use of v51-04 codified pattern after v5h3-01 / v53-02 / v53-03). Commit `a693d23` pushed origin/master; Vercel auto-deployed; production songs/* + back-stitch APPLIED. Patterns established: "v5.4 bootstrap-script pattern (firebase-admin + MigrationFirestore reuse + 2-segment marker doc + snapshot collection for sticky-memory-safe rollback)"; "MIME-type filter at bootstrap edge (charts only — PDF + MusicXML; folders/audio/docs/spreadsheets excluded) keeps library_index → songs/* mapping precise"; "Hotfix-class /ui-ux-pro-max consultation pattern: present candidate paths with pros/cons, lock smallest-fix path that doesn't violate boundary locks." (2026-05-08)

**v5.3 Editor UX Repair (rescoped 2026-05-02 to insert v5h3 hotfix) — ✅ ALL 4 PHASES LOOP COMPLETE (milestone closed with PENDING-UAT marker per Daniel "push and finish the milestone"), 2026-05-02**
- [x] v53-03 Polymorphic Add menu — Split-button (primary indigo "+ Song" CTA + chevron 5-tile popover for non-Song TrackTypes) restored from old AddBar (commit `d8c0442`, deleted in v50-02 amputation) into v50-05 spreadsheet substrate. Daniel-locked Option B at /paul:discuss-phase. Ported icon colors (Section muted / Reading amber-300 / Prayer blue-300 / Transition emerald-300 / Stage note muted) — color is enhancement on icon shape + text label (Color-Only HIGH rule satisfied). Tile size ≥48 fine / ≥56 coarse with gap-2 (≥8px touch-spacing); aria-label="Add track of another type" on icon-only chevron; jest-axe ZERO violations on rest + chevron-open states. Long-press disambiguation via explicit `onContextMenu` preventDefault on chevron + every tile + primary AddRowPlaceholder trigger (defense-in-depth; AddBar lives outside row scope but cost-zero defense applied). AddRowPlaceholder consumed by AddBar's primary CTA (Recent / Library / Custom three-CommandGroup pattern from v53-02; cmdk filters all groups together; `value={song.title}` only). Single applyEdit('set','tracks',...) write path preserved. 5 source files (AddBar.tsx new ~180 LOC + AddRowPlaceholder.tsx Recent group + Song trigger + onContextMenu + SetlistGrid.tsx handleAddTrackOfType + AddBar mount + 2 new test files). Suite 1575 → 1597 (+22). tsc clean; `next build` Compiled successfully in 6.8s. Commit `3a321c9` pushed origin/master; Vercel auto-deploying. Single-context execution; no agent dispatches (per CARL [FRESH] "Work in current context unless task exceeds 500 LOC"; total source delta ~+285 LOC). Daniel approved AC-8 sight-unseen with "do it" per v51-04 + v52-03/04 + v53-02 precedent. Two auto-fixed test-fixture issues (curly-quote regex + jest-axe `aria-dialog-name` disable for v51-01-locked TouchOrPopover Radix dialog wrapper) — essential test quality, no scope creep. **Harness Fidelity Gate counter unchanged at 1 of 3** — AddBar surface OUTSIDE protected list; applyEdit signature unchanged. Patterns established: split-button + chevron-popover canonical polymorphic affordance / long-press preventDefault discipline near row-context-menu surfaces / boundary-locked substrate axe rule disable with documented rationale / AddBar.tsx + AddRowPlaceholder.tsx co-design (layout vs. picker concerns). (2026-05-02)
- [x] v53-02 Chart binding picker fix + ChartCell discoverability — cmdk value-format fix at both substrate sites (ChartBindPopover.tsx:152,179 + AddRowPlaceholder.tsx:138 — `${title} ${id}` → `${title}` only); typing-to-filter restored end-to-end. New "Recent" CommandGroup above "Library" in ChartBindPopover (cap 5; sorted by `songs.recent[0].performedAt` desc; reads existing v50-04 fields — NO Dexie schema bump). New `src/lib/songs/prime.ts` library priming helper + SetlistGridHydrator fire-once-per-mount post-hydration effect (one-shot getDocs; NO new snapshot listener; per-mount sentinel; fail-soft via logger.warn). Chart `<th>` and `<td>` carry `sticky right-0` against existing overflow-x-auto wrapper (locked at checkpoint:decision after /ui-ux-pro-max consultation; standard Excel/Sheets pin-column pattern; preserves muscle memory; always visible regardless of horizontal scroll). z-index recipe (header z-20, body z-5, thead z-10) wins both vertical occlusion + horizontal sibling stacking. 4 source files modified + 1 new (prime.ts). Suite 1560 → 1575 (+15; jest-axe ZERO violations on sticky-right grid). **Harness Fidelity Gate first production exercise** — waiver counter 1 of 3 (clause-(b) waiver: SetlistGridHydrator priming-adjacent additive one-shot getDocs; no engine path; UAT closes the gap; v5.4 phase 1 ticket open). Two dan-executor agent dispatches (Task 1 Goal 1 vertical slice + Sub-task 2b sticky-right) per v52-05 / v5h3-01-02/03 precedent. Daniel approved AC-7 sight-unseen with "Go". Commit `bc754b4` pushed origin/master. (2026-05-02)
- [x] v5h3-01 Save-loss recurrence hotfix (NEW phase inserted via rescope 2026-05-02) — End-to-end across 4 plans. **v5h3-01-01:** research + diagnose; 6 hypotheses surfaced from v53-01 iPad UAT capture; code-scan ruled out 3 (H-SL-2/3/4 definitive); HUMAN-ACTION production capture deferred per Daniel "continue autonomously"; anti-pattern audit confirmed all v5h-01 fixes intact (rules + LWW guards + outbox-pending guard + engine writeback atomicity + Sentry instrumentation) — recurrence is NEW failure mode. **v5h3-01-02:** instrumentation Option B shipped; Sentry breadcrumbs at 5 hot write paths + IndexedDB `edit_log` table + upload-on-mount; commit `1d8d94c`. Mid-execution Daniel UAT surfaced reconciliation-modal evidence → NEW H-SL-7 (HIGH confidence) → pivoted plan from "instrumentation only / wait" → "diagnose + ship targeted fix today". **v5h3-01-03:** H-SL-7 fix shipped; engine writeback now threads server `updatedAt` into pending outbox rows for same `(collection, docId)`; rapid same-doc edits no longer trigger phantom VersionMismatchError; v50-06-02 reconciliation contract preserved (AC-3 explicit test); commit `36e9fa1`. **v5h3-01-04:** postmortem at `.paul/postmortems/v5h3-01-save-loss-recurrence.md` + binding **Harness Fidelity Gate** subsection added to PROJECT.md §Constraints (escalates v5h-01 §5 action item #2 from "opportunistic" → BLOCKING for any future data-flow phase; v5.4 phase 1 ship target). Daniel-loop UAT discipline (codified v51-04) validated 2x same-day: morning UAT surfaced recurrence; mid-execution UAT pivoted plan from instrumentation-only to ship targeted fix. Suite 1528 → 1560 (+32 across phase). v53-02..04 soft-blocked behind v5h3-01 PENDING-UAT clear earlier same day per Daniel "no block, keep building". (2026-05-02)
- [x] v53-01 Recursive research (3 parallel tracks) — End-to-end in 1 plan. 3 parallel dan-researcher subagents covered Track A (ChartBind diagnosis HIGH confidence: cmdk value-format scoring bug + library hydration timing) + Track B (old-editor archaeology — git-spelunked v50-02 deletion commits with verdict table; AddBar.tsx from `d8c0442` RECOMMENDED for port-back; chart-preview from SongRow collapsed-state file-name link RECOMMENDED with risk callouts) + Track C (polymorphic Add option set + chart-peek option set). iPad UAT capture (NOT deferred; codified v51-04 discipline applied at research-time). RESEARCH-SYNTHESIS.md with confidence matrix + RESCOPE recommendation: insert v5h3 hotfix BEFORE v53-02..04 (save-loss recurrence surfaced via UAT — same class as v5h-01 2026-04-27, 6 hypotheses LOW confidence). Daniel approved RESCOPE at synthesis decision checkpoint + dropped chart-verification entirely from v5.3 scope ("don't worry about this. Fix the other pieces."). Patterns established: code-read confidence firming via follow-up Q1/Q2/Q3 pass when iPad UAT capture deferred / per-phase deferred Daniel-loop UAT as the verification gate / cluster affirmation via cross-track signal / old-editor archaeology = git-spelunk + verdict table format anchored to postmortem-cited risks. Zero source-code changes; boundary diff empty. (2026-05-02)
- [x] ~~v53-04 Editor affordance pass~~ — ❌ COLLAPSED 2026-05-02. Original phase scope was "whatever Track B surfaces beyond polymorphic Add menu as port-back-worthy"; Track B's only remaining candidate (chart-preview port-back from `SongRow` collapsed-state file-name link) died with chart-verify drop earlier same day. Net zero remaining scope; phase removed from ROADMAP table; empty directory removed; v5.3 milestone shape became 4 phases instead of 4-with-collapse. Pattern: phases can be removed during milestone if scope evaporates; preserve original rationale for archive. (2026-05-02)

**v5.2 Band-Onboarding Hardening — ✅ ALL 5 PHASES SHIPPED (milestone-close UAT pending), 2026-04-30**
- [x] v52-05 Default-template management — Closes Issue 6 in 1 plan (v52-05-01, single vertical-slice commit `cf30d62` + Firebase rules deploy). Track D Option C admin-curated pointer doc shipped at `config/defaults` (codebase convention — matches `config/featured` / `config/congregation` / `config/admins` neighbors; NOT Track D's hypothetical `system/templates` path). Doc shape: `{ shabbat_morning?: setlistId, friday_night?: setlistId, updatedAt, updatedBy }`. Phase 1 scope: shabbat_morning + friday_night only (per OQ Q3 lock). Future ServiceType expansion is additive (set + label, no schema migration). New `match /config/defaults` Firestore rule (read: signed-in; write: admin per OQ Q2). Service helpers `getDefaultForServiceType` + `setDefaultForServiceType` added to `createSetlistService` — get returns null on missing/invalid; set uses `setDoc(merge: true)` so each call updates only one key, preserving other ServiceType pointers. `findLastMatchingService` consults pointer FIRST; on missing/dangling/repurposed pointer silently falls through to legacy 20-most-recent query (OQ Q5 silent-fallback lock — no Sentry, no toast, no warning; pointer absence is normal lifecycle). UI entry point: NEW "Save as Default for {Shabbat Morning | Friday Night}" menu item in BOTH UpcomingSetlistCard + SetlistCard kebabs (between "Save as Template" and "Delete"; Star icon; data-testid="setlist-card-save-as-default"). Gated on `isAdmin && type ∈ PHASE_1_DEFAULT_TYPES`. **Decisions:** **path `config/defaults` (NOT `system/templates`)** — codebase convention; **SetlistCards kebab entry point (NOT editor kebab from Track D OQ Q4)** — v52-03 explicitly removed the always-disabled SetlistGridTopBar kebab as visual debt; SetlistCards kebab is now always-visible on iPad post-v52-04, making it the natural surface; **service helpers do NOT call engine.pump()** — pointer is a regular Firestore doc, not on outbox path; **no confirm dialog** — overwriting an existing default is non-destructive (the previous canonical setlist remains in setlists/{id} untouched); **AuditAction union extension auto-fix** — adding 'set_as_default' triggered exhaustive-map TypeScript error in SetlistHistoryPanel.tsx; added matching label "Set as default for service" (essential type-system fix, not scope creep); **vi.resetAllMocks() in v52-05 test describe** — vi.clearAllMocks doesn't reset mockResolvedValueOnce queues; resetAllMocks does; **single vertical-slice commit** with rules-then-code deploy ordering enforced inside Task 4 (firebase deploy → git commit → git push) per v50-05 cutover lesson. **Plan revision mid-flight:** initial plan misclassified rules-deploy as `checkpoint:human-action`; Daniel feedback "you have firebase cli so you don't need me" prompted same-day revision — Task 4 folded into auto task. Saved feedback memory `feedback_firebase_cli.md` for future plans. /ui-ux-pro-max gate satisfied (carryover from v52-04). 9 files modified (7 source + 2 test; 0 new files). Suite 1528 → 1536 (+8 cases: 5 in setlist-firebase.test.ts + 3 in use-setlist-dashboard.test.ts). tsc + next build clean. Boundary diff confirms 9 source files; 2 beyond plan's stated files_modified are auto-fixes (setlist-audit.ts AuditAction union + SetlistHistoryPanel.tsx exhaustive map). Engine FSM, state-machine, init.ts, snapshot-listener, ReconciliationProvider, write.ts, sticky-memory contract (v50-04), v52-02 + v52-03 + v52-04 contracts ALL preserved (boundaries respected). Daniel approved with explicit "Approved" at HUMAN-VERIFY checkpoint (NOT sight-unseen — milestone-close phase warranted careful surface review). AC-7 dangling-pointer fallback UAT optional, not exercised — covered by unit test. Patterns established: "Admin-curated pointer doc at config/{name} for cross-cutting curation"; "Service-helper pointer-first lookup with silent fallback to legacy query — graceful degradation, no telemetry on absence"; "Two-method service-layer pattern for admin-curated pointers (getXForY + setXForY)"; "Phase-1 scope-gating in UI via const-set + .includes() — additive expansion to other enum values requires only set extension, no schema migration"; "vi.resetAllMocks() (not vi.clearAllMocks) when tests sequence mockResolvedValueOnce queues across describes." (2026-04-30)
- [x] v52-04 Touch affordance + Edit CTA hierarchy on setlist cards — Closes Issues 5 + 7 in 1 plan (v52-04-01, single vertical-slice commit `814a50d`). **Issue 5 (3 P0 hover-reveals from Track C audit):** Append `[@media(pointer:coarse)]:opacity-100` to existing `opacity-0 + group-hover:opacity-100` hover-reveal classes on UpcomingSetlistCard kebab (SetlistCards.tsx:80), SetlistCard kebab (SetlistCards.tsx:208), and CalendarDayCell empty-day "Plan Service" placeholder (CalendarDayCell.tsx:104). iPad now sees the kebab + Friday/Saturday "Plan Service" placeholder at rest without tap-to-hover; desktop md+ hover-reveal preserved (md: prefix preserved on SetlistCards; transition-opacity duration-300 preserved on CalendarDayCell). v50-05-04 floor pattern. **Issue 7 (CTA hierarchy on setlist cards):** Promote "Edit Setlist" (UpcomingSetlistCard lines 136-143) and "Edit" (SetlistCard lines 255-262) from `variant="secondary"` (muted gray) + `bg-muted hover:bg-muted/80 text-foreground` overrides → `variant="brand"` (solid `bg-brand text-brand-foreground hover:bg-brand/85`). Redundant overrides removed; `flex-1 rounded-xl font-bold` layout class preserved. Clone buttons untouched (UpcomingSetlistCard `bg-brand/10 hover:bg-brand/20 ... border border-brand/20` and SetlistCard `text-brand/80 hover:text-brand hover:bg-brand/5` — both correct as tinted-brand secondary). Result: solid brand = primary = Edit; tinted brand = secondary = Clone. Color family unified, weight differentiated. Decisions: **`variant="brand"` not `variant="default"`** (codebase distinguishes `primary` indigo OKLCH from `brand` worship-band accent; Edit lives on a brand-tinted card → use brand to keep accent family cohesive); **drop `bg-muted` overrides at variant swap** (variant carries the styling; leaving overrides would conflict with bg-brand); **Clone buttons untouched** (already correct secondary weight per Track C analysis); **no new tests** (pure Tailwind/variant deltas; no pre-existing SetlistCards.test.tsx; no snapshot tests affected; visual hierarchy is verified by Daniel UAT not snapshot diffs); **P1 audit findings deferred** (C-04 SetlistCards watermark opacity-10/20, C-05 HeroCard arrow opacity-60/100 — cosmetic only, per Track C recommendation, revisit in future polish milestone). 2 files modified, ~7 source LOC delta. Suite 1528/1528 (pre-existing parallel-suite flake didn't surface this run). tsc + next build clean. Boundary diff confirms changes ONLY under SetlistCards.tsx + CalendarDayCell.tsx; no Button component changes (consumes existing `variant="brand"`); no Tailwind theme changes (consumes existing `[@media(pointer:coarse)]:` modifier). v52-02 (cell/picker) + v52-03 (SyncIndicator) contracts preserved. /ui-ux-pro-max gate satisfied (carryover from v52-03; Track C audit + plan-time decisions covered design ground). Daniel approved sight-unseen at HUMAN-VERIFY checkpoint with "Go"; AC-7 real-iPad UAT deferred to standing Daniel-loop discipline (failures route to v52-04-02 follow-up plan in same phase per v51-04 rule). Patterns established: "Always-visible on `(pointer:coarse)` for hover-reveal controls that are the sole path to critical actions — append `[@media(pointer:coarse)]:opacity-100` to existing hover-reveal classes; preserve desktop hover-reveal"; "Two-button CTA hierarchy in branded surfaces: primary uses solid `variant=brand`, secondary uses tinted `bg-brand/10` (or text-brand subtle) — color family unified, weight differentiated, no new hue introduced for primary." (2026-04-30)
- [x] v52-03 SyncIndicator failure UX overhaul — Closes Issues 1 + 4 in 1 plan (v52-03-01, single vertical-slice commit `e69e23a`). **Issue 4** (kebab "red line"): SetlistGridTopBar.tsx kebab `<button>` block + `onOverflow?` prop + unused `MoreVertical` import all removed; SyncIndicator becomes the only trailing action in the topbar. SetlistGrid caller never passed `onOverflow` so prop removal is non-breaking by construction. **Issue 1** (terminal `failed` FSM state with no in-app recovery): new `src/lib/sync/cleanup.ts` exports `clearFailedOutboxRows({ db? })` — deletes only `status === 'failed'` rows from Dexie outbox; preserves `pending` / `sending` (in-flight work never lost); does NOT call `engine.pump()` directly (engine's existing interval-based drain observes the now-clean outbox naturally; mirrors v50-06-03's "write to Dexie, let pump observe" pattern). SyncIndicator gains `defaultRetryFailed` async fallback that wires the cleanup helper when no `onRetryFailed` prop passed (production path) — failed-state action button is enabled by default; `onClick = state === 'conflict' ? resolveConflictHandler : retryFailedHandler`. Auth-staleness pairing: when `lastError` matches `/permission|auth|denied|unauthenticated|unauthorized/i` regex, an inline `<button>` ("Sign out and back in") renders below the inline error pill (added in v51-h01) and calls `useAuth().signOut()` on click; Track B Q2 firming established auth-claim staleness as the plausible co-factor with phantom-row blocking on iPad. ≥44px tap target on coarse pointer via `[@media(pointer:coarse)]:min-h-[44px] [@media(pointer:coarse)]:py-2` per v50-05-04 floor. Decisions: **no confirm dialog** before clearFailedOutboxRows (failed = dead-letter; deletion is loss-of-no-progress, not destructive of in-flight work — /ui-ux-pro-max "Confirm Destructive Actions" rule doesn't fit this semantic; extra modal would harm tablet recovery flow); **neutral text-zinc-300 link color** instead of red-300 from plan draft (red-on-red blends action into severity description, weakens hierarchy; neutral zinc reads as a distinct action while preserving semantic red for the error pill); **mt-1.5 spacing** between error pill and sign-out link instead of mt-0.5 from plan draft (per /ui-ux-pro-max Touch Spacing rule ≥8px between adjacent visual/tappable elements); **`useAuth()` called unconditionally** in SyncIndicator (rules-of-hooks compliance; AuthContext default value provides no-op `signOut` so existing 7 SyncIndicator tests without an AuthProvider keep working unchanged); **import `getDb` from `@/lib/local/schema`** not `db` from `@/lib/local/db` (codebase pattern — `db` is not exported as a constant; auto-fix from plan draft assumption). New `src/lib/sync/__tests__/cleanup.test.ts` (4 cases: deletes-only-failed / preserves-pending-and-sending / idempotent-empty / concurrent-pending-insert-preserved) + extended SyncIndicator.test.tsx (+6 v52-03-01 cases: button-enabled-by-default-no-prop / explicit-prop-wins-over-default / clicking-failed-button-calls-cleanup / sign-out-renders-only-for-auth-keywords / no-link-for-non-auth-errors / clicking-link-calls-signOut). Suite 1518 → 1528 (+10; exceeds plan estimate of +6-8). tsc clean; next build clean. Boundary diff confirms changes ONLY under files_modified; engine FSM, state-machine.ts, init.ts, snapshot-listener.ts, ReconciliationProvider, write.ts, firestore.rules all unchanged. v51-01 + v52-02 cell/picker contracts preserved. Pre-existing parallel-suite test-isolation flake (rotates between `route-auth.test.ts` and `SetlistGridHydrator.test.tsx`; both pass 23/23 in isolation) flagged in SUMMARY but not blocking — unrelated to v52-03 surface area. /ui-ux-pro-max BLOCKING gate satisfied at APPLY entry; queried for destructive-action-confirmation + touch-target + error-recovery patterns; drove the zinc-300 + mt-1.5 refinements. Daniel approved sight-unseen at HUMAN-VERIFY checkpoint with "do it"; AC-6 real-iPad UAT deferred to standing Daniel-loop discipline (failures route to v52-03-02 follow-up plan in same phase per v51-04 rule). Patterns established: "Outbox cleanup primitives live in src/lib/sync/cleanup.ts (additive, write-only-to-Dexie, no engine coupling)"; "Indicator default-handler fallback wires recovery affordances when parent doesn't pass explicit onRetryFailed (analogous to v50-06-02's useReconciliationModalOptional fallback for onResolveConflict)"; "Inline error pill + neutral-toned recovery action below severity-colored description." (2026-04-30)
- [x] v52-02 iPad focus + cmdk system fix — Closes Issues 2 + 3 across 2 plans. **v52-02-01 (`61eae6c`)**: TouchOrPopover gained `suppressAutoFocus?: boolean` opt-in prop (default false); existing `event.preventDefault()` now gated by `(suppressAutoFocus && isCoarse)`; DropdownCell discrete-mode passes `suppressAutoFocus={mode === 'discrete'}` so Key/Type/AddRow/Bulk-Key/Bulk-Type pickers preserve v51-01 no-keyboard-on-open intent and searchable mode (Lead/ChartBind/Bulk-Lead/AddRow library lookup) drops the suppression so cmdk CommandInput auto-focuses on open and the iPad keyboard pops, enabling immediate typing-to-filter (Issue 3 fix). Decision: opt-in (default false) instead of opt-out — discrete pickers explicitly opt in; future TouchOrPopover consumers default to platform-correct Radix focus behavior. Decision: 1 obsolete v51-01 contract test replaced rather than supplemented (the old assertion contradicts new contract). 3 new v52-02 contract tests (default-no-suppress on coarse / opt-in suppress on coarse / opt-in on fine isCoarse-gate-scopes-to-touch). Suite 1513 → 1515 (+2 net). **v52-02-02 (`f061c80`)**: TextCell.tsx reads `useMediaQuery('(pointer:coarse)')` and gates a new `onClick` branch on it — coarse pointer single-tap calls `onFocus()` then `enterEditMode()` so the input renders with autoFocus and the iPad keyboard pops on first tap. Desktop unchanged: click only focuses (preserves arrow-into-cell + Enter-to-edit + Escape keyboard nav). `onDoubleClick` + `handleButtonKeyDown` unchanged. Read-only investigations confirmed MobileEditSheet (plain `<input>`/`<textarea>`) and CreationWizard (shadcn `<Input>` plain wrapper) are case (ii) — already work on iPad without TextCell pattern; no follow-up plan needed. New `TextCell.test.tsx` with 3 contract tests (no prior dedicated coverage). Suite 1515 → 1518 (+3). Combined v52-02 phase delta: ~+30 LOC source + 1 new test file + 3 contract tests added/replaced; tsc + next build clean. /ui-ux-pro-max gate satisfied at v52-02-01 APPLY entry; auto-honored for v52-02-02 (same session). Daniel UAT approved post-deploy for both plans. Pattern established: "opt-in suppression vs blanket-on rule for Radix Popover focus contracts on touch — default to platform-correct behavior; require explicit opt-in for surface-specific exceptions"; "cell-level coarse-tap-to-edit pattern: any future cell with button → input two-state pattern that needs touch single-tap-to-edit follows TextCell precedent." (2026-04-30)
- [x] v52-01 Recursive research — End-to-end in 1 plan (v52-01-01). 4 parallel dan-researcher subagents covered Issues 2+3 (Track A iPad text-input focus regression — confirmed shared substrate via TouchOrPopover preventDefault leak), Issues 1+4 (Track B SyncIndicator state UX — independent fixes despite same surface; Issue 4 hard-disabled kebab `disabled={!onOverflow}` at SetlistGridTopBar.tsx:65; Issue 1 missing recovery affordance for terminal `failed` FSM state), Issue 5 (Track C touch affordance audit — 3 P0 hover-to-reveal findings in SetlistCards.tsx + CalendarDayCell.tsx), Issue 6 (Track D template-management — Option C `system/templates` pointer doc recommended). 1 follow-up Q1/Q2/Q3 firming pass on Issue 1 pushed it to HIGH confidence without iPad UAT data (no existing recovery affordance + auth-claim staleness as compounding factor + `failed` state is terminal). Task 2 HUMAN-ACTION iPad UAT capture DEFERRED to per-phase post-deploy Daniel-loop UAT (codified discipline from v51-04); deviation recorded in STATE.md. RESEARCH-SYNTHESIS.md with 7-row root-cause confidence matrix; all 7 issues HIGH confidence; 0 LOW; no round-2 research triggered. Daniel approved synthesis with 6 default OQ answers locked: Q1=(a) SetlistCards / Q2=admin-only template write / Q3=phased Shabbat morning + Erev Shabbat first / Q4=editor kebab entry point / Q5=silent fallback on deleted pointer / Q6=remove always-disabled kebab from SetlistGridTopBar. Cluster affirmations: Issues 2+3 SHARE root cause (single fix); Issues 1+4 INDEPENDENT (separate tasks within v52-03); Issues 5+7 file-bundled in SetlistCards.tsx (single v52-04 plan). No v52-h hotfix split needed. Wave 1 plans v52-02..05 unblocked and parallel-eligible. Pattern established: "code-read confidence firming via follow-up Q1/Q2/Q3 pass when iPad UAT capture deferred"; "per-phase deferred Daniel-loop UAT as the verification gate (codified discipline from v51-04 applied at execution-time rather than research-time)"; "cluster affirmation via cross-track signal." Zero source-code changes; boundary diff empty. (2026-04-30)

**v5.1 Editor UX Polish — ✅ COMPLETE (4 of 4 phases shipped, band-onboarding gate cleared), 2026-04-27**
- [x] v51-04 Vocal Lead label rename + Daniel-loop UAT codification + gig-packet print smoke — End-to-end in 1 plan (v51-04-01). Six-surface "Lead" → "Vocal Lead" terminology rename across the editor (SetlistGrid column header), batch-edit popover (BulkPopover label/aria/placeholder/emptyHint), mobile edit sheet (field label + input aria-label), importer modal (preview-table `<th>Key/Lead</th>` → `Key/Vocal Lead` + performer-cell placeholder), and gig-packet print pipeline (cover-page column header + colLead x-coord shift left 20pt for "Vocal Lead" header to fit at 10pt Helvetica-Bold without overflowing colTransKey/colNotes). The 6th surface (`Key/Lead` th) was caught during the Task 1 grep audit and added inside scope (the planning grep had used a stricter pattern that missed compound headers). Internal identifiers preserved verbatim per boundary lock: `leadMusician` (DB field), `lead` (patch alias in SetlistGrid), `setlistLeads`/`libraryLeads`/`knownLeads` (autocomplete arrays), `LeadCell` component, `setLead`/`commitLead` handlers, `isLeader`/`onLeaderSetPosition` (perform-mode band leader controlling perf-view position broadcasting — distinct concept), `band_leader` UserRole literal in roles.ts, `"Led by: ${rabbi}"` print line at print-pipeline.ts:257 (the rabbi who leads the service — distinct from per-song vocal lead). New `testId?: string` prop on BulkPopoverProps decouples user-facing label from testid stem (`idStem = testId ?? String(label).toLowerCase()`); the Vocal Lead bulk passes `testId="lead"` to preserve `batch-action-lead-trigger` / `batch-action-lead-popover` testid stability per plan boundary. Type/Key bulks left unchanged (their labels still match their stems, fall through to the label-derived default). PROJECT.md gained a "UAT Discipline (data-flow fixes)" subsection under Constraints codifying the Daniel-loop UAT cadence per postmortem v5h-01 §5 action item #4: every fix touching data flow (sync engine / Dexie schema or writes / snapshot-listener / lazy-hydration / perf-view rendering / editor cell-commit / Firestore rules) gets a Daniel UAT pass on real production before milestone close; UAT failures route to a new plan in same phase; only after UAT passes does `/paul:audit-milestone` run; cross-references the v5.0-hotfix track-edit save-loss postmortem as the originating incident. Decision: **add testId prop instead of slug-renaming testids** (additive prop change is the minimal way to honor the testid-stability boundary; pattern carries forward to any future label rename in components with label-derived testids). Decision: **shift colLead by 20pt left in print-pipeline** (10pt Helvetica-Bold "Lead" ~24pt vs "Vocal Lead" ~52pt; original colLead-to-next-column gaps were 50pt with-trans / 45pt no-trans, both insufficient; pre-flight column-width math is now a documented pattern for future PDF column-header rewrites). Single cohesive feature commit (`233d8b5`); 6 source files modified + 1 PROJECT.md update + 2 plan/summary metadata. Suite 1513 → 1513 (no new tests, no regressions — full suite verified the rename is text-only); next build clean. Boundary diff empty for `src/types/`, `src/lib/sync/`, `src/lib/local/`, `src/components/performance/`, `src/lib/roles.ts`, `firestore.rules`. UAT approved by Daniel ("go") post-deploy; gig-packet print smoke verified end-to-end on a real Erev Shabbat / Shabbat morning setlist with mixed track types (songs + readings + prayers + transitions) + assigned musicians + rabbi: cover page lists every track in order, "Vocal Lead" column reads correctly, "Led by: {rabbi}" intact, eventDate + setlist title correct, per-musician transpositions render at correct semitone offsets. (2026-04-27)
- [x] v51-03 Smart create-setlist wizard (date-aware via Hebcal) — End-to-end in 1 plan (v51-03-01). `/ui-ux-pro-max` consulted at APPLY entry per SPECIAL-FLOWS.md BLOCKING gate; queried ux "wizard dialog primary secondary tertiary CTA hierarchy" + shadcn stack "shadcn dialog disabled tooltip touch target". Three offers surface in a card-framed pre-form strip the moment a service date is picked: **Clone last {service-name} ({date})** primary brand-colored CTA + **Use a template** + **Start from scratch** as text-link options (≥44px tap targets per v50-05-04 floor). New `findLastMatchingService(serviceType, beforeDate?)` on createSetlistService queries the 20 most-recent setlists, infers each candidate's effective service type from `templateType` (with `'festival'` fan-out matching sukkot/simchat_torah/passover/shavuot specific types) or falls back to `getServiceContext(eventDate).type` for legacy setlists. New generic `cloneSetlist(source, targetDate)` extracted; legacy `cloneForNextWeek(source)` refactored as a thin wrapper preserving its public surface so EmptyState's "Make next week's" CTA is untouched. `useCreationWizard` exposes `mode` ('idle'|'clone'|'template'|'scratch') / `cloneSource` / `cloneSourceLoading`; useEffect on eventDate triggers the lookup with auto-default-to-clone-when-mode='idle' (handleTemplateSelect locks mode='template' BEFORE setEventDate so the lookup effect respects explicit user intent). Sticky-memory contract from v50-04 verified intact — cloned tracks are byte-identical copies; new ChartBindPopover bindings still pull fresh sticky values via `seedTrackFromSong` at READ time; `defaults.ts` NOT modified (boundary diff empty for grid/, defaults.ts, local/, sync/, performance/, firestore.rules). Decision: **skip shadcn Tooltip dependency** (not present in src/components/ui/) — no-match path uses AC-5 hide-with-explanatory-text alternative ("No prior {service-name} to clone yet — pick a template or start from scratch below."); cleaner than adding a primitive for one disabled-state caption. Decision: **date picker moved to top of form** — the offer strip reacts to date, so burying it in a 2-col grid below name was wrong information architecture; full-width date field is also more tablet-friendly. Pure helper extracted (`setlistMatchesServiceType`) so 6 of 13 setlist-firebase tests run zero-mock against the type-resolution logic. Single cohesive feature commit (`f30e819`); 5 files (1 new test file + 4 modified — setlist-firebase.ts, use-creation-wizard.ts, use-creation-wizard.test.ts, CreationWizard.tsx). Suite 1495 → 1513 (+18: 13 new in setlist-firebase.test.ts covering pure-helper match/festival-bucket/inference/null + service mocked-firestore round-trip; 5 new in use-creation-wizard.test.ts under "v51-03 clone path" covering auto-default-to-clone + clone create() invocation + scratch fallback + template override + verbatim-copy assertion); next build clean. Daniel UAT approved ("go") at HUMAN-VERIFY checkpoint after Vercel deploy. Deferred: optional Sentry breadcrumb for `creation_mode` tag (toast already differentiates the 3 modes — "Cloned from…" / "Created from {template}…" / "{name} created!" — which satisfies AC-8's user-facing requirement; punt to v51-04 if needed). (2026-04-27)
- [x] v51-02 Editor readability + visual hierarchy (desktop + tablet) — End-to-end in 1 plan (v51-02-01). `/ui-ux-pro-max` consulted at APPLY entry per SPECIAL-FLOWS.md BLOCKING gate; queried design-system "dashboard data-table dense dark professional editor" + ux "data table density hierarchy row height padding" + color "dark mode hierarchy contrast muted text" + shadcn stack "data table responsive layout"; produced `v51-02-01-DESIGN-CONTRACT.md` with 3 implementable option sets (A Tight Compact / B Comfortable Dense / C Hierarchical Spacious). Decision: **Option B Comfortable Dense** — 44px desktop / 48px tablet outer rows (down from ~56/68); column widths narrowed (type 120→104, key 80→72, bpm 72→64; lead capped 156, notes 220 so title flex-fills as primary tier); tier-class hierarchy via redundant cues (weight + color): T1 title `text-sm font-semibold text-foreground`, T2 key `text-sm font-medium tabular-nums text-indigo-200`, T3 lead/type/bpm `text-[13px] font-normal text-muted-foreground` (bpm tabular-nums), T4 notes `text-xs font-normal text-muted-foreground/75`; section rows (type='header'\|'section') framed with `bg-indigo-500/[0.08] + border-l-2 border-indigo-400/50 + border-t border-indigo-500/25` and smallcaps title banner `text-xs font-bold uppercase tracking-[0.1em] text-indigo-100`; selection opacity 5%→8% so it harmonizes with section tint. Implementation single-file (SetlistGrid.tsx) — all 8 cell components already accepted className via cn(); tier classes flow from COLUMNS array; zero churn to cell internals; outer row math = inner h-10/h-11 cell trigger + py-0.5 TD padding (preserves 44px touch floor on `pointer:coarse`). 1 cohesive vertical-slice commit (`c40d880`); 5 files (1 new DESIGN-CONTRACT + 1 new PLAN + 1 new SUMMARY + 2 modified — SetlistGrid.tsx + SetlistGrid.a11y.test.tsx). Suite 1492 → 1495 (+3 a11y cases: mixed header+content axe scan; section-row className tokens; tier classes per row type); next build clean. Mobile parallel render path (MobileCardList/MobileRowCard/MobileEditSheet) + picker internals (TouchOrPopover/KeyCell-tabs/DropdownCell-mode from v51-01) + sync engine + perf-view (SetlistView from v5h-01-03) + firestore.rules — all boundary-locked, `git diff --stat` confirmed ZERO line changes. UAT approved by Daniel post-deploy on desktop + iPad. Realism adjustment logged in SUMMARY: contract draft `py-1.5/py-2.5` → applied `py-0.5/py-0.5` to hit contract user-facing 44/48 target given existing inner cell heights — lesson for future contracts: factor inner element heights before committing pixel padding. (2026-04-27)
- [x] v51-01 Picker rework (all 6 dropdown sites) — End-to-end in 1 plan (v51-01-01). TouchOrPopover Sheet branch removed → always-anchored Radix Popover; `onOpenAutoFocus(preventDefault)` on `(pointer:coarse)` so cmdk CommandInput stays visible without auto-popping the iPad system keyboard. DropdownCell gained `mode='discrete'|'searchable'` + `renderPickerContent` slot; discrete mode skips CommandInput entirely (used by Key + Type + Bulk-Key/Type + AddRow); searchable mode keeps CommandInput but unfocused on touch (Lead + ChartBind + Bulk-Lead + AddRow library lookup). KeyCell rewritten with KEY_OPTIONS_MAJOR (12) + KEY_OPTIONS_MINOR (12) chromatic ascending C → B; storage values preserved verbatim (no Firestore migration); display labels unify enharmonics as `C♯/D♭`. Radix Tabs (shadcn) for Major | Minor with smart default tab inference (ends-in-m → Minor). 44px tap targets + 8px row spacing + selected-state font-semibold + indigo highlight on `(pointer:coarse)` for stage-distance scanability. KEY_OPTIONS_DATA preserved as union for BatchActionBar back-compat. 3 atomic commits (`6671254` + `c11a5c4` + `304e940`); 9 files (1 new test + 8 modified); +432/−163 LOC. Suite 1481 → 1492 (+11 KeyCell.test.tsx cases incl. axe ZERO violations); tsc + next build clean. Pushed to `origin master`; Vercel auto-deploying. Decision: tabs-suppress (Radix Tabs + suppress ChartBind keyboard on touch) — symmetric "no keyboard until deliberate tap" rule across all 6 sites; informed by /ui-ux-pro-max database (shadcn Tabs primitive + "Hover vs Tap" HIGH-severity rule). /ui-ux-pro-max BLOCKING gate satisfied at APPLY entry. (2026-04-27)

**v5.0-hotfix Track-Edit Save-Loss Fix — ✅ COMPLETE (1 of 1 phases shipped), 2026-04-27**
- [x] v5h-01 Track-edit save-loss diagnosis + fix — End-to-end across 4 plans (3 PLAN files; v5h-01-03 was an architectural pivot from a planned execute fix, captured in its SUMMARY's Deviations section). **v5h-01-01:** Research + production state capture (HUMAN-ACTION DevTools → IndexedDB → `crc-local`/`outbox`) confirmed root cause was NOT engine-side (3 ranked handoff hypotheses all WRONG): missing `match /tracks/{trackId}` + `match /songs/{songId}` blocks in `firestore.rules` from v50-05 cutover; default-deny silently rejected every track write; per-doc drain ordering blocked subsequent edits behind failed `set` rows; SetlistGridHydrator re-primed legacy embedded `setlists/{id}.tracks[]` over stuck-pending local edits. Decision E+F+B (defense-in-depth). **v5h-01-02:** Rules deployed via `firebase deploy --only firestore:rules` to crcmusiccharts (commit ships at `0c2921d`); SetlistGridHydrator outbox-pending guard around `db.{setlists,tracks}.put`; snapshot-listener strict-equality LWW guard at both setlists + tracks branches; `property-failures.test.ts` AC-1 flipped from `it.fails` → `it`. Diagnostic chain to close UAT: 142 stuck outbox rows (46 failed + 96 pending blocked); auth token stale post-rules-deploy → sign-out/in restored `role: "admin"`; reset-and-drain snippet flipped 46 failed → pending → engine retried with fresh token; cell-edits started persisting. **v5h-01-03:** Perf-view architectural refactor (4 iterations: `f83d75d` reverted, `8971223` + `4aa6840` superseded on Firestore subscription semantics; `92b1902` final). `useSetlistPerformance` rewritten: reads tracks from Dexie via `useLiveQuery(() => db.tracks.where('setlistId').equals(setlistId).sortBy('order'))` matching the editor's data path; mounts snapshot-listener inside perf-view for cross-device delivery; embedded fallback retained ONLY for unhydrated legacy setlists; public-view short-circuit preserved. 18 brittle onSnapshot mock tests replaced with 15 focused tests using `fake-indexeddb` + listener test seam. Daniel UAT 2026-04-27: edit a track key in editor → in-app back button → perf-view → fresh key visible immediately. "worked!" **v5h-01-04 (this plan, postmortem):** `.paul/postmortems/v5h-01-save-loss.md` captures 5 lessons + 5 action items (cutover rules-audit gate proposal; kitchen-sink harness fidelity gaps with Firebase emulator + thin RTL editor↔perf-view test recommended; perf-view 4-iteration architectural-rethink lesson with `metadata.fromCache` is source not freshness + 2-3-strikes architectural-rethink rule; auth-claim staleness incident; Daniel-loop UAT cadence as v5.x norm; Issue 2 iPad key-picker UI routing rule). ROADMAP corrected from 3-plan claim → actual 4-plan structure. Phase total: +1 postmortem + ~+250 LOC docs; suite 1474 → 1481 (+7 across the hotfix); production data loss = 0 confirmed; affected user count = 1 (Daniel; band not yet onboarded); time-to-resolution = ~6h end-to-end on 2026-04-27. /ui-ux-pro-max NOT required (rules + observability + docs). Next: `/paul:audit-milestone v5.0-hotfix` closes the milestone, then v5.1 UX overhaul. (2026-04-27)

**v5.0 Bulletproof Editor — 7 of 7 phases complete (UAT pending), 2026-04-27**
- [x] v50-07 Migration, kitchen-sink, cutover — End-to-end across 5 plans. **v50-07-01:** Production audit via new `scripts/audit-v50.ts` (read-only): 29 setlists, 24 with embedded tracks (650 total), 0 distinct songIds (legacy uses `id`/`fileId` not `songId`), `songs/*` empty, top-level `tracks/*` empty, 10 setlists carry `liveState` orphan. Pre-existing MARKER_PATH bug discovered (3-seg collection vs. 2-seg doc; never surfaced because tests use a fake adapter). User selected Option C Hybrid Lazy Hydration. **v50-07-02:** MARKER_PATH patched (`system/migrations/v50` → `system/v50Migration`); new `scripts/scrub-livestate.ts` with dry-run / apply / rollback / force; production scrub applied (10 setlists' liveState removed; rollback snapshots in `migrations/livestate-scrub/snapshot/{id}`); 14 unit tests on in-memory FakeFirestore. **v50-07-03:** `LocalSetlist.hydrated?: boolean` added (additive non-indexed schema bump per v50-04 rule). SetlistGridHydrator gained fire-once-per-mount lazy-hydration effect after Dexie priming, gated on `hydration === 'done' && !initialSetlist.hydrated && initialTracks.length > 0`: fans out `applyEdit({op:'set', collection:'tracks', doc:t}, {withoutUndo:true})` for every legacy embedded track via Promise.all, then `applyEdit({op:'update', collection:'setlists', docId, patch:{hydrated:true}, expectedUpdatedAt:initialSetlist.updatedAt}, {withoutUndo:true})` after fan-out succeeds; errors warn-log + retry on next mount. `useSetlistPerformance` dual-reads via `onSnapshot(query(collection(db,'tracks'), where('setlistId','==',setlistId)))`: prefers top-level when length > 0 (sorted by `order` ascending), falls back to `setlistData?.tracks` so 24 not-yet-hydrated legacy setlists keep rendering. 9 new vitest. **v50-07-04:** Kitchen-sink fast-check property in property-failures.test.ts: KitchenSinkAdapter (SharedRemote + online toggle + expectedUpdatedAt precondition) + KSAction grammar (edit-set/update/delete + toggle-online + force-quit + cross-tab via direct SharedRemote mutation + lazy-hydrate mirroring Hydrator's cascade + tick); 50 CI iterations / 10 local with 8s per-iteration safety timeout; asserts AC-9 no-data-loss + per-doc drain ordering + no orphaned 'sending' + lazy-hydration idempotency. OfflineToggleAdapter lifted from inside v50-06-03 describe to module scope. Mid-build runaway in clock.advance × backoff retry storm (4-op shrunk counterexample) replaced with bare pump() quiesce; failed/pending rows still observable in outbox satisfies AC-9. Decision = harness-only (Playwright spec skipped per Task 0; ~200 LOC of mock-Firebase infra avoided). New `npm run test:kitchensink` script. 3 new vitest + extensive harness. **v50-07-05:** Sentry observability shipped via new `src/lib/sync/sentry-capture.ts` helper centralizing tag/level/extra (level='error' for dead-letter / write-atomicity, 'warning' for lazy-hydration / snapshot-listener; tags string-coerced; telemetry try/catches own SDK calls — engine NEVER crashes because Sentry uninitialized). Wired at 6 silent-failure sites: SetlistGridHydrator lazy-hydration catch + engine.ts dead-letter BEFORE existing dispatch + 4 snapshot-listener swallow paths. Per-feature explicit non-capture: 'conflict' state (UX, not failure), per-attempt drains (alert fatigue), payload contents (PII discipline). UAT-PLAN.md (15-item smoke + 7 scenarios + coverage map) + SHIP-CHECKLIST.md (8-step deploy verification + 1-page band onboarding + first-week monitoring playbook with rollback procedure) authored. 6 new vitest. Phase total: ~+2,400 LOC; +52 vitest cases (1442 → 1474); 24 legacy setlists primed for lazy-migration; 10 setlists' liveState scrubbed; bulletproof loop both proven (harness) and observable (Sentry). UAT execution itself is post-plan — Rabbi Daniel + one band member over 1–2 weekly cycles, then `/paul:audit-milestone` closes v5.0. /ui-ux-pro-max BLOCKING for v50-07-03 only; v50-07-01 + 02 + 04 + 05 are backend / scripts / test infra / observability / docs. (2026-04-27)

**v5.0 Bulletproof Editor — 6 of 7 phases complete, 2026-04-26 (superseded by v50-07 entry above)**
- [x] v50-01 Architecture & design — ARCHITECTURE.md sign-off; locked stack (Dexie + outbox / TanStack Table v8 headless + @dnd-kit + Radix + cmdk), normalized rows + LWW per-document doc model, 6-state sync FSM, per-song global sticky memory, one-shot in-place migration with rollback snapshots, spreadsheet-shaped editor wireframes (desktop + iPad + phone + WCAG AA), amputation scope for AI chat + live-swap UI (2026-04-26)
- [x] v50-02 Dead-code amputation — Deleted AI chat (ChatPanel + chat-store + chat-prompt + /api/chat + integration points), live-swap UI (SwapPicker, SwapChangeToast, /live receiver, proxy carve-out), and song-groups + canLiveSwap permission system (swapTrack(), liturgicalSlot field). Three atomic commits; 32 files changed; **net −2,363 LOC**; 1281/1281 tests passing; tsc clean; next build success. Most of the v3.0/v4.0 swap infrastructure was already absent from prior teardowns (LeaderConsole, SwapBottomSheet, song-groups module, canLiveSwap field, isNotTooFrequent rule all already gone). Production data left in place for v50-07 migration to scrub. (2026-04-26)
- [x] v50-03 Local-first sync engine — Dexie 4.4 LocalDb (5 stores: setlists, tracks, songs, outbox, meta) + atomic `applyEdit()` (entity row + outbox row in one Dexie tx) + 6-state sync FSM + backoff retry [500,1000,2000,4000,8000]ms × 5 attempts + dead-letter + cross-tab BroadcastChannel single-leader lock (5s lease) + fast-check property harness proving no committed write is silently lost. Per-doc drain ordering invariant (LWW correctness) added after the property test surfaced the bug. Built fully standalone — zero imports from `src/components`/`src/hooks`/`src/app`; v50-05 editor cutover plugs in cleanly. Three atomic commits (`cb73dcc` + `6cf34d7` + `0a94a9c`); 39 new tests (1320/1320 total); tsc clean; next build success. (2026-04-26)
- [x] v50-04 Song catalog & sticky memory — Dexie schema bumped to v2 (additive `defaults: { key, lead, bpm }` + `recent[]` cap 5 on songs; non-destructive v1→v2 upgrade). New `src/lib/songs/defaults.ts` helper module: `seedTrackFromSong(id)` read-through + `propagateTrackEditToSong(id, patch, setlistId)` 1s-debounced write-back (per-song independent timers, FIFO cap-5, routes through `applyEdit('update', 'songs', ...)` → existing v50-03 sync engine handles Firestore commit). New `scripts/migrate-v50.ts` Firestore one-shot backfill: `--dry-run` / apply / `--force` / `--rollback` / `--help`; abstract `MigrationFirestore` interface keeps tests admin-SDK-free; setlist-invariance sha256 hash check is the regression guard against accidental setlist mutation; per-song snapshots in `migrations/v50/snapshot/{songId}` enable rollback; `system/migrations/v50` marker enforces idempotency. Three atomic commits (`58d2725` + `d73e891` + `d13da61`); 25 new tests (3 schema + 9 helper + 13 migration); 1344/1345 total (1 pre-existing flake in cross-tab-lock unrelated, deferred to v50-06); tsc clean; next build clean. Zero changes to legacy editor surface. Production migration apply deferred to v50-07 cutover. (2026-04-26)
- [x] v50-06 Concurrent-edit safety + offline + cross-tab — End-to-end across 3 plans. **v50-06-01:** Cross-tab-lock test deflaked (30/30 deterministic; production primitive untouched). FirestoreAdapter contract extended: `commitOutboxRow → CommitResult{updatedAt?}`; ProductionFirestoreAdapter re-reads doc post-commit; engine writes new `updatedAt` back to local row inside the SAME Dexie tx that deletes the outbox row (atomic, with `if(existing)` guard). LocalTrack + LocalSong gained explicit `updatedAt?: number`. expectedUpdatedAt threaded through every track-update applyEdit call site (16 sites + executeEntry undo/redo reading LIVE updatedAt at undo-time). Two-writer race describe block in property-failures harness produces VersionMismatchError end-to-end with addressable failed outbox row. 8 new vitest. **v50-06-02:** ReconciliationProvider mounted at /setlists/[id] subscribes to engine 'conflict' state via `useSyncStatus` + reads failed-status outbox rows via `useLiveQuery`; auto-opens on conflict transition; user-dismissable Cancel/Esc; SyncIndicator action button re-opens via `useReconciliationModalOptional` fail-soft hook. FirestoreAdapter interface gained `readDoc(collection, docId) → RemoteDocSnapshot|null`; `init.ts` tracks `adapterSingleton` + exports `getSyncAdapter()`. Per-row card renders title + per-field DIFF (informational; filtered) + per-row "Keep mine / Take theirs" radio (default 'theirs' per §6.9 safe default); "Resolve all and save" iterates `engine.resolveConflict(localId, choice, { newExpectedUpdatedAt })` sequentially. Property-failures harness extended with `setupTwoWriterRace` helper + 'mine' branch + 'theirs' branch tests (5/5 deterministic). ReconciliationProvider component test (~420 LOC, 11 cases) covers all 7 ACs incl. 3 jest-axe scans (ZERO violations). 13 new vitest. **v50-06-03:** New `src/lib/sync/snapshot-listener.ts` exports `startSnapshotListener({ setlistId, db })` — subscribes to `setlists/{id}` + `tracks where setlistId == X` via firebase/firestore onSnapshot; writes deliveries directly into Dexie via `db.put` (NOT applyEdit — server-authoritative; mirrors SetlistGridHydrator's idempotent priming pattern). Two safety guards: (1) outbox-pending guard — any outbox row for the docId means a local edit is in flight, skip both put and delete; (2) LWW guard — only put if `remote.updatedAt > local.updatedAt`. Listener errors swallowed + warn-logged; never throws out of callbacks. Mounted in SetlistGridHydrator post-hydration via `useEffect`. Test-seam SnapshotSubscriber interface lets unit tests inject hand-rolled fakes — 8 vitest cases without booting Firestore. Property-failures harness extended with passive-listener-closes-'theirs'-staleness-gap + sequential-offline-drain-in-order describe blocks (per-doc drain ordering invariant from v50-03 validated end-to-end under realistic airplane-mode flow; 5/5 deterministic). Performance-view audit landed Outcome 2: `useSetlistPerformance` reads legacy `setlists/{id}.tracks[]` embedded array via `useSafeFirestoreSync`; v50-05-01 writes top-level `tracks/{id}` collection; production data is split-brain; routed forward to v50-07 as explicit deliverable. 11 new vitest. Total phase delivery: ~+750 LOC; +27 vitest cases (1410 → 1442); zero new dependencies; zero engine API changes after v50-06-02. /ui-ux-pro-max invoked at v50-06-02 APPLY entry per SPECIAL-FLOWS.md; optional in v50-06-01 + v50-06-03 per audit-driven scope. (2026-04-26)
- [x] v50-05 Spreadsheet editor UI cutover — End-to-end across 5 plans. **v50-05-01:** Built SetlistGrid + ProductionFirestoreAdapter + LazyClientComponents engine boot (TanStack Table v8 headless + dexie-react-hooks live query + 8 columns + cell editing via Radix Popover + cmdk + drag-reorder via @dnd-kit + add-row + Backspace delete + sync indicator + empty state). 29 new vitest. Legacy editor still served route. **v50-05-02:** Cutover landed. SetlistGridHydrator (Option A: server-fetch in Server Component → Hydrator primes Dexie via direct db.put → SetlistGrid mounts) replaced legacy mount; ChartCell click → ChartBindPopover (cmdk + library) → applyEdit with seedTrackFromSong defaults. Deleted ~−6,300 LOC (use-setlist-logic 818 + setlist-flush + setlist-draft + flush-schema + SetlistEditorV2 + 17 v2/ sub-components + /api/setlist/flush route). 9 new vitest. **v50-05-03:** Multi-select via drag-handle modifier-click (Cmd/Ctrl/Shift; anchor-aware extendRange + pruneTo for stale-row surgery) + sticky BatchActionBar (Type/Key/Lead/Delete bulk via parallel applyEdit + per-songId propagation; selection PRESERVED across bulk-set, CLEARED on bulk-delete) + DeleteConfirmProvider (React context + Radix AlertDialog + ConfirmInfo discriminated union) replacing window.confirm. Discovered + documented dnd-kit aria-pressed override pattern (place app-level ARIA AFTER `{...attributes}` spread). 44 new vitest. **v50-05-04:** iPad / pointer-coarse Sheet swap across 6 dropdown sites via new `<TouchOrPopover>` wrapper (single integration point for DropdownCell covering Key/Lead/Type, AddRowPlaceholder, ChartBindPopover, BatchActionBar's BulkPopover); 44px-min touch targets via `[@media(pointer:coarse)]:` Tailwind classes; drag column 44→52px on coarse; ChartBindPopover lifted to controllable open state; right-click ContextMenu (Edit/Bind chart/Duplicate/Delete) with selection-aware action targeting + 500ms long-press for touch via synthetic contextmenu MouseEvent dispatch (workaround for @radix-ui/react-context-menu 2.2.16 having no controlled `open` prop on Root); global window.matchMedia stub via vitest setupFiles. 17 new vitest. **v50-05-05:** Mobile parallel render path keyed on `(max-width: 767px)` (NOT a Tailwind responsive trick — separate component tree with touch-tuned semantics): MobileCardList + MobileRowCard + MobileEditSheet (full-screen Radix Sheet with form fields + Move up/down/Bind/Delete); long-press 500ms on cards opens ContextMenu mirroring desktop. Cmd/Ctrl-Z + Cmd/Ctrl-Shift-Z undo via plain zustand store (NOT zundo — temporal middleware's wrong granularity for per-cell-blur snapshots): manual pushEntry / popUndo / popRedo + per-key burst coalescing (UNDO_BURST_MS=500ms; first-prev wins, latest-new wins) + cap 50 + ephemeral; applyEdit augmented with `ApplyEditOptions` (withoutUndo + undoKey); composite-undo entries for bulk-set / bulk-delete / drag-end / Duplicate row (one user gesture = one undo step); INPUT/TEXTAREA/SELECT/contenteditable skip per v4.2 P2-04 precedent. WCAG AA audit via jest-axe + axe-core: 7 axe scan cases + 1 keyboard Tab-order case → ZERO violations on first run (design system internalized correctly across all of v50-05). 33 new vitest. Total phase delivery: ~+13k / −6.3k LOC; +159 vitest cases (1218→1410); jest-axe ZERO a11y violations; production /setlists/[id] serves desktop + iPad + phone audiences. (2026-04-26)

**v4.4 Deferred Audit Sweep — Architectural Polish — 5 of 8 phases complete (3 deferred), 2026-04-15**
- [x] v4.4 Phase 1: Data-layer atomicity — DL-001/002/003/012/013/014 (scheduling assign/decline transactions) (2026-04-15)
- [x] v4.4 Phase 2: Denormalization reconciliation — user/setlist rename fan-out (DL-010) (2026-04-15)
- [x] v4.4 Phase 3: Client async safety — 11 AbortController sites + 3 stale closures + UX-007 retry cap + 5-test suite (2026-04-15)
- [x] v4.4 Phase 5: Observability — request-ID via AsyncLocalStorage + chat SSE meta/heartbeat/done + client error enrichment (L-001 + S-004) (2026-04-15)
- [x] v4.4 Phase 6: Modal state hygiene — UX-001/002/011/015/018 closed, last R2B "must fix before release" items (2026-04-15)
- ⏭ v4.4 Phase 4 (file-size refactor): deferred to v4.5 as P2 cosmetic
- ⏭ v4.4 Phase 7 (type-safety tail) + Phase 8 (perf tail): deferred to v4.5 as P2 polish

**v4.3 Deep Audit Remediation — 10 of 10 phases complete, 2026-04-15**
- [x] v4.3 Phase 1: Recursive audit (83 findings synthesized into P0/P1/P2 action list) (2026-04-14)
- [x] v4.3 Phase 2: P0 Security Triage — S01 chat prompt injection sanitization, S03 drive-file auth (2026-04-14)
- [x] v4.3 Phase 3: Bridge Credentials Design — S02/CRIT-003 audit log + admin email on redemption (2026-04-14)
- [x] v4.3 Phase 4: P0 Data Integrity — D01 orphan cascade server-side, D02 .passthrough schemas, D03 assign race (2026-04-14)
- [x] v4.3 Phase 5: P0 Bugs + UX — B01 silent catches, B02 alert-store, U01 touch targets, U02 keyboard (2026-04-14)
- [x] v4.3 Phase 6: P1 Security + Bugs — S04 QR role gate, S05 schema wontfix, B03 monitor-client disconnect race, B06 swapTrack array guard (B04/B05 confirmed false-positive on re-review) (2026-04-15)
- [x] v4.3 Phase 7: P1 Data sweep — D05 eventDate shape in unassign (D04 found to be auto-indexed by Firestore, no change needed) (2026-04-15)
- [x] v4.3 Phase 9: Role-Claim Sync — self-service sync-claims endpoint + client drift handler + server-signed __session_role companion cookie retiring the 945478b proxy hotfix (2026-04-15)
- [x] v4.3 Phase 10: Auth Deep-Dive Hardening (added mid-cycle) — fail-fast env + initAdmin guards + armed loop-breaker + cold-load race kill + drift-repair module with 3× retry + restored Firestore isMember() gate + Playwright CI + cross-tab sign-out (2026-04-15)

**v4.2 UX Polish & Band Onboarding — 8 of 8 phases complete, 2026-04-14**
- [x] v4.2 Phase 1: Recursive research — 53+ findings, bugs/gaps mapped (2026-04-13)
- [x] v4.2 Phase 1.1: Concurrent-edit safety — runTransaction + rev precondition + merge banner (2026-04-13)
- [x] v4.2 Phase 1.2: Offline truthiness — IndexedDB blob store, honest offline state (2026-04-13)
- [x] v4.2 Phase 1.3: Security hardening — storage.rules in VC, 10-char bridge code, rate limits on nudge-admin + calendar-feed (2026-04-13)
- [x] v4.2 Phase 2: Weekly workflow polish — save-reliability flush route + "Saved Ns ago" ticker; single-step wizard + congregation-driven rabbi list; past-list DESC + role-aware hero CTA + referrer-back; service-notes always visible + Save-as-Template + "Gig Packet" copy unification + global Cmd/Ctrl+Z (2026-04-13)
- [x] v4.2 Phase 3: Stage UX for the band — SwapPicker keyboard/iOS polish (03-01); PDFOverlay ErrorBoundary + Esc + unified wrapper (03-02); server-side setlist-updated broadcast + musician swap toast (03-03); per-track transposition display + amber cue-note contrast + ≥44px leader header + IDB-backed offline indicator (03-04) (2026-04-14)
- [x] v4.2 Phase 4: Editor ergonomics + noise cleanup — canEditSetlist helper, apiFetch timeout + PDFViewer abort, role-aware OnboardingCard, toast hygiene, inline Move-Up/Down, modal consolidation audit (2026-04-14)
- [x] v4.2 Phase 5: Navigation + schedule hygiene — mobile Schedule tab, UnifiedCalendar JSDoc correction, dead musician_availability indexes removed, orphan /settings/users + /settings/sound routes deleted, SetlistDrawer + monitor-live/commands/pending audited-live (2026-04-14)

### Validated (Recently Shipped)

- [x] v3.4 Phase 1: LeaderConsole mounted on performance page — collapsible panel, live mode accessible
- [x] v3.4 Phase 2: Setlist permissions broadened + print outline includes all items + rabbi "Led by:" on cover page
- [x] v3.4 Phase 3: Print outline fix (absorbed into Phase 2)
- [x] v3.4 Bugfix: CSP allow hebcal.com + strip undefined from clone data
- [x] v3.0 Phase 2: Swap UI — SwapButton, SwapBottomSheet, SwapToast, 3-tap director swap flow
- [x] v3.0 Phase 1: Song groups & swap infrastructure — types, permissions, security rules, API routes, admin UI
- [x] v2.6: Deprecation cleanup, tech debt & setlist UX — 3 phases complete
- [x] v2.6 Phase 1: Setlist row layout — key next to title, inline amber notes, dual-tint alternating rows
- [x] v2.6 Phase 2: Next.js & Sentry deprecation cleanup — proxy rename, instrumentation-client, global-error
- [x] v2.6 Phase 3: Technical debt cleanup — leader→band_leader migration script, build-info silence
- [x] v2.5: Bugsweep & test coverage — 19 phases, 1117 tests, zero warnings, full backend hardening
- [x] v2.0: Schedule & workflow fixes — all 3 phases complete
- [x] v1.9: Auth stability & deferred cleanup — Phases 1-5
- [x] v1.8: Mobile UX overhaul — navigation, responsive layout, schedule redesign
- [x] v1.7: Print pipeline overhaul, key signature position, monitor buses — Phases 1-5

### Planned (Next)

- To be defined in next milestone

### Out of Scope

- To be identified during planning

## Target Users

**Primary:** CRC worship band musicians
- Use tablets/iPads on music stands during live services
- Need instant transposition for their instrument (Bb trumpet, Eb sax, etc.)
- Need monitor mix control without leaving their position

**Secondary:** Community jam participants
- Load setlists and charts on personal phones
- Need simple read-only access to current song/chart

## Context

**Business Context:**
Central Reform Congregation worship services and rehearsals. Also used for community jam events with broader participation.

**Technical Context:**
- Next.js 16 (App Router) on Vercel
- Firebase (Auth, Firestore, Storage)
- Google Drive for file intake
- Gemini AI for chord detection and chat
- PWA for offline tablet/phone use

## Constraints

### Technical Constraints
- Must run on Vercel serverless
- Venue has wifi; PWA/SW removed in v2.5 (caused stale deploy issues)
- Must perform well on tablets and phones (touch-first)
- Firebase ecosystem for auth, database, storage

### Business Constraints
- Single congregation use case (CRC)
- Musicians are not technical — UX must be frictionless

### UAT Discipline (data-flow fixes)
Every fix that touches data flow — sync engine, Dexie schema or writes, snapshot-listener, lazy-hydration, perf-view rendering, editor cell-commit, Firestore rules — gets a Daniel UAT pass against real production before milestone close. UAT failures route to a new plan in the same phase; only after UAT passes does `/paul:audit-milestone` run.

Codified after the v5.0-hotfix track-edit save-loss incident (2026-04-27): the kitchen-sink fast-check harness (1468/1468 green) couldn't see the missing `tracks/{id}` + `songs/{id}` Firestore rules because it has no security-rules layer + no perf-view path coverage + zero-latency in-memory adapters that miss cache-vs-fresh races. Daniel's UAT against real production caught it as path "P" (cell-edit a key, navigate away, return — key reverts). Postmortem and full action items at `.paul/postmortems/v5h-01-save-loss.md` (action item #4).

### Harness Fidelity Gate (binding from v5.3)

The kitchen-sink fast-check harness (v50-07-04) uses in-memory zero-latency adapters with no security-rules layer and no perf-view-path coverage. It has missed two production data-flow bugs:
- **v5.0-hotfix (2026-04-27)** — missing `firestore.rules` for `tracks/{id}` + `songs/{id}` collections; default-deny silently rejected every track write. Root cause invisible to harness (no rules layer). See `.paul/postmortems/v5h-01-save-loss.md` §Lessons.2.
- **v5h3 (2026-05-02)** — engine writeback did not thread server `updatedAt` into pending outbox rows for the same doc; rapid same-doc edits raised phantom VersionMismatchError; v50-06-02 reconciliation modal opened in single-user context. Root cause invisible to harness (in-memory adapter never produced rapid-same-doc-edit-before-writeback-completes sequence). See `.paul/postmortems/v5h3-01-save-loss-recurrence.md` §Lessons.3.

The remediation has been deferred since v5h-01-04 (2026-04-27) under "opportunistic" framing. Twice-implicated is no longer acceptable. The remediation work:
- **Firebase Local Emulator Suite integration** in the property-failures harness — runs real rules + auth + cache-then-fresh delivery + transaction precondition timing in-process during a subset of property iterations. Addresses Gap A (rules layer) + Gap C (cache-vs-fresh races) from v5h-01 §Lessons.2.
- **Thin RTL editor↔perf-view cross-view propagation test pair** — runs the editor cell-commit path AND `useSetlistPerformance` against the same in-memory Firestore + Dexie; asserts cross-view propagation invariant. Addresses Gap B (perf-view path divergence) from v5h-01 §Lessons.2.

**Binding semantics (from v5.3 onward):** any plan whose `files_modified` touches sync engine (`src/lib/sync/`), Dexie schema/writes (`src/lib/local/schema.ts`, `src/lib/local/write.ts`), snapshot-listener (`src/lib/sync/snapshot-listener.ts`), lazy-hydration (`src/components/setlist/grid/SetlistGridHydrator.tsx`), perf-view rendering (`src/hooks/use-setlist-performance.ts`), editor cell-commit (`src/components/setlist/grid/cells/`), or Firestore rules (`firestore.rules`) MUST do one of:
- (a) Land AFTER the harness-fidelity remediation has shipped (preferred path), OR
- (b) Carry an explicit per-plan waiver under `<boundaries>` SCOPE LIMITS naming the open v5.4 harness-fidelity ticket and the specific reason this plan ships before it (e.g., "P0 production hotfix; cannot block on harness work; UAT closes the gap for this plan"). Waivers are tracked across plans; three waivers in a row triggers re-prioritization to v5.4 phase 1.

**Owner:** Rabbi Daniel. **Target:** v5.4 phase 1 (first phase of v5.4 milestone, before any v5.4 data-flow phase). **Codified by:** v5h3-01-04 postmortem.

**v54-02 status (in-progress 2026-05-08):** Plan 01 (emulator infra) ships Firestore + Auth emulator config in `firebase.json`, opt-in `npm run test:emulator` script + separate `vitest.emulator.config.ts` (glob `*.emulator.test.ts(x)`; main suite unchanged), CI `emulator-tests` job in `.github/workflows/ci.yml` using `actions/setup-java@v4` + `firebase emulators:exec`, and one canary test `src/lib/sync/__tests__/engine.emulator.test.ts` proving wiring. Plan 02 (RTL editor↔perf-view propagation pair + H-SL-7 regression canary) lands in v54-02-02. **Counter resets to 0 only when both plans ship and CI proves the H-SL-7 canary catches a v5h3-01-class regression.**

**v60-03 status (2026-05-12) — Harness Fidelity Gate counter: 0 of 3 ✅ RESET.** v54-02-02 H-SL-7 regression canary shipped (folded forward as v60-03). Canary at `src/lib/sync/__tests__/engine.emulator.test.ts` exercises the FULL stack (SyncEngine + ProductionFirestoreAdapter-equivalent + real Firestore via emulator + Dexie outbox) — two rapid same-doc edits with the same `expectedUpdatedAt`; with the v5h3-01-03 writeback fix at engine.ts:282-321 active, both drain to idle in ~352ms; with the writeback block locally commented out (working-tree only, restored before commit), the canary FAILS with the engine never reaching idle within the 10-second polling deadline (recorded verbatim in `v60-03-01-SUMMARY.md`). Locked decision Q6 satisfied: counter reset to 0/3 backed by *proof*, not existence. **v53-02 clause-(b) waiver (SetlistGridHydrator priming-adjacent one-shot getDocs): RESOLVED — replaced by real-Firestore canary in v60-03-01.** Wave 3 engine-touching phases (v60-04 server reads → v60-05 editor/perform reads → v60-06 dashboard + backfill → v60-07 writer strip → v60-08 cleanup) are now unblocked. Future Wave 3+ plans that touch the engine, Dexie writes, or rules SHOULD extend the emulator canary set rather than re-take a waiver under clause (b). The full waiver-counter mechanism stays in place for any future class of test-infra-uncovered bug that surfaces.

**Local-machine prereq (multi-machine memory rule):** Firebase emulators require Java JDK 11+ on PATH.
- Windows: `winget install Microsoft.OpenJDK.21` (system-scope; needs admin).
- macOS: `brew install openjdk@21`.
- Linux: `apt install openjdk-21-jdk` (or distro equivalent).
CI's `emulator-tests` job uses `actions/setup-java@v4` so PR coverage doesn't depend on local installs. If a developer's machine lacks Java, `npm run test:emulator` will fail with a clear Java-not-found error — the main `vitest run` (1615 tests) keeps working.

## Tech Stack

| Layer | Technology | Notes |
|-------|------------|-------|
| Framework | Next.js 16 | App Router |
| UI | Tailwind CSS 4 + shadcn/ui | |
| State | Zustand | |
| Auth | Firebase Auth | |
| Database | Firestore | |
| Storage | Firebase Storage (CDN) + Google Drive (intake) | |
| AI | Google Gemini | OCR, chat |
| Testing | Vitest + Playwright | 1117 tests |
| Deploy | Vercel | |

## Links

| Resource | URL |
|----------|-----|
| Repository | https://github.com/RavBogard/sheet-music-app |
| Production | CentralReform.live |

## Key Decisions

| Decision | Phase | Impact |
|----------|-------|--------|
| withAiSlot as preferred AI concurrency API | Phase 2 | Pattern for all future AI callers |
| Drive timeout via Promise.race | Phase 2 | googleapis doesn't support AbortSignal |
| uploadToStorage keeps throwing, reads get StorageResult | Phase 3 | Consistent pattern for Storage callers |
| Missing context tracked in AI prompt (not silent) | Phase 3 | AI responses note missing data sources |
| BroadcastChannel for cross-tab cache invalidation | Phase 3 | Tabs stay in sync after library sync |
| Ref-based callbacks for effect dep stability | Phase 4 | Pattern for all hooks with callback deps |
| Broadened useSafeFirestoreSync ref type to DocumentData | Phase 4 | Eliminates all caller `as any` casts |
| stripUndefined/Deep for Firestore sanitization (not JSON roundtrip) | v1.5 P1 | Preserves Timestamp instances, explicit filtering |
| One server-side setlist-write path: `src/lib/setlist-write.ts` (firebase-admin direct write, not the client applyEdit outbox) | v70-07 | createSetlistServerSide + updateSetlistServerSide consumed by import/execute, doc-commit, AND the MCP write tools — no competing write paths |
| Doc-import = 5 independent sibling routes chained client-side (extract-document → extract-structure → resolve → commit-document), no orchestration route | v70-07 | Each step independently testable; the ImporterModal owns the chaining |
| Recording binding deferred entirely for v70-07 — recordingCandidates surfaced in the preview, not bound on commit | v70-07 | Clean future addition; v70-07 commit stays metadata + chart-binding only |
| cancelled-flag before every state update after async boundaries | v1.5 P1 | Pattern for all hooks with async + state |
| All standard auth routes use createApiHandler | v1.5 P2 | withAuth reserved for complex patterns (dual auth, streaming, mixed auth, dev-only) |
| SectionErrorBoundary for admin crash isolation | Phase 4 | Sections fail independently |
| pdfjs.version in worker URL for cache busting | v1.3.1 | Prevents stale worker mismatch after deploys |
| Ref-based uid tracking in useMonitorConnection | v1.3.1 | Prevents effect churn during iPad auth token refresh |
| visibilitychange as iOS Safari reconnection trigger | v1.3.1 | beforeunload doesn't fire on iOS Safari |
| displayName overlay for song rename (Firestore, not Drive) | v1.4 P1 | Preserves Drive filenames, rename is Firestore-only |
| Conditional withSentryConfig (only when DSN set) | v1.5 P6 | Zero build overhead without Sentry |
| Offline write queue not needed (Firebase native persistence) | v1.5 P6 | Firestore SDK handles offline writes natively |
| Firebase Storage is sole file source (Drive = intake only) | v1.6 P2 | Simplified file serving, removed Drive fallback |
| Key-left setlist layout for live performance | v1.6 P3 | Keys prominent next to song names for tablet glanceability |
| chat + drive/file routes stay on withAuth permanently | v1.6 P4 | SSE streaming and mixed browser auth don't fit createApiHandler |
| COOP: same-origin-allow-popups for Google sign-in | v1.7 P1 | Allows popup sign-in while maintaining isolation |
| signInWithRedirect as popup fallback | v1.7 P1 | Mobile browsers that block popups get working sign-in |
| 10s suppress window for SW update banner | v1.7 P1 | Prevents false "update available" on fresh page load |
| Direct sync PDF generation (bypass Inngest) | v1.7 P3 | Inngest not configured on Vercel; sync matches working personal/public routes |
| Remove Firestore polling from PrintModal | v1.7 P3 | No background job = no job status to poll |
| Key badge right of title (reversed key-left) | v1.7 P4 | User preference; inline after title is cleaner |
| All services as schedule default (not personal) | v1.8 P3 | Everyone sees full schedule; "Mine" toggle for personal filter |
| Single subscription with client-side filter | v1.8 P3 | subscribeToAllUpcomingAssignments for all users, filter locally |
| Dual subscription merge for schedule page | v2.0 P1 | Subscribe to both setlists and assignments; setlists-first ensures all services visible |
| Key column before Lead on gig packet cover page | v2.0 P3 | Key is most glanceable info after song title for musicians |
| Non-song items excluded from PDF charts section | v2.0 P3 | Readings/prayers don't have charts; saves paper, reduces confusion |
| TemplateContext with type:string instead of widening ServiceType | v2.5 P1 | Template keys are superset of ServiceType; avoids breaking union |
| hasSeconds() type guard for Firestore Timestamp-like fields | v2.5 P1 | Pattern for safely checking Timestamp objects in scheduling routes |
| Mock objects exported from helpers, vi.mock() stays in test file | v2.5 P3 | Vitest hoisting requires vi.mock at file scope; helpers provide objects only |
| beforeAll dynamic import for route handlers | v2.5 P3 | Eliminates cold-start flakiness in API route tests |
| clearFirestoreIndexedDB() for corrupted persistence recovery | v2.5 P6.1 | Shared helper used by SwCleanup, ErrorBoundary, and unhandledrejection listener |
| PWA/SW fully removed, next-pwa uninstalled | v2.5 P6.1 | SW caused stale deploys, PDF worker failures, cache breaks; venue has wifi |
| Annotation feature removed entirely | v2.5 P7 | Unused by worship band; simplifies toolbar before Phase 8 UX fixes |
| react-pdf AnnotationLayer.css preserved | v2.5 P7 | PDF.js built-in, not custom annotation code |
| All existing setlists assigned to admin UID | v2.5 P8.1 | Rabbi Daniel created all setlists; backfill script sets ownerId |
| Band leaders granted read access to all setlists | v2.5 P8.1 | isBandLeader() added to Firestore read rule |
| Missing ownerId treated as legacy accessible data | v2.5 P8.1 | Truthiness guard prevents redirect on legacy docs |
| Fuse.js for MobileTabBar search over library store | v2.5 P10.1 | Reuses hydrated library data; no API round-trip for song search |
| sessionStorage for last-opened setlist tracking | v2.5 P10.1 | Tab-scoped, auto-clears on close; Setlist button navigates to most-relevant setlist |
| Hidden placeholder for balanced 3-col layout without monitor | v2.5 P10.1 | Invisible div maintains spacing when monitor feature unavailable |
| Three-tier responsive: default → md: (768px) → lg: (1024px) | v2.5 P13 | Tablet gets dedicated single-row toolbar between phone two-row and desktop |
| Swipe threshold 1.5x (up from 1.1x) for zoomed navigation | v2.5 P13 | Musicians who zoom slightly can still swipe between charts |
| Auto-hide timeout 15s (up from 8s) for live performance | v2.5 P13 | More time for stage musicians to interact with toolbar |
| Batch musician prefs via db.getAll() in scheduling cron | v2.5 P14 | Eliminates N+1 reads; single batch for up to 100 musicians |
| AbortController per-file via Map ref in use-offline | v2.5 P14 | Prevents state updates after unmount during downloads |
| Notification create restricted to admin/band_leader | v2.5 P14 | Server writes via admin SDK; client-side only for privileged roles |
| coverOnly early-return in print pipeline | v2.5 P15 | Skips all PDF fetch/merge/transposition for cover-page-only prints |
| coverOnly included in content hash | v2.5 P15 | Prevents full-packet cache from serving as cover-only and vice versa |
| WriteBatch for delete-user, runTransaction for set-role | v2.5 P18 | Firestore ops atomic; Auth ops best-effort after commit |
| ApiErrorResponse {error, code?, details?} standard shape | v2.5 P18 | Consistent error responses via apiError() helper |
| config/admins Firestore doc for super-admin bootstrap | v2.5 P18 | Replaces hardcoded UID in firestore.rules; manageable without redeploy |
| CRON_SECRET/SUPER_ADMIN_UID optional in env.mjs | v2.5 P18 | Runtime guards still enforce; dev envs don't break on missing vars |
| bg-white/[opacity] for dark-mode alternating rows | v2.6 P1 | --muted OKLCH has baked-in alpha; bg-white is predictable |
| Dual-tint rows (0.03/0.07) instead of tint/black | v2.6 P1 | Both rows readable on dark backgrounds |
| canLiveSwap mirrors soundEngineer pattern (profile + custom claim) | v3.0 P1 | Consistent permission model across features |
| Hybrid song grouping: liturgicalSlot tag + config/songGroups doc | v3.0 P1 | Tags = source of truth, config = display metadata |
| affectedKeys().hasOnly() for field-level setlist update rules | v3.0 P1 | Swap users restricted to tracks/liveState/trackCount only |
| swapLiveTrack: single updateDoc + fire-and-forget audit | v3.0 P1 | Atomic swap, non-blocking audit trail |
| isNotTooFrequent() rule: 2s minimum between swaps | v3.0 P1 | Prevents accidental double-taps at Firestore level |
| storage.rules: isMember() via custom claim only (no Firestore cross-read) | v4.2 P1.3 | Storage rules cannot read Firestore; legacy config/admins bootstrap cannot be mirrored — accept cost |
| Dedicated `bridgeSetup` rate-limit tier (5/min) for credential exchange | v4.2 P1.3 | Credential endpoints deserve stricter bound than app-wide 60/min |
| 10-char unambiguous setup codes (~50 bits) over alphabet expansion | v4.2 P1.3 | Humans copy the code; ambiguity-free alphabet preserved, entropy comes from length |
| Unload flush: fetch keepalive + Bearer, not sendBeacon | v4.2 P2-01 | App auth is Bearer-header only; sendBeacon can't set headers → would 401 |
| New /api/setlist/flush route mirrors Phase 1.1 StaleWriteError precondition server-side | v4.2 P2-01 | Unload save path goes through Admin SDK; same version-check semantics as in-page save |
| Text + dot SaveStatus (dot scans at stage distance, text gives context) | v4.2 P2-01 | Glanceable + informative; no regression on existing visual cue |
| Single-step creation wizard with inline Template dropdown | v4.2 P2-02 | Shaves one click from 90% weekly flow; Template remains accessible as a Select |
| Storage rules isMember() uses custom claim only (no Firestore cross-read) | v4.2 P1.3 | Storage rules cannot call Firestore; config/admins bootstrap not mirrored — accept cost |
| Two distinct modals for create vs edit (NamePrompt / EditDetails) | v4.2 P2-02 | One overloaded modal was showing wrong title and missing rabbi/notes fields on edit path |
| Hardcoded rabbi list replaced with useCongregation() | v4.2 P2-02 | Admins can edit rabbiProfiles in Firestore without a code push; wizard + overflow submenu stay in sync |
| NextServiceCard is the live hero; HeroCard is dead code | v4.2 P2-03 | Role-aware CTA change applied to NextServiceCard (the rendered component); HeroCard left untouched for a future cleanup plan |
| router.back() gated on same-origin + href-not-self | v4.2 P2-03 | Referrer-based back respects in-app history but doesn't exit the app via search-engine referrers |
| Service Notes always visible (no "+ Add" gate) | v4.2 P2-04 | Progressive disclosure on a primary authoring field is an anti-pattern; placeholder carries the purpose |
| Field-aware global keyboard shortcuts | v4.2 P2-04 | Skip on INPUT/TEXTAREA/SELECT/contenteditable so native field undo works |
| Canonical "Gig Packet" copy (drop "Print") | v4.2 P2-04 | "Print" undersells a multi-page PDF; "Gig Packet" is the in-house term |
| AsyncLocalStorage for request-ID propagation across all API routes | v4.4 P5 | Logger auto-tags every call within a request scope; no manual plumbing at call sites |
| globalThis.__requestIdGetter__ resolver in logger to avoid client-bundle ALS leak | v4.4 P5 | Static import of node:async_hooks broke webpack via error-boundary.tsx |
| Chat SSE adds additive meta/heartbeat/done frames; assistant token format byte-identical | v4.4 P5 | ChatPanel parser unchanged (ignores unknown event types); zero client churn for observability |
| CollapsibleSection localStorage opt-in via storageKey prop | v4.4 P6 | Backward compatible — sites without storageKey keep in-memory behavior |
| Modal state-reset effect deps = [isOpen] only (NOT initial* props) | v4.4 P6 | Adding initial* deps would clobber in-progress edits on parent re-render |
| ref-stabilise prop callbacks in long-lived effect handlers | v4.4 P3 | TempoFlash/PDFOverlay Escape handler invokes latest callback, not the one captured at mount |
| PDFViewer retry cap at 3 attempts → terminal error UI | v4.4 P3 | Prevents infinite thrash on consistently-broken charts |
| applyEdit() = one Dexie transaction over (entity store + outbox) | v50-03 | Atomic write contract — entity row + outbox enqueue land together or roll back together; foundation of "bulletproof" |
| Per-doc drain ordering — block later same-doc rows when earlier row is sending/failed/not-yet-due | v50-03 | LWW per-document correctness; transient failure on row N can't let row N+1 same-doc leapfrog. Fix discovered by property test counterexample |
| Auth refresh + retry happens in-loop (single drain pass), not via re-queue | v50-03 | Second-attempt result resolves directly to Idle/Failed; cleaner than encoding "refresh used" via attempts field |
| Orphaned 'sending' rows reset on engine.start() (post force-quit recovery) | v50-03 | Prevents permanent doc-blocking under per-doc ordering rule |
| FakeClock injection > vi.useFakeTimers for Dexie-touching tests | v50-03 | vi races with fake-indexeddb microtask scheduling; manual FakeClock + macrotask flush is deterministic. Pattern reusable for v50-04..v50-06 |
| Sticky-memory debounce default = 1000ms (overridable via opts.debounceMs) | v50-04 | Matches ARCHITECTURE.md §4.3 explicitly; v50-05 editor inherits this default; tests use shorter values via clock injection |
| Migration core abstracted behind MigrationFirestore interface (FIELD_DELETE_SENTINEL for FieldValue.delete) | v50-04 | Tests run without firebase-admin; CLI adapter wires the real SDK. Pattern reusable for future migration scripts |
| Orphan-track filter applied to BOTH dry-run and apply paths | v50-04 | Honest dry-run counts (caught by failing test: dry-run reported 4 candidates while apply only wrote 3 — the difference was an orphan track silently skipped at apply time only) |
| Schema bumps to v(2) are additive non-indexed only; new indexed fields require v(3) | v50-04 | Lookups happen by id; over-indexing wastes IDB. Pattern carries to v50-05/06 |
| Touch detection via useMediaQuery('(pointer: coarse)') NOT viewport width | v50-05-04 | iPad Pro at 1024px is still touch; viewport detection misses it AND over-triggers on resized desktop. Reusable for any future touch-aware affordance |
| Single TouchOrPopover wrapper for all 6 dropdown swap sites (Popover↔Sheet on coarse) | v50-05-04 | Symmetry — same wrapper, six consumers (DropdownCell + AddRow + ChartBind + BulkPopover). asChild flows through preserving trigger refs |
| Long-press for touch via synthetic contextmenu MouseEvent dispatch | v50-05-04 | @radix-ui/react-context-menu 2.2.16 has NO controlled `open` prop on Root. Re-emit `new MouseEvent('contextmenu', {...})` on trigger — Radix's internal listener catches and opens. Reusable for any uncontrolled Radix primitive listening for a specific event |
| Real timers (NOT vi.useFakeTimers) for component setTimeout-driven tests | v50-05-04 | Reinforces v50-03 lesson — fake timers conflict with fake-indexeddb microtask scheduling and Dexie live-query teardown. Real `await sleep(ms)` adds ~Ns per case — cheap |
| Parallel mobile render path keyed on `(max-width: 767px)` (NOT Tailwind responsive) | v50-05-05 | Existing TanStack Table breaks ~640px; touch semantics differ enough (long-press menu, full-screen Sheet, no inline cell editing) that separate component tree is right. iPad ≥ 768px keeps the table |
| Plain zustand store with manual pushEntry over zundo's temporal middleware | v50-05-05 | Per-cell-blur burst coalescing needs explicit per-action snapshots, NOT state-snapshot-on-every-setter. zundo's wrong granularity. One less dep |
| applyEdit reads prevDoc BEFORE transaction, pushes snapshot AFTER commit (gated by withoutUndo) | v50-05-05 | Failed writes leave no phantom undo entries. withoutUndo escape hatch for engine-internal cascades + the undo handler replaying inverses |
| Composite undo entries for multi-row user actions (bulk-set / bulk-delete / drag-end / Duplicate row) | v50-05-05 | One user gesture = one undo step. Snapshot prevDocs first, fire applyEdit({withoutUndo:true}) fanout, push ONE composite entry with N legs |
| Per-cell-blur burst key default `${collection}:${docId}:${field-set}` | v50-05-05 | Multi-field patches get one coalescing window; single-field patches get per-field windows. UNDO_BURST_MS=500ms matches v50-04 sticky-memory debounce |
| INPUT/TEXTAREA/SELECT/contenteditable skip for global Cmd-Z at SetlistGrid root | v50-05-05 | Native field undo wins when typing into a form field. Same skip set as v4.2 P2-04 + v50-05-03 Esc handler |
| WCAG AA via jest-axe at component-test level — ZERO violations on first run | v50-05-05 | 7 axe scan cases + 1 keyboard Tab case; axeOpts disables 5 harness-context false positives. Design system internalized correctly across v50-05; no in-place fixes needed |
| Reconciliation modal granularity = per-row "Keep mine / Take theirs" (NOT per-field) | v50-06-02 | Substrate API engine.resolveConflict is per-row; per-field would need new engine surface OR UI-side merge plumbing. Diff display still per-field (informational). Matches GitHub/Figma merge UX. Per-field deferred to v50-06-03+ if conflict patterns demand granular merge |
| FirestoreAdapter.readDoc added to interface (vs new engine API) for reconciliation modal diff | v50-06-02 | Keeps engine class lean; mirrors v50-06-01 CommitResult{updatedAt?} interface-extension precedent. Adapter singleton tracked in init.ts alongside engine; getSyncAdapter() exposes it to UI without engine API expansion |
| Engine-state-driven modal: subscribe via useSyncStatus selector + useLiveQuery on outbox status (NOT engine event listener) | v50-06-02 | Provider auto-opens on conflict transition; user-dismissable; SyncIndicator action button re-opens via useReconciliationModalOptional fail-soft hook. Reusable pattern for future engine-state-driven UI |
| Snapshot listener bypasses applyEdit + outbox (direct db.put) | v50-06-03 | Going through applyEdit creates a feedback loop (server delivery → outbox row → engine drain → re-write). Server data is authoritative; engine drain remains the only write path. Mirrors SetlistGridHydrator's idempotent priming pattern for live deliveries |
| Outbox-pending guard via .filter() table-scan (not compound index) | v50-06-03 | Outbox is small (<~50 rows); compound index would force Dexie v(3) schema bump per v50-04 'additive non-indexed only' rule. Filter scan negligible vs. schema migration cost |
| Snapshot listener mounts in SetlistGridHydrator (per-route lifetime), NOT init.ts (app-global) | v50-06-03 | Per-route lifetime matches setlistId scope; init.ts stays for app-global concerns (engine + adapter). Future route consumers (perf view post-v50-07) opt in the same way |
| SnapshotSubscriber test-seam interface over wrapping firebase/firestore directly | v50-06-03 | Unit tests inject hand-rolled fakes — no firebase mocks, no module mocking gymnastics. Production wiring is a 30-line factory inside the same module. Pattern reusable for any future server-authoritative live data feature |
| Block B (sequential offline drain) drops expectedUpdatedAt threading from PLAN AC-5 | v50-06-03 | Single-writer offline sequential edits with threaded preconditions self-conflict on reconnect (rows 2..N's baseline=initial, server=ts1, → VersionMismatch). Test isolates per-doc ordering invariant from that gap; gap documented + routed forward as additive plan if real-world airplane-mode patterns demand fixing |
| Perf-view audit Outcome 2 — defer migration + read-side bridge to v50-07 | v50-06-03 | useSetlistPerformance reads legacy setlists/{id}.tracks[]; v50-05-01 writes top-level tracks/{id}; production split-brain is acceptable per band-not-in-production constraint; v50-07 is the migration phase. Routed forward as explicit deliverable, not "nice-to-have" |
| MARKER_PATH must be 2-segment doc, not 3-segment collection (Firestore enforces alternation) | v50-07-02 | Pre-existing migrate-v50.ts bug (`system/migrations/v50` → 3-seg = collection, not doc). Tests used a fake adapter that didn't enforce real Firestore segment alternation. Migration MARKER_PATH conventions now: 2-seg `system/{name}` doc; per-item snapshots use 4-seg `migrations/{name}/snapshot/{id}` doc |
| Option C Hybrid Lazy Hydration over up-front bulk migration | v50-07-03 | Only 24 legacy setlists; band not in production; bulk-script migration adds ops complexity for marginal benefit. App-layer lazy-hydration cascades through the existing v50-03 sync engine on first edit-open — no special migration code path, automatic rollback (just don't open the setlist), idempotent across mounts via fanoutStartedRef + hydrated flag |
| Lazy hydration: Promise.all fan-out + final hydrated:true update via existing applyEdit path | v50-07-03 | Each applyEdit owns its own Dexie tx; outbox drain handles per-doc ordering; one-shot system migration cascade uses withoutUndo:true (not user intent → not on undo stack); expectedUpdatedAt threaded on the final update so it doesn't race a concurrent leader edit on the setlist doc |
| Dual-read prefers top-level when length > 0 (NOT when hydrated:true) | v50-07-03 | Subscription delivery is racy with the hydrated flag; preferring data over flag means perf-view follows actual arrived data without waiting for the marker. Smoother service-time experience |
| Test-seam props with production defaults (`applyEdit`, `startSnapshotListener`) | v50-07-03 | Established by v50-06-03; prevents test-only branches in production code; kitchen-sink + unit tests assert the cascade without booting Firestore/Dexie outbox |
| Bare pump() quiesce instead of clock.advance for chaos property tests | v50-07-04 | Driving FakeClock through backoff retry timers ran away in tight loops when VersionMismatch kept firing under cross-tab + lazy-hydration interaction (4-op fast-check counterexample). Bare pump() drains pending without firing timers; failed/pending rows still observable in outbox = AC-9 satisfied. Pattern reusable for any test composing failure modes prone to retry storms |
| Per-iteration safety timeout via Promise.race on fast-check property | v50-07-04 | Without it, runaway shapes time out the entire test (240s) and hide the trigger. With it, fast-check sees the iteration as a property failure and shrinks to the offending input. AC-2 measurably improved; future regressions in engine drain surface as readable counterexamples |
| Cross-tab race simulation via direct SharedRemote mutation (not second engine) | v50-07-04 | Two-engine scenarios add ~80 LOC of setup; direct mutation that bumps remote.updatedAt achieves the same invariant trigger (next local update with threaded expectedUpdatedAt → VersionMismatchError → 'failed' outbox row → engine 'conflict'). v50-06-01 already covers genuine two-engine race; kitchen-sink only needs the trigger |
| Centralized captureSyncFailure wrapper over direct Sentry.captureException calls | v50-07-05 | Tag/level/extra shape would drift across 6+ direct call sites otherwise. ~70 LOC of helper + types beats 6 copy-pastes that subtly diverge. Single source of truth for tag conventions |
| Tag values string-coerced inside the Sentry helper | v50-07-05 | Sentry's tag indexer requires strings; numbers like attempts:5 must become '5' for filter dropdowns to work. Call sites pass natural types; the wrapper handles coercion. Undefined/null fields dropped from tags (no "undefined" string leak) |
| Telemetry wraps own Sentry.captureException in try/catch | v50-07-05 | Engine MUST NOT crash because Sentry is uninitialized (in tests, when DSN env is absent) or because the SDK throws on transport failure. "Sentry throws → captureSyncFailure swallows" asserted by unit test |
| Capture on dead-letter (5th attempt → 'failed'), NOT on every retry | v50-07-05 | Per-attempt would alert-fatigue. The dead-letter signal IS the user-visible failure (their edit didn't land at all after retries). Sentry dashboard stays signal, not noise |
| Do NOT capture 'conflict' state transitions to Sentry | v50-07-05 | Conflict is user-facing UX (reconciliation modal opens), not backend failure. The reconciliation modal IS the response. Capturing would alert-fatigue on legitimate two-writer races |
| Do NOT capture payload contents to Sentry | v50-07-05 | Only stable identifiers (setlistId, docId, op) reach Sentry. User-authored notes / song titles deliberately omitted (PII discipline). Pattern carries to all future v5.x instrumentation |
| Locked Option B Comfortable Dense for editor density (44/48 outer rows) | v51-02-01 | DESIGN-CONTRACT.md surfaced 3 options via /ui-ux-pro-max consultation; B chosen for meaningful density tightening + section framing + tablet tap comfort + lowest implementation risk + redundant tier hierarchy cues (weight AND color). Future tier swaps are single-line edits to TIER1_TITLE/TIER2_KEY/etc. constants in SetlistGrid.tsx |
| Section-row detection uses isSectionRow(t) covering 'header' OR 'section' | v51-02-01 | TrackType union (src/types/models.ts) defines 'header'; TypeCell picker writes 'section'. Pre-existing mismatch defensively double-checked rather than fixed — future plan touching the type column should reconcile (out of v51-02 scope per boundaries) |
| Skip shadcn Tooltip dependency in CreationWizard offer strip | v51-03-01 | Tooltip primitive absent from src/components/ui/. AC-5 explicitly allowed hide-with-explanatory-text alternative. Avoiding a new dependency for a single disabled-state caption keeps the no-dep budget. If a future phase needs Tooltip broadly, install via `npx shadcn@latest add tooltip` and revisit |
| Service-type matching is a pure exported helper (`setlistMatchesServiceType`) | v51-03-01 | The riskiest part of v51-03 is the `templateType` (6 values) → `ServiceType` (11 values) resolution + festival-bucket fan-out + legacy fallback to `getServiceContext(eventDate)`. Extracting it as a pure function lets 6 of 13 tests run zero-mock against this logic. Pattern carries to future date-aware features that need to classify setlists |
| Festival templateType matches sukkot / simchat_torah / passover / shavuot specific service types | v51-03-01 | Legacy data shape: `templateType` was added with only 6 buckets including `'festival'` as a catch-all; ServiceType later expanded to 11 specific holiday types. Treating `'festival'` as a multi-match bucket means a Sukkot user request matches an old festival-tagged setlist without requiring a data migration. `'other'` continues to fall through to inference (legacy compatibility) |
| Mode auto-defaults to 'clone' only when current mode is 'idle' | v51-03-01 | `handleTemplateSelect` calls `setMode('template')` BEFORE `setEventDate(baseDate)` so when the eventDate effect fires, it sees mode≠'idle' and skips the auto-flip. Prevents "user picked a template, lookup fired, mode flipped to clone" footgun. Future date-driven affordances should follow the same "respect explicit user intent" rule |
| Add `testId` prop to components with label-derived testids before renaming labels | v51-04-01 | When BulkPopover's testid stem was auto-derived from `label.toLowerCase()`, renaming `label="Lead"` → `label="Vocal Lead"` broke `batch-action-lead-trigger` (became `batch-action-vocal lead-trigger` with embedded space). Adding an additive `testId?` prop with `idStem = testId ?? String(label).toLowerCase()` resolution lets the user-facing label change while explicit consumers (testId="lead") preserve the test seam. Pattern: any future component label rewrite in label-derived-testid components adds the override prop FIRST, then renames |
| Codify Daniel-loop UAT cadence in PROJECT.md as a project-level discipline | v51-04-01 | After v5.0-hotfix track-edit save-loss (kitchen-sink harness 1468/1468 green missed missing tracks/{id}+songs/{id} firestore.rules; Daniel UAT against real production caught it as path "P"), institutionalize the UAT cycle for every data-flow-touching fix. The harness proves bulletproof under the property-test grammar; Daniel-loop UAT proves bulletproof under the actual weekly worship workflow. Connects to the user's "bulletproof and easy and intuitive" requirement before band onboarding |
| Daniel-loop UAT validated by surfacing v5.3 save-loss recurrence DURING research-phase capture (not post-deploy hotfix) | v53-01-01 | First validation of the codified discipline against a research-phase UAT (vs. post-execute UAT). Caught a v5h-01-class bug before any v5.3 code shipped → triggered RESCOPE to insert v5h3 hotfix. Pattern: if research-phase UAT surfaces NEW high-severity findings outside plan scope, synthesis MUST recommend rescope (not approve, not round-2). Production state capture is the disambiguator and belongs in the inserted hotfix's reproduce phase, not in extended research |
| Old-editor archaeology = git-spelunk deletion commit + verdict table (RECOMMENDED / REJECTED / DEFERRED) anchored to postmortem-cited risks | v53-01-01 | Track B established the format: Pattern \| Old SHA \| What-it-did-well \| Risk-if-ported \| Verdict. Anti-pattern callouts MANDATORY (cite postmortem). Reusable for any future amputation/rebuild milestone — there will be more (the "skibidytoiletrizz" regret about new editor UX is itself a v50-amputation aftermath; future amputations will have the same archaeology question) |
| When research surfaces a one-root-cause-two-surfaces finding, bundle the fix | v53-01-01 | AddRow no-suggestions and ChartBind picker filter share identical useLiveQuery + cmdk value pattern (ChartBindPopover.tsx:63-67/123 vs. AddRowPlaceholder.tsx:43-47/138). Smallest-fix path covers both surfaces in one commit. Pattern: when 2+ surfaces have identical substrate code, fix at substrate; do NOT split into per-surface plans |
| Long-press disambiguation as defense-in-depth on tappable elements near v50-05-04 row-context-menu surfaces | v53-03-01 | AddBar's chevron + every tile + primary AddRowPlaceholder trigger carry explicit `onContextMenu={(e) => e.preventDefault()}` even though AddBar lives outside row scope (positional analysis says no conflict). 4-line edits = cost zero; bug from collision is hard to catch in unit tests. Reusable pattern: any tappable affordance near a row-contextmenu synthesizer gets explicit preventDefault — trust positioning is not enough |
| When boundary-locked substrates produce structurally-mandated axe rule violations, document the rule disable in-test with rationale (vs. mutating the substrate) | v53-03-01 | Radix Popover.Content auto-applies `role="dialog"` triggering `aria-dialog-name`; chevron trigger + inner role='grid' both carry aria-labels. TouchOrPopover is v51-01 boundary-locked → cannot add `contentAriaLabel` prop. Disable `aria-dialog-name` rule in chevron-open scan with documented rationale; rest-state scan keeps full ruleset. Pattern reusable for any future axe scan of boundary-locked Radix popovers |
| Recent / Library / Custom three-CommandGroup picker is canonical for multi-source cmdk pickers | v53-03-01 | Second consumer (after v53-02 ChartBindPopover) of the pattern; AddRowPlaceholder reuses it verbatim for the primary "+ Song" CTA. Pattern: any future cmdk picker that needs frequency-based + alphabetical + free-text choice surfaces follows this template (cap 5 on Recent; sort by `recent[0].performedAt` desc; cmdk shouldFilter narrows all groups together; `value={song.title}` only — no id concat) |
| Split-button + chevron-popover is canonical for polymorphic affordances | v53-03-01 | Primary CTA (cmdk picker for the dominant choice) + sibling chevron Popover (tile grid for discrete alternatives) inside a single flex container with `border-l border-white/10` separator. Tile grid uses `role="grid"` + `aria-label`; each tile is `<button role="gridcell">` with min-h floors (48 fine / 56 coarse) + 8px gap + colored Lucide icon + bold label below. Reusable for any 4-8 discrete option grid where icon-color contributes a discoverability signal |
| songs/{libId} = library_index doc id directly | v54-01-01 | Back-stitch trivializes (`track.fileId === song.id === library_index.id`); v50-04's "distinct songId" was theoretical, never shipped. All future song-catalog work assumes 1:1 mapping with library_index for chart-shaped rows. Eliminates a class of future migration headaches. |
| MIME-type filter at bootstrap edge (PDF + MusicXML only; non-chart entries excluded) | v54-01-01 | library_index contains 19 folders + 57 audio + 8 docs + 4 octet-stream + 3 spreadsheets etc. mixed with charts; bootstrap-songs filtered 89 non-chart entries out at the edge so `songs/*` stays chart-only. Pattern: when a Drive-backed inventory mixes file types, filter at the bootstrap edge by MIME, not at every consumer. |
| Bootstrap-script template: firebase-admin + MigrationFirestore reuse + 2-segment marker doc + snapshot collection | v54-01-01 | Reusable for future library/data backfills. `system/{operationName}` marker for idempotency; `migrations/{operationName}/snapshot/{docId}` for sticky-memory-safe rollback. Mirrors `migrate-v50.ts` shape — codified as the v5.4 bootstrap pattern. |
| Single-source invariant: writes enforce it, reads do not fall back | v54-01-02 | When v54-01-01 locked `songs.id === songs.fileId`, the picker write paths had to mirror BOTH fields explicitly — falling back at the read site (SetlistRow.tsx:47) to `songId` would have silently masked future invariant violations. Pattern: write-time invariant enforcement > read-time fallback. |
| trackCount reconciler in SetlistGridHydrator (not in each track-mutating handler) | v54-01-03 | Single coupling point fixes ALL legacy setlists automatically on next open; no engine touch; 800ms debounce + ref-tracked last-written prevents listener-echo write loop. Pattern reusable for any derived-state field that's currently stale on legacy docs. |
| Bundle local-state side-fixes when an adjacent phase exposes them | v54-02-01 | `package.json` 0.2.6 → 5.4.0 + `update-build-info.js` skip-bump on shallow clones bundled with the emulator infra phase. Closes deferred "Pre-existing local-only state cleanup" without spinning up a separate phase. Pattern: opportunistic bundling when an unrelated phase touches the same surface incidentally. |
| Harness phases can split when local-prereqs block iteration | v54-02-01 | Plan 01 (infra, reviewable standalone) shipped; Plan 02 (canary, needs iteration against Java install that's unavailable locally) deferred. Counter NOT reset because "infra alone" doesn't satisfy the binding semantics. Pattern: separate infra from iteration when prereqs block; defer the iteration-heavy plan; ship the standalone-reviewable infra. |
| Halt code; run a writes/reads/sync/hot-paths audit when patches reveal a half-migrated data model | v5.4 milestone (2026-05-12) | After 8 P0 patches in a morning, user halted patching: "stop with the bandaids." Four scope-narrowed parallel audit agents + one synthesizer produced the v6.0 design doc. Pattern: when band-aids reveal a structural gap, freeze code, run scope-narrowed audits in parallel, synthesize before writing new code. |
| Fold-forward labels on milestone reconciliation | v5.4 → v6.0 close | Deferred-but-relevant phases get explicit reassignment in the new milestone (v54-02-02 → v60-03; v54-03 → v60-09; Mobile AddBar → v60-10) instead of staying orphaned in the old milestone's deferred bucket. Pattern: when a new master plan supersedes an in-progress milestone, fold deferrals forward with explicit labels rather than cancelling them. |
| Browser-smoke before phase close mandatory from v6.0 onward | v6.0 design doc | User instruction made permanent: vitest green is necessary but not sufficient; each phase ships a Daniel-runs-this-in-Safari checklist as AC-N. Antidote to band-aid drift. |
| Silent last-write-wins on conflict retry (sole-admin app) | v6.0 design doc | True two-writer conflicts essentially impossible on a sole-admin app; user fatigue with modals takes priority over edge-case fidelity. Sentry capture for visibility, no user-facing surface. |
| Per-row policy flag (`forceLwwOnConflict`) for one-shot engine behavior override (NOT a new SyncEvent or state transition) | v60-01-01 | Keeps state-machine.ts boundary-locked; user-initiated retry opts INTO silent LWW via a flag on the outbox row that the engine's VersionMismatch branch consumes. v60-08 deletion is a clean ALTER (no FSM unwind). Pattern reusable for future user-initiated drain overrides (e.g., explicit "discard local" → `forceDiscardOnConflict`) without growing the event set. |
| v60-01 silent-LWW branch runs BEFORE the legacy-stamp self-heal in engine.ts VersionMismatch handler | v60-01-01 | If a manual retry hits a legacy unstamped doc, user intent (overwrite) wins over auto-heal-once policy. More deterministic; cleaner mental model: user-initiated path takes precedence over auto-heal. |
| `'conflict-resolution'` SyncFailureFeature is warning level (NOT in ERROR_LEVEL_FEATURES) | v60-01-01 | Expected behavior on sole-admin app per locked decision #4 — not an incident. Telemetry lets Daniel monitor frequency; if it ever ticks above daily, the sole-admin assumption is breaking and the conflict path needs richer UX. Don't page on these events; keep them observable but quiet. |
| Auto-fix bundling at the type-system boundary counts as essential, not scope creep | v60-01-01 | Adding `'conflict-resolution'` to `SyncFailureFeature` union was a one-line type-system extension necessary for the engine's `captureSyncFailure` call to compile. v54-02-01 AuditAction extension precedent confirmed this is not scope creep. Pattern: type-system extensions at boundaries are essential auto-fixes; document in SUMMARY but don't split into a separate plan. |
| Bump content-hash `cacheVersion` in the same change that alters rendering logic | v70-01-02 | When the print-pipeline image SKIP guard was replaced with real embedding, `computeContentHash` `cacheVersion` was bumped 2→3 in the same commit — the Storage result-cache is keyed on rendering logic, so a stale-shape cached PDF (image tracks missing) is a silent correctness bug. Pattern: any change to PDF rendering output bumps the cacheVersion constant. |
| Image print-page layout: 18pt margin, aspect-fit, centered, no caption | v70-01-02 | /ui-ux-pro-max verdict — musicians read charts on stage, so maximize chart size (rejected the plan's proposed 36pt margin); never crop (loses bars/lyrics); no title caption (house style is max-density/no-decoration and the cover page already lists title/key/lead). Consistent with edge-merged PDF chart pages. |
| `embedImageTrack` returns boolean, never throws — one bad image can't kill the packet | v70-01-02 | The entire fetch+decode+embed body is wrapped in one try/catch; `embedJpg`/`embedPng` decode throws (CMYK, progressive, corrupt) are caught, logged with a structured warn, and return `false`. Mirrors the PDF merge path's per-track resilience. Audit must-have. Pattern: per-item embed helpers in a batch pipeline degrade gracefully + observably. |
| Server-side consumers of track file-type must backstop via `library_index.{fileId}.mimeType` | v70-01-02-fix | Picker/chart-binder track docs carry neither `mimeType` nor `fileName` (only `{id,title,fileId,type}`) — so `print-pipeline.isImageTrack()` had no signal for Drive-synced image charts and silently dropped them. Fix: batched `db.getAll()` of `library_index` mimeTypes before per-track routing — server-side parallel of v70-01-01 Task 4's client-side `useLibraryStore` backstop. Pattern: never trust the client-forwarded track type signal; resolve from the authoritative collection. |
| `recordings/{id}` = NEW top-level collection mirroring `songs/{id}` rules; optional `songId` FK; own `recordings/` Storage prefix | v70-02-01 | v7.0 constraint #5 — recordings are NOT embedded on song/setlist docs. Rules mirror songs (read isMember, write band-leader/admin) — band-internal, not public-perform-view. `songId` optional so a recording can exist standalone or song-linked. Separate Storage namespace keeps the chart sync engine away from recordings. Composite index (songId+createdAt) shipped with the foundation, not deferred. |

---
*PROJECT.md — Updated when requirements or context change*
*Last updated: 2026-05-14 after Phase v70-08 ✅ COMPLETE (v7.0 Wave 5 — best-practice audit + remediation; 4 plans / all LOOP COMPLETE) — **v7.0 MILESTONE COMPLETE, close-eligible**. v70-08-01 the audit: 5 parallel scope-narrowed dimension agents (security / accessibility / performance / code-quality / UX-consistency) → synthesized `v70-08-AUDIT.md` (0 P0 · 9 unique P1 · 22 P2 · 15 P3) — the v7.0 surface held up well, zero P0. v70-08-02 import-route hardening: `band_leader` role gates on extract-document/extract-structure/resolve (the audit's #1 P1 — a billed-Gemini + library-enumeration surface open to any signed-in member), `eventDate` Date.parse schema validation, real Zod schemas (`ResolvedTrackSchema`/`ParsedItemSchema`) replacing `z.array(z.any())`, early MIME gate + 50-page PDF cap, and `recordings/file/[id]` fixed properly — new `verifySessionCookieRequest` verifies the HttpOnly `__session` cookie (a real auth boundary, replacing the forgeable `Sec-Fetch-*` heuristic). v70-08-03 ImporterModal accessibility + UX: file dropzones converted to keyboard-reachable `<label htmlFor>` + `sr-only` input (the cascading P1 — the whole doc flow was keyboard-dead), doc-aware processing copy, "Start over" non-destructive interview back, `bg-brand` CTAs, date-fns preview date, zero-track commit guard, `<select>` focus ring, MobileRowCard "Lead"→"Vocal Lead", RecordingBindPopover `<audio>` aria-label + `loaded`-flag loading row; `/ui-ux-pro-max` invoked; new `ImporterModal.a11y.test.tsx`. v70-08-04 doc-import performance: `getServerLibraryLean()` (`.select()` projection + 60s module-TTL cache for the resolve hot path), resolve-fetch timeout + a shared `AbortController` (closing the modal cancels in-flight work), `commit-document` `maxDuration` + a single atomic batch write in `createSetlistServerSide`, `import 'server-only'` guards on the mammoth/heic-convert modules. next build ✓ EXIT 0 across all 4 plans; touched suites green; grid 41-failure dead-table baseline held; 2 essential auto-fixes (a stale MobileRowCard test label assertion; a vitest `server-only` no-op alias). Constraint 12 satisfied — the end-of-milestone audit ran and its P1s are remediated. Fold-forward to v7.1: ImageScoreViewer a11y, dead SetlistGrid TanStack-table block, duplicated Levenshtein matcher, 3-route chain collapse, touch-target sizing. Next: `/paul:complete-milestone` to close v7.0 + open the next milestone.*
*Last updated: 2026-05-14 after Phase v70-07 ✅ COMPLETE (v7.0 Wave 4 — interview form + setlist preview + commit; 3 plans / all LOOP COMPLETE). The doc-driven pipeline is feature-complete end-to-end: upload .docx/.pdf/.txt → extract → Gemini structure → resolve → structured interview form → read-only preview → a created, chart-bound setlist. v70-07-01 `src/lib/setlist-write.ts` (createSetlistServerSide + updateSetlistServerSide — the one server-side write path; MCP coordination point; interim-committed + pushed). v70-07-02 ImporterModal "Upload Document" option + extract→structure→resolve client chain + interview form (`interview-defaults.ts` helpers) + read-only grouped preview. v70-07-03 `src/lib/setlist-import/commit.ts` (commitDocumentSetlist — flatten resolved {sections,tracks} → interleaved headers + libraryMatch→bound chart → createSetlistServerSide) + `POST /api/setlists/import/commit-document` route + wired the preview "Create Setlist" button. next build ✓; emulator suite 56/56 (HFG coverage, counter 0/3 held); setlist-import 39/39; tsc clean. /ui-ux-pro-max BLOCKING satisfied (plans 02+03). 1 minor deviation (service-type inference keyword-only). Recording binding DEFERRED entirely (Daniel decision). Next: `/paul:plan v70-08` (best-practice audit + remediation — Wave 5; 4–5 parallel researcher agents; BLOCKS milestone close). v70-08 is the last phase before v7.0 milestone close.*
*Last updated: 2026-05-14 after Phase v70-06 ✅ COMPLETE (v7.0 Wave 3 — resolve + missing-chart + recording-match; single-plan phase / LOOP COMPLETE). Backend slice: `src/lib/setlist-import/resolve.ts` (resolveSetlistStructure — annotates v70-05's { sections, tracks } with libraryMatch | missingChart + recordingCandidates[]; mirrors import/parse's levenshtein/0.82 match; partitions library by mimeType; discriminated never-throws; PROPOSE-ONLY, zero Firestore writes per Daniel 2026-05-14) + `POST /api/setlists/import/resolve` route. next build ✓; setlist-import suite 26/26; no new deps; HFG N/A. doc → text → structure → resolve chain complete. Zero deviations. Next: `/paul:plan v70-07` (interview form + setlist preview + commit — Wave 4; /ui-ux-pro-max BLOCKING; **v70-07 authors the server-callable setlist-write module — ping Daniel to agree the signature before designing it, per the MCP-workstream handoff**).*
*Last updated: 2026-05-14 after Phase v70-09 ✅ COMPLETE (out-of-sequence polish jump — setlist metadata editor; single-plan phase / LOOP COMPLETE). `SetlistMetaEditSheet.tsx` — Sheet form (name/eventDate/serviceType/rabbi) opened by a pencil button in SetlistGridTopBar; Save → changed-fields-only `applyEdit('update','setlists',…)` patch (eventDate as ISO string); SetlistGrid useLiveQuery on the setlist doc → live header. Closes Issue 2. NO engine touch / NO new deps / NO new API route. next build ✓; 10 new tests PASS; grid suite zero new regressions vs the 41-failure dead-table baseline. /ui-ux-pro-max consulted. Zero deviations. NEW: parallel MCP workstream active (Daniel handoff 2026-05-14) — milestone work stays in the setlist-component / setlist-import / setlist-write-path lane; do NOT touch src/app/api/mcp, src/lib/mcp, settings MCP block, bridge/, mcpTokens in firestore.rules. v70-07 coordination point: the server-callable setlist-write module is authored in v70-07 and consumed by MCP write tools — agree the signature with Daniel first. Next: back to roadmap sequence — `/paul:plan v70-06` (resolve + missing-chart + recording-match).*
*Last updated: 2026-05-14 after Phase v70-05 ✅ COMPLETE (v7.0 Wave 3 — Gemini structured extraction; single-plan phase / LOOP COMPLETE). Backend slice: `src/lib/setlist-import/extract-structure.ts` (extractSetlistStructure — raw doc text → geminiFlash() → Zod-validated `{ sections[], tracks[] }`; discriminated never-throws StructureResult; malformed/empty/gemini_error carry raw model output for human review; optional fields `.nullish()` → normalized) + `POST /api/setlists/import/extract-structure` JSON route (sibling of extract-document/parse/execute; 200/422/502 mapping). NO UI / NO library-recording resolution / NO persistence (v70-06 + v70-07 scope). No new deps. next build ✓; extract-structure 9/9, full setlist-import suite 18/18 (no regressions); HFG 0/3 held; /ui-ux-pro-max N/A (backend-only). Zero deviations — plan executed exactly as written. doc → text → structure chain now complete. Next (Daniel-directed jump 2026-05-14): `/paul:plan v70-09` (setlist metadata editor — edit setlist name/date; /ui-ux-pro-max BLOCKING), then back to v70-06 in sequence.*
*Last updated: 2026-05-14 after Phase v70-04 ✅ COMPLETE (v7.0 Wave 2 — doc upload + text extraction; single-plan phase / LOOP COMPLETE). Foundation slice: `src/lib/setlist-import/extract-document.ts` (extractDocumentText — .docx via mammoth / .pdf via the shared server-side pdfjs loader / .txt trivial; discriminated never-throws ExtractResult) + `POST /api/setlists/import/extract-document` multipart route (sibling of import/parse + import/execute). NO UI / NO Gemini / NO persistence — the doc-import UI extends the existing ImporterModal in v70-05/v70-07. One new dep: mammoth@^1.12.0. `getPdfjs` exported from pdf-chord-extractor.ts for shared server-side reuse. next build ✓; extract-document 9/9 + pdf-chord-extractor 16/16; HFG 0/3 held; /ui-ux-pro-max N/A (backend-only). One qualify deviation: the .pdf test mocks pdfjs (jsdom can't run real pdfjs — the sibling test mocks it identically; plan's permitted fallback). Unblocks v70-05. Next: `/paul:plan v70-05` (Gemini structured extraction — Wave 3).*
*Last updated: 2026-05-14 after Phase v70-03 ✅ COMPLETE (v7.0 Wave 2 — per-track media affordances; 2 plans / both LOOP COMPLETE). v70-03-01 chart click-through: MobileRowCard's chart indicator → `<a target="_blank">` to the existing `/api/drive/file` serving URL when bound; `stopPropagation` isolates it from the card tap-to-edit; unbound icon unchanged. v70-03-02 recording-bind UI: `RecordingBindPopover` + `RecordingCell` (AudioLines) wired into MobileRowCard beside the chart icon — lists a song's recordings newest-first with inline `<audio>`, band-leader/admin upload; backed by new `/api/recordings/upload` + `/api/recordings/file/[id]` routes + `recordings-client.ts` + `uploadRecordingToStorage`/`downloadFromStoragePath` helpers, all on the v70-02 model with NO storage.rules change (admin-side serving route IS the access control). Both plans re-spec'd mid-flow after discovering the original target (SetlistGrid's TanStack table) is dead code — MobileRowCard is the sole live render path; dead table block flagged as tech debt. next build ✓; live-path grid tests green; HFG 0/3 held (no engine/data-layer touches). /ui-ux-pro-max BLOCKING gate satisfied both plans. NEW standing pattern: human-verify checkpoints no longer block APPLY — they accumulate in `.paul/UAT-PENDING.md` for milestone-end verification (feedback-uat-checklist memory). Next: `/paul:plan v70-04` (doc upload + text extraction — Wave 2).*
*Last updated: 2026-05-14 after Phase v70-02 ✅ COMPLETE (v7.0 Wave 1 foundation — recordings data model; single-plan phase / LOOP COMPLETE). NEW `recordings/{id}` Firestore collection: Recording type (songId? FK + notes attribution), getRecordingStoragePath helper (recordings/{id}.{ext}), rules block mirroring songs/{id} + composite index (songId+createdAt) — both deployed to production. Emulator rules test 10/10; HFG counter held at 0/3 (no clause-(b) waiver). next build ✓; suite 1650/52 (zero new regressions). Boundary-clean additive slice — no upload route / UI / lib helper (deferred to v70-03 / v70-06 by design). Also this session: v70-01-02 UAT follow-up fix (library_index mimeType backstop in print-pipeline — image charts now embed; commit 1fef342) closed Daniel's "PNG not in printed packet" report. Next: `/paul:plan v70-03` (per-track media affordances — Wave 2; /ui-ux-pro-max BLOCKING) OR pull v70-09 (setlist metadata editor) forward for Issue 2.*
*Last updated: 2026-05-14 after Phase v70-01 ✅ COMPLETE (v7.0 Wave 0 foundation — image-chart support; 2 plans / both LOOP COMPLETE). v70-01-01 shipped upload + view + toolbar-guard (heic-convert, ImageScoreViewer, transposer disabled with tooltip, three-layer mimeType→extension→useLibraryStore detection); v70-01-02 shipped print embed (embedImageTrack — 18pt-margin aspect-fit page, personal-route fileName/mimeType propagation, PrintModal banner removed, cacheVersion 2→3) — enterprise-audit-hardened (try/catch around pdf-lib decode throws, graceful degradation with observable logger.warn, AC-6 + degraded-path test, 25MB upload-cap as the documented image ceiling). next build ✓; print-pipeline 26/26 + print-modal 23/23; full suite 1649/52 (52-failure baseline matches v60-12 exactly — zero new regressions). HFG 0/3 held (no engine touches). /ui-ux-pro-max BLOCKING gate satisfied both plans. Image charts now end-to-end: upload + view + toolbar-aware + print embed — unblocks the v70-05 May 15 `dodi li (sher).png` canary. PENDING-UAT carry-forwards (v51-04 pattern, 8th use): v70-01-01 AC-3/AC-4 + v70-01-02 print human-verify against deployed commits over the worship cycle. Next: `/paul:plan v70-02` (recordings data model — Wave 1 foundation).*
*Last updated: 2026-05-12 after Phase v60-01 ✅ LOOP COMPLETE — PENDING-UAT (v6.0 Wave 1, 1 of 10 phases). SyncIndicator conflict click rewire + silent last-write-wins-on-retry — closes Daniel's #1 fatigue surface (latched "Conflict — review" pill now that reconciliation modal is force-disabled). Per-row `forceLwwOnConflict` policy flag on `OutboxRow`; engine's new VersionMismatch branch (BEFORE legacy-stamp self-heal) consumes the flag for silent LWW + Sentry capture + DRAIN_RETRY_PENDING → DRAIN_OK. State-machine.ts / snapshot-listener.ts / write.ts / firestore.rules untouched. 36/36 tests in v60-01-touched files green (cleanup 7/7 / engine 15/15 / SyncIndicator 14/14); `check:types` exit 0. HFG counter unchanged at 1/3 (additive branch inside existing self-heal scope). PENDING-UAT carried forward — Daniel browser-smoke checklist appended to PLAN.md for execution against deployed commit (v51-04 codified pattern, 6th consecutive use). Auto-fixes documented in SUMMARY: SyncFailureFeature union extension + `status: 'pending'` reset (latter caught by Qualify). Latent bug surfaced in adjacent legacy-stamp self-heal (same missing status reset); routes to v60-01-02 if production signal. Surfaced for v6.0 milestone triage: 52 pre-existing test failures in 10 files on master (NOT v60-01-introduced; confirmed via stash-pop regression test). Next: `/paul:plan v60-02` (pagehide/visibilitychange blur — mid-edit text protection, ~15 LOC; parallel-safe with v60-01) then Wave 2 v60-03 (Java install + emulator canary, BLOCKS Wave 3 engine phases).*

*Last updated: 2026-05-12 after Milestone v5.4 ✅ COMPLETE (Hotfix + Harness Fidelity). 2 phases shipped (v54-01 picker bootstrap + thead hotfix across 3 plans / v54-02-01 Firebase Local Emulator Suite infra) + 8 P0 sync/UX patches + 2 dead-code cleanups + architectural audit producing v6.0 design doc. Fold-forward to v6.0: v54-02-02 → v60-03 (Java install + canary) / v54-03 → v60-09 (cross-device library sync) / Mobile AddBar → v60-10. **`.paul/MILESTONE-CONTEXT.md` locks v6.0 — Tracks Single-Source-of-Truth: 10 phases in 4 waves; 12 decisions locked.** Closed with PENDING-UAT marker (v51-04 pattern, 5th milestone). v5.0 + v5.2 + v5.3 + v5.4 (incl. today's 8 P0 patches) all PENDING-UAT against deployed commits over upcoming worship cycle. HFG counter held at 1/3 carry-forward to v6.0 (resets at v60-03). Next: `/paul:milestone` to open v6.0 from MILESTONE-CONTEXT.md, then `/paul:apply v60-01` (SyncIndicator conflict click rewire) targeting Monday 2026-05-13 start.*
*Last updated: 2026-05-02 after Milestone v5.3 ✅ COMPLETE (4 phases / 7 plans / +69 tests within milestone / 1597/1597 suite at close). Editor UX Repair shipped end-to-end in single ~12h session: v53-01 recursive research → mid-milestone RESCOPE inserting v5h3-01 save-loss recurrence hotfix (4 plans incl. binding Harness Fidelity Gate codification at v5h3-01-04 postmortem) → v53-02 chart-bind picker fix + sticky-right ChartCell discoverability → v53-03 polymorphic Add menu (split-button + 5 colored tiles); v53-04 ❌ COLLAPSED at close. **Milestone closed with PENDING-UAT marker per Daniel "push and finish the milestone" judgment call** — explicit override of v51-04-codified "UAT closes the milestone" rule; UAT continues against deployed commits over upcoming worship cycle (Erev Shabbat + Shabbat morning); failures route to in-phase v5*-02 follow-up plans. Daniel-loop UAT discipline (codified v51-04) validated 3x in single milestone (research-phase capture / mid-execution pivot / sight-unseen approval contexts) — proven across 3 distinct invocation modes. Harness Fidelity Gate now BINDING from v5.3 onward; counter at 1 of 3 after milestone close (v53-02 used clause-(b) waiver; v53-03 unchanged); v5.4 phase 1 = Firebase Local Emulator Suite + RTL editor↔perf-view test pair. v5.0 + v5.2 milestones still in own PENDING-UAT close paths. Next: `/paul:discuss-milestone` to define v5.4 scope.*
*Last updated: 2026-04-27 after Milestone v5.1 ✅ COMPLETE (4 phases / 4 plans). Editor UX Polish band-onboarding gate cleared. Next: `/paul:audit-milestone v5.0` closes the parent v5.0 milestone (pending UAT since 2026-04-27 — v5.1 polish was the prerequisite work to make Daniel's full v5.0 UAT comfortable on iPad). Suite 1513/1513.*
*Last updated: 2026-04-27 after Phase v51-03 (Smart create-setlist wizard with date-aware Clone CTA)*
*Last updated: 2026-04-27 after Phase v51-02 (Editor readability + visual hierarchy)*
*Last updated: 2026-04-27 after milestone v5.0-hotfix COMPLETE (1 phase / 4 plans). Track-edit save-loss surfaced by Daniel UAT 2026-04-27 path "P" diagnosed + fixed end-to-end same day: missing firestore.rules for tracks/{id} + songs/{id} (v50-05 cutover gap) → E+F+B fix at commit `0c2921d` + perf-view architectural refactor at commit `92b1902` (Dexie via useLiveQuery + listener mount). Postmortem at `.paul/postmortems/v5h-01-save-loss.md` captures 5 lessons + 5 action items for v5.x. v5.0 milestone still 🟡 PENDING-UAT — close path is now: v5.1 UX overhaul ships → Daniel UAT smoke passes → `/paul:audit-milestone v5.0` closes. Suite 1481/1481.*
*Last updated: 2026-05-02 after Phase v53-01 ✅ COMPLETE (1 plan, research). 3 parallel research tracks (ChartBind diagnosis / old-editor archaeology / polymorphic Add design) + iPad UAT. Daniel UAT surfaced **save-loss recurrence** (same class as v5h-01) — RESCOPE: v5.3 milestone shape changed from [v53-01..04] → [v53-01 ✓ / v5h3-01 (NEW hotfix) / v53-02 / v53-03] (v53-04 likely collapses). Old-editor polymorphic Add menu (commit `d8c0442` `AddBar.tsx`) RECOMMENDED to port back to v53-03. Chart-verification peek DROPPED from v5.3 per Daniel. Next: `/paul:plan` v5h3-01-01 (reproduce + diagnose save-loss recurrence) — Daniel captures IndexedDB outbox + Safari Web Inspector state from this morning's affected setlist.*
*Last updated: 2026-05-02 after Phase v5h3-01 ✅ LOOP COMPLETE (4 plans / 4 plans) — PENDING-UAT (Daniel weekly worship cycle on `36e9fa1`). v5h3-01-01 research+diagnose (6→3 hypotheses by code-scan; HUMAN-ACTION deferred → autonomous mode); v5h3-01-02 auto-capture instrumentation (Sentry breadcrumbs at 5 hot write paths + IndexedDB `edit_log` + upload-on-mount; commit `1d8d94c`); v5h3-01-03 H-SL-7 phantom-VersionMismatch fix (engine writeback threads server `updatedAt` into pending outbox rows for same doc; commit `36e9fa1`; v50-06-02 reconciliation contract preserved); v5h3-01-04 postmortem at `.paul/postmortems/v5h3-01-save-loss-recurrence.md` + binding **Harness Fidelity Gate** subsection added to §Constraints (escalates v5h-01 §5 action item #2 from "opportunistic" → BLOCKING for any future data-flow phase; v5.4 phase 1 ship target). Daniel-loop UAT discipline validated 2x same-day: morning UAT surfaced recurrence; mid-execution UAT pivoted plan from "instrumentation only" → "ship targeted fix today". Suite 1528 → 1560 (+32). v53-02..04 soft-blocked behind v5h3-01 PENDING-UAT clear.*
*Last updated: 2026-05-02 after Phase v53-03 ✅ LOOP COMPLETE (1 plan / 1 plan) — PENDING-UAT (Daniel weekly worship cycle on `3a321c9`). Polymorphic Add menu (split-button) restored end-to-end on v50-05 spreadsheet substrate per Daniel-locked Option B at /paul:discuss-phase: primary indigo "+ Song" CTA opens v53-02-substrate cmdk picker (Recent / Library / Custom three CommandGroups; Recent ranking via existing v50-04 `SongRecentEntry.performedAt`; cap 5; cmdk filters all groups together; `value={song.title}` only); sibling chevron Popover reveals 5 colored tiles (Section muted / Reading amber-300 / Prayer blue-300 / Transition emerald-300 / Stage note muted) — one tap inserts row of that TrackType via single applyEdit('set','tracks',...) write path. Long-press disambiguation: explicit `onContextMenu` preventDefault on chevron + every tile + primary AddRowPlaceholder trigger (defense-in-depth against v50-05-04 row contextmenu synthesizer; positional analysis says AddBar lives outside row scope but cost-zero defense applied). New AddBar.tsx (~180 LOC) + AddRowPlaceholder.tsx modified (Recent group + "Song" trigger label with indigo Plus icon + onContextMenu) + SetlistGrid.tsx wired (handleAddTrackOfType + AddBar mount). Boundary clean: ZERO touches to sync engine, Dexie schema, snapshot-listener, lazy-hydration, perf-view, cells/, firestore.rules, ChartBindPopover, mobile parallel render path, admin panels. Suite 1575 → 1597 (+22 cases — comprehensive AC matrix coverage; not scope creep). jest-axe ZERO violations on AddBar (rest + chevron-open with `aria-dialog-name` disabled per documented rationale — TouchOrPopover v51-01-locked). **Harness Fidelity Gate counter unchanged at 1 of 3** — AddBar surface is OUTSIDE protected list; applyEdit signature unchanged. Daniel approved AC-8 sight-unseen with "do it" per v51-04 + v52-03/04 + v53-02 precedent. Single-context execution; no agent dispatches. /ui-ux-pro-max gate satisfied at APPLY entry (queried touch-target sizing + icon-color contrast on dark + ARIA Labels + focus states + split-button patterns). v53-04 collapse decision deferred to milestone audit or Daniel pull-in during UAT — original Track B-only-remaining-candidate (chart-preview port-back) died with chart-verify drop earlier today; net: v53-04 has zero remaining scope.*
*Last updated: 2026-05-02 after Phase v53-02 ✅ LOOP COMPLETE (1 plan / 1 plan) — PENDING-UAT (Daniel weekly worship cycle on `bc754b4`). Soft-block from v5h3-01 PENDING-UAT lifted earlier same day per Daniel "no block, keep building". Goal 1 systemic-fix path (cmdk value-format fix at ChartBindPopover.tsx:152,179 + AddRowPlaceholder.tsx:138; new Recent CommandGroup above Library cap 5 sorted by `songs.recent[0].performedAt` desc — derives from existing v50-04 fields, NO Dexie schema bump; new `src/lib/songs/prime.ts` library priming helper + SetlistGridHydrator fire-once-per-mount post-hydration effect with primedRef sentinel — one-shot getDocs, NO new snapshot listener). Goal 2 = sticky-right ChartCell column locked at checkpoint:decision after /ui-ux-pro-max consultation (z-index recipe: header z-20 / body z-5 / thead z-10 wins both vertical occlusion + horizontal sibling stacking; opaque bg + border-l visual seam; standard Excel/Sheets pin-column behavior; trade-off: row hover/selection tints hidden on chart cell only). Suite 1560 → 1575 (+15; jest-axe ZERO violations on sticky-right grid). **Harness Fidelity Gate FIRST PRODUCTION EXERCISE** — waiver counter 1 of 3 (SetlistGridHydrator priming-adjacent touch; additive one-shot getDocs; no engine path; UAT closes the gap; v5.4 phase 1 ticket open). Two more waivers in a row triggers re-prioritization to v5.4 phase 1. Daniel approved AC-7 sight-unseen with "Go" per v51-04 + v52-03/04 precedent. Two dan-executor agent dispatches (Task 1 Goal 1 vertical slice + Sub-task 2b sticky-right) per v52-05 / v5h3-01-02/03 precedent. v53-03 (polymorphic Add menu — port `AddBar.tsx` from `d8c0442`) unblocks; v53-04 likely collapses pending Daniel decision.*
*Last updated: 2026-05-08 after Phase v54-01 ✅ LOOP COMPLETE (1 plan / 1 plan) — PENDING-UAT (Daniel weekly worship cycle on `a693d23`). Inaugural v5.4 phase closed two regressions surfaced on v5.3 PENDING-UAT during Daniel-loop UAT same morning: (a) `+ Song` picker empty since v50-05 cutover — `songs/*` collection never bootstrapped from `library_index`; foreseen failure mode v50-07-01-DRY-RUN-REPORT.md:163 + v53-02-01-SUMMARY.md:212 / (b) spreadsheet thead overlapped first body row — v53-02-01's `overflow-x-auto` wrapper made the table ancestor a CSS scroll container, breaking sticky-thead viewport-pinning. New `scripts/bootstrap-songs.ts` (firebase-admin + MigrationFirestore reuse + 2-segment marker doc + snapshot collection for sticky-memory-safe rollback) populated 364 chart docs in production + back-stitched 385 setlist tracks via `track.fileId === songs/{id}` mapping; MIME-type filter (PDF + MusicXML only) added mid-phase when production dry-run revealed 89 non-chart entries (folders/audio/docs/spreadsheets) inflating the count from 366 expected to 455. Thead repair: removed `overflow-x-auto` wrapper, bumped `top-[3.25rem]` → `top-[3.75rem]` per v51-02-01-DESIGN-CONTRACT.md:18 lockstep, opaque header bg. /ui-ux-pro-max BLOCKING gate satisfied at checkpoint:decision (locked path-a sticky on th/td directly + literal top + extensions kept over path-b display:grid). Daniel approved with "go" override at checkpoint:human-verify (4th use of v51-04 codified pattern). Suite 1597 → 1615 (+18 bootstrap-songs tests). Harness Fidelity Gate counter unchanged at 1 of 3 (admin script bypasses sync engine; thead path-a touched no engine seams). Patterns established: v5.4 bootstrap-script template / MIME-type filter at bootstrap edge / hotfix-class /ui-ux-pro-max consultation pattern. Closes deferred v50-07-02b sub-phase. Remaining v5.4 phases: v54-02 Harness Fidelity Gate phase 1 (BINDING) / v54-03 cross-device library staleness fix + library_index↔songs/* continuous sync / mobile parallel-render AddBar variant / pre-existing local-only state cleanup.*
*Last updated: 2026-05-13 after Phase v60-09 ✅ LOOP COMPLETE (single-plan phase v60-09-01) — PENDING-UAT. Closes the v54-03 fold-forward cross-device library sync gap. New `subscribeSongsLibrary` snapshot listener at SetlistGridHydrator replaces v53-02-01's one-shot `primeSongsLibrary` — rename / archive / upload mutations propagate to all open clients via Firestore onSnapshot. Write-side parity: rename + archive + upload routes mirror title/normalizedTitle/status to `songs/{fileId}` via Promise.allSettled (non-fatal songs failure). LocalSong gains `status?: 'active' | 'archived'` field; picker filters `status !== 'archived'` at the Dexie query layer in ChartBindPopover + AddRowPlaceholder; archived hidden from BOTH Library and Recent groups. Sticky memory (recent[] + defaults) preserved through archive cycle (songs doc not deleted). Emulator-backed test (`subscribe.emulator.test.ts`) ships full round-trip coverage (5/5 GREEN: initial / modify / archive / delete / unsubscribe) — preserves HFG counter at 0/3 via real-Firestore coverage, no clause-(b) waiver consumed. tsc EXIT=0; next build `Compiled successfully in 12.3s`; vitest 1615/52 (+3 new mirror tests vs v60-08 baseline 1612/52, 52-failure pre-existing-orthogonal set preserved exactly). LOC delta ~+170 production (subscribe.ts new file dominates); AC-8 DRIFT approved-in-plan. /ui-ux-pro-max consulted at Task 1 entry: 3 verdicts (Dexie-layer filter / hide archived from Library + Recent / silent mid-session removal). 1 auto-fix during APPLY: emulator test pivoted from firebase/firestore client SDK to firebase-admin SDK (jsdom lacks streaming APIs — SubscribeAdapter abstraction made it a single-file swap). Patterns established: snapshot-listener helper with provider-agnostic DocChange[] (adapter swap allows test/prod SDK interchange); single-mount anchor with ref-guard; real-Firestore emulator coverage as HFG-preserving alternative to clause-(b) waivers for engine-adjacent listeners. **Wave 4 closing alongside v60-10** (Mobile AddBar variant, parallel session, committed first at `6288c97`); v6.0 milestone close eligible once both PENDING-UAT clear. Parallel-session coordination: zero file overlap by plan-time boundary contract; sequential commits — v60-10 committed first with PROJECT.md + its bundle, v60-09 rebased + committed second with STATE.md + ROADMAP.md capturing both sessions' metadata.*

*Last updated: 2026-05-13 after Phase v60-08 ✅ LOOP COMPLETE (single-plan phase v60-08-01) — PENDING-UAT. Closes the v50-05 → v6.0 tracks migration story end-to-end: top-level `tracks/{id}` is now the single source of truth in code (no reader fallback, no schema field, no interface field). Embedded-array fallback removed from `getTracksForSetlist` + `getTracksForSetlistClient` + `fetchTracksForSetlistClient`; `tracks` dropped from `setlistSchema` AND from the hand-defined `Setlist` interface; 22 consumer sites across 10 production files migrated to v60-06 denorms (`trackCount`/`fileIds`/`songCount`); `setlist-firebase` clone/duplicate/save-template paths refactored to fetch source tracks via helper before cloning; `matrix/route.ts` widened to local `SetlistWithTracks` type; track-title search predicate in `useSetlistDashboard` deleted (re-introducible via v60-06-04 Dexie-bulk pattern if needed). Backfill executed by Claude (`--apply`): 5 migrated / 5 skip-hydrated / 5 skip-empty / 0 errors; idempotency marker set. Spec-issue diagnostic at mid-APPLY: plan estimated ≤2 consumer sites; reality was 22 — Daniel approved expansion within this commit (option 2). Production delta -80 LOC net; total -110 across 26 files (cleanup plan — net-negative is the success signal). Suite 1612/52 — failure count matches v60-07 baseline exactly (-4 passing from deliberate test removal of dropped behaviors). tsc EXIT=0; next build `Compiled successfully in 6.8s`. HFG counter 0/3 held. /ui-ux-pro-max gate N/A (data-layer only). Patterns established: helper-fetch indirection for clone/duplicate paths; ABI-stable param retention (`_setlistData` void-cast); spec-issue diagnostic at mid-APPLY (pause, present scope reality, let user choose expand vs defer). **Wave 3 (v60-04..v60-08) fully closed.** **v60-09 + v60-10 (Wave 4) UNBLOCKED.***

*Last updated: 2026-05-08 after autonomous milestone batch — Daniel directive "Whatever you recommend. Continue. Autonomously. Through the entire milestone. Then push." Three things shipped: (1) **v54-01-02 perform-view fix-up** at `6735f48` (PUSHED mid-run) — UAT regression caught by Daniel ("can't click on charts after creating new setlist"); root cause: handlePickSong + handleBindChart wrote tracks with songId only, omitting fileId. SetlistRow.tsx:47 gates clickability on track.fileId. Per v54-01-01's locked invariant `songs/{id}.id === fileId === library_index doc id`, both write paths now mirror song.id to fileId. +2 regression tests; v51-04 same-phase routing pattern. (2) **v54-02-01 Firebase Local Emulator Suite infrastructure** — emulators block in firebase.json (Firestore + Auth), separate vitest.emulator.config.ts (glob `*.emulator.test.{ts,tsx}`; main suite excludes via new `exclude` array), `npm run test:emulator(:ci)` scripts using `firebase emulators:exec`, CI emulator-tests job (actions/setup-java@v4 temurin/21), one canary test proving wiring round-trip. (3) **build-info cleanup side-fix** — package.json `0.2.6` → `5.4.0` (eliminates regression baseline); `scripts/update-build-info.js` no longer recomputes version when shallow-clone makes git describe fail (preserves pkg.version as-is, breaks the regression loop). **DEFERRED:** v54-02-02 (H-SL-7 regression canary on emulator) requires local Java install for safe iteration; `winget install Microsoft.OpenJDK.21 --scope user` failed on this machine. v54-03 (cross-device library sync) deferred — would inherit clause-(b) waiver moving HFG counter to 2/3, AND v5h3-01 + v54-01-02 both demonstrate engine-seam phases need the harness gate first. Mobile AddBar variant deferred — frontend, /ui-ux-pro-max BLOCKING, needs UAT iteration. **HFG counter unchanged at 1 of 3.** Suite: 1615 baseline (v54-01-01) + 2 (v54-01-02 regression) = 1617 in main config; +2 emulator canary excluded from main; grid suite 173/173 serial. v5.0 + v5.2 + v5.3 + v54-01 + v54-02-01 all PENDING-UAT against deployed commits over upcoming worship cycle.*

*Last updated: 2026-06-08 after Phase v11-02b Org-aware token minting ✅ COMPLETE (1 plan). Self-service MCP mint paths now stamp the token's org from the minting user's orgIds claim (getPrimaryOrgForMinting) — tenant members onboard via plain login → correctly org-scoped bearer, no manual handoff; CRC unchanged. feat(v11-02b) `2db15f36d9` deployed; unit 9/9 + emulator 3/3. Inserted between v11-02 and v11-03 per Daniel's "fix it now." Next: v11-03 (domain + branding).*

*Last updated: 2026-06-08 after Phase v11-02 MCP org-scoping ✅ COMPLETE (4 plans / all LOOP COMPLETE). The MCP tenant wall (caller-org resolution + read isolation + write wall) shipped to prod as `feat(v11-02)` `c7da31ac2a`; David Lazaroff's `brotherslazaroff` bearer + `orgIds` claim issued on his existing account; live e2e 12/12 against www.centralreform.live/api/mcp proves full tenant isolation (BL reads/creates BL-only, cannot touch CRC; CRC unaffected) — Brothers Lazaroff is the first live second tenant. v11.0 at 2 of 5 phases. Next: v11-03 (brotherslazaroff.live domain + branding + vocab trim, /ui-ux-pro-max BLOCKING). UAT-PENDING: Daniel securely hands David the raw bearer (tokenId 93JMXhT1OspFsWDMmb9V) + David's UX confirmation.*

*Last updated: 2026-06-10 after Phase v11.3-01 Anon access correctness ✅ COMPLETE (2 plans / both LOOP COMPLETE). First phase of v11.3 Worthiness & Access (oracle docs/ACCESS-POLICY.md v0.3). BUG-5: /api/library/file/[id] reworked from a default-auth db-*-only route into a public chart proxy mirroring /api/drive/file (requireAuth:false + isTrusted Sec-Fetch/bearer gate + chart tier + fetchFileById for upload-*); db-* digitized_charts now anon-public. Verify-first (code trace + prod probe) refined BUG-5 → P2: Perform renders via /api/drive/file (anon-OK), so the blank-cold-device-render P1 escalation was unfounded; real fix = anon "open chart in new tab" on legacy db-* + the oracle anon-✅ contradiction; no client edit (parseFileId correct). BUG-4 (D-Q2): /api/library/chord-cache GET+POST + /api/ai/transposer/scan POST opened to anon (requireAuth:false); scan carries an anon-only ai-tier rate-limit (authed unchanged — it had no limiter → zero regression); anon scan results persist (cost control); PATCH/DELETE stay role-gated; no client edit (apiFetch sends anon, flow proceeds on 200). Decisions: (1) chord-cache POST anon-open so anon scans persist → fewer total Gemini scans; (2) rate-limit scan for anon only to avoid authed regression. Gates green both plans: tsc · route tests 6 (BUG-5) + 7 (BUG-4) green · SKIP_ENV_VALIDATION=1 next build exit 0. CRC byte-identical (authed/in-app trusted path unchanged). UAT-PENDING (live, non-blocking): anon db-* open-in-new-tab + cold-device upload-* Perform render; anon transpose renders chords; authed transpose unchanged. Phases 02–05 remain (agent chart-upload, hygiene, /perform perf, P3 polish).*
