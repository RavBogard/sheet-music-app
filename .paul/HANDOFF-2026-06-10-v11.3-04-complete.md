# PAUL Handoff

**Date:** 2026-06-10
**Status:** paused (clean — phase boundary; v11.3-04 shipped to prod + verified)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (centralreform.live) — multi-tenant worship-band app (CRC + Brothers Lazaroff). Daniel authors setlists via Claude + MCP; the browser app is the band/consumer surface (Perform mode on iPads). Next.js 16 + React 19 + Firebase (`crcmusiccharts`); git repo at the `sheet-music-app/` subdirectory; `.paul/` is inside it.
**Core value:** The band gets the right charts + recordings on their iPads each week; Daniel authors conversationally via Claude + MCP.

---

## Current State

**Milestone:** 🚧 v11.3 — Worthiness & Access (5 phases). Oracle: `docs/ACCESS-POLICY.md` v0.3.
**Phase:** **4 of 5 COMPLETE** → next is **v11.3-05 (P3 polish, the LAST phase)**. Plans: none active.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY     [v11.3-04 ✅ PHASE COMPLETE — at the v11.3-04 → v11.3-05 boundary]
  ✓        ✓        ✓
```

**Git:** `master`, tip **`c0b0ab3367`** (`feat(v11.3-04)`, pushed `558c34e2d6..c0b0ab3367`, Vercel deployed + LIVE). Push `origin master` (NOT `master:main`). Multi-computer → `git pull` first.
**Uncommitted:** post-commit edits to `.paul/STATE.md` (Git-State hash line + CLS-verified status) and `.paul/UAT-PENDING.md` (CLS marked verified) — normal doc-lag; ride the next commit.

---

## What Was Done (this session)

- **`/paul:resume`** → reconciled (the only post-handoff commit `558c34e2d6` was a docs-only pause), archived the consumed handoff.
- **Phase v11.3-04 /perform performance (BUG-2) — COMPLETE, all 3 plans PLAN→APPLY→UNIFY, shipped as `feat(v11.3-04)` `c0b0ab3367`:**
  - **v11.3-04-01 (VERIFY-FIRST, no code):** built `scripts/v11-3-04-webvitals-slice.mjs` (field-RUM slicer, n=1068) + synthetic 820×1180 iPad Playwright capture → `.paul/research/PERFORM-PERF-CHARACTERIZATION-2026-06-10.md`. **Reversed the hypothesis:** chart-image reflow REFUTED — the chart viewer (`/perform/setlist/[id]`) is healthy (CLS 0.010); the regression is the **`/perform` LISTING** (field p75 CLS 0.200 / TTFB 1633ms cold).
  - **v11.3-04-02 (CLS):** root cause = the anon QR sign-in card (`QRSignIn.tsx` in `PublicSetlistListing.tsx`) gated on `{!user && !authLoading}` mounted ~380px tall ABOVE the lists after client auth resolved, shoving them down (0.187). Fix = reserve the card slot during `authLoading` for expected-anon (gated on the existing `cachedUser` localStorage hint exposed by `auth-context`) via a card-height skeleton + `QRSignIn` consistent `min-h-[15rem]`. Authed returners get no reserve (no new collapse-shift); real card markup unchanged (CRC byte-identical). +`src/components/performance/__tests__/perform-cls.test.tsx`.
  - **v11.3-04-03 (TTFB/FCP):** `perform/page.tsx` was `force-dynamic` and `await`ed `getAllSetlists` before sending a byte. Fix = Suspense-stream the listing (flush `PublicSetlistSkeleton` shell, stream the async `PerformListing` child); query/org-scoping/`selectVisiblePublicSetlists` slice relocated VERBATIM → v11-04-01 per-tenant correctness + Cycle-12 F-C12-001 wire slice preserved. **Decision: stream not cache** (synthetic cold TTFB 214ms vs field 1633ms → cold-start/geo dominates; cache adds tenant-key leak risk).
  - Gates green every loop: tsc 0 · vitest 19/19 (3 new CLS + 16 public-view) · `SKIP_ENV_VALIDATION=1 next build` clean.
  - **VERIFIED ON LIVE DEPLOY:** Playwright @ 820×1180 on prod `/perform` → **CLS 0.000** on two consecutive cold loads (was a reproducible 0.187). The CLS fix is proven on the band's real iPad viewport.

---

## What's In Progress

- Nothing mid-flight. Clean phase boundary. v11.3-04 shipped + CLS-verified.

---

## What's Next

**Immediate:** `/paul:plan` for **Phase v11.3-05 — P3 polish** (the LAST phase of v11.3):
- **BUG-6:** `manifest-brotherslazaroff.json` serves the HTML app shell (PWA install broken on broslaz). Check `src/proxy.ts` matcher excludes only `manifest.json`, not org-suffixed variants.
- **F-6:** cold landing fires `/api/auth/qr` POST → 429 then self-heals; `/api/web-vitals` also 429s. Rate-limit tuning or client backoff. **NOTE: F-6 is the SAME QR-card surface (`QRSignIn.tsx`) as v11.3-04-02's CLS fix — coordinate; don't undo the slot reservation.**

**After v11.3-05:** milestone v11.3 closes via `/paul:complete-milestone`. (v11.4 = Publish & Notify / D8.)

**Outstanding UAT (RUM-deferred, non-blocking):** v11.3-04 field TTFB/FCP — after ~1–7 days of traffic, re-run `node scripts/v11-3-04-webvitals-slice.mjs` and compare `/perform` cold(navigate) TTFB/FCP to the 1633/3551ms baseline. If still high → cold-start-bound → action the Vercel fluid-compute/region follow-up (STATE Deferred Issues), NOT more app code.

---

## Binding Context (do not re-litigate)

- **Oracle:** `docs/ACCESS-POLICY.md` v0.3; err-public prime directive.
- **Scope walls:** D8 publish/notify → v11.4; BUG-3/BUG-8/Policy-Q1 closed by policy; F-4 + Cowork sandbox proxy out; D2 anon recordings is the ⚠️ veto cell.
- **Autonomy posture (binding):** run autonomously, auto-commit + push per phase to `master`, bake decisions into PLANs. Quality floor: tsc + tests + AC proof per task; `SKIP_ENV_VALIDATION=1 next build` before deployable; **/ui-ux-pro-max BLOCKING on UI-touching phases** (v11.3-05's manifest/QR work likely qualifies). Daniel paces with "go" per PAUL step; STOP at phase boundaries / STOP-gates / cross-tenant leak.
- **Tooling gotchas:** Bash tool is bash (`<<'EOF'` heredocs for commit msgs). No SA creds on this box → admin-SDK scripts mint temp ADC from the firebase-CLI refresh token (see `scripts/v11-3-04-webvitals-slice.mjs`). `next build` catches client/server bundle-boundary breaks tsc/vitest miss. Stage phase files EXPLICITLY (never `git add -A` — repo root has many untracked junk dirs).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (read first) |
| `.paul/ROADMAP.md` | v11.3 milestone + § Phase v11.3-05 |
| `docs/ACCESS-POLICY.md` | The oracle (v0.3) |
| `.paul/phases/v11.3-04-perform-performance/` | Completed phase 4 (3 PLAN + 3 SUMMARY) |
| `.paul/research/PERFORM-PERF-CHARACTERIZATION-2026-06-10.md` | BUG-2 characterization + ranked fixes |
| `scripts/v11-3-04-webvitals-slice.mjs` | Field-RUM slicer (re-run for the deferred TTFB UAT) |
| `.paul/UAT-PENDING.md` | Non-blocking live checks (CLS ✅ verified; TTFB pending RUM) |

---

## Resume Instructions

1. `git pull` (multi-computer).
2. Read `.paul/STATE.md` for latest position.
3. Run `/paul:resume` (or `/paul:plan` directly for Phase v11.3-05).

---

*Handoff created: 2026-06-10 — paused at the v11.3-04 → v11.3-05 phase boundary (v11.3-04 shipped to prod `c0b0ab3367`, CLS verified live).*
