# PAUL Handoff

**Date:** 2026-06-16
**Status:** PAUSED at a clean milestone boundary — **v11.5 COMPLETE**, nothing in flight.

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — multi-tenant worship/band sheet-music app. Browser = band/consumer surface (Perform mode on iPads); Daniel authors via Claude + MCP. 2 live tenants: CRC + Brothers Lazaroff (brotherslazaroff.live).
**Core value:** the band gets the right charts + recordings on their iPads each week; the web app's job is performance + quick edits.

---

## Current State

**Version:** `11.5.0` (package.json + paul.json + tag `v11.5.0`, all pushed to origin).
**Milestone:** ✅ **v11.5 — Bulletproof Performance COMPLETE** (opened 2026-06-11, closed 2026-06-16).
**Phase / Plan:** None active — awaiting v11.6.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY     [milestone complete — ready for next]
  ○        ○        ○
```

---

## What Was Done (this session)

Resumed at the v11.5-05 phase boundary and drove the LAST phase (Consumer polish) end-to-end, then closed the milestone:

- **Plan 01 (Q6+Q3) `dda03624f7`** — org-vocab public-listing subtitle ("Public sets" on BL) + branded, QR-aware expired/used copy on `/qr/[code]`. CRC byte-identical.
- **Plan 02 (Q5+F4) `bd7d786ec2`** — `displayChartTitle()` strips stray `.pdf`/`.docx` on consumer titles (both tenants; intentional CRC change) + read-time `track.key` resolution from `library_index` in `server-tracks.ts` (reaches anon via SSR frame). **Live BL probe → verdict (B):** BL charts have NO keys anywhere, so the resolution is forward-safe but BL badges need authoring.
- **Plan 03 (Q4) `14ebb2448e`** — anon `/setlists` cleanup: `isNonTestSetlist` filter (all viewers) + zero-track-draft suppression (anon-only); `SetlistCards` Edit/Download gated behind `canEdit={!!user}`.
- **Milestone close `43dd8e34b8` + tag `v11.5.0`** — MILESTONES.md entry, ROADMAP archived to `.paul/milestones/v11.5.0-ROADMAP.md`, PROJECT/ROADMAP/STATE/paul.json evolved, version → 11.5.0. All pushed.

Quality floor held throughout: every plan tsc-clean + tests green + `next build --webpack` exit 0, /ui-ux-pro-max consulted, auto-committed + pushed to prod `master`.

---

## What's In Progress

Nothing. Clean tree, all pushed. (Working tree has only pre-existing untracked research/screenshot artifacts unrelated to PAUL.)

---

## What's Next

**Immediate:** `/paul:discuss-milestone` (or `/paul:milestone`) to scope **v11.6**. Candidate backlog (from ROADMAP § Next Milestone):
- Photo-of-paper-chart import (dropped from v11.5; recon preserved in ROADMAP § Phase v11.5-03).
- F3 library browse density/filters (thumbnails, composer/recency metadata, search).
- F5 comms design layer (waits on Antigravity mural/BL mockups).
- Deferred infra adjacents: recordings-collection org-scoping + `/api/recordings/upload` orgId stamp, `finalize_chart_upload` signed-URL org-stamp gap, SERVICE_TYPE_LABELS vocab table, v7.0 fold-forward re-triage.

**Owner action (not code) carried out of v11.5:**
- **F4 — author keys on the 18 Brothers Lazaroff charts** via MCP `update_song` / `edit_library_entry`. The read-time resolution shipped in v11.5-05-02 will surface key badges on `/perform` automatically once keys exist. Live setlist: "Tower Grove Farmer's Market" (`qAzY0sJJGcad8mUqEZF0`). See `.paul/research/v11-5-05-02-f4-bl-key-probe.md`.

**Open UAT (non-blocking, `.paul/UAT-PENDING.md`):** v11.5-05 Q3/Q5/Q6 consumer copy + Q4 anon `/setlists` + F4 BL badges (post-authoring); plus carried items (H1 7-tablet zoom, F2 live-director, loginable-test-account checks).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (milestone complete) |
| `.paul/ROADMAP.md` | § Next Milestone = v11.6 candidate backlog |
| `.paul/MILESTONES.md` | § v11.5 permanent record |
| `.paul/milestones/v11.5.0-ROADMAP.md` | Archived v11.5 roadmap snapshot |
| `.paul/research/v11-5-05-02-f4-bl-key-probe.md` | F4 verdict-B evidence + authoring follow-up |

---

## Resume Instructions

1. `git pull` first (multi-computer box). master is prod; push `origin master` (NOT master:main).
2. `/paul:resume` → it will route to `/paul:discuss-milestone` for v11.6.
3. Autonomy posture binding (v11.x): auto-commit + push per phase to master; quality floor held (tsc + tests + `next build --webpack` exit 0); /ui-ux-pro-max BLOCKING on UI phases; Daniel paces with "go"/"1" per PAUL step.
4. **Operational standing facts:** David Lazaroff STAYS admin (settled — don't re-raise). `/perform` cold·mobile CLS 0.250 + Vercel TTFB are INFRA follow-ups, not app code.

---
*Handoff created: 2026-06-16 (v11.5 milestone complete)*
