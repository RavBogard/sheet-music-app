# PAUL Handoff

**Date:** 2026-06-11
**Status:** paused (clean — phase boundary, committed + pushed to prod)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (centralreform.live) — multi-tenant worship-band app (CRC + Brothers Lazaroff). Daniel authors setlists via Claude + MCP; the browser app is the band/consumer surface (Perform mode on iPads).
**Core value:** The band gets the right charts + recordings on their iPads each week; Daniel authors conversationally via Claude + MCP.

---

## Current State

**Milestone:** 🚧 v11.3 — Worthiness & Access (5 phases). Oracle: `docs/ACCESS-POLICY.md` v0.3 (a finding is a bug only if it contradicts a cell).
**Phase:** 2 of 5 COMPLETE → **next is Phase v11.3-03 (Harness & hygiene, P2)**. Plans: none active.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY     [v11.3-02 ✅ phase complete + committed + pushed — ready to PLAN v11.3-03]
  ○        ○        ○
```

**Git:** `master`, in sync with `origin/master`, tip **`89f4af7fd2`** (v11.3-02 phase commit, Vercel auto-deploying). Push `origin master` (NOT `master:main`). Multi-computer → `git pull` before next session.

---

## What Was Done (this session)

- **`/paul:resume`** → reconciled state (2 benign docs commits past the recorded tip), archived the consumed handoff.
- **Phase v11.3-02 Agent chart-upload path — COMPLETE (2 plans, both PLAN→APPLY→UNIFY):** closes David's Cowork chart-upload dead-end (`.paul/research/BUG-cowork-chart-upload-2026-06-10.md`).
  - **v11.3-02-01 (primary):** `import_chart_from_drive` now converts server-side — Google Docs/Sheets/Slides via the existing `DriveClient.exportDoc` (files.export), and `.docx`/`.xlsx`/`.pptx` (+ legacy Office) via convert-on-copy (copy→Google doc→export PDF→delete temp). New `DriveClient.fetchAsPdf` + pure `driveSourceIsConvertible` classifier; `importChartFromDrive` routes convertible types through it (single shared `processChartUpload` call). Folders + non-exportable native types still refuse; binary/dryRun paths unregressed. 58/58 chart-upload emulator tests.
  - **v11.3-02-02 (secondary):** inline chunked upload — `begin_chunked_chart_upload` → `append_chart_upload_chunk` ×N → `commit_chunked_chart_upload` on the `upload_sessions` substrate (for non-Drive sources where the signed-URL PUT is proxy-blocked in Cowork and inline base64 exceeds the ~25K-token cap). Commit reassembles chunks in index order, delegates to the shared `finalizeChartUpload`, and org-stamps the result. Only begin+commit are rate-limited (per-chunk would exhaust the 10/min cap). 20/20 upload-session emulator tests.
  - Phase commit `feat(v11.3-02): agent chart-upload path` (`89f4af7fd2`) + pushed. Gates green every loop: tsc · emulator suites · `SKIP_ENV_VALIDATION=1 next build`. CRC byte-identical.

---

## What's In Progress

- Nothing mid-flight. Clean phase boundary. v11.3-02 shipped to prod.

---

## What's Next

**Immediate:** `/paul:plan` for **Phase v11.3-03 — Harness & hygiene** (P2):
- **BUG-9:** `/test-login` missing from `proxy.ts` `publicPrefixes` → 307 to /login before code consumption. Fix + regression test.
- **BUG-7:** `GET /api/auth/qr?code=<malformed-with-/>` → 500; must be 4xx (v11.2 error contract).
- **BUG-1:** orphan `[role-*]` rows in CRC `library_index`. **VERIFY FIRST:** whether `revoke_test_account` / `cleanup_all_test_data` cascade revoked-account library uploads; confirm coverage, then delete the two orphans.
- Each fixed BUG gets a regression test citing its stress-report coverage cell.

**After that:** v11.3-04 (/perform perf — BUG-2), v11.3-05 (P3 polish — BUG-6/F-6). D8 publish/notify is v11.4 (NOT this milestone).

---

## Binding Context (do not re-litigate)

- **Oracle:** `docs/ACCESS-POLICY.md` v0.3 is the bug arbiter; err-public prime directive (err toward letting someone see a chart; writes/admin gated).
- **Scope walls:** D8 → v11.4; BUG-3 (RUM)/BUG-8 (member library)/Policy-Q1 closed by policy (no code); F-4 dup setlist + Cowork sandbox proxy out; D2 anon **recordings** stays the ⚠️ veto cell (charts only).
- **Autonomy posture (binding):** run autonomously, auto-commit + push per phase to `master`, bake decisions into PLANs. Quality floor: tsc + tests + AC proof per task; `SKIP_ENV_VALIDATION=1 npx next build` before declaring deployable; emulator tests where rules change; /ui-ux-pro-max BLOCKING on UI-touching phases. STOP only for ambiguity / unresolvable gate failure / cross-tenant leak. Daniel paces with "go" per PAUL step.
- **Tooling gotcha:** the Bash tool is bash — use `<<'EOF'` heredocs for commit messages, not `@'…'@` (that's PowerShell; leaks a stray `@`).

---

## Deferred from this phase (for visibility)

- **finalize_chart_upload (signed-URL path) does not org-stamp** — the chunked `commit` stamps its result, but the signed-URL `finalize_chart_upload` shares finalize's missing-stamp gap → its uploads land default-org. Small follow-up: add `org` param to `finalizeChartUpload` + pass `orgFrom(extra)` in its handler.
- **Live UAT-PENDING** (no service-account creds on this box): real Google-Doc/.docx import + multi-chunk inline upload via the deployed broslaz/CRC MCP. See `.paul/UAT-PENDING.md`.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (read first) |
| `.paul/ROADMAP.md` | v11.3 milestone + phase detail (§ Phase v11.3-03) |
| `docs/ACCESS-POLICY.md` | The oracle (v0.3) |
| `.paul/phases/v11.3-02-agent-chart-upload/` | Completed phase 2 (PLAN + SUMMARY ×2) |
| `.paul/UAT-PENDING.md` | Non-blocking live checks (incl. v11.3-02 import/chunked) |
| `src/lib/mcp/tools/library-upload.ts` · `library-upload-session.ts` · `src/lib/google-drive.ts` | Phase v11.3-02 implementation |

---

## Resume Instructions

1. `git pull` (multi-computer).
2. Read `.paul/STATE.md` for latest position.
3. Run `/paul:resume` or `/paul:plan` for Phase v11.3-03.

---

*Handoff created: 2026-06-11 — paused at the v11.3-02 → v11.3-03 phase boundary (v11.3-02 shipped to prod).*
