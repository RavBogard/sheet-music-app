# PAUL Handoff

**Date:** 2026-06-08
**Status:** paused (clean phase boundary)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — band charts/recordings on iPads; Daniel authors setlists via Claude + MCP.
**Core value:** The band gets the right charts + recordings on their iPads each week, and Daniel authors setlists conversationally via Claude + MCP.

---

## Current State

**Milestone:** v11.0 Brothers Lazaroff Multi-Tenant (1 of 5 phases complete)
**Phase:** v11-01 Tenant foundation — **✅ COMPLETE** (committed + pushed to prod master `a2ea9713ad`)
**Plan:** none active. Ready to PLAN v11-02.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [v11-01 PHASE COMPLETE — next: PLAN v11-02]
```

---

## What Was Done (this session — entire phase v11-01, 4 plans)

- **v11-01-01** — org model + tenant registry + membership claims (`src/lib/org/`: registry.ts, types.ts, membership.ts; default-crc backward-compat for claimless users).
- **v11-01-02** — orgId stamped at all 5 server create sites (setlist+seeded tracks, addTrack, bulkAddTracks, library_index+songs, recording); tracks inherit parent setlist's orgId; default "crc". `orgid-stamping.emulator.test.ts`.
- **v11-01-03** — `src/lib/org/backfill-orgid.ts` + emulator test + prod `.mjs` runner/RUNBOOK. **Ran the prod backfill: 2,105 docs stamped orgId="crc"** (setlists 48, tracks 459, library_index 668, songs 930, recordings 0) + seeded `orgs/{crc,brotherslazaroff}`; idempotency verified (re-run wouldStamp:0).
- **v11-01-04** — org-scoped Firestore rules (WRITE-ISOLATION, reads unchanged/err-public; `callerOrgs`/`orgCreateOk`/`orgUpdateOk` helpers + `orgs` block) + 42 blocking emulator rules tests + **deployed to prod** (`firebase deploy --only firestore:rules`). The emulator gate caught + fixed a CRC lock-out bug (bare `token.orgIds` access throws → use `.get('orgIds',[])`).
- Phase transition: PROJECT.md + ROADMAP.md evolved; single `feat(v11-01)` commit (scoped, no untracked junk) pushed to `origin master`.

---

## What's In Progress

- Nothing mid-flight. Phase is closed cleanly. (One tiny uncommitted STATE.md "Git State" note delta — cosmetic, will fold into v11-02's first commit.)

---

## What's Next

**Immediate:** `/paul:plan v11-02` — MCP org-scoping. The milestone's **largest surface**: org-scoped bearer auth, resolve the caller's org per tool call, thread `orgId` through the ~108 live MCP tools, issue David Lazaroff's `brotherslazaroff` bearer. Recommended for a fresh session (deserves focused planning).

**After that:** v11-03 (brotherslazaroff.live host routing + branding + vocab trim, /ui-ux-pro-max BLOCKING) → v11-04 (BL consumer surface + David onboarding + e2e UAT) → v11-05 (cross-tenant isolation security audit, close gate).

---

## Key Context for v11-02

- **Org claim model** (`src/lib/org/membership.ts`): `getOrgIdsFromClaims` — missing/empty `orgIds` claim → `[DEFAULT_ORG_ID]` ("crc"). CRC users carry NO claim today. v11-02 issues David's explicit `orgIds:['brotherslazaroff']` claim.
- **Rules already enforce** cross-tenant write isolation (deployed). v11-02's MCP layer must resolve the caller's org and stamp it on writes / filter reads by it — the rules are the backstop, the MCP tools are the primary scoping.
- **err-public holds WITHIN a tenant**, hard wall ACROSS tenants. Cross-tenant READ filtering of the public surfaces (setlists/tracks are public-read) is deferred to the app query layer (v11-03 host routing) — v11-02 is about MCP auth/tools.
- **Reusable prod-script auth** (this box has no SA creds, no gcloud): convert the firebase CLI refresh token (`~/.config/configstore/firebase-tools.json` → `tokens.refresh_token`) into a temp `authorized_user` ADC json (public firebase-tools OAuth client_id/secret), point `GOOGLE_APPLICATION_CREDENTIALS` at it, delete after. `firebase deploy` uses the CLI directly (no ADC needed).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview (v11-01 ✅, v11-02 next) |
| `.paul/phases/v11-01-tenant-foundation/*-SUMMARY.md` | What each v11-01 plan shipped |
| `src/lib/org/` | Org model, registry, membership, backfill |
| `firestore.rules` | Org-scoped rules (deployed) |

---

## Resume Instructions

1. `git pull` (multi-computer workflow — origin master is at `a2ea9713ad`).
2. Read `.paul/STATE.md` for latest position.
3. Run `/paul:resume` (or directly `/paul:plan v11-02`).

---

*Handoff created: 2026-06-08*
