# PAUL Handoff

**Date:** 2026-06-08
**Status:** paused (clean phase boundary — v11-02 + v11-02b both complete & deployed; ready to PLAN v11-03)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — band charts/recordings on iPads; Daniel authors setlists via Claude + MCP.
**Core value:** The band gets the right charts + recordings on their iPads each week, and Daniel authors setlists conversationally via Claude + MCP.
**Current milestone:** v11.0 Brothers Lazaroff Multi-Tenant — make the app multi-tenant so David Lazaroff gets his own org-scoped instance (eventually on brotherslazaroff.live).

---

## Current State

**Milestone:** v11.0 Brothers Lazaroff Multi-Tenant — **2 of 5 numbered phases complete** (+ one inserted sub-phase v11-02b).
**Phase:** v11-02b ✅ COMPLETE → **next: v11-03 Domain + branding (NOT STARTED)**.
**Plan:** none active — ready to PLAN v11-03.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     (v11-02 all 4 plans ✓ · v11-02b ✓ — both deployed)
```

**App:** package.json `10.1.0`. Production branch `master`; push `origin master` (NOT `master:main`). origin/master IN SYNC.

---

## What Was Done (this session)

A big session — two full phases shipped to production:

- **v11-02 MCP org-scoping (4 plans, all LOOP COMPLETE) — Brothers Lazaroff is the FIRST LIVE SECOND TENANT:**
  - v11-02-01 caller-org resolution foundation (orgId on the mcpTokens doc; `verifyBearer` resolves it; `orgFrom(extra)` seam; 117-token prod backfill).
  - v11-02-02 READS org-scoped (6 tools; list/search filter + get-by-id not-found wall; emulator 7/7).
  - v11-02-03 WRITES walled (`loadEditableSetlist` chokepoint for 8 tools + per-tool guards on delete/recompute/clone/clone-from-template/update_song/delete_chart; create-stamp on create/clone/template + `stampOrg` on the 3 chart-create tools; not-found wall, no leaky cross_tenant_denied; emulator 8/8).
  - v11-02-04 issued David's `brotherslazaroff` bearer (tokenId `93JMXhT1OspFsWDMmb9V`) + `orgIds:['brotherslazaroff']` claim by MERGE (role preserved) on his existing band_leader account (uid `HTks9a8YRiVCQ5lVipUJcBsWjnB3`); shipped `feat(v11-02)` to prod; **live e2e 12/12** against www.centralreform.live/api/mcp (BL reads/creates BL-only, cannot touch CRC, CRC unaffected).
- **v11-02b Org-aware token minting (1 plan, inserted before v11-03):** Daniel asked why David needs a hand-issued bearer — root cause: the self-service mint paths (`/api/mcp/tokens` + `/api/mcp/oauth/token`) hard-defaulted new tokens to `crc`. Fixed: `getPrimaryOrgForMinting(uid)` (reuses v11-01-01's `getUserOrgIds`) threaded into both routes. Tenant members now self-onboard via plain login → correctly org-scoped bearer, no manual handoff. unit 9/9 + emulator 3/3; deployed; prod-verified David→brotherslazaroff.

**Commits (all pushed to origin master, all deployed):** `feat(v11-02)` c7da31ac2a · `test(v11-02)` 779eab0a54 · `docs(v11-02)` 243a8a98b8 · `feat(v11-02b)` 2db15f36d9 · `docs(v11-02b)` 10499a7623.

---

## What's In Progress

Nothing mid-flight. Both phases closed cleanly and deployed. The working tree is clean (origin/master in sync).

---

## What's Next

**Immediate:** PLAN **v11-03 — Domain + branding**: brotherslazaroff.live host→tenant resolution on the shared deployment; Brothers Lazaroff branding (band chrome, NOT synagogue indigo); synagogue→band vocab trim (gig/venue/set, not service/sanctuary/rabbi; trim service-type + rabbi UI). **/ui-ux-pro-max is BLOCKING on this UI phase** (per the v11.0 autonomy directive). Note: `resolveOrgIdByDomain(host)` already exists in `src/lib/org/registry.ts` (host→org seam, localhost/*.vercel.app → crc).

⚠️ **Branding + vocab are Daniel/David product/design calls** (the milestone's sanctioned product-ambiguity stop). Recommend `/paul:discuss-phase v11-03` to pin the look + the vocab term-list BEFORE planning, rather than inventing defaults. Daniel's earlier hint: dark/photographic band chrome; "Brothers Lazaroff"; gig/venue/set.

**After that:** v11-04 (BL consumer surface perform/print + onboarding + fold in the deferred cross-tenant collections) → v11-05 (cross-tenant isolation security audit, close gate).

---

## Key Context for v11-03

- **Multi-tenant model is LIVE + proven:** MCP caller-org seam (`orgFrom`/`rowOrg`/`stampOrg` in `src/lib/mcp/org-context.ts`); reads + writes both tenant-isolated; self-service minting org-aware (`getPrimaryOrgForMinting` in `src/lib/org/membership.ts`). Org registry + host resolver in `src/lib/org/registry.ts`.
- **Canonical prod MCP endpoint:** https://www.centralreform.live/api/mcp (apex 307-redirects to www; `curl -L` DROPS the auth header — hit www directly). Reusable isolation probe: `scripts/e2e-bl-tenant-probe.mjs` (DAVID_BEARER + CRC_BEARER env) — for the v11-05 audit.
- **DEFERRED to v11-04** (still cross-tenant today): templates READ/LIST scoping, roster/musicians (`users`, `scheduling_assignments`, `musician_availability`), congregation, service-personnel — read + write.
- **Local `next build` caveat:** fails at page-data collection on `/api/cron/aggregate-corrections` ("Invalid environment variables" — needs `CRON_SECRET`, a Vercel-injected var absent from this box's `.env.local`). NOT a code defect; rely on the Vercel build (full env) as the build gate.
- **Prod-script auth on this box** (no SA creds, no gcloud): firebase-CLI refresh token (`~/.config/configstore/firebase-tools.json`) → temp `authorized_user` ADC json (public firebase-tools client_id/secret) → `GOOGLE_APPLICATION_CREDENTIALS`; delete after. Used by issue-bl-bearer.mjs + supervisor-prod-bearer.mjs + the prod backfills.

---

## UAT-PENDING (human, non-blocking)

- David's UX confirmation: he can self-onboard via Claude Desktop login (now gets a BL-scoped token automatically) OR use the manual bearer (tokenId `93JMXhT1OspFsWDMmb9V`; revoke+re-mint if lost). Pair with `docs/onboarding-brotherslazaroff.md`. Server-side isolation already proven e2e.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (current position, git state, resume context) |
| `.paul/ROADMAP.md` | Phase overview (v11-01 ✅ · v11-02 ✅ · v11-02b ✅ · v11-03 next) |
| `.paul/phases/v11-02-mcp-org-scoping/v11-02-04-SUMMARY.md` | David's bearer + prod deploy + live e2e |
| `.paul/phases/v11-02b-org-aware-minting/v11-02b-01-SUMMARY.md` | Org-aware self-service minting |
| `src/lib/mcp/org-context.ts` | orgFrom / rowOrg / stampOrg seam |
| `src/lib/org/registry.ts` | org registry + `resolveOrgIdByDomain(host)` (v11-03's host seam) |
| `src/lib/org/membership.ts` | getUserOrgIds / getPrimaryOrgForMinting |

---

## Resume Instructions

1. `git pull` (multi-computer — other boxes may push v7.1 `.coord/` work).
2. Read `.paul/STATE.md` (Session Continuity) + the two v11-02 summaries above.
3. Run `/paul:resume` — it will route to `/paul:plan v11-03`. Consider `/paul:discuss-phase v11-03` first to pin branding + vocab with Daniel.

---

*Handoff created: 2026-06-08*
