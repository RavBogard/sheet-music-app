# PAUL Handoff

**Date:** 2026-06-08
**Status:** paused (clean phase boundary — v11-03 COMPLETE, deployed, AND live-verified on brotherslazaroff.live; ready to PLAN v11-04)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — band charts/recordings on iPads; setlists authored via Claude + MCP.
**Core value:** The band gets the right charts + recordings on their iPads each week, and Daniel authors setlists conversationally via Claude + MCP.
**Current milestone:** v11.0 Brothers Lazaroff Multi-Tenant — make the app multi-tenant so David Lazaroff gets his own org-scoped instance on **brotherslazaroff.live** (now LIVE + branded).

---

## Current State

**Milestone:** v11.0 — **3 of 5 numbered phases complete** (v11-01 ✅ · v11-02 ✅ · v11-02b ✅ · v11-03 ✅).
**Phase:** v11-03 Domain + branding ✅ COMPLETE → **next: v11-04 BL consumer surface + onboarding (NOT STARTED)**.
**Plan:** none active — ready to PLAN v11-04.
**App:** package.json `10.1.0`. Production branch `master`; push `origin master` (NOT `master:main`). origin/master IN SYNC, tree clean.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY        [v11-03 PHASE COMPLETE — next: PLAN v11-04]
  ✓        ✓        ✓
```

---

## What Was Done (this session)

A full phase, end-to-end, autonomously (per the v11.0 autonomy directive):

- **Domain config doc** → `docs/brotherslazaroff-domain-setup.md` (Daniel's Vercel + Squarespace DNS steps). **Daniel completed the DNS; brotherslazaroff.live is live.**
- **`/paul:discuss-phase v11-03`** → `CONTEXT.md`: taste calls locked — dark+photographic · navy brand from brotherslazaroff.com · per-tenant conditional vocab (CRC untouched).
- **v11-03-01** org-context foundation — Edge `x-org-id` header (mirrors `x-nonce`) → `<html data-org>` + client `OrgProvider`/`useOrg` (defaults crc outside a provider).
- **v11-03-02** Brothers Lazaroff branding — scoped `[data-org="brotherslazaroff"]` + `.dark[data-org=...]` navy CSS-var theme (hue 252 vs CRC indigo 275), `forcedTheme=dark`, `getOrgBranding()`, org-aware dark photographic login hero + wordmark. CRC indigo+amber untouched.
- **v11-03-03** vocab + UI trim — `label(org,key)` + `hidesLiturgicalFields()`; `SetlistMetaEditSheet` hides service-type + rabbi for BL, band vocab. CRC byte-identical.
- **Phase transition** — `feat(v11-03)` committed + pushed; Vercel prod READY.
- **LIVE VERIFICATION caught a real bug** → `fix(v11-03)`: layout/login passed the `x-org-id` header (an *org id*) through `resolveOrgIdByDomain` (matches *domains*) → fell to crc default; CRC masked it; no local dev meant prod was the only place it showed. Fixed with `coerceOrgId()` + regression test. **Re-verified live:** `www.brotherslazaroff.live` → `<html data-org="brotherslazaroff">` + "Brothers Lazaroff" wordmark; `www.centralreform.live` still `data-org="crc"`.

**Commits (all pushed to origin master, all Vercel prod READY):** `feat(v11-03)` `e51d352036` · `fix(v11-03)` `30bc6c7483` · `docs(v11-03)` `482dbdba5f`.

**Quality:** tsc clean · full unit suite **3292 passed / 0 failed** · eslint 0 errors.

---

## What's In Progress

Nothing mid-flight. v11-03 closed cleanly, deployed, and live-verified. Working tree clean, origin/master in sync.

---

## What's Next

**Immediate:** PLAN **v11-04 — BL consumer surface + onboarding**:
- Perform-view + gig-packet print scoped to the BL org.
- David's BL org membership + empty-library seed; end-to-end UAT (David authors via MCP → views/prints a gig packet on brotherslazaroff.live).
- **Fold in the deferred work:**
  - From v11-02: org-scope the still-cross-tenant collections — **templates (read/list), roster/musicians (`users`, `scheduling_assignments`, `musician_availability`), congregation, service-personnel** (read + write).
  - From v11-03-03: de-synagogue the **CreationWizard / perform view / display cards** vocab — now unblocked once the `congregation` collection + liturgical templates are org-scoped.
- **/ui-ux-pro-max BLOCKING** (UI phase). `git pull` first (multi-computer).

**After that:** v11-05 — cross-tenant isolation security audit (close gate; reuse `scripts/e2e-bl-tenant-probe.mjs`).

---

## Optional / non-blocking

- Apex `brotherslazaroff.live` currently redirects to `www` (canonical). If Daniel prefers bare apex as canonical, it's a one-setting change in Vercel.
- Daniel may drop a real live-performance photo into the login hero's `--bl-hero-image` slot (currently a navy stage-light gradient).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (current position, git, resume context) |
| `.paul/ROADMAP.md` | Phase overview (v11-01..03 ✅ · v11-04 next) |
| `.paul/phases/v11-03-domain-branding/CONTEXT.md` | BL taste decisions (branding + vocab) |
| `.paul/phases/v11-03-domain-branding/v11-03-0{1,2,3}-SUMMARY.md` | Per-plan outcomes |
| `docs/brotherslazaroff-domain-setup.md` | DNS/domain ops doc (DONE) |
| `src/lib/org/registry.ts` | org registry + `resolveOrgIdByDomain` (host→org) + `coerceOrgId` (header→org) |
| `src/lib/org/{org-context,branding,vocab}.ts` | client org context · brand metadata · vocab/field-trim |
| `src/proxy.ts` | Edge `x-org-id` resolution | 
| `src/app/globals.css` | `[data-org="brotherslazaroff"]` navy theme block + `bl-hero` |

---

## Resume Instructions

1. `git pull` (multi-computer — other boxes may push v7.1 `.coord/` work).
2. Read `.paul/STATE.md` (Session Continuity) + the three v11-03 SUMMARYs + CONTEXT.md.
3. Run `/paul:resume` — it will route to `/paul:plan v11-04`. Consider `/paul:discuss-phase v11-04` first (consumer-surface scope + how aggressive the cross-tenant fold-in should be).

---

*Handoff created: 2026-06-08*
