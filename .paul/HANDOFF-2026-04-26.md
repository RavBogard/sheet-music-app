# PAUL Handoff

**Date:** 2026-04-26
**Status:** paused — clean stopping point between phases

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — worship band setlist/chart app
**Core value:** Musicians can instantly access setlists, transpose charts, and control monitor mixes — live on any device, no paper or prep needed.

---

## Current State

**Milestone:** v5.0 — Bulletproof Editor (Local-First Rewrite) — **3 of 7 phases complete (43%)**
**Phase:** v50-04 of 7 — Song catalog & sticky memory — **Ready to plan**
**Plan:** None yet

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     [Loop reset — ready for v50-04 PLAN]

v50-01:  ✓ ──▶ ✓ ──▶ ✓     [Architecture & design — complete]
v50-02:  ✓ ──▶ ✓ ──▶ ✓     [Dead-code amputation — complete]
v50-03:  ✓ ──▶ ✓ ──▶ ✓     [Local-first sync engine — complete]
```

**Working tree:** clean. **Branch:** master, up to date with origin/master.

---

## What Was Done (this session)

Shipped phase **v50-03 Local-first sync engine** end-to-end (PLAN → APPLY → UNIFY) in one session.

- Installed `dexie@4.4.2` + `dexie-react-hooks@1.1.7` + `fast-check@3.23.2` (dev)
- Built `src/lib/local/` — Dexie singleton with 5 stores per ARCHITECTURE.md §2.2 + atomic `applyEdit()` (entity row + outbox row in one Dexie transaction)
- Built `src/lib/sync/` — pure FSM (Idle/Dirty/Saving/Conflict/Failed/Offline), Firestore adapter typed errors, BroadcastChannel cross-tab lock (5s lease + heartbeat), drain engine with backoff `[500,1000,2000,4000,8000]ms × 5 attempts`, dead-letter, version-mismatch routing, in-loop auth refresh + retry, offline auto-resume, zustand status store
- Wrote a `fast-check` property harness (20 random scenarios × ≤30 actions, seed 12345) proving every committed `applyEdit()` is observable in EXACTLY ONE of {Firestore mock, outbox.pending|failed} — never silently lost
- **Real bug surfaced and fixed by the property harness itself:** transient failure on row N could let row N+1 (same docId) leapfrog on the server, violating LWW per-document semantics. Engine now blocks later same-doc rows when an earlier row is sending/failed/not-yet-due
- 39 new tests; **1320/1320 total green**; tsc clean; `next build` success
- 4 atomic commits on master, pushed to origin: `cb73dcc` (IDB foundation) + `6cf34d7` (sync engine) + `0a94a9c` (property harness) + `9df0a1a` (phase close — SUMMARY + STATE/ROADMAP/PROJECT updates + handoff archive)
- Stale 2026-04-18 handoff archived to `.paul/handoffs/archive/`

**Engine is fully standalone** — verified zero imports from `src/components`, `src/hooks`, or `src/app`. Old editor write path (`src/lib/setlist-firebase.ts` etc.) untouched. v50-05 editor cutover plugs in cleanly.

---

## What's In Progress

Nothing — v50-03 fully closed at a clean checkpoint.

---

## What's Next

**Immediate:** `/paul:plan` for **v50-04 Song catalog & sticky memory**.

ARCHITECTURE.md §4 binds this phase. Key deliverables:
- Extend `songs` Dexie store with `defaults: { key, lead, bpm }` + `recent[]` (cap 5, FIFO) — Dexie schema version bump to v2
- Backfill script `scripts/migrate-v50.ts` (dry-run + idempotent + rollback) walks every `setlists/*` doc, populates `songs/{id}.defaults` from most-recent occurrence
- Read-through pattern from Dexie for the library picker (no Firestore round-trip on cell render)
- New Firestore writes to `songs/*` go through `applyEdit('update', ...)` — engine handles propagation
- No UI changes in this phase — that's v50-05's editor cutover

**After that:** v50-05 (Spreadsheet editor cutover, `/ui-ux-pro-max` required), then v50-06 (concurrent-edit safety + offline + cross-tab), then v50-07 (migration & cutover).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | v5.0 phase overview (3/7 complete) |
| `.paul/PROJECT.md` | Project facts + decisions |
| `.paul/phases/v50-01-architecture/ARCHITECTURE.md` | Binding architecture (§4 = song catalog spec for v50-04) |
| `.paul/phases/v50-03-sync-engine/v50-03-01-SUMMARY.md` | What just shipped |
| `src/lib/local/{schema,types,write}.ts` | Dexie foundation — extend in v50-04 |
| `src/lib/sync/engine.ts` | Sync engine — already drives applyEdit through to Firestore |

---

## Key Context (don't relearn)

- **Band is NOT in production right now** (waiting on dependability). Broken-for-band periods during the rewrite are acceptable. No parallel-editor scaffolding, no feature flags, hard cutover.
- **Push to `origin master`** (not `master:main`). Deploy straight to production on Vercel; no preview branches.
- **User works from multiple computers** — pull before starting any session.
- **Dexie schema bumps:** v50-04 will need `db.version(2).stores(...)` because we're adding indexed fields to `songs`. Dexie auto-migrates additive schema changes.
- **Test pattern locked in:** Dexie-touching tests use FakeClock injection (NOT `vi.useFakeTimers` — races with fake-indexeddb's microtask scheduling). Use macrotask flush helper from `engine.test.ts`.
- **Per-doc ordering invariant** is now part of the engine contract. Any future change to `drainOnce()` must preserve "only oldest pending row per (collection, docId) drains; sending/failed rows block later same-doc rows."

---

## Outstanding (carryover, not blocking v50-04)

- Firestore rules deploy verification (carryover from 2026-04-18): confirm `firebase deploy --only firestore:rules` has landed before any phase that touches rules.
- Setlist SEUI history audit after the next gig wraps — informs v45-01 retroactively (v45-01 already shipped; this is for forensic record).
- Property test `numRuns` is 20, not 100. Crank to 100+ in monthly soak runs.
- `openai` npm dep + `template-parser.ts` left as orphans from v50-02 amputation. Safe to delete in a future dependency-cleanup pass.

---

## Resume Instructions

1. Run `git pull` (multi-computer workflow)
2. Run `/paul:resume` (will load STATE, archive this handoff)
3. Or run `/paul:plan` directly — STATE is current, plan v50-04 from ARCHITECTURE.md §4

---

*Handoff created: 2026-04-26*
