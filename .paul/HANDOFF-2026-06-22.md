# PAUL Handoff

**Date:** 2026-06-22
**Status:** paused (clean phase boundary — v11.7-05 complete, all pushed)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (centralreform.live) — multi-tenant (CRC + Brothers Lazaroff on brotherslazaroff.live).
**Core value:** The band gets the right charts + recordings on their iPads each week; Daniel authors setlists conversationally via Claude + MCP (the MCP server is the PRIMARY authoring surface; the browser app is the band/consumer surface).

---

## Current State

**Version:** 11.7.0
**Milestone:** 🚧 v11.7 — Discoverability & Deferred Backlog (MCP Asymmetry Principle) — **5 of 7 phases complete**
**Phase:** v11.7-05 (F3 library density) ✅ COMPLETE (2/2) → next is v11.7-06 (infra adjacents)

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY     [v11.7-05 complete; phase boundary]
  ✓        ✓        ✓
```

All work committed + pushed to `origin master`. Working tree has only unrelated untracked research/screenshot files (not part of any phase).

---

## What Was Done (this session, 2026-06-22)

1. **Resumed** at the v11.7-04-complete boundary; `git pull` clean.
2. **Closed v11.7-04 (P2 reverse-index sweep)** — `feat(v11.7-04)` `89f8c4c8f9`. NEW MCP `find_setlists_from_template({templateId})` (setlists.ts; reverse of `clone_setlist_from_template`'s `sourceTemplateId`) + `find_contact({email?,nameContains?})` (contacts.ts; reverse of create/delete_contact, assertEditor-gated). **Index-free** — single-field query + in-memory `rowOrg` cross-tenant wall, NO composite index / NO deploy (templateId is org-unique → no collision; matches the v11.7-03 idiom). +8 emulator cases. Qualify caught+fixed a Timestamp→null date bug (route through `serializeSetlist`). tsc 0 / emulator 24/24 / build 0.
3. **Discussed + planned + shipped v11.7-05 (F3 library density), 2 plans:**
   - **05-01** `feat(v11.7-05-01)` `325fae1e9a` — densified `LibraryFileRow` into a Logic-Pro track-list row + split the filename composer parenthetical into a dimmed sub-label (NEW pure `src/lib/library/chart-composer.ts` `splitChartComposer`); key + recency now visible at all widths; ≥44px tap floor (`min-h-11`). 8 + 5 new tests.
   - **05-02** `feat(v11.7-05-02)` `f3c5750614` — bind picker (`ChartBindPopover` + `ChartBindDialog`) rows now show composer + sparse key (`song.defaults?.key`) via a NEW shared `ChartPickerItemContent`. ChartBindPopover test +1 (10/10).
   - **DECISION (Daniel 2026-06-22 via /paul:discuss):** all 3 library surfaces in scope; **TEXT-ONLY — NO thumbnails** (overrides the ROADMAP "thumbnails" wording). Verify-first found search + key/topic/recency FILTERS already existed → phase = density + composer, not a rebuild.

Quality floor held everywhere: tsc 0 / library 53 + setlist-grid 167 tests / `next build --webpack` 0. /ui-ux-pro-max consulted on the UI work.

---

## What's In Progress

Nothing mid-flight. Clean phase boundary.

---

## What's Next

**Immediate:** `/paul:plan` for **Phase v11.7-06 — Infra adjacents** (backend, no UI).

**Scope (from STATE.md → Deferred Issues):**
- **recordings-collection org-scoping** — `subscribeRecordingsForSong` is songId-only + `/api/recordings/upload` hardcodes `orgId: DEFAULT_ORG_ID`. Fix: stamp the upload from host `x-org-id`, THEN host-filter the subscribe (order matters — filtering first would hide ALL broslaz recordings).
- **`finalize_chart_upload` (signed-URL path) org-stamp gap** — add `org` param to `finalizeChartUpload` + pass `orgFrom(extra)`.
- **anon chord-cache org-scoping** — `/api/library/chord-cache` POST (chordData) + PATCH (nativeKey/lastUsed) write by fileId with no tenancy check; org-scope BOTH together.
- **cheap v7.0 fold-forward hygiene** — `extractApiError` unification, dead TanStack block in `SetlistGrid`, unused `Recording.durationSeconds`, recordings/upload caps, etc.

⚠️ **If any fix needs a prod backfill** (e.g. stamping `orgId` on existing recordings): single-owner executor + dry-run + idempotency marker + rollback (autonomy directive). This is why this phase warrants a deliberate start.

**After that:** 07 broslaz authed design pass (UI; /ui-ux-pro-max BLOCKING) → milestone close.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (read first — Session Continuity has the next-action detail) |
| `.paul/ROADMAP.md` | v11.7 phase table + § v11.7-06 detail |
| `.paul/phases/v11.7-05-library-density/v11.7-05-02-SUMMARY.md` | Last completed plan |
| `.paul/UAT-PENDING.md` | 2 non-blocking iPad-density checks (05-01 + 05-02) to confirm on real hardware |
| `src/lib/library/chart-composer.ts` | `splitChartComposer` (reusable) |
| `src/components/setlist/grid/ChartPickerItemContent.tsx` | shared bind-picker row content |

---

## Constraints / Posture (carried)

- **Autonomy directive binding:** run autonomously, auto-commit + push per phase to `master`; chain PLAN→APPLY→UNIFY; STOP only at phase/milestone boundaries, STOP-gates (prod side-effects: publish/notify, destructive migration, auth changes), or genuine 2+-option decisions. Firebase deploys/backfills are AUTO tasks (single-owner = executor; dry-run + rollback).
- **Quality floor:** tsc clean + tests + AC proof; `SKIP_ENV_VALIDATION=1 npx next build --webpack` before declaring deployable; emulator tests where rules/queries change; /ui-ux-pro-max BLOCKING on UI phases (07).
- **err-public cross-tenant HARD WALL:** all org-scoping in 06 must preserve CRC behavior (CRC pool is 100% crc-stamped) and never leak across tenants. err-public WITHIN a tenant; hard wall ACROSS.
- **No publish/notify, no assignment-scheduling, no photo-import** (Daniel).
- Push `origin master` (NOT `master:main`). Multi-computer — `git pull` first next session. Master may have advanced via parallel `.coord`/v7.1 cowork commits (it did this session) — pull first.

---

## Resume Instructions

1. `git pull` (multi-computer; master moves via parallel cowork too).
2. Read `.paul/STATE.md` (Current Position + Session Continuity).
3. Run `/paul:resume` (or go straight to `/paul:plan` for v11.7-06).

---

*Handoff created: 2026-06-22*
