---
phase: v11.4-04-membership-toggle-backfill
plan: 01
subsystem: admin-membership
tags: [org-membership, admin, manage-people, multi-tenant, d8, toggle]

requires: []
provides:
  - admin Band-access (CRC/BL/Both) control on ALL non-pending rows (was leader-only)
affects: [v11.4-04-02 default-both rollout (backfill consumes the same orgIds the toggle sets)]

tech-stack:
  added: []
  patterns:
    - "Org membership applies to all roles (leaders=authoring, musicians/members=audience candidacy)"

key-files:
  created:
    - src/components/admin/UserRow.test.tsx
  modified:
    - src/components/admin/UserRow.tsx

key-decisions:
  - "Membership control opened to ALL non-pending rows (Daniel 2026-06-11: everyone incl. leaders) — supersedes v11.1-02-02 authoring-tier-only scoping"
  - "Pure UI-gating change — set-role/updateUserRole already accept orgIds for any role"

duration: ~20min
started: 2026-06-11
completed: 2026-06-11
---

# Phase v11.4-04 Plan 01: All-roles Band-access toggle (D8 item 5) Summary

**The admin "Band access" tri-state control (CRC only / Brothers Lazaroff only / Both) now shows on every non-pending /manage→People row — musicians and members, not just leaders — so an admin can set any person's org membership before/after the Plan-02 default-both backfill. Pure UI-gating change (the claim+doc write path already accepts orgIds for any role); all gates green.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~20 min |
| Tasks | 2 completed |
| Files | 1 created + 1 modified |
| Tests | UserRow.test.tsx 3/3 (new — first UserRow test) |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: control + badge on all non-pending rows (admin only) | Pass | `showOrgMembership = isCurrentAdmin && effectiveRole !== 'pending'`; badge ungated from isLeaderTier; non-admin viewer sees no control (asserted) |
| AC-2: setting a musician to "Both" writes both orgIds | Pass | select "Both" → confirm → `updateUserRole('m1','musician',['crc','brotherslazaroff'])` (set-role writes doc + Auth claim lockstep) |
| AC-3: copy reflects all roles | Pass | confirm dialog generalized: belongs-to / authoring for leaders / setlists+notifications for musicians |

## Accomplishments

- `UserRow.tsx`: opened the existing Band-access `<Select>` + membership badge (desktop + mobile) from leader-only to all non-pending rows; generalized the confirm-dialog copy + the MEMBERSHIP comment header (notes the v11.4-04 everyone-incl-leaders ratification). No change to `MEMBERSHIP_TO_ORGIDS` / `confirmMembershipChange` / `updateUserRole` / the `set-role` route — the write path was already role-agnostic.
- New `UserRow.test.tsx` (first test for this component): control shows for a musician row under an admin, hidden for a non-admin, and "Both" posts `['crc','brotherslazaroff']`. Radix Select/AlertDialog driven via RTL + userEvent with jsdom shims.

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Control on all non-pending rows | Daniel ratified membership applies to everyone incl. leaders (cross-tenant authoring) | Supersedes v11.1-02-02 authoring-tier-only scoping (recorded in STATE) |
| Pure UI change, no API touch | `set-role` + `updateUserRole` already accept orgIds for any role | Minimal surface; write path unchanged |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 0 | — |

**Total impact:** None. Executed as written.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| None (benign React `checked`-without-onChange warning on the row checkbox — pre-existing optional `onSelect`) | No action |

## Next Phase Readiness

**Ready:**
- v11.4-04-02 (default-both rollout): new-account default + backfill script (diagnose/dry-run/apply/rollback, per-user snapshot, claim+doc lockstep) with the prod `--apply` as a human-action gate. The toggle from this plan sets the same orgIds the backfill will mass-stamp.
- After 02 → v11.4 milestone closes (`/paul:complete-milestone`; ACCESS-POLICY oracle → v0.4).

**Concerns:**
- 1 UAT item appended (live admin check — control shows for a musician + persists).

**Blockers:**
- None.

---
*Phase: v11.4-04-membership-toggle-backfill, Plan: 01*
*Completed: 2026-06-11*
