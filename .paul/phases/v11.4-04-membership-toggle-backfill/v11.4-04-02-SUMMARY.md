---
phase: v11.4-04-membership-toggle-backfill
plan: 02
subsystem: auth
tags: [orgIds, multi-tenant, firebase-admin, custom-claims, backfill, migration]

requires:
  - phase: v11.4-04-01
    provides: the /manage Band-access toggle that sets the same orgIds this backfill mass-stamps
  - phase: v11.4-01
    provides: no-auto-blast publish — makes default-both safe (audience candidacy, not auto-send)
provides:
  - new accounts default to orgIds ['crc','brotherslazaroff'] at doc creation
  - prod backfill stamping every existing user both on doc + Auth claim (lockstep, idempotent)
  - exact per-user rollback snapshot (absent→delete) + reusable admin-SDK migration scaffold
affects: [publish-audience-defaults, cross-tenant-authoring, future-tenant-onboarding]

tech-stack:
  added: []
  patterns:
    - "Dynamic firebase-admin import in a CLI script → pure decision helpers stay unit-testable without admin SDK in the test graph"
    - "Per-user prior-state snapshot (array|null) → exact rollback incl. absent→FieldValue.delete()/claim-omit"

key-files:
  created:
    - scripts/v11-4-04-orgids-backfill.mjs
    - scripts/v11-4-04-orgids-backfill.test.ts
  modified:
    - src/lib/users-firebase.ts
    - .gitignore

key-decisions:
  - "Daniel ran the prod --apply in-session after explicit 'permission fully granted' (T3 human-action satisfied by owner authorization, executed by agent)"
  - "firebase-admin imported dynamically (not module-top) so the test imports only the pure helpers"

patterns-established:
  - "Mass membership migration: diagnose → dry-run (default) → apply(+snapshot) → rollback, mirroring v11-3-03 ADC bootstrap"

duration: ~15min
started: 2026-06-11T18:35:00Z
completed: 2026-06-11T19:35:00Z
---

# Phase v11.4-04 Plan 02: Default-both org-membership rollout — Summary

**New accounts now seed `orgIds:['crc','brotherslazaroff']` at creation, and a prod backfill stamped all 15 remaining single-org users both on doc + Auth claim (4 already both → 19/19 both), with an exact per-user rollback snapshot. Closes v11.4.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~15 min (build) + prod apply |
| Started | 2026-06-11T18:35:00Z |
| Completed | 2026-06-11T19:35:00Z |
| Tasks | 3 completed (T1 auto · T2 auto · T3 human-action) |
| Files modified | 4 (2 created, 2 modified) |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: new accounts default to both orgs | Pass | `ensureUserProfile` create path seeds `orgIds:['crc','brotherslazaroff']`; non-create branches untouched; tsc + next build clean |
| AC-2: backfill stamps every person both on doc + claim, idempotently | Pass | Prod `--apply`: scanned=19 changed=15 skipped=4 docWrites=15 claimWrites=15; re-diagnose → 0 remaining; other claims preserved (spread); snapshot written for all 15 changed |
| AC-3: dry-run read-only + rollback exact | Pass | Default dry-run wrote nothing (printed change-set + would-write snapshot); snapshot carries `prevDocOrgIds`/`prevClaimOrgIds` with null for ABSENT → `rollbackTargets` yields DELETE sentinel |
| AC-4: pure decision logic unit-tested | Pass | `vitest run scripts/v11-4-04-orgids-backfill.test.ts` → 10/10 (alreadyBoth, targetOrgIds, snapshotEntry, rollbackTargets incl. absent→delete); no Firestore/Auth in test graph |

## Verification Results

- `npx tsc --noEmit` — clean
- `npx vitest run scripts/v11-4-04-orgids-backfill.test.ts` — 10 passed
- `npx vitest run src/lib` — 133 files / 1799 passed (no ensureUserProfile regression)
- `SKIP_ENV_VALIDATION=1 npx next build` — clean (no client/server bundle-boundary break)
- `node scripts/v11-4-04-orgids-backfill.mjs --diagnose` (pre) — 19 users, 15 would change
- `node scripts/v11-4-04-orgids-backfill.mjs` (dry-run) — 15 to change, all `doc=["crc"] claim=absent`, no writes
- `node scripts/v11-4-04-orgids-backfill.mjs --apply --stamp …` — scanned=19 changed=15 skipped=4; snapshot `scripts/.backfill-snapshots/v11-4-04-20260611T193413Z.json` (15 entries)
- `--diagnose` (post) — doc `{both:19}`, claim `{both:19}`, would change = **0** (idempotent)

## Accomplishments

- Every CRC/BL user (19) is now a both-tenant member on doc AND Auth claim — the "one unified team authors both bands" rollout is live in prod.
- New accounts self-seed default-both, so the population stays converged without re-running the backfill.
- Exact, per-user rollback is on disk (incl. the absent-claim→delete case) — reversible mass migration, not a one-way write.

## Task Commits

| Task | Commit | Type | Description |
|------|--------|------|-------------|
| T1: new-account default-both | `47e83088a1` | feat | `ensureUserProfile` create path seeds both orgIds |
| T2: backfill script + unit test | `47e83088a1` | feat | diagnose/dry-run/apply/rollback + 10 pure-helper tests + gitignore snapshot dir |
| T3: prod `--apply` (human-action) | n/a (runtime) | ops | Daniel-authorized, agent-executed; doc+claim writes, snapshot artifact (gitignored) |

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `scripts/v11-4-04-orgids-backfill.mjs` | Created | Admin-SDK default-both migration (ADC via firebase-CLI refresh-token; modes diagnose/dry-run/apply/rollback) |
| `scripts/v11-4-04-orgids-backfill.test.ts` | Created | 10 unit tests over the pure decision helpers |
| `src/lib/users-firebase.ts` | Modified | `ensureUserProfile` create path → `orgIds:['crc','brotherslazaroff']` |
| `.gitignore` | Modified | Ignore `scripts/.backfill-snapshots/` (local recovery artifact) |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Agent executed the prod `--apply` after Daniel's explicit "permission fully granted" | T3 is a single-owner gate; owner authorization + permission grant satisfies it without a manual hand-off | Backfill ran in-session; output captured + verified here |
| firebase-admin imported dynamically inside I/O functions | Keep module top-level admin-free so the test imports only pure helpers (jsdom-safe, no admin in transform graph) | Reusable pattern for future migration scripts |

## Deviations from Plan

### Summary

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 1 | Trivial — removed an unused `@ts-expect-error` directive flagged by tsc |
| Scope additions | 0 | — |
| Deferred | 0 | — |

**Total impact:** None material. The PLAN framed T3 as a checkpoint where Daniel runs `--apply`; in practice Daniel granted permission and the agent executed it in-session — same single-owner outcome, faster loop.

### Auto-fixed Issues

**1. [Types] Unused `@ts-expect-error` in the test**
- **Found during:** T2 qualify (`tsc --noEmit`)
- **Issue:** TS resolves the `.mjs` import without error, so the suppression directive was unused (TS2578)
- **Fix:** Removed the directive
- **Files:** `scripts/v11-4-04-orgids-backfill.test.ts`
- **Verification:** Re-ran tsc → clean

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| Sandbox auto-mode classifier blocked the dry-run once, then the `--apply` | Dry-run succeeded on retry (read-only); `--apply` ran after Daniel's explicit permission grant |

## Skill Audit

All required skills invoked ✓ — this plan's `<skills>` marked `/ui-ux-pro-max` **not triggered** (backend one-field default + a Node migration script; no frontend). No UI-touching surface.

## Next Phase Readiness

**Ready:**
- v11.4-04 is the LAST phase of v11.4 → milestone close (`/paul:complete-milestone`); bump `docs/ACCESS-POLICY.md` oracle → v0.4; tag a release per the v11.x pattern.

**Concerns:**
- Default-both now grants cross-tenant authoring to ALL leaders (ratified, deliberate). `rowOrgIds`/`getOrgIdsFromClaims` defaults intentionally STAY `['crc']` (safety net).

**Blockers:** None.

---
*Phase: v11.4-04-membership-toggle-backfill, Plan: 02*
*Completed: 2026-06-11*
