---
phase: v11.4-04-membership-toggle-backfill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/users-firebase.ts
  - scripts/v11-4-04-orgids-backfill.mjs
  - scripts/v11-4-04-orgids-backfill.test.ts
autonomous: false
---

<objective>
## Goal
Roll out **default-both org membership** to every person (D8 item 5, final plan): (a) new accounts
get `orgIds: ['crc','brotherslazaroff']` at doc creation; (b) a prod backfill stamps EVERY existing
person both — on the user doc AND the Auth custom claim (lockstep) — with an exact, per-user
rollback. Per Daniel's ratified scope, this includes band_leaders/admins (cross-tenant authoring).

## Purpose
Closes v11.4: with no-auto-blast shipped (v11.4-01), default-both is safe — it makes everyone a
default publish-AUDIENCE candidate (musicians) and grants cross-tenant authoring (leaders), per the
ratified "one unified team authors both bands" call. After this, v11.4 closes (oracle → v0.4).

## Output
- `users-firebase.ts`: `ensureUserProfile` creates new docs with `orgIds: ['crc','brotherslazaroff']`.
- `scripts/v11-4-04-orgids-backfill.mjs`: admin-SDK migration (firebase-CLI refresh-token ADC),
  modes `--diagnose` / dry-run (default) / `--apply` / `--rollback <snapshot.json>`, stamping doc +
  claim both, idempotent (skip already-both), writing a per-user prior-state snapshot for exact
  reversal (incl. FieldValue.delete() for previously-absent orgIds).
- A unit test for the script's pure decision helpers.
- The prod `--apply` runs as a **human-action checkpoint** (single-owner; mass auth-claims write).
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work
@.paul/phases/v11.4-04-membership-toggle-backfill/v11.4-04-01-SUMMARY.md   # the toggle sets the same orgIds this backfill mass-stamps

## Source Files
@src/lib/users-firebase.ts                       # ensureUserProfile create path (role:'pending', line ~49) — add orgIds default
@src/lib/org/membership.ts                       # rowOrgIds / getOrgIdsFromClaims / orgIdsEqual (default ['crc'] STAYS)
@src/app/api/admin/set-role/route.ts             # claim+doc lockstep write pattern to mirror in the script
@scripts/v11-3-03-library-orphan-sweep.mjs       # the prod-script ADC bootstrap + flag pattern to MIRROR

## Verified current behavior (read this session)
- New user doc is created by `ensureUserProfile(user)` (users-firebase.ts) with `role:'pending'`
  and NO orgIds → rowOrgIds defaults ['crc']. This is a CLIENT-side write of the user's OWN doc
  (allowed by the users rule `uid == request.auth.uid`); orgIds on the doc is membership data only
  — it does NOT grant cross-tenant READ (that's claim-based via callerOrgs), so a self-set doc
  orgIds=both is safe (authoring still requires the role + claim).
- `set-role` writes orgIds to the user doc + Auth claim in lockstep (the pattern the script mirrors).
- `getOrgIdsFromClaims`/`rowOrgIds` default to ['crc'] (CRC-safety net) — KEEP. "Both" is explicit
  data written by this rollout, NOT a flipped global default.
- Prod-script auth on this box (no SA creds): firebase-tools `refresh_token` → temp `authorized_user`
  ADC → `GOOGLE_APPLICATION_CREDENTIALS`, delete temp in finally (v11-3-03 + add-auth-domains pattern).
- Backfill scope RATIFIED (Daniel 2026-06-11): EVERYONE incl. leaders.
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /ui-ux-pro-max | n/a | NOT triggered — backend (one-field default) + a Node migration script; no frontend. | — |
</skills>

<acceptance_criteria>

## AC-1: new accounts default to both orgs
```gherkin
Given a brand-new user signs in and ensureUserProfile creates their users/{uid} doc
When the doc is created
Then it carries orgIds: ['crc','brotherslazaroff'] (default-both) — existing docs are NOT
  re-created or modified by this path
```

## AC-2: the backfill stamps every person both on doc + claim, idempotently
```gherkin
Given the prod backfill runs with --apply over all users
When it processes each user
Then it sets users/{uid}.orgIds AND the Auth custom claim orgIds to ['crc','brotherslazaroff'],
  preserving all other claims (role, soundEngineer); a user already at both (doc AND claim) is
  skipped (idempotent — re-running --apply changes nothing); a per-user snapshot of prior doc+claim
  orgIds (incl. an ABSENT marker) is written for every CHANGED user
```

## AC-3: dry-run is read-only and rollback is exact
```gherkin
Given the backfill runs in the default (dry-run) mode
When it runs
Then it writes/sends NOTHING — it only reports the change-set + would-write snapshot; AND given a
  snapshot from a prior --apply, `--rollback <snapshot>` restores each user's doc+claim orgIds to
  the recorded prior value, deleting the orgIds field where the prior state was ABSENT
```

## AC-4: the pure decision logic is unit-tested
```gherkin
Given the script's pure helpers (already-both detection, next-orgIds, snapshot-entry builder,
  rollback-target resolution incl. the absent→delete case)
When the unit test runs
Then each is verified without touching Firestore/Auth (no prod dependency in the test)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: New accounts default to both orgs</name>
  <files>src/lib/users-firebase.ts</files>
  <action>
    In `ensureUserProfile`, the create path that writes the new user doc with `role:'pending'`:
    add `orgIds: ['crc','brotherslazaroff']` to the created doc (default-both at creation). Only the
    CREATE path — do NOT add orgIds to any update/lastLoginAt path (existing docs are owned by the
    backfill + the toggle, and orgIdsEqual/sync-claims manage drift). Add a one-line comment citing
    v11.4-04 default-both. Prefer referencing the org ids via the existing registry/DEFAULT if a
    clean import exists; a literal ['crc','brotherslazaroff'] is acceptable (matches UserRow's
    MEMBERSHIP_TO_ORGIDS.both) — do not invent a new constant module.
    Avoid: changing the role default (stays 'pending'), touching the non-create branches, or
    altering rowOrgIds/getOrgIdsFromClaims defaults.
  </action>
  <verify>npx tsc --noEmit; grep shows orgIds:['crc','brotherslazaroff'] only in ensureUserProfile's create branch</verify>
  <done>AC-1 satisfied: new user docs are created with both orgIds; existing docs untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill script (diagnose/dry-run/apply/rollback) + unit test</name>
  <files>scripts/v11-4-04-orgids-backfill.mjs, scripts/v11-4-04-orgids-backfill.test.ts</files>
  <action>
    Create `scripts/v11-4-04-orgids-backfill.mjs` mirroring v11-3-03's ADC bootstrap (refreshToken
    from firebase-tools.json → temp authorized_user ADC → GOOGLE_APPLICATION_CREDENTIALS, unlink in
    finally; PROJECT='crcmusiccharts'). Behavior:
    - Iterate ALL `users` docs. For each, read current doc orgIds + the Auth custom claim orgIds
      (auth.getUser(uid).customClaims.orgIds). TARGET = ['crc','brotherslazaroff'].
    - SKIP (idempotent) when BOTH doc orgIds AND claim orgIds already equal TARGET (order-insensitive).
    - `--diagnose`: read-only; print the distribution (how many users at crc-only / bl-only / both /
      absent, doc vs claim) and the count that WOULD change. No writes.
    - default (dry-run): read-only; print the per-user change-set + the snapshot that WOULD be written.
      No doc writes, no setCustomUserClaims.
    - `--apply`: for each changed user, write users/{uid}.orgIds = TARGET (merge/update) AND
      setCustomUserClaims(uid, {...existingClaims, orgIds: TARGET}) (preserve role/soundEngineer/etc.)
      — claim+doc lockstep like set-role. Accumulate a snapshot array of
      { uid, prevDocOrgIds: <array|null>, prevClaimOrgIds: <array|null> } for every CHANGED user
      (null = field ABSENT). Write the snapshot to `scripts/.backfill-snapshots/v11-4-04-<UTC-ish>.json`
      — derive the timestamp from a passed `--stamp <iso>` arg (Date.now()/new Date() are fine in a
      standalone Node script, but accept --stamp for determinism); print the snapshot path.
    - `--rollback <snapshot.json>`: read the snapshot; for each entry restore doc + claim orgIds to
      prev: if prev is an array → write it; if prev is null (ABSENT) → remove the field (doc:
      FieldValue.delete(); claim: setCustomUserClaims with orgIds omitted). Idempotent + safe to
      re-run.
    - SAFETY: dry-run is the default; --apply/--rollback require the explicit flag. Never drop other
      claims. Log a summary (scanned / changed / skipped). Guard: refuse --rollback without a readable
      snapshot file.
    Factor the PURE decision logic into exported helpers so they're unit-testable WITHOUT Firestore:
      `alreadyBoth(docOrgIds, claimOrgIds)`, `targetOrgIds()`, `snapshotEntry(uid, docOrgIds, claimOrgIds)`,
      `rollbackTargets(entry)` (returns {doc, claim} where a null prev → a DELETE sentinel). Keep the
      admin-SDK I/O in main() calling these helpers.
    Create `scripts/v11-4-04-orgids-backfill.test.ts` (vitest; the config already includes
    `scripts/**/*.test.ts` + strips shebangs on .mjs import) importing those pure helpers:
      - alreadyBoth: true only when BOTH are the two-tenant set (order-insensitive); false for
        crc-only / bl-only / absent / doc-both-but-claim-crc.
      - targetOrgIds: ['crc','brotherslazaroff'].
      - snapshotEntry: records prev arrays + null for absent.
      - rollbackTargets: array prev → that array; null prev → the DELETE sentinel (for doc + claim).
    Avoid: writing anything in dry-run/diagnose; hardcoding a real ADC; importing firebase-admin into
    the test (test only the pure helpers); flipping rowOrgIds/getOrgIdsFromClaims defaults.
  </action>
  <verify>npx vitest run scripts/v11-4-04-orgids-backfill.test.ts (green); `node scripts/v11-4-04-orgids-backfill.mjs --diagnose` and the default dry-run both run read-only against prod and print the change-set (proves connection + logic; NO writes)</verify>
  <done>AC-2 + AC-3 + AC-4 satisfied at the build+dry-run level: idempotent stamp + snapshot + exact rollback logic, unit-tested, dry-run proven read-only.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The prod default-both backfill — a mass write to ALL users' doc orgIds + Auth custom claims, granting every person both tenants (incl. leaders → cross-tenant authoring).</what-built>
  <how-to-verify>
    Single-owner prod run (Daniel, or whoever owns this run — claim it first):
    1. `firebase login` is active on this box (the dry-run already proved ADC).
    2. Review the dry-run output from Task 2 (who changes, how many).
    3. Run the snapshot-producing apply:
       `node scripts/v11-4-04-orgids-backfill.mjs --apply --stamp "$(date -u +%Y%m%dT%H%M%SZ)"`
    4. Confirm the printed summary (scanned/changed/skipped) + that a snapshot file was written under
       `scripts/.backfill-snapshots/`.
    5. Spot-check: in /manage People a few rows now show "CRC + BL"; a `list_setlists`/preview on the
       BL connector shows the broadened default audience.
    Rollback if needed: `node scripts/v11-4-04-orgids-backfill.mjs --rollback scripts/.backfill-snapshots/<file>.json`.
  </how-to-verify>
  <resume-signal>Type "applied" (with the changed-count) to continue to UNIFY + milestone close, or describe any issue.</resume-signal>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- `rowOrgIds` / `getOrgIdsFromClaims` defaults (stay `['crc']` — the CRC-safety net).
- `set-role` / `updateUserRole` / `sync-claims` (the script mirrors set-role's lockstep; it does not
  modify these routes).
- The v11.4-01 recipients_required guard, v11.2-02 org wall, v11-06-02 no-arg-injection seam.
- ensureUserProfile's non-create branches (role default, lastLoginAt update).

## SCOPE LIMITS
- The snapshot directory `scripts/.backfill-snapshots/` is a local run artifact — do NOT commit
  generated snapshot JSONs (gitignore the dir or only commit a .gitkeep/README).
- No Firestore rules change (membership reads/writes already covered; orgIds on users is not
  rules-gated by org — it's uid-gated).
- No new-tenant generalization (TARGET is the two current orgs); revisit if a 3rd tenant lands.
- No real publish/notify (this only changes membership data; sends still require the explicit picker).
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` clean
- [ ] `npx vitest run scripts/v11-4-04-orgids-backfill.test.ts` green
- [ ] `npx vitest run src/lib` green (no regression from the ensureUserProfile change)
- [ ] `SKIP_ENV_VALIDATION=1 npx next build` clean
- [ ] `--diagnose` + dry-run run read-only against prod and print the change-set (no writes)
- [ ] **[human-action]** prod `--apply` run by the single owner; snapshot written; spot-check passes
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- New accounts default to both orgs; every existing person stamped both on doc + claim (idempotent).
- Exact rollback available via the per-user snapshot (incl. absent→delete).
- Defaults helper unchanged (`['crc']` safety net); no rules change; no auto-send.
- tsc + script unit test + src/lib + next build green; dry-run proven; prod apply human-run.
- v11.4 ready to close (`/paul:complete-milestone`; ACCESS-POLICY oracle → v0.4).
</success_criteria>

<output>
After completion, create `.paul/phases/v11.4-04-membership-toggle-backfill/v11.4-04-02-SUMMARY.md`
</output>
