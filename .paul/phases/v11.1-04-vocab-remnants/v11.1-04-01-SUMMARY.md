---
phase: v11.1-04-vocab-remnants
plan: 01
subsystem: ui
tags: [multi-tenant, vocab, i18n-lite, org-voice, nextjs]

requires:
  - phase: v11-03 / v11-05-05 (org vocab layer)
    provides: label(orgId,key) + OVERRIDES band voice + useOrg() hook
provides:
  - vocab keys upcomingSection / createNewSetlistHeading / matrixTitle
  - SetlistDashboard + SetlistMatrixView headers routed through the vocab layer
affects: [future tenants (headers now org-aware)]

tech-stack:
  added: []
  patterns:
    - "Dashboard/matrix section headers resolve via label(useOrg(), key) — no inline liturgical literals"

key-files:
  created: []
  modified:
    - src/lib/org/vocab.ts
    - src/lib/org/__tests__/vocab.test.ts
    - src/components/setlist/SetlistDashboard.tsx
    - src/components/setlist/SetlistMatrixView.tsx

key-decisions:
  - "broslaz band-voice strings follow existing OVERRIDES voice: Upcoming Shows / Create New Set / Set Matrix"
  - "SERVICE_TYPE_LABELS vocab-table refactor DEFERRED — gated-away for broslaz (selector hidden via hidesLiturgicalFields); separate larger refactor"

patterns-established:
  - "New per-tenant header text = one vocab key (crc base byte-identical + broslaz override), consumed via label(useOrg(), key)"

duration: ~20min
started: 2026-06-09
completed: 2026-06-09
---

# Phase v11.1-04 Plan 01: broslaz liturgical vocab sweep Summary

**The setlist dashboard section headers ("Upcoming Services", "Create New Setlist") and the matrix view title ("Liturgical Matrix") — the last hardcoded synagogue strings that bypassed the org vocab layer — now resolve via `label(useOrg(), key)`, so brotherslazaroff.live reads "Upcoming Shows" / "Create New Set" / "Set Matrix"; CRC byte-identical. v11.1 milestone content complete.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~20 min |
| Started | 2026-06-09 |
| Completed | 2026-06-09 |
| Tasks | 3 completed (3/3 E/Q PASS) |
| Files modified | 4 (incl. 1 test) |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: broslaz reads band voice | Pass | "Upcoming Shows" / "Create New Set" / "Set Matrix" via OVERRIDES.brotherslazaroff |
| AC-2: CRC byte-identical | Pass | label("crc",…) returns "Upcoming Services" / "Create New Setlist" / "Liturgical Matrix" verbatim → dashboard + matrix unchanged |
| AC-3: vocab-driven (no inline literals) | Pass | grep confirms no `>Upcoming Services<` / `>Create New Setlist<` / `>Liturgical Matrix<` literals remain; both components import useOrg + label |
| AC-4: quality gates | Pass | tsc clean · full suite 3339/0 · SKIP_ENV_VALIDATION=1 next build clean |

## Accomplishments

- Closed the last broslaz liturgical-vocab remnants (the v11-05-05 pass missed the dashboard headers + matrix title).
- Established the pattern fully: every per-tenant header now flows through `label(useOrg(), key)`.
- v11.1 milestone content complete (4/4 phases): branding, multi-org authoring, library isolation, vocab.

## Task Commits

Single phase commit under the v11.x autonomy posture; hash in STATE Git State after transition.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/org/vocab.ts` | Modified | +3 keys (upcomingSection/createNewSetlistHeading/matrixTitle); crc base + broslaz overrides |
| `src/lib/org/__tests__/vocab.test.ts` | Modified | +3 byte-identical-base / band-override assertions |
| `src/components/setlist/SetlistDashboard.tsx` | Modified | useOrg+label; :134 + :144 headers via label() |
| `src/components/setlist/SetlistMatrixView.tsx` | Modified | useOrg+label; :65 title via label() |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| broslaz strings = Upcoming Shows / Create New Set / Set Matrix | Consistent with existing band-voice OVERRIDES (set/Shows) | No new ambiguity; baked into vocab.ts |
| SERVICE_TYPE_LABELS refactor DEFERRED | The service-type selector/strip is hidden for broslaz via hidesLiturgicalFields → those labels never render on broslaz; a vocab-driven service-type table is a separate larger change | Logged as deferred issue |
| Leave SetlistDashboard:175 "Library & Past Events" | Org-neutral phrasing; not a liturgical remnant | Out of scope |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 1 | SERVICE_TYPE_LABELS vocab table (logged) |

**Total impact:** Plan executed exactly as written. No fixture breaks.

### Deferred Items

- **SERVICE_TYPE_LABELS vocab-driven table** — the Shabbat Morning/Friday Night/Erev Shabbat/Rosh Hashanah labels are hardcoded in SetlistCards / CreationWizard / SetlistMetaEditSheet / interview-defaults + the SetlistMatrixView `<option>`s. Gated-away for broslaz today (selector hidden), so not a live remnant; convert to a vocab-driven table if a non-synagogue tenant ever needs service-type-like categories.

## Issues Encountered

None.

## Next Phase Readiness

**Ready:**
- v11.1 milestone content is COMPLETE (4/4 phases shipped to prod). Next: `/paul:complete-milestone` to archive v11.1 (after the standing UAT-PENDING items are confirmed on the live tenant).

**Concerns:**
- recordings-collection scoping + SERVICE_TYPE_LABELS table remain as logged deferred follow-ups (neither blocks broslaz's lived experience).

**Blockers:**
- None.

**Standing UAT-PENDING (non-blocking, Daniel hands-on on the live tenant before formal milestone close):**
- broslaz authed nav: "Brothers Lazaroff" + BL monogram (desktop + iPad-WebKit).
- broslaz /library: only broslaz charts (tab + add-songs picker + header search); admin "All sites" toggle reveals full pool.
- broslaz dashboard: "Upcoming Shows" / "Create New Set"; matrix "Set Matrix".
- broslaz MCP authoring: connect Claude Desktop to https://www.brotherslazaroff.live/api/mcp → author a test setlist → lands broslaz.
- admin: set a leader's Band access in /manage → People → badge updates + persists.
- CRC: all of the above unchanged (byte-identical).

---
*Phase: v11.1-04-vocab-remnants, Plan: 01*
*Completed: 2026-06-09*
