---
phase: v11.1-04-vocab-remnants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/org/vocab.ts
  - src/lib/org/__tests__/vocab.test.ts
  - src/components/setlist/SetlistDashboard.tsx
  - src/components/setlist/SetlistMatrixView.tsx
autonomous: true
---

<objective>
## Goal
Close the remaining broslaz liturgical-vocab remnants — the hardcoded synagogue strings that bypass the org vocab layer (`label(org,key)`) and render wrong on brotherslazaroff.live: the `SetlistDashboard` section headers ("Upcoming Services", "Create New Setlist") and `SetlistMatrixView`'s "Liturgical Matrix" title. Route them through the vocab layer with band-voice overrides. CRC byte-identical.

## Purpose
v11-05-05's vocab pass converted the live edit surface + cards + wizard, but the dashboard section headers + matrix title were never routed through `label()` — they still say "Services" on a band's site. This is the LAST v11.1 phase; closing it completes the milestone.

## Output
- `vocab.ts` gains the missing keys (`upcomingSection`, `createNewSetlistHeading`, `matrixTitle`) with crc base (byte-identical) + broslaz band-voice overrides.
- `SetlistDashboard` resolves its three section headers via `useOrg()` + `label()` (currently zero vocab usage there).
- `SetlistMatrixView` resolves its title via `label()`.
- vocab unit test extended; CRC byte-identical asserted.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@src/lib/org/vocab.ts
@src/components/setlist/SetlistDashboard.tsx
@src/components/setlist/SetlistMatrixView.tsx

## Key facts established at plan time (Explore audit)
- Client org source = `useOrg(): OrgId` from `@/lib/org/org-context.tsx` (Context, defaults crc). Pattern: `const org = useOrg(); label(org, key)`. Already used by SetlistCards/CreationWizard/SetlistMetaEditSheet.
- `vocab.ts` `label(orgId,key)` + `OVERRIDES.brotherslazaroff` band voice ("set"/"Shows"/"Past shows"/"Plan Show"/"Upcoming Shows & Sets"). Adding keys follows this exact shape.
- **SetlistDashboard.tsx** uses NO vocab today (only `useCongregation()` for the greeting). Hardcoded headers: `:134` `<h3>Create New Setlist</h3>`, `:144` `<h4>Upcoming Services</h4>`, `:175` "Library & Past Events" (org-NEUTRAL — leave as-is).
- **SetlistCards.tsx** placeholder card already uses `label(org,'planPlaceholder')` → "Plan Service"/"Plan Show" DONE. Liturgical fields (Service-type selector, Rabbi input, Save-as-Default) already gated by `hidesLiturgicalFields(org)`.
- **SERVICE_TYPE_LABELS** (Shabbat Morning/Friday Night/Erev Shabbat…) in 4 files are gated-away for broslaz (the selector/strip is hidden via `hidesLiturgicalFields`) → NOT a live broslaz remnant. OUT OF SCOPE.
- **SetlistMatrixView.tsx:65** renders a hardcoded `<h2>Liturgical Matrix</h2>` (+ hardcoded Friday Night/Shabbat Morning options at :71-72). The title is the broslaz-wrong string; relabel via vocab.
- broslaz band-voice replacements (consistent with existing OVERRIDES): "Upcoming Services" → "Upcoming Shows"; "Create New Setlist" → "Create New Set"; "Liturgical Matrix" → "Set Matrix".
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /ui-ux-pro-max | required (BLOCKING) | Before the UI edits (Task 2) | ○ |

## Skill Invocation Checklist
- [ ] /ui-ux-pro-max loaded before Task 2
</skills>

<acceptance_criteria>

## AC-1: broslaz dashboard + matrix read in band voice
```gherkin
Given a user on brotherslazaroff.live viewing the setlist dashboard
When the section headers render
Then they read "Upcoming Shows" and "Create New Set" (not "Upcoming Services" / "Create New Setlist")
And the matrix view title reads "Set Matrix" (not "Liturgical Matrix")
```

## AC-2: CRC byte-identical
```gherkin
Given a user on centralreform.live
When the dashboard + matrix render
Then the headers read exactly "Create New Setlist", "Upcoming Services", "Library & Past Events", and "Liturgical Matrix" — unchanged from before this plan
```

## AC-3: vocab-driven (no new hardcoded liturgical header strings)
```gherkin
Given the new headers
When inspected
Then they resolve via label(org, key) (no inline synagogue literals in the JSX), with crc base + broslaz overrides in vocab.ts
```

## AC-4: quality gates
```gherkin
Given the implementation is complete
When tsc, the unit suite, and SKIP_ENV_VALIDATION=1 next build run
Then tsc is clean, all tests pass (incl. vocab cases), and the build compiles with no bundle-boundary error
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add the missing vocab keys (crc base byte-identical + broslaz overrides)</name>
  <files>src/lib/org/vocab.ts, src/lib/org/__tests__/vocab.test.ts</files>
  <action>
    1. `src/lib/org/vocab.ts`: extend `VocabKey` with `"upcomingSection" | "createNewSetlistHeading" | "matrixTitle"`.
       - BASE (crc, byte-identical to current hardcodes): `upcomingSection: "Upcoming Services"`, `createNewSetlistHeading: "Create New Setlist"`, `matrixTitle: "Liturgical Matrix"`.
       - OVERRIDES.brotherslazaroff: `upcomingSection: "Upcoming Shows"`, `createNewSetlistHeading: "Create New Set"`, `matrixTitle: "Set Matrix"`.
    2. `src/lib/org/__tests__/vocab.test.ts`: add assertions — crc returns the three base strings verbatim; brotherslazaroff returns the three overrides. (If the test file doesn't exist yet, create it with a focused describe block; check first.)
    Avoid: changing any existing key's value (CRC byte-identical across the whole vocab table).
  </action>
  <verify>`npx vitest run src/lib/org/__tests__/vocab.test.ts` green; grep confirms the 3 keys exist in BASE + OVERRIDES with the specified strings.</verify>
  <done>AC-3 (vocab-driven keys defined) + the data for AC-1/AC-2.</done>
</task>

<task type="auto">
  <name>Task 2: Route the dashboard + matrix headers through label()</name>
  <files>src/components/setlist/SetlistDashboard.tsx, src/components/setlist/SetlistMatrixView.tsx</files>
  <action>
    INVOKE /ui-ux-pro-max FIRST (BLOCKING) — confirm the relabeled headers keep the dashboard/matrix visual hierarchy intact on iPad WebKit.
    1. `SetlistDashboard.tsx`: import `useOrg` from `@/lib/org/org-context` + `label` from `@/lib/org/vocab`; `const org = useOrg()` in the component body. Replace the hardcoded header text:
       - `:134` `Create New Setlist` → `{label(org, 'createNewSetlistHeading')}`
       - `:144` `Upcoming Services` → `{label(org, 'upcomingSection')}`
       - LEAVE `:175` "Library & Past Events" (org-neutral; not a liturgical remnant).
    2. `SetlistMatrixView.tsx`: import `useOrg` + `label`; `const org = useOrg()`; replace `:65` `Liturgical Matrix` → `{label(org, 'matrixTitle')}`. (Leave the hardcoded service-type `<option>`s at :71-72 OUT OF SCOPE — they're the SERVICE_TYPE_LABELS surface, gated/irrelevant for broslaz and a larger refactor; note as deferred.)
    Avoid: touching any other strings, the SERVICE_TYPE_LABELS records, or liturgical fields already gated by hidesLiturgicalFields. Don't alter the greeting (useCongregation) path.
  </action>
  <verify>grep: SetlistDashboard + SetlistMatrixView contain no inline "Upcoming Services" / "Create New Setlist" / "Liturgical Matrix" literals in JSX (now via label()); both import useOrg + label. Reasoning: org='crc' → label() returns the exact prior strings.</verify>
  <done>AC-1 (broslaz band voice) + AC-2 (CRC byte-identical) satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Full quality gate + CRC byte-identical confirmation</name>
  <files>(verification + any fixes surfaced)</files>
  <action>
    Run the v11.1 quality floor:
    1. `npx tsc --noEmit` → 0 errors.
    2. Full unit suite green (baseline 3339 + new vocab cases).
    3. `SKIP_ENV_VALIDATION=1 npx next build` → clean (this phase touches client components + a pure lib).
    4. CRC byte-identical confirmation: for org=crc, label() returns "Upcoming Services" / "Create New Setlist" / "Liturgical Matrix" verbatim → dashboard + matrix render unchanged. Document in SUMMARY.
    Fix any gate failure within plan scope and re-run.
  </action>
  <verify>tsc clean; vitest all-green; `SKIP_ENV_VALIDATION=1 next build` exits 0; CRC byte-identical reasoning recorded.</verify>
  <done>AC-4 satisfied; phase deployable; v11.1 milestone ready to close.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any existing vocab.ts key value (CRC byte-identical across the table).
- The `hidesLiturgicalFields` gating + the liturgical fields it already hides (Service-type selector, Rabbi input, Save-as-Default, wizard strip) — already correct.
- SetlistCards `planPlaceholder` usage (already done) + the greeting `useCongregation()` path.
- `:175` "Library & Past Events" (org-neutral, intentionally left).

## SCOPE LIMITS
- Dashboard section headers + matrix title ONLY. SERVICE_TYPE_LABELS (Shabbat Morning/Friday Night/Erev Shabbat across SetlistCards/CreationWizard/SetlistMetaEditSheet/interview-defaults) is OUT OF SCOPE — gated-away for broslaz; a vocab-driven service-type table is a separate, larger refactor (note as deferred).
- The hardcoded `<option>` service types in SetlistMatrixView:71-72 — out of scope (same SERVICE_TYPE_LABELS surface); deferred.
- No Firestore/rules change → no emulator test.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] tsc clean
- [ ] full vitest suite green (incl. new vocab cases)
- [ ] `SKIP_ENV_VALIDATION=1 next build` clean
- [ ] /ui-ux-pro-max invoked before Task 2 (BLOCKING)
- [ ] CRC byte-identical (dashboard + matrix headers unchanged)
- [ ] broslaz reads "Upcoming Shows" / "Create New Set" / "Set Matrix"
- [ ] AC-1..AC-4 met
</verification>

<success_criteria>
- broslaz dashboard + matrix headers read in band voice; CRC byte-identical.
- All three headers resolve via the vocab layer (no inline liturgical literals).
- Full quality floor green → v11.1 milestone ready to close.
</success_criteria>

<output>
After completion, create `.paul/phases/v11.1-04-vocab-remnants/v11.1-04-01-SUMMARY.md`.
Log the DEFERRED SERVICE_TYPE_LABELS vocab-table refactor to STATE Deferred Issues.
Append standing UAT-PENDING: Daniel confirms broslaz dashboard reads "Upcoming Shows"/"Create New Set" + matrix "Set Matrix"; CRC unchanged.
Note: this is the final v11.1 phase — UNIFY transition routes to /paul:complete-milestone.
</output>
