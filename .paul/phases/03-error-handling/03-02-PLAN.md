---
phase: 03-error-handling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/schemas.ts
  - src/lib/congregation-store.ts
  - src/components/admin/people/AccessAuditLog.tsx
  - src/components/admin/LiveServiceSection.tsx
  - src/components/library/SongChartsLibrary.tsx
  - src/components/setlist/v2/SetlistEditorV2.tsx
  - src/components/ui/SectionErrorBoundary.tsx
  - src/app/(main)/error.tsx
  - src/hooks/use-offline.ts
autonomous: true
---

<objective>
## Goal
Migrate remaining console.error/console.warn calls to the logger utility and fix the empty catch block in use-offline.ts.

## Purpose
Console statements bypass the structured logging pipeline (which routes to Sentry in production). Migrating ensures all errors are observable in the error tracking system.

## Output
9 files with console.error/warn replaced by logger calls + 1 empty catch block fixed.
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Source Files
@src/types/schemas.ts
@src/lib/congregation-store.ts
@src/components/admin/people/AccessAuditLog.tsx
@src/components/admin/LiveServiceSection.tsx
@src/components/library/SongChartsLibrary.tsx
@src/components/setlist/v2/SetlistEditorV2.tsx
@src/components/ui/SectionErrorBoundary.tsx
@src/app/(main)/error.tsx
@src/hooks/use-offline.ts
</context>

<acceptance_criteria>

## AC-1: Zero console.error/console.warn in production code
```gherkin
Given a search for console.error and console.warn in src/ (excluding test files)
When all files are scanned
Then zero matches are found (all migrated to logger)
```

## AC-2: Empty catch block eliminated
```gherkin
Given src/hooks/use-offline.ts
When the catch block at line ~124 is reached
Then it logs the error instead of silently swallowing it
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Migrate console.error/warn to logger</name>
  <files>src/types/schemas.ts, src/lib/congregation-store.ts, src/components/admin/people/AccessAuditLog.tsx, src/components/admin/LiveServiceSection.tsx, src/components/library/SongChartsLibrary.tsx, src/components/setlist/v2/SetlistEditorV2.tsx, src/components/ui/SectionErrorBoundary.tsx, src/app/(main)/error.tsx</files>
  <action>
    For each file, replace `console.error(...)` with `logger.error(...)` and `console.warn(...)` with `logger.warn(...)`. Add `import { logger } from "@/lib/logger"` where not already imported.
    
    Specific files:
    1. **schemas.ts** (line ~177): `console.error` → `logger.error`
    2. **congregation-store.ts** (line ~77): `console.error` → `logger.error`
    3. **AccessAuditLog.tsx** (line ~38): `console.error` → `logger.error`
    4. **LiveServiceSection.tsx** (lines ~64, ~69): 2x `console.error` → `logger.error`
    5. **SongChartsLibrary.tsx** (line ~207): `console.error` → `logger.error`
    6. **SetlistEditorV2.tsx** (line ~214): `.catch(console.error)` → `.catch(e => logger.error("[SetlistEditor]:", e))`
    7. **SectionErrorBoundary.tsx** (line ~22): `console.error` → `logger.error`
    8. **error.tsx** (line ~14): `console.error` → `logger.error`
    
    Note: For client components ('use client'), logger still works — it's a simple utility that logs to console in dev and to Sentry in production. No server-only dependency.
  </action>
  <verify>grep -rn "console\.\(error\|warn\)" src/ --include="*.ts" --include="*.tsx" | grep -v test | grep -v node_modules should return 0 matches</verify>
  <done>AC-1 satisfied: Zero console.error/warn in production code</done>
</task>

<task type="auto">
  <name>Task 2: Fix empty catch block in use-offline.ts</name>
  <files>src/hooks/use-offline.ts</files>
  <action>
    At line ~124, there's an empty `catch { }` that silently swallows errors.
    
    Read the surrounding code to understand what operation is being caught. Add appropriate logging:
    - If it's a cache miss or expected failure, use `logger.warn`
    - If it's unexpected, use `logger.error`
    - Check if there's an abort controller — don't log if the operation was intentionally cancelled
  </action>
  <verify>grep "catch" src/hooks/use-offline.ts should show no empty catch blocks</verify>
  <done>AC-2 satisfied: Empty catch block eliminated</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any test files
- src/lib/logger.ts (the logger itself)
- Error message text (keep existing messages, just change the function)

## SCOPE LIMITS
- Only replace console.error/warn with logger equivalents
- Do not change error handling logic or add new error handling
- Do not refactor component structure

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `npx vitest run` — no regressions
- [ ] Zero `console.error` or `console.warn` in production code (excluding tests)
- [ ] Zero empty catch blocks
</verification>

<success_criteria>
- All console.error/warn migrated to logger
- Empty catch block fixed
- No regressions
</success_criteria>

<output>
After completion, create `.paul/phases/03-error-handling/03-02-SUMMARY.md`
</output>
