---
phase: v11.7-01-verify-first-audit
plan: 01
type: research
status: complete
completed: 2026-06-18
---

# SUMMARY — v11.7-01-01: Verify-first MCP discovery-gap audit

## What was done
Read-only verification of the MCP read/discovery-gap audit's behavior claims against the **deployed** MCP source (3 parallel Explore agents, every verdict cited `file:line`). Output: `.paul/research/v11-7-01-mcp-discovery-verify-REPORT-2026-06-18.md`. **No production code touched** (`git status --short src/` empty).

## Result (E/Q: PASS)
- **AC-1 ✓** — every P0–P5 claim has a CONFIRMED/REFUTED verdict + citation.
- **AC-2 ✓** — asymmetry inventory for all 7 proposed tools (data source · query path · tenant seam · gating tier · feasibility).
- **AC-3 ✓** — `search_setlists` index decision recorded + v7.0 fold-forward re-triaged.

## Key findings (these reshape 02–07)
- **P0–P3 claims ALL CONFIRMED.** `list_musicians` instrument-gate (`roster.ts:114–126`); write tools take arbitrary uid; `delete_chart` reference-walk is in **`library-upload.ts:787–846`** (correction: not `setlist-write.ts`); `sourceTemplateId` written (`templates.ts:878`), `delete_template` no-cascade; `create_contact` email-dedup.
- **2 corrections vs the audit's assumptions:**
  1. **`library_signals` is NOT a Firestore collection** — it's an in-process event emitter (`library-events.ts:85–105`). So **P5 `list_recent_library_changes` is BLOCKED** (needs a new persistent collection + write path) → **defer to v11.8**.
  2. **P4 `list_recent_commands` is BLOCKED** — acks are ephemeral (TTL ~5min, no `createdAt`, not indexed/org-scoped) → needs a new bridge command-log → **defer to v11.8**.
- **find_user (P0):** drop the instrument gate; role/email viable now (email via in-memory filter at current scale; new `(orgId,email)` index optional), name=app-side substring; tenant via `orgFrom`+`rowOrgIds` (no leakage); gate `assertEditor`. Cheap `includeProfileless` interim on `list_musicians` available.
- **find_setlists_referencing_chart (P1):** CHEAP — surface the existing `delete_chart` walk, zero new index.
- **search_setlists (P1):** app-side filtering acceptable at current scale (≤~50 setlists); composite index only if it becomes a bottleneck.
- **Sweep trim (P2–P5/phase 04):** commit **find_setlists_from_template** (+ index `setlists(orgId,sourceTemplateId)`) and **find_contact** (cheap); DEFER list_recent_commands + list_recent_library_changes to v11.8.
- **v7.0 fold-forward:** 11/13 still live, mostly in-app library-UI tech debt → route the cheap ones into phase 06.

## Ready-to-plan verdicts
02 ✅ · 03 ✅ · 04 ⚠️ READY (trimmed: 2 tools defer to v11.8) · 05 ✅ · 06 ✅ · 07 ✅.

## Deviations
None. Plan executed as written; the two BLOCKED/defer findings are outcomes of the audit, not scope changes.

## Files
- Created: `.paul/research/v11-7-01-mcp-discovery-verify-REPORT-2026-06-18.md`
- Production code: none.
