---
phase: v11.5-04-hygiene-harness
plan: 01
subsystem: mcp
tags: [mcp, error-contract, publish, contacts, error-code-map, schema-docs]

requires:
  - phase: v11.4-03
    provides: contacts collection + MCP CRUD + publish_setlist ad-hoc recipients (D8)
  - phase: v11.5-01-02
    provides: chord-cache anon PATCH (unrelated; surfaced the pre-existing tsc concern)
provides:
  - contact_not_found now carries HTTP-like code 404 (was defaulting to 500)
  - publish_setlist schema documents the D8 recipient-resolution contract
affects: [v11.5-04-02, v11.5-04-03, any agent branching on MCP error.code]

tech-stack:
  added: []
  patterns: [ERROR_CODE_MAP entry as the migration path for a new non-500 machine_code]

key-files:
  created: []
  modified:
    - src/lib/mcp/errors.ts
    - src/lib/mcp/tools/index.ts
    - src/lib/mcp/__tests__/mcp-contacts.emulator.test.ts

key-decisions:
  - "M-11 fixed via ERROR_CODE_MAP entry (not a call-site errorCode override) — central, covers every contact_not_found emitter"
  - "M-10 is doc-only: publish_setlist runtime untouched; only the schema description + recipients describe changed"

patterns-established:
  - "New non-500 machine_code → add a row to ERROR_CODE_MAP rather than per-call errorCode"

duration: ~25min
started: 2026-06-15T10:16:00Z
completed: 2026-06-15T14:11:00Z
---

# Phase v11.5-04 Plan 01: Error contracts (M-11 + M-10) Summary

**`contact_not_found` now returns HTTP-like `code: 404` (was 500), proven by a new emulator test; `publish_setlist`'s schema documents the D8 recipient contract (account `uid` vs ad-hoc `name`+`email`, saved-contacts reuse). Doc + one-line map entry; no runtime behavior change beyond the error code.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~25 min (within session) |
| Started | 2026-06-15T10:16:00Z |
| Completed | 2026-06-15T14:11:00Z |
| Tasks | 2 completed (2/2 PASS) |
| Files modified | 3 |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: `contact_not_found` is a 404 | **Pass** | Added `contact_not_found: 404` to `ERROR_CODE_MAP`. New emulator test asserts `error.code === 404` + `machine_code === "contact_not_found"` for BOTH a missing-in-own-org id and a real cross-org id (wall preserved — indistinguishable). `mcp-contacts.emulator.test.ts` 7/7 green. |
| AC-2: `publish_setlist` schema documents D8 | **Pass** | `description` + `recipients` `.describe()` now state: recipient = account `uid` (in-app/push/SMS) OR ad-hoc `{name,email}` (email only); `audience` preset only when `recipients` omitted; saved contacts (`list_contacts`/`create_contact`) reused as `recipients[]`, surfaced as `preview_publish.savedContacts`. Runtime/zod shape unchanged. |

## Accomplishments

- M-11: missing/cross-org contact errors now branch as "fix your id" (404), not "server bug" (500), for any agent reading `error.code`. Fixed centrally at the map, so every `contact_not_found` emitter benefits.
- M-10: the only contract an agent sees for `publish_setlist` recipients now spells out the two recipient kinds and the saved-contacts reuse path — closing the v11.4-03 D8 documentation gap.
- Added a permanent regression test for the 404 contract (incl. the cross-org-wall non-leak case).

## Task Commits

Single plan commit (code + plan/summary docs + STATE/ROADMAP drop-bookkeeping + handoff archive); exact hash recorded in STATE.md § Git State after push.

| Task | Type | Description |
|------|------|-------------|
| Task 1: M-11 contact_not_found → 404 | fix + test | ERROR_CODE_MAP entry + emulator assertion |
| Task 2: M-10 publish_setlist D8 schema doc | docs | description + recipients describe |

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/mcp/errors.ts` | Modified | Added `contact_not_found: 404` to ERROR_CODE_MAP (404 block) |
| `src/lib/mcp/tools/index.ts` | Modified | publish_setlist `description` + `recipients` describe → D8 contract (doc-only) |
| `src/lib/mcp/__tests__/mcp-contacts.emulator.test.ts` | Modified | New M-11 test: code:404 for missing + cross-org |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Fix M-11 at ERROR_CODE_MAP, not via per-call `errorCode` | Central — covers every `contact_not_found` emitter, not just the delete path | Future contact-not-found sites inherit 404 automatically |
| M-10 doc-only | publish runtime already correct; the gap was the contract an agent reads | Zero behavior risk; schema text is the deliverable |

## Deviations from Plan

### Summary

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 0 | Plan executed as written |

**Total impact:** None — plan executed exactly as specified.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| `tsc --noEmit` fails at `src/app/api/library/chord-cache/route.test.ts:164` | **Pre-existing** — stash-verified present at baseline HEAD `e7d9773017`, NOT caused by this plan (chord-cache = H5/v11.5-01-02 test). Does not affect `next build --webpack` (test files excluded). Logged to STATE Blockers; recommended fold into Plan 03 (harness) or a quick task. |

## Next Phase Readiness

**Ready:**
- v11.5-04-02 (library hygiene — junk/isTest filter + ingestion guard + orphan delete, VERIFY-FIRST cascade) — independent of this plan.
- v11.5-04-03 (harness & ops) — should ALSO fix the pre-existing chord-cache tsc error so `tsc` is clean before phase close.

**Concerns:**
- `tsc` is not clean on master until the chord-cache test type error is fixed (pre-existing, tracked).

**Blockers:** None.

---
*Phase: v11.5-04-hygiene-harness, Plan: 01*
*Completed: 2026-06-15*
