---
phase: v11.5-04-hygiene-harness
plan: 03
subsystem: testing
tags: [mcp, test-harness, monitor, orgIds, multi-tenant, upload-sessions, tsc]

requires:
  - phase: v11.5-01
    provides: orgIds membership model (getOrgIdsFromClaims, claim+doc lockstep)
  - phase: v11.3-02
    provides: chunked inline upload (begin/append/commit) + upload_sessions
provides:
  - H8 monitor-bus assignment release in the test-account revoke/cleanup cascade
  - F-8 orgIds provisioning on create_test_account (multi-tenant test accounts)
  - M-12 60-min chunked-upload session TTL + Drive-primary tool descriptions
  - tsc-clean master (carried chord-cache route.test.ts type error cleared)
affects: [v11.5-04-04 (ops+docs STOP-gate), future cowork multi-tenant stress runs]

tech-stack:
  added: []
  patterns:
    - "Nested-map cascade: config/monitor.busAssignments is walked + filtered by userId (not deleteByQuery) since assignments aren't a uid-keyed collection"
    - "Explicit-only org stamping: orgIds claim/doc written ONLY when requested, keeping the crc-default path byte-equivalent (getOrgIdsFromClaims defaults to [crc])"

key-files:
  created: []
  modified:
    - src/lib/mcp/tools/test-tokens.ts
    - src/lib/mcp/tools/library-upload-session.ts
    - src/lib/mcp/tools/index.ts
    - src/app/api/library/chord-cache/route.test.ts
    - src/lib/mcp/__tests__/mcp-test-tokens.emulator.test.ts
    - src/lib/mcp/__tests__/mcp-upload-session.emulator.test.ts

key-decisions:
  - "H8 scoped to CRC config/monitor only — test accounts are crc-stamped, broslaz has no sound desk"
  - "M-12 introduced a SEPARATE CHUNKED_SESSION_TTL_MS (60m) rather than bumping the shared constant; signed-URL path stays 10m (bounded by its signed PUT URL)"
  - "F-8 omitted-orgIds path kept byte-equivalent (no claim/field written; token orgId = DEFAULT_ORG_ID)"

patterns-established:
  - "Best-effort cascade steps (monitor release) try/catch + logger.warn + return 0 — a cleanup sweep never crashes on a peripheral read"

duration: ~45min
started: 2026-06-16
completed: 2026-06-16
---

# Phase v11.5-04 Plan 03: Harness hardening Summary

**Four autonomous MCP/harness fixes — H8 monitor-bus cascade, F-8 orgIds on create_test_account, M-12 60-min chunked TTL + Drive-primary tool docs, and the chord-cache tsc fix that restores a clean `tsc --noEmit` on master.**

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: H8 monitor-bus cascade on revoke/cleanup | Pass | `releaseMonitorBusAssignments` walks `config/monitor.busAssignments`, drops `userId===uid` entries, nulls emptied slots, reports `cascaded.monitorBusAssignments`. 3 emulator tests (remove+null+spare-others, no-op, cleanup-inherits). |
| AC-2: F-8 orgIds on create_test_account | Pass | `orgIds` → user doc + Auth claims + token `orgId` (=orgIds[0]); omitted = crc-default byte-equivalent (no claim/field; token crc); unknown org → `invalid_org_id` before any Auth user created. 3 emulator tests. |
| AC-3: M-12 chunked TTL + agent-path docs | Pass | `CHUNKED_SESSION_TTL_MS`=60m on `beginChunkedChartUpload`; signed-URL path unchanged at 10m (test L184-190 still ≤11m); begin/append/commit descriptions name `import_chart_from_drive` as PRIMARY, chunked as egress-blocked fallback. 1 emulator test asserts ~60m. |
| AC-4: tsc clean | Pass | `mockReturnValueOnce`→`mockResolvedValueOnce` (async `checkRateLimit`); `npx tsc --noEmit` exit 0 — the carried `chord-cache/route.test.ts:164` error is cleared. |

## Verification Results

| Gate | Result |
|------|--------|
| Emulator suites (test-tokens + upload-session) | **63 passed** (6 new: 3 H8 + 3 F-8 + 1 M-12 = 7 new cases, 63 total) |
| chord-cache route.test.ts | **9 passed** |
| `npx tsc --noEmit` | **exit 0** (carried concern cleared) |
| `SKIP_ENV_VALIDATION=1 npx next build --webpack` | **exit 0** |

## Accomplishments

- **H8** — closed the orphaned-uid-on-a-live-bus gap: revoking/cleaning a test account now releases its monitor-bus assignments. Implemented as a nested-map walk (not `deleteByQuery`) because `config/monitor.busAssignments` is keyed by bus-index, not uid.
- **F-8** — `create_test_account` can now provision broslaz-only or cross-tenant (`['crc','brotherslazaroff']`) test accounts, unblocking multi-tenant role-boundary stress testing; crc-default path is byte-identical to before.
- **M-12** — chunked inline uploads get a realistic 60-min window (a 100-chunk file through MCP args no longer races a 10-min TTL); tool descriptions now steer agents to Drive staging first.
- **tsc-clean master** — the v11.5-04-01 carried concern is resolved; `tsc --noEmit` passes.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/mcp/tools/test-tokens.ts` | Modified | H8 `releaseMonitorBusAssignments` + cascade wiring + `monitorBusAssignments` in result; F-8 `orgIds` arg/schema/validation + doc/claim/token stamping + result field |
| `src/lib/mcp/tools/library-upload-session.ts` | Modified | M-12 `CHUNKED_SESSION_TTL_MS`=60m on chunked begin; module-doc note |
| `src/lib/mcp/tools/index.ts` | Modified | M-12 begin/append/commit descriptions — Drive-primary + 60-min window |
| `src/app/api/library/chord-cache/route.test.ts` | Modified | tsc fix: `mockReturnValueOnce`→`mockResolvedValueOnce` |
| `src/lib/mcp/__tests__/mcp-test-tokens.emulator.test.ts` | Modified | +`config` wipe; 3 H8 + 3 F-8 tests |
| `src/lib/mcp/__tests__/mcp-upload-session.emulator.test.ts` | Modified | M-12 60-min TTL test |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| H8 scoped to CRC `config/monitor` | Test accounts crc-stamped; broslaz has no sound desk | No org-namespaced monitor handling needed |
| Separate `CHUNKED_SESSION_TTL_MS` (60m) vs bumping shared constant | Signed-URL path is a single PUT, bounded by the signed URL's own 10-min expiry — only the chunked path needs the longer window | Signed-URL behavior byte-stable |
| Explicit-only orgIds stamping | `getOrgIdsFromClaims` treats missing claim as `[crc]`, so the default path needs no write | Omitted path byte-equivalent to pre-F-8 |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 0 | — |

**Total impact:** None — plan executed exactly as written.

## Issues Encountered

None.

## Skill Audit

SPECIAL-FLOWS requires `/ui-ux-pro-max` only for frontend work. Plan 03 touched no UI → not applicable. No gap.

## Next Phase Readiness

**Ready:**
- `tsc --noEmit` clean on master — phase can close cleanly after Plan 04.
- Multi-tenant test accounts (F-8) available for any future cross-tenant stress work.

**Concerns:**
- None.

**Blockers:**
- None. Plan 04 (ops+docs STOP-gate) is the remaining phase work: `delete_chart`-author verification, VERIFY-FIRST prod junk-row deletion (real data → `autonomous:false`), `.docx` test fixture, BL-tier bearer doc. Plan 04's UNIFY triggers the phase transition.

---
*Phase: v11.5-04-hygiene-harness, Plan: 03*
*Completed: 2026-06-16*
