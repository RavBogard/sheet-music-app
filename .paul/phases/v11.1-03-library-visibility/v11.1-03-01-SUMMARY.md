---
phase: v11.1-03-library-visibility
plan: 01
subsystem: ui
tags: [multi-tenant, library, org-scoping, nextjs, ssr, rowOrg, display-filter]

requires:
  - phase: v11-02 (MCP org-scoping)
    provides: rowOrg filter pattern + library_index orgId stamping
  - phase: v11-01 (tenant foundation)
    provides: orgId on library_index/songs + CRC backfill (2105 docs)
provides:
  - Host-org display isolation for the chart library (Library tab + bind-picker + header search)
  - getServerLibrary/getServerLibraryLean optional orgId host-filter (per-org lean cache)
  - /api/library/list default host-org filter + admin-gated allSites opt-out
  - org-neutral library tab labels for non-crc tenants + admin "All sites" toggle
affects: [v11.1-04 (vocab — shares the library tab surface), future tenants]

tech-stack:
  added: []
  patterns:
    - "Host-filter at the fetch/SSR layer → the shared useLibraryStore feeds tab + bind-picker + header search, so all inherit isolation from one filter point"
    - "Admin escape hatch via a gated query param (allSites, getServerUser().isAdmin) + queryKey element for re-fetch"

key-files:
  created: []
  modified:
    - src/lib/server-library.ts
    - src/lib/__tests__/server-library.test.ts
    - src/app/(main)/library/page.tsx
    - src/app/api/library/list/route.ts
    - src/hooks/use-library.ts
    - src/hooks/__tests__/use-library.test.ts
    - src/components/library/SongChartsLibrary.tsx
    - src/components/library/__tests__/song-charts-library.test.tsx

key-decisions:
  - "Shared flag DEFERRED (Daniel) — pure host-isolation now; libraries effectively disjoint today"
  - "Filter scope = tab + bind-picker + header search (Daniel), with admin-only All-sites toggle; achieved by filtering at the fetch layer so the shared store feeds all three"
  - "recordings-collection scoping DEFERRED — separate surface with its own bug (/api/recordings/upload hardcodes orgId=crc), would mis-filter if done naively"

patterns-established:
  - "New tenant library isolation is free once orgId is stamped — getServerLibrary(orgId) + the route's hostOrg filter handle it"

duration: ~35min
started: 2026-06-09
completed: 2026-06-09
---

# Phase v11.1-03 Plan 01: Library generic-tab visibility Summary

**The in-app chart library now host-isolates per tenant — brotherslazaroff.live's Library tab, chart-bind picker, and header search show only broslaz `library_index` charts (CRC byte-identical), with an admin-only "All sites" toggle revealing the full cross-tenant pool; direct chart access is never gated (err-public).**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~35 min |
| Started | 2026-06-09 |
| Completed | 2026-06-09 |
| Tasks | 3 completed (3/3 E/Q PASS) |
| Files modified | 8 (incl. 3 test files) |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: broslaz tab shows only broslaz charts | Pass | getServerLibrary(orgId) SSR + /api/library/list hostOrg filter (rowOrg===host); shared store → bind-picker + header search inherit it |
| AC-2: CRC byte-identical | Pass | crc filter is a visual no-op (CRC pool 100% crc-stamped); labels unchanged; non-admin + default view identical. Only addition: admin-only, off-by-default All-sites toggle |
| AC-3: admin All-sites reveals full pool; access not gated | Pass | toggle re-fetches with allSites=true (gated to admins); serving routes untouched → direct links always work |
| AC-4: non-admins cannot widen scope | Pass | route gates allSites behind getServerUser().isAdmin; param is a no-op otherwise |
| AC-5: quality gates | Pass | tsc clean · full suite 3339/0 · SKIP_ENV_VALIDATION=1 next build clean |

## Accomplishments

- broslaz Library tab/picker/search de-cluttered to broslaz-only charts across all sub-tabs (Charts/Uploads/Audio); CRC visibly unchanged.
- Filter applied once at the fetch/SSR layer → the shared `useLibraryStore` propagates isolation to the bind-picker + header search automatically (no per-surface wiring).
- Admin "All sites" escape hatch for cross-tenant authoring/binding; display-only (err-public preserved — no access gating).
- org-neutral tab labels for broslaz ("Charts" + no Shireinu); CRC keeps "CRC Charts / Shireinu".

## Task Commits

Executed under the v11.x autonomy posture as a single phase commit; hash recorded in STATE Git State after transition.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/server-library.ts` | Modified | getServerLibrary/getServerLibraryLean optional `orgId` filter (rowOrg); per-org lean cache (Map) |
| `src/app/(main)/library/page.tsx` | Modified | Resolve host org (x-org-id) → getServerLibrary(orgId) + `org` prop to SongChartsLibrary |
| `src/app/api/library/list/route.ts` | Modified | Default host-org filter; admin-gated `allSites=true` opt-out (getServerUser().isAdmin) |
| `src/hooks/use-library.ts` | Modified | `allSites` flag threaded through fetchLibrary + useLibrary + queryKey |
| `src/components/library/SongChartsLibrary.tsx` | Modified | `org` prop → org-neutral tab labels; admin-only "All sites" toggle |
| `src/lib/__tests__/server-library.test.ts` | Modified | +4 org-filter / per-org-cache cases |
| `src/hooks/__tests__/use-library.test.ts` | Modified | queryKey fixture updated for the new allSites element |
| `src/components/library/__tests__/song-charts-library.test.tsx` | Modified | +3 org-label / admin-toggle cases (mutable auth mock) |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Shared flag DEFERRED | Libraries effectively disjoint today; avoids data-field + admin-UI scope on a live surface | Pure host-isolation; add Shared later if a real shared-chart need appears |
| Filter at the fetch layer | The shared store feeds tab + bind-picker + header search → one filter point covers all three | Minimal, coherent change; admin toggle flips one param |
| recordings-collection DEFERRED | `/api/recordings/upload` hardcodes orgId=crc → naive host-filter would hide all recordings on broslaz; it's a distinct per-song surface, not the tab clutter | Logged as deferred issue (fix the stamp first) |

## Deviations from Plan

### Summary

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 1 | Test fixture (intentional change) |
| Scope additions | 0 | — |
| Deferred | 1 | recordings-collection scoping (logged) |

**Total impact:** Plan executed as written; one expected fixture update.

### Auto-fixed Issues

**1. [test] use-library queryKey fixture**
- **Found during:** Task 3 (full suite)
- **Issue:** `use-library.test.ts` asserted the exact queryKey `['library','v2','all',true,true]`; adding the `allSites` element changed it.
- **Fix:** updated the expectation to include the trailing `false` (allSites default). Re-verified 8/8.
- **Files:** src/hooks/__tests__/use-library.test.ts

### Deferred Items

- **recordings-collection org-scoping** — `subscribeRecordingsForSong` (RecordingBindPopover) is songId-only; AND `/api/recordings/upload` hardcodes `orgId: DEFAULT_ORG_ID` (line 107). Host-filtering the subscribe requires fixing the upload stamp first (else all recordings vanish on broslaz). Small follow-up; not the Library-tab clutter this phase targeted.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| Edit tool requires a prior Read-tool record (Windows) | Re-read library/page.tsx before editing; no impact |

## Next Phase Readiness

**Ready:**
- v11.1-04 (broslaz liturgical vocab sweep, P1) — the last v11.1 phase; touches the org vocab layer (label(org,key)/vocab.ts) for "Plan Service/Show" + "Upcoming Services" + dashboard headers. Independent of this phase's read-path work.

**Concerns:**
- recordings-collection scoping deferred (see above) — track for a follow-up.

**Blockers:**
- None.

**Standing UAT-PENDING (non-blocking):** Daniel loads /library on brotherslazaroff.live → confirms only broslaz charts in the tab + add-songs picker + header search; admin "All sites" toggle reveals the full pool; CRC /library unchanged.

---
*Phase: v11.1-03-library-visibility, Plan: 01*
*Completed: 2026-06-09*
