---
phase: v11.5-05-consumer-polish
plan: 01
type: execute
status: complete
autonomous: true
files_modified:
  - src/lib/org/vocab.ts
  - src/components/performance/PublicSetlistListing.tsx
  - src/app/qr/[code]/page.tsx
  - src/lib/org/__tests__/vocab.test.ts
---

# v11.5-05-01 SUMMARY — Q6 + Q3 consumer copy/branding

First (lowest-risk) plan of the LAST v11.5 phase. Pure display/copy — no data,
no rules, no writes, no new dependencies. CRC rendered strings byte-identical.

## What shipped

### Q6 — public listing subtitle is org-vocab-driven
- New `publicListingSubtitle` `VocabKey`. `BASE = "Public setlists"` (the prior
  hardcoded CRC string, verbatim → CRC byte-identical). `OVERRIDES.brotherslazaroff
  = "Public sets"` (band voice, consistent with the existing "set"/"Shows" overrides).
- `PublicSetlistListing.tsx:146` hardcoded `<p>Public setlists</p>` →
  `{label(org, 'publicListingSubtitle')}`. `org` (`useOrg()`) and `label` were
  already in scope (component already uses `label(org, 'pastSection')` at :310).

### Q3 — QR expired/used error is branded + QR-aware
- `qr/[code]/page.tsx` (client component) `state === "expired"` branch (the
  shared 410=used + 404=expired target):
  - Added `useOrg()` + `getOrgBranding(org).shortName` as top-level hooks (NOT
    inside the conditional return).
  - Heading `Session Expired` → `Sign-in code expired`.
  - Body reworded to name the tenant, cover BOTH causes, and give a recovery
    action: *"This {orgName} sign-in code expired or was already used. Ask for a
    fresh QR code on the device you're signing in to."* — no hardcoded "CRC" or
    "iPad" leak on the BL host (the remaining "iPad" strings are in the
    out-of-scope success/ready/approving states, untouched per boundaries).
- Auth/PUT flow, the 410/404→expired mapping, and all other states unchanged.

## Qualify (E/Q PASS — both tasks)
- `npx vitest run src/lib/org/__tests__/vocab.test.ts` → **6 passed** (new
  `publicListingSubtitle` cases: crc `"Public setlists"`, bl `"Public sets"`).
- `npx tsc --noEmit` → clean (exit 0).
- `SKIP_ENV_VALIDATION=1 npx next build --webpack` → **exit 0** (qr client page +
  listing compile; no bundle-boundary break).
- grep: no `Public setlists` left in `PublicSetlistListing.tsx`; no hardcoded
  `CRC`/`iPad` in the qr expired branch.

## AC results
- **AC-1** ✅ BL `/perform` subtitle = "Public sets".
- **AC-2** ✅ CRC `/perform` subtitle = "Public setlists" (BASE byte-identical).
- **AC-3** ✅ branded, QR-aware expired/used copy on both hosts (orgName-driven).

## Decisions
- Tenant named in the BODY (not heading) — reads naturally as
  "This {orgName} sign-in code…" and satisfies AC-3's "heading OR body names the
  tenant"; heading stays a clean, scannable "Sign-in code expired".
- Device noun = neutral "the device you're signing in to" (not "iPad") so the BL
  host doesn't imply Apple hardware — the QR-scan source is host-agnostic.

## Deviations
None.

## Next
UNIFY this plan → author + apply Plan 02 (Q5 filename strip + F4 BL key-badge
verify-first) → Plan 03 (Q4 anon /setlists junk filter) → phase close =
`/paul:complete-milestone` for v11.5 (this is the LAST v11.5 phase).
