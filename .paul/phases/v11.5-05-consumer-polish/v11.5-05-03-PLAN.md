---
phase: v11.5-05-consumer-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/use-setlist-dashboard.ts
  - src/hooks/__tests__/use-setlist-dashboard.test.ts
  - src/components/setlist/SetlistDashboard.tsx
  - src/components/setlist/SetlistCards.tsx
autonomous: true
---

<objective>
## Goal
Q4 — clean up the `/setlists` archive for ANON visitors (CRC surface), two parts
from the design audit (line 225, `crc-setlists-1180.png`):
- **Hide write controls from anon** — the per-card **Edit** button + **Download**
  (offline) icon currently render unconditionally; anon should not see them.
- **Suppress junk drafts from the anon archive** — drop test fixtures (always) +
  empty/abandoned drafts ("New Setlist", "testing") from what anon sees.

## Purpose
Last plan of the LAST v11.5 phase → its close = the v11.5 milestone close. The
`/setlists` archive is a consumer surface (it serves anon/incognito by design —
`subscribeToAllSetlists` runs pre-auth since firestore.rules opened
`allow read: if true`). Today it shows anon a flat A–Z wall including test
fixtures + empty drafts, plus Edit/Download affordances anon can't use.

## Output
- `use-setlist-dashboard.ts` derived list filtered: `isNonTestSetlist`
  UNCONDITIONALLY (surface parity with `/perform` + `DashboardClient`) +
  zero-track drafts hidden for ANON only (owner keeps their drafts).
- `SetlistCards` Edit + Download gated behind a new `canEdit` prop (`!!user`).
- Unit tests for both.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@src/hooks/use-setlist-dashboard.ts
@src/components/setlist/SetlistDashboard.tsx
@src/components/setlist/SetlistCards.tsx
@src/components/performance/public-setlist-order.ts

## Verified facts (probed at plan time — trust these)
- **Junk filter gap CONFIRMED:** `use-setlist-dashboard.ts` derives
  `displayedSetlists`/`upcoming`/`pastOrNoDate` from `allSetlists` with ONLY
  search/rabbi filters — NO `isNonTestSetlist`. `/perform` (`splitPublicSetlists`)
  AND `DashboardClient.tsx:200` (`setlists.filter(isNonTestSetlist)`) both apply
  it; `/setlists` is the lone surface that doesn't → test fixtures + drafts leak.
- **The shared predicate exists:** `isNonTestSetlist(s)` in
  `src/components/performance/public-setlist-order.ts` — drops `isTest === true`
  AND `isTestUid(s.ownerId)`. Reuse it verbatim (do NOT re-implement).
- **Anon reaches this surface:** the subscription comment (use-setlist-dashboard.ts:126-132)
  documents that it runs pre-auth for incognito; `firestore.rules allow read: if true`.
  `user`/`isMember` in the hook are null/false for anon.
- **Write-control leak CONFIRMED in `SetlistCards.tsx`:** both `UpcomingSetlistCard`
  (inline Edit btn :135-140, Download icon :147-153, overflow Edit :174 / Download
  :178) and `SetlistCard` (inline Edit :298-299) render Edit/Download with NO user
  gate. Only `canDelete`/`canDuplicate` are gated today (passed from
  SetlistDashboard :164-166 / :196-198). `onEdit` = `handleSelect` (→ `/setlists/[id]`
  authoring); `onDownload` = offline save — both authed-only actions.
- **Card OPEN/tap + Perform nav are NOT write controls** — viewing stays public
  (err-public). Only the explicit Edit + Download affordances are in scope.
- `Setlist` has `trackCount?` (+ `songCount?`) — zero-track = `(trackCount ?? songCount ?? 0) === 0`.

## Decision baked (no checkpoint — clearly-right per the predicate's stated purpose)
- `isNonTestSetlist` applies **UNCONDITIONALLY** (all viewers) — matches
  DashboardClient + /perform; test fixtures are never wanted in the human archive
  (cleanup is via MCP, not this list).
- Zero-track "junk draft" suppression applies **ANON-ONLY** (`!user`) — an empty
  "New Setlist"/"testing" draft is noise to a public browser, but the OWNER must
  still see their in-progress draft. Owner-safe, audit-aligned.
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /ui-ux-pro-max | required | Before the consumer-UI control-gating change | ✓ (loaded this session) |

**BLOCKING:** /ui-ux-pro-max MUST be loaded before APPLY (UI-touching phase). Already loaded.

## Skill Invocation Checklist
- [x] /ui-ux-pro-max loaded
</skills>

<acceptance_criteria>

## AC-1: Test fixtures never appear on /setlists (any viewer)
```gherkin
Given setlists including isTest:true and test-uid-owned rows
When the /setlists dashboard derives its displayed lists
Then those rows are excluded (isNonTestSetlist), for anon AND authed — parity
  with /perform and the (main) DashboardClient
```

## AC-2: Empty drafts hidden from anon, kept for owners
```gherkin
Given a real (non-test) setlist with zero tracks ("New Setlist"/"testing")
When an ANON visitor views /setlists
Then it is not shown
And when a signed-in user views /setlists
Then it IS shown (owners keep their in-progress drafts)
```

## AC-3: Anon sees no per-card Edit or Download control
```gherkin
Given an ANON visitor on /setlists
When a setlist card renders
Then neither the inline Edit button, the Download-for-offline icon, nor their
  overflow-menu equivalents appear
And a signed-in user still sees Edit + Download
And the card's open/tap and Perform navigation remain available to anon (viewing is public)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1 (Q4 data): filter test fixtures + anon empty-drafts in the dashboard hook</name>
  <files>src/hooks/use-setlist-dashboard.ts, src/hooks/__tests__/use-setlist-dashboard.test.ts</files>
  <action>
    In `use-setlist-dashboard.ts`:
    - Import `isNonTestSetlist` from `@/components/performance/public-setlist-order`.
    - Apply the visibility filter where `displayedSetlists` is derived (or a new
      memo feeding it) so BOTH `upcoming` and `pastOrNoDate` inherit it:
        const visible = allSetlists.filter(isNonTestSetlist)            // unconditional
        const archiveVisible = user ? visible : visible.filter(hasTracks)  // anon: drop empties
      where `hasTracks(s) = (s.trackCount ?? s.songCount ?? 0) > 0`. Then run the
      existing search/rabbi filters over `archiveVisible` instead of `allSetlists`.
    - `user` is already in scope (the hook's `user`). Keep the `placeholders`
      logic untouched (it already gates on `user`).
    In the test file: add cases — (a) isTest:true + test-uid rows excluded for both
    anon (user=null) and authed; (b) a zero-track non-test row is excluded when
    user=null but present when user set; (c) a normal row with tracks always shows.
    Avoid: re-implementing the predicate; filtering `placeholders`; changing the
    search/rabbi behavior; touching the subscription or SSR fetch.
  </action>
  <verify>npx vitest run src/hooks/__tests__/use-setlist-dashboard.test.ts — new cases green; tsc clean.</verify>
  <done>AC-1 + AC-2 satisfied: test fixtures gone for all; empty drafts hidden from anon, kept for owners.</done>
</task>

<task type="auto">
  <name>Task 2 (Q4 write controls): gate per-card Edit + Download behind canEdit (anon hides)</name>
  <files>src/components/setlist/SetlistCards.tsx, src/components/setlist/SetlistDashboard.tsx</files>
  <action>
    In `SetlistCards.tsx`:
    - Add `canEdit: boolean` to `UpcomingCardProps` and `PastCardProps`.
    - In `UpcomingSetlistCard`: wrap the inline Edit button (:135-140), the inline
      Download icon (:147-153), and the overflow "Edit Setlist" (:174) + "Download
      for Offline" (:178) items in `{canEdit && (…)}`. Leave the card root open
      (:89 "Open setlist") + the Perform action intact.
    - In `SetlistCard`: gate the inline Edit button (:298-299) on `{canEdit && (…)}`.
      (It has no inline Download; its overflow already shows only canDuplicate/canDelete.)
    - Keep the overflow trigger rendering whenever ANY item is visible — adjust the
      existing `{(canDuplicate || canDelete) && …}` guard to also include `canEdit`
      where the menu now holds Edit/Download (UpcomingSetlistCard), so the menu
      isn't empty/orphaned for anon.
    In `SetlistDashboard.tsx`: pass `canEdit={!!user}` to both `<UpcomingSetlistCard>`
    (:151) and `<SetlistCard>` (:185).
    Use /ui-ux-pro-max judgment so the card still reads as tappable-to-view for anon
    (no dangling empty action row). Avoid: gating the card open/Perform; changing
    canDelete/canDuplicate; adding new deps.
  </action>
  <verify>SKIP_ENV_VALIDATION=1 npx next build --webpack exits 0; tsc clean; manual read confirms Edit + Download are inside `{canEdit && …}` in both cards and SetlistDashboard passes `canEdit={!!user}`.</verify>
  <done>AC-3 satisfied: anon sees no Edit/Download affordance; authed unchanged; viewing still public.</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- `isNonTestSetlist` (reuse as-is) / `splitPublicSetlists` / the `/perform` path.
- The Firestore subscription, `firestore.rules`, or the SSR `getSetlistsPage` fetch
  (the visible fix lives in the hook's derivation; no wire/rules change).
- `canDelete`/`canDuplicate` gating, the card open/tap, Perform navigation.
- The `placeholders` (Saturday-slot) logic — already `user`-gated.

## SCOPE LIMITS
- Q4 ONLY (this is the last plan). No new collections, writes, rules, or deps.
- "Junk draft" = test fixture (all viewers) + zero-track (anon only). Do NOT add
  name-pattern matching ("New Setlist") — zero-track is the robust, owner-safe signal.
- Do NOT SSR-wire-filter for anon (setlist names aren't PII; out of scope — unlike
  /perform's PII cap). Visible-fix only.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] npx vitest run src/hooks/__tests__/use-setlist-dashboard.test.ts — green (new cases)
- [ ] npx tsc --noEmit — no new errors
- [ ] SKIP_ENV_VALIDATION=1 npx next build --webpack — exits 0 (SetlistDashboard/SetlistCards client)
- [ ] read-back: Edit + Download inside `{canEdit && …}`; `canEdit={!!user}` passed; isNonTestSetlist + anon zero-track filter in the hook
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Q4 shipped: anon /setlists is clean (no test fixtures, no empty drafts, no Edit/Download).
- Authed dashboard behavior preserved (owners see drafts + full controls).
- Quality floor held: tsc clean + hook test green + `next build --webpack` exit 0.
- /ui-ux-pro-max consulted for the card control gating.
- Auto-committed + pushed to `origin master`.
</success_criteria>

<output>
After completion, create `.paul/phases/v11.5-05-consumer-polish/v11.5-05-03-SUMMARY.md`.
Append a non-blocking UAT item to `.paul/UAT-PENDING.md`: "v11.5-05 Q4 — on CRC
/setlists as ANON: no test fixtures, no empty 'New Setlist' drafts, no per-card
Edit/Download; as a signed-in user: drafts + Edit + Download all still present."
This is the LAST v11.5 plan — its UNIFY triggers the milestone close.
</output>
