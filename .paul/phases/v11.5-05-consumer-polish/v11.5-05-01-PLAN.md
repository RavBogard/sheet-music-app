---
phase: v11.5-05-consumer-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/org/vocab.ts
  - src/components/performance/PublicSetlistListing.tsx
  - src/app/qr/[code]/page.tsx
  - src/lib/org/__tests__/vocab.test.ts
autonomous: true
---

<objective>
## Goal
Two consumer-facing copy/vocab polish items from the v11.5 design audit, both
CRC byte-identical:
- **Q6** — the public listing subtitle "Public setlists" becomes org-vocab-driven
  so Brothers Lazaroff reads **"Public sets"** (band voice), CRC unchanged.
- **Q3** — the QR sign-in approval page's expired/used-code error is **branded**
  (per-tenant name) and **QR-aware** ("This sign-in code expired or was already
  used — ask for a fresh QR on the device").

## Purpose
First (lowest-risk) plan of the LAST v11.5 phase. Pure display/copy — no data,
no rules, no writes. Tightens the consumer surface on both live tenants without
touching CRC's rendered strings (Q6) and gives a confusing dead-end error a
clear, branded recovery instruction (Q3).

## Output
- New `publicListingSubtitle` vocab key (BASE = current CRC string verbatim; BL
  override "Public sets") + the public listing consuming it.
- Org-branded, reworded expired/used state on `/qr/[code]`.
- Vocab unit-test coverage for the new key on both orgs.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@src/lib/org/vocab.ts
@src/components/performance/PublicSetlistListing.tsx
@src/app/qr/[code]/page.tsx
@src/lib/org/branding.ts

## Verified facts (probed at plan time — trust these)
- `vocab.ts`: `label(orgId, key)` returns `OVERRIDES[orgId]?.[key] ?? BASE[key]`.
  Pattern for adding a key: append to `VocabKey` union, add to `BASE`, add the
  BL override under `OVERRIDES.brotherslazaroff`. `hidesLiturgicalFields` lives here too.
- `PublicSetlistListing.tsx:146` — `<p className="text-sm text-muted-foreground">Public setlists</p>`
  is HARDCODED. The component already imports `label` and `useOrg`, and already
  uses `label(org, 'pastSection')` at line 310 — so the seam is in place.
- `qr/[code]/page.tsx` — a CLIENT component (`"use client"`). Both 410 (code
  consumed/used) and 404 (not found/expired) map to the SAME `state="expired"`
  branch (lines 35-36), rendering "This QR code has expired. Ask for a new one
  on the iPad." (lines 103-112). It does NOT currently import `useOrg`/branding.
- Org name source: `getOrgBranding(org).shortName` → "CRC Music" / "Brothers Lazaroff".
  `useOrg()` (`@/lib/org/org-context`) reads `<html data-org>` (Edge-proxy set),
  defaults to `crc` outside a provider.
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /ui-ux-pro-max | required | Before any consumer-UI copy/branding change (Q3 + Q6) | ○ |

**BLOCKING:** /ui-ux-pro-max MUST be loaded before APPLY proceeds (UI-touching phase per milestone constraint #3).

## Skill Invocation Checklist
- [ ] /ui-ux-pro-max loaded (run command or confirm)
</skills>

<acceptance_criteria>

## AC-1: BL public listing subtitle reads "Public sets"
```gherkin
Given a visitor on brotherslazaroff.live /perform (org = brotherslazaroff)
When the public setlist listing header renders
Then the subtitle reads "Public sets" (not "Public setlists")
```

## AC-2: CRC public listing subtitle is byte-identical
```gherkin
Given a visitor on centralreform.live /perform (org = crc)
When the public setlist listing header renders
Then the subtitle reads exactly "Public setlists" (unchanged from today)
```

## AC-3: QR expired/used copy is branded + QR-aware
```gherkin
Given a musician opens /qr/[code] for a code that is expired or already used
  (API returns 410 or 404)
When the error state renders
Then the heading/body name the tenant (CRC Music or Brothers Lazaroff) AND the
  copy states the code "expired or was already used" AND instructs the user to
  ask for a fresh QR on the device
And on the CRC host the message is recognizably CRC-branded; on the BL host,
  BL-branded (no hardcoded "CRC" leak)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add publicListingSubtitle vocab key + consume it on the public listing</name>
  <files>src/lib/org/vocab.ts, src/components/performance/PublicSetlistListing.tsx, src/lib/org/__tests__/vocab.test.ts</files>
  <action>
    In `vocab.ts`:
    - Add `"publicListingSubtitle"` to the `VocabKey` union (group it near
      `publicListingTitle`).
    - In `BASE`, add `publicListingSubtitle: "Public setlists"` — this MUST be
      the current CRC string verbatim so CRC stays byte-identical (AC-2).
    - In `OVERRIDES.brotherslazaroff`, add `publicListingSubtitle: "Public sets"` (AC-1).
    In `PublicSetlistListing.tsx`:
    - Replace the hardcoded `Public setlists` text on the subtitle `<p>` (line ~146)
      with `{label(org, 'publicListingSubtitle')}`. `label` and `org` are already
      in scope (org from `useOrg()`, label imported).
    In `vocab.test.ts`:
    - Add assertions: `label('crc','publicListingSubtitle') === 'Public setlists'`
      and `label('brotherslazaroff','publicListingSubtitle') === 'Public sets'`.
    Avoid: changing any other vocab value; introducing a new translation seam —
    reuse the existing `label()` exactly like `pastSection`.
  </action>
  <verify>npx vitest run src/lib/org/__tests__/vocab.test.ts — new cases green; grep confirms no remaining hardcoded "Public setlists" in PublicSetlistListing.tsx</verify>
  <done>AC-1 + AC-2 satisfied: BL renders "Public sets", CRC renders "Public setlists" (BASE byte-identical).</done>
</task>

<task type="auto">
  <name>Task 2: Brand + reword the QR expired/used error copy</name>
  <files>src/app/qr/[code]/page.tsx</files>
  <action>
    Make the `state === "expired"` branch (currently lines ~103-112) org-aware:
    - Import `useOrg` from `@/lib/org/org-context` and `getOrgBranding` from
      `@/lib/org/branding`; resolve `const org = useOrg()` and
      `const orgName = getOrgBranding(org).shortName` inside the component
      (top-level with the other hooks, NOT inside the conditional return).
    - Reword the body copy to be QR-aware and cover BOTH causes (the branch
      already handles 410=used and 404=expired): e.g.
      "This sign-in code expired or was already used. Ask for a fresh QR on the
      <device>." Keep it short; the heading may incorporate `orgName`
      (e.g. `Sign-in code expired`) — branding can be the heading or an aria/label
      so there is no hardcoded "CRC" on the BL host.
    - Do NOT change the 410/404→expired mapping, the success/ready/approving
      states, or any auth/approval logic. Copy + branding only.
    Use /ui-ux-pro-max judgment for the exact wording/hierarchy; keep the existing
    `<Shell>` layout + `AlertCircle` icon.
    Avoid: introducing a new fetched/derived string; altering the network/PUT flow;
    leaking "CRC"/"iPad" as the only device noun on the BL host (use neutral
    "device" or the org-appropriate term).
  </action>
  <verify>SKIP_ENV_VALIDATION=1 npx next build --webpack exits 0 (client component compiles, no bundle-boundary break); manual read confirms no hardcoded "CRC" in the expired branch and copy names "expired or was already used" + "fresh QR".</verify>
  <done>AC-3 satisfied: branded, QR-aware expired/used copy on both hosts.</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Any existing `BASE` vocab value (CRC byte-identical invariant #2).
- The QR approval/auth flow (`handleApprove`, the PUT to `/api/auth/qr`, the
  410/404 status mapping, success/ready/approving states).
- `splitPublicSetlists` / listing filtering, the sign-in card, the "Next service"
  card — out of scope for this plan.

## SCOPE LIMITS
- This plan is Q6 + Q3 ONLY. Q5 (filename strip), F4 (key badges), Q4 (anon
  /setlists) are Plans 02 + 03.
- `test-login`/`TestLoginClient.tsx` is an internal harness surface, NOT a
  consumer host — leave its copy alone (out of Q3 consumer scope).
- No data writes, no Firestore rules, no new dependencies.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] npx vitest run src/lib/org/__tests__/vocab.test.ts — green (new subtitle cases)
- [ ] npx tsc --noEmit — no new errors
- [ ] SKIP_ENV_VALIDATION=1 npx next build --webpack — exits 0 (qr client page + listing)
- [ ] grep: no hardcoded "Public setlists" left in PublicSetlistListing.tsx; no hardcoded "CRC" in the qr expired branch
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Q6 + Q3 shipped; CRC rendered strings unchanged (byte-identical).
- Quality floor held: tsc clean + vocab tests green + `next build --webpack` exit 0.
- /ui-ux-pro-max consulted for the Q3 wording/hierarchy.
- Auto-committed + pushed to `origin master` per autonomy posture.
</success_criteria>

<output>
After completion, create `.paul/phases/v11.5-05-consumer-polish/v11.5-05-01-SUMMARY.md`.
Append a non-blocking UAT item to `.paul/UAT-PENDING.md`: "v11.5-05 Q3 — confirm
branded expired/used QR copy on BOTH hosts (CRC + broslaz); v11.5-05 Q6 — confirm
'Public sets' subtitle on broslaz /perform, 'Public setlists' on CRC."
</output>
