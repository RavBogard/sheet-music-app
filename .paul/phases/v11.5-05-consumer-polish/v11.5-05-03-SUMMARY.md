---
phase: v11.5-05-consumer-polish
plan: 03
type: execute
status: complete
autonomous: true
files_modified:
  - src/hooks/use-setlist-dashboard.ts
  - src/hooks/__tests__/use-setlist-dashboard.test.ts
  - src/components/setlist/SetlistCards.tsx
  - src/components/setlist/SetlistDashboard.tsx
---

# v11.5-05-03 SUMMARY — Q4 anon `/setlists` cleanup

Last plan of the LAST v11.5 phase. CRC consumer surface; no prod writes, no rules,
no new deps.

## What shipped

### Q4a — suppress junk/test drafts (data)
- `use-setlist-dashboard.ts`: `displayedSetlists` now filters `allSetlists` through
  the shared `isNonTestSetlist` **unconditionally** (surface parity with `/perform`
  + `DashboardClient`) and additionally drops zero-track drafts for **anon only**
  (`user ? visible : visible.filter(hasTracks)`). Both `upcoming` and `pastOrNoDate`
  inherit it (they derive from `displayedSetlists`). `hasTracks` = `(trackCount ??
  songCount ?? 0) > 0`. Owners still see their in-progress drafts.

### Q4b — hide write controls from anon (UI)
- `SetlistCards.tsx`: added `canEdit: boolean` to both `UpcomingCardProps` and
  `PastCardProps`. Gated the inline **Edit** button + **Download** (offline) icon
  + their overflow-menu equivalents on `{canEdit && …}`; wrapped `UpcomingSetlistCard`'s
  overflow menu in `{(canEdit || canDuplicate || canDelete) && …}` so anon (all
  false) gets no orphaned trigger. `SetlistCard`'s inline Edit gated on `canEdit`.
- `SetlistDashboard.tsx`: passes `canEdit={!!user}` to both cards.
- Card open/tap + Perform navigation untouched — viewing stays public (err-public).

## Qualify (E/Q PASS — both tasks)
- `npx vitest run src/hooks/__tests__/use-setlist-dashboard.test.ts` → **24 passed**
  (+4 new: isTest/test-uid dropped for authed + anon; zero-track hidden for anon,
  kept for signed-in).
- `npx tsc --noEmit` → clean.
- `SKIP_ENV_VALIDATION=1 npx next build --webpack` → **exit 0**.
- Read-back: `canEdit` gates Edit/Download in both cards; `canEdit={!!user}` passed;
  `isNonTestSetlist` + anon zero-track filter in the hook. Only `SetlistDashboard`
  renders these cards (no other call site needs the new prop).

## AC results
- **AC-1** ✅ test fixtures excluded for all viewers (parity).
- **AC-2** ✅ empty drafts hidden from anon, kept for signed-in owners.
- **AC-3** ✅ anon sees no Edit/Download; authed unchanged; viewing still public.

## Decisions
- `isNonTestSetlist` unconditional; zero-track suppression anon-only (owner-safe) —
  baked in the plan, no checkpoint (clearly-right per the predicate's stated purpose
  + the audit's "suppress junk drafts from the anon archive").
- Did NOT name-match ("New Setlist") — zero-track is the robust, owner-safe signal.
- Did NOT SSR-wire-filter (setlist names aren't PII; unlike /perform's cap) — visible-fix only.

## Deviations
None.

## Next
This is the last plan of v11.5-05. Its UNIFY triggers the phase transition =
**v11.5 milestone close** (`/paul:complete-milestone`).
