# Plan 04-01: Regression Sweep & Deferred Fixes

## Goal

Close out v1.6 milestone by fixing the pre-existing test failure, migrating remaining withAuth routes, verifying clearSaveTimer wiring, and doing a final verification pass.

## Tasks

### Task 1: Fix route-auth.test.ts publish 403→400

**Root cause:** Test passes `{ name: 'Alice', uid: 'u1' }` as musician, but the Zod schema in `/api/setlist/publish/route.ts` requires `email: z.string()`. Zod validation fails (400) before auth check (403).

**Fix:** Add `email` field to test musician objects in `src/app/api/__tests__/route-auth.test.ts`.

### Task 2: Migrate 4 remaining withAuth → createApiHandler routes

**Routes:**
1. `src/app/api/test-gemini/route.ts` — admin-only GET (Low complexity)
2. `src/app/api/bridge/setup-code/route.ts` — POST (auth + manual admin check), GET (unauthenticated)
3. `src/app/api/chat/route.ts` — authenticated POST (High complexity, ~300 lines)
4. `src/app/api/drive/file/[fileId]/route.ts` — special mixed auth (browser context detection) — SKIP migration, too risky

**Approach:** Convert routes 1-3 to `createApiHandler`. Route 4 (drive/file) has special browser-context auth that doesn't fit `createApiHandler` — leave as-is with a comment explaining why.

### Task 3: Verify clearSaveTimer wiring

**Status:** Already wired in `src/app/perform/[id]/page.tsx:99`. Annotation store manages its own lifecycle. No additional wiring needed — mark as verified.

### Task 4: Add comment to drive/file route explaining withAuth retention

**File:** `src/app/api/drive/file/[fileId]/route.ts`
**Change:** Add comment at top explaining why this route uses withAuth directly.

### Task 5: Run full test suite and verify

- All tests pass (including the now-fixed route-auth test)
- Build compiles clean

## Sequence

1. Task 1 (test fix) — quick win
2. Task 2 (withAuth migration — 3 routes)
3. Task 4 (drive/file comment)
4. Task 5 (verification)

## Verification

- [ ] route-auth.test.ts publish tests pass (403 as expected)
- [ ] test-gemini, bridge/setup-code, chat routes use createApiHandler
- [ ] drive/file route has explanatory comment
- [ ] TypeScript compiles clean
- [ ] Build passes
- [ ] Full test suite: 0 failures (excluding flaky timing tests)
