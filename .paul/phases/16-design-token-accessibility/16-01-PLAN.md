---
phase: 16-design-token-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/performance/FlowItemView.tsx
  - src/components/performance/MetronomeControl.tsx
  - src/components/layout/GlobalAlertBanner.tsx
  - src/components/ui/error-state.tsx
  - src/components/nav/DesktopHeader.tsx
  - src/components/audio/AudioPlayer.tsx
  - src/components/calendar/CalendarHeader.tsx
  - src/components/library/SongChartsLibrary.tsx
  - src/components/music/TransposerMenu.tsx
  - src/components/admin/SoundSystemSection.tsx
  - src/components/admin/UserRow.tsx
  - src/app/(main)/manage/page.tsx
  - src/app/(main)/schedule/page.tsx
  - src/app/(main)/settings/page.tsx
  - src/app/(main)/manage/templates/page.tsx
  - src/app/(main)/manage/templates/TemplateEditor.tsx
  - src/app/(main)/settings/sound/page.tsx
autonomous: true
---

<objective>
## Goal
Replace all hardcoded colors with design system tokens and add aria-label to all 31 icon-only buttons missing accessibility labels. Improve sr-only coverage and fix generic alt text.

## Purpose
Ensures consistent theming (dark-first OKLCH indigo palette) and meets WCAG 2.1 AA accessibility requirements for screen reader users — critical for a production app used on diverse devices.

## Output
All hardcoded color values replaced with Tailwind CSS design tokens; all icon-only buttons have aria-label attributes; logo alt text is descriptive.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@src/components/performance/FlowItemView.tsx
@src/components/performance/MetronomeControl.tsx
@src/components/layout/GlobalAlertBanner.tsx
@src/components/ui/error-state.tsx
@src/components/nav/DesktopHeader.tsx
@src/components/audio/AudioPlayer.tsx
@src/components/calendar/CalendarHeader.tsx
@src/components/library/SongChartsLibrary.tsx
@src/components/music/TransposerMenu.tsx
@src/components/admin/SoundSystemSection.tsx
@src/components/admin/UserRow.tsx
@src/app/(main)/manage/page.tsx
@src/app/(main)/schedule/page.tsx
@src/app/(main)/settings/page.tsx
@src/app/(main)/manage/templates/page.tsx
@src/app/(main)/manage/templates/TemplateEditor.tsx
@src/app/(main)/settings/sound/page.tsx
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /ui-ux-pro-max | required | Before frontend token/accessibility work | ○ |

**BLOCKING:** Required skills MUST be loaded before APPLY proceeds.

## Skill Invocation Checklist
- [ ] /ui-ux-pro-max loaded (run command or confirm)
</skills>

<acceptance_criteria>

## AC-1: Hardcoded Colors Replaced with Design Tokens
```gherkin
Given FlowItemView.tsx uses `bg-black text-white`
When the design token cleanup is applied
Then FlowItemView uses `bg-background text-foreground` instead
And MetronomeControl uses `bg-background/40` instead of `bg-black/40`
And error-state.tsx uses `bg-destructive/10` instead of `bg-red-500/10`
And all hardcoded red borders/hovers use `destructive` token equivalents
```

## AC-2: Dark Mode Variants on GlobalAlertBanner
```gherkin
Given GlobalAlertBanner has bg/border colors without dark: variants
When dark mode is active
Then all alert types (info, warning, error) have proper dark: bg and border classes
And text colors retain their existing dark: variants
```

## AC-3: All Icon-Only Buttons Have aria-label
```gherkin
Given there are 31 icon-only buttons missing aria-label
When accessibility labels are added
Then every Button with size="icon" or containing only an icon has an aria-label
And labels are descriptive (e.g., "Previous month", "Play", "Transpose up")
```

## AC-4: Logo Alt Text is Descriptive
```gherkin
Given DesktopHeader.tsx has alt="Logo"
When the alt text is updated
Then the image has alt="Central Reform Congregation logo"
```

## AC-5: Build and Tests Pass
```gherkin
Given all design token and accessibility changes are applied
When the TypeScript build and test suite run
Then there are no new type errors, build errors, or test failures
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Replace Hardcoded Colors with Design Tokens</name>
  <files>
    src/components/performance/FlowItemView.tsx,
    src/components/performance/MetronomeControl.tsx,
    src/components/layout/GlobalAlertBanner.tsx,
    src/components/ui/error-state.tsx
  </files>
  <action>
    1. FlowItemView.tsx line 105: Replace `bg-black text-white` with `bg-background text-foreground`
    2. MetronomeControl.tsx line 25: Replace `bg-black/40` with `bg-background/40`
    3. GlobalAlertBanner.tsx lines 17-19: Add `dark:` variants to bg and border colors:
       - info: bg → `bg-blue-500/10 dark:bg-blue-400/10`, border → `border-blue-500/20 dark:border-blue-400/20`
       - warning: bg → `bg-yellow-500/10 dark:bg-yellow-400/10`, border → `border-yellow-500/20 dark:border-yellow-400/20`
       - error: bg → `bg-red-500/10 dark:bg-red-400/10`, border → `border-red-500/20 dark:border-red-400/20`
    4. error-state.tsx line 23: Replace `bg-red-500/10` with `bg-destructive/10`
       line 34: Replace `border-red-500/20` with `border-destructive/20`, `hover:bg-red-500/10` with `hover:bg-destructive/10`, `hover:text-red-400` with `hover:text-destructive`, `text-red-500` with `text-destructive`

    Avoid: Changing any layout, padding, or structural classes — only swap color values.
    Avoid: Touching admin panel styling (out of scope per project rules).
  </action>
  <verify>npx tsc --noEmit && npx next build (or npm run build)</verify>
  <done>AC-1 and AC-2 satisfied: All hardcoded colors replaced with tokens, dark variants added</done>
</task>

<task type="auto">
  <name>Task 2: Add aria-label to Icon-Only Buttons and Fix Alt Text</name>
  <files>
    src/components/audio/AudioPlayer.tsx,
    src/components/calendar/CalendarHeader.tsx,
    src/components/library/SongChartsLibrary.tsx,
    src/components/music/TransposerMenu.tsx,
    src/components/admin/SoundSystemSection.tsx,
    src/components/admin/UserRow.tsx,
    src/components/nav/DesktopHeader.tsx,
    src/app/(main)/manage/page.tsx,
    src/app/(main)/schedule/page.tsx,
    src/app/(main)/settings/page.tsx,
    src/app/(main)/manage/templates/page.tsx,
    src/app/(main)/manage/templates/TemplateEditor.tsx,
    src/app/(main)/settings/sound/page.tsx
  </files>
  <action>
    Add aria-label to all 31 icon-only buttons missing labels. Use descriptive, action-oriented labels:

    - AudioPlayer.tsx: "Skip back 10 seconds", "Play"/"Pause", "Skip forward 10 seconds", "Mute"/"Unmute"
    - CalendarHeader.tsx: "Previous month", "Next month"
    - SongChartsLibrary.tsx: "Back to library"
    - TransposerMenu.tsx: "Transpose down", "Transpose up"
    - SoundSystemSection.tsx: "Scan network"
    - UserRow.tsx: contextual labels based on button action (e.g., "Edit user", "Delete user")
    - manage/page.tsx: "Back"
    - schedule/page.tsx: "Previous month", "Next month"
    - settings/page.tsx: contextual labels based on button action
    - manage/templates/page.tsx: "Back to manage"
    - TemplateEditor.tsx: "Delete template"
    - settings/sound/page.tsx: "Back to settings"

    Fix DesktopHeader.tsx line 90: Change alt="Logo" to alt="Central Reform Congregation logo"

    Avoid: Adding sr-only spans where aria-label suffices — aria-label is preferred for icon buttons.
    Avoid: Changing button behavior, styling, or structure.
  </action>
  <verify>grep -r 'size="icon"' src/ --include="*.tsx" | grep -v aria-label | grep -v title (should return 0 results from our modified files)</verify>
  <done>AC-3 and AC-4 satisfied: All icon-only buttons labeled, logo alt text descriptive</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any component logic, state management, or event handlers
- Layout structure, padding, margins, or spacing
- Admin panel styling (out of scope per project rules)
- Existing aria-label values (only ADD missing ones)
- Test files

## SCOPE LIMITS
- Only replace colors explicitly listed in roadmap Phase 16 scope
- Do not refactor component structure
- Do not add new sr-only spans (aria-label is sufficient for buttons)
- Do not create new utility classes or design token abstractions

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds with no errors
- [ ] `npx vitest run` passes (no regressions)
- [ ] Zero `bg-black` in FlowItemView.tsx
- [ ] Zero `bg-black/40` in MetronomeControl.tsx
- [ ] Zero `bg-red-500` in error-state.tsx
- [ ] All GlobalAlertBanner bg/border have dark: variants
- [ ] `grep -r 'size="icon"' src/ --include="*.tsx" | grep -v 'aria-label\|title'` returns no results from modified files
- [ ] DesktopHeader alt text says "Central Reform Congregation logo"
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Build succeeds, all 1113+ tests pass
- Zero hardcoded colors remain in targeted files
- All icon-only buttons in scope have aria-label
</success_criteria>

<output>
After completion, create `.paul/phases/16-design-token-accessibility/16-01-SUMMARY.md`
</output>
