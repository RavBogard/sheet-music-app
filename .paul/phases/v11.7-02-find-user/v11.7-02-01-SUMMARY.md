---
phase: v11.7-02-find-user
plan: 01
subsystem: mcp
tags: [mcp, roster, find_user, tenancy, asymmetry-principle, directory]

requires:
  - phase: v11.7-01-verify-first-audit
    provides: confirmed P0 behavior (list_musicians instrument gate at roster.ts:114-126) + asymmetry inventory + ready-to-plan verdict
provides:
  - find_user MCP resolver (email/nameContains/role/includeProfileless) over users collection
  - includeProfileless flag on list_musicians (interim unblock)
  - buildMusicianRow opt-in allowProfileless mode (profileless users no longer force-dropped)
affects: [v11.7-03-setlist-reverse-lookup, v11.7-04-reverse-index-sweep, suggest_musicians, suggest_band]

tech-stack:
  added: []
  patterns: ["read/resolve partner for every act-by-uid tool (Asymmetry Principle); in-memory tenant + attribute filters over the by-role query (no new index at current scale)"]

key-files:
  created: []
  modified:
    - src/lib/mcp/tools/roster.ts
    - src/lib/mcp/tools/index.ts
    - src/lib/mcp/__tests__/mcp-roster.emulator.test.ts

key-decisions:
  - "find_user DEFAULTS to includeProfileless:true — it is a directory; surfacing instrument-less people (David) is the whole point. Pass false to narrow to musicians."
  - "All filters (email exact/CI, name substring, role) run IN-MEMORY over the existing by-role query — no new Firestore index, no deploy, viable at current roster scale per v11.7-01 audit."
  - "buildMusicianRow gains an opt-in {allowProfileless} flag; the 3 suggest_*/getMusicianProfile callers stay flagless → behavior byte-identical (instrument gate intact there)."

patterns-established:
  - "Directory resolver = by-role query + rowOrgIds tenant wall + in-memory attribute filters + assertEditor gate (mirrors listMusicians)."

duration: ~35min
started: 2026-06-19T08:05:00Z
completed: 2026-06-19T08:30:00Z
---

# Phase v11.7-02 Plan 01: find_user (P0 user directory) Summary

**Shipped `find_user` — the Asymmetry-Principle directory resolver that surfaces every org user by email/name/role INCLUDING instrument-less band leaders (the David-is-invisible fix) — plus an `includeProfileless` interim on `list_musicians`. Editor-gated, tenant-scoped, no new index, no deploy.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~35 min |
| Tasks | 3 completed |
| Files modified | 3 (src) + PAUL docs |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: find_user surfaces instrument-less users (by name/email/role) | **PASS** | emulator: David (no instrument) found by `nameContains`, case-insensitive `email`, and `role:band_leader`; `instrument:null` preserved |
| AC-2: find_user tenant-scoped (cross-tenant wall) | **PASS** | emulator: broslaz-only user absent for CRC caller; dual-org user present |
| AC-3: editor gate + empty filter = full directory | **PASS** | emulator: plain musician → `forbidden_role`; `{}` returns whole org directory, sorted by displayName |
| AC-4: list_musicians includeProfileless interim | **PASS** | emulator: default drops profileless; `includeProfileless:true` surfaces with `instrument:null`/`instrumentLabel:null` |
| AC-5: suggest_* pool unchanged | **PASS** | flagless buildMusicianRow callers untouched; full roster emulator suite 58/58 green (no regression) |

## Verification Results

- `SKIP_ENV_VALIDATION=1 npx tsc --noEmit` → **0 errors**
- `firebase emulators:exec … vitest run … mcp-roster.emulator.test.ts` → **58 passed (58)** (7 new + 51 pre-existing)
- `SKIP_ENV_VALIDATION=1 npx next build --webpack` → **exit 0** (deployable; no route-export / bundle-boundary break)
- `grep "find_user"` confirms registration; `includeProfileless` present on both tools

## Accomplishments

- `find_user({email?, nameContains?, role?, includeProfileless?})` live MCP tool — the read partner that resolves a uid by name/email before `assign_musician` / `assign_monitor_bus` / `get_musician_profile`.
- David Lazaroff (band_leader, no instrument) is now discoverable; `suggest_*` coverage gap for profileless leaders addressed by the same row-builder fix path.
- `list_musicians` gained `includeProfileless` as a cheap interim, default-off (no behavior change for existing callers).

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/mcp/tools/roster.ts` | Modified | `buildMusicianRow` opt-in `allowProfileless`; new `findUser` resolver + `FindUserArgs`/`FindUserResult`; `includeProfileless` on `ListMusiciansArgs` + threaded into `listMusicians` |
| `src/lib/mcp/tools/index.ts` | Modified | Import + register `find_user` (assertEditor via resolver, `orgFrom` tenant scope); `includeProfileless` arg on `list_musicians` schema |
| `src/lib/mcp/__tests__/mcp-roster.emulator.test.ts` | Modified | +7 cases: find_user by name/email/role, tenant wall, gate refusal, empty-directory+sort, includeProfileless narrowing; list_musicians includeProfileless |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| find_user defaults `includeProfileless:true` | It is a directory; the entire purpose is finding people like David who have no instrument | Empty/name/email/role calls return profileless users by default; `false` narrows to musicians |
| In-memory filters, no new index | v11.7-01 audit confirmed viable at current roster scale; keeps the phase deploy-free | `(orgId,email)` index deferred unless scale forces it |
| Opt-in flag on shared `buildMusicianRow` (not a parallel builder) | Minimal change; preserves byte-identical behavior for `suggest_*`/`get_musician_profile` | No regression risk to the instrument-gated pool |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 0 | — |

**Total impact:** Plan executed exactly as written.

## Issues Encountered

None. (Firestore emulator was not running; ran via the project's `firebase emulators:exec` wrapper which starts/tears down automatically.)

## Next Phase Readiness

**Ready:**
- find_user resolver pattern (by-role query + rowOrgIds wall + in-memory filters + assertEditor) is the template for v11.7-03's `find_setlists_referencing_chart` / `search_setlists` and v11.7-04's `find_contact`.

**Concerns:**
- If the roster grows large, the in-memory email filter / `(orgId,email)` index becomes worth revisiting (documented, not currently a bottleneck).

**Blockers:** None.

---
*Phase: v11.7-02-find-user, Plan: 01*
*Completed: 2026-06-19*
