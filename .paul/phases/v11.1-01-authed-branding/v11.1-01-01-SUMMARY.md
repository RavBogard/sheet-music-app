---
phase: v11.1-01-authed-branding
plan: 01
subsystem: ui
tags: [multi-tenant, branding, nav, nextjs, ssr, org-aware, monogram]

requires:
  - phase: v11-03 (brotherslazaroff branding/vocab)
    provides: getOrgBranding seam + x-org-id Edge resolution + <html data-org> theme tokens
  - phase: v11-04-02 (BL consumer surface)
    provides: data-driven getOrgBranding pattern for the public/perform + metadata surfaces
provides:
  - Org-aware authed nav (DesktopHeader + MobileHeader) wordmark + logo resolved from the host org
  - getOrgBranding.logoUrl field (crc "/logo.jpg", broslaz "")
  - OrgLogo component (img when logoUrl set, else brand-colored initials monogram)
affects: [v11.1-03 (library visibility — same (main) nav surface), future tenants (one branding entry → correct nav)]

tech-stack:
  added: []
  patterns:
    - "Server-resolved host-org branding props passed through AppNavigation to nav headers (flash-free SSR), congregation store as live-update fallback"
    - "logoUrl='' → brand-colored initials monogram (bg-brand/text-brand-foreground), auto-upgrades to <img> when a path is set"

key-files:
  created:
    - src/components/nav/OrgLogo.tsx
    - src/components/nav/__tests__/org-logo.test.tsx
  modified:
    - src/lib/org/branding.ts
    - src/lib/org/__tests__/branding.test.ts
    - src/app/(main)/layout.tsx
    - src/components/nav/AppNavigation.tsx
    - src/components/nav/DesktopHeader.tsx
    - src/components/nav/MobileHeader.tsx

key-decisions:
  - "Empty logoUrl → circular initials monogram (Daniel 2026-06-09), not a shipped asset; auto-upgrades to <img> if a logoUrl is ever set"
  - "Monogram styled bg-brand + text-brand-foreground (solid fill, near-white text) for guaranteed contrast + per-org color flip, vs translucent bg-brand/15 which risked low contrast on the navy theme"
  - "Server branding props authoritative for first paint; congregation store kept as live-update fallback (no flash, no store removal)"

patterns-established:
  - "New tenant branding = one getOrgBranding entry (incl. logoUrl); nav follows automatically"

duration: ~25min
started: 2026-06-09
completed: 2026-06-09
---

# Phase v11.1-01 Plan 01: Org-aware authed branding Summary

**The authenticated top-nav (DesktopHeader + MobileHeader) now resolves its wordmark + logo from the host org via server-derived `getOrgBranding` props — brotherslazaroff.live shows "Brothers Lazaroff" + a navy "BL" monogram instead of the CRC wordmark + `/logo.jpg`, with no first-paint flash; CRC is byte-identical.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~25 min |
| Started | 2026-06-09 |
| Completed | 2026-06-09 |
| Tasks | 3 completed (3/3 E/Q PASS) |
| Files modified | 6 modified + 2 created |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: broslaz authed nav shows its own brand | Pass | serverOrgShortName="Brothers Lazaroff" + serverLogoUrl="" → OrgLogo renders navy "BL" monogram; server props render at SSR so no CRC flash before the congregation store syncs |
| AC-2: CRC authed nav byte-identical | Pass | crc → serverLogoUrl="/logo.jpg" → identical `<img>` markup + classes + wordmark "CRC Music". 1 invisible micro-deviation: mobile alt "Logo"→"Central Reform Congregation logo" (a11y improvement; no test depended on it) |
| AC-3: data-driven logoUrl (empty → monogram) | Pass | OrgLogo: non-empty logoUrl → `<img>`; "" → `role="img"` monogram, no `<img>`. Covered by org-logo.test.tsx (4 tests) |
| AC-4: quality gates | Pass | tsc clean · full suite 3332 passed / 0 failed / 80 skipped · `SKIP_ENV_VALIDATION=1 next build` clean (bundle boundary intact) |

## Accomplishments

- Closed the last consumer-visible CRC-branding leak on the 2nd live tenant: broslaz authed nav is now self-branded (wordmark + navy monogram), flash-free at SSR.
- Established the reusable `OrgLogo` img-or-monogram pattern + `getOrgBranding.logoUrl` field — a new tenant's nav branding is now a single data entry.
- Wired `MobileHeader` (previously received no props) into the server-branding plumbing.
- Full v11.1 quality floor green; CRC visibly unchanged.

## Task Commits

Executed under the v11.x autonomy posture as a single phase commit (not per-task); see Files Created/Modified. Phase commit hash recorded in STATE Git State after transition.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/org/branding.ts` | Modified | Added `logoUrl` to OrgBranding (crc "/logo.jpg", broslaz "") |
| `src/lib/org/__tests__/branding.test.ts` | Modified | Asserts per-org logoUrl + unknown→crc fallback |
| `src/app/(main)/layout.tsx` | Modified | Resolves host org from x-org-id → passes serverOrgShortName + serverLogoUrl to AppNavigation |
| `src/components/nav/AppNavigation.tsx` | Modified | Added serverOrgShortName/serverLogoUrl props; threads them to DesktopHeader + MobileHeader |
| `src/components/nav/DesktopHeader.tsx` | Modified | Logo/wordmark via OrgLogo + server-branding-first |
| `src/components/nav/MobileHeader.tsx` | Modified | Accepts branding props; logo/wordmark via OrgLogo |
| `src/components/nav/OrgLogo.tsx` | Created | img-or-monogram org logo (pure/SSR-safe) |
| `src/components/nav/__tests__/org-logo.test.tsx` | Created | 4 tests: CRC img alt, non-CRC img alt, empty→monogram, initials derivation |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Empty logoUrl → initials monogram | Daniel 2026-06-09 (AskUserQuestion) — visual parity with CRC's circular logo, no asset dependency, ships now | Auto-upgrades to `<img>` the day a broslaz logoUrl is set |
| Monogram = solid `bg-brand` + `text-brand-foreground` | Guaranteed ≥4.5:1 contrast + auto per-org color flip (navy under `[data-org="brotherslazaroff"]`); translucent bg risked low contrast on navy | Crisp, on-brand, accessible badge |
| Server props authoritative, store kept as fallback | Eliminates the SSR/pre-sync CRC flash without removing the live-update path | No behavior regression for CRC; broslaz correct at first paint |

## Deviations from Plan

### Summary

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Micro-deviation | 1 | Invisible a11y improvement |

**Total impact:** Plan executed as written. One intentional micro-deviation below.

### Auto-fixed Issues

None.

### Micro-deviation

**1. [a11y] Mobile logo alt text standardized**
- **Found during:** Task 2 (OrgLogo + nav wiring)
- **Issue:** Desktop CRC alt was "Central Reform Congregation logo" but mobile was the generic "Logo"; OrgLogo derives one alt per (logoUrl, shortName).
- **Fix:** OrgLogo special-cases logoUrl==="/logo.jpg" → "Central Reform Congregation logo", so mobile now matches desktop. Invisible (alt attribute); strictly more descriptive. Confirmed no test asserted the old "Logo".
- **Files:** src/components/nav/OrgLogo.tsx
- **Verification:** org-logo.test.tsx asserts the CRC alt; visible CRC nav byte-identical.

### Deferred Items

None.

## Issues Encountered

None. (Tooling note: on this Windows box the Edit tool requires a prior Read-tool record before editing — re-read `(main)/layout.tsx` mid-task; no impact on output.)

## Next Phase Readiness

**Ready:**
- v11.1-03 (library generic-tab visibility, P1) — the next P1 phase; shares the `(main)` nav surface but touches library read paths, not nav branding. No dependency conflict.
- OrgLogo + getOrgBranding.logoUrl available for any future per-tenant chrome.

**Concerns:**
- None. broslaz `logoUrl` remains "" by design (monogram); set a path if/when a real asset exists.

**Blockers:**
- None.

**Standing UAT-PENDING (non-blocking):** Daniel to load an authed brotherslazaroff.live page and confirm "Brothers Lazaroff" + navy BL monogram in both desktop + mobile (iPad WebKit) nav, and confirm CRC nav unchanged.

---
*Phase: v11.1-01-authed-branding, Plan: 01*
*Completed: 2026-06-09*
