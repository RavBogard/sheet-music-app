---
phase: 05-deferred-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/api/test-gemini/route.ts, eslint.config.mjs, src/lib/roles.ts, src/lib/api-auth.ts, src/lib/server-auth.ts, src/middleware.ts, src/components/admin/UserRow.tsx, src/components/admin/PeopleSection.tsx, src/components/setlist/v2/MusicianPicker.tsx, src/app/api/admin/set-role/route.ts, src/app/api/setlist/email-packets/route.ts, src/app/api/bridge/setup-code/route.ts, src/lib/notification-store.ts, src/app/api/push/send/route.ts, firestore.rules, src/lib/__tests__/roles.test.ts]
autonomous: true
---

<objective>
## Goal
Clean up three deferred items: (1) remove test-gemini production endpoint, (2) fix ESLint 9 config, (3) remove all 'leader' backward-compat code (LOW-004).

## Purpose
Remove dead code and legacy compat that adds cognitive overhead. Fix linting so `npm run lint` works for CI/local use.

## Output
- test-gemini route deleted
- `npm run lint` passes
- No references to legacy 'leader' role remain in codebase
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Delete test-gemini production endpoint</name>
  <files>src/app/api/test-gemini/route.ts</files>
  <action>Delete the entire file. It's a test/debug endpoint that shouldn't be in production.</action>
  <verify>File no longer exists. TypeScript compiles.</verify>
</task>

<task type="auto">
  <name>Task 2: Fix ESLint 9 config</name>
  <files>eslint.config.mjs, package.json</files>
  <action>
    The eslint-config-next spreads config but react-hooks plugin may not resolve properly in ESLint 9 flat config.

    1. Run `npm run lint` to confirm the exact error
    2. If react-hooks plugin missing: install `eslint-plugin-react-hooks` as devDependency
    3. If config format issue: adjust eslint.config.mjs to properly register plugins
    4. Verify `npm run lint` completes (warnings OK, errors not OK)
  </action>
  <verify>`npm run lint` runs without plugin resolution errors.</verify>
</task>

<task type="auto">
  <name>Task 3: Remove legacy 'leader' role from codebase</name>
  <files>Multiple — see list below</files>
  <action>
    Remove all backward-compat references to the 'leader' role string. The canonical role is 'band_leader'.

    **NOTE:** Before removing code, need to check if any Firestore users still have role='leader'.
    Create a one-time migration script or admin API call to update them first.

    Files to update (remove 'leader' backward compat):
    1. `src/lib/roles.ts` — Remove 'leader' from UserRole type, remove from ROLE_HIERARCHY
    2. `src/lib/api-auth.ts` — Remove `|| userRole === 'leader'` checks (lines 73, 103)
    3. `src/lib/server-auth.ts` — Remove `|| role === "leader"` (line 45)
    4. `src/middleware.ts` — Remove `|| role !== 'leader'` (line 76)
    5. `src/components/admin/UserRow.tsx` — Remove effectiveRole compat (line 141)
    6. `src/components/admin/PeopleSection.tsx` — Remove `|| u.role === ('leader' as string)` (line 51)
    7. `src/components/setlist/v2/MusicianPicker.tsx` — Remove `|| u.role === ('leader' as string)` (line 205)
    8. `src/app/api/admin/set-role/route.ts` — Remove 'leader' from zod enum (line 9)
    9. `src/app/api/setlist/email-packets/route.ts` — Remove 'leader' from Firestore where-in (line 48)
    10. `src/app/api/bridge/setup-code/route.ts` — Remove 'leader' from includes array (line 37)
    11. `src/lib/notification-store.ts` — Remove 'leader' from where-in (line 198)
    12. `firestore.rules` — Remove leader backward compat line (line 27)
    13. `src/lib/__tests__/roles.test.ts` — Update/remove leader-specific tests
    14. `src/app/api/push/send/route.ts` — Remove leader references
  </action>
  <verify>
    - grep for 'leader' finds zero role-related matches
    - TypeScript compiles
    - Tests pass (after updating role tests)
  </verify>
</task>

</tasks>

<boundaries>
## SCOPE LIMITS
- Do NOT touch LOW-005 (logger levels) or LOW-006 (CORS) — accepted as-is
- Do NOT change any auth flow logic beyond removing 'leader' string checks
- If any Firestore users have role='leader', migrate them via admin API before removing code
</boundaries>

<verification>
- [ ] test-gemini route deleted
- [ ] `npm run lint` runs without plugin errors
- [ ] No 'leader' role references remain (except comments explaining the migration)
- [ ] TypeScript compiles (`npx tsc --noEmit`)
- [ ] All tests pass (`npx vitest run`)
</verification>
