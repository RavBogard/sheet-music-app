---
phase: v11.7-03-setlist-reverse-lookup
plan: 01
subsystem: mcp
tags: [mcp, setlists, reverse-lookup, asymmetry-principle, tenancy, tracks]

requires:
  - phase: v11.7-01-verify-first-audit
    provides: confirmed delete_chart reference-walk location (library-upload.ts:787-846), tracks-are-top-level, app-side-filter-acceptable verdict
  - phase: v11.7-02-find-user
    provides: the resolver pattern (query + rowOrg tenant wall + in-memory/app-side filters)
provides:
  - find_setlists_referencing_chart MCP tool (which live setlists bond a chart, by fileId/songId)
  - search_setlists MCP tool (find setlists by trackTitle/leadMusician/templateType)
affects: [v11.7-04-reverse-index-sweep]

tech-stack:
  added: []
  patterns: ["reverse-index read partner surfacing an existing internal walk (delete_chart); ungated-but-tenant-scoped setlist reads consistent with list_setlists/get_setlist"]

key-files:
  created:
    - src/lib/mcp/__tests__/mcp-setlists-reverse.emulator.test.ts
  modified:
    - src/lib/mcp/tools/setlists.ts
    - src/lib/mcp/tools/index.ts

key-decisions:
  - "Both tools are UNGATED authenticated reads (NOT assertEditor) — setlist data is public-by-design and the sibling reads (list_setlists/get_setlist) are ungated; tenant-scoped via rowOrg only. find_user gated because it returns people/PII; setlists do not."
  - "find_setlists_referencing_chart surfaces the delete_chart walk but returns ALL live in-tenant matches (cap 200 + truncated flag) vs delete_chart's existence-cap; dangling-parent tracks excluded + counted."
  - "search_setlists is app-side: templateType filters at setlist level (no track read); trackTitle/leadMusician read tracks per candidate. No new index — getAllSetlists({org}) uses the existing (orgId,date) index."

patterns-established:
  - "Reverse-index reads reuse the write-side's internal walk and add tenant-scoping + completeness."

duration: ~30min
started: 2026-06-19T14:10:00Z
completed: 2026-06-19T14:32:00Z
---

# Phase v11.7-03 Plan 01: setlist reverse-lookup Summary

**Shipped two P1 Asymmetry-Principle setlist read tools — `find_setlists_referencing_chart` (which LIVE setlists bond a chart, by fileId/songId — surfacing delete_chart's internal walk) and `search_setlists` (find setlists by trackTitle/leadMusician/templateType, app-side). Both tenant-scoped, ungated-authenticated (sibling-consistent), no new index, no deploy.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~30 min |
| Tasks | 3 completed |
| Files modified | 2 + 1 new test |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: find_setlists_referencing_chart returns live setlists, excludes dangling | **PASS** | emulator: chart-A → S-crc present, dead-parent track excluded, danglingTracksIgnored≥1 |
| AC-2: supports songId + validates empty input | **PASS** | songId → S2-crc; `{}` → invalid_argument envelope |
| AC-3: search_setlists by trackTitle/leadMusician/templateType + validates | **PASS** | each filter returns only matching setlists w/ matchedTracks; `{}` → invalid_argument |
| AC-4: both tools enforce cross-tenant wall | **PASS** | CRC caller never sees broslaz setlists; BL caller never sees CRC matches |
| AC-5: gating matches sibling reads (not over-gated) | **PASS** | both ungated-authenticated + tenant-scoped, like list_setlists/get_setlist |

## Verification Results

- `SKIP_ENV_VALIDATION=1 npx tsc --noEmit` → **0 errors**
- new emulator suite `mcp-setlists-reverse.emulator.test.ts` → **9 passed (9)**
- existing `setlists.test.ts` (mock) → **12 passed (12)** (no regression)
- `SKIP_ENV_VALIDATION=1 npx next build --webpack` → **exit 0** (deployable)
- grep confirms both tools registered in `index.ts`

## Accomplishments

- `find_setlists_referencing_chart({ fileId?, songId? })` — the read partner to delete_chart's `chart_in_use` refusal; answers "where is this chart used?" returning live in-tenant setlists + the matching track, excluding dangling-parent tracks.
- `search_setlists({ trackTitle?, leadMusician?, templateType? })` — content search over the caller-org's setlists, returning matchedTracks per setlist.
- Reused the delete_chart walk shape + the find_user resolver pattern; zero new Firestore index.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/mcp/tools/setlists.ts` | Modified | `findSetlistsReferencingChart` + `searchSetlists` resolvers (+ logger import) |
| `src/lib/mcp/tools/index.ts` | Modified | Import + register both tools (ungated handler, `orgFrom` tenant scope) |
| `src/lib/mcp/__tests__/mcp-setlists-reverse.emulator.test.ts` | Created | 9 emulator cases (reference walk, dangling exclusion, songId, content filters, tenant wall, validation) |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Ungated reads (no assertEditor) | Setlist data is public-by-design ([[feedback_setlist_public_policy]]) + err-public; sibling reads (list_setlists/get_setlist) are ungated | Any authenticated MCP caller may use them, scoped to their org |
| Return ALL live matches (cap 200 + truncated flag) | A reverse-lookup needs completeness, unlike delete_chart's existence check | No silent truncation; logs on cap |
| App-side search, no new index | v11.7-01 audit: acceptable at current scale; getAllSetlists({org}) uses existing (orgId,date) index | No deploy this phase |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 0 | — |

**Total impact:** Plan executed exactly as written.

## Issues Encountered

None.

## Next Phase Readiness

**Ready:**
- v11.7-04 (P2 sweep: `find_setlists_from_template` + `find_contact`) can reuse these patterns. Note `find_setlists_from_template` DOES want a new `(orgId, sourceTemplateId)` index (deploy as an AUTO task when 04 ships it).

**Concerns:**
- search_setlists reads tracks per candidate setlist when trackTitle/leadMusician is set — O(setlists) reads, fine at current scale; revisit with a content index only if the catalog grows large.

**Blockers:** None.

---
*Phase: v11.7-03-setlist-reverse-lookup, Plan: 01*
*Completed: 2026-06-19*
