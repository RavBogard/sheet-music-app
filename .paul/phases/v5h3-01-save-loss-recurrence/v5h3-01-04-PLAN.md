---
phase: v5h3-01-save-loss-recurrence
plan: 04
type: execute
wave: 1
depends_on: ["v5h3-01-03"]
files_modified:
  - .paul/postmortems/v5h3-01-save-loss-recurrence.md
  - .paul/PROJECT.md
autonomous: true
---

<objective>
## Goal
Close phase v5h3-01 with (1) a written postmortem at `.paul/postmortems/v5h3-01-save-loss-recurrence.md` covering the H-SL-7 phantom-VersionMismatch mechanism + the mid-execution-UAT pivot + the Round-2 Option B instrumentation outcome, and (2) a BINDING harness-fidelity gate in PROJECT.md that prevents future data-flow phases from shipping until the v5h-01 §5 / v5h3 twice-implicated harness gap is closed.

## Purpose
v5h-01-04 left the harness-fidelity remediation (Firebase emulator + thin RTL editor↔perf-view test pair) as an "opportunistic" action item. v5h3-01-01..03 are evidence the deferral was wrong — H-SL-7 slipped through the same harness for the same reason (in-memory zero-latency adapters miss real Firestore semantics). Daniel's standing ask is **systemic fixes, not bandaids** (auto-memory). A postmortem without a binding gate is a third deferral. The PROJECT.md constraint converts "opportunistic" → blocking; the postmortem captures what was learned twice.

## Output
- `.paul/postmortems/v5h3-01-save-loss-recurrence.md` — new file (~150-250 lines); cross-links to v5h-01 postmortem; documents H-SL-7 mechanism, mid-execution UAT pivot, Daniel-loop UAT discipline second validation today, harness-fidelity gap re-implication, escalation from "opportunistic" → "binding gate"
- `.paul/PROJECT.md` — extended Constraints section with new "Harness Fidelity Gate" subsection naming the work, owner, and blocking semantics
- Phase v5h3-01 closes with 4 of 4 plans complete; v53-02..04 unblock once Daniel weekly worship cycle UAT confirms the v5h3-01-03 fix holds
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work (genuinely needed for this postmortem)
@.paul/postmortems/v5h-01-save-loss.md
@.paul/postmortems/v5h3-01-save-loss-recurrence-investigation.md
@.paul/phases/v5h3-01-save-loss-recurrence/v5h3-01-01-SUMMARY.md
@.paul/phases/v5h3-01-save-loss-recurrence/v5h3-01-02-SUMMARY.md
@.paul/phases/v5h3-01-save-loss-recurrence/v5h3-01-03-SUMMARY.md
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /ui-ux-pro-max | NOT required | This plan modifies docs only (postmortem + PROJECT.md constraint); zero source/test changes; same precedent as v5h-01-04 + v50-07-05 | N/A |

No skill blocking gate. Plan is autonomous=true; no checkpoints; pure documentation deliverables.
</skills>

<acceptance_criteria>

## AC-1: Postmortem written and cross-linked
```gherkin
Given v5h3-01-01..03 are LOOP COMPLETE and the H-SL-7 fix is deployed (commit 36e9fa1)
When I open `.paul/postmortems/v5h3-01-save-loss-recurrence.md`
Then I see TL;DR / Timeline / Root Cause (H-SL-7 mechanism: stale expectedUpdatedAt threading on rapid same-doc edits) / What Got Shipped (refs commits 1d8d94c + 36e9fa1) / Lessons (mid-execution UAT pivot, Daniel-loop UAT validated 2nd time today, harness-fidelity gap RE-IMPLICATED) / Action Items (with owner + target + blocking semantics) / Appendix
And the document cross-links to `v5h-01-save-loss.md` (parent postmortem) and `v5h3-01-save-loss-recurrence-investigation.md` (investigation artifact)
And the document does NOT modify the existing v5h-01 postmortem (treated as canonical for that incident)
```

## AC-2: Harness-fidelity gate codified in PROJECT.md
```gherkin
Given PROJECT.md Constraints section currently has "UAT Discipline (data-flow fixes)" subsection only
When the plan completes
Then PROJECT.md Constraints section contains a NEW "Harness Fidelity Gate" subsection that:
  - Names the gap (in-memory zero-latency adapters + no security-rules layer + no perf-view-path cross-view test in the kitchen-sink harness)
  - Names the remediation (Firebase Local Emulator Suite integration + thin RTL editor↔perf-view cross-view propagation test)
  - Names the blocking semantics: "Any future phase that touches sync engine / Dexie schema or writes / snapshot-listener / lazy-hydration / perf-view rendering / editor cell-commit / Firestore rules requires the harness-fidelity work to be SHIPPED OR an explicit per-plan waiver under <boundaries> SCOPE LIMITS naming v5.4 ticket"
  - Cross-references both v5h-01 §5 action item #2 (the original deferral) and the new v5h3-01-04 postmortem (the re-implication evidence)
  - Names the v5.4 milestone as the binding ship target
And no other PROJECT.md sections are altered
```

## AC-3: Phase v5h3-01 ships 4 of 4 plans
```gherkin
Given STATE.md shows v5h3-01 at 3 of 4 plans complete
When this plan loop closes (PLAN ✓ → APPLY ✓ → UNIFY ✓)
Then phase v5h3-01 stands at 4 of 4 plans complete
And STATE.md reflects "v5h3-01 LOOP COMPLETE"; phase ready to be marked closed pending only Daniel weekly worship cycle UAT
And the v5h3-01-04 commit explicitly stages `.paul/phases/v5h3-01-save-loss-recurrence/v5h3-01-04-*.md` + `.paul/postmortems/v5h3-01-save-loss-recurrence.md` + `.paul/PROJECT.md` (no orphaned PAUL artifacts per feedback memory)
```

## AC-4: Boundaries respected; suite + build remain green
```gherkin
Given the plan modifies only `.paul/postmortems/v5h3-01-save-loss-recurrence.md` (new) + `.paul/PROJECT.md`
When `npm test` and `npm run build` run after the changes
Then full suite is 1560/1560 (no regression from v5h3-01-03 close)
And `next build` is clean (TypeScript + Next compile)
And `git diff sheet-music-app/src/ sheet-music-app/firestore.rules sheet-music-app/firestore.indexes.json` is empty (no source/rules/indexes touched)
And boundary diff confirms ONLY `.paul/PROJECT.md` + `.paul/postmortems/v5h3-01-save-loss-recurrence.md` modified outside the phase directory
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Write v5h3-01 postmortem</name>
  <files>sheet-music-app/.paul/postmortems/v5h3-01-save-loss-recurrence.md</files>
  <action>
    Create new file (do NOT extend v5h-01-save-loss.md — that's the canonical postmortem for the v5h-01 incident; this is a sibling for the v5h3 recurrence). Mirror the v5h-01 postmortem structure for consistency.

    **Required sections:**

    1. **Header block:** Date 2026-05-02, Author "Rabbi Daniel + PAUL session", Severity P0 (data corruption symptom: phantom reconciliation modal mid-edit on single-user iPad; affected user count 1 — Daniel; band not yet onboarded), Status RESOLVED, Phase v5h3-01 (4 plans), Final commit `36e9fa1`, Suite 1528 → 1560 (+32 across v5h3-01-02 + v5h3-01-03), Production data corruption: 0 confirmed (reconciliation modal preserved user edits via "Keep mine"). Companion artifact links to: v5h3-01-01-SUMMARY, v5h3-01-02-SUMMARY, v5h3-01-03-SUMMARY, v5h-01-save-loss.md (parent), v5h3-01-save-loss-recurrence-investigation.md (research).

    2. **TL;DR (single paragraph):** v50-06-02's reconciliation modal ("Keep mine / Take theirs") was firing for SINGLE-USER rapid same-doc edits on Daniel's iPad. Root cause: engine writeback updated server-stamped `updatedAt` on the entity row but did NOT thread the new `updatedAt` into pending outbox rows for the same doc; rapid same-doc Edit-2 (queued before Edit-1's writeback completed) carried a STALE `expectedUpdatedAt`; Firestore tx precondition failed; engine raised VersionMismatchError; FSM transitioned to `conflict`; modal opened. v50-06-02 contract was correct under sane threading; threading itself was the bug. v5h3-01-01 code-scan ruled out 3 of 6 hypotheses (H-SL-2/3/4 definitive); 3 stayed open (H-SL-1/5/6) pending production evidence; HUMAN-ACTION DEFERRED per Daniel "continue autonomously". v5h3-01-02 shipped auto-capture instrumentation (Sentry breadcrumbs at 5 hot write paths + IndexedDB edit_log + upload-on-mount) — Round-2 Option B from the investigation. **Mid-instrumentation-build, Daniel reported the reconciliation modal symptom.** That single sentence pivoted diagnosis from "6 hypotheses, wait for next recurrence" → "H-SL-7 HIGH confidence, ship the fix today" (NEW H-SL-7 entered the matrix). v5h3-01-03 shipped the fix at `36e9fa1`: engine writeback now threads server `updatedAt` into pending outbox rows for the same `(collection, docId)` atomically with existing writeback; v50-06-02 reconciliation contract preserved (AC-3 explicit test using FailNthCallAdapter for genuine multi-writer). The kitchen-sink harness (v50-07-04) didn't catch H-SL-7 for the same reason it didn't catch v5h-01: in-memory zero-latency adapters never produce rapid-same-doc-edit-with-stale-expectedUpdatedAt sequences. The harness-fidelity gap from v5h-01 §5 action item #2 is now twice-implicated; this postmortem escalates it from "opportunistic" → "binding gate" before the next data-flow phase ships.

    3. **Timeline:** Use a markdown table with `Date | Event` columns. Include:
       - 2026-04-27 v5h-01 phase ✅ COMPLETE (4 plans). v5.0-hotfix milestone closed pending UAT. Action item #2 deferred.
       - 2026-04-27..2026-05-02 v5.1 + v5.2 ship (5 + 5 phases). Daniel-loop UAT discipline codified in v51-04 + PROJECT.md.
       - 2026-05-02 morning Daniel iPad UAT during v53-01 synthesis surfaces save-loss recurrence symptom ("some edits saved, some didn't") on a fresh setlist. v5.3 milestone is rescoped to insert v5h3-01 hotfix BEFORE v53-02..04 (decisions table in STATE.md captures the rescope).
       - 2026-05-02 v5h3-01-01 PLAN: research + reproduce + diagnose. HUMAN-ACTION checkpoint (iPad inspection) DEFERRED per Daniel "I've already refreshed" + "continue autonomously". Code-scan only.
       - 2026-05-02 v5h3-01-01 APPLY: 6-hypothesis code-scan. H-SL-2/3/4 RULED OUT definitively. H-SL-1/5/6 STILL OPEN (cannot disambiguate without production evidence). Anti-pattern audit PASSES — all v5h-01 fixes intact. Round-2 decision checkpoint: Daniel selects Option B (auto-capture instrumentation only). v5h3-01-01 LOOP COMPLETE.
       - 2026-05-02 v5h3-01-02 PLAN: auto-capture instrumentation (Sentry breadcrumbs at 5 hot sites + IndexedDB edit_log table + upload-on-mount). autonomous=true, type=execute, ~1.5h dispatched to dan-executor agent.
       - 2026-05-02 v5h3-01-02 APPLY in progress; Daniel reports MID-EXECUTION via UAT message: "I'm in the middle of making setlists right now and having the terrible bugs around saving. it seems to be related to the 'keep mine' vs 'take theirs' things.... EVEN THOUGH I AM THE ONLY ONE USING THE SETLIST OR EDITING IT." That single sentence is high-signal: reconciliation modal only fires on VersionMismatchError; single-user context = phantom conflict. NEW H-SL-7 enters the hypothesis matrix at HIGH confidence.
       - 2026-05-02 v5h3-01-02 LANDS at `1d8d94c`. Pushed origin master. Vercel auto-deploys instrumentation.
       - 2026-05-02 v5h3-01-03 PLAN written WHILE v5h3-01-02 deploy propagates. Engine writeback fix threading expectedUpdatedAt into pending outbox rows for same doc, atomic with existing writeback, status==='pending' filter, same-collection+same-docId filter. AC-3 explicit test for v50-06-02 reconciliation contract preservation (FailNthCallAdapter for genuine multi-writer).
       - 2026-05-02 v5h3-01-03 APPLY: dispatched to dan-executor. 28 LOC source + 387 LOC tests; commit `36e9fa1` lands first try; Suite 1557 → 1560. Pushed origin master.
       - 2026-05-02 v5h3-01-03 LOOP COMPLETE. Phase 75% (3 of 4 plans). Daniel resumes editing after Vercel deploy; phantom reconciliation modal stops firing for single-user rapid same-doc edits.
       - 2026-05-02 (this plan) v5h3-01-04 postmortem + harness-fidelity gate codification. Time-to-resolution: ~3h end-to-end on 2026-05-02 from initial UAT signal to deployed fix; ~90min more for instrumentation + diagnosis + fix; this postmortem closes the phase.

    4. **Root Cause section:** Explain H-SL-7 mechanism precisely:
       - **Editor cell-commit path:** DropdownCell/TextCell.commit fires `onCommit(draft)` → grid handler invokes `applyEdit('update', 'tracks', { docId, patch, expectedUpdatedAt: row.updatedAt })` where `row.updatedAt` is the value from `useLiveQuery` at render time.
       - **Engine drain:** drainOnce reads pending outbox row, calls adapter `commitOutboxRow({ row, expectedUpdatedAt })` → production Firestore adapter wraps in `runTransaction` with precondition `tx.get(docRef).data().updatedAt === expectedUpdatedAt`; on success bumps with `serverTimestamp()`.
       - **Engine writeback (engine.ts:259-282):** on success, opens `db.transaction('rw', db.outbox, db[collection], ...)` → deletes outbox row + puts entity with new server-stamped updatedAt (atomic). **THE BUG:** the writeback DID NOT update `expectedUpdatedAt` on OTHER pending outbox rows for the same `(collection, docId)`. Rapid Edit-2 queued before Edit-1's writeback completed → Edit-2's outbox row carries Edit-1's STALE expectedUpdatedAt.
       - **Failure mode:** Engine drains Edit-2 → adapter precondition fails → VersionMismatchError → engine.ts:309-316 marks row failed + dispatches DRAIN_VERSION_MISMATCH → FSM → conflict → ReconciliationProvider opens modal. User clicks "Keep mine" → engine.resolveConflict reads current local entity updatedAt → re-queues with newExpectedUpdatedAt → succeeds. Edit IS preserved (no data loss); modal IS the user-visible symptom.
       - **The fix:** v5h3-01-03 added one operation INSIDE the existing writeback tx — `db.outbox.where('[collection+docId+status]').equals([collection, docId, 'pending']).modify({ expectedUpdatedAt: result.updatedAt })`. Filter on status==='pending' (don't touch sending mid-flight or failed terminal). Filter on collection+docId (don't touch cross-doc rows). Folded into existing `if (result.updatedAt !== undefined && row.op !== 'delete')` gate. Atomic with outbox.delete + entity.put — all three roll back together or commit together.
       - **Why v50-06-02 contract was correct:** VersionMismatch IS the right behavior for genuine concurrent multi-writer scenarios. The contract assumed sane expectedUpdatedAt threading. Threading itself was the unaudited substrate-level bug. The v5h3-01-03 fix narrows VersionMismatch's trigger condition to genuine multi-writer; AC-3 (FailNthCallAdapter) explicitly tests genuine-multi-writer still raises VersionMismatch + opens modal.

    5. **What Got Shipped section:** Markdown table with `Commit | Plan | Change` columns:
       - `1d8d94c` — v5h3-01-02 — Auto-capture instrumentation: Sentry breadcrumbs at 5 hot write paths (TextCell.commit, DropdownCell.commit, applyEdit success/error, engine.drainOnce success/handleAdapterError-entry/dead-letter, snapshot-listener per-change post-tx) + Dexie schema bump v2→v3 (additive `edit_log: '++id, ts'` table) + upload-on-mount helper (FIFO-capped 500 rows; clearUploaded(maxId) after upload). 4 created files + 7 modified. +313 / -9 LOC. +29 tests. No-PII enforcement test. Fail-soft (try/catch around all instrumentation calls).
       - `36e9fa1` — v5h3-01-03 — Engine writeback expectedUpdatedAt threading fix. engine.ts +28 LOC. property-failures.test.ts +387 LOC (new "v5h3-01-03: pending-outbox expectedUpdatedAt threading" describe with 3 cases: rapid-same-doc-no-mismatch / genuine-multi-writer-still-conflicts / cross-doc-untouched). v50-06-02 reconciliation contract preserved (AC-3 explicit). Suite 1557 → 1560.

       Cross-link: research artifact at `.paul/postmortems/v5h3-01-save-loss-recurrence-investigation.md` — code-scan verdicts, 6-hypothesis matrix, anti-pattern audit, mid-execution evidence section adding H-SL-7 + H-SL-8 at HIGH/MEDIUM-HIGH confidence.

    6. **Lessons section** (numbered subheadings; mirror v5h-01 postmortem format):

       **Lesson 1 — Mid-execution UAT signals are worth pivoting on.** Daniel's mid-build UAT message ("Keep mine / Take theirs... I am the only one using the setlist") had MORE diagnostic value than the entire morning's code-scan. Reconciliation modal in single-user context is a HIGH-signal disambiguator: it categorically rules out save-loss-without-attempt (H-SL-1/2/3) AND auth-error classes (H-SL-5) AND points directly at VersionMismatch + expectedUpdatedAt threading. Pattern: when a UAT message arrives mid-execution, inspect for high-signal disambiguators BEFORE dismissing it as "after current plan ships". For v5h3 specifically, the message arrived ~30min into the v5h3-01-02 instrumentation build and pivoted the entire trajectory: instrumentation still shipped (still useful for FUTURE classes of save-loss) but the diagnosis no longer needed to wait for it.

       **Lesson 2 — Daniel-loop UAT discipline (codified v51-04) validated for the SECOND time today.** First validation: morning UAT during v53-01 synthesis surfaced the recurrence at all (without it, v5h3-01 wouldn't have been opened). Second validation: mid-execution UAT pivoted v5h3-01-02 → v5h3-01-03 the same day. Both validations reinforce that real-iPad UAT against real production is the irreplaceable diagnostic surface for this app's data-flow class. The harness can prove invariants under its grammar; it cannot prove the grammar covers the user's actual workflow.

       **Lesson 3 — Harness-fidelity gap from v5h-01 §5 action item #2 is now twice-implicated; deferral was wrong.** v5h-01-04 framed the remediation as "Owner: Rabbi Daniel. Target: opportunistic during v5.1 OR before next major cutover." Five weeks passed; v5.1 + v5.2 + v53-01 all shipped without the remediation. v5h3 was not a cutover (no new collections; no rules changes); it was a substrate-level threading bug in a path the kitchen-sink harness exercises every CI run. The harness still didn't catch it because the in-memory zero-latency adapter never produced the rapid-same-doc-edit-before-writeback-completes sequence. **The harness's edit-grammar is the gap, not just the rules layer or the perf-view path.** v5h3-01-03 added 3 regression tests at the existing harness level (in-memory adapter); those tests caught the bug AFTER it surfaced from production but couldn't have caught it BEFORE because the grammar wasn't there. The remediation needs to expand harness fidelity in two dimensions:
       - **Real-Firestore-semantics fidelity** (Firebase Local Emulator Suite): rules layer + auth + cache-then-fresh delivery + transaction precondition timing — addresses Gap A + C from v5h-01.
       - **Cross-view path coverage** (thin RTL editor↔perf-view test pair): addresses Gap B from v5h-01; minimal fixture asserting cross-view propagation invariant.

       The escalation from "opportunistic" → "binding gate" lives in PROJECT.md (Task 2 of this plan). The TL;DR: any future phase that touches data flow ships behind the harness-fidelity work or carries an explicit per-plan waiver under <boundaries> SCOPE LIMITS naming the v5.4 ticket. The third deferral is what we are explicitly preventing.

       **Lesson 4 — Investigation document quality matters when HUMAN-ACTION is deferred.** v5h3-01-01's investigation doc (research artifact, NOT this postmortem) ruled out 3 of 6 hypotheses by code-scan alone, then captured the H-SL-7 hypothesis under "NEW EVIDENCE" when Daniel's mid-execution UAT arrived. Without the rigorous code-scan + ruled-out matrix, the H-SL-7 jump would have looked like a guess; with it, the jump was traceable: Daniel's evidence ruled out the open hypotheses (H-SL-1 weakened to LOW, H-SL-5 ruled out, H-SL-6 crystallized into H-SL-7) and crystallized one of them. Pattern: when HUMAN-ACTION is deferred, the code-scan diagnostic doc IS the investigation surface — its quality determines how fast follow-up evidence can be triaged into a ranked candidate.

    7. **Action Items section:** Markdown table with `# | Action | Owner | Target | Blocking?` columns:
       - **#1 (BINDING)** — Codify "Harness Fidelity Gate" in PROJECT.md Constraints section. Names the work (Firebase Local Emulator Suite integration + thin RTL editor↔perf-view cross-view propagation test). Names the blocking semantics (any future phase touching data flow ships behind harness-fidelity work OR carries explicit per-plan waiver under boundaries SCOPE LIMITS naming the v5.4 ticket). Owner: Rabbi Daniel. Target: this plan (v5h3-01-04 Task 2). Blocking? YES — gate is binding once codified.
       - **#2** — Open v5.4 milestone with first phase scoped to harness-fidelity remediation (emulator + RTL pair). Owner: Rabbi Daniel. Target: at v5.3 milestone close (post-band-onboarding UAT cycle). Blocking? GATES v5.4+ data-flow phases per Action #1.
       - **#3** — Daniel weekly worship cycle UAT against deployed fix (commits `1d8d94c` + `36e9fa1`) over 1-2 cycles. Validates the phantom reconciliation modal stops firing AND that no DIFFERENT save-loss class surfaces (instrumentation will Sentry-capture if any does). Owner: Rabbi Daniel. Target: opportunistic during regular weekly use. Blocking? Gates v5h3-01 phase close (treats phase as PENDING-UAT until Daniel signs off).
       - **#4** — If instrumentation captures a NEW save-loss class (H-SL-1, H-SL-8, or unidentified) during the UAT cycle, route to a follow-up plan in v5h3-01 phase per v51-04 rule. Owner: next `/paul:resume` session that processes the Sentry signal. Blocking? Conditional.
       - **#5** — Cross-reference the v5h-01 postmortem's Action #5 (codify 2-3-strikes architectural-rethink rule) — note that v5h3 did NOT exhibit a 2-3-strike pattern; the fix landed first try because v50-06-02 contract pre-existed and the fix was a substrate-level threading addition. v5h-01 Action #5 remains low priority. Owner: Rabbi Daniel. Blocking? No.

    8. **Deferred Items section:** Markdown table:
       - H-SL-1 (TextCell single-tap-to-edit blur/commit race) — was NOT this incident's cause (post-evidence weakened to LOW); STILL OPEN as a code-pattern concern; instrumentation now in production will catch it if it surfaces.
       - H-SL-8 (snapshot-listener delivery bumps local.updatedAt between edit-prep and edit-commit) — MEDIUM-HIGH confidence at investigation time; outbox-pending guard SHOULD prevent it but specific edge cases remain (listener-before-outbox-write ordering, cross-tab, initial delivery before first edit). NOT addressed by v5h3-01-03 fix; could surface under different rapid-edit + cross-tab scenarios. Routed to instrumentation watch + future plan if surfaced.
       - Snapshot-listener mounts in TWO places (editor + perf-view) — carried over from v5h-01 deferred items. LWW guards make redundant writes no-ops; network traffic doubled in rare both-views-open case. Low priority.

    9. **Appendix:** Markdown table with metric rows:
       - Affected user count: 1 (Daniel; band not yet in production)
       - Production data corruption (confirmed): 0 (modal preserved edits via "Keep mine"; user-visible UX symptom only — no lost edits)
       - Time-to-resolution: ~3h from morning UAT signal to deployed fix; ~5h end-to-end including instrumentation + postmortem
       - Plans shipped: 4 (v5h3-01-01 research, v5h3-01-02 instrumentation, v5h3-01-03 fix, v5h3-01-04 this postmortem)
       - Suite at close: 1560/1560 (was 1528 at v5h3-01 phase open)
       - LOC delta across the phase: ~+700 (instrumentation +313 source + tests; fix +28 source + 387 tests; doc only this plan)
       - Files modified across the phase: src/lib/sync/{engine.ts, sentry-capture.ts, init.ts, snapshot-listener.ts, edit-log.ts (new), edit-log-upload.ts (new)}, src/lib/local/{schema.ts, types.ts, write.ts}, src/components/setlist/grid/cells/{TextCell.tsx, DropdownCell.tsx}, plus test files
       - Final commits: `1d8d94c` (v5h3-01-02), `36e9fa1` (v5h3-01-03)
       - Hypothesis matrix at investigation time: 6 starting; 3 ruled out definitively (H-SL-2/3/4); 3 still open (H-SL-1/5/6) → after mid-execution evidence: H-SL-5 ruled out, H-SL-6 crystallized into H-SL-7 (HIGH) + H-SL-8 (MEDIUM-HIGH); fix targeted H-SL-7 only

    **Style guidelines (mirror v5h-01 postmortem):**
    - Each Lesson is its own subheading with a memorable lead phrase.
    - Use markdown tables for Timeline, What Got Shipped, Action Items, Deferred Items, Appendix.
    - Cross-link aggressively to v5h-01 postmortem + investigation doc + the 3 SUMMARYs.
    - Be specific about commit hashes, line numbers, file paths.
    - Avoid speculative content — only document what evidence supports.
    - Closing line at the bottom: "Postmortem closes phase v5h3-01 (4 of 4 plans). Phase status PENDING-UAT until Daniel weekly worship cycle confirms fix holds. Then `/paul:audit-milestone v5.3` after v53-02..04 ship to close v5.3 milestone."

    Avoid: writing the actual fix code in the postmortem (the code is in `36e9fa1`); duplicating the investigation doc verbatim (cross-link instead); making the postmortem a tutorial (assume reader has access to the cross-linked artifacts).

    Target: 150-250 lines markdown.
  </action>
  <verify>
    - `wc -l sheet-music-app/.paul/postmortems/v5h3-01-save-loss-recurrence.md` returns a number between 150 and 350 (target 150-250 with some slack)
    - `grep -c '^## ' sheet-music-app/.paul/postmortems/v5h3-01-save-loss-recurrence.md` returns >= 7 (TL;DR + Timeline + Root Cause + What Got Shipped + Lessons + Action Items + Deferred + Appendix)
    - `grep -c 'v5h-01-save-loss.md' sheet-music-app/.paul/postmortems/v5h3-01-save-loss-recurrence.md` >= 2 (cross-link present)
    - `grep -c 'v5h3-01-save-loss-recurrence-investigation.md' sheet-music-app/.paul/postmortems/v5h3-01-save-loss-recurrence.md` >= 1 (investigation cross-link)
    - `grep -c 'H-SL-7' sheet-music-app/.paul/postmortems/v5h3-01-save-loss-recurrence.md` >= 5 (H-SL-7 mechanism documented)
    - `grep -c '36e9fa1\|1d8d94c' sheet-music-app/.paul/postmortems/v5h3-01-save-loss-recurrence.md` >= 4 (both commits referenced multiple times)
    - `git diff sheet-music-app/.paul/postmortems/v5h-01-save-loss.md` empty (parent postmortem NOT modified)
  </verify>
  <done>AC-1 satisfied: postmortem exists, structured per template, cross-linked, parent postmortem untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Codify "Harness Fidelity Gate" in PROJECT.md Constraints section</name>
  <files>sheet-music-app/.paul/PROJECT.md</files>
  <action>
    Append a new subsection under `## Constraints` immediately AFTER the existing `### UAT Discipline (data-flow fixes)` subsection (lines ~181-184). Do NOT modify the existing UAT Discipline subsection or any other PROJECT.md section.

    **New subsection (insert after the UAT Discipline block, before `## Tech Stack`):**

    ```markdown
    ### Harness Fidelity Gate (binding from v5.3)

    The kitchen-sink fast-check harness (v50-07-04) uses in-memory zero-latency adapters with no security-rules layer and no perf-view-path coverage. It has missed two production data-flow bugs:
    - **v5.0-hotfix (2026-04-27)** — missing `firestore.rules` for `tracks/{id}` + `songs/{id}` collections; default-deny silently rejected every track write. Root cause invisible to harness (no rules layer). See `.paul/postmortems/v5h-01-save-loss.md` §Lessons.2.
    - **v5h3 (2026-05-02)** — engine writeback did not thread server `updatedAt` into pending outbox rows for the same doc; rapid same-doc edits raised phantom VersionMismatchError; v50-06-02 reconciliation modal opened in single-user context. Root cause invisible to harness (in-memory adapter never produced rapid-same-doc-edit-before-writeback-completes sequence). See `.paul/postmortems/v5h3-01-save-loss-recurrence.md` §Lessons.3.

    The remediation has been deferred since v5h-01-04 (2026-04-27) under "opportunistic" framing. Twice-implicated is no longer acceptable. The remediation work:
    - **Firebase Local Emulator Suite integration** in the property-failures harness — runs real rules + auth + cache-then-fresh delivery + transaction precondition timing in-process during a subset of property iterations. Addresses Gap A (rules layer) + Gap C (cache-vs-fresh races) from v5h-01 §Lessons.2.
    - **Thin RTL editor↔perf-view cross-view propagation test pair** — runs the editor cell-commit path AND `useSetlistPerformance` against the same in-memory Firestore + Dexie; asserts cross-view propagation invariant. Addresses Gap B (perf-view path divergence) from v5h-01 §Lessons.2.

    **Binding semantics (from v5.3 onward):** any plan whose `files_modified` touches sync engine (`src/lib/sync/`), Dexie schema/writes (`src/lib/local/schema.ts`, `src/lib/local/write.ts`), snapshot-listener (`src/lib/sync/snapshot-listener.ts`), lazy-hydration (`src/components/setlist/grid/SetlistGridHydrator.tsx`), perf-view rendering (`src/hooks/use-setlist-performance.ts`), editor cell-commit (`src/components/setlist/grid/cells/`), or Firestore rules (`firestore.rules`) MUST do one of:
    - (a) Land AFTER the harness-fidelity remediation has shipped (preferred path), OR
    - (b) Carry an explicit per-plan waiver under `<boundaries>` SCOPE LIMITS naming the open v5.4 harness-fidelity ticket and the specific reason this plan ships before it (e.g., "P0 production hotfix; cannot block on harness work; UAT closes the gap for this plan"). Waivers are tracked across plans; three waivers in a row triggers re-prioritization to v5.4 phase 1.

    **Owner:** Rabbi Daniel. **Target:** v5.4 phase 1 (first phase of v5.4 milestone, before any v5.4 data-flow phase). **Codified by:** v5h3-01-04 postmortem.
    ```

    Verify the new subsection inserts cleanly without breaking the existing markdown flow (`### UAT Discipline (data-flow fixes)` block remains intact above; `## Tech Stack` heading remains intact below).

    Avoid: modifying any other PROJECT.md section; rewriting the UAT Discipline subsection; changing line counts in the Validated/Active requirements list; touching the Tech Stack table.
  </action>
  <verify>
    - `grep -n '### Harness Fidelity Gate' sheet-music-app/.paul/PROJECT.md` returns exactly one match
    - `grep -c 'twice-implicated' sheet-music-app/.paul/PROJECT.md` >= 1
    - `grep -c 'binding\|Binding' sheet-music-app/.paul/PROJECT.md` >= 2 (gate is binding, semantics named)
    - `grep -c 'v5h3-01-save-loss-recurrence.md' sheet-music-app/.paul/PROJECT.md` >= 1 (cross-link to new postmortem)
    - `grep -c 'v5h-01-save-loss.md' sheet-music-app/.paul/PROJECT.md` >= 1 (cross-link to parent postmortem)
    - The lines ordering is preserved: `### UAT Discipline (data-flow fixes)` precedes `### Harness Fidelity Gate (binding from v5.3)` precedes `## Tech Stack` — verify with `grep -n '^###\|^## ' sheet-music-app/.paul/PROJECT.md`
    - `git diff sheet-music-app/.paul/PROJECT.md` shows ONLY additions (no deletions of UAT Discipline or any prior content)
  </verify>
  <done>AC-2 satisfied: Harness Fidelity Gate subsection added immediately after UAT Discipline; binding semantics + cross-references + owner/target named; no other PROJECT.md sections altered.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE

- `sheet-music-app/.paul/postmortems/v5h-01-save-loss.md` — existing parent postmortem is canonical for the v5h-01 incident; new postmortem cross-links, does NOT extend or modify
- `sheet-music-app/.paul/postmortems/v5h3-01-save-loss-recurrence-investigation.md` — preserved as research artifact; do NOT rewrite or merge into the new postmortem
- `sheet-music-app/.paul/phases/v5h3-01-save-loss-recurrence/v5h3-01-{01,02,03}-PLAN.md` and `-SUMMARY.md` — historical record; do NOT amend
- `sheet-music-app/src/**/*` — zero source code changes (all code work shipped in 02 + 03)
- `sheet-music-app/firestore.rules`, `sheet-music-app/firestore.indexes.json`, `sheet-music-app/storage.rules` — unchanged
- `sheet-music-app/.paul/PROJECT.md` UAT Discipline subsection (lines ~181-184) — do NOT modify; new Harness Fidelity Gate subsection is APPENDED after it
- `sheet-music-app/.paul/PROJECT.md` Tech Stack / Links / Key Decisions / Target Users / Context / Requirements sections — unchanged
- `sheet-music-app/.paul/ROADMAP.md` — unchanged this plan (v5.4 milestone entry will be added when /paul:new-milestone runs after v5.3 closes; not this plan's job)
- `sheet-music-app/.paul/STATE.md` — workflow updates STATE.md at plan-creation time per spec; do NOT pre-emptively edit it inside Tasks 1 or 2
- `sheet-music-app/package.json`, `sheet-music-app/src/build-info.json` — pre-existing dirty state from dev script (auto-memory note); do NOT stage on this plan's commit
- All tests, all .test.ts/.test.tsx files — zero test changes (no new harness infra in this plan; that's v5.4 phase 1)

## SCOPE LIMITS

- This plan does NOT build the harness-fidelity remediation (Firebase emulator + RTL pair). That work is named, owned, and scheduled for v5.4 phase 1. Building it here would expand v5h3 scope into v5.4 territory and conflict with the "v5.4 milestone is its own thing" framing.
- This plan does NOT add a v5.4 milestone entry to ROADMAP.md. /paul:new-milestone is the right entry point for that, and it runs at v5.3 close (post-band-onboarding UAT cycle), not now.
- This plan does NOT close phase v5h3-01 in the strict sense — phase remains PENDING-UAT per Action Item #3 (Daniel weekly worship cycle UAT). The 4-of-4 plans flag flips on this plan's UNIFY; phase status flips from "75% complete" to "100% PLANS COMPLETE — PENDING-UAT".
- This plan does NOT update v5h-01 postmortem to cross-link to v5h3 postmortem (one-way cross-link from child → parent only; future consolidation could revisit but not this plan).
- This plan does NOT address Issue 2 (iPad key-picker UI) deferred from v5h-01 — separate routing per v5h-01 deferred items table.
- This plan does NOT add Sentry breadcrumb assertions or test cases — instrumentation testing was scoped in v5h3-01-02 and is sufficient.

</boundaries>

<verification>

Before declaring plan complete:
- [ ] `npm test --prefix sheet-music-app` returns 1560/1560 passing (no regression — this plan adds zero source/test code)
- [ ] `npm run build --prefix sheet-music-app` exits clean (no markdown changes affect Next build, but verify per CLAUDE.md feedback memory)
- [ ] `git diff sheet-music-app/src/ sheet-music-app/firestore.rules sheet-music-app/firestore.indexes.json sheet-music-app/storage.rules` empty
- [ ] `git diff --stat sheet-music-app/.paul/` shows EXACTLY:
      - `sheet-music-app/.paul/PROJECT.md` modified (additions only)
      - `sheet-music-app/.paul/postmortems/v5h3-01-save-loss-recurrence.md` created
      - `sheet-music-app/.paul/phases/v5h3-01-save-loss-recurrence/v5h3-01-04-PLAN.md` created (this file)
      - `sheet-music-app/.paul/phases/v5h3-01-save-loss-recurrence/v5h3-01-04-SUMMARY.md` created (at UNIFY)
      - `sheet-music-app/.paul/STATE.md` modified (at PLAN + UNIFY)
- [ ] Postmortem lints OK as markdown (no stray code fences; tables render; cross-links resolve to existing files)
- [ ] PROJECT.md "## Constraints" section section ordering preserved: Technical Constraints → Business Constraints → UAT Discipline → Harness Fidelity Gate → (next `## Tech Stack` heading)
- [ ] All 4 acceptance criteria (AC-1..4) marked Pass in SUMMARY at UNIFY

</verification>

<success_criteria>

- Postmortem at `.paul/postmortems/v5h3-01-save-loss-recurrence.md` exists, 150-350 lines, structured per spec, cross-linked
- PROJECT.md gained `### Harness Fidelity Gate (binding from v5.3)` subsection with binding semantics + named work + named owner + named v5.4 target
- Phase v5h3-01 plan count: 4 of 4 (this plan UNIFY closes the count; phase enters PENDING-UAT state)
- Zero source/test/rules/indexes changes; full suite + build remain green
- Daniel-loop UAT discipline + Harness Fidelity Gate now BOTH live in PROJECT.md as binding constraints (paired complementary gates: UAT catches what harness misses; harness-fidelity raises the floor of what harness CAN catch)
- Commit explicitly stages `.paul/postmortems/v5h3-01-save-loss-recurrence.md` + `.paul/PROJECT.md` + `.paul/phases/v5h3-01-save-loss-recurrence/v5h3-01-04-*.md` (no orphaned PAUL artifacts; per `feedback_paul_phase_commits.md`)

</success_criteria>

<output>
After completion, create `.paul/phases/v5h3-01-save-loss-recurrence/v5h3-01-04-SUMMARY.md` capturing:
- The 4 ACs results (Pass / Fail / N/A)
- LOC + line counts for postmortem and PROJECT.md additions
- Final commit hash + push status to `origin master`
- Phase status update: 4 of 4 plans complete; phase enters PENDING-UAT pending Daniel weekly worship cycle confirmation
- Action Items #1 (codification — done in this plan), #2 (v5.4 phase 1 ticket — pending), #3 (UAT — pending) status
- Boundary diff confirmation (no source/rules/indexes touched)
</output>
