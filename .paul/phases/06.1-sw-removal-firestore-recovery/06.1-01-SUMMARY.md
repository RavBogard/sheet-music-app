---
phase: 06.1-sw-removal-firestore-recovery
plan: 01
subsystem: database
tags: [firestore, indexeddb, service-worker, error-recovery]

requires:
  - phase: none
    provides: N/A (standalone crash fix)
provides:
  - clearFirestoreIndexedDB() shared helper for IndexedDB corruption recovery
  - Automatic Firestore crash detection and self-healing via unhandledrejection + ErrorBoundary
  - Proactive IDB cleanup in SwCleanup after old SW removal
affects: [06.1 plan 02 (SW removal), any future Firestore persistence changes]

tech-stack:
  added: []
  patterns: [sessionStorage-gated auto-recovery to prevent infinite reload loops]

key-files:
  created: []
  modified:
    - src/lib/firebase.ts
    - src/components/offline/SwCleanup.tsx
    - src/components/error-boundary.tsx

key-decisions:
  - "SSR guard: typeof window checks for all browser APIs (indexedDB, sessionStorage, window.addEventListener)"
  - "Safari <17 fallback: known Firestore DB name pattern when indexedDB.databases() unavailable"
  - "Single recovery attempt per session via sessionStorage flag to prevent infinite reload loops"

patterns-established:
  - "Firestore IDB recovery: clearFirestoreIndexedDB() is the canonical way to clear corrupted persistence"

duration: ~10min
started: 2026-03-11T21:10:00Z
completed: 2026-03-11T21:20:00Z
---

# Phase 6.1 Plan 01: Firestore Crash Fix Summary

**Firestore IndexedDB corruption auto-recovery — clears corrupted persistence and reloads on INTERNAL ASSERTION FAILED errors**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~10min |
| Tasks | 3 completed |
| Files modified | 3 |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: Firestore recovers from corrupted IndexedDB | Pass | unhandledrejection listener + ErrorBoundary both detect assertion errors, clear IDB, reload (once per session) |
| AC-2: SwCleanup proactively clears stale Firestore IndexedDB | Pass | After unregistering old PWA SWs, deletes all Firestore IDB databases; Firebase Messaging SW untouched |
| AC-3: Build and type checks pass | Pass | tsc --noEmit: zero errors, next build: success, vitest: 877/877 passing |

## Accomplishments

- Added `clearFirestoreIndexedDB()` shared helper to firebase.ts — enumerates and deletes Firestore IDB databases with Safari fallback
- Added `unhandledrejection` listener in firebase.ts that auto-recovers from Firestore assertion failures (one attempt per session)
- Enhanced SwCleanup to proactively clear Firestore IDB after unregistering old PWA service workers
- Added Firestore crash detection to ErrorBoundary's `componentDidCatch` for synchronous assertion errors

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/firebase.ts` | Modified | Added clearFirestoreIndexedDB() export + unhandledrejection auto-recovery listener |
| `src/components/offline/SwCleanup.tsx` | Modified | Proactive IDB cleanup after SW unregistration |
| `src/components/error-boundary.tsx` | Modified | Firestore assertion error detection + auto-recovery in componentDidCatch |

## Decisions Made

None — followed plan as specified.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## Next Phase Readiness

**Ready:**
- Firestore crash fix deployed — users with corrupted IDB will auto-recover
- Plan 02 (SW code removal) can proceed — crash fix is independent of code cleanup

**Concerns:**
- None

**Blockers:**
- None

---
*Phase: 06.1-sw-removal-firestore-recovery, Plan: 01*
*Completed: 2026-03-11*
