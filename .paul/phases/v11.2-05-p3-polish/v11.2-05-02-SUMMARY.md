---
phase: v11.2-05-p3-polish
plan: 02
subsystem: api
tags: [mcp, serialization, firestore, timestamp, jsonResult, boundary]

requires:
  - phase: v11.2-05-01
    provides: same phase, sibling plan (no code dependency)
provides:
  - serializeTimestamps() boundary normalizer — every MCP tool response renders timestamps ISO
affects: [any current/future MCP tool that echoes a Firestore re-read]

tech-stack:
  added: []
  patterns:
    - "Single-boundary response normalization in jsonResult (one chokepoint, not per-tool)"

key-files:
  created:
    - src/lib/mcp/serialize-timestamps.ts
    - src/lib/mcp/__tests__/serialize-timestamps.test.ts
  modified:
    - src/lib/mcp/tools/index.ts

key-decisions:
  - "Single serializeTimestamps() pass in jsonResult (ROADMAP-chosen) over per-tool patching — fixes add_track + any latent sibling uniformly"
  - "Pure + firebase-admin-free (duck-type the Timestamp) to avoid dragging admin into a bundle"
  - "Strict two-key guard ({_seconds,_nanoseconds} or {seconds,nanoseconds}) so a domain object with a `seconds` field isn't misread"

patterns-established:
  - "serializeTimestamps is the canonical MCP timestamp-encoding gate"

duration: ~35min
started: 2026-06-11T13:40:00Z
completed: 2026-06-11T13:50:00Z
---

# Phase v11.2-05 Plan 02: BUG-8 Summary

**Every MCP tool response now renders Firestore timestamps as ISO-8601 strings via a single `serializeTimestamps()` pass in `jsonResult` — no more raw `{_seconds,_nanoseconds}` (add_track_to_setlist.updatedAt).**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~35 min |
| Started | 2026-06-11T13:40:00Z |
| Completed | 2026-06-11T13:50:00Z |
| Tasks | 1 completed (E/Q PASS) |
| Files modified | 3 (2 new) |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: raw Firestore Timestamps → ISO at the boundary | Pass | unit "AC-1 add_track echo" + jsonResult wire; no `_seconds` survives stringify |
| AC-2: serializeTimestamps correct + non-destructive | Pass | 10 cases: admin Timestamp, raw/client shapes, nesting+arrays, passthrough, idempotent |
| AC-3: isError + existing shapes preserved | Pass | isError computed from un-mutated envelope; non-timestamp fields untouched; tsc + build clean |

## Verification Results

- `npx tsc --noEmit` → clean (exit 0)
- `npx vitest run src/lib/mcp/__tests__/serialize-timestamps.test.ts` → **10/10**
- `SKIP_ENV_VALIDATION=1 npx next build` → clean (exit 0)

## Accomplishments

- Closed the last v11.2 stress-test finding: MCP timestamp serialization is now uniform (ISO) across every tool, at one chokepoint.
- Fix is idempotent over the already-ISO paths (update_track via normalizeTrackEcho) and over re-runs.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/mcp/serialize-timestamps.ts` | Created | Pure deep timestamp→ISO normalizer (firebase-admin-free) |
| `src/lib/mcp/__tests__/serialize-timestamps.test.ts` | Created | 10 unit cases (conversion + passthrough + AC-1 + idempotency) |
| `src/lib/mcp/tools/index.ts` | Modified | jsonResult applies serializeTimestamps before JSON.stringify |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Single boundary pass over per-tool fix | One chokepoint; covers add_track + latent siblings; ROADMAP-chosen | No per-tool whack-a-mole |
| firebase-admin-free duck-typing | Keep the helper bundle-safe | No admin import risk |
| Strict two-key guard for the serialized shape | Avoid misreading domain objects with a `seconds` field | Non-destructive |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 0 | — |

**Total impact:** Plan executed exactly as written.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| Test used `Timestamp.toJSON()` which the installed admin SDK Timestamp lacks | Built the raw `{_seconds,_nanoseconds}` from `.seconds`/`.nanoseconds` getters instead |

## Next Phase Readiness

**Ready:**
- Phase v11.2-05 complete (BUG-6 + BUG-7 + BUG-8 all shipped) → milestone v11.2 is 5/5. Next: `/paul:complete-milestone` (tag v11.2.0, full PROJECT.md writeup).

**Concerns / Blockers:**
- None.

---
*Phase: v11.2-05-p3-polish, Plan: 02*
*Completed: 2026-06-11*
