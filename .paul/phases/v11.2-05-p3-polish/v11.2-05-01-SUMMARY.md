---
phase: v11.2-05-p3-polish
plan: 01
subsystem: ui
tags: [branding, multi-tenant, org-resolution, perform, chord-chart, text-viewer, broslaz]

requires:
  - phase: v11.1-01
    provides: host-resolved nav branding (coerceOrgId(x-org-id) → getOrgBranding, OrgLogo) — the pattern this plan mirrors into the dashboard hero
provides:
  - host-correct (main) dashboard hero brand (no CRC leak on broslaz)
  - word-atomic wrap-mode rendering in TextScoreViewer (text chord charts)
affects: [v11.2-05-02 (BUG-8, same phase), any future dashboard-hero or text-chart work]

tech-stack:
  added: []
  patterns:
    - "Dashboard server components resolve host org via coerceOrgId(x-org-id) → getOrgBranding, same seam as the (main) layout"
    - "Chord/lyric chunks grouped word-atomic for flex-wrap; break only at lyric whitespace boundaries"

key-files:
  modified:
    - src/app/(main)/page.tsx
    - src/app/(main)/DashboardClient.tsx
    - src/components/music/TextScoreViewer.tsx
    - src/components/music/__tests__/text-score-viewer.test.tsx

key-decisions:
  - "BUG-6 fix mirrors the v11.1-01 nav pattern (host branding) into the dashboard hero + page server component"
  - "Keep the existing <img border-brand/20> for logo-present (CRC byte-identical); OrgLogo monogram only when logoUrl is empty (broslaz)"
  - "Also fixed the sibling OnboardingCard brand leak (same leaky source) since the host value was in hand"
  - "BUG-7: fit-mode render left byte-identical; word-grouping applied only in wrap mode where the fragmentation occurs"

patterns-established:
  - "groupChunksIntoWords: non-breaking word units for chord-over-lyric wrap rendering"

duration: ~50min
started: 2026-06-11T13:00:00Z
completed: 2026-06-11T13:35:00Z
---

# Phase v11.2-05 Plan 01: BUG-6 + BUG-7 Summary

**The authed dashboard hero now shows the host org's brand (no "CRC MUSIC" on broslaz), and text chord charts keep every lyric word intact in wrap mode.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~50 min |
| Started | 2026-06-11T13:00:00Z |
| Completed | 2026-06-11T13:35:00Z |
| Tasks | 2 completed (both E/Q PASS) |
| Files modified | 4 |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: dashboard hero brand host-correct (BUG-6) | Pass | page.tsx resolves host org; hero server-first → BL on broslaz, CRC byte-identical |
| AC-2: no hardcoded CRC brand in hero (BUG-6) | Pass | grep-clean: no `/logo.jpg` literal nor bare `congregation.shortName` in hero block (only comments + greeting effect) |
| AC-3: lyric words stay intact (BUG-7) | Pass | wrap-mode chunks grouped into non-breaking word units; fit mode unchanged |
| AC-4: chord rendering regression-locked (BUG-7) | Pass | new word-integrity test asserts the word lives in one `.inline-flex` group |

## Verification Results

- `npx tsc --noEmit` → clean (exit 0)
- `npx vitest run src/components/music/__tests__/text-score-viewer.test.tsx` → **9/9** (8 prior + 1 new BUG-7 case)
- `SKIP_ENV_VALIDATION=1 npx next build` → clean (exit 0; frontend bundle-boundary guard)
- grep: no `/logo.jpg` literal nor bare `{congregation.shortName}` in the dashboard hero block
- Skill audit: `/ui-ux-pro-max` loaded this session ✓ (verdict: BUG-6 is a brand-source swap, BUG-7 a wrap-grouping fix; no visual-design regression — CRC byte-identical, BL monogram matches the nav)

## Accomplishments

- Closed the last consumer-visible CRC brand leak on the broslaz authed dashboard (hero + onboarding), reusing the proven v11.1-01 host-branding seam.
- Text chord charts no longer fragment a lyric word across wrapped lines on iPads — readability fix for the band's Perform surface.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/app/(main)/page.tsx` | Modified | Resolve host org (coerceOrgId(x-org-id)→getOrgBranding); pass serverShortName/serverLogoUrl; getServerCongregationConfig(orgId) |
| `src/app/(main)/DashboardClient.tsx` | Modified | Hero (+ OnboardingCard) host-resolve brand server-first via OrgLogo + heroShortName |
| `src/components/music/TextScoreViewer.tsx` | Modified | groupChunksIntoWords; wrap mode renders non-breaking word groups; fit mode byte-identical |
| `src/components/music/__tests__/text-score-viewer.test.tsx` | Modified | New wrap-mode word-integrity test (BUG-7) |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Mirror v11.1-01 nav pattern into the dashboard hero | Single proven host-branding seam; consistency | No new abstraction; low risk |
| Keep `<img border-brand/20>` for CRC, OrgLogo monogram for empty logoUrl | CRC byte-identical (OrgLogo uses border-border) | Constraint 2 honored |
| Fix OnboardingCard brand prop too | Same leaky source; host value already computed | Closes a sibling leak (minor scope addition) |
| Word-group only in wrap mode | Fragmentation only manifests under flex-wrap; fit mode already aligned | Fit render unchanged |

## Deviations from Plan

### Summary

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 1 | OnboardingCard brand prop (same bug class, one token) |
| Deferred | 0 | — |

**Total impact:** Plan executed as written; the one addition (OnboardingCard) closes a confirmed sibling leak with a trivial, in-scope change.

### Deferred Items

None.

## Issues Encountered

None. (Pre-existing a11y stderr warnings on the TextScoreViewer control-bar icon buttons are unrelated — cycle35 P2-001, out of scope.)

## Next Phase Readiness

**Ready:**
- v11.2-05-02 (BUG-8 — MCP timestamp serialization) is the phase's last plan; on its close → transition + `/paul:complete-milestone` (tag v11.2.0).

**Concerns:**
- None.

**Blockers:**
- None.

---
*Phase: v11.2-05-p3-polish, Plan: 01*
*Completed: 2026-06-11*
