---
phase: v11.5-02-performance-surface
plan: 02
subsystem: ui
tags: [perform, ttfb, web-vitals, verify-first, tonight-set, public-listing, ssr, F1, H7]

requires:
  - phase: v11.3-04
    provides: the /perform Suspense-streaming first-byte path + the web-vitals RUM sink/probe
  - phase: cycle-12
    provides: selectVisiblePublicSetlists SSR wire-slice + F-C12-001 leak-guard test
provides:
  - H7 verdict — /perform cold TTFB re-verified INFRA-BOUND (Vercel follow-up; no app churn)
  - F1 — a one-tap "next service" CTA on the public /perform index + pure firstUpcomingSetlist() helper
  - restored F-C12-001 SSR wire-byte leak-guard test (was pre-broken by the v11.3-04-03 streaming refactor)
affects: [v11.5-02-03 (F2 in-Perform key change), any future /perform-index work]

tech-stack:
  added: []
  patterns:
    - "firstUpcomingSetlist(setlists, now): pure 'next service' selector reusing splitPublicSetlists"
    - "Testing a Suspense-streamed RSC page: reach the inner async child via element.props.children.type()"

key-files:
  created:
    - .paul/research/v11-5-02-02-ttfb-verify-2026-06-14.md
  modified:
    - src/components/performance/public-setlist-order.ts
    - src/components/performance/__tests__/public-setlist-order.test.ts
    - src/components/performance/PublicSetlistListing.tsx
    - src/app/perform/__tests__/page.test.tsx

key-decisions:
  - "H7 is verify-first: probe confirmed TTFB flat/infra-bound (query already off first-byte path) → Vercel follow-up, ZERO /perform app churn"
  - "F1 eyebrow is 'Next service' (accurate when the soonest set is days out), not a literal 'Tonight' — err on clarity"
  - "Hero derives from the in-scope upcoming[0] on the already-org-scoped data → SSR first paint, no CLS, no new fetch, no cross-tenant leak"
  - "Fixed the pre-broken F-C12-001 test (test-only; page.tsx untouched) since F1 lands on this surface + AC-3 depends on the guard"

patterns-established:
  - "Pure 'next service' selector + a hero that reuses it; no duplicate date logic"

duration: ~40min
started: 2026-06-14T17:30:00Z
completed: 2026-06-14T17:46:00Z
---

# Phase v11.5-02 Plan 02: H7 TTFB verify + F1 "next service" entry Summary

**H7 (verify-first): re-ran the web-vitals slice probe — `/perform` cold TTFB is ~1823–1990ms p75 (flat vs the ~1633ms baseline, NOT improved); the Firestore query is already off the first-byte path (Suspense streaming, v11.3-04-03), so the residual is serverless cold-start + geo → logged a Vercel infra follow-up with ZERO `/perform` app churn. F1 (build): added a one-tap "next service" CTA hero to the PUBLIC `/perform` index (the band's iPad surface, previously list-only), driven by a new pure `firstUpcomingSetlist()` helper over the already-org-scoped data, rendered in the SSR first paint (no CLS). Also restored the F-C12-001 SSR leak-guard test that the v11.3-04-03 streaming refactor had silently left red for ~4 days.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~40 min |
| Tasks | 3 completed |
| Files created | 1 |
| Files modified | 4 |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: H7 TTFB re-measured + verdict, no blind churn | **Pass** | Probe (1232 obs): cold TTFB 1823–1990ms p75 = flat/infra-bound. Verdict + Vercel follow-up in findings note + STATE Deferred. `git diff` shows NO /perform app-code change. |
| AC-2: public /perform shows a "next service" entry that opens it | **Pass** | When `upcoming.length>0`, a primary CTA hero names `upcoming[0]` + its date and links to `/perform/setlist/{id}`. `firstUpcomingSetlist` unit-proven (soonest wins). |
| AC-3: fallback + tenancy + SSR/CLS contract intact | **Pass** | No upcoming → no hero (helper returns null; gated on `upcoming.length>0`). Uses the org-scoped `setlists` already in the component. SSR wire-byte/5-cap/isTest-filter contract green (page.test.tsx 6/6). Hero in SSR first paint → no post-hydration shift. |

## Verification Results

- `SKIP_ENV_VALIDATION=1 npx tsc --noEmit` → adds **0** errors (lone pre-existing `chord-cache/route.test.ts` error unchanged, unrelated).
- `SKIP_ENV_VALIDATION=1 npx vitest run public-setlist-order.test.ts perform/__tests__/page.test.tsx` → **16/16** (3 new `firstUpcomingSetlist` + the 6 restored SSR-regression).
- `SKIP_ENV_VALIDATION=1 npx next build --webpack` → **exit 0**; `/perform` compiled.
- /ui-ux-pro-max invoked before building the F1 hero (touch-target/focus-ring/no-shift guidance applied).
- grep: PublicSetlistListing renders a `Link` to `/perform/setlist/${next.id}` for `upcoming[0]`, gated on `upcoming.length>0`.

## Accomplishments

- Closed H7 the right way — measured, found it infra-bound, and did NOT churn an already-optimal route.
- The band's iPad Perform surface now offers a one-tap jump to the soonest service instead of list-scanning.
- Recovered a real public-surface leak-guard (test-fixture / member-email / 5-cap) that had been silently red.

## Task Commits

| Task | Type | Description |
|------|------|-------------|
| Task 1: H7 verify-first probe + verdict | docs | Findings note + STATE Deferred (Vercel follow-up); no app code |
| Task 2: F1 "next service" CTA hero + firstUpcomingSetlist helper | feat | public-setlist-order.ts + PublicSetlistListing.tsx |
| Task 3: unit tests + restore SSR leak-guard | test | public-setlist-order.test.ts (+3) + page.test.tsx harness fix |

Committed as a single `feat(v11.5-02-02)` plan commit (code + tests + findings + SUMMARY + STATE).

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| H7 → infra-bound, no app churn | Query already off first-byte path; TTFB flat → cold-start/geo, per standing guidance | Protects an optimal route; Vercel follow-up owns the lever |
| Eyebrow "Next service" not "Tonight" | Soonest upcoming may be days out; accurate-over-cute | Bulletproof copy in all cases |
| Hero from in-scope upcoming[0], SSR-painted | Reuse org-scoped data + existing logic; no CLS/leak | Minimal, contract-safe |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 1 | Restored the pre-broken F-C12-001 SSR leak-guard test (test-only; page.tsx untouched) |
| Scope additions | 0 | — |
| Deferred | 1 | `/perform` cold·MOBILE CLS 0.250 (n=8) — a separate CLS axis, flagged in the H7 note for a future look |

**Auto-fix detail — F-C12-001 SSR wire-byte test was pre-broken:**
- **Found during:** Task 3 verify (`page.test.tsx` 6/6 red even with my src changes stashed → confirmed pre-existing on clean HEAD `6f72ca42af`).
- **Cause:** the v11.3-04-03 streaming refactor moved the fetch+filter from `PerformPage` into the inner async `PerformListing` (Suspense child), so `initialSetlists` is no longer a top-level prop of `PerformPage`; the test still read `PerformPage().props.initialSetlists` → `undefined`. **The guard itself is intact** — `page.tsx:92` still calls `selectVisiblePublicSetlists(raw)` at the SSR boundary (no real leak).
- **Fix:** test-only — a `renderInitialSetlists()` helper that reaches the streamed child via `element.props.children.type()` and invokes it. `page.tsx` unchanged (boundary respected).
- **Verification:** page.test.tsx 6/6 green.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| page.test.tsx 6/6 failing | Diagnosed pre-existing (stash-and-recheck on clean HEAD) → stale harness from streaming refactor → fixed test to invoke the Suspense child; guard confirmed intact via page.tsx:92 |

## Next Phase Readiness

**Ready:** Next plan = **v11.5-02-03 (F2)** — in-Perform leader-only shared key change (/ui-ux-pro-max + UAT). After: 04 = H1 (landscape auto-fit + per-chart calibration).

**Concerns:** None for F1/H7. Two flagged-not-actioned items: the `/perform` cold·mobile CLS 0.250 (future CLS look) and the standing Vercel TTFB infra follow-up (Daniel/infra, not app code).

**Blockers:** None.

---
*Phase: v11.5-02-performance-surface, Plan: 02*
*Completed: 2026-06-14*
