---
phase: v11.5-02-performance-surface
plan: 04
subsystem: ui
tags: [perform, zoom, calibration, zustand, localStorage, ipad, pdfviewer, osmd]
requires:
  - phase: v11.5-02-01..03
    provides: the performance surface (seekable audio, next-service CTA, live-director); H1 is the last gap
provides:
  - per-device persisted per-chart zoom (chartZoom map in useMusicStore, keyed by fileId)
  - tappable Fit reset on the PerformanceToolbar zoom-% readout (setZoom(1) → auto-fit baseline)
tech-stack:
  added: []
  patterns:
    - "per-chart store state restored on queue transitions, mirroring track.transposition"
    - "viewing prefs persist per-device (localStorage), NOT shared via Firestore — distinct from live-director writes"
key-files:
  created:
    - src/lib/__tests__/chart-zoom-calibration.test.ts
  modified:
    - src/lib/store.ts
    - src/components/performance/PerformanceToolbar.tsx
    - src/components/performance/__tests__/performance-toolbar.test.tsx
key-decisions:
  - "Per-device (localStorage) calibration, NOT shared Firestore — Daniel ratified 2026-06-14 (zoom is a per-eyesight/screen viewing pref; must not override another musician's view)"
  - "No viewer changes: PDFViewer already fits-to-width + reflows on rotate; restoring the right per-chart zoom is sufficient"
patterns-established:
  - "chartZoom[fileId] restored in setQueue/nextSong/prevSong/jumpToSong alongside transposition; written through by setZoom; persisted via partialize"
duration: ~35min
started: 2026-06-14T21:00:00Z
completed: 2026-06-14T21:35:00Z
---

# Phase v11.5-02 Plan 04: H1 — landscape auto-fit + persisted per-chart calibration

**Per-device per-chart zoom now sticks (keyed by `fileId`, restored on every chart open via the store, persisted to localStorage) and the zoom-% readout doubles as a one-tap Fit reset — closing the last gap of the performance surface. No Firestore, no viewer changes.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~35 min |
| Tasks | 3 completed (all PASS on first qualify) |
| Files modified | 3 (+1 created) |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: Per-chart zoom remembered across navigation | Pass | `chart-zoom-calibration.test.ts` — A=1.4/B=1 isolation; next/prev/jump restore |
| AC-2: Per-chart zoom survives reload (per-device) | Pass | localStorage `music-storage.state.chartZoom` asserted present; global `zoom` key asserted absent (no Firestore write) |
| AC-3: Fit reset returns chart to auto-fit baseline | Pass | store: setZoom(1) → chartZoom[fileId]=1; toolbar: Fit button calls setZoom(1) |
| AC-4: Orientation change preserves calibration | Pass (by construction) | viewers read store `zoom` × their fit/width baseline; multiplier survives rotate; covered by UAT for live confirmation |

## Verification Results

- `vitest run` (calibration + toolbar suites): **22 passed** (5 new + 17 toolbar incl. new Fit test).
- `tsc --noEmit`: clean on all changed files (sole error is the pre-existing, unrelated `chord-cache/route.test.ts`).
- `SKIP_ENV_VALIDATION=1 npx next build --webpack`: **exit 0** (client-bundle boundary safe).
- Skill audit: `/ui-ux-pro-max` invoked before the toolbar edit ✓ (required by SPECIAL-FLOWS).

## Accomplishments

- Added `chartZoom: Record<fileId, number>` to `useMusicStore`: written through by `setZoom`, restored per chart on every queue transition (mirrors `transposition`), persisted via `partialize` (replacing the session-global `zoom` key).
- Turned the toolbar zoom-% readout into an accessible ≥44px Fit button (aria-label, ghost hover, cursor-pointer, no layout shift) that resets the active chart to its auto-fit baseline.
- Verified against code that PDF + MusicXML already fit-to-width and reflow on rotate → no viewer churn needed; the deliverable is purely per-chart persistence + the Fit affordance.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/store.ts` | Modified | `chartZoom` map + write-through `setZoom` + per-chart restore in setQueue/nextSong/prevSong/jumpToSong + partialize (drop global `zoom`) + reset |
| `src/components/performance/PerformanceToolbar.tsx` | Modified | zoom-% readout → tappable Fit reset (setZoom(1)), a11y + 44px target |
| `src/lib/__tests__/chart-zoom-calibration.test.ts` | Created | 5 store unit tests (AC-1/2/3 + scope-limit guard) |
| `src/components/performance/__tests__/performance-toolbar.test.tsx` | Modified | +1 Fit-control test (AC-3) |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Per-device (localStorage), not shared Firestore | Zoom is a per-eyesight/screen-size viewing preference; sharing would force one leader's zoom onto heterogeneous iPads | No write seam / rules / follower-propagation; simpler + bulletproof. Diverges from the recon's Option B (mirror transposition); Daniel ratified per-device 2026-06-14 |
| No viewer (PDF/OSMD) changes | Verified PDFViewer already fits-to-width + reflows on rotate; OSMD already fitBase×zoom | "Landscape auto-fit" requirement already met; H1 reduces to per-chart persistence + Fit reset |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 2 | Image-chart calibration (native pinch, doesn't read store.zoom) + standalone `/perform/[fileId]` route (uses setFile, no queue) — both documented as SCOPE LIMITS, not regressions |

**Total impact:** Executed as written; the per-device-vs-shared fork was settled with Daniel before APPLY.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| None | — |

## Next Phase Readiness

**Ready:** Phase v11.5-02 (performance surface) is complete (4/4). Next phase v11.5-03 (photo-of-paper-chart import).
**Concerns:** None.
**Blockers:** None.
**UAT (non-blocking, appended to UAT-PENDING):** 7-tablet iPad fleet — per-chart persist across nav/reload, rotate preservation, two-device per-device proof, PDF+MusicXML honor / image unaffected, Fit reset.

---
*Phase: v11.5-02-performance-surface, Plan: 04*
*Completed: 2026-06-14*
