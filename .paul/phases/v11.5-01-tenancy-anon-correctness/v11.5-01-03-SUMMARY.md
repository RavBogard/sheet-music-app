---
phase: v11.5-01-tenancy-anon-correctness
plan: 03
subsystem: api
tags: [mcp, authz, tenancy, band_leader, library, org-scoping, firestore-emulator]

requires:
  - phase: v11.4-04
    provides: band_leaders ratified as the authoring tier (membership = everyone, default-both)
  - phase: v11-02-03
    provides: the update_song org-scoping pattern (orgId + rowOrg + tenantMismatch→not-found)
provides:
  - band_leader in-place library-metadata editing (tags/title/key/bpm/leadMusician), org-scoped
  - the cross-tenant write wall on editEnrichment (previously absent on this path)
affects: [v11.5-04 (library hygiene — same edit/curation surface)]

tech-stack:
  added: []
  patterns:
    - "Editor-tier gate variant (assertLibraryEditor) returning the caller role for field-level + org-level authz"
    - "Reused update_song's rowOrg/orgId tenancy wall on a second MCP write path"

key-files:
  created:
    - src/lib/mcp/__tests__/edit-library-entry-authz.emulator.test.ts
  modified:
    - src/lib/mcp/tools/library-review.ts
    - src/lib/mcp/tools/index.ts

key-decisions:
  - "Band_leaders scoped to connector org (orgFrom); admin left byte-identical (unscoped) — preserves cross-org curation"
  - "collection stays admin-only (forbidden_field) even for band_leaders; curation-authority decision"
  - "Cross-tenant edit returns row_not_found (never reveals existence), reusing update_song's pattern"

patterns-established:
  - "MCP write paths that lacked tenancy checks adopt the rowOrg(orgId)!==callerOrg → not-found wall"

duration: ~20min
started: 2026-06-12T19:03:00Z
completed: 2026-06-12T19:29:00Z
---

# Phase v11.5-01 Plan 03: band_leader library-edit lockout (H9) Summary

**Relaxed `editEnrichment` (the `edit_library_entry`/`edit_enrichment` MCP tools) from admin-only to admin-OR-band_leader for the curation-safe field subset, AND added the cross-tenant write wall the path never had — so a band_leader can re-tag a chart in place (bonds preserved) but only within their own tenant, `collection` stays admin-only, and admin behavior is byte-identical.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~20 min |
| Started | 2026-06-12T19:03:00Z |
| Completed | 2026-06-12T19:29:00Z |
| Tasks | 3 completed |
| Files modified | 3 (1 created, 2 modified) |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: band_leader edits tags in-org, in place | **Pass** | Emulator: crc band_leader sets li-crc tags → ok, doc.tags replaced, row id unchanged (bond anchor intact) |
| AC-2: cross-tenant edit denied as not-found | **Pass** | Emulator: crc band_leader on li-bl (orgId BL) → `row_not_found`, li-bl unchanged |
| AC-3: collection stays admin-only | **Pass** | Emulator: band_leader `{collection}` → `forbidden_field` (no write); admin same edit → ok |
| AC-4: admin byte-identical | **Pass** | Emulator: admin edits li-bl from crc context (wall skipped) → ok; admin sets collection → ok |

## Verification Results

- `SKIP_ENV_VALIDATION=1 npx tsc --noEmit` → **exit 0**.
- emulator authz test (`firebase emulators:exec … vitest … edit-library-entry-authz.emulator.test.ts`) → **5/5 passed** (AC-1/2/3/4 + legacy-no-orgId resolution).
- `SKIP_ENV_VALIDATION=1 npx next build --webpack` → **clean** (server-module change; no client/server bundle-boundary break).
- grep: `editEnrichment` uses `assertLibraryEditor` (not `assertAdmin`); the other five review-queue tools still `assertAdmin`; both handlers pass `orgFrom(extra)`; descriptions no longer say "Admin-only".

## Accomplishments

- Made the authoring tier (band_leaders) functional for routine library curation — David's field-reported blocker is structurally fixed (no more delete-and-re-import to change a tag).
- Closed a latent tenancy gap: `editEnrichment` previously had ZERO org check (admin-trusted); it now enforces the cross-tenant wall for non-admin callers.
- Reused the proven `update_song` tenancy pattern + the existing `assertEditor`-style tier on a second write path — no new abstractions.

## Task Commits

| Task | Commit | Type | Description |
|------|--------|------|-------------|
| Task 1: editor-tier gate + collection guard | `d7cbb1a4e0` | fix | assertLibraryEditor + forbidden_field; handlers pass orgFrom(extra); descriptions |
| Task 2: cross-tenant org wall | `d7cbb1a4e0` | fix | orgWall (rowOrg(orgId)!==org → row_not_found) in dryRun+commit; admin unscoped |
| Task 3: emulator authz test | `d7cbb1a4e0` | test | 5 emulator cases (committed atomically with the fix) |

Plan metadata + STATE/paul.json/UAT: separate `docs(v11.5-01)` commit.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/mcp/tools/library-review.ts` | Modified | `assertLibraryEditor` gate + collection guard + cross-tenant orgWall in `editEnrichment` |
| `src/lib/mcp/tools/index.ts` | Modified | Thread `orgFrom(extra)` into both handlers; corrected tool descriptions |
| `src/lib/mcp/__tests__/edit-library-entry-authz.emulator.test.ts` | Created | Emulator-backed authz regression (all 4 ACs + legacy) |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Scope band_leaders to connector org; admin unscoped | Preserve Daniel's cross-org curation; wall the new band_leader access | Admin path byte-identical (AC-4); David walled to his tenant |
| `collection` admin-only via forbidden_field | Collection placement is a curation-authority decision | Band_leaders edit content fields, not library structure |
| Cross-tenant → `row_not_found` (not a distinct code) | Never reveal a cross-tenant row exists | Matches `update_song`'s tenantMismatch behavior |

## Deviations from Plan

### Summary

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 0 | — |

**Total impact:** None — plan executed exactly as written.

### Deferred Items

None.

## Issues Encountered

None. (Discovered during exec that `runWrite`'s `validate()` callback runs on BOTH dryRun and real-run, so the orgWall in that callback already covers the commit path; the commit-callback wall is kept as harmless defense-in-depth.)

## Next Phase Readiness

**Ready:**
- H9 shipped; band_leaders can self-serve curation. Phase v11.5-01 still has H5 (Plan 02) unplanned.
- **Operational follow-up (NOT code):** revert David from admin → band_leader now that the proper path is live (once live-UAT confirms). Reminder carried in STATE + UAT-PENDING.

**Concerns:**
- None. No rules change (admin SDK bypasses rules); admin behavior unchanged.

**Blockers:**
- None.

---
*Phase: v11.5-01-tenancy-anon-correctness, Plan: 03*
*Completed: 2026-06-12*
