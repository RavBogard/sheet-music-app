---
phase: v11.5-01-tenancy-anon-correctness
plan: 02
subsystem: api
tags: [anon, chord-cache, transpose, auth, rate-limit, err-public, D-Q2]

requires:
  - phase: v11.3-01
    provides: the anon GET/POST stance on /api/library/chord-cache (requireAuth:false + api rate-limit)
provides:
  - anon persistence of the auto-detected native key (kills the transpose PATCH 401 + recompute loop)
  - a field-level authz pattern on a requireAuth:false route (trust fields gated via ctx.auth)
affects: [v11.5-02 (the performance/transpose surface)]

tech-stack:
  added: []
  patterns:
    - "Field-level authz on a requireAuth:false route: ctx.auth (AuthResult|null) gates the trust fields"

key-files:
  created: []
  modified:
    - src/app/api/library/chord-cache/route.ts
    - src/app/api/library/chord-cache/route.test.ts

key-decisions:
  - "Anon may persist nativeKey(forced 'auto') + lastUsed*; chordsVerified + manual provenance stay authed-musician"
  - "Add the api rate-limit the PATCH lacked (shared bucket — no double-punish per D-Q2)"
  - "No org-scoping here (matches the existing anon POST precedent) — logged as a Deferred follow-up for both writes"

patterns-established:
  - "requireAuth:false + per-field guard on ctx.auth?.isMusician for mixed anon/authed write routes"

duration: ~25min
started: 2026-06-12T20:03:00Z
completed: 2026-06-12T20:47:00Z
---

# Phase v11.5-01 Plan 02: anon chord-cache write 401 (H5) Summary

**`PATCH /api/library/chord-cache` was `role:'musician'`, so the transposer's `saveNativeKey(…, 'auto')` — which fires for every viewer — 401'd for anon, spewing console noise and never caching the detected key (every anon visitor re-detected). Opened the PATCH to anon for the benign derived/display fields (nativeKey forced `'auto'`, lastUsed*) with the `chordsVerified` trust flag + manual provenance still authed-musician, and added the missing `api` rate-limit.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~25 min |
| Started | 2026-06-12T20:03:00Z |
| Completed | 2026-06-12T20:47:00Z |
| Tasks | 2 completed |
| Files modified | 2 |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: anon persists auto nativeKey | **Pass** | Test: anon PATCH `{nativeKey:'G',nativeKeySource:'auto'}` → 200, written with source 'auto'; no 401 |
| AC-2: trust flag stays authed-only | **Pass** | anon `chordsVerified` → 401, no write; anon `nativeKeySource:'manual'` coerced to 'auto' |
| AC-3: authed unchanged | **Pass** | authed musician sets chordsVerified + honors manual provenance; GET/POST/DELETE untouched |
| AC-4: anon write rate-limited | **Pass** | checkRateLimit('api') added to PATCH; limited → returns the limit response, no write (shared bucket) |

## Verification Results

- `SKIP_ENV_VALIDATION=1 npx tsc --noEmit` → **exit 0**.
- `SKIP_ENV_VALIDATION=1 npx vitest run src/app/api/library/chord-cache/route.test.ts` → **9/9** (3 existing + 6 new PATCH cases).
- `SKIP_ENV_VALIDATION=1 npx next build --webpack` → **clean** (route-touching; webpack = Vercel).
- grep: PATCH = `{ requireAuth: false, schema }`; `checkRateLimit` in PATCH; `chordsVerified` guarded on `auth?.isMusician`; anon `nativeKeySource` coerced to 'auto'.

## Accomplishments

- Eliminated the anon transpose PATCH 401 → console-noise → recompute loop on the consumer Perform surface; the auto-detected native key now caches for the next visitor (cost + UX win, same rationale as the v11.3-01-02 anon POST).
- Kept the verification trust flag (`chordsVerified`) and manual key provenance authed-musician — anon can persist derived data but cannot assert human-curated trust.
- Closed the PATCH's missing rate-limit (it had none).

## Task Commits

| Task | Commit | Type | Description |
|------|--------|------|-------------|
| Task 1: PATCH field-split authz + rate-limit | `cd97ab21a3` | fix | requireAuth:false + ctx.auth guard + checkRateLimit |
| Task 2: regression tests | `cd97ab21a3` | fix | 6 PATCH cases (committed atomically with Task 1) |

Phase-close docs (PROJECT/ROADMAP/STATE) in the `feat(v11.5-01)` phase commit.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/app/api/library/chord-cache/route.ts` | Modified | PATCH → requireAuth:false + field guard + rate-limit |
| `src/app/api/library/chord-cache/route.test.ts` | Modified | 6 PATCH regression cases (old anon-401 test replaced) |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Anon writes nativeKey('auto') + lastUsed*; chordsVerified/manual gated | nativeKey(auto) is derived data like the anon-POST chords; trust + human provenance must stay authed | Fixes the bug; preserves the trust signal |
| Add api rate-limit (shared bucket) | D-Q2 "rate-limited"; PATCH had none | No double-punish vs cold-load 429s |
| No org-scoping in H5 | Matches the existing un-scoped anon POST precedent | Logged Deferred — org-scope both anon writes together later |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 1 | org-scope the anon chord-cache writes (logged to Deferred Issues) |

**Total impact:** None — plan executed as written; one pre-flagged Deferred recorded.

## Issues Encountered

None. (Verify-first corrected the assumed vector: the 401 was `saveNativeKey('auto')`, not `saveLastUsedKey` which has no caller.)

## Next Phase Readiness

**Ready:** Phase v11.5-01 (Tenancy + anon correctness) is COMPLETE 3/3 (H4 + H9 + H5). Next: v11.5-02 (the performance surface) — same transpose/Perform area, so this anon-correctness baseline is in place first.

**Concerns:** None. CRC behavior unchanged; authed path unchanged.

**Blockers:** None.

---
*Phase: v11.5-01-tenancy-anon-correctness, Plan: 02*
*Completed: 2026-06-12*
