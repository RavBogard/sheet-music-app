---
phase: v11.5-01-tenancy-anon-correctness
plan: 01
subsystem: ui
tags: [tenancy, branding, nextjs, app-router, server-component, x-org-id, multi-tenant]

requires:
  - phase: v11.1-01
    provides: the (main)/layout.tsx server-branding pattern + AppNavigation serverOrg* props seam
provides:
  - host-org-aware branding on the /perform/setlist subtree (detail page + track sub-route)
  - server-prop regression test for Perform-setlist nav branding
  - H4 stress cell (B4) in the run-3 stress prompt
affects: [v11.5-01-02 (H5 anon chord-cache), v11.5-02 (performance surface — same /perform group)]

tech-stack:
  added: []
  patterns:
    - "Server-layout host-org branding resolution applied to a route group OUTSIDE (main)"
    - "Server-component prop-tree assertion test (stub the client nav island, render-not-mount)"

key-files:
  created:
    - src/app/perform/setlist/__tests__/layout.test.tsx
  modified:
    - src/app/perform/setlist/layout.tsx
    - .paul/research/STRESS-TEST-PROMPT-2026-06-RUN3.md

key-decisions:
  - "Branding props ONLY — no serverIsAdmin/serverIsAuthed parity (auth-render change on a public route is out of H4 scope)"
  - "One layout fix covers detail page + track/[trackId] sub-route (nested under the same layout)"

patterns-established:
  - "Any /perform/* route group needing host branding mirrors (main)/layout.tsx's coerceOrgId+getOrgBranding resolution"

duration: ~25min
started: 2026-06-11T00:00:00Z
completed: 2026-06-12T14:05:00Z
---

# Phase v11.5-01 Plan 01: Tenancy + anon correctness (H4) Summary

**Converted `perform/setlist/layout.tsx` from a `"use client"` pass-through into an async server layout that resolves the host org from `x-org-id` and threads the branding trio into `<AppNavigation>` — closing the last known CRC-branding leak on a broslaz consumer route (the `/perform/setlist` group lived outside the `(main)` group that v11.1-01 fixed).**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~25 min (across 2 sessions; paused mid-APPLY) |
| Started | 2026-06-11 (Task 1) |
| Completed | 2026-06-12T14:05:00Z |
| Tasks | 2 completed |
| Files modified | 3 (1 created, 2 modified) |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: broslaz host → BL branding in Perform-setlist nav | **Pass** | Test asserts the real `getOrgBranding("brotherslazaroff")` trio is passed; anti-leak assertions confirm `serverOrgShortName !== "CRC Music"` and `serverLogoUrl !== "/logo.jpg"` |
| AC-2: crc host (and absent header) → byte-identical to today | **Pass** | Both `x-org-id="crc"` and absent-header cases pass the crc trio (`"CRC Music"` / `"/logo.jpg"` / `""`); `coerceOrgId(null)` → crc default |
| AC-3: fix covers whole /perform/setlist subtree, branding-only | **Pass** | One layout governs detail page + `track/[trackId]`; only the 3 branding props added — no auth/role/nav-link props changed |

## Verification Results

- `SKIP_ENV_VALIDATION=1 npx vitest run src/app/perform/setlist/__tests__/layout.test.tsx` → **3/3 passed** (AC-1 broslaz, AC-2a crc, AC-2b absent header).
- `SKIP_ENV_VALIDATION=1 npx tsc --noEmit` → **exit 0** (clean).
- `SKIP_ENV_VALIDATION=1 npx next build --webpack` → **clean**; `/perform/setlist/[id]` + `/perform/setlist/[id]/track/[trackId]` build as ƒ (dynamic) — correct for a header-reading layout. No route-export or client/server bundle-boundary errors (the two REUSABLE LESSONS that tsc/vitest miss).
- grep: layout no longer carries `"use client"`; passes `branding.shortName/logoUrl/wordmarkUrl`.

## Accomplishments

- Eliminated the CRC-identity leak in the top nav on broslaz `/perform/setlist/[id]` and its track sub-route — first-paint host-correct, flash-free.
- Established that the server-branding pattern (v11.1-01) now covers a route group outside `(main)`; documented the seam for the rest of `/perform/*` (relevant to v11.5-02).
- Added a render-not-mount server-prop regression test that stubs the heavy `AppNavigation` client island while exercising the real `coerceOrgId` + `getOrgBranding`.

## Task Commits

| Task | Commit | Type | Description |
|------|--------|------|-------------|
| Task 1: host-org-aware server layout | `180c9b666e` | fix | layout.tsx client→async-server; branding trio into AppNavigation |
| Task 2: regression test + H4 stress cell | `180c9b666e` | fix | layout.test.tsx (BL/crc/absent) + B4 cell in run-3 stress prompt (committed atomically with Task 1) |

Plan metadata + STATE/ROADMAP/paul.json: separate `docs(v11.5-01)` commit.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/app/perform/setlist/layout.tsx` | Modified | Client pass-through → async server layout resolving `x-org-id`→branding |
| `src/app/perform/setlist/__tests__/layout.test.tsx` | Created | Server-prop regression (AC-1/2/3) citing stress cell B4 |
| `.paul/research/STRESS-TEST-PROMPT-2026-06-RUN3.md` | Modified | Added B4 live first-paint visual confirm cell |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Branding props only (no auth-parity props) | serverIsAdmin/serverIsAuthed would change auth-rendering on a public route — not the H4 leak | Keeps scope to the leak; auth stays client-resolved as before |
| Commit Task 1+2 atomically | The test/stress-cell qualify the same single code change | One coherent fix commit |

## Deviations from Plan

### Summary

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 0 | — |

**Total impact:** None — plan executed exactly as written across the 2 sessions.

### Deferred Items

None — plan executed exactly as written.

## Issues Encountered

None. (Session was paused between Task 1 and Task 2; resumed cleanly from `.paul/HANDOFF-2026-06-12.md`.)

## Next Phase Readiness

**Ready:**
- Phase v11.5-01 Plan 02 (H5 — anon chord-cache write 401) can proceed; it is a server-route fix, independent of this UI change.
- The `/perform/*` server-branding seam is documented for v11.5-02 (the performance surface touches the same route group).

**Concerns:**
- None. CRC byte-identical; BL host-correct.

**Blockers:**
- None.

---
*Phase: v11.5-01-tenancy-anon-correctness, Plan: 01*
*Completed: 2026-06-12*
