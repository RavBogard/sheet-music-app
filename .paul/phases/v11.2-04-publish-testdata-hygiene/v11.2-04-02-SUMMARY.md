---
phase: v11.2-04-publish-testdata-hygiene
plan: 02
subsystem: testing
tags: [mcp, cleanup_all_test_data, isTest, firestore, dashboard, perform, test-isolation]

requires:
  - phase: v11.2-04-01
    provides: BUG-4 preview_publish unbonded-song flag (sibling plan in same phase; no code dependency)
provides:
  - cleanup_all_test_data owner-independent isTest setlist flag-sweep (full-sweep-mode only)
  - shared isNonTestSetlist predicate (public-setlist-order.ts) reused by /perform + (main) dashboard
  - authed dashboard now hides isTest setlists (Upcoming/recent/past)
affects: [v11.2-05, future test-data hygiene, any new human-facing setlist surface]

tech-stack:
  added: []
  patterns:
    - "Single shared non-test visibility predicate at the data boundary; surfaces filter once, never drift"
    - "Owner-independent flag-sweep runs AFTER owner-cascade so it needs no dedup bookkeeping"

key-files:
  created:
    - .paul/phases/v11.2-04-publish-testdata-hygiene/v11.2-04-02-PLAN.md
  modified:
    - src/lib/mcp/tools/test-tokens.ts
    - src/lib/mcp/__tests__/mcp-test-tokens.emulator.test.ts
    - src/components/performance/public-setlist-order.ts
    - src/app/(main)/DashboardClient.tsx
    - src/components/performance/__tests__/public-setlist-order.test.ts

key-decisions:
  - "library_index flag-sweep DROPPED — isTest exists on Setlist only; charts/songs are owner-cascaded (verified, not spec-trusted)"
  - "Dashboard filters isTest unconditionally (mirror /perform); authoring dashboard is a summary, not a test-management surface"
  - "Flag-sweep gated to full-sweep mode (prefix omitted) to preserve parallel-cowork uidPrefix isolation"

patterns-established:
  - "isNonTestSetlist is the canonical human-visibility gate for setlists"

duration: ~40min
started: 2026-06-11T11:30:00Z
completed: 2026-06-11T11:45:00Z
---

# Phase v11.2-04 Plan 02: BUG-5 test-data hygiene Summary

**`cleanup_all_test_data` now sweeps real-owner `isTest:true` setlists (full-sweep mode), and the authed dashboard hides isTest setlists via a predicate shared with `/perform`.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~40 min |
| Started | 2026-06-11T11:30:00Z |
| Completed | 2026-06-11T11:45:00Z |
| Tasks | 2 completed (both E/Q PASS) |
| Files modified | 5 (1 new PLAN) |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: real-owner isTest setlist swept + tracks (full mode) | Pass | emulator: setlist+2 tracks gone, real setlist survives, `aggregate["setlists.isTest"]==1` |
| AC-2: prefix mode does NOT run flag-sweep (isolation) | Pass | emulator: flag-only setlist survives a `{prefix:"isox"}` sweep; no isTest aggregate key |
| AC-3: self-inclusion (caller is a test bearer) | Pass | emulator: driver test bearer + flag-only setlist both removed, zero failures |
| AC-4: dashboard hides isTest; /perform parity preserved | Pass | shared `isNonTestSetlist`; public-view suite green; tsc + next build clean |

## Verification Results

- `npx tsc --noEmit` → clean (exit 0)
- `firebase emulators:exec ... vitest mcp-test-tokens.emulator.test.ts` → **28/28** (3 new flag-sweep cases)
- `npx vitest run src/components/performance/__tests__/` → **23/23** (4 new predicate cases; public-view parity green)
- `SKIP_ENV_VALIDATION=1 npx next build` → clean (exit 0; bundle-boundary guard)
- Skill audit: `/ui-ux-pro-max` invoked for Task 2 ✓ (verdict: pure row-filter, no visual change)

## Accomplishments

- Test-data namespace is now self-cleaning even for fixtures authored under a real admin uid (the BL stress-test gap).
- `/perform` and the authed dashboard share one non-test visibility predicate — the two surfaces can no longer drift.
- Owner-independent sweep designed to need no dedup (runs post-cascade) and to preserve parallel-cowork isolation (full-sweep-mode only).

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/mcp/tools/test-tokens.ts` | Modified | `cleanupAllTestDataCore`: additive `setlists where isTest==true` sweep (+tracks), full-sweep-mode only |
| `src/lib/mcp/__tests__/mcp-test-tokens.emulator.test.ts` | Modified | 3 new cases: AC-1/AC-2/AC-3 |
| `src/components/performance/public-setlist-order.ts` | Modified | Exported `isNonTestSetlist`; replaced inline check |
| `src/app/(main)/DashboardClient.tsx` | Modified | Filter isTest at subscription boundary (covers Upcoming/recent/past) |
| `src/components/performance/__tests__/public-setlist-order.test.ts` | Modified | 4 new predicate unit cases |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Drop `library_index` flag-sweep | `isTest` is a Setlist-only field (verified `models.ts:109`); charts/songs owner-cascaded, no flag exists | ROADMAP wording was unimplementable as written; recorded as known limitation |
| Dashboard filters unconditionally | Mirrors /perform; dashboard is a glanceable summary, not a test-mgmt surface; doesn't gate real performer data | Consistency without an err-public violation |
| Flag-sweep full-sweep-mode only | A prefixed (cowork) sweep must not cross-delete sibling fixtures | Preserves uidPrefix isolation contract |

## Deviations from Plan

### Summary

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 1 | library_index flag-sweep (no field) — logged |

**Total impact:** Plan executed as written; the one "deviation" (library_index) was pre-identified at plan time and baked into the PLAN's scope-correction, not discovered mid-execution.

### Deferred Items

- **library_index/songs isTest flag-sweep:** no `isTest` field on those collections. Real-owner "test" charts have no marker today. Revisit only if a chart-level isTest flag is ever introduced (would require touching every upload write-path).

## Issues Encountered

None.

## Next Phase Readiness

**Ready:**
- Phase v11.2-04 complete (BUG-4 + BUG-5 both shipped). `isNonTestSetlist` available for any future human-facing setlist surface.

**Concerns:**
- None. The library_index limitation is documented and low-impact (test charts are still owner-cascaded).

**Blockers:**
- None.

---
*Phase: v11.2-04-publish-testdata-hygiene, Plan: 02*
*Completed: 2026-06-11*
