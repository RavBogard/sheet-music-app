---
phase: v11.2-04-publish-testdata-hygiene
plan: 01
subsystem: mcp
tags: [mcp, preview-publish, unbonded-songs, chart-bond, publish-gate, agent-ux]

requires: []
provides:
  - preview_publish surfaces unbondedSongCount + unbondedSongs[] (song rows with no fileId AND no songId)
  - recommendation escalates to review_first when unbonded song rows exist (hard_block precedence preserved)
affects: [v11.2-04-02 (BUG-5 cleanup/dashboard), v11.2-05]

tech-stack:
  added: []
  patterns: [preview gate reuses isSongType + getTracksForSetlist to detect chart-less song rows chartHealth cannot see]

key-files:
  created: []
  modified:
    - src/lib/mcp/tools/preview-publish.ts
    - src/lib/mcp/__tests__/mcp-w01-preview-and-bonds.emulator.test.ts

key-decisions:
  - "Unbonded-song def = isSongType(type) && !fileId && !songId; non-song rows (header/reading/prayer/transition/note) are intentionally chart-less and excluded"
  - "Unbonded song rows → review_first (publishable but operator should bond/remove first); missing/shortcut hard_block keeps precedence"
  - "unbondedSongCount/unbondedSongs are additive result fields — no schema/arg change, wire-compatible"

patterns-established:
  - "The preview recommendation gate must account for rows chartHealth can't probe (unbonded song rows), not just bonded-chart health"

duration: ~25min
started: 2026-06-11T10:48:00Z
completed: 2026-06-11T11:00:00Z
---

# Phase v11.2-04 Plan 01: preview_publish unbonded-song flag — BUG-4 Summary

**`preview_publish` now counts `type:"song"` rows with no chart bond (fileId AND songId both null) — they render blank in Perform but were invisible to the recommendation gate (which only probes bonded-row chartHealth). It surfaces `unbondedSongCount`/`unbondedSongs[]` and escalates the recommendation to `review_first`.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~25 min |
| Tasks | 2 (2 PASS) |
| Files modified | 2 |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: unbonded song rows counted + surfaced + review_first | Pass | emulator: bonded "Oseh Shalom" + blank song row → unbondedSongCount 1, unbondedSongs lists it, recommendation "review_first" |
| AC-2: intentionally chart-less non-song rows not flagged | Pass | emulator: bonded song + reading + header rows → unbondedSongCount 0, recommendation "publish" |
| AC-3: hard_block precedence + count still surfaced | Pass | emulator: missing-chart bonded + blank song → recommendation "hard_block", unbondedSongCount still 1 |

## Verification Results

- `npx tsc --noEmit` → clean (after `setlist ?? {}` narrowing fix)
- `firebase emulators:exec --only firestore,auth "npx vitest run --config vitest.emulator.config.ts mcp-w01-preview-and-bonds"` → **14/14** (3 new BUG-4 cases + 11 existing)
- `SKIP_ENV_VALIDATION=1 npx next build` → clean

## Accomplishments

- `previewPublish` reuses the canonical `isSongType` predicate + `getTracksForSetlist` (the same load `verify_setlist_charts` uses) to detect chart-less song rows the bonded-only `chartHealth` probe can't see.
- Additive result fields (`unbondedSongCount`, `unbondedSongs[]`) — no MCP schema/arg change, wire-compatible.
- recommendation gate: `hard_block` (missing/shortcut) > `review_first` (flaggedBonds OR unbondedSongCount) > `publish`.

## Task Commits

Single per-plan commit `feat(v11.2-04-01)` (phase v11.2-04 has a 2nd plan 04-02 BUG-5 still to come):

| Task | Type | Description |
|------|------|-------------|
| Task 1 | feat | preview-publish.ts — unbondedSongCount/unbondedSongs[] + review_first escalation |
| Task 2 | test | mcp-w01-preview-and-bonds emulator — 3 BUG-4 cases |

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/mcp/tools/preview-publish.ts` | Modified | count unbonded song rows; escalate recommendation; +2 result fields |
| `src/lib/mcp/__tests__/mcp-w01-preview-and-bonds.emulator.test.ts` | Modified | 3 BUG-4 cases (unbonded→review_first, non-song excluded, hard_block precedence) |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| review_first (not hard_block) for unbonded songs | row is publishable; operator may intend it (rare) but should be warned | non-blocking advisory; matches flaggedBonds severity |
| Exclude non-song types via isSongType | header/reading/prayer/transition/note are chart-less by design | no false positives on liturgical/structural rows |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 1 | tsc narrowing (`setlist ?? {}`) — essential |
| Scope additions | 0 | — |
| Deferred | 0 | — |

### Auto-fixed Issues

**1. [Code] tsc: getTracksForSetlist 3rd arg possibly-undefined**
- **Found during:** Task 1 qualify (`tsc` TS2345 at preview-publish.ts:185)
- **Issue:** the setlist-doc var is `Record<string,unknown> | undefined` (from the `.exists ? data : undefined` ternary); `getTracksForSetlist` requires a non-undefined record.
- **Fix:** pass `setlist ?? {}` (by this point the setlist exists — publishSetlist would have errored otherwise — but the type needs narrowing).
- **Verification:** tsc clean on re-run.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| AC-1 test (all-unbonded setlist) returned undefined fields | `publish_setlist` refuses an all-unbonded setlist with `no_bonded_songs` (400) — fires even on dryRun, BEFORE the preview gate. Adjusted the test to the realistic MIXED scenario (a bonded song masking a blank one). Production code was already correct (AC-2/AC-3 proved it). |

## Next Phase Readiness

**Ready:** v11.2-04-02 (BUG-5 — `cleanup_all_test_data` sweep owner-real `isTest` setlists + `(main)` dashboard `isTest` filter decision). Phase v11.2-04's 2nd + final plan.

**Concerns:** None.

**Blockers:** None.

**Note for BUG-5 + future work:** an ALL-unbonded setlist is already protected by publish's `no_bonded_songs` gate (even on dryRun) — BUG-4 only matters for MIXED setlists where bonded songs mask blank ones. Good to remember when reasoning about publish-refusal vs preview-advisory layering.

---
*Phase: v11.2-04-publish-testdata-hygiene, Plan: 01*
*Completed: 2026-06-11*
