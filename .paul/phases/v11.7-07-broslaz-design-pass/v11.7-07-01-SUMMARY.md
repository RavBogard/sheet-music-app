---
phase: v11.7-07-broslaz-design-pass
plan: 01
subsystem: ui
tags: [branding, theming, multi-tenant, css-tokens, oklch, nav, accessibility]

requires:
  - phase: v11.1-05
    provides: BL teal forceDark theme + wordmark + OrgLogo monogram fallback
  - phase: v11.7-06
    provides: data-layer cross-tenant org-scoping (the wall this surface reads)
provides:
  - BL-scoped teal active-state glow (desktop nav + mobile tab bar) replacing CRC-indigo bleed
  - --nav-active-glow token (CRC default + BL teal override)
  - leader-wall + teal-glow human-verify item in UAT-PENDING
affects: [v11.7-07-02, v11.7-milestone-close]

tech-stack:
  added: []
  patterns:
    - "Theme-scoped glow accents via :root token + [data-org] override (zero component edits for token consumers)"

key-files:
  created:
    - .paul/research/v11-7-07-01-broslaz-authed-design-audit.md
  modified:
    - src/app/globals.css
    - src/components/nav/DesktopHeader.tsx

key-decisions:
  - "Implement only P1-1 (indigo→teal glow bleed); contrast + nav tap targets already pass; sub-44px shared controls are Plan 02's scope"
  - "CRC byte-identical via :root --nav-active-glow = exact prior literal; only BL overrides change"

patterns-established:
  - "When a hardcoded brand accent bleeds across tenants, lift it to a :root token (default = verbatim CRC value) and override under [data-org] — keeps CRC byte-identical and fixes token consumers with no component edits"

duration: ~20min
started: 2026-06-22
completed: 2026-06-22
---

# Phase v11.7-07 Plan 01: Authed Brothers Lazaroff design pass + leader-wall check

**The CRC-indigo brand-glow (hue 275) that bled into the BL teal theme on the desktop active nav link and the mobile tab bar's active/center tabs now glows teal — fixed BL-scoped, CRC byte-identical.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~20 min |
| Tasks | 3 (audit / fix / non-blocking human-verify) |
| Files modified | 2 code + 1 research doc + UAT-PENDING |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: Authed BL surface audited | Pass | `.paul/research/v11-7-07-01-broslaz-authed-design-audit.md` — severity-ranked, file/token-referenced; 1 P1 + PASS notes + Plan-02 deferrals |
| AC-2: P0/P1 fixed, BL-scoped, CRC byte-identical | Pass | Token override under `[data-org="brotherslazaroff"]`; CRC `:root` tokens unchanged; tsc 0; nav+org 39/39; `next build --webpack` 0 |
| AC-3: Cross-org leader-wall reads correctly | Pass (queued) | Non-blocking `checkpoint:human-verify` appended to `.paul/UAT-PENDING.md` (teal-glow visual + both-org leader sees only BL data/branding, reverse-check on CRC) |

## Accomplishments

- Found and fixed the one genuine BL-specific brand-consistency defect: the active-state glow was hardcoded CRC indigo (`oklch(... 275 ...)`) on shared nav chrome, so BL's teal theme glowed purple on its most-used surfaces (the iPad mobile tab bar + desktop active nav link).
- Fix is provably CRC byte-identical: new `:root --nav-active-glow` defaults to the exact prior desktop literal; only the three glow tokens are re-pointed to teal (hue 210) under `[data-org="brotherslazaroff"]`. MobileTabBar (which already consumes `var(--shadow-brand-glow)`/`-soft`) picks up teal with zero component edits.
- Confirmed contrast (foreground/muted/brand on the teal canvas) and in-nav tap targets (`min-h-11`) already meet the bar — no busywork.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/app/globals.css` | Modified | Added `:root --nav-active-glow` (= prior CRC literal); under `[data-org="brotherslazaroff"]` retuned `--shadow-brand-glow`, `--shadow-brand-glow-soft`, `--nav-active-glow` to teal hue 210 |
| `src/components/nav/DesktopHeader.tsx` | Modified | Active nav link glow: inline `oklch(...275...)` literal → `shadow-[var(--nav-active-glow)]` |
| `.paul/research/v11-7-07-01-broslaz-authed-design-audit.md` | Created | /ui-ux-pro-max audit findings |
| `.paul/UAT-PENDING.md` | Modified | Added v11.7-07-01 leader-wall + teal-glow verify item |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Implement only P1-1 | It's the single real BL-specific defect; contrast + nav targets already pass | Tight, low-risk change; no scope creep |
| sub-44px search input + avatar → Plan 02 | They're shared CRC+BL chrome, not BL-specific → belong to the 40→44px in-app sweep | Keeps this pass BL-only |

Skill audit: `/ui-ux-pro-max` (required per SPECIAL-FLOWS.md) invoked before Tasks 1 and 2 ✓.

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 2 (sub-44px controls → Plan 02; P3 micro-label/wordmark accepted) | Logged in audit doc |

**Total impact:** None — plan executed as written; the audit narrowed the fix to one P1.

## Issues Encountered

None.

## Next Phase Readiness

**Ready:** Plan 02 (folded deferred UI items: 40→44px in-app tap-target sweep incl. this surface's search input + avatar; ImporterModal P3 polish) — CRC+BL shared.

**Concerns:** None.

**Blockers:** None.

---
*Phase: v11.7-07-broslaz-design-pass, Plan: 01*
*Completed: 2026-06-22*
