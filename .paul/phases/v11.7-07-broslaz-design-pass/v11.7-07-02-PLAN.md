---
phase: v11.7-07-broslaz-design-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/nav/DesktopHeader.tsx
  - .paul/research/v11-7-07-02-tap-target-importer-closeout.md
autonomous: true
---

<objective>
## Goal
Close out the two folded deferred UI items from the v7.0 fold-forward (40→44px in-app
tap targets + ImporterModal P3 polish) with a verify-first pass: confirm the
component-level work is already done, fix the one genuine band-facing residual (the
desktop-header search input), and explicitly accept/document the rest.

## Purpose
Last plan of v11.7-07 (and the milestone). The deferred items were slated here; verify-first
(binding) shows most are already satisfied — this records that honestly and fixes only what's
real, rather than churning low-traffic authoring UI Daniel bypasses (he authors via MCP).

## Output
- Tap-target audit/close-out doc at `.paul/research/v11-7-07-02-tap-target-importer-closeout.md`.
- DesktopHeader search input bumped to the 44px floor.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@src/components/nav/DesktopHeader.tsx
@src/components/ui/button.tsx
@src/components/setlist/importer/ImporterModal.tsx

## Key facts (verified against deployed code this session)
- `Button` (src/components/ui/button.tsx) is ALREADY `default: h-11`, `icon: size-11` (≥44px).
  Perform controls were bumped to `h-11`/`w-11` (v11.6-02 WS-20); library rows are `min-h-11`
  (v11.7-05); desktop nav anchors + logo link are `min-h-11` (C10I1-004). The component-level
  40→44px work is effectively complete.
- Genuine band-facing residual: DesktopHeader search `<Input className="… h-9 …">` (36px) — a
  text field on every authed page.
- ImporterModal is an in-app AUTHORING surface; Daniel authors via MCP and bypasses it (browser
  app = band/consumer surface). Its only sub-44 controls are dense inline-table editors in the
  review step (`h-7`/`h-8`) — a legitimate data-table-editor exception; bumping them would bloat
  the table. The modal already has loading states, `aria-live`, focus rings, and error toasts.
- This plan is CRC+BL SHARED (a11y improvement) — NOT BL-byte-identical-constrained (that was
  Plan 01's scope). CRC changes here are expected/desired.
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /ui-ux-pro-max | required | Before Task 1 | ○ |

**BLOCKING:** /ui-ux-pro-max MUST be invoked before APPLY proceeds.

## Skill Invocation Checklist
- [ ] /ui-ux-pro-max loaded (run command or confirm)
</skills>

<acceptance_criteria>

## AC-1: Band-facing tap targets meet the 44px floor
```gherkin
Given the authed in-app shell (nav header, mobile header/tab bar, library rows, Perform controls)
When tap targets are audited against the 44px floor
Then all primary band-facing interactive controls are ≥44px
And the desktop-header search input is raised from h-9 (36px) to the 44px floor
And tsc is clean and `SKIP_ENV_VALIDATION=1 npx next build --webpack` exits 0
```

## AC-2: Deferred items closed-out honestly
```gherkin
Given the v7.0 fold-forward deferred items (40→44px sweep + ImporterModal P3)
When verify-first is applied
Then the close-out doc records which items were already satisfied (Button h-11/size-11, Perform, library, nav)
And records the accepted exceptions (ImporterModal dense inline-table editors; low-traffic authoring surface)
And nothing low-value is churned
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Verify-first tap-target audit + fix the band-facing residual</name>
  <files>src/components/nav/DesktopHeader.tsx, .paul/research/v11-7-07-02-tap-target-importer-closeout.md</files>
  <action>
    Invoke /ui-ux-pro-max (REQUIRED). Confirm against code that Button (h-11/size-11), Perform
    controls (h-11/w-11), library rows (min-h-11), and desktop nav anchors/logo (min-h-11) already
    meet the 44px floor — record this in the close-out doc. Then fix the genuine residual:
    DesktopHeader search `<Input>` `h-9` → `h-11` (keep `pl-9`, rounded-full, text-sm, and all other
    classes; the icon `top-1/2 -translate-y-1/2` stays centered). Re-check MobileHeader controls
    (hamburger is `size="icon"` = size-11 ✓) and note any other genuine band-facing residual found;
    fix only band-facing ones.
    Avoid: bumping dense data-table inline editors; touching Perform (already done); broad
    restyling beyond the height fix.
  </action>
  <verify>tsc --noEmit exits 0; `SKIP_ENV_VALIDATION=1 npx next build --webpack` exits 0; grep confirms DesktopHeader search input is `h-11`; close-out doc lists already-satisfied surfaces + the fix.</verify>
  <done>AC-1 satisfied: band-facing targets ≥44px; search input raised; build green.</done>
</task>

<task type="auto">
  <name>Task 2: ImporterModal P3 triage (verify-first) — document outcome</name>
  <files>.paul/research/v11-7-07-02-tap-target-importer-closeout.md</files>
  <action>
    Invoke /ui-ux-pro-max (already loaded). Review ImporterModal against P3 polish criteria.
    Record the finding: it is a low-traffic authoring surface (Daniel authors via MCP), already
    carries loading states / aria-live / focus rings / error toasts, and its only sub-44 controls
    are dense inline-table editors (legitimate exception). Conclude no churn warranted UNLESS a
    concrete, cheap, genuine P3 defect is found — if one is, fix it in ImporterModal.tsx and add it
    to files_modified + the doc; otherwise document accept-as-is with rationale.
    Avoid: inventing polish to look busy; bumping the dense review-table inputs.
  </action>
  <verify>Close-out doc has an ImporterModal section with an explicit verdict (accept-as-is OR the specific fix applied); if a fix was applied, tsc + build still green.</verify>
  <done>AC-2 satisfied: ImporterModal triaged + recorded honestly.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Perform-mode controls (already h-11/w-11, v11.6-02) — no re-churn.
- The `Button` component size variants (already correct).
- firestore.rules, MCP tools, API routes, data-layer (presentation-only plan).
- Dense inline-table editors in ImporterModal's review step (legitimate data-table exception).

## SCOPE LIMITS
- Fix only genuine BAND-FACING sub-44 residuals; do not sweep authoring-only or admin UI.
- No new features; no broad restyling.
- No publish/notify, no assignment-scheduling, no photo-import.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] /ui-ux-pro-max invoked before Task 1
- [ ] DesktopHeader search input = `h-11` (AC-1)
- [ ] Already-satisfied surfaces + accepted exceptions recorded in close-out doc (AC-2)
- [ ] tsc --noEmit exits 0
- [ ] `SKIP_ENV_VALIDATION=1 npx next build --webpack` exits 0
- [ ] No low-value churn (no dense-table-editor bloat, no Perform re-churn)
</verification>

<success_criteria>
- Band-facing tap targets at the 44px floor; one real residual fixed
- Deferred items closed honestly (verify-first), no busywork
- Quality floor held; phase v11.7-07 ready to transition → milestone close
</success_criteria>

<output>
After completion, create `.paul/phases/v11.7-07-broslaz-design-pass/v11.7-07-02-SUMMARY.md`.
This is the LAST plan of v11.7-07 → UNIFY triggers phase transition → `/paul:complete-milestone` (tag v11.7.0).
</output>
