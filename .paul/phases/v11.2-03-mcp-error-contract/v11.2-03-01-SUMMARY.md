---
phase: v11.2-03-mcp-error-contract
plan: 01
subsystem: mcp
tags: [mcp, error-contract, http-status, error-envelopes, upload, agent-ux]

requires: []
provides:
  - song_not_found→404, reorder_failed→400 in ERROR_CODE_MAP
  - shared uploadFailureEnvelope() — all 4 chart-upload tools map processChartUpload status by cause (dedup→409, fault→500)
  - mcp-error-contract unit test (status class per machine_code)
affects: [v11.2-03-02 (BUG-3 bulk per-row envelope)]

tech-stack:
  added: []
  patterns: [upload tools share one uploadFailureEnvelope contract sourced from processChartUpload's status+code]

key-files:
  created:
    - src/lib/mcp/__tests__/mcp-error-contract.test.ts
  modified:
    - src/lib/mcp/errors.ts
    - src/lib/mcp/tools/library-upload.ts
    - src/lib/mcp/tools/library-upload-session.ts

key-decisions:
  - "DEVIATION (improvement): reuse the existing duplicate_detected_in_library + result.status pattern via a shared helper instead of inventing chart_already_exists/chart_conversion_failed map codes"
  - "uploadFailureEnvelope sources error.code from processChartUpload's own status field (it already computes 400/409/422/500)"

patterns-established:
  - "MCP boundary must surface a tool's computed HTTP status, not flatten everything to a 500 catch-all machine_code"

duration: ~30min
started: 2026-06-10T09:10:00Z
completed: 2026-06-10T09:30:00Z
---

# Phase v11.2-03 Plan 01: MCP error contract — BUG-2 status codes Summary

**Deterministic client errors now return the correct HTTP-class code instead of 500: `add_track_to_setlist` unknown songId → 404, `reorder_setlist` bad ids → 400, chart-upload dedup → 409; genuine internal faults stay 500.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~30 min |
| Tasks | 3 (3 PASS) |
| Files modified | 4 (3 edited + 1 new test) |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: not-found + bad-input codes corrected | Pass | unit: `codeFor("song_not_found")===404`, `codeFor("reorder_failed")===400` |
| AC-2: chart upload errors mapped by cause | Pass | unit: dedup→409 `duplicate_detected_in_library`; too_large/empty_file→400; convert_failed→422; server_error/unknown→500 |
| AC-3: positive controls unchanged | Pass | unit: stale_version→409, not_found/setlist_not_found→404; unknown code→500 (reserve-500 held) |

## Verification Results

- `npx tsc --noEmit` → exit 0
- `npx vitest run mcp-error-contract` → 12/12
- `firebase emulators:exec … vitest run … mcp-chart-upload` → **55/55** (regression: existing empty_file/invalid_type/dedup/chart_in_use assertions all still pass)
- `SKIP_ENV_VALIDATION=1 npx next build` → clean

## Accomplishments

- `ERROR_CODE_MAP` gains `song_not_found:404` + `reorder_failed:400` — corrects those codes at EVERY existing call site (add_track/update_track/swap_chart/update_song/get_song; reorder) with zero per-site change.
- New shared `uploadFailureEnvelope(result, extras)` maps `processChartUpload`'s `code`+`status` to the right envelope; all 4 upload tools (`upload_chart`, `save_scraped_chart`, `finalize_chart_upload`, `import_chart_from_drive`) now route through it — eliminating the prior drift where only `import_chart_from_drive` mapped dedup→409.

## Task Commits

Single per-plan commit `feat(v11.2-03-01)` (phase has a 2nd plan 03-02 still to come):

| Task | Type | Description |
|------|------|-------------|
| Task 1 | fix | ERROR_CODE_MAP song_not_found→404, reorder_failed→400 |
| Task 2 | fix | shared uploadFailureEnvelope; 4 upload wrappers routed through it |
| Task 3 | test | mcp-error-contract.test.ts (codeFor + envelope mapping) |

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/mcp/errors.ts` | Modified | +song_not_found:404, +reorder_failed:400 |
| `src/lib/mcp/tools/library-upload.ts` | Modified | new `uploadFailureEnvelope` helper; upload_chart/save_scraped_chart/import_chart_from_drive use it |
| `src/lib/mcp/tools/library-upload-session.ts` | Modified | finalize_chart_upload uses the helper |
| `src/lib/mcp/__tests__/mcp-error-contract.test.ts` | Created | response-contract unit test |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Reuse `duplicate_detected_in_library` + `result.status` (vs new map codes) | processChartUpload already computes status+code; import_chart_from_drive already did it | Fewer codes, less surface; all 4 upload tools de-drifted onto one contract |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 1 | Refactored import_chart_from_drive onto the shared helper (was already correct) — de-drift, no behavior change |
| Deferred | 0 | — |

**Total impact:** Plan's INTENT fully met; mechanism improved. The plan assumed new `chart_already_exists`/`chart_conversion_failed` map codes; execution found `processChartUpload.status`/`.code` + the existing `duplicate_detected_in_library` pattern and reused them. Those two speculative map entries were added then reverted (net: only song_not_found + reorder_failed added). Classification: spec-mechanism refinement; ACs unchanged.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| tsc rejected `RichErrorEnvelope as Record<string,unknown>` in the test | Changed to `as unknown as Record<...>` |

## Next Phase Readiness

**Ready:** v11.2-03-02 (BUG-3 bulk per-row envelope) — the phase's 2nd plan. `bulk_add_tracks` per-row `BulkAddResult.error` is a bare string; needs `{code, machine_code, message}` shape (server-tracks-write.ts `bulkAddTracks`) + assertion updates.

**Concerns:** None. (Note: BUG-3's `song_not_found` per-row case will now resolve to 404 thanks to this plan's map addition.)

**Blockers:** None.

---
*Phase: v11.2-03-mcp-error-contract, Plan: 01*
*Completed: 2026-06-10*
