---
phase: v11.2-03-mcp-error-contract
plan: 02
subsystem: mcp
tags: [mcp, error-contract, bulk-add, error-envelopes, agent-ux, rich-error-body]

requires:
  - phase: v11.2-03-01
    provides: song_not_found→404 in ERROR_CODE_MAP (per-row song-not-found relies on it); uploadFailureEnvelope pattern
provides:
  - bulk_add_tracks per-row error is structured RichErrorBody {code, machine_code, message} (was bare string)
  - title_required:400 + batch_rolled_back:409 in ERROR_CODE_MAP
  - per-row machine codes: song_not_found(404) / title_required(400) / batch_rolled_back(409) / server_error(500)
affects: [v11.2-04 (BUG-4/5 hygiene), v11.2-05 (BUG-6/7/8 polish), any future bulk-tool error-shape work]

tech-stack:
  added: []
  patterns: [bulk per-row errors share the single-row RichErrorBody contract; code derived via codeFor() so ERROR_CODE_MAP stays single source of truth]

key-files:
  created: []
  modified:
    - src/lib/mcp/errors.ts
    - src/lib/mcp/server-tracks-write.ts
    - src/lib/mcp/__tests__/mcp-setlist-write.emulator.test.ts

key-decisions:
  - "DEVIATION (auto-fix): a 4th BulkAddResult.error site (best-effort addTrack catch ~:1742) was not in the plan's enumerated sites; mapped to server_error(500) — genuine internal fault on an already-validated row"
  - "title_required added to the map as 400 — also corrects the single add_track path (was 500 via the ?? fallback), consistent with BUG-2's deterministic-client-error intent"
  - "batch_rolled_back(409) for atomic-rollback collateral rows — the row itself is valid; a sibling failed, so it's a batch-level conflict the caller resolves by fixing the sibling and retrying"
  - "bulk_update_tracks per-row shape left as bare strings — explicitly out of scope (BUG-3 is bulk_ADD only); its emulator assertions untouched and still green"

patterns-established:
  - "Bulk per-row failures must carry the same RichErrorBody as single-row tools so agents branch on machine_code, not regex-match prose"

duration: ~20min
started: 2026-06-11T10:25:00Z
completed: 2026-06-11T10:45:00Z
---

# Phase v11.2-03 Plan 02: bulk_add_tracks per-row error envelope — BUG-3 Summary

**`bulk_add_tracks` per-row errors now ship the structured `{code, machine_code, message}` RichErrorBody (was a bare string), achieving parity with the single-row write tools — the last MCP write tool emitting prose-only per-row errors.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~20 min |
| Tasks | 3 (3 PASS) |
| Files modified | 3 |
| Error sites converted | 4 (3 pre-validation + 1 best-effort catch) |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: per-row errors structured with correct codes | Pass | emulator: song-not-found→`{404, song_not_found}`; missing-title→`{400, title_required}`; atomic collateral→`{409, batch_rolled_back}` |
| AC-2: shape parity with single-row tools via codeFor() | Pass | per-row `error` is RichErrorBody; `code` derived `codeFor(machine_code)`, no hardcoded codes |
| AC-3: positive controls + top-level envelope unchanged | Pass | happy-path rows still `{index, ok:true, trackId, order}` no-error; top-level `bulk_add_failed` (empty_tracks/too_many_tracks) unchanged; bulk_update_tracks string assertions still green |

## Verification Results

- `npx tsc --noEmit` → clean (surfaced the 4th error site; fixed, re-ran clean)
- `npx vitest run mcp-error-contract` → **12/12** (03-01 unit, no regression)
- `firebase emulators:exec --only firestore,auth "npx vitest run --config vitest.emulator.config.ts mcp-setlist-write"` → **78/78** (updated bulk_add assertions + untouched bulk_update assertions all pass)
- `SKIP_ENV_VALIDATION=1 npx next build` → clean (bundle-boundary guard)

## Accomplishments

- `BulkAddResult.error` type changed `string → RichErrorBody`; every per-row failure cause now carries an agent-parseable machine_code + correct HTTP-class code.
- Two new `ERROR_CODE_MAP` rows: `title_required:400` (also fixes single add_track, prev 500) and `batch_rolled_back:409`.
- Type change verified fully contained: `index.ts:988` JSON-serializes the whole result → wire updates transparently; no MCP-boundary edit needed in `setlist-write.ts`.

## Task Commits

Single per-plan commit `fix(v11.2-03-02)` (phase v11.2-03 now complete — both plans shipped):

| Task | Type | Description |
|------|------|-------------|
| Task 1 | fix | errors.ts — +title_required:400, +batch_rolled_back:409 |
| Task 2 | fix | server-tracks-write.ts — BulkAddResult.error string→RichErrorBody at 4 sites |
| Task 3 | test | emulator assertions: bulk_add per-row error → object shape (code+machine_code+message) |

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/mcp/errors.ts` | Modified | +title_required:400, +batch_rolled_back:409 |
| `src/lib/mcp/server-tracks-write.ts` | Modified | BulkAddResult.error → RichErrorBody; machine_code per cause at 4 sites; codeFor()-derived |
| `src/lib/mcp/__tests__/mcp-setlist-write.emulator.test.ts` | Modified | bulk_add per-row error assertions → object shape |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| title_required→400 in map | also corrects single add_track (was 500); aligns with BUG-2 intent | single + bulk title errors both 400 now |
| batch_rolled_back→409 | atomic collateral row is valid; sibling failed = batch-level conflict | agent reads committed:false + finds the real failing row's machine_code |
| bulk_update_tracks left as string | BUG-3 scoped to bulk_ADD only | sibling tool unchanged; potential future follow-up |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 1 | 4th error site, essential for type-correctness |
| Scope additions | 0 | — |
| Deferred | 0 | — |

### Auto-fixed Issues

**1. [Code] 4th BulkAddResult.error assignment site not enumerated in plan**
- **Found during:** Task 2 qualify (`tsc --noEmit` reported TS2322 at server-tracks-write.ts:1746)
- **Issue:** the best-effort mode `catch (err)` block assigned `error: err.message` (a string) — the plan enumerated the 3 pre-validation push sites + 2 result-map sites but missed this runtime-exception path.
- **Fix:** wrapped it in `{ code: codeFor("server_error"), machine_code: "server_error", message: err.message }` — a genuine internal fault on an already-validated row, so 500 is correct.
- **Files:** src/lib/mcp/server-tracks-write.ts (~:1742)
- **Verification:** tsc clean on re-run; emulator best-effort tests green.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| Default vitest config excludes `*.emulator.test.ts` | Ran via `--config vitest.emulator.config.ts` with firestore+auth emulators (the `test:emulator` script form) |

## Next Phase Readiness

**Ready:** Phase v11.2-03 (MCP error contract) COMPLETE — both BUG-2 (03-01) and BUG-3 (03-02) shipped. Next is **v11.2-04** (BUG-4 `preview_publish` unbonded-song flag + BUG-5 `cleanup_all_test_data` owner-real isTest sweep + dashboard isTest filter).

**Concerns:** None. (`bulk_update_tracks` still emits bare-string per-row errors — a known, deliberately-scoped-out asymmetry; convert in a future plan if agent-parseability matters there.)

**Blockers:** None.

---
*Phase: v11.2-03-mcp-error-contract, Plan: 02*
*Completed: 2026-06-11*
