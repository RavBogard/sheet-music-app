---
phase: v11.7-06-infra-adjacents
plan: 03
type: execute
autonomous: true
---

<objective>
## Goal
Close the v7.0 fold-forward tail: remove the one verified dead item
(`Recording.durationSeconds`) and record the verify-first triage that retired
the rest of the list. Final plan in v11.7-06.
</objective>

<context>
@.paul/PROJECT.md
@src/types/models.ts
</context>

<acceptance_criteria>
## AC-1: Dead field removed, nothing breaks
Given `Recording.durationSeconds` has zero readers/writers in src (verified)
When the optional field is removed from the `Recording` type
Then tsc is clean, recordings tests pass, and `next build --webpack` succeeds
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Remove the unused Recording.durationSeconds field</name>
  <files>src/types/models.ts</files>
  <action>
    Remove the `durationSeconds?: number` field from the `Recording` interface
    (models.ts:180). Verified dead: a single occurrence (the definition), zero
    readers/writers across src. No runtime effect (optional, never written/read).
  </action>
  <verify>npx tsc --noEmit && SKIP_ENV_VALIDATION=1 npx vitest run src/app/api/recordings/upload src/components/setlist/grid/__tests__/RecordingBindPopover.test.tsx</verify>
  <done>AC-1 satisfied: field gone, tsc 0, recordings tests green.</done>
</task>
</tasks>

<verify-first triage (the rest of the v7.0 fold-forward list — NOT actioned)>
- `extractApiError` unification — STALE: 0 matches in src (already unified/removed).
- "dead TanStack block in SetlistGrid" — NON-ISSUE: the only `@tanstack` is
  `react-table` (the grid IS a table — live, in use); no `@tanstack/react-query`
  anywhere (0 useQuery/useMutation/QueryClient).
- recordings/upload caps — ALREADY DONE: 25MB MAX_FILE_SIZE present.
- 40→44px in-app targets + ImporterModal P3 polish — UI: deferred to v11.7-07
  (the broslaz design pass, /ui-ux-pro-max BLOCKING) to keep v11.7-06 backend-only.
</verify-first>

<output>
After completion, create `.paul/phases/v11.7-06-infra-adjacents/v11.7-06-03-SUMMARY.md`
</output>
