---
phase: 02-security-api-consistency-v15
plan: 04
type: execute
wave: 1
depends_on: ["02-03"]
files_modified:
  - src/app/api/ai/transposer/route.ts
  - src/app/api/ai/transposer/scan/route.ts
  - src/app/api/ai/chord-validate/route.ts
  - src/app/api/ai/omr/route.ts
  - src/app/api/drive/health/route.ts
  - src/app/api/drive/metadata/route.ts
  - src/app/api/drive/save/route.ts
autonomous: true
---

<objective>
## Goal
Migrate final 7 straightforward withAuth routes (AI/4 + drive/3) to createApiHandler. The remaining 4 routes (drive/file, chat, bridge/setup-code, test-gemini) stay on withAuth due to complex auth patterns.

## Purpose
Completes the withAuth→createApiHandler migration. After this, only 4 routes retain withAuth — all with justified reasons (dual auth, streaming, mixed auth, dev-only).

## Output
7 route files modified. withAuth usage drops from 10 to 4 (3 justified + 1 dev-only).
</objective>

<context>
@.paul/phases/02-security-api-consistency-v15/02-03-SUMMARY.md
@src/lib/api-wrapper.ts
</context>

<acceptance_criteria>

## AC-1: All 7 Routes Use createApiHandler
```gherkin
Given a route file previously using withAuth directly
When migrated to createApiHandler
Then the file exports via createApiHandler with zero direct withAuth imports
```

## AC-2: Rate Limiting Preserved
```gherkin
Given a route that calls checkRateLimit
When migrated
Then checkRateLimit is still called as first line inside handler
```

## AC-3: Error Responses Sanitized
```gherkin
Given routes that previously leaked error details in 500 responses
When migrated
Then createApiHandler's catch returns generic "Internal server error"
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Migrate AI routes (4 routes)</name>
  <files>
    src/app/api/ai/transposer/route.ts,
    src/app/api/ai/transposer/scan/route.ts,
    src/app/api/ai/chord-validate/route.ts,
    src/app/api/ai/omr/route.ts
  </files>
  <action>
    For each AI route:
    - Replace withAuth import with createApiHandler from @/lib/api-wrapper
    - Wrap handler with createApiHandler, no role required
    - Keep checkRateLimit as first handler line
    - Keep maxDuration exports
    - These are all POST routes with JSON bodies but complex AI payloads (images, base64) — use z.any() passthrough or no schema (body read manually inside handler for image data)
    - ai/omr: uses checkRateLimit('ai') — preserve this tier
    - ai/transposer/scan: uses req.json() for { imageData, existingChords } — add minimal Zod schema or skip (image data)
    - Remove outer try/catch (createApiHandler handles it)
    - Use ctx.auth instead of auth, ctx.req instead of request/req
  </action>
  <verify>grep -r "withAuth" on these 4 files returns zero matches</verify>
  <done>AC-1, AC-2, AC-3 satisfied for AI routes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate drive routes (health, metadata, save)</name>
  <files>
    src/app/api/drive/health/route.ts,
    src/app/api/drive/metadata/route.ts,
    src/app/api/drive/save/route.ts
  </files>
  <action>
    - drive/health: GET, role: 'admin', no schema, no rate limit
    - drive/metadata: POST, no role, add Zod schema for { fileIds: z.array(z.string()) }
    - drive/save: POST, no role, add Zod schema for { sourceFileId: z.string(), xmlContent: z.string(), name: z.string().optional() }
    - Keep maxDuration and dynamic exports
    - Use ctx.auth, ctx.req, ctx.body
    - Remove outer try/catch
  </action>
  <verify>grep -r "withAuth" on these 3 files returns zero matches. npx tsc --noEmit passes.</verify>
  <done>AC-1, AC-2, AC-3 satisfied for drive routes</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- src/lib/api-wrapper.ts
- src/app/api/drive/file/[fileId]/route.ts (dual auth — stays on withAuth)
- src/app/api/chat/route.ts (streaming — stays on withAuth)
- src/app/api/bridge/setup-code/route.ts (mixed auth GET/POST — stays on withAuth)
- src/app/api/test-gemini/route.ts (dev-only — stays on withAuth)

## SCOPE LIMITS
- Only migrate the 7 listed routes
- Do NOT add Zod schemas for binary/image data payloads
</boundaries>

<verification>
- [ ] grep withAuth in src/app/api shows only 4 files (drive/file, chat, bridge, test-gemini)
- [ ] npx tsc --noEmit passes
</verification>

<success_criteria>
- 7 routes migrated, 4 remain on withAuth (justified)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.paul/phases/02-security-api-consistency-v15/02-04-SUMMARY.md`
</output>
