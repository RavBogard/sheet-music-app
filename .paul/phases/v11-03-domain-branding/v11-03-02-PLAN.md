---
phase: v11-03-domain-branding
plan: 02
type: execute
wave: 1
depends_on: ["v11-03-01"]
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/lib/org/branding.ts
  - src/lib/org/__tests__/branding.test.ts
  - src/app/login/page.tsx
autonomous: true
---

<objective>
## Goal
Give the `brotherslazaroff` tenant its own **dark + photographic, navy-accented** chrome, keyed off the `data-org` hook from v11-03-01 — re-skinning the whole app via scoped CSS-variable overrides + a branded login hero — while leaving CRC's indigo+amber theme **100% unchanged**.

## Purpose
brotherslazaroff.live must feel like a band, not a synagogue. Brand source (CONTEXT.md): their real navy-blue (brotherslazaroff.com) rendered on the app's dark canvas, live-performance photography, bold display wordmark. This is the visual taste call Daniel reacts to.

## Output
- `src/app/globals.css`: `[data-org="brotherslazaroff"]` token-override block (navy palette) mirroring the existing `[data-theme="v2"]` precedent + a BL hero utility.
- `src/app/layout.tsx`: `forcedTheme="dark"` for the BL org (dark-first identity); CRC keeps system theme.
- `src/lib/org/branding.ts`: pure `getOrgBranding(orgId)` → { shortName, tagline, forceDark } (+ test).
- `src/app/login/page.tsx`: org-aware — BL renders a dark photographic hero + "Brothers Lazaroff" wordmark; CRC path byte-identical.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/phases/v11-03-domain-branding/CONTEXT.md
@.paul/phases/v11-03-domain-branding/v11-03-01-SUMMARY.md

## Source Files
@src/app/globals.css
@src/app/layout.tsx
@src/components/theme-provider.tsx
@src/app/login/page.tsx
@src/lib/org/registry.ts
@src/lib/org/org-context.tsx
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /ui-ux-pro-max | required | Before this visual phase | ✓ (loaded this session — band/dark/photographic design system pulled) |

## Skill Invocation Checklist
- [x] /ui-ux-pro-max loaded — design system: dark canvas #0F0F23-class, bold display, photographic hero, 200-300ms transitions, no-emoji/SVG icons, WCAG AA contrast.
</skills>

<acceptance_criteria>

## AC-1: BL re-skins to navy dark theme; CRC unchanged
```gherkin
Given a request resolved to org "brotherslazaroff" (<html data-org="brotherslazaroff">)
When any page renders
Then the brand/accent/ring/background tokens resolve to the navy BL palette (hue ~252, distinct from CRC indigo ~275)
And the org is forced into dark theme
And a request resolved to "crc" renders the existing indigo+amber theme with NO change (tokens, fonts, forcedTheme all unchanged)
```

## AC-2: BL login shows band chrome; CRC login byte-identical
```gherkin
Given an unauthenticated visit to /login
When the host resolves to "brotherslazaroff"
Then the login shows a dark photographic hero + "Brothers Lazaroff" wordmark + band tagline (not the CRC round logo / "Sign in to access the music library" congregation chrome)
And when the host resolves to "crc" the login renders exactly as it does today (getServerCongregationConfig path untouched)
```

## AC-3: Branding resolver is pure + correct
```gherkin
Given getOrgBranding(orgId)
When called with "brotherslazaroff"
Then it returns { shortName: "Brothers Lazaroff", tagline: <band copy>, forceDark: true }
And when called with "crc" it returns forceDark: false (CRC keeps system theme + congregation-config chrome)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Branding resolver + BL navy theme tokens</name>
  <files>src/lib/org/branding.ts, src/lib/org/__tests__/branding.test.ts, src/app/globals.css</files>
  <action>
    `src/lib/org/branding.ts` (pure, no React/Firestore):
    - `export type OrgBranding = { shortName: string; tagline: string; forceDark: boolean }`
    - `getOrgBranding(orgId: OrgId): OrgBranding` —
      brotherslazaroff → { shortName: "Brothers Lazaroff", tagline: "Songs & setlists for the band", forceDark: true }
      crc (and default) → { shortName: "CRC Music", tagline: "", forceDark: false }
      Keep crc.forceDark=false so CRC theme behavior is untouched. shortName for crc is a
      fallback only — the login CRC path keeps using getServerCongregationConfig (see Task 3).

    `src/app/globals.css` — add a BL theme block AFTER the `.dark {…}` block (so equal-specificity
    `.dark` tokens are overridden) modeled on the existing `[data-theme="v2"]` precedent. Scope:
    `[data-org="brotherslazaroff"]` for base + `.dark[data-org="brotherslazaroff"]` (higher specificity
    than `.dark`) for the dark canvas. Navy palette (hue ~252, NOT CRC's 275 indigo):
      --brand: oklch(0.60 0.17 252); --brand-foreground: oklch(0.98 0 0);
      --ring: oklch(0.60 0.15 252 / 0.5);
      --background: oklch(0.12 0.02 255); --foreground: oklch(0.97 0.005 250);
      --card: oklch(0.17 0.025 255 / 0.5); --card-foreground: oklch(0.97 0.005 250);
      --popover: oklch(0.17 0.03 255 / 0.78); --popover-foreground: oklch(0.97 0.005 250);
      --primary: oklch(0.97 0.005 250); --primary-foreground: oklch(0.12 0.02 255);
      --secondary: oklch(0.45 0.16 252 / 0.4); --secondary-foreground: oklch(0.90 0.01 250);
      --accent: oklch(0.24 0.04 255); --accent-foreground: oklch(0.97 0.005 250);
      --border: oklch(0.30 0.03 255 / 0.45); --input: oklch(0.30 0.03 255 / 0.55);
      --sidebar*, --ring etc. shifted to hue 252/255 to match (mirror the .dark sidebar block).
    Also add a `--shadow-brand-glow` override is NOT needed (reads --brand at use sites? it's a static
    oklch literal at :root lines 209-211 — leave it; it's a soft indigo glow, acceptable, or add a BL
    override if trivial). Add a `@utility bl-hero` for the login hero treatment:
      layered dark radial "stage light" gradients in navy + a hook for an optional photo via
      `background-image: var(--bl-hero-image, none)` (cover, center), so Daniel can later drop a real
      live-performance photo without a code change and there is NO broken <img> if absent.
    Avoid: touching `:root`, `.dark`, or `[data-theme="v2"]` existing values — ADD a new scoped block only.
  </action>
  <verify>`npx vitest run src/lib/org/__tests__/branding.test.ts` green (asserts BL + crc returns); `npx tsc --noEmit` clean; grep confirms new `[data-org="brotherslazaroff"]` block exists and `:root`/`.dark` blocks are unchanged.</verify>
  <done>AC-1 (tokens) + AC-3: BL navy tokens scoped; resolver pure + correct; CRC token blocks untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Force dark theme for the BL org in the root layout</name>
  <files>src/app/layout.tsx</files>
  <action>
    Using the `orgId` already resolved in layout (v11-03-01), import `getOrgBranding` from
    `@/lib/org/branding` and compute `const { forceDark } = getOrgBranding(orgId)`.
    Pass `forcedTheme={forceDark ? "dark" : undefined}` to `<ThemeProvider>` (theme-provider.tsx
    spreads props straight to next-themes, which supports forcedTheme). When undefined, next-themes
    behaves exactly as today (defaultTheme="system" enableSystem) → CRC unchanged. With forcedTheme
    BL always carries `class="dark"` on <html>, so `.dark[data-org="brotherslazaroff"]` tokens apply.
    Avoid: changing defaultTheme/enableSystem/disableTransitionOnChange/nonce or provider order.
  </action>
  <verify>`npx tsc --noEmit` clean; inspection: `forcedTheme` is `"dark"` only when org is brotherslazaroff, else `undefined`.</verify>
  <done>AC-1 (forced dark for BL, CRC theme behavior unchanged).</done>
</task>

<task type="auto">
  <name>Task 3: Org-aware login hero (BL band chrome; CRC byte-identical)</name>
  <files>src/app/login/page.tsx</files>
  <action>
    In the server component, resolve org: `import { headers } from "next/headers"` is already used
    elsewhere? No — add `resolveOrgIdByDomain((await headers()).get("x-org-id"))` from
    `@/lib/org/registry`, and `getOrgBranding` from `@/lib/org/branding`.
    Branch:
    - **org !== "brotherslazaroff"** → return the EXISTING markup verbatim (CRC/default path: the
      getServerCongregationConfig logo + shortName + "Sign in to access the music library" chrome).
      Do not alter a single class here.
    - **org === "brotherslazaroff"** → render a band variant of the SAME structural shell (same
      `<main id="main-content">`, same `glass-card` + `<LoginClient />`, same legal `<nav>` and noscript)
      but with: a full-bleed `bl-hero` background layer (dark photographic gradient), a bold uppercase
      "Brothers Lazaroff" wordmark using `font-display` + tight tracking (NOT the round logo img),
      the band `tagline` from getOrgBranding, and navy `bg-brand` ambient glow. Keep LoginClient,
      legal nav, noscript, Privacy/Terms/etc. identical (auth + compliance must not regress).
    Accessibility: wordmark in an `<h1>`; hero layer `aria-hidden`; maintain ≥4.5:1 text contrast on the
    dark hero; keep cursor/focus states. No emojis — SVG only if any icon is added.
    Avoid: removing the legal nav / noscript / LoginClient; changing the CRC branch; hardcoding the band
    name (read from getOrgBranding so v11-03-03 vocab can extend it).
  </action>
  <verify>`npx tsc --noEmit` clean; `npx next lint` (or eslint on the file) clean; manual read confirms CRC branch byte-identical to prior file and BL branch keeps LoginClient + legal nav + noscript. Full unit suite `npx vitest run` green (no regression).</verify>
  <done>AC-2: BL login shows band hero + wordmark; CRC login unchanged; auth/compliance surfaces intact.</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- `:root`, `.dark`, and `[data-theme="v2"]` token values in globals.css (ADD a new `[data-org="brotherslazaroff"]` block only).
- The CRC/default branch of login/page.tsx (must stay byte-identical — congregation-config chrome).
- `<LoginClient />`, the legal `<nav>`, and the `<noscript>` block (auth + GDPR/SMS compliance).
- next-themes config beyond adding `forcedTheme` (no defaultTheme/enableSystem changes).
- Any vocab/labels for in-app terms or rabbi/service-type fields — that is v11-03-03.
- src/proxy.ts / org-context.tsx (v11-03-01, stable).

## SCOPE LIMITS
- No new npm dependencies. Reuse Righteous (`--font-display`) for the wordmark; a distinct BL display font is a possible future polish, not this plan.
- No real photo binary is shipped — the hero uses a CSS gradient treatment + an optional `--bl-hero-image` slot Daniel can fill later (documented in UAT). No broken <img> if absent.
- Theme tokens + login only. The authed in-app shell re-skins automatically via the scoped tokens; no per-component branding edits.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` clean
- [ ] `npx vitest run src/lib/org/__tests__/branding.test.ts` green
- [ ] Full unit suite `npx vitest run` green (no regression)
- [ ] eslint clean on edited files (route/page export rules — see feedback_nextjs_route_exports)
- [ ] Code inspection: `:root`/`.dark`/`[data-theme="v2"]` unchanged; CRC login branch unchanged
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- BL renders navy dark+photographic chrome app-wide via scoped tokens; forced dark.
- BL login = band hero + wordmark; CRC login + theme provably unchanged.
- getOrgBranding pure + tested; tsc clean; full unit suite green.
</success_criteria>

<output>
After completion, create `.paul/phases/v11-03-domain-branding/v11-03-02-SUMMARY.md`.
UAT note: Daniel to (a) eyeball BL chrome at brotherslazaroff.live (or by forcing data-org locally) and (b) optionally drop a live-performance photo for the hero `--bl-hero-image` slot.
</output>
