---
phase: v11.7-05-library-density
plan: 02
subsystem: ui
tags: [library, bind-picker, cmdk, composer, tailwind]

requires:
  - phase: v11.7-05-01
    provides: splitChartComposer pure helper (reused)
provides:
  - Composer sub-label + sparse key badge on the chart-bind picker rows (ChartBindPopover + ChartBindDialog)
  - Shared ChartPickerItemContent presentational row component
affects: []

tech-stack:
  added: []
  patterns: [shared presentational row content for the two verbatim-duplicate pickers; show-when-present key from the local mirror]

key-files:
  created:
    - src/components/setlist/grid/ChartPickerItemContent.tsx
  modified:
    - src/components/setlist/grid/ChartBindPopover.tsx
    - src/components/setlist/grid/ChartBindDialog.tsx
    - src/components/setlist/grid/__tests__/ChartBindPopover.test.tsx

key-decisions:
  - "Key shown WHEN present (song.defaults?.key) — sparse-safe; no sync/backfill to populate it. Composer via splitChartComposer(title)."
  - "Shared ChartPickerItemContent component (vs inline ×4) — the two pickers duplicate the row verbatim; one component prevents drift."

patterns-established:
  - "Bind-picker rows: title + dimmed composer + right-aligned sparse key, single dense cmdk line; cmdk value stays the full title so composer-in-title still matches search."

duration: ~20min
started: 2026-06-22T10:08:00Z
completed: 2026-06-22T10:18:00Z
---

# Phase v11.7-05 Plan 02: Bind-picker metadata Summary

**The chart-bind picker rows (both ChartBindPopover and ChartBindDialog) now show a dimmed composer sub-label + a compact key badge (when known) instead of bare title — text-only, still dense and filterable.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~20 min |
| Tasks | 2 completed |
| Files modified | 4 (1 created, 3 modified) |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: composer sub-label | Pass | splitChartComposer(title) → dimmed `text-muted-foreground`; popover test asserts separate "Hashkivenu" + "Klepper-Freelander" |
| AC-2: sparse key badge | Pass | `song.defaults?.key` → compact mono badge; omitted when absent; single-line |
| AC-3: both pickers covered | Pass | shared `ChartPickerItemContent` used by both (3 refs each: import + 2 row blocks); dialog covered by construction |
| AC-4: tsc 0 / tests / build 0 | Pass | tsc 0 · setlist-grid 167 passed/78 pre-existing-skipped · `next build --webpack` 0 |

## Accomplishments

- New `ChartPickerItemContent` shared presentational row (icon + title + dimmed composer + sparse key) — used by both pickers, killing the verbatim row duplication.
- `ChartBindPopover` + `ChartBindDialog` rows now carry composer + key; removed the now-unused `FileText` import from both (moved into the shared component).
- Extended `ChartBindPopover.test.tsx` (+1 → 10 cases) proving composer-split + key render + sparse-safety.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/components/setlist/grid/ChartPickerItemContent.tsx` | Created | shared row content (title + composer + sparse key) |
| `src/components/setlist/grid/ChartBindPopover.tsx` | Modified | rows use the shared component; FileText import removed |
| `src/components/setlist/grid/ChartBindDialog.tsx` | Modified | same |
| `src/components/setlist/grid/__tests__/ChartBindPopover.test.tsx` | Modified | +1 composer/key case |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Shared `ChartPickerItemContent` component | The two pickers duplicate the row verbatim; one component prevents future drift and made the 4-block edit a single change | Minor scope addition (1 file) beyond the plan's "edit inline ×4" — lower risk, DRY |
| Key shown only when present | The local `LocalSong` mirror carries `defaults?.key` but it's not guaranteed populated; no sync/backfill in scope | Sparse-safe badge, no empty slots; no sync-layer churn |

## Deviations from Plan

### Summary
| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 1 | Shared component (vs inline) — lower-risk, see Decisions |
| Deferred | 0 | — |

### Plan-spec note
- Plan said "replace the bare span inline in BOTH"; implemented as a shared `ChartPickerItemContent` instead (same result, less duplication). Removed the now-dead `FileText` imports as part of the swap.

## Issues Encountered
None.

## Next Phase Readiness

**Ready:** Phase v11.7-05 COMPLETE (both plans shipped). Next phase: **v11.7-06 (infra adjacents** — recordings/finalize/anon-chord-cache org-scoping + cheap v7.0 fold-forward hygiene; backend, no UI).

**Concerns:** None. iPad UAT items (05-01 + 05-02) are non-blocking, logged.

**Blockers:** None.

---
*Phase: v11.7-05-library-density, Plan: 02*
*Completed: 2026-06-22*
