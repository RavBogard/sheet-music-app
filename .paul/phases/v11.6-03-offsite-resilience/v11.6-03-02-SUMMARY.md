---
phase: v11.6-03-offsite-resilience
plan: 02
subsystem: offline
tags: [offline, perform, setlist, save-offline, onsnapshot, ipad]
requires:
  - phase: v11.6-03-offsite-resilience
    provides: decision C (close all 5 gaps); verified gap locations
provides:
  - Open set survives a transient offline onSnapshot error (WS-12)
  - Definitive "Saved N/N" offline-readiness signal (WS-13)
affects: [v11.6-03-03 (WS-10 MusicXML), v11.6-03-04 (WS-29/30)]
key-files:
  created:
    - src/app/perform/setlist/[id]/perform-error-gate.ts
    - src/app/perform/setlist/[id]/perform-error-gate.test.ts
  modified:
    - src/app/perform/setlist/[id]/SetlistPerformClient.tsx
    - src/components/performance/SaveOfflineButton.tsx
key-decisions:
  - "WS-13 was largely already implemented in SaveOfflineButton (readyCount/total/all/partial + icon+color+aria); shipped the minimal clarity delta (Saved N/N) not a new blocking gate (anti-over-engineering)"
patterns-established:
  - "Fatal-error gating extracted to a pure helper (shouldShowFatalSetlistError) — testable without rendering the heavy Perform client"
duration: ~25min
started: 2026-06-18T15:15:00Z
completed: 2026-06-18T15:40:00Z
---

# Phase v11.6-03 Plan 02: WS-12 + WS-13 (camp-relevant off-site P2s)

**An already-open Perform set now survives a transient offline `onSnapshot` error instead of blanking to a full-screen error (WS-12), and the Save-offline control shows an explicit "Saved N/N" so the band can confirm every chart is cached before leaving wifi (WS-13).**

## AC Results
| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1 WS-12: open set survives offline error | Pass | `shouldShowFatalSetlistError(errorMessage, hasLoadedTracks)` — fatal screen only when no loaded tracks; SetlistPerformClient guards on `tracks.length > 0`. 3 unit cases. |
| AC-2 WS-13: definitive readiness signal | Pass | SaveOfflineButton all-ready label `Saved ${total}/${total}` (was bare "Saved"); partial unchanged ("Save M/N"); icon+color+aria already present. New all-ready count test. |

## Verification
- `perform-error-gate` 3/3 · `SaveOfflineButton` 10/10 (new all-ready count case; existing "Saved" assertions still pass) · `setlist-view` 5/5 (no regression)
- `tsc --noEmit` 0 · `SKIP_ENV_VALIDATION=1 next build --webpack` 0
- Commit `7ac307812f`, pushed origin master (deploying).

## Verify-first note
WS-13 was already substantially built (SaveOfflineButton's readyCount/total + all/partial states + WCAG-checked color/icon/aria). Per anti-over-engineering, shipped only the minimal clarity delta (explicit N/N in the all-ready state) rather than a new blocking pre-departure gate — the report itself classified WS-13 as already-mitigated.

## Deviations
None — built per plan; WS-13 scoped down honestly to the real gap.

## Next
Plan v11.6-03-03 — WS-10 (SmartScoreViewer MusicXML IDB-first). Then 04 (WS-29/30).

## UAT (real 11" iPad, post-deploy, non-blocking)
- WS-12: open a set, drop wifi (incognito), force an onSnapshot error → the set stays on screen (not a full-screen error).
- WS-13: open a set → the Save-offline control reads "Saved N/N" once every chart is cached (amber "Save M/N" while partial).
