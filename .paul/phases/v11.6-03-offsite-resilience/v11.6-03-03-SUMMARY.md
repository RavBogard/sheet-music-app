---
phase: v11.6-03-offsite-resilience
plan: 03
subsystem: ui
tags: [musicxml, osmd, offline, indexeddb, perform, ipad-webkit, smartscoreviewer]
requires:
  - phase: v11.6-03-01
    provides: offline-gap verification (WS-10 row → confirmed non-IDB-first fetch)
provides:
  - SmartScoreViewer resolves MusicXML bytes IDB-first by fileId (network-fallback /api/drive/file/<id>)
  - PDFOverlay musicxml branch passes fileId (no fileUrl blob: pipe)
  - offline MusicXML rendering on the band iPads
affects: [v11.6-03-04, v11.7-musicxml]
tech-stack:
  added: []
  patterns:
    - "Viewer source resolution: IDB-first by fileId → Blob API read (no fetch(blob:)) → network fallback (mirrors Text/Audio/PDF)"
key-files:
  created: []
  modified:
    - src/components/music/SmartScoreViewer.tsx
    - src/components/performance/PDFOverlay.tsx
    - src/components/music/__tests__/smart-score-viewer.test.tsx
key-decisions:
  - "Replace the url prop with fileId (no backward-compat) — the lone production mount is updated in the same plan"
  - "Preserve the XML-vs-.mxl byte sniff: plain MusicXML → osmd.load(string), compressed .mxl → osmd.load(Blob)"
patterns-established:
  - "All non-image Perform viewers (Text/Audio/MusicXML) now self-resolve IDB-first by fileId; fileUrl is Image-only"
duration: ~35min
started: 2026-06-18T15:00:00Z
completed: 2026-06-18T15:30:00Z
---

# Phase v11.6-03 Plan 03: WS-10 MusicXML offline IDB-first Summary

**SmartScoreViewer now resolves MusicXML bytes IDB-first by `fileId` (offline-idb `getFile` → Blob, read via the Blob API with no `fetch(blob:)` round-trip), falling back to `/api/drive/file/<id>` only on a cache miss — so MusicXML reads off-line on the band's iPads like every other chart type.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~35 min |
| Tasks | 2 completed |
| Files modified | 3 |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: Offline MusicXML renders from IDB cache | Pass | New "IDB-first" test: `getFile` → Blob(XML), `osmd.load(XML)`, `fetch` NOT called |
| AC-2: Online, uncached MusicXML renders via network | Pass | New "Network-fallback" test: `getFile`→null → `fetch('/api/drive/file/file-net')` → load |
| AC-3: Plain XML + compressed .mxl both load | Pass | XML→`load(string)`; .mxl (PK zip header)→`load(Blob)`; Q-DETECT-1 key-heal suite still green |

## Verification Results

- `npx tsc --noEmit` → exit 0
- `npx vitest run smart-score-viewer.test.tsx` → **32/32 pass** (29 prior + 3 new: IDB-first / network-fallback / .mxl-Blob)
- `npx vitest run src/components/performance src/components/music` → pdf-overlay + smart-score green; **6 failures are PRE-EXISTING on master** (baseline-confirmed by stashing the WS-10 edits and re-running: same 6 fail — `public-view` listing-cap [date-dependent], `perform-cls` slot-reservation, `pdf-viewer` width-0). None touch the WS-10 surface.
- `SKIP_ENV_VALIDATION=1 npx next build --webpack` → exit 0 (client-component bundle/route safety)

## Accomplishments

- Closed the last viewer doing a bare network `fetch` for its source — MusicXML now matches the IDB-first contract of Text/Audio/PDF.
- Removed the `fileUrl` blob: round-trip for MusicXML, eliminating the iPad WebKit `fetch(blob:)` race for this viewer.
- Established that `fileUrl` in PDFOverlay is now Image-only (comment corrected).

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/components/music/SmartScoreViewer.tsx` | Modified | `url` prop → `fileId`; dropped `sourceUrl` state + `?_v=2` cache-bust effect; load effect resolves bytes IDB-first (`getFile`→`blob.arrayBuffer()`) → network fallback (`/api/drive/file/<id>`); preserved XML/.mxl sniff, fit-zoom, transpose/resize effects, and Q-DETECT-1 key-heal |
| `src/components/performance/PDFOverlay.tsx` | Modified | musicxml branch passes `fileId` (was `url={fileUrl}`); `fileUrl` scope comment corrected to Image-only |
| `src/components/music/__tests__/smart-score-viewer.test.tsx` | Modified | All `url=` → `fileId=`; added `@/lib/offline-idb` mock (`mockGetFile`); 3 new cases (IDB-first / network-fallback / .mxl-Blob); S7 test re-framed to fileId-wins-over-stale-aiXmlContent |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Replace `url` with `fileId` (no backward-compat shim) | Lone production mount (PDFOverlay) updated in the same plan; mirrors Text/Audio contract | Cleaner contract; removes the blob: pipe for MusicXML |
| `aiXmlContent` kept as the no-fileId inline fallback | Preserves the (currently-unused) inline-XML injection path without re-introducing the stale-AI-XML bug (S7) | No behavior change for the live mount |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 0 | — |

**Total impact:** Executed exactly as planned. No scope creep.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| 6 failing tests in the music/performance dirs | Baseline-confirmed PRE-EXISTING on master (stash + re-run); date-dependent + unrelated suites, NOT a WS-10 regression |

## Skill Audit

`/ui-ux-pro-max` (required by SPECIAL-FLOWS for UI-touching phase 03) — **invoked ✓**. Confirmed no UX regression: loading overlay (`role=status`, reduced-motion spinner) and "Failed to load music XML." error card are byte-identical; offline behavior strictly improves (cache-hit renders instead of erroring).

## Next Phase Readiness

**Ready:**
- Phase 03 slice 04 (WS-29 audio offline note + WS-30 SW online-session caching window) — the last slice to close phase v11.6-03.

**Concerns:**
- Forward-risk only this weekend: no `.musicxml/.xml/.mxl` rows in the 3 Camp Sabra sets, so this isn't exercised live until MusicXML adoption. Real-device offline MusicXML open remains a UAT item when a MusicXML chart exists.

**Blockers:** None.

---
*Phase: v11.6-03-offsite-resilience, Plan: 03*
*Completed: 2026-06-18*
