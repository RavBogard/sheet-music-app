---
phase: v11.6-02-perform-reading-airtight
plan: 07
subsystem: ui
tags: [perform, ipad, drawer, queue-nav, dead-code]

requires:
  - phase: v11.6-02-01
    provides: PDFOverlay playbackQueue includes every fileId-bearing row; store jumpToSong restores transposition/zoom/fitMode for the target
  - phase: v11.6-01
    provides: stress-triage report (WS-08/28 source+line cites)
provides:
  - In-chart Setlist drawer song-open jumps WITHIN the live queue (Next/Prev preserved) (WS-08)
  - Dead unmounted SwipeOverlay + orphaned swipe-anim keyframe removed (WS-28)
affects: [phase-02 CLOSE — this is the final plan of v11.6-02]

tech-stack:
  added: []
  patterns:
    - "Drawer row open = store jumpToSong(globalIndex), NOT router.push to the standalone single-chart route — keeps the playbackQueue + chevron traversal intact"

key-files:
  created:
    - src/components/performance/__tests__/setlist-drawer.test.tsx
  modified:
    - src/components/performance/SetlistDrawer.tsx
    - src/app/globals.css
  deleted:
    - src/components/performance/SwipeOverlay.tsx

key-decisions:
  - "WS-08 reuses the existing store jumpToSong (no new state); globalIndex IS the playbackQueue index (proven by the existing isCurrent === queueIndex)"
  - "WS-17 (dead currentTrackIndex/setCurrentPosition position-highlight) DEFERRED → active v7.1 cycle-13a leader-broadcast lane, which owns that plumbing and whose feature is the real fix — touching it here would collide"
  - "WS-28 is delete-only — no swipe-gesture implementation (out of narrow-hardening scope; recorded as a v11.7 candidate)"

patterns-established:
  - "In-Perform navigation stays inside the queue surface; router.push to /perform/<fileId> is reserved for the empty-queue public-picker entry path"

duration: ~20min
started: 2026-06-18T11:35:00Z
completed: 2026-06-18T11:52:00Z
---

# Phase v11.6-02 Plan 07: Drawer-nav (phase-02 close)

**Opening a song from the in-chart Setlist drawer now jumps WITHIN the live playbackQueue (Next/Prev keep traversing the full setlist) instead of dead-ending on the standalone single-chart route (WS-08); the unmounted SwipeOverlay that falsely promised a swipe gesture is gone (WS-28). This is the final plan of phase v11.6-02.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~20 min |
| Tasks | 2 of 2 completed (all PASS) |
| Files modified | 3 (1 created, 1 deleted) |
| Quality gates | tsc 0 · `next build --webpack` 0 · setlist-drawer 3/3 (new) + setlist-drawer-queue-row 6/6 (no regression) · `grep SwipeOverlay\|swipe-anim src` → none |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: Drawer song-open keeps the queue (WS-08) | Pass | QueueRow `onOpen` now `jumpToSong(globalIndex)` + `setOpen(false)`; test asserts click → `jumpToSong(idx)` and `router.push` NOT called. Queue length unchanged → Next/Prev preserved. |
| AC-2: Non-openable rows still no-op (WS-08 guard) | Pass | `if (!track.fileId) return` retained; test asserts a no-fileId flow row calls neither `jumpToSong` nor `router.push`. |
| AC-3: SwipeOverlay removed (WS-28) | Pass | `SwipeOverlay.tsx` deleted, `@keyframes swipe-anim` + the reduced-motion `[style*="swipe-anim"]` block removed from globals.css; `grep` confirms zero references; tsc + build clean. |

## Accomplishments

- WS-08 (the one remaining P1 of the phase): mid-service, tapping another song in the drawer no longer drops the whole set to a "Song 1 of 1" dead-end — it jumps in-queue and the chevrons still walk the full setlist.
- WS-28: removed dead code that advertised "Swipe left or right to change songs" while no swipe handler exists (never shown — unmounted — but a false promise in the codebase) + its orphaned keyframe.
- Reused the existing, already-tested `jumpToSong` (restores transposition/zoom/fitMode per target chart) — no new store state.

## Task Commits

Single squashed plan commit (per-plan pattern). See STATE.md Git State for the hash.

| Task | Type | Description |
|------|------|-------------|
| Task 1: WS-08 drawer jump | fix | QueueRow open → `jumpToSong(globalIndex)` not `router.push` |
| Task 2: WS-28 dead code | chore | delete SwipeOverlay.tsx + orphaned swipe-anim keyframe |
| Tests | test | new setlist-drawer.test.tsx (3 cases, virtualizer/store/router mocked) |

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/components/performance/SetlistDrawer.tsx` | Modified | WS-08: `jumpToSong` in destructure + queue-preserving open handler |
| `src/components/performance/__tests__/setlist-drawer.test.tsx` | Created | queue-preserving open proven (jumpToSong vs router.push, no-fileId no-op) |
| `src/app/globals.css` | Modified | WS-28: removed orphaned `swipe-anim` keyframe + reduced-motion rule |
| `src/components/performance/SwipeOverlay.tsx` | Deleted | WS-28: unmounted dead code (false swipe promise) |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| WS-08 reuses store `jumpToSong(globalIndex)` | `globalIndex` IS the playbackQueue index (proven by existing `isCurrent === queueIndex`); jumpToSong already restores transposition/zoom/fitMode | No new state; in-queue jump preserves Next/Prev |
| WS-17 DEFERRED → cycle-13a | The `currentTrackIndex`/`setCurrentPosition` plumbing is owned by the active v7.1 leader-broadcast lane, whose feature is the real fix; PAUL touching it would collide | Recorded in STATE deferred issues; boundary respected |
| WS-28 delete-only | Implementing swipe-to-navigate is a feature, out of this narrow hardening milestone | Logged as a v11.7 candidate |
| `router` import retained | Still used by the empty-queue public-picker `handleSelectSetlist` path | Only the QueueRow open handler changed |

## Deviations from Plan

### Summary

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 0 | — |

**Total impact:** Plan executed exactly as written (WS-17 was pre-deferred in the PLAN, not a deviation).

### Deferred Items

None new — WS-17's deferral to cycle-13a was baked into the PLAN.
