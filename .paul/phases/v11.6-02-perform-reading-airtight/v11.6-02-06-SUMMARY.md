---
phase: v11.6-02-perform-reading-airtight
plan: 06
subsystem: ui
tags: [perform, ipad, image, zoom, retry, zustand]

requires:
  - phase: v11.6-02-05
    provides: store `zoom` restored per-device on queue transition; PDF-only fit toggle (image charts excluded)
  - phase: v11.6-01
    provides: stress-triage report (WS-06/15 source+line cites)
provides:
  - Image charts honor store zoom (CSS zoom, pannable) (WS-06)
  - Image load-error Retry that re-attempts (remount) (WS-15)
affects: [v11.6-02-07 (drawer-nav, phase-02 close)]

tech-stack:
  added: []
  patterns:
    - "CSS `zoom` (not transform: scale) to scale + grow the layout box so an overflow-auto container can pan a zoomed image"
    - "Image retry via <img> remount (key=nonce), NOT a cache-bust query param — keeps signed Storage URLs valid"

key-files:
  created:
    - src/components/music/__tests__/image-score-viewer.test.tsx
  modified:
    - src/components/music/ImageScoreViewer.tsx

key-decisions:
  - "CSS `zoom` over transform: scale (grows scroll area → pannable)"
  - "Retry remounts the <img> (signature-safe) rather than cache-busting the url"
  - "role=alert on the error state (a11y announcement)"

patterns-established:
  - "Image viewer reads store zoom like PDF/text; zoom 1 = object-contain fit baseline"

duration: ~25min
started: 2026-06-18T10:55:00Z
completed: 2026-06-18T11:10:00Z
---

# Phase v11.6-02 Plan 06: Image Viewer Summary

**Image charts now honor the toolbar Zoom (CSS `zoom`, scrollable/pannable, fit-1 baseline unchanged) and a failed image load offers a Retry that re-attempts — matching PDF/text viewer parity.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~25 min |
| Tasks | 2 of 2 completed (all PASS) |
| Files modified | 2 (1 created) |
| Quality gates | tsc 0 · `next build --webpack` 0 · image-score-viewer 6/6 · pdf-overlay 28/28 (no regression) |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: Toolbar Zoom scales the image (WS-06) | Pass | `useMusicStore(s=>s.zoom)` → `style={{ zoom }}` in an `overflow-auto` container; zoom 1 = object-contain baseline; pannable when zoomed (CSS zoom grows the layout box). |
| AC-2: Scales from restored per-device zoom (WS-06) | Pass | Reads the same store `zoom` the queue transition restores from chartZoom; test asserts zoom 2 reflected. |
| AC-3: Failed load offers a re-attempting Retry (WS-15) | Pass | Error state is `role="alert"` + ≥44px Retry; click → loading + `reloadNonce` bump → `<img key={nonce}>` remounts → re-requests; subsequent load shows the image. |

## Accomplishments

- WS-06: image charts were the one viewer where the toolbar Zoom buttons were inert; they now scale the image and pan when zoomed past the screen.
- WS-15: replaced the dead "try refreshing" text with an announced, recoverable Retry (PDF/text parity).
- jsdom confirmed `style={{ zoom }}` renders unitless (`style.zoom === "1.5"`) and type-checks; no regression to `pdf-overlay` (28/28).

## Task Commits

Single squashed plan commit (per-plan pattern). See STATE.md Git State for the hash.

| Task | Type | Description |
|------|------|-------------|
| Task 1: WS-06 zoom | feat | ImageScoreViewer reads store zoom (CSS zoom, overflow-auto pan) |
| Task 2: WS-15 retry | feat | role=alert error + ≥44px Retry remounting <img> via reloadNonce |
| Tests | test | new image-score-viewer.test.tsx (6 cases) |

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/components/music/ImageScoreViewer.tsx` | Modified | WS-06 store-zoom scaling + WS-15 Retry |
| `src/components/music/__tests__/image-score-viewer.test.tsx` | Created | zoom application + retry recovery + url-change reset (6) |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| CSS `zoom` (not `transform: scale`) | Grows the layout box so overflow-auto can pan a zoomed image | iPad WebKit + Chrome supported; Firefox 126+ |
| Retry remounts `<img>` (no cache-bust) | Image urls can be signed Storage URLs whose signature covers the query string | Signature-safe re-attempt |
| `role="alert"` on error | a11y announcement (ui-ux guidance) | Error is announced, not visual-only |

## Deviations from Plan

### Summary

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 0 | — |

**Total impact:** Plan executed exactly as written.

### Deferred Items

None — plan executed exactly as written.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| Write tool refused to create the new test file ("File has not been read yet") | Created via Bash heredoc (absolute path) instead; no impact on output. |

## Next Phase Readiness

**Ready:**
- All Perform viewers (PDF, text, image) now honor store zoom; image + PDF + text all have recoverable load errors.

**Concerns:**
- CSS `zoom` pan behavior + real image decode are jsdom-unverifiable → real-iPad UAT (appended).
- Image charts have zero live weight this weekend (forward-hardening).

**Blockers:** None. Phase 02 has ONE plan left — Plan 07 (drawer-nav: WS-08 / WS-17 / WS-28) → phase-02 close.

---
*Phase: v11.6-02-perform-reading-airtight, Plan: 06*
*Completed: 2026-06-18*
