---
phase: v11.6-02-perform-reading-airtight
plan: 03
subsystem: ui
tags: [transpose, music-math, chord-spelling, slash-chord]

requires:
  - phase: v11.6-01-stress-sweep-triage
    provides: WS-25 finding (slash-bass spelled with root's scale)
provides:
  - Transposed slash-bass spelled Bb/Eb (never A#/D#) while keeping in-key context
affects: [v11.6-02-04]

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - src/lib/music-math.ts
    - src/lib/music-math.test.ts

key-decisions:
  - "Pass the root's useFlats as the bass preferFlats hint so 10/3 force-flat (never A#/D#) but in-key spellings (G/B→A/C#) are preserved — NOT a pure own-context respell (that would wrongly produce A/Db)."

patterns-established: []

duration: ~15min
started: 2026-06-17
completed: 2026-06-17
---

# Phase v11.6-02 Plan 03: WS-25 slash-bass spelling — Summary

**A transposed slash-chord bass no longer spells the never-used A#/D# (it spells Bb/Eb), while in-key bass spellings like G/B→A/C# are preserved.**

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: Transposed slash-bass never spells A#/D# (WS-25) | Pass | `transposeChord('F/A',1)`=`F#/Bb`, `transposeChord('F/D',1)`=`F#/Eb` (unit-tested) |
| AC-2: In-key slash-bass spelling unchanged | Pass | `G/B`+2=`A/C#`, `Am/G`+2=`Bm/A`, `D/F#`+2=`E/G#` (regression-locked) |

## What Was Built

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/music-math.ts` | Modified | Slash branch spells bass via `shouldUseFlats(newBassIndex, normalizedBassAcc, useFlats)` — Bb/Eb forced on pc 10/3, root's `useFlats` hint keeps in-key context |
| `src/lib/music-math.test.ts` | Modified | +2 it-blocks (WS-25 cases + regression-lock); 47 cases total, all green |

## Verification Results

- `npx vitest run src/lib/music-math.test.ts` → 47 passed
- `tsc --noEmit` → exit 0
- `SKIP_ENV_VALIDATION=1 npx next build --webpack` → exit 0

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Bass uses `shouldUseFlats(...,useFlats)` not a pure own-context respell | A pure respell would turn the correct in-key `A/C#` into `A/Db`; passing the root's choice as the hint keeps key context while the unconditional 10→Bb/3→Eb rules fix the A#/D# bug | No regression to existing slash spellings |

## Deviations from Plan

None — plan executed as written. WS-19/23/24 deferred to v11.7 (recorded in STATE Deferred Issues), as planned.

## Next Phase Readiness

**Ready:** Phase 02 text-chart work complete (Plans 02 + 03). Only Plan 04 remains (PDF/image + toolbar + drawer-nav).

**Concerns:** None — deterministic, fully unit-covered, no UAT dependency.

**Blockers:** None. Next: Plan 04 (WS-05 stuck-spinner, WS-07 multi-page PDF nav, WS-08 drawer nav-death, WS-22 transpose-unreachable + WS-06/14–18/26–28).

---
*Phase: v11.6-02-perform-reading-airtight, Plan: 03*
*Completed: 2026-06-17*
