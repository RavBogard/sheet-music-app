---
phase: v11.6-02-perform-reading-airtight
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/performance/SetlistDrawer.tsx
  - src/components/performance/__tests__/setlist-drawer.test.tsx
  - src/components/performance/SwipeOverlay.tsx
  - src/app/globals.css
autonomous: true
---

<objective>
## Goal
Close the FINAL drawer-nav defect of phase 02: opening a song from the in-chart
Setlist drawer must keep the setlist queue (Next/Prev) intact (WS-08, P1), and
remove the stale unmounted SwipeOverlay that promises a swipe gesture that
doesn't exist (WS-28).

## Purpose
WS-08 is the one remaining P1 in the phase: mid-service, a musician opens the
drawer to jump to another song and lands on a dead-end single-chart route ("Song
1 of 1", greyed chevrons) — losing the whole set. This is the last "reading
airtight" gap. WS-28 is dead-code hygiene that removes a misleading promise from
the codebase. This plan's UNIFY closes phase v11.6-02.

## Output
- `SetlistDrawer.tsx`: the in-chart QueueRow open handler jumps within the live
  queue (`jumpToSong`) instead of `router.push` to the single-chart route.
- New/updated `setlist-drawer.test.tsx` proving the queue-preserving open.
- `SwipeOverlay.tsx` deleted; its orphaned `swipe-anim` keyframe removed from
  globals.css.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md
@.paul/research/v11-6-01-stress-triage-REPORT-2026-06-17.md

## Prior Work (genuinely needed)
# WS-01 (Plan 01) made the PDFOverlay playbackQueue include every fileId-bearing
# row (incl. bonded prayer/reading) — so jumpToSong to a flow row opens its chart.
# The store's jumpToSong(index) restores transposition/zoom/fitMode for the target
# (verified by chart-zoom-calibration + fit-mode tests).
@.paul/phases/v11.6-02-perform-reading-airtight/v11.6-02-01-SUMMARY.md

## Source Files
@src/components/performance/SetlistDrawer.tsx
@src/lib/store.ts
@src/components/performance/SwipeOverlay.tsx
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /ui-ux-pro-max | required | Before touching the Perform drawer surface | ○ |

**BLOCKING:** /ui-ux-pro-max MUST be loaded before APPLY proceeds (Perform UI).

## Skill Invocation Checklist
- [ ] /ui-ux-pro-max loaded (run command or confirm)
</skills>

<acceptance_criteria>

## AC-1: Drawer song-open keeps the setlist queue (WS-08, P1)
```gherkin
Given a chart is open in Perform (playbackQueue populated) and the in-chart
  Setlist drawer is open
When the player taps another song row (a row with a fileId)
Then the app jumps to that song WITHIN the existing queue (store queueIndex
  becomes that row's index) and the drawer closes
And it does NOT router.push to the single-chart /perform/<fileId> route
And Next/Prev still traverse the full setlist (queue length unchanged)
```

## AC-2: Non-openable rows still no-op (WS-08 guard)
```gherkin
Given the drawer is open
When the player taps a row with no fileId (an unbonded flow item)
Then nothing happens (no jump, no navigation) — unchanged from today
```

## AC-3: SwipeOverlay removed (WS-28)
```gherkin
Given SwipeOverlay is unmounted dead code (rendered nowhere in src)
When the codebase is built
Then SwipeOverlay.tsx no longer exists, its orphaned `swipe-anim` keyframe is
  removed from globals.css, and no import references remain
And the build + typecheck pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: WS-08 — drawer open jumps within the queue (no route nav)</name>
  <files>src/components/performance/SetlistDrawer.tsx, src/components/performance/__tests__/setlist-drawer.test.tsx</files>
  <action>
    In `SetlistDrawer`, the drawer's rows are built directly from the store
    `playbackQueue` (`globalIndex` IS the playbackQueue index — confirmed by the
    existing `isCurrent = globalIndex === queueIndex`). The in-chart QueueRow
    `onOpen` (currently ~SetlistDrawer.tsx:402-406) does
    `router.push(`/perform/${track.fileId}`)` → the standalone single-chart route
    with `onNavigate={()=>{}}`, which drops the queue and kills Next/Prev.
    - Add `jumpToSong` to the `useMusicStore()` destructure (alongside
      `playbackQueue, queueIndex, setQueue`).
    - Change the QueueRow `onOpen` to jump within the live queue:
        onOpen={() => {
          if (!track.fileId) return
          jumpToSong(globalIndex)   // stay in the setlist Perform surface
          setOpen(false)
        }}
      Keep the `!track.fileId` early-return (non-openable rows stay no-op).
    - Leave `router` in place (still used by `handleSelectSetlist` for the
      empty-queue public-picker path); only the QueueRow open handler changes.
    - TEST: add `setlist-drawer.test.tsx`. Mock `@tanstack/react-virtual`'s
      `useVirtualizer` to expose all items as virtual rows (so jsdom renders the
      QueueRows), mock `@/lib/store` (populated playbackQueue + `jumpToSong` spy +
      `queueIndex`), mock `next/navigation` (router.push spy), and the
      auth/org/setlist-firebase/client-tracks/local-schema deps. Assert: clicking
      a song row calls `jumpToSong(thatIndex)` and does NOT call `router.push`;
      clicking a no-fileId row calls neither.
    Avoid: changing the empty-queue public-picker `handleSelectSetlist` flow;
    touching QueueRow's render contract.
  </action>
  <verify>`npx vitest run setlist-drawer` AC-1/AC-2 pass; `npx tsc --noEmit` 0.</verify>
  <done>AC-1 + AC-2 satisfied: drawer open jumps in-queue, Next/Prev preserved, no-fileId rows no-op.</done>
</task>

<task type="auto">
  <name>Task 2: WS-28 — delete the stale SwipeOverlay + orphaned keyframe</name>
  <files>src/components/performance/SwipeOverlay.tsx, src/app/globals.css</files>
  <action>
    SwipeOverlay is unmounted dead code — no component imports/renders it (only
    its own file + the `swipe-anim` keyframe in globals.css reference it). It
    advertises "Swipe left or right to change songs" but there is no swipe
    handler and nav is chevron-only, so the promise is false (though never shown,
    since it's unmounted).
    - First RE-CONFIRM no live import: grep `SwipeOverlay` across `src/**/*.tsx`
      — expect zero render sites (only the component file). If a render site is
      found, STOP and report (do not delete a mounted component).
    - Delete `src/components/performance/SwipeOverlay.tsx`.
    - Remove the now-orphaned `@keyframes swipe-anim` block from
      `src/app/globals.css` (grep-confirm `swipe-anim` has no other consumer).
    Avoid: implementing swipe-to-navigate (out of scope — narrow hardening, not a
    feature; recorded as a v11.7 candidate); removing any keyframe still in use.
  </action>
  <verify>`grep -rn "SwipeOverlay\|swipe-anim" src` returns nothing; `npx tsc --noEmit` 0; `SKIP_ENV_VALIDATION=1 npx next build --webpack` 0.</verify>
  <done>AC-3 satisfied: SwipeOverlay.tsx gone, orphaned keyframe removed, no references, build clean.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `currentTrackIndex` / `setCurrentPosition` in `use-setlist-performance.ts` and
  their `SetlistView`/`SetlistPerformClient` wiring — WS-17 is DEFERRED: that
  leader-position plumbing is owned by the active parallel v7.1 cycle-13a
  leader-broadcast workstream, whose leader-broadcast feature is the real fix
  (makes `currentTrackIndex` live). Touching it here would collide.
- QueueRow's render/affordance contract (the bonded-flow-item glyph + current
  highlight) — only the open handler wiring changes.
- The empty-queue public-setlist picker path (`handleSelectSetlist`).

## SCOPE LIMITS
- No swipe-gesture implementation (WS-28 is delete-only).
- No leader-broadcast / live-position work (cycle-13a owns it).
- No new store state; reuse the existing `jumpToSong`.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] /ui-ux-pro-max was loaded
- [ ] `npx vitest run setlist-drawer` green (queue-preserving open proven)
- [ ] `grep -rn "SwipeOverlay\|swipe-anim" src` returns nothing
- [ ] `npx tsc --noEmit` exits 0
- [ ] `SKIP_ENV_VALIDATION=1 npx next build --webpack` exits 0
- [ ] Real-iPad UAT item appended to .paul/UAT-PENDING.md (drawer jump preserves Next/Prev)
- [ ] All acceptance criteria (AC-1..AC-3) met
</verification>

<success_criteria>
- WS-08 closed (drawer open preserves the setlist queue); WS-28 closed (dead code removed)
- WS-17 explicitly deferred to cycle-13a (recorded in SUMMARY + STATE deferred issues)
- Build + types + tests clean; committed and pushed to `origin master`
- This is the LAST plan of phase 02 → UNIFY triggers the phase-02 transition
</success_criteria>

<output>
After completion, create `.paul/phases/v11.6-02-perform-reading-airtight/v11.6-02-07-SUMMARY.md`
</output>
