---
phase: 01-mobile-admin-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/UserRow.tsx
autonomous: true
---

<objective>
## Goal
Add sound engineer and canLiveSwap toggle buttons to the mobile UserRow section, so admins can manage permissions from their phone during live services.

## Purpose
Rabbi Daniel manages musician permissions from his phone during services. The toggle buttons are currently hidden below the sm breakpoint, making this impossible on mobile.

## Output
UserRow mobile section with functional toggle buttons and visible permission badges.
</objective>

<context>
@src/components/admin/UserRow.tsx
</context>

<acceptance_criteria>

## AC-1: Toggle buttons visible on mobile
```gherkin
Given a band leader viewing the People section on a phone (<640px)
When they see a UserRow for a musician
Then sound engineer and canLiveSwap toggle buttons are visible and tappable
And the buttons have 44px minimum touch targets
```

## AC-2: Badges visible on mobile
```gherkin
Given any admin viewing UserRow on mobile
When they look at a musician's row
Then role badge, sound engineer badge, and swap badge are visible
```

## AC-3: No desktop regression
```gherkin
Given the desktop layout (>=640px)
When viewing UserRow
Then the layout is unchanged from before
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add toggle buttons and delete to mobile UserRow section</name>
  <files>src/components/admin/UserRow.tsx</files>
  <action>
    The mobile actions section (lines 315-346, `sm:hidden`) currently shows:
    - Left: role/permission badges
    - Right: role dropdown only
    
    Add the sound engineer, canLiveSwap, and delete toggle buttons between the badges and the role dropdown. Use the same button components as the desktop version but ensure 44px touch targets.
    
    Layout for mobile actions row:
    ```
    [badges] .................. [🎧] [🔄] [🗑] [Role ▼]
    ```
    
    Copy the same conditional rendering logic from the desktop section (lines 232-287) for the three toggle buttons. The buttons already use size="icon" which gives 44px.
  </action>
  <verify>npx tsc --noEmit; grep "handleSoundEngineerToggle\|handleLiveSwapToggle\|handleDelete" src/components/admin/UserRow.tsx | wc -l should show 6+ (3 desktop + 3 mobile)</verify>
  <done>AC-1, AC-2, AC-3 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Desktop layout (the `hidden sm:flex` sections)
- Role change AlertDialog (Phase 1 from v3.1)
- handleSoundEngineerToggle / handleLiveSwapToggle / handleDelete logic

## SCOPE LIMITS
- Only modify the mobile section of UserRow
- No new components or files
</boundaries>

<verification>
- [ ] npx tsc --noEmit passes
- [ ] npx vitest run — no regressions
- [ ] Toggle buttons appear in mobile section
- [ ] Desktop layout unchanged
</verification>

<success_criteria>
- Mobile UserRow has sound engineer + canLiveSwap + delete toggles
- 44px touch targets on all buttons
- No regressions
</success_criteria>

<output>
After completion, create `.paul/phases/01-mobile-admin-controls/01-01-SUMMARY.md`
</output>
