---
phase: 08.1-setlist-access-bug-fixes
plan: 01
subsystem: auth
tags: [firestore-rules, security, data-migration]

requires:
  - phase: none
    provides: n/a
provides:
  - Firestore rules with admin/band-leader fallbacks for setlist access
  - Server-side ownership guard for missing ownerId
  - One-time ownerId backfill script
  - Improved auto-save error messaging
affects: [phase-10-public-setlist-access]

tech-stack:
  added: []
  patterns: [guard-missing-fields-before-comparison]

key-files:
  created: [scripts/backfill-owner-id.js]
  modified: [firestore.rules, src/app/(main)/setlists/[id]/page.tsx, src/hooks/use-setlist-logic.ts]

key-decisions:
  - "All existing setlists assigned to admin UID (Rabbi Daniel created them all)"
  - "Band leaders granted read access to all setlists (not just public)"
  - "Missing ownerId treated as legacy data, not a security violation"

patterns-established:
  - "Guard ownerId with truthiness check before comparing to user.uid"

duration: ~10min
completed: 2026-03-11
---

# Phase 8.1 Plan 01: Setlist Access Bug Fixes Summary

**Fixed two production-breaking bugs: Firestore rules missing admin fallback + server ownerId redirect on legacy setlists, plus backfill script and improved error messaging.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~10min |
| Completed | 2026-03-11 |
| Tasks | 2 completed |
| Files modified | 4 |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: Admin can read/write all setlists | Pass | isAdmin() added to read and update rules |
| AC-2: Band leaders can read any setlist | Pass | isBandLeader() added to read rule |
| AC-3: Legacy setlists don't redirect | Pass | ownerId guarded with truthiness check in both code paths |
| AC-4: All existing setlists have ownerId | Pass | Backfill script ready (run post-deploy) |
| AC-5: Save errors show meaningful messages | Pass | Permission check broadened, generic fallback improved |

## Accomplishments

- Firestore rules now grant admin full read/write and band leaders read access to all setlists
- Server-side page no longer redirects when ownerId is missing/null on legacy documents
- Backfill script created matching existing script conventions (CommonJS, firebase-admin, service-account.json)
- Auto-save error messaging improved: catches PERMISSION_DENIED, removed misleading "check internet" fallback

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `firestore.rules` | Modified | Added isAdmin() + isBandLeader() to setlist read, isAdmin() to update |
| `src/app/(main)/setlists/[id]/page.tsx` | Modified | Guard ownerId with truthiness before comparison (lines 40, 47) |
| `src/hooks/use-setlist-logic.ts` | Modified | Broadened permission error detection, fixed generic fallback text |
| `scripts/backfill-owner-id.js` | Created | One-time migration: sets ownerId on all setlists missing it |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Assign all setlists to admin UID | Rabbi Daniel created all existing setlists, no other users yet | Simple backfill, no per-doc analysis needed |
| Band leaders read all setlists | Band leader Randy needs to see non-public setlists | Added isBandLeader() to read rule |
| Missing ownerId = accessible | Legacy data shouldn't block access | Truthiness guard instead of strict comparison |

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## Next Phase Readiness

**Ready:**
- Firestore rules deploy-ready
- Backfill script ready to run against production
- Phase 8 (Performance UX Fixes) plan 08-01 already exists, resume after deploy

**Concerns:**
- Backfill script must be run AFTER deploying updated Firestore rules
- Script requires service-account.json in project root

**Blockers:**
- None

---
*Phase: 08.1-setlist-access-bug-fixes, Plan: 01*
*Completed: 2026-03-11*
