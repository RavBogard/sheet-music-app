---
phase: 04-quality-deps-v15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - eslint.config.mjs
  - src/components/home/__tests__/next-service-card.test.tsx
  - src/components/monitor/__tests__/connection-status.test.ts
  - src/components/monitor/__tests__/fader-interaction.test.ts
  - src/components/monitor/__tests__/graceful-degradation.test.tsx
  - src/components/monitor/__tests__/mute-toggle.test.ts
  - src/components/performance/__tests__/pdf-overlay.test.tsx
  - src/components/performance/__tests__/public-view.test.tsx
  - src/components/performance/__tests__/setlist-view.test.tsx
  - src/components/setlist/__tests__/flow-item-editing.test.tsx
  - src/components/setlist/__tests__/inline-editing.test.tsx
  - src/lib/prefetch.test.ts
autonomous: true
---

<objective>
## Goal
Fix vitest default environment to jsdom, re-enable ESLint exhaustive-deps rule, and confirm all remaining Phase 4 roadmap items are already resolved.

## Purpose
Eliminates per-file test environment boilerplate and restores a safety-net lint rule, completing the quality & deps phase.

## Output
- vitest.config.ts default: jsdom
- 11 test files: @vitest-environment comments removed
- ESLint exhaustive-deps: re-enabled as "warn"
- Phase 4 complete (deps, fonts, indexes already current)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@vitest.config.ts
@eslint.config.mjs
</context>

<acceptance_criteria>

## AC-1: Vitest default environment is jsdom
```gherkin
Given vitest.config.ts has environment: 'node'
When environment is changed to 'jsdom'
Then all 11 test files with @vitest-environment jsdom overrides can have those comments removed
And all tests pass with npx vitest run
```

## AC-2: ESLint exhaustive-deps re-enabled
```gherkin
Given eslint.config.mjs has "react-hooks/exhaustive-deps": "off"
When rule is changed to "warn"
Then npx eslint src/ produces zero warnings (codebase already clean)
```

## AC-3: All tests pass
```gherkin
Given both changes are applied
When npx vitest run is executed
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Change vitest default to jsdom and remove overrides</name>
  <files>vitest.config.ts, 11 test files listed in files_modified</files>
  <action>
    1. In vitest.config.ts, change `environment: 'node'` to `environment: 'jsdom'`
    2. In each of the 11 test files, remove the `// @vitest-environment jsdom` comment line
    3. Do NOT change any test logic or assertions
  </action>
  <verify>npx vitest run — all tests pass</verify>
  <done>AC-1 and AC-3 satisfied: Default jsdom, overrides removed, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Re-enable ESLint exhaustive-deps as warn</name>
  <files>eslint.config.mjs</files>
  <action>
    1. In eslint.config.mjs, change `"react-hooks/exhaustive-deps": "off"` to `"react-hooks/exhaustive-deps": "warn"`
    2. Run eslint to verify zero warnings
  </action>
  <verify>npx eslint src/ produces zero warnings</verify>
  <done>AC-2 satisfied: Rule re-enabled, codebase clean</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any test assertions or test logic
- Any hook implementations
- package.json dependencies (already current)
- Font configuration (already optimized)
- firestore.indexes.json (already complete)

## SCOPE LIMITS
- This plan covers all Phase 4 roadmap items (most are already resolved)
- No dependency upgrades needed (all current)
- No font changes needed (already subset)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run` — all tests pass
- [ ] `npx eslint src/` — zero errors/warnings
- [ ] `npx tsc --noEmit` — zero type errors
- [ ] No @vitest-environment jsdom comments remain in test files
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- vitest.config.ts default is jsdom
- ESLint exhaustive-deps is "warn" with zero violations
</success_criteria>

<output>
After completion, create `.paul/phases/04-quality-deps-v15/04-01-SUMMARY.md`
</output>
