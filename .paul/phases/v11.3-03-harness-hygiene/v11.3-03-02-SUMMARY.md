---
phase: v11.3-03-harness-hygiene
plan: 02
subsystem: testing
tags: [mcp, test-isolation, library_index, orphan-sweep, firestore, admin-sdk, hygiene]

requires:
  - phase: v11.3-03-01
    provides: same phase (harness & hygiene); independent fix
provides:
  - sweep_orphan_test_data now covers orphan library_index rows (owner-absent test-shape uploadedBy) + best-effort Storage purge
  - scripts/v11-3-03-library-orphan-sweep.mjs (prod probe/sweep; dry-run default, --diagnose, --apply)
affects: [future test-data hygiene; any orphan-sweep extension]

tech-stack:
  added: []
  patterns: [orphan = test-shape owner uid + absent users/{uid}; reuse bestEffortStorageDelete for library_index; admin-SDK via firebase-CLI refresh-token ADC]

key-files:
  created:
    - scripts/v11-3-03-library-orphan-sweep.mjs
  modified:
    - src/lib/mcp/tools/test-tokens.ts
    - src/lib/mcp/__tests__/mcp-test-tokens.emulator.test.ts

key-decisions:
  - "library_index orphan owner field is uploadedBy (matches CASCADE_FIELDS revoke path); doc id == fileId for best-effort Storage purge."
  - "Prod delete (Task 3) was a no-op — verify-first probe proved both BUG-1 rows already cleaned out-of-band; durable code coverage is the deliverable."

patterns-established:
  - "Orphan-sweep coverage now spans setlists + setlistTemplates + library_index; the owner-absent test-shape predicate is the single source of truth."

duration: ~35min
started: 2026-06-10T20:45:00Z
completed: 2026-06-10T21:20:00Z
---

# Phase v11.3-03 Plan 02: BUG-1 — library_index orphan coverage Summary

**`sweep_orphan_test_data` now also sweeps owner-absent test-shape `library_index` rows (+ best-effort Storage bytes), closing the cleanup-cascade gap — and a verify-first prod probe confirmed the two `[role-*]` rows from the stress report were already cleaned out-of-band, so no prod delete was needed.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~35 min |
| Tasks | 2 auto PASS + 1 checkpoint resolved; Task 3 (prod-delete) no-op |
| Files | 2 modified + 1 created |
| Gates | tsc clean · emulator 36/36 (incl. 2 new library_index cases) · `SKIP_ENV_VALIDATION=1 next build` OK |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: library_index orphans swept (§BUG-1) | Pass | emulator: orphan listed in dryRun, deleted on force (swept.library_index===1), best-effort Storage attempted |
| AC-2: no false positives (live owner / non-test uid) | Pass | emulator: live-owner row + non-test-uid row neither listed nor deleted |
| AC-3: two CRC [role-*] prod orphans removed | N/A — already clean | verify-first probe: 0 orphans across 713 rows; ids `upload-0d872e08-…`/`upload-cef9ddf9-…` absent (3-way confirm) — cleaned out-of-band since run-1 |
| AC-4: verify-first + force-gated | Pass | dry-run default; probe ran first; no write performed (nothing to delete) |

## Accomplishments

- Extended `sweepOrphanTestDataCore`: added a `library_index` scan (owner field `uploadedBy`), reusing the existing `orphanFilter` (test-shape + absent `users/{uid}`); on real sweep, deletes orphan docs and calls the existing `bestEffortStorageDelete` (doc id == fileId). Extended `scanned`/`swept`/`orphans[]` types + `storageDeleted`/`storageFailed`, and the tool description.
- 2 new emulator regression cases (orphan swept; live-owner + non-test rows spared), citing run-1 §BUG-1.
- `scripts/v11-3-03-library-orphan-sweep.mjs` — prod probe/sweep with admin-SDK auth via the firebase-CLI refresh-token → temp `authorized_user` ADC (no SA creds on this box); dry-run default, `--diagnose`, `--apply`.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/mcp/tools/test-tokens.ts` | Modified | `library_index` added to orphan sweep + types + tool description |
| `src/lib/mcp/__tests__/mcp-test-tokens.emulator.test.ts` | Modified | +2 library_index orphan regression cases (§BUG-1) |
| `scripts/v11-3-03-library-orphan-sweep.mjs` | Created | Prod probe/sweep (dry-run / --diagnose / --apply) |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Reuse `bestEffortStorageDelete` + `orphanFilter` | Mirrors the revoke path's `library_index`+storage semantics; no new abstraction | Consistent behavior; minimal surface |
| Skip the prod delete (Task 3) | Verify-first proved the two orphans already gone (no rows to delete) | No prod write; durable coverage is the deliverable |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 1 | `--diagnose` mode added to the script to characterize the [role-*] rows when the orphan filter matched 0 — investigation aid, no behavior change |
| Deferred | 0 | — |

### Notable deviation (benign — reality diverged from plan premise)

**Task 3 prod-delete was a no-op.** The plan assumed the two `[role-*] tiny` rows
(`upload-0d872e08-…`, `upload-cef9ddf9-…`, both `orgId:"crc"`) from run-1 §BUG-1 were still
live. The verify-first dry-run probe found **0** test-shape owner-absent `library_index`
orphans across all 713 CRC rows; the two specific ids are absent (confirmed by stable
id-prefix, title scan, and the orphan filter). They were cleaned out-of-band since the
2026-06-10 run (loginable-test-accounts phase / v11.2-04-02 hygiene / a prior revoke). The
human-verify checkpoint's purpose (gating a destructive write) was therefore moot — no write
performed. The durable coverage fix stands regardless.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| Orphan filter matched 0 (expected 2) | Added `--diagnose` + exact id-prefix check → confirmed rows already gone; no action needed |
| No SA creds on this box | Script mints temp `authorized_user` ADC from firebase-CLI refresh token (logged in as daniel@centralreform.org) |

## Next Phase Readiness

**Ready:**
- Phase v11.3-03 complete (both plans). Next: v11.3-04 (/perform performance — BUG-2; VERIFY-FIRST cold vs steady CLS).

**Concerns:**
- None. The orphan-sweep coverage is durable; recurrence is now caught.

**Blockers:** None.

---
*Phase: v11.3-03-harness-hygiene, Plan: 02*
*Completed: 2026-06-10*
