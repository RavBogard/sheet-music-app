---
phase: v11.3-03-harness-hygiene
plan: 01
subsystem: auth
tags: [proxy, middleware, edge, qr, error-contract, public-routes, vitest]

requires:
  - phase: loginable-test-accounts
    provides: the /test-login headless sign-in page + /api/auth/qr poll endpoint it calls
provides:
  - /test-login reachable unauth (public allow-list entry, exact match)
  - GET /api/auth/qr returns 4xx (not 500) for malformed ?code
affects: [v11.3-03-02 (BUG-1, same phase), any future public-route or QR change]

tech-stack:
  added: []
  patterns: [exact-match public route over prefix to avoid sibling over-broadening; validate caller input to 4xx before backend access]

key-files:
  created:
    - src/app/api/auth/qr/__tests__/route.test.ts
  modified:
    - src/proxy.ts
    - src/__tests__/proxy-auth.test.ts
    - src/app/api/auth/qr/route.ts

key-decisions:
  - "BUG-9 fixed via publicExactRoutes (exact), NOT publicPrefixes — /test-login has no child routes; prefix would expose /test-login-* siblings."
  - "BUG-7 reuses the POST/PUT regex /^[A-Z0-9]{6}$/ and validates BEFORE initAdmin() so legit codes still pass and the test needs no emulator."

patterns-established:
  - "Public pre-auth pages whose capability is a single-use code go in publicExactRoutes."
  - "API GET handlers validate caller-supplied identifiers to 400 before any Firestore .doc() lookup."

duration: ~20min
started: 2026-06-10T20:24:00Z
completed: 2026-06-10T20:44:00Z
---

# Phase v11.3-03 Plan 01: Harness & hygiene (BUG-9 + BUG-7) Summary

**`/test-login` is now public (so the harness can consume its `?code`), and `GET /api/auth/qr` with a malformed code returns 400 instead of crashing the Firestore lookup into a 500 — each with a regression test citing its stress-report cell.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~20 min |
| Tasks | 2 completed (2 PASS) |
| Files modified | 3 modified + 1 created |
| Gates | tsc clean · vitest 33/33 · `SKIP_ENV_VALIDATION=1 next build` OK |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: unauth /test-login passes through (§BUG-9) | Pass | proxy test: `location` null for `/test-login` and `/test-login?code=ABC123` |
| AC-2: allow-list not over-broadened (exact, not prefix) | Pass | proxy test: `/test-login-elsewhere` still → `/login` |
| AC-3: malformed code → 400 pre-Firestore (§BUG-7) | Pass | qr test: `?code=foo%2Fbar` → 400 "Invalid code format", `collection` never called |
| AC-4: valid + missing code unregressed | Pass | qr test: missing → 400 "Missing code"; valid `ABC123` → 404 lookup (not 400) |

## Accomplishments

- **BUG-9:** added `'/test-login'` to `publicExactRoutes` in `src/proxy.ts` (exact match, documented rationale). Unauth visits no longer 307 to `/login` before the page reads `?code`.
- **BUG-7:** `GET /api/auth/qr` now rejects any `code` failing `/^[A-Z0-9]{6}$/` with a 400 **before** `initAdmin()`/Firestore — killing the odd-segment `.doc(code)` throw that surfaced as a 500.
- Two regression suites: 3 new proxy cases (§BUG-9) + a new co-located QR route test with 3 cases (§BUG-7). Both re-runnable from the stress prompt's cells.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/proxy.ts` | Modified | `/test-login` → `publicExactRoutes` (exact, with comment) |
| `src/__tests__/proxy-auth.test.ts` | Modified | +3 BUG-9 cases (passthrough, ?code passthrough, sibling-gated) |
| `src/app/api/auth/qr/route.ts` | Modified | GET validates `code` format → 400 before Firestore |
| `src/app/api/auth/qr/__tests__/route.test.ts` | Created | +3 BUG-7 cases (malformed→400, missing→400, valid→404) |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Exact-match (`publicExactRoutes`) not prefix | `/test-login` has no children; prefix would expose `/test-login-*` | Tightest correct public surface; ROADMAP's "publicPrefixes" phrasing superseded |
| Validate before `initAdmin()` | 400 must precede backend access; keeps the unit test emulator-free | QR GET test is hermetic |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 1 | Test-only type fix, no scope change |
| Scope additions | 0 | — |
| Deferred | 0 | — |

### Auto-fixed Issues

**1. [test] `checkRateLimit` mock spread-type error (TS2556)**
- **Found during:** Task 2 qualify (tsc step)
- **Issue:** `vi.fn(async () => null)` gave a no-arg signature; `(...args) => mock(...args)` spread failed typecheck.
- **Fix:** typed the mock `vi.fn<(...args: unknown[]) => Promise<unknown>>(...)` (mirrors `admin-test-session` route test).
- **Verification:** `npx tsc --noEmit` exit 0; qr test 3/3 re-run green.

## Skill Audit

`.paul/SPECIAL-FLOWS.md` requires `/ui-ux-pro-max` only for frontend UI/UX phases. This plan
touched edge middleware + one API route + vitest only — **no UI**, so no required skill applied. No gap.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| None | — |

## Next Phase Readiness

**Ready:**
- Phase v11.3-03 Plan 02 (BUG-1) — the cleanup-cascade-coverage finding is captured: `CASCADE_FIELDS` already cascades `library_index` by `uploadedBy`, but `sweep_orphan_test_data` covers only setlists/templates → the two `[role-*]` orphans are owner-record-absent rows it can't reach. Plan 02 extends the sweep to `library_index` (+ emulator test) then deletes the two prod orphans (VERIFY-FIRST live probe).

**Concerns:**
- Plan 02 includes a destructive prod data op (delete 2 orphan rows) — single-owner executor, verify-first probe before deletion.

**Blockers:** None. Phase commit happens at phase close (after Plan 02).

---
*Phase: v11.3-03-harness-hygiene, Plan: 01*
*Completed: 2026-06-10*
