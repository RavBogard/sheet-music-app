---
phase: v11.6-04-authoring-publish
plan: 01
subsystem: publish-delivery
tags: [notifications, publish, qr-signin, react, firestore, mcp]

requires:
  - phase: v11.6-01
    provides: WS-11 + WS-31 stress-triage findings (deliver/access gaps)
provides:
  - In-app publish notifications link to the live /perform/setlist/{id} route (WS-11)
  - QR phone page recovers from a not-yet-registered (404) session via bounded retry (WS-31)
affects: [v11.6-04-02 (WS-21 QR auth STOP-gate), v11.6-04-03 (finalize + live send)]

tech-stack:
  added: []
  patterns:
    - "Distinguish 404 (not-registered-yet → retry) from 410 (expired → terminal) on the QR validity poll"

key-files:
  created:
    - src/app/qr/[code]/__tests__/qr-code-page.test.tsx
  modified:
    - src/lib/mcp/tools/setlist-publish.ts
    - src/app/api/setlist/publish/route.ts
    - src/app/qr/[code]/page.tsx
    - src/lib/mcp/__tests__/mcp-publish-setlist.emulator.test.ts

key-decisions:
  - "WS-31 retry stays in the existing 'loading' state (Checking session…) — no new UI, no blank screen"
  - "Persistent 404 past the recovery window resolves to 'expired' (the iPad never registered)"

patterns-established:
  - "QR validity poll: 410 → expired immediately; 404 → bounded ~5s retry; ok → ready; else → error"

duration: ~50min
started: 2026-06-18T21:30:00Z
completed: 2026-06-18T22:19:00Z
---

# Phase v11.6-04 Plan 01: Deliver-airtight (WS-11 + WS-31) Summary

**The in-app "setlist published" bell now opens the real Perform setlist route (was a dead 404), and the QR sign-in phone page recovers from an early scan instead of falsely declaring the code expired.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~50 min |
| Started | 2026-06-18T21:30:00Z |
| Completed | 2026-06-18T22:19:00Z |
| Tasks | 2 completed |
| Files modified | 4 modified + 1 created |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: In-app bell notification opens the setlist (WS-11) | Pass | Both in-app `link:` sites → `/perform/setlist/{id}`; grep shows zero `/setlist/{id}` dead links; publish emulator test asserts the written notification doc link starts with `/perform/setlist/`. |
| AC-2: Early QR scan recovers instead of falsely expiring (WS-31) | Pass | 404→200 sequence ends in "ready" (Sign In with Google), staying on "Checking session…" during the retry; ≥2 fetches confirm the retry fired. |
| AC-3: Genuinely expired/used session still shows expired (WS-31 guard) | Pass | 410 → "expired" heading immediately, single fetch (no retry); persistent-404 → "expired" after a bounded (>1, ≤6) number of retries. |

## Accomplishments

- WS-11 (P1): closed the dead-link gap on the highest-traffic publish channel — the in-app bell — so a musician who taps it lands on the setlist, matching the already-correct push/SMS links.
- WS-31 (P3): the QR sign-in page no longer punishes an early scan; a phone that opens the link before the iPad's background registration POST lands now retries silently and recovers.
- Regression coverage that the Phase-01 sweep can re-run: the publish emulator suite asserts the in-app link target, and a new `qr-code-page.test.tsx` covers all three QR poll outcomes with fake timers.

## Task Commits

Single plan commit (project convention — code + tests + PLAN + SUMMARY together):

| Task | Type | Description |
|------|------|-------------|
| Task 1: WS-11 in-app notification link | fix | Both in-app `link:` fields → `/perform/setlist/{id}` + emulator link assertion |
| Task 2: WS-31 QR 404 retry | fix | Bounded 404-vs-410 retry on the QR validity poll + new page test |

Commit hash recorded in STATE.md Git State after push.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/lib/mcp/tools/setlist-publish.ts` | Modified | In-app notification `link:` → `/perform/setlist/${args.setlistId}` (WS-11) |
| `src/app/api/setlist/publish/route.ts` | Modified | In-app notification `link:` → `/perform/setlist/${setlistId}` (WS-11) |
| `src/app/qr/[code]/page.tsx` | Modified | Validity poll distinguishes 404 (bounded retry) from 410 (expired) (WS-31) |
| `src/lib/mcp/__tests__/mcp-publish-setlist.emulator.test.ts` | Modified | Asserts in-app notification link starts with `/perform/setlist/` |
| `src/app/qr/[code]/__tests__/qr-code-page.test.tsx` | Created | 3 cases: 404→200 recovers, 410 expired, persistent-404 expired |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| WS-31 retry reuses the existing "loading" state | No new UI; the page already shows "Checking session…" — clean, no blank screen, no layout shift (ui-ux-pro-max) | Minimal surface change; CRC byte-identical |
| Persistent 404 → "expired" | A 404 that never clears means the iPad never registered the session — functionally expired from the phone's view | Bounds the retry; no infinite poll |

## Deviations from Plan

### Summary

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 1 | Test-assertion selector specificity (no product impact) |
| Scope additions | 0 | — |
| Deferred | 0 | — |

**Total impact:** None on product. One test assertion was made more specific.

### Auto-fixed Issues

**1. [Test] `/code expired/i` matched two elements**
- **Found during:** Task 2 (QR page test, first run)
- **Issue:** `getByText(/code expired/i)` matched both the `<h1>` and the body copy → ambiguous-match error.
- **Fix:** switched to `getByRole("heading", { name: /sign-in code expired/i })`.
- **Files:** `src/app/qr/[code]/__tests__/qr-code-page.test.tsx`
- **Verification:** test file 3/3 green after the change.

### Deferred Items

None — plan executed as written.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| Emulator tests require a running Firestore+Auth emulator | Ran via `firebase emulators:exec` (project's `test:emulator` pattern) — 29/29 green |

## Verification Results

- `grep "link: \`/setlist/"` on both files → no matches (zero dead-route links remain).
- Publish emulator suite: **29/29** (incl. the new in-app link assertion in the happy-path fan-out).
- `qr-code-page.test.tsx`: **3/3**.
- `npx tsc --noEmit` → exit 0.
- `SKIP_ENV_VALIDATION=1 npx next build --webpack` → exit 0.

Skill audit: `/ui-ux-pro-max` invoked before Task 2 (required by SPECIAL-FLOWS.md) ✓.

## Next Phase Readiness

**Ready:**
- Deliver path (in-app bell + QR sign-in resilience) is airtight for the weekend.
- Plan 02 (WS-21 QR approval claim-lag remedy) is next — a **STOP-gate** (auth/permission change, `autonomous: false`): requires approval before APPLY.

**Concerns:**
- None. WS-21 touches the QR approval gate in `src/app/api/auth/qr/route.ts` — explicitly out of scope here and protected by this plan's boundaries.

**Blockers:**
- None.

---
*Phase: v11.6-04-authoring-publish, Plan: 01*
*Completed: 2026-06-18*
