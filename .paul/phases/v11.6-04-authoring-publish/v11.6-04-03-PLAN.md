---
phase: v11.6-04-authoring-publish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
---

<objective>
## Goal
Finalize the three real weekend setlists via MCP (keys/BPM/order/bonds — propose →
Daniel confirms → apply) and run the **publish round-trip**: per-set `preview_publish`
dryRun → Daniel's explicit per-set "send" → live `publish_setlist`. Closing this plan
closes the v11.6 milestone.

## Purpose
This is the milestone's payoff: the band actually receives the right charts on their
iPads for the Camp Sabra weekend (Jun 19–20). The deliver/access path was made airtight
in Plans 01 (in-app bell link + QR early-scan) and 02 (QR claim-lag). This plan does the
real authoring polish + the real send.

## Output
- The 3 sets finalized (any agreed key/BPM/order/bond fixes applied via MCP) and
  `verify_setlist_charts` clean.
- Each set published live to the band (in-app + push + email + SMS) on Daniel's go.
- Real-send confirmations appended to `.paul/UAT-PENDING.md`; STATE/ROADMAP transition;
  v11.6 ready for `/paul:complete-milestone`.

## STOP-GATE
`autonomous: false`. Two gates: (1) the finalization edits are proposed and only applied
after Daniel confirms (he authors; I execute); (2) the **live publish/notify send
notifies real band members** — fired only on Daniel's explicit per-set "send". Daniel
answered (2026-06-18): "I drive finalization via MCP" + "dryRun first, then I send on
your go."

## CRITICAL — correct set IDs (verified live 2026-06-18)
Publish ONLY these three (all `orgId: crc`, all `publishedAt: null`):
- **Shir Shabbat — Juneteenth — June 19** — `a84f8cce-176e-4b5e-9653-4df71db6f5ba` (v31)
- **Camp Sabra — Havdalah & Kids Singalong — June 20** — `7e005452-7c42-4cdc-b27d-ff0c78b6667b` (v10)
- **Camp Sabra — Staff Concert — June 20** — `c2075393-d994-4643-ade3-432a6864d87f` (v26)
**DO NOT publish** `7c640a8a-358e-48ee-8523-6b8a0eca9d05` — it is the superseded
"Camp Sabra — Staff Concert (Late Night) — June 20 **(OLD)**" set (the stale anchor ID
in earlier notes). The live staff concert is `c2075393…`.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Live state probed 2026-06-18 (read-only)
- All 3 correct sets: `verify_setlist_charts` ok=bonded, 0 missing / 0 unreachable /
  0 shortcut. Unbonded rows are intentional headers/readings/lead-live songs.
- Minor finalization candidates observed (NOT blockers — Daniel confirms in Task 2):
  - Shir Shabbat: track 0 "roadto" keyless (intro?).
  - Havdalah: "Eliyahu_Hanavi_(fast) Dm" keyless (title implies Dm); "Oseh shalom -
    Nava tehila.pdf" keyless; "David Melech Yisrael" note "Confirm key w/ David".
  - Staff Concert: "summertime chords", "aint no sunshine chords",
    "Hallelujah (G)" keyless.

## MCP tools (this plan is data/ops, not code)
- Read: get_setlist, verify_setlist_charts, preview_publish
- Write (finalize): update_track (key/bpm/order fields), reorder_setlist, bond tools
- Send: publish_setlist (real send requires explicit recipients[] — auto-derive is
  refused on a real publish; derive the band roster from the dryRun audience and pass
  it explicitly)
</context>

<acceptance_criteria>

## AC-1: The three sets are finalized and chart-clean
```gherkin
Given the 3 correct weekend sets
When finalization edits Daniel confirms are applied via MCP
Then verify_setlist_charts returns 0 missing / 0 unreachable for each, and the
  confirmed key/order changes are present (re-read via get_setlist).
```

## AC-2: Publish is previewed before any send
```gherkin
Given a finalized set
When preview_publish(dryRun) runs for it
Then Daniel is shown the exact recipient roster (count + role breakdown), per-row chart
  health, and the recommendation — and NO notification is sent during preview.
```

## AC-3: Live send happens only on explicit per-set approval, to the right sets
```gherkin
Given Daniel has reviewed a set's preview and explicitly says "send" for THAT set
When publish_setlist runs with the confirmed explicit recipients
Then the band receives it (in-app + push + email + SMS as applicable), the set's
  publishedAt is set, and the OLD staff-concert set (7c640a8a) is never published.
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Verify-first probe + finalization-gap report</name>
  <files></files>
  <action>
    For each of the 3 CORRECT set IDs: get_setlist + verify_setlist_charts (markOrphaned:false)
    + preview_publish (dryRun, audience:'band'). Confirm c2075393 (staff concert) is
    chart-clean (not yet verified at plan time). Compile a per-set gap report:
    keyless song rows, order anomalies (e.g. Havdalah has a gap at order 11; Staff
    Concert orders skip — confirm intended vs. needs reorder), any bond mismatch, and
    the dryRun recipient roster + recommendation. Do NOT write anything. Do NOT touch
    7c640a8a (OLD).
  </action>
  <verify>3 gap reports produced; all 3 sets show 0 missing/0 unreachable; recipient roster captured per set.</verify>
  <done>AC-1 (probe half) + AC-2 inputs ready.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Which finalization edits to apply to each set (keys/BPM/order/bonds), if any.</decision>
  <context>Daniel authors; I execute. The sets are already publish-ready (chart-clean), so edits are optional polish. Present the Task-1 gap report per set; Daniel picks edits or says "publish as-is".</context>
  <options>
    <option id="apply-edits">
      <name>Apply the confirmed edits</name>
      <pros>Keys/order exactly right for the band on stage</pros>
      <cons>More MCP writes; each bumps the set version</cons>
    </option>
    <option id="as-is">
      <name>Publish as-is</name>
      <pros>Sets are already chart-healthy; fastest path</pros>
      <cons>Minor keyless rows remain (musicians infer from chart)</cons>
    </option>
  </options>
  <resume-signal>Per set: list the edits to apply, or say "as-is".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply confirmed finalization edits via MCP + re-verify</name>
  <files></files>
  <action>
    Apply ONLY the edits Daniel confirmed in Task 2, via update_track (key/bpm),
    reorder_setlist (if an order fix was confirmed), and bond tools (only if a bond
    change was confirmed). Use dryRun where the tool supports it to self-check, then the
    real write. After writes, re-run verify_setlist_charts for each edited set to confirm
    still 0 missing/0 unreachable, and get_setlist to confirm the changes landed. If
    Daniel chose "as-is" for a set, skip it. Never edit 7c640a8a.
  </action>
  <verify>get_setlist shows the confirmed edits; verify_setlist_charts still clean for each edited set.</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Each finalized set, with a fresh preview_publish(dryRun) showing recipients + chart health + recommendation.</what-built>
  <how-to-verify>
    For each set, I present the dryRun: recipient count + role breakdown, per-row health
    (expect all ok), snapshotDiff, and recommendation (expect 'publish'). Daniel reviews
    the roster and the set identity (name + id), and approves EACH set individually.
    This is the live-send STOP-gate.
  </how-to-verify>
  <resume-signal>Per set, type "send <set name>" to authorize that real publish, or "hold".</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Live publish each approved set + log + transition</name>
  <files></files>
  <action>
    For ONLY the sets Daniel authorized in Task 4: call publish_setlist with explicit
    recipients (the band roster surfaced by the dryRun — passed explicitly since real
    publish refuses auto-derivation), note/subject as Daniel specifies (default subject
    = set name). After each send, report the delivery counts (in-app/push/email/SMS) and
    confirm publishedAt is now set (get_setlist). Append a real-send confirmation line per
    set to .paul/UAT-PENDING.md (date, set id, recipient count, channels). Then write the
    plan SUMMARY. Because this is the LAST plan in the phase, hand off to the phase
    transition (PROJECT/ROADMAP evolve, phase ✅, commit the .paul bookkeeping) → v11.6
    ready for /paul:complete-milestone. NEVER publish 7c640a8a.
  </action>
  <verify>Each approved set: publish_setlist returns success with delivery counts; get_setlist shows publishedAt set; UAT-PENDING updated; SUMMARY written.</verify>
  <done>AC-3 satisfied; phase v11.6-04 complete; milestone ready to close.</done>
</task>

</tasks>

<boundaries>

## DO NOT
- Publish or edit `7c640a8a-358e-48ee-8523-6b8a0eca9d05` (OLD staff concert) — ever.
- Send any real notification before Daniel's explicit per-set "send" (Task 4 gate).
- Apply any finalization edit not confirmed in Task 2.
- Pass `markOrphaned: true` to verify_setlist_charts (read-only probe only).
- Change application code — this plan is MCP data/ops only.

## SCOPE LIMITS
- Only the 3 correct weekend sets. No other setlists.
- No new charts/bonds unless a Task-2-confirmed gap requires it.
- err-public/CRC-tenant: all 3 are crc-org; no cross-tenant action.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] All 3 correct sets verify_setlist_charts clean (0 missing/0 unreachable).
- [ ] Confirmed finalization edits applied + re-verified (or "as-is" recorded).
- [ ] Each authorized set published; publishedAt set; delivery counts captured.
- [ ] OLD set (7c640a8a) untouched.
- [ ] UAT-PENDING updated with real-send confirmations; SUMMARY written.
- [ ] Phase transition done → v11.6 ready to close.
</verification>

<success_criteria>
- The band has the three weekend sets on their iPads, delivered live, on Daniel's go.
- Correct sets only; OLD set never published.
- v11.6 milestone complete.
</success_criteria>

<output>
After completion, create `.paul/phases/v11.6-04-authoring-publish/v11.6-04-03-SUMMARY.md`,
then run the phase transition (last plan in phase).
</output>
