---
phase: v11.3-04-perform-performance
plan: 02
subsystem: ui
tags: [cls, web-vitals, perform, react, hydration, auth-gate, skeleton, tailwind]

requires:
  - phase: v11.3-04-01
    provides: characterization pinning CLS to the authLoading-gated QR card mount
provides:
  - /perform listing CLS fix — sign-in card slot reserved during authLoading (expected-anon) so the lists don't shift
  - perform-cls.test.tsx regression (cites web-vitals /perform CLS cell)
affects: [v11.3-04-03, v11.3-05]

tech-stack:
  added: []
  patterns:
    - "Reserve async-content slot via synchronous auth hint (cachedUser localStorage) — reserve for expected-anon, skip for expected-authed, so neither cohort shifts"

key-files:
  created:
    - src/components/performance/__tests__/perform-cls.test.tsx
  modified:
    - src/components/performance/PublicSetlistListing.tsx
    - src/components/auth/QRSignIn.tsx

key-decisions:
  - "Reserve the sign-in-card slot only when !cachedUser (expected-anon) — avoids introducing a collapse-shift for authed returners who have zero list-shift today"
  - "Reserved slot = a card-height skeleton (not the real QRSignIn) so no /api/auth/qr poll fires during authLoading and heights match by construction"

patterns-established:
  - "Use the existing cachedUser hint as a synchronous pre-auth-resolve signal for CLS-safe optimistic layout"

duration: ~30min
started: 2026-06-10T02:42:00Z
completed: 2026-06-10T03:12:00Z
---

# Phase v11.3-04 Plan 02: /perform Listing CLS Fix Summary

**Killed the /perform CLS by reserving the anon QR sign-in card's slot during authLoading (gated on the synchronous `cachedUser` hint) so the Upcoming/Past lists no longer get shoved down when the card mounts post-auth-resolve — with zero regression for authed returners and byte-identical CRC card markup.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~30 min |
| Tasks | 2/2 PASS |
| Files modified | 2 (+1 test created) |
| Gates | tsc 0 · vitest 19/19 · next build clean |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: Sign-in card no longer shifts the listing on mount | **Pass** | Slot reserved during `authLoading && !cachedUser` via a card-height skeleton; real card swaps into the held space. Structural regression test green. Literal pixel CLS<0.1 → post-deploy synthetic check (logged UAT; no local dev). |
| AC-2: No regression for authed users or CRC byte-identity | **Pass** | Real anon card markup unchanged. Authed returners (`cachedUser` present) get NO reserve → their no-card layout is identical to before (no new collapse-shift). Test (b) pins this. QR/auth logic + `/api/auth/qr` untouched. |
| AC-3: Regression coverage cites the web-vitals cell | **Pass** | `perform-cls.test.tsx` (3 tests) green; header comment cites the web-vitals `/perform` CLS cell (baseline 0.200 → target <0.1). |

## Accomplishments

- Reserved the sign-in card slot using the **existing** `cachedUser` localStorage hint (exposed by `auth-context`) — no new storage logic; reserve for expected-anon, skip for expected-authed, so neither cohort shifts.
- QRSignIn given a consistent `min-h-[15rem]` across registering/active/signing-in/error so its internal state transitions don't shrink the card and shift the lists.
- Added a 3-case structural regression (`perform-cls.test.tsx`) + confirmed the 16 existing public-view tests still pass (incl. the "does not flash the card while loading" test, which my skeleton-placeholder approach preserves).

## Task Commits

| Task | Commit | Type | Description |
|------|--------|------|-------------|
| Task 1: reserve card slot + QRSignIn min-height | (uncommitted) | fix | PublicSetlistListing.tsx + QRSignIn.tsx |
| Task 2: CLS regression test | (uncommitted) | test | perform-cls.test.tsx |

**Commit deferred to phase close** — rides the `feat(v11.3-04)` phase commit with plan 03 (FIX-2), per the per-phase-commit autonomy posture.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/components/performance/PublicSetlistListing.tsx` | Modified | Reserve sign-in card slot during authLoading for expected-anon (`!cachedUser`); real card markup unchanged |
| `src/components/auth/QRSignIn.tsx` | Modified | `min-h-[15rem]` + `justify-center` across all render states |
| `src/components/performance/__tests__/perform-cls.test.tsx` | Created | CLS regression (reserve/no-reserve/swap), cites the web-vitals /perform CLS cell |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Reserve only when `!cachedUser` | Authed returners have ZERO list-shift today (pill is in the header row); a blanket reserve would introduce a collapse-shift for them | CLS-zero for BOTH cohorts in the common path |
| Reserved slot is a skeleton, not real QRSignIn | Avoid firing `/api/auth/qr` poll during authLoading + match height by construction | No double QR registration; deterministic height |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 0 | — |

**Total impact:** None — executed as written. Implementation was *cleaner* than planned: the plan anticipated possibly hardcoding a height; the `cachedUser` hint + structural skeleton avoided both a magic number and an authed-cohort regression.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| Can't measure literal CLS in jsdom | Pinned the structural invariant (reserve/no-reserve/swap) in the test; literal CLS<0.1 verified post-deploy via the characterization § 2 synthetic recipe (logged to UAT-PENDING). |

## Skill audit

`/ui-ux-pro-max` — **invoked ✓** (loaded before Task 1; guided the "reserve space for async content" approach + skeleton-as-loading-feedback + confirmed CRC visual identity). Required-skill gate satisfied.

## Next Phase Readiness

**Ready:**
- v11.3-04-03 (FIX-2: cold-SSR TTFB/LCP) can proceed — independent surface (`perform/page.tsx` SSR), no overlap with this UI fix.

**Concerns:**
- Post-deploy: confirm synthetic /perform CLS<0.1 @ 820×1180 once the phase ships (logged non-blocking UAT).

**Blockers:** None.

---
*Phase: v11.3-04-perform-performance, Plan: 02*
*Completed: 2026-06-10*
