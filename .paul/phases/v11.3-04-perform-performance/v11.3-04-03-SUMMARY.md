---
phase: v11.3-04-perform-performance
plan: 03
subsystem: performance
tags: [ttfb, fcp, web-vitals, perform, nextjs, suspense, streaming, ssr, rsc]

requires:
  - phase: v11.3-04-01
    provides: characterization pinning TTFB/FCP to the blocking force-dynamic getAllSetlists query
provides:
  - /perform listing streamed (Suspense) — Firestore query off the TTFB/FCP critical path
  - field-measured regression contract (re-run slice probe) + cold-start infra caveat
affects: [v11.3-05]

tech-stack:
  added: []
  patterns:
    - "Stream a force-dynamic route: fast shell + async data child in <Suspense> with the existing skeleton as fallback; relocate (not change) the data fetch to keep wire payload byte-identical"

key-files:
  created: []
  modified:
    - src/app/perform/page.tsx

key-decisions:
  - "Stream, not cache — query isn't the dominant cold cost (synthetic 214ms vs field 1633ms = cold-start/geo); caching would add the v11-04-01 tenant-key leak risk for marginal gain"
  - "Relocate the query verbatim into the streamed child — zero logic change → v11-04-01 scoping + Cycle-12 wire slice preserved by construction"

patterns-established:
  - "Field-measure TTFB post-deploy (synthetic can't reproduce cold-start); route residual cold-start to an infra (Vercel) follow-up, not app churn"

duration: ~25min
started: 2026-06-10T03:14:00Z
completed: 2026-06-10T03:39:00Z
---

# Phase v11.3-04 Plan 03: /perform Cold-SSR Streaming (FIX-2) Summary

**Took the blocking `getAllSetlists` Firestore query off the /perform first-byte path by flushing a `PublicSetlistSkeleton` shell and streaming the listing via `<Suspense>` — query/org-scoping/wire-slice relocated verbatim, so TTFB/FCP improve without touching per-tenant correctness or the Cycle-12 payload.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~25 min |
| Tasks | 2/2 PASS |
| Files modified | 1 (`perform/page.tsx`) |
| Gates | tsc 0 · vitest 19/19 · next build clean (/perform ƒ dynamic) |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: Listing query no longer blocks first byte | **Pass** | Page returns `<Suspense fallback={<PublicSetlistSkeleton/>}>` synchronously; `headers()`+`getAllSetlists`+slice moved into async `PerformListing` child → shell flushes before the query. |
| AC-2: Per-tenant correctness + wire slice + CRC byte-identity | **Pass** | Query/`org`/`selectVisiblePublicSetlists` relocated verbatim (diff is a move). public-view tests (SSR seed, isTest filter, publishedAt-null, 5-cap) all green → wire slice intact. `force-dynamic` kept. |
| AC-3: Honest field-measured regression cite | **Pass** | Field re-run contract + cold-start caveat recorded here + appended to UAT-PENDING. No over-promised in-app TTFB number. |

## Accomplishments

- Streamed `/perform` so first paint is independent of the Firestore round-trip (skeleton shell flushes immediately), targeting the field TTFB 1633ms / FCP 3551ms cold p75.
- Preserved v11-04-01 per-tenant scoping + Cycle-12 F-C12-001 wire slice by relocating the query untouched.
- Kept the SSR-prefetch benefit (C11 F-M2-006 — flaky-sanctuary tablets still get real cards, now non-blocking).

## Task Commits

| Task | Commit | Type | Description |
|------|--------|------|-------------|
| Task 1: Suspense-stream the listing | (phase commit) | perf | `src/app/perform/page.tsx` |
| Task 2: field-regression contract + caveat | (docs) | docs | SUMMARY + UAT-PENDING |

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `src/app/perform/page.tsx` | Modified | Shell + `<Suspense>` + async `PerformListing` child; query relocated verbatim |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Stream, not cache | Synthetic cold TTFB 214ms vs field 1633ms → cold-start/geo dominates, not the query; cache adds v11-04-01 tenant-key leak risk | Lower-risk fix; correctness preserved trivially |
| Relocate query verbatim | Zero logic change keeps v11-04-01 scoping + Cycle-12 wire slice byte-identical | No re-verification of tenant isolation needed |
| Cold-start residual → infra follow-up | Streaming can't fix serverless cold boot; chasing it in app code = churn | Logged as Deferred Issue (Vercel fluid-compute/region) |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 1 | Cold-start TTFB residual → infra (Vercel) follow-up, logged |

**Total impact:** None — executed as written.

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| Can't measure field TTFB locally (synthetic ≪ field) | Post-deploy field re-run of `scripts/v11-3-04-webvitals-slice.mjs` is the proof; logged to UAT-PENDING with the cold-start caveat. |

## Skill audit

`/ui-ux-pro-max` — **invoked ✓** (loaded earlier this session for plan 02; covers the /perform render/loading-path gate). No gap.

## Next Phase Readiness

**Ready:** Phase v11.3-04 complete (3/3 plans). Next milestone phase: v11.3-05 (P3 polish — BUG-6 broslaz PWA manifest + F-6 cold-load 429 self-heal). Note F-6 touches the same QR card surface as plan 02's CLS fix.

**Concerns:** Post-deploy — confirm field TTFB/FCP movement; if cold-start-bound, action the Vercel infra follow-up.

**Blockers:** None.

---
*Phase: v11.3-04-perform-performance, Plan: 03*
*Completed: 2026-06-10*
