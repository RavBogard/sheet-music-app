---
phase: v11.3-04-perform-performance
plan: 01
subsystem: performance
tags: [web-vitals, cls, lcp, ttfb, perform, rum, playwright, firestore, force-dynamic]

requires:
  - phase: cycle-7 C7I4-005
    provides: get_web_vitals_summary + webVitalsObservations sink (the field-RUM source)
provides:
  - VERIFY-FIRST characterization of the /perform BUG-2 regression
  - scripts/v11-3-04-webvitals-slice.mjs (field-RUM slicer by surface×navigationType×deviceType)
  - ranked 2-fix list (FIX-1 QR-card height reserve / FIX-2 cold-SSR cache-or-stream) → input to v11.3-04-02
affects: [v11.3-04-02, v11.3-05]

tech-stack:
  added: []
  patterns:
    - "Field-RUM slice probe: raw webVitalsObservations grouped by navigationType (cold/warm) + deviceType (iPad) — finer than get_web_vitals_summary's surface-only buckets"
    - "Synthetic CWV attribution: Playwright iPad-viewport (820×1180) buffered PerformanceObserver for layout-shift sources + LCP element + nav timing, cold vs warm"

key-files:
  created:
    - scripts/v11-3-04-webvitals-slice.mjs
    - .paul/research/PERFORM-PERF-CHARACTERIZATION-2026-06-10.md
  modified: []

key-decisions:
  - "Split the phase: 01 = measure-only (no src change); 02 = the fix. Honors VERIFY-FIRST (no speculative fixes)."
  - "Headline hypothesis (chart-image reflow) REFUTED — viewer is healthy; the listing page is the regression."

patterns-established:
  - "Field-RUM cold/warm/device slicing reusable for any future route-perf characterization"
  - "Re-run the slice probe post-deploy to validate field TTFB (synthetic can't reproduce real-user cold TTFB)"

duration: ~35min
started: 2026-06-10T02:05:00Z
completed: 2026-06-10T02:40:00Z
---

# Phase v11.3-04 Plan 01: /perform Performance Characterization (VERIFY-FIRST) Summary

**Characterized BUG-2 with field RUM (n=1068) + synthetic iPad capture: the regression is the `/perform` LISTING page — CLS 0.187 from the QR sign-in card growing post-paint, and cold-dominated TTFB/LCP/FCP from `force-dynamic` SSR — NOT chart-image reflow (the chart viewer is healthy at CLS 0.010). Produced a ranked 2-fix list for v11.3-04-02.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~35 min |
| Tasks | 3 completed (3/3 PASS) |
| Files created | 2 (1 probe script, 1 research doc) |
| src/** modified | 0 (measurement-only plan) |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: Cold-vs-steady decomposition from field RUM | **Pass** | Dominant cell = `cold(navigate)·desktop` (n=44/41); cold TTFB 1633ms vs warm 447; regression is `/perform` listing, NOT `/perform/setlist/[id]` viewer (CLS 0.010). |
| AC-2: CLS source + LCP element via synthetic iPad capture | **Pass** | CLS 0.1869 source = QR sign-in card (`section.bg-card` "Scan with your phone to sign in") pushing the Upcoming/Past `section.flex.flex-col.gap-3` blocks down; deterministic (cold==warm). LCP element = first service card `<h3>` title text. Chart-image-reflow hypothesis refuted. |
| AC-3: Ranked, regression-citable fix list → Plan 02 | **Pass** | FIX-1 (QR height reserve → CLS) + FIX-2 (cold-SSR cache-or-stream → TTFB/LCP), each tagged {metric, locus, approach, regression cite, risk}; + Plan-02 scope + out-of-scope handoff. |

## Accomplishments

- **Reversed the working hypothesis with evidence.** Field RUM showed `/perform/setlist/[id]` (the chart viewer) is healthy (CLS 0.010 / LCP 1368); the BUG-2 numbers belong entirely to the `/perform` listing. "Chart-image reflow" is dead.
- **Pinpointed both root causes to specific code loci:** CLS → `src/components/auth/QRSignIn.tsx` (in `PublicSetlistListing.tsx`); cold TTFB/LCP/FCP → `src/app/perform/page.tsx` `force-dynamic` + per-request `getAllSetlists` query.
- **Built a reusable field-RUM slicer** (`scripts/v11-3-04-webvitals-slice.mjs`) that answers cold/warm + iPad/other + listing/viewer — questions `get_web_vitals_summary` can't.
- **Surfaced an adjacency:** the CLS culprit (QR card) is the same surface as F-6's `/api/auth/qr` 429 (v11.3-05) — those phases touch the same component.

## Task Commits

| Task | Commit | Type | Description |
|------|--------|------|-------------|
| Task 1: field-RUM slice probe | (uncommitted) | feat | `scripts/v11-3-04-webvitals-slice.mjs` |
| Task 2: synthetic iPad capture | n/a | — | Playwright session against prod (no file artifact) |
| Task 3: characterization doc | (uncommitted) | docs | `.paul/research/PERFORM-PERF-CHARACTERIZATION-2026-06-10.md` |

**Commit deferred to phase close:** verify artifacts (probe + doc) ride the `feat(v11.3-04)` phase commit alongside the v11.3-04-02 fix, per the per-phase-commit autonomy posture. No `src/**` touched → nothing deploys from this plan.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `scripts/v11-3-04-webvitals-slice.mjs` | Created | Read-only field-RUM slicer (surface × navigationType × deviceType → p75); firebase-CLI ADC pattern. Re-run post-FIX-2 to verify field TTFB drop. |
| `.paul/research/PERFORM-PERF-CHARACTERIZATION-2026-06-10.md` | Created | The characterization: field table, synthetic attribution, ranked fix list, Plan-02 handoff. |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| Split phase into verify (01) + fix (02) | VERIFY-FIRST constraint — don't pre-bake fixes | 02 authored from measured causes, zero further discovery |
| FIX-2 must use an **org-keyed** cache (or streaming) | `force-dynamic` exists to fix the v11-04-01 cross-tenant cache bug (path-keyed ISR served broslaz CRC's setlists) | Constrains 02's approach; CRC byte-identity + per-tenant correctness are hard gates |
| Track-page/`/setlists/[id]` CLS 0.4–0.61 left out of scope | Low-traffic, separate cause (real chart reflow), not the BUG-2 listing headline | Logged for backlog/v11.3-05, not fixed in v11.3-04 |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | — |
| Scope additions | 0 | — |
| Deferred | 0 | Plan executed as written |

**Total impact:** None — executed exactly as specified. (One finding *reversed the plan's stated suspicion* about the cause, but that is the intended output of a VERIFY-FIRST plan, not a deviation.)

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| Synthetic TTFB (214/28ms) ≪ field cold TTFB (1633ms) | Expected — synthetic runs from a warm/fast edge. Documented that FIX-2 must be validated against **field** RUM (re-run the slice probe post-deploy), not synthetic. |

## Skill audit

`/ui-ux-pro-max` (SPECIAL-FLOWS required for UI-touching phases): **not triggered for Plan 01** — measurement-only, zero UI mutation. It is BLOCKING for v11.3-04-02 (the fix), which mutates `/perform` rendering. No gap.

## Next Phase Readiness

**Ready:**
- v11.3-04-02 can be planned with zero further discovery — ranked fix list + loci + regression cites + risk notes all in the characterization doc.
- Reusable slice probe in place for the FIX-2 field regression check.

**Concerns:**
- FIX-2 is architecturally sensitive: any caching MUST be org-keyed (v11-04-01) and preserve the Cycle-12 F-C12-001 wire-payload slice + CRC byte-identity. Streaming is the lower-risk fallback.

**Blockers:** None.

---
*Phase: v11.3-04-perform-performance, Plan: 01*
*Completed: 2026-06-10*
