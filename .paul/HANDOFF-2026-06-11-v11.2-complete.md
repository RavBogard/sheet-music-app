# PAUL Handoff

**Date:** 2026-06-11 (session 12)
**Status:** paused — clean (milestone v11.2 COMPLETE; everything committed + pushed + tagged)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** CRC Music (sheet-music-app) — multi-tenant (CRC + Brothers Lazaroff). Next.js + Firebase (`crcmusiccharts`); git repo at the `sheet-music-app/` subdirectory; `.paul/` is inside it.
**Core value:** The band gets the right charts + recordings on their iPads each week; Daniel authors setlists conversationally via Claude + MCP (the browser app is the band/consumer surface only).

---

## Current State

**Version:** `11.2.0` (`package.json`), tag `v11.2.0`.
**Milestone:** ✅ **v11.2 MCP Stress-Test Fixes — COMPLETE 2026-06-11** (5/5 phases, 8 plans). No PAUL milestone active.
**Phase / Plan:** none active.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     [no loop active — milestone complete]
```

**Git:** `master` tip `f163e90ce3` (milestone-close commit), in sync with `origin/master`. Tag `v11.2.0` on `f27ae7bc5f` (the phase-05 ship), pushed. Working tree clean.

---

## What Was Done (this session — all shipped to `master`)

Closed milestone v11.2 end-to-end — all 9 Brothers Lazaroff MCP/Perform stress-test bugs, CRC byte-identical throughout. Phases this session:
- **v11.2-04 (BUG-4 + BUG-5)** `52a3dea57d` — `preview_publish` flags unbonded `type:song` rows (`review_first`); `cleanup_all_test_data` gained an owner-independent `setlists where isTest==true` flag-sweep (full-sweep-mode only, prefix-isolation preserved); authed dashboard hides isTest setlists via a shared `isNonTestSetlist` predicate reused from `/perform`. **Scope correction:** `isTest` is a Setlist-only field — `library_index` flag-sweep dropped (charts/songs are owner-cascaded).
- **v11.2-05-01 (BUG-6 + BUG-7)** `06f2db3176` — dashboard hero (+ OnboardingCard) host-resolve brand server-first (`coerceOrgId(x-org-id)`→`getOrgBranding`, mirrors v11.1-01 nav) so broslaz shows its own brand, not "CRC MUSIC"; `TextScoreViewer` wrap-mode chunks grouped word-atomic (`groupChunksIntoWords`) so a lyric word never fragments.
- **v11.2-05-02 (BUG-8)** `f27ae7bc5f` — single `serializeTimestamps()` pass in `jsonResult` (`src/lib/mcp/serialize-timestamps.ts`, pure/firebase-admin-free) so every MCP tool response renders timestamps ISO, never raw `{_seconds,_nanoseconds}`.
- **Milestone close** `f163e90ce3` — MILESTONES.md entry, PROJECT.md evolved, ROADMAP ✅ + archived (`.paul/milestones/v11.2.0-ROADMAP.md`), package.json 11.1.0→11.2.0, paul.json complete, tag `v11.2.0`.

(Earlier in the session — before this handoff's window — v11.2-04-01/BUG-4 and the v11.2-03 BUG-2+3 close also landed; full chain is in `.paul/MILESTONES.md` § v11.2.)

---

## What's In Progress

Nothing. Working tree clean, no active loop.

---

## What's Next

**No blocking PAUL work.** Pick one when you resume:
1. **`/paul:discuss-milestone`** (or `/paul:milestone`) — scope the next milestone. Direction is Daniel's call (MEMORY.md `# Core Workflow` + the 2026-05-15 MCP-first pivot are the guide: MCP authoring completeness + band/consumer surface polish).
2. **Resume v7.1 `.coord/` hardening** — cycle-13 (`.coord/cycle-13-CHARTER.md`), independent of the PAUL loop.

**Open on Daniel (non-blocking, live/safe, emulator-proven):** reconnect Claude Desktop BL connector to `https://www.brotherslazaroff.live/api/mcp`, then run UAT-PENDING: (1) BUG-1 create→propose→commit on BL; (2) BUG-9 `preview_publish` on a BL setlist shows the BL roster size (not 17). Existing token is crc-pinned → a reconnect is required.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (read first) |
| `.paul/MILESTONES.md` § v11.2 | The completed-milestone record |
| `.paul/milestones/v11.2.0-ROADMAP.md` | Archived ROADMAP snapshot at v11.2 close |
| `src/lib/mcp/serialize-timestamps.ts` | BUG-8 boundary normalizer (canonical MCP timestamp gate) |
| `src/components/performance/public-setlist-order.ts` | `isNonTestSetlist` (shared /perform + dashboard non-test predicate) |

---

## Resume Instructions

1. `git pull` first (multi-computer box).
2. Read `.paul/STATE.md` for latest position.
3. Run `/paul:resume` (or go straight to `/paul:discuss-milestone`).
4. Push `origin master` (NOT `master:main`).

---
*Handoff created: 2026-06-11 (session 12)*
