# PAUL Handoff

**Date:** 2026-06-08
**Status:** paused (clean loop boundary — v11-02 at 2/4 plans)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — band charts/recordings on iPads; Daniel authors setlists via Claude + MCP.
**Core value:** The band gets the right charts + recordings on their iPads each week, and Daniel authors setlists conversationally via Claude + MCP.

---

## Current State

**Milestone:** v11.0 Brothers Lazaroff Multi-Tenant (make the app multi-tenant; first 2nd tenant = Brothers Lazaroff on brotherslazaroff.live).
**Phase:** v11-02 MCP org-scoping — **IN PROGRESS, 2 of 4 plans complete.**
**Plan:** none active (v11-02-02 closed). Next: PLAN v11-02-03.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [v11-02-01 ✓✓✓ and v11-02-02 ✓✓✓ both LOOP COMPLETE]
```

⚠️ **All v11-02 work is UNCOMMITTED in the working tree** (by design — the v11.0 directive commits once per PHASE, after v11-02-04). cwd branch `master`. See "Uncommitted files" below.

---

## What Was Done (this session)

- **Resumed** PAUL; decomposed v11-02 (MCP org-scoping) into 4 vertical-slice plans.
- **v11-02-01 — caller-org resolution foundation (LOOP COMPLETE, behavior-neutral):** `verifyBearer` resolves `orgId` from the `mcpTokens` doc (default `crc`) → MCP route stashes it on `AuthInfo.extra` → new `orgFrom(extra)` seam. All 4 mint sites stamp `orgId` (minted children inherit caller org). Ran prod `mcpTokens` backfill: **117 docs stamped crc**, idempotency verified live. tsc clean; auth 11/11 + org-context 4/4 + backfill-emulator 3/3; full suite 3272/0.
- **v11-02-02 — org-scope MCP READS (LOOP COMPLETE, the leak-class core):** all **6 read tools** tenant-isolated — `list_setlists`/`search_library`/`list_library`/`search_chart_text` filter to caller org; `get_setlist`/`get_song` are a cross-tenant **not-found wall**; `search_chart_text` chords scope drops cross-tenant parent charts. `SongRecord.orgId` surfaced; `bond-corrections` alternatives scoped to the setlist's org. Emulator isolation proof 7/7; full suite 3272/0.
- Both SUMMARYs written; STATE/ROADMAP/paul.json updated each loop.

---

## What's In Progress

- Nothing mid-flight. Both loops closed cleanly. The phase is half done (2/4); v11-02-03 + v11-02-04 remain.

---

## What's Next

**Immediate:** `/paul:plan v11-02-03` — org-scope MCP **WRITES** + cross-tenant write **DENIAL**. This is the write-side security wall and is security-critical:
- **Stamp the caller's org** on every MCP create (v11-01-02 stamps default crc at the server create sites; v11-02-03 makes MCP creates stamp the *caller's* org).
- **Deny cross-tenant mutation** of existing docs: `update_setlist`, `add_track_to_setlist`, `bulk_add_tracks`, `update_track`, `bulk_update_tracks`, `reorder_setlist`, `remove_track`, `delete_setlist`, `swap_chart`, `clone_setlist`, library-upload tools, `update_song` (song-metadata). Resolve the target doc's `orgId`, refuse if ≠ caller org (mirror v11-02-02's get-by-id not-found wall, or a `cross_tenant_denied` rich envelope).
- **Why it matters:** MCP tools use the Admin SDK, which BYPASSES Firestore rules — so the v11-01-04 rules do NOT backstop MCP writes. The application layer is the only control. A BL caller can currently mutate a CRC doc by id.
- Emulator test proving cross-tenant writes are denied + creates stamp the caller's org.

**After that:** v11-02-04 (issue David's `brotherslazaroff` bearer + `orgIds:['brotherslazaroff']` claim + onboarding doc + live e2e) → **then the single `feat(v11-02)` phase commit + push origin master** → v11-03 (host routing/branding, /ui-ux-pro-max BLOCKING) → v11-04 → v11-05 (isolation audit, close gate).

---

## Key Context for v11-02-03

- **Seam:** `orgFrom(extra)` + `rowOrg(orgId)` in `src/lib/mcp/org-context.ts`. `rowOrg(x) = x || DEFAULT_ORG_ID`. `orgFrom` throws on unauthenticated.
- **Pattern (mirror reads):** for by-id mutations, fetch the target, `if (rowOrg(data.orgId) !== callerOrg)` → refuse. For creates, stamp `orgId: callerOrg`. Thread `orgFrom(extra)` from the `index.ts` write-tool registration sites (like the 6 read sites already do).
- **Design decisions already baked (keep consistent):** org from the token doc (not Auth claims); default-crc on absent orgId; `org` param OPTIONAL default crc on the data-layer fns (prod passes explicit `orgFrom(extra)`, CRC-data tests default crc); in-memory / per-doc checks (no new Firestore indexes).
- **Write data layer lives in** `src/lib/mcp/tools/setlist-write.ts`, `src/lib/mcp/tools/library-upload.ts`, `src/lib/mcp/tools/clone-setlist.ts`, `src/lib/mcp/tools/song-metadata.ts`, `src/lib/mcp/server-tracks-write.ts` (+ `src/lib/setlist-write.ts` shared server writer). Read these first.
- **DEFERRED (out of v11-02 scope — not org-stamped, cross-tenant today, read+write):** `setlistTemplates` (templates), roster/musicians (`users`, `scheduling_assignments`, `musician_availability`), congregation, service-personnel. Flag for a follow-up or fold into v11-04.
- **Prod-script auth on this box** (no SA creds, no gcloud): convert the firebase-CLI refresh token (`~/.config/configstore/firebase-tools.json` → `tokens.refresh_token`) into a temp `authorized_user` ADC json (public firebase-tools client_id/secret), point `GOOGLE_APPLICATION_CREDENTIALS` at it, delete after. Used cleanly for the v11-02-01 token backfill.

---

## Uncommitted files (working tree, branch master)

**v11-02-01:** `src/lib/mcp/auth.ts`, `tokens.ts`, `src/app/api/mcp/route.ts`, `tools/mint-admin-bearer.ts`, `tools/admin-test-session.ts`, `tools/test-tokens.ts`, `tools/index.ts`, + new `src/lib/mcp/org-context.ts`, `backfill-token-orgid.ts`, `scripts/backfill-token-orgid.mjs`, + tests (`auth.test.ts`, `org-context.test.ts`, `backfill-token-orgid.emulator.test.ts`, 4 token-flow emulator tests).
**v11-02-02:** `src/lib/mcp/server-songs.ts`, `org-context.ts` (rowOrg), `tools/setlists.ts`, `tools/library.ts`, `tools/chart-text-search.ts`, `tools/index.ts`, `tools/bond-corrections.ts`, + new `__tests__/org-scope-reads.emulator.test.ts`.
**Plus** `.paul/` (STATE, ROADMAP, paul.json, 2 PLANs + 2 SUMMARYs, this handoff).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview (v11-02 at 2/4) |
| `.paul/phases/v11-02-mcp-org-scoping/v11-02-01-SUMMARY.md` | Resolution foundation |
| `.paul/phases/v11-02-mcp-org-scoping/v11-02-02-SUMMARY.md` | Read isolation (+ "writes still unscoped" concern) |
| `src/lib/mcp/org-context.ts` | `orgFrom` + `rowOrg` seam |

---

## Resume Instructions

1. `git pull` (multi-computer workflow; uncommitted v11-02 work is local-only on this box).
2. Read `.paul/STATE.md` + `v11-02-02-SUMMARY.md` (Next Phase Readiness section).
3. Run `/paul:resume` (or directly `/paul:plan v11-02-03`).

---

*Handoff created: 2026-06-08*
