# PAUL Handoff

**Date:** 2026-06-19
**Status:** paused (clean phase boundary — v11.7-01 complete)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (centralreform.live) — multi-tenant (CRC + Brothers Lazaroff on brotherslazaroff.live).
**Core value:** The band gets the right charts + recordings on their iPads each week; Daniel authors setlists conversationally via Claude + MCP (the MCP server is the PRIMARY authoring surface; the browser app is the band/consumer surface).

---

## Current State

**Version:** 11.7.0 (package.json/paul.json/STATE aligned)
**Milestone:** 🚧 v11.7 — Discoverability & Deferred Backlog (1 of 7 phases complete)
**Phase:** v11.7-01 (Verify-first audit) ✅ COMPLETE → next is v11.7-02 (P0 `find_user`)

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY     [v11.7-01 complete; phase boundary]
  ✓        ✓        ✓
```

---

## What Was Done (this session, 2026-06-18→19)

1. **Closed v11.6 milestone** — tag `v11.6.0`, MILESTONES entry + archive + ROADMAP reorg + version bump, pushed `4e30913c2a`.
2. **Opened v11.7** — Discoverability & Deferred Backlog. Headline = MCP **Asymmetry Principle** (every write/act-by-id tool needs a read/resolve-id path). BROAD scope (Daniel): MCP discoverability + folded deferred backlog (F3 library density, infra org-scoping, broslaz design pass). 7 phases.
3. **Dropped photo-import** — Daniel: "not a feature I'm looking for" (declined twice; OFF roadmap — see memory `feedback_no_photo_import`).
4. **Completed Phase v11.7-01** — read-only verify-first MCP discovery-gap audit (3 parallel Explore agents, every verdict cited `file:line`). Report: `.paul/research/v11-7-01-mcp-discovery-verify-REPORT-2026-06-18.md`. Committed `94960fa5ae`, pushed. No production code.

## Key Findings (v11.7-01 — these drive 02–07)

- **P0–P3 audit claims ALL CONFIRMED.** `list_musicians` hides instrument-less users (`roster.ts:114–126`) — that's the David-is-invisible bug; write tools take arbitrary uid; `delete_chart` reference-walk is at **`library-upload.ts:787–846`** (correction: not `setlist-write.ts`); `create_contact` email-dedups; `delete_template` no-cascade; `sourceTemplateId` written (`templates.ts:878`).
- **2 corrections:** `library_signals` is an **in-process event emitter** (`library-events.ts`), NOT a Firestore collection; command acks are ephemeral (TTL ~5min, no `createdAt`). → **P4 `list_recent_commands` + P5 `list_recent_library_changes` DEFER to v11.8** (blocked on new infra, not MCP-only adds).
- **search_setlists:** tracks are top-level `tracks/{id}` (setlistId FK); **app-side filtering acceptable at current scale**; composite index optional.
- **find_setlists_referencing_chart:** CHEAP — surface the existing `delete_chart` walk, zero new index.
- **v7.0 fold-forward:** 11/13 still live, mostly in-app library-UI tech debt → route cheap ones into phase 06.

---

## What's Next

**Immediate:** `/paul:plan` for **Phase v11.7-02 — P0 `find_user`** (the MCP headline / David-is-invisible fix).

**Verified-ready spec (from v11.7-01):** NEW `find_user({email?, nameContains?, role?, includeProfileless?})` on the `users` collection — **DROP the instrument gate**; role + email viable now (email via in-memory filter at current scale; `(orgId,email)` index optional), name = app-side substring; tenant-scope via `orgFrom` + `rowOrgIds` (no cross-tenant leakage); gate `assertEditor` (admin|band_leader). Consider shipping the cheap `includeProfileless` interim on `list_musicians` in the same plan. Fixes `suggest_musicians`/`suggest_band` coverage as a side effect.

**After that:** 03 P1 setlist reverse-lookup → 04 P2–P5 sweep (TRIMMED: find_setlists_from_template + find_contact only) → 05 F3 library density → 06 infra org-scoping → 07 broslaz design pass.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (read first) |
| `.paul/ROADMAP.md` | v11.7 phase table + per-phase focus + constraints |
| `.paul/research/v11-7-01-mcp-discovery-verify-REPORT-2026-06-18.md` | The audit findings + asymmetry inventory + ready-to-plan verdicts |
| `.paul/phases/v11.7-02-find-user/` | Next phase dir (empty — plan goes here) |
| `src/lib/mcp/tools/roster.ts` | `list_musicians`/`suggest_*`/`get_musician_profile`/`assign_musician` (the P0 surface) |
| `src/lib/mcp/org-context.ts` | `orgFrom`/`rowOrgIds` tenant-scoping seam |

---

## Constraints / Posture (carried)

- **Autonomy directive binding:** run autonomously, auto-commit + push per phase to `master`; chain PLAN→APPLY→UNIFY without stopping; STOP only at phase/milestone boundaries, STOP-gates (prod side-effects), or genuine 2+-option decisions. v11.7 Stream A is read-only / Stream B infra has no publish-send → **no STOP-gates expected.**
- **Quality floor:** tsc clean + tests + AC proof; `SKIP_ENV_VALIDATION=1 npx next build --webpack` before declaring deployable; emulator-backed tests where rules/queries change; MCP validation surfaces as `result.isError` prose (never JSON-RPC error).
- **err-public cross-tenant HARD WALL:** all new resolvers tenant-scoped, no cross-tenant uid/email leakage.
- **No publish/notify or assignment-scheduling work** (Daniel). **No photo-import** (Daniel).
- Push `origin master` (NOT `master:main`). Multi-computer — `git pull` first next session.

---

## Resume Instructions

1. `git pull` (multi-computer).
2. Read `.paul/STATE.md` (Current Position + Session Continuity).
3. Run `/paul:resume` (or go straight to `/paul:plan` for v11.7-02).

---

*Handoff created: 2026-06-19*
