# PAUL Handoff

**Date:** 2026-06-19
**Status:** paused (clean phase boundary — v11.7-03 complete, all pushed)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (centralreform.live) — multi-tenant (CRC + Brothers Lazaroff on brotherslazaroff.live).
**Core value:** The band gets the right charts + recordings on their iPads each week; Daniel authors setlists conversationally via Claude + MCP (the MCP server is the PRIMARY authoring surface; the browser app is the band/consumer surface).

---

## Current State

**Version:** 11.7.0
**Milestone:** 🚧 v11.7 — Discoverability & Deferred Backlog (MCP Asymmetry Principle) — **3 of 7 phases complete**
**Phase:** v11.7-03 (P1 setlist reverse-lookup) ✅ COMPLETE → next is v11.7-04 (P2 sweep)

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY     [v11.7-03 complete; phase boundary]
  ✓        ✓        ✓
```

All work committed + pushed to `origin master`. Working tree has only unrelated untracked research/docs files (not part of any phase).

---

## What Was Done (this session, 2026-06-19)

1. **Resumed** from HANDOFF-2026-06-19 (prior) at the v11.7-01-complete boundary.
2. **Closed v11.7-02 (P0 `find_user`)** — commit `9f149b589c`. NEW `find_user({email?,nameContains?,role?,includeProfileless?})` over `users` (drop instrument gate via opt-in `buildMusicianRow({allowProfileless})`; defaults include-profileless = the David-is-invisible fix) + `includeProfileless` on `list_musicians`. assertEditor-gated, tenant-scoped (`orgFrom`+`rowOrgIds`), in-memory filters. 7 emulator cases. tsc 0 / roster emulator 58/58 / build 0.
3. **Closed v11.7-03 (P1 setlist reverse-lookup)** — commit `ad18509b0d`. `find_setlists_referencing_chart({fileId?,songId?})` (surfaces the `delete_chart` walk; live+tenant, dangling-excluded) + `search_setlists({trackTitle?,leadMusician?,templateType?})` (app-side). **Ungated-authenticated** (NOT assertEditor — setlist-public-by-design) + tenant-scoped via `rowOrg`. 9 emulator cases. tsc 0 / setlist-rev emulator 9/9 / setlists.test 12/12 / build 0.

Both phases: backend/MCP only, no UI, no new index, no deploy. Each pushed to master (Vercel auto-deploys).

---

## What's In Progress

Nothing mid-flight. Clean phase boundary.

---

## What's Next

**Immediate:** `/paul:plan` for **Phase v11.7-04 — P2–P5 sweep (TRIMMED)**.

**Verified-ready spec (from v11.7-01 audit):**
- `find_setlists_from_template({templateId})` = `where orgId== && sourceTemplateId==` (`sourceTemplateId` written at `templates.ts:878`). **Needs a NEW composite index `setlists(orgId, sourceTemplateId)`** → `firebase deploy --only firestore:indexes` as an AUTO task (per autonomy directive; single-owner executor). This is the first v11.7 phase touching prod infra beyond a code push — NOT a STOP-gate.
- `find_contact({email?, nameContains?})` = filter over the org `contacts` collection (the dedupe path already reads it at `contacts.ts:106–122`); `assertEditor`-gated like the other contact tools.
- **DEFER `list_recent_commands` (P4) + `list_recent_library_changes` (P5) → v11.8** — both blocked on new bridge/persistent-collection infra (not MCP-only adds).

Pattern to reuse: the find_user / setlist-reverse resolvers (query + tenant wall + emulator test).

**After that:** 05 F3 library density (UI; /ui-ux-pro-max BLOCKING) → 06 infra org-scoping + cheap v7.0 fold-forward → 07 broslaz design pass (UI; /ui-ux-pro-max BLOCKING).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (read first) |
| `.paul/ROADMAP.md` | v11.7 phase table |
| `.paul/research/v11-7-01-mcp-discovery-verify-REPORT-2026-06-18.md` | The audit — asymmetry inventory + ready-to-plan verdicts (drives 04–07) |
| `.paul/phases/v11.7-03-setlist-reverse-lookup/v11.7-03-01-SUMMARY.md` | Last completed phase |
| `.paul/phases/v11.7-04-reverse-index-sweep/` | Next phase dir (empty — plan goes here) |
| `src/lib/mcp/tools/templates.ts` | `find_setlists_from_template` surface (sourceTemplateId at :878) |
| `src/lib/mcp/tools/contacts.ts` | `find_contact` surface (dedupe read at :106–122) |
| `src/lib/mcp/tools/setlists.ts` / `roster.ts` | Resolver patterns shipped this milestone |

---

## Constraints / Posture (carried)

- **Autonomy directive binding:** run autonomously, auto-commit + push per phase to `master`; chain PLAN→APPLY→UNIFY without stopping; STOP only at phase/milestone boundaries, STOP-gates (prod side-effects: publish/notify, destructive migration, auth changes), or genuine 2+-option decisions. A Firebase **index** deploy is an AUTO task, not a STOP-gate.
- **Quality floor:** tsc clean + tests + AC proof; `SKIP_ENV_VALIDATION=1 npx next build --webpack` before declaring deployable; emulator-backed tests where queries change; MCP validation surfaces as `result.isError` prose.
- **err-public cross-tenant HARD WALL:** all new resolvers tenant-scoped, no cross-tenant leakage. Setlist data is public-by-design (reverse-lookup tools are ungated); people/PII tools (find_user, find_contact) are `assertEditor`-gated.
- **No publish/notify or assignment-scheduling work** (Daniel). **No photo-import** (Daniel).
- Push `origin master` (NOT `master:main`). Multi-computer — `git pull` first next session.
- Emulator tests run via `firebase emulators:exec --only firestore,auth "npx vitest run --config vitest.emulator.config.ts <path>"`.

---

## Resume Instructions

1. `git pull` (multi-computer).
2. Read `.paul/STATE.md` (Current Position + Session Continuity).
3. Run `/paul:resume` (or go straight to `/paul:plan` for v11.7-04).

---

*Handoff created: 2026-06-19*
