# PAUL Handoff

**Date:** 2026-06-14 (evening — supersedes this file's earlier byte-truncation-fire version)
**Status:** TWO P0 chart fires RESOLVED + verified live this day. PAUSED mid-PLAN on v11.5-02-04 H1 (recon done, PLAN not written) — **resume H1 next.**

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — multi-tenant worship/band sheet-music app. Browser = band/consumer surface (Perform mode on iPads); Daniel authors via Claude + MCP. 2 live tenants: CRC + Brothers Lazaroff.
**Core value:** the band gets the right charts + recordings on their iPads each week; the web app's job is performance + quick edits.

---

## ✅ FIRE #1 — chart-byte truncation (earlier 2026-06-14, FIXED `ba38ad67b2`)

H3 (`c687db99ee`) advertised `Accept-Ranges` on EVERY `/api/drive/file/[fileId]` response incl. CHARTS → pdf.js range-fetched → Vercel CDN cached 206 partials, replayed as truncated 200s → charts failed. Fix: Range only for `audio/*`; charts plain 200, no Accept-Ranges; 206/416 `no-store`. Regression test `src/app/api/drive/file/[fileId]/route.test.ts` (6/6). Verified live.

## ✅ FIRE #2 — text charts didn't render for the BAND (later 2026-06-14, FIXED `d72b978914`)

**Symptom (Daniel-reported):** charts "we've made" still failing — e.g. 3 tracks on setlist `7e005452…` (David Melech, Take Me Home, Wagon Wheel). **All 3 are `text/plain` chord charts and their bytes were 100% intact** (this was NOT fire #1).

**Root cause:** `resolveViewerKind` (PDFOverlay) routes an extension-less `upload-{uuid}` text chart to TextScoreViewer only if it has `track.mimeType` OR a hydrated `library_index` row. The Perform route **hydrates `useLibraryStore.allFiles` only for LEADERS** (`LibraryHydrator`, gated on band_leader/admin; `useLibrary` needs auth), and `bulkAddTracks`/clone **never stamped `track.mimeType`**. So musicians / anon / public got NO signal → default `'pdf'` → text bytes hit PDFViewer → fail. PDF charts worked because default-pdf was correct. **Role-dependent → invisible to Daniel as admin.** Real blast radius: all 7 text charts on `7e005452` + the other June-20 service (`7c640a8a` Staff Concert, 14 tracks).

**Data heal (DONE):** `scripts/v11-5-fix-track-mimetype-global.mjs` — standalone admin-SDK mirror of `backfill_track_mimetype` (member MCP bearer 403s on the write tool). Global, idempotent, dry-run-first. Applied: **42 tracks / 6 setlists** stamped (28 pdf + 14 text), 0 skipped, re-run confirms 0 remaining. Both June-20 Camp Sabra services healed. Verified live **anonymous**: David Melech + Shake It Off text charts render (chords/structure/lyrics), 0 console errors.

**Code fix (DONE, `d72b978914`, pushed master → Vercel deploying):**
- Read-time (bulletproof, ALL write paths): `getTracksForSetlist` (`src/lib/server-tracks.ts`) backfills missing `mimeType` from `library_index` when building the Perform SSR frame. Zero extra reads once stamped; read-only; covers all 6 server consumers.
- Write-time: `bulkAddTracks` (`src/lib/mcp/server-tracks-write.ts`) now stamps `mimeType` via the tool resolver (`readLibraryMimeType` in `setlist-write.ts`), mirroring `add_track_to_setlist`.
- Tests: `src/lib/__tests__/server-tracks-mime-enrich.test.ts` (5) + setlists/track-count (19) green. tsc clean (changed files; pre-existing chord-cache/route.test.ts error is unrelated). `next build --webpack` exit 0.

**No further fire action needed.** See memory [[project_track_mimetype_render_outage]].

---

## Current State (PAUL — paused, all committed + pushed)

**Milestone:** v11.5 — Bulletproof Performance. **Phase v11.5-02 (performance surface) — 3 of 4 plans done:**
- 01 H3 seekable audio ✅ `c687db99ee`
- 02 H7/F1 ✅ `27c93a788b` (TTFB infra-bound + "next service" CTA)
- 03 F2 ✅ `80086d1f71` (closed as already-shipped — live-director)
- 04 H1 — **PLAN not written; recon COMPLETE** in `.paul/research/v11-5-02-04-H1-recon-2026-06-14.md`.

**Loop Position (H1):**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     (recon done; PLAN pending)
```

---

## What Was Done (this session)

- Diagnosed + resolved FIRE #2 (text-chart render outage) end to end: live probe → root cause (mimeType routing + leader-only hydration) → global data heal → durable code fix → live anon verification → committed/pushed (`d72b978914` + STATE `20990027f4`).
- Confirmed FIRE #1 (byte truncation) still fixed; the two are distinct.

## What's In Progress

- **H1 PLAN** — recon done, plan unwritten. Resume from `.paul/research/v11-5-02-04-H1-recon-2026-06-14.md` (design fork + recommended surface).

## What's Next

**Immediate:** write the v11.5-02-04 H1 PLAN from the recon note → APPLY → UNIFY → phase transition (H1 = LAST plan of v11.5-02). UI-touching → **/ui-ux-pro-max BLOCKING**; iPad UAT before close.
**Fires:** none open. **Operational (unchanged):** revert David admin→band_leader at Daniel's discretion. **Flagged (not actioned):** `/perform` cold·mobile CLS 0.250 + Vercel TTFB infra follow-up.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/research/v11-5-02-04-H1-recon-2026-06-14.md` | H1 recon (resume the plan from here) |
| `src/lib/server-tracks.ts` | Perform SSR frame — read-time mimeType backfill (fire #2 fix) |
| `scripts/v11-5-fix-track-mimetype-global.mjs` | Global mimeType heal (dry-run default, `--apply`) |
| `src/components/performance/resolveViewerKind.ts` / `PDFOverlay.tsx` | Chart viewer routing |

## Resume Instructions

1. `git pull` first (multi-computer). master is prod; push `origin master` (NOT master:main).
2. Read `.paul/STATE.md` → loop position.
3. `/paul:resume` → write H1 PLAN from the recon note → APPLY → UNIFY → phase transition.

---

*Handoff created: 2026-06-14 evening*
