# PAUL Handoff

**Date:** 2026-04-18
**Status:** paused — user installing Firebase CLI to deploy Firestore rules

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — worship band setlist/chart app
**Core value:** Musicians can instantly access setlists, transpose charts, and control monitor mixes — live on any device, no paper or prep needed.

---

## Current State

**Version:** v4.4 (complete) — awaiting v4.5 scope
**Phase:** None active (between milestones)
**Plan:** None

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     [Milestone v4.4 complete — awaiting next milestone]
```

---

## What Was Done (This Session)

- Pulled remote master to sync this (stale) local machine, resolved all merge conflicts (accepted remote)
- Diagnosed two production bugs from console errors:
  1. **"Failed to save" toasts** — `Transaction.update()` called with `undefined` field values in nested track objects
  2. **"Missing or insufficient permissions"** — `system/globalAlert` collection had no Firestore security rule
- Found a third issue during research: `flush-schema.ts` missing `tune` and `pageNumber` fields (strict validation would reject flush saves)
- Applied three fixes:
  1. `src/lib/setlist-firebase.ts:139` — changed `stripUndefined()` to `stripUndefinedDeep()` (matches pattern used by `createSetlist` and `swapTrack`)
  2. `firestore.rules` — added `system/globalAlert` rule (signed-in read, admin write) before deny-all fallback
  3. `src/lib/flush-schema.ts` — added `tune` and `pageNumber` fields to `flushTrackSchema`
- All 1324 tests pass, TypeScript clean
- Committed and pushed to origin/master (commit `23725597`)

---

## What's In Progress

- **Firestore rules deployment pending** — user is installing Firebase CLI on this machine to run `firebase deploy --only firestore:rules`. The code fix is pushed but the globalAlert rule won't take effect in production until deployed.

---

## What's Next

**Immediate:** Deploy Firestore rules to production via `firebase deploy --only firestore:rules`

**After that:** Verify fixes on production (no more "failed to save" toasts, no more globalAlert permission errors in console). Then `/paul:discuss-milestone` to define v4.5 scope.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview |
| `.paul/MILESTONES.md` | Milestone history |
| `firestore.rules` | Firestore security rules (needs deploy) |
| `src/lib/setlist-firebase.ts` | Setlist CRUD with Firestore transactions |
| `src/lib/flush-schema.ts` | Strict write-boundary schemas for /api/setlist/flush |

---

## Key Context

- This local machine was stale — user has been working on a different computer
- Missing `.env.local` on this machine (full `npm run build` fails due to missing Firebase env vars — not a code issue)
- Firebase CLI was not installed; user is installing it now
- Vercel auto-deploys master to production (code changes are live), but Firestore rules require separate `firebase deploy`

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Deploy Firestore rules if not yet done: `firebase deploy --only firestore:rules`
3. Verify production — no console errors on setlist edit
4. Run `/paul:resume` or `/paul:progress`

---

*Handoff created: 2026-04-18*
