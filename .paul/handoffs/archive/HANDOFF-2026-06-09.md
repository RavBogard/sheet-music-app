# PAUL Handoff

**Date:** 2026-06-09 (session 3 — pause after v11-05-03 loop + v11-05-04 plan)
**Status:** paused (clean — tree clean, ahead 6 local commits, nothing pushed)

---

## READ THIS FIRST

You have no prior context. This document + STATE.md tell you everything.

**Project:** sheet-music-app (centralreform.live) — band charts/recordings on iPads; setlists authored via Claude + MCP.
**Core value:** The band gets the right charts + recordings on their iPads each week, and Daniel authors setlists conversationally via Claude + MCP.
**Current milestone:** v11.0 Brothers Lazaroff Multi-Tenant — org-scoped instance for David Lazaroff on **brotherslazaroff.live**.

---

## Current State

**App:** package.json `10.1.0`. Production branch `master`; push `origin master` (NOT `master:main`).
**Milestone:** v11.0 — 6 phases: v11-01..04 ✅ · **v11-05 IN PROGRESS** · v11-06 (isolation audit) pending.
**Phase v11-05** (cross-tenant collection scoping) — **5 vertical slices**:
```
PLAN ──▶ APPLY ──▶ UNIFY        [01 ✅ · 02 ✅ · 03 ✅ — v11-05-04 PLAN ✓, awaiting APPLY]
  ✓        ○        ○     (3 of 5 loop-complete; 05 still to plan)
```
⚠️ The PAUL file-count heuristic will think the phase is complete (3 PLANs / 3 SUMMARYs) — it is NOT. Slices 04 (planned, not applied) + 05 (not planned) remain.

**Local commits this session (UNPUSHED — origin/master..HEAD, ahead 6):**
- `e166be2928` feat(v11-05-01) — templates
- `87483e29b9` feat(v11-05-02) — users roster
- `b9186ac449` docs(paul) — session-2 pause
- `9568fdc7a4` feat(v11-05-03) — scheduling_assignments (read+write)
- `f294812ace` docs(v11-05-03) — close loop
- `1bb1f067f0` docs(v11-05-04) — congregation plan

---

## What Was Done (this session)

1. `/paul:resume` → confirmed state vs git (ahead, nothing to pull).
2. **v11-05-03 (scheduling_assignments)** FULL LOOP (plan→apply→unify→commit `9568fdc7a4`):
   - Single `orgId` denormalized from the parent setlist (new `rowOrg` helper in membership.ts), stamped at create in `assignMusiciansService` (one path, HTTP+MCP).
   - 8 cross-tenant caller-facing reads filtered in-memory (crc-default): client `subscribeToAllUpcomingAssignments`; web suggest-band/history(+`scheduling_history`)/remind; MCP listPendingAssignments/list_musicians_on_date/suggestBand/list_service_personnel.
   - Inherit/intentional left untouched (setlistId/uid-scoped + the all-tenant cron).
   - Gates: tsc 0 · eslint 0 · full suite **3312/0** · mcp-roster emulator **50/50** (+4 org cases) · backfill node-check clean.
3. **v11-05-04 (congregation singleton)** PLANNED + committed `1bb1f067f0` (NOT applied).

---

## What's In Progress

Nothing mid-edit. Tree clean. v11-05-04 PLAN written and ready to apply.

---

## What's Next

**Immediate:** `/paul:apply .paul/phases/v11-05-collection-scoping/v11-05-04-PLAN.md` — congregation singleton → per-org via doc-id namespacing (`congregationDocId(org)`: crc bare = zero migration, others `congregation__{org}`). 3 tasks: (1) helper + thread org through `getServerCongregationConfig` (server-auth:98) + print-pipeline:662 + roster suggestBand:812 + suggest-band route:51 + MCP getCongregationContext (orgFrom @ index.ts:510); (2) client `congregation-store.ts` reads `coerceOrgId(document.documentElement.dataset.org)` → congregationDocId; (3) seed `config/congregation__brotherslazaroff` (scripts/seed-bl-congregation.mjs) + emulator/unit cases. PLAN has the full code-grounded map.

**After that:** v11-05-05 (CreationWizard in-app orgId-stamp + de-synagogue vocab — **/ui-ux-pro-max BLOCKING**) → phase-close deploy gate → **v11-06** isolation audit.

---

## PHASE-CLOSE DEPLOY GATE (batch — Daniel chose batch-at-close)

Run scripts (dry-run→inspect→`--apply`) THEN push the whole phase once:
- **v11-05-01 (HARD):** `node scripts/backfill-orgid-v11.mjs --apply` — list_templates equality filter; CRC templates vanish unless stamped first.
- **v11-05-02 (SOFT):** `node scripts/backfill-user-orgids.mjs --apply` + `node scripts/fix-david-orgids-claim.mjs --apply`.
- **v11-05-03 (SOFT):** `node scripts/backfill-assignment-orgids.mjs --apply` (scheduling_assignments + scheduling_history).
- **v11-05-04 (HARD-ish for correct BL identity):** `node scripts/seed-bl-congregation.mjs --apply` (once written by 04's apply).
- Prod-script auth on this box: firebase-CLI refresh-token → temp ADC (see STATE Blockers/Concerns).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live state — Loop Position has the slice map + DEPLOY GATE |
| `.paul/phases/v11-05-collection-scoping/CONTEXT.md` | phase rationale + 3 baked decisions |
| `.paul/phases/v11-05-collection-scoping/v11-05-04-PLAN.md` | the ready-to-apply plan |
| `.paul/phases/v11-05-collection-scoping/v11-05-0{1,2,3}-SUMMARY.md` | shipped slices |
| `src/lib/org/membership.ts` | `rowOrg` (single) / `rowOrgIds` (array) helpers |
| `src/lib/org/registry.ts` | `coerceOrgId` / `DEFAULT_ORG_ID` (home for `congregationDocId`) |

---

## Reusable gotchas (carry forward)

- v11.0 AUTONOMY directive is binding: auto-proceed plan→plan & phase→phase, auto-commit per phase, batch push at phase close, bake decisions (no checkpoint:decision). Quality floor held (tsc+tests+emulator+AC proof). STOP only for ambiguity / unresolvable failure / cross-tenant leak.
- Shared-signature changes retroactively break pre-existing test fixtures — audit + update them (hit scheduling-firebase.test.ts + use-calendar-data.test.ts this session).
- Emulator org cases go INTO an existing working `*.emulator.test.ts` harness (mcp-roster), not a fresh file (vitest app/instance split).
- Commit with `git commit -F <file>` (or a heredoc), NOT the PowerShell `@'...'@` here-string via Bash (leaks `@` into the subject).
- In-memory rowOrg filter with crc-default is the v11-05 scoping pattern — CRC-safe with no backfill, no composite index.

---

## Resume Instructions

1. `git pull` (multi-computer — rebase the 6 local v11-05 commits on top of any v7.1 `.coord/` pushes).
2. Read `.paul/STATE.md` (Loop Position + Session Continuity).
3. `/paul:resume` → routes to `/paul:apply .paul/phases/v11-05-collection-scoping/v11-05-04-PLAN.md`.

---
*Handoff created: 2026-06-09 (session 3)*
