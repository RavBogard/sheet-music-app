# PAUL Handoff

**Date:** 2026-06-09 (session 4 — v11-05 SHIPPED, paused at milestone 5/6)
**Status:** paused (clean — tree clean, in sync with origin/master, prod READY)

---

## READ THIS FIRST

You have no prior context. This document + STATE.md tell you everything.

**Project:** sheet-music-app (centralreform.live) — band charts/recordings on iPads; setlists authored via Claude + MCP.
**Core value:** The band gets the right charts + recordings on their iPads each week, and Daniel authors setlists conversationally via Claude + MCP.
**Current milestone:** v11.0 Brothers Lazaroff Multi-Tenant — org-scoped instance for David Lazaroff on **brotherslazaroff.live**.

---

## Current State

**App:** package.json `10.1.0`. Production branch `master`; push `origin master` (NOT `master:main`).
**Milestone v11.0:** 6 phases — v11-01..05 ✅ COMPLETE + SHIPPED · **v11-06 (isolation audit) is the only one left** (the close gate).

```
PLAN ──▶ APPLY ──▶ UNIFY        [v11-05: all 5 slices ✅ — SHIPPED + LIVE-VERIFIED]
  ✓        ✓        ✓
```

**Git:** `master` IN SYNC with `origin/master` (pushed through `3cae68945f`). Tree clean. Vercel prod READY. `git pull` first next session (multi-computer).

---

## What Was Done (this session)

1. `/paul:resume` → reconciled state (archived the prior session-3 handoff).
2. **v11-05-04** (congregation per-org doc-id) FULL LOOP → `9ec721bc41`. tsc 0, suite 3316/0, emulator 9/9.
3. **v11-05-05** (in-app orgId stamp + de-synagogue vocab) PLAN + FULL LOOP → `d0eaef4a27`. tsc 0, suite **3323/0**, /ui-ux-pro-max invoked. Closed v11-05 code.
4. **PHASE-CLOSE DEPLOY GATE** ran end-to-end (dry-run→inspect→--apply each, in order):
   - `backfill-orgid-v11 --apply` — 4 setlistTemplates → orgId='crc' (HARD gate satisfied).
   - `fix-david-orgids-claim --apply` (ran FIRST) — David claim → ['crc','brotherslazaroff'].
   - `backfill-user-orgids --apply` — 19 users (18→[crc], David→[crc,brotherslazaroff]), 0 errors.
   - `backfill-assignment-orgids` — 0 docs, no-op.
   - `seed-bl-congregation --apply` — config/congregation__brotherslazaroff written.
   - Prod-script auth: firebase-CLI refresh-token → temp ADC (built ad-hoc, deleted after).
5. **DEPLOY-FIX:** first push ERRORED at compile (`Can't resolve 'fs'`) — client modules import `org/membership.ts` whose lazy `firebase-admin` import was pulled into the client bundle. Split `getUserOrgIds`/`getPrimaryOrgForMinting` → `src/lib/org/membership-server.ts` (membership.ts now pure). Verified `SKIP_ENV_VALIDATION=1 next build` (compiles + 92 pages). `fix(v11-05)` `f3448a8395`. Captured the lesson in STATE Blockers + the `next build` memory.
6. Re-pushed → Vercel READY → live probe: CRC /perform `data-org="crc"` "CRC Music" (intact); BL /perform `data-org="brotherslazaroff"` "Brothers Lazaroff".
7. Phase close: STATE/ROADMAP/paul.json → v11-05 complete, milestone 5/6, advanced to v11-06. `docs(v11-05)` `3cae68945f`.

---

## What's In Progress

Nothing mid-edit. Tree clean, all pushed. v11-05 fully shipped + verified.

---

## What's Next

**Immediate:** `/paul:plan v11-06` — **cross-tenant isolation security audit** (the milestone close gate).
MUST cover:
- The **DEFERRED** `leadHistory`/`getAllSetlists` cross-tenant read (from v11-05-04 — `getCongregationContext` returns all-tenant `leadHistory`; setlists are public-by-design so it's a UX nit, but the audit should decide whether to scope it via getAllSetlists's v11-04-03 opt-in `org` filter).
- Live-verify every v11-05 collection (templates / users / scheduling_assignments / congregation) is BL-isolated + CRC-intact, plus MCP reads/writes.
- Reusable probe: `scripts/e2e-bl-tenant-probe.mjs` (needs `DAVID_BEARER` + `CRC_BEARER` env).
- Quality floor: emulator-backed rules tests + the audit itself are mandatory/blocking.

**After that:** v11-06 close → milestone v11.0 complete (`/paul:complete-milestone`).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live state — Loop Position has the v11-05 SHIPPED record + the next-build lesson in Blockers |
| `.paul/ROADMAP.md` | v11-01..05 ✅, v11-06 remaining |
| `scripts/e2e-bl-tenant-probe.mjs` | Reusable BL/CRC isolation probe for v11-06 |
| `src/lib/org/membership.ts` / `membership-server.ts` | Pure helpers (rowOrg/rowOrgIds) vs server resolvers — keep the split |
| `src/lib/org/registry.ts` | coerceOrgId / congregationDocId / DEFAULT_ORG_ID |

---

## Reusable gotchas (carry forward)

- **`tsc`+`vitest` miss client/server bundle-boundary breaks — only `next build` does.** Run `SKIP_ENV_VALIDATION=1 npx next build` before declaring any phase touching shared lib/client modules deployable. (Caused this session's deploy ERROR.) See STATE Blockers + [[feedback_nextjs_route_exports]].
- Prod-script auth on this box: firebase-CLI refresh-token (`~/.config/configstore/firebase-tools.json`) → temp `authorized_user` ADC (firebase-tools public OAuth client) → `GOOGLE_APPLICATION_CREDENTIALS`; delete after.
- v11.0 AUTONOMY directive still binding (auto-proceed, auto-commit/push per phase, dry-run-inspect-before-apply on backfills). STOP only for ambiguity / unresolvable failure / cross-tenant leak / CRC lock-out.
- Commit with `git commit -F <file>` or a bash heredoc, NOT the PowerShell `@'...'@` here-string via Bash.

---

## Resume Instructions

1. `git pull` (multi-computer).
2. Read `.paul/STATE.md` (Loop Position + Session Continuity + Blockers).
3. `/paul:resume` → routes to `/paul:plan v11-06` (isolation audit, the close gate).

---
*Handoff created: 2026-06-09 (session 4)*
