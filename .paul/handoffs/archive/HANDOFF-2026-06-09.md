# PAUL Handoff

**Date:** 2026-06-09
**Status:** paused (clean — v11-04-01 SHIPPED + LIVE-VERIFIED; v11-04-02 planned & committed, ready to APPLY)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — band charts/recordings on iPads; setlists authored via Claude + MCP.
**Core value:** The band gets the right charts + recordings on their iPads each week, and Daniel authors setlists conversationally via Claude + MCP.
**Current milestone:** v11.0 Brothers Lazaroff Multi-Tenant — give David Lazaroff his own org-scoped instance on **brotherslazaroff.live** (now LIVE, branded, tenant-isolated; sign-in working).

---

## Current State

**Milestone:** v11.0 — now **6 phases** (split 2026-06-08): v11-01 ✅ · v11-02 ✅ · v11-02b ✅ · v11-03 ✅ · **v11-04 🚧 (1 of 3 plans done)** · v11-05 (collection scoping) · v11-06 (isolation audit).
**Phase:** v11-04 BL consumer surface + onboarding — **in progress**.
**Plan:** v11-04-01 ✅ LOOP COMPLETE + shipped. **v11-04-02 ✅ planned (PLAN.md written), awaiting APPLY.**
**App:** package.json `10.1.0`. Production branch `master`; push `origin master` (NOT `master:main`). origin/master IN SYNC, tree clean.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY        [v11-04-02 PLAN ✓ — next: APPLY]
  ✓        ○        ○
```

---

## What Was Done (this session, 2026-06-09)

1. **Resumed + split the milestone** — carved cross-tenant collection scoping out of v11-04 into a new **v11-05**; isolation audit → **v11-06**. (Daniel: "split".)
2. **v11-04-01 — fixed a LIVE cross-tenant data leak** (`feat(v11-04-01)` `c606992756`). `brotherslazaroff.live/perform` was serving CRC's setlists (the public web read path was never org-scoped — v11-02 only walled MCP). Org-scoped BOTH the SSR admin fetch (`getAllSetlists` opt-in `org` filter) and the client Firestore subscription (`subscribeToAllSetlists` + `useOrg()`); made `/perform` **per-host dynamic** (dropped shared path-keyed ISR); added `(orgId,date)` index (deployed); +5 regression tests. tsc clean · full suite 3297/0 · eslint 0. **LIVE-VERIFIED:** BL /perform shows zero CRC setlists (empty BL state); CRC /perform unchanged.
3. **Fixed BL Google sign-in** (`fix(v11-04)` `dd1593592c`). It failed with `auth/unauthorized-domain` — BL domains weren't on Firebase Auth's authorized-domains allowlist (project-wide config). Added `brotherslazaroff.live` + `www.` via new reusable **`scripts/add-auth-domains.mjs`** (Identity Toolkit admin API, firebase-CLI-token auth). **LIVE-VERIFIED via Playwright:** sign-in now opens the real Google OAuth page. This is auth config (works now, no deploy needed).
4. **Confirmed infra is complete** — apex + www both on Vercel serving the app (apex 301→www); nothing more needed in Vercel, Squarespace DNS, OR Google Cloud OAuth (Firebase routes OAuth through `crcmusiccharts.firebaseapp.com/__/auth/handler`, which is domain-independent — only Firebase authorized-domains is per-tenant).
5. **Planned v11-04-02** (`docs` `fe9e5001f1`) — org-aware branding/metadata.

**Commits (all pushed to origin master):** `feat(v11-04-01)` `c606992756` · `docs(v11-04-01)` `199079df70` · `fix(v11-04)` `dd1593592c` · `docs(v11-04-02)` `fe9e5001f1`.

---

## What's In Progress

Nothing mid-flight. v11-04-01 closed + shipped + verified. v11-04-02 is fully planned and committed; not yet executed.

---

## What's Next

**Immediate:** `/paul:apply v11-04-02` — org-aware consumer branding/metadata. **`/ui-ux-pro-max` is BLOCKING** (UI phase; required before Task 1). Three tasks:
1. Extend `src/lib/org/branding.ts` with metadata/PWA fields (data-driven; CRC values byte-identical).
2. Root `src/app/layout.tsx` static metadata → `generateMetadata()` (org title/og/twitter/appleWebApp/metadataBase/manifest) + create `public/manifest-brotherslazaroff.json`.
3. `PublicSetlistListing` "CRC Music" wordmark → org shortName + `perform/page.tsx` title vocab (add a `vocab.ts` key).

Then **v11-04-03** — David onboarding + empty-library verify + **authed-dashboard read scoping** (the still-cross-tenant `getUpcomingSetlists`/`getRecentSetlists`/`getSetlistsPage` + the non-/perform `subscribeToAllSetlists` callers: DashboardClient, SetlistDrawer, use-add-to-setlist, use-setlist-dashboard) + e2e UAT.

**After v11-04:** v11-05 (cross-tenant collection scoping: templates/roster/congregation/personnel R+W + CreationWizard vocab) → v11-06 (isolation audit, close gate).

---

## Known remaining cross-tenant gaps (NOT leaks on the public surface — tracked)

- **Authed dashboard reads still cross-tenant:** `getUpcomingSetlists`/`getRecentSetlists`/`getSetlistsPage` (via `server-auth.ts`, `(main)/setlists`, `/api/setlists/page`) and `subscribeToAllSetlists` callers other than PublicSetlistListing. Opt-in org filter means they're unchanged (no regression), but an authed BL user would see CRC setlists in the dashboard. → **v11-04-03**.
- The opt-in `org` filter on `getAllSetlists`/`subscribeToAllSetlists` = secure only when the caller passes org. Public /perform always does; document for future callers.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (Session Continuity has the resume pointer) |
| `.paul/ROADMAP.md` | Phase overview (v11-01..03 ✅ · v11-04 1/3 · v11-05/06 pending) |
| `.paul/phases/v11-04-consumer-surface-onboarding/CONTEXT.md` | v11-04 scope + taste calls + the live-findings log |
| `.paul/phases/v11-04-consumer-surface-onboarding/v11-04-01-SUMMARY.md` | What shipped in 01 |
| `.paul/phases/v11-04-consumer-surface-onboarding/v11-04-02-PLAN.md` | The next plan to APPLY |
| `src/lib/server-setlists.ts` | `getAllSetlists` opt-in org filter (01) |
| `src/lib/org/{branding,vocab,registry,org-context}.ts` | org branding/vocab/coerce/useOrg seams |
| `scripts/add-auth-domains.mjs` | add a new tenant host to Firebase Auth authorized domains |
| `docs/brotherslazaroff-domain-setup.md` | DNS/Vercel/Firebase-auth onboarding checklist (all done) |

---

## Resume Instructions

1. `git pull` (multi-computer — other boxes may push v7.1 `.coord/` work).
2. Read `.paul/STATE.md` (Session Continuity) + `v11-04-02-PLAN.md`.
3. Run `/paul:resume` — it routes to `/paul:apply v11-04-02`. Load `/ui-ux-pro-max` first (BLOCKING for the UI phase), then apply.

---
*Handoff created: 2026-06-09*
