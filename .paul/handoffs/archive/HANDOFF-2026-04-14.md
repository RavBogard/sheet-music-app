# PAUL Handoff

**Date:** 2026-04-14
**Status:** paused — context-clear preparation
**Session length:** Phase 3 closer (1 plan) + Phase 4 (6 plans + 1 audit-only)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — Next.js app for the worship band
**Core value:** Musicians instantly access setlists, transpose charts, control monitor mixes — live on any device, no paper.

**Critical user preferences (from memory):**
- Push to `origin master`, NOT `origin master:main`
- Deploy straight to production on Vercel — no preview branches
- Never use local dev server — push to prod (no users yet)
- App must be "bulletproof and easy and intuitive" before band onboarding
- User works from multiple computers — pull before starting
- /ui-ux-pro-max skill REQUIRED for any frontend phase per `.paul/SPECIAL-FLOWS.md`

---

## Current State

**Milestone:** v4.2 UX Polish & Band Onboarding — 6 of 8 phases complete (75%)
**Phase 4** (Editor Ergonomics + Noise Cleanup) — wrapped this session: 6 plans + 1 audit note
**Phase 5** (Navigation + Schedule Hygiene) — only remaining phase before milestone close

**Loop position (idle, ready for Phase 5 PLAN):**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [last loop closed; ready to start Phase 5]
```

Last commit: `cbf7a31` — `docs(v4.2-P4-07): audit note — triple-modal "chain" intentionally not consolidated`
Branch: `master`, clean working tree, pushed to `origin/master`. Vercel auto-deploys to prod.

---

## What Was Done This Session

### Phase 3 closer
- **P3-04 (411faad)** — Performance view polish: per-track transposition override in key badge, amber cue-notes bumped to `text-amber-300`, liturgical header is now a ≥44px `<button>` for leaders, `PerformanceOfflineIndicator` reads IDB via `listFileIds` and reports `OFFLINE — N/M CHARTS READY` (graceful fallback). 12 new tests.

### Phase 4 (this session's main work)
- **P4-01 (cbe11da)** — `src/lib/setlist-permissions.ts` with `canEditSetlist(setlist, auth)`; 3 client call sites migrated. `formatEventDate` dedupe (ScheduleCard's divergent local renamed to `formatAssignmentDate`; remind-route now delegates to canonical). `useUpcomingPrep.isLoading` reads subscription's loading flag (was stuck-true on empty snapshot). 10 new tests.
- **P4-02 (037a66f)** — `apiFetch` opt-out 30s `timeout` option; internal AbortController merges with caller signal. `PDFViewer` 60s timeout + abort-on-unmount via effect-owned controller. 5 new tests. Memo audit found already-done — all 5 useSafeFirestoreSync callers already useMemo their refs.
- **P4-03 (2aef757)** — `src/components/dashboard/OnboardingCard.tsx` — single role-aware component with `pickVariant()` (kills the stacking bug). DashboardClient collapsed ~145 lines into one invocation. `TransferSetlistDialog` migrated to AlertDialog primitives. `SetlistHistoryPanel` restore guard moved from `window.confirm` to AlertDialog. 6 new tests.
- **P4-04 (d261734)** — `ONBOARDING_INSTRUMENT_KEYS` exported from `lib/musician-profile.ts`; OnboardingCard now writes slug keys (e.g., `acoustic_guitar`) matching what Settings already writes. `REQUIRED_INSTRUMENTS` promoted to export; scheduling route uses it instead of duplicating.
- **P4-05 (c9d6c89)** — Toast hygiene: collapsed three `toast.dismiss + toast.success/error` triples to single `toast.success/error(..., { id })` calls. Fixed real bug in use-creation-wizard (`toast.dismiss()` no-arg was killing every visible toast).
- **P4-06 (38f4ced)** — Move-Up + Move-Down icon buttons inside the expanded inline panel for both Song and Flow rows (`SongInlineFields` + `FlowInlineFields`). SetlistEditorV2 pre-computes neighbour ids and curries `moveTrack`. SwapPicker `max-h-[70vh]` → `md:max-h-[85vh]`.
- **P4-07 (cbf7a31)** — Audit-only. Investigated the "triple-modal chain" (SearchOverlay + AddSongsModal + AddToSetlistSheet). Decided NOT to consolidate — they serve three distinct intents (bulk-add to setlist; single-song search/replace; pick-a-setlist for a song). Documented rationale in `.paul/phases/04-editor-cleanup/04-07-AUDIT-NOTE.md`.

---

## What's In Progress

Nothing. All loops closed, all SUMMARYs written, all commits pushed to `origin/master`.

---

## What's Next

**Immediate:** Plan Phase 5 — Navigation + Schedule Hygiene. Per ROADMAP.md scope:
- Add Schedule tab to mobile bottom bar
- Strip blockout/availability UI from Schedule page; keep as read-only calendar of services + setlists
- Delete dead `musician_availability` composite index + orphaned code in `UnifiedCalendar.tsx`
- Delete orphan routes `/settings/users` and `/settings/sound`
- Remove `SetlistDrawer` if dead (confirm vs `SetlistView`)
- Trace and remove `monitor-live/commands/pending` if dead
- Preserve RSVP flow, `scheduling_assignments` collection, MusicianPicker on setlists, publish-and-notify emails

**After that:** Milestone v4.2 audit + close, then v4.3 (or band onboarding if Phase 5 is the last gate).

---

## Quality State at Pause

- `npx tsc --noEmit` — clean
- `npx vitest run` — 1153 tests passing across 94 files
- 1 pre-existing failure in `src/components/library/__tests__/song-charts-library.test.tsx` (env-vars / `@t3-oss/env-nextjs` validation) — unrelated to this session, predates it; out of scope and untouched
- Vercel: all commits auto-deployed to prod

---

## Deferred Items (not lost — for next milestone or follow-up plans)

1. One-shot migration to normalize legacy `musicianProfile.instrument` human-string values (e.g., `"Guitar"`) to slug keys (`"acoustic_guitar"`). Low priority — Settings flow normalizes per-user on next visit.
2. Inline-badge UX brainstorm + replacement of routine success toasts. Needs design judgment; not a mechanical refactor.
3. Error-toast sweep on user-initiated silent-catch paths. Each call site needs per-handler judgement; many existing silent-catches are correctly silent (storage failures in private mode, optional fetches).
4. `LibrarySearchList` extraction from the three search modals — the real cleanup hiding behind the misnamed "triple-modal chain" item. Worth a dedicated future plan with /ui-ux-pro-max review.
5. Multi-row keyboard reorder (Cmd+Up/Down) in setlist editor.
6. z-index design tokens — deferred until a bare-metal use case appears (current AlertDialog primitives own their z internally).
7. CRIT-003 bridge credentials design discussion (carried from v1.3).
8. LOW-004 `leader → band_leader` Firestore data migration (carried from v1.3).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state — read this first on resume |
| `.paul/ROADMAP.md` | v4.2 phase overview; Phase 5 scope is documented there |
| `.paul/PROJECT.md` | Project context, tech stack, decisions log (just updated for Phase 3) |
| `.paul/SPECIAL-FLOWS.md` | Lists /ui-ux-pro-max as required for any frontend phase |
| `.paul/phases/04-editor-cleanup/` | All 6 P4 plans + summaries + 04-07 audit note |
| `sheet-music-app/src/lib/setlist-permissions.ts` | New helper from P4-01 |
| `sheet-music-app/src/lib/musician-profile.ts` | INSTRUMENT_PRESETS + ONBOARDING_INSTRUMENT_KEYS |
| `sheet-music-app/src/components/dashboard/OnboardingCard.tsx` | New role-aware card from P4-03 |

---

## Resume Instructions

1. New session: `/paul:resume` (or just describe the task). It will read STATE.md and route.
2. STATE.md's Session Continuity points the resume command at this handoff and at Phase 5 as the next action.
3. If starting Phase 5: `/paul:plan` then load `/ui-ux-pro-max` before APPLY.
4. If auditing: read `.paul/phases/04-editor-cleanup/*-SUMMARY.md` + the audit note.
5. Pull before starting (user works from multiple computers).

---

*Handoff created: 2026-04-14, end of long Phase 4 session*
