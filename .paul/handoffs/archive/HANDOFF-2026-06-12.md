# PAUL Handoff

**Date:** 2026-06-12
**Status:** paused (mid-APPLY, Plan v11.5-01-01 — between Task 1 and Task 2)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — multi-tenant worship/band sheet-music app. Browser = band/consumer surface (Perform mode on iPads); Daniel authors via Claude + MCP. 2 live tenants: CRC (centralreform.live) + Brothers Lazaroff (brotherslazaroff.live).
**Core value:** The band gets the right charts + recordings on their iPads each week; the web app's job is **performance + quick edits** (doctrine: bulletproof > novel).

---

## Current State

**Milestone:** v11.5 — Bulletproof Performance (🚧 OPENED 2026-06-11; 5 phases; oracle `docs/ACCESS-POLICY.md` v0.4).
**Phase:** v11.5-01 of 5 — Tenancy + anon correctness (split: 01 = H4 / 02 = H5).
**Plan:** v11.5-01-01 (H4 header branding leak) — **APPLY in progress.**

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ◐        ○      (APPLY: Task 1 done+qualified; Task 2 not started)
```

---

## What Was Done

- **v11.5 milestone opened** (`/paul:discuss-milestone` → `/paul:milestone`): ROADMAP Active Milestone + 5 phase-detail sections + 7 constraints; STATE re-pointed; `paul.json` synced; 5 phase dirs created; MILESTONE-CONTEXT.md consumed + deleted; v11.6 backlog repointed.
- **Plan v11.5-01-01 created + approved** (`.paul/phases/v11.5-01-tenancy-anon-correctness/v11.5-01-01-PLAN.md`; standard, 2 tasks, autonomous).
- **VERIFY-FIRST (H4) complete** — leak vector confirmed: `/perform/setlist/[id]` (+ its `track/[trackId]` sub-route) live OUTSIDE the `(main)` route group. `perform/setlist/layout.tsx` was a `"use client"` layout rendering `<AppNavigation/>` with NO props → `DesktopHeader`/`MobileHeader` fell back to the `useCongregation()` CRC default ("CRC Music" + `/logo.jpg`). The v11.1-01 `(main)`-layout server-branding fix never reached this group.
- **`/ui-ux-pro-max` loaded** (BLOCKING gate satisfied) — confirmed this is plumbing (brand consistency + FOUC elimination), no new visual design.
- **Task 1 DONE + QUALIFIED (PASS):** converted `src/app/perform/setlist/layout.tsx` from a client pass-through to an **async server** layout that resolves `coerceOrgId((await headers()).get("x-org-id"))` → `getOrgBranding(orgId)` and threads `serverOrgShortName`/`serverLogoUrl`/`serverWordmarkUrl` into `<AppNavigation>` (exact mirror of `src/app/(main)/layout.tsx`). One fix covers detail page + track sub-route. Branding-only (no auth/role prop changes). `SKIP_ENV_VALIDATION=1 npx tsc --noEmit` → **clean (exit 0)**.

---

## What's In Progress

- **Task 2 — NOT STARTED** (resume here). Two parts:
  1. **Regression test** — create `src/app/perform/setlist/__tests__/layout.test.tsx`. Mirror `src/app/perform/__tests__/page.test.tsx`: mock `next/headers` with a mutable `mockOrgHeader`; **stub** `@/components/nav/AppNavigation` (avoids its heavy client-import chain); `import SetlistPerformLayout from "../layout"`; `await SetlistPerformLayout({children: null})` returns a Fragment whose `props.children = [<AppNavigation .../>, null]` — find the AppNavigation element and read its props. Use the **real** `coerceOrgId` + `getOrgBranding` (don't mock — exercise true resolution).
     - **Case A (AC-1, broslaz):** `mockOrgHeader="brotherslazaroff"` → props equal `getOrgBranding("brotherslazaroff")` trio: `serverOrgShortName==="Brothers Lazaroff"`, `serverLogoUrl===""`, `serverWordmarkUrl==="/brands/brotherslazaroff/wordmark.png"`. Anti-leak: `serverOrgShortName !== "CRC Music"` AND `serverLogoUrl !== "/logo.jpg"`.
     - **Case B (AC-2, crc + absent):** `mockOrgHeader="crc"` and `mockOrgHeader=null` → both give crc trio: `"CRC Music"` / `"/logo.jpg"` / `""` (byte-identical default).
     - Header the file with an H4 comment citing the stress cell id.
  2. **H4 stress cell (Task 2b)** — append an H4 cell to `.paul/research/STRESS-TEST-PROMPT-2026-06-RUN3.md` (NOT `-2026-06.md`): "open /perform/setlist/<id> on brotherslazaroff.live → header shows BL wordmark/monogram + 'Brothers Lazaroff', never 'CRC Music'/CRC logo; repeat on centralreform.live → CRC brand unchanged." Use the per-executor BUG-ID range convention already in that file.

**Reference data (confirmed this session, for the test):**
- `getOrgBranding(orgId)` returns `{shortName, logoUrl, wordmarkUrl, ...}`; `BRANDING[orgId] ?? BRANDING.crc`.
  - crc: `shortName:"CRC Music"`, `logoUrl:"/logo.jpg"`, `wordmarkUrl:""`
  - brotherslazaroff: `shortName:"Brothers Lazaroff"`, `logoUrl:""`, `wordmarkUrl:"/brands/brotherslazaroff/wordmark.png"`
- `coerceOrgId(value: string|null|undefined): OrgId` — returns value if known, else `DEFAULT_ORG_ID` (crc). So `null`/unknown → crc.

---

## What's Next

**Immediate:** Finish Task 2 (test + stress cell), then run the plan's verification gates:
- `SKIP_ENV_VALIDATION=1 npx vitest run src/app/perform/setlist/__tests__/layout.test.tsx` — green (BL + crc + absent cases)
- `SKIP_ENV_VALIDATION=1 npx next build --webpack` — clean (route/layout-touching + client→server conversion: catches the route-export + client/server bundle-boundary lessons; `--webpack` = what Vercel runs)
- tsc already clean from Task 1 (re-confirm after the test lands).

**After that:** `/paul:unify .paul/phases/v11.5-01-tenancy-anon-correctness/v11.5-01-01-PLAN.md` to close the loop, write SUMMARY, append the NON-BLOCKING UAT item (live broslaz-iPad visual confirm) to `.paul/UAT-PENDING.md`, and auto-commit+push to `master` (autonomy posture). Then chain to **Plan v11.5-01-02 (H5 — anon chord-cache write 401, server route, no UI gate)**.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | v11.5 Active Milestone + phase sections |
| `.paul/phases/v11.5-01-tenancy-anon-correctness/v11.5-01-01-PLAN.md` | The plan being executed (H4) |
| `src/app/perform/setlist/layout.tsx` | **Task 1 fix (done)** — host-org-aware server layout |
| `src/app/(main)/layout.tsx` | Reference pattern the fix mirrors (DO NOT touch) |
| `src/app/perform/__tests__/page.test.tsx` | Test template for Task 2 |
| `src/lib/org/branding.ts` · `src/lib/org/registry.ts` | `getOrgBranding` / `coerceOrgId` (consume, don't modify) |
| `.paul/research/MILESTONE-BRIEF-v11.5-bulletproof-performance.md` | Ratified v11.5 spec |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position.
2. Confirm Task 1 is in the tree: `git diff src/app/perform/setlist/layout.tsx` (async server layout, branding props).
3. **Re-load `/ui-ux-pro-max`** before touching UI (BLOCKING gate for this plan; a fresh session won't have it loaded — though Task 2 is test+docs, the plan's UI gate is already satisfied for the code change).
4. Execute Task 2 (test + stress cell), run the 3 verification gates, then `/paul:unify` the plan.
5. Autonomy posture binding: run autonomously, auto-commit+push per phase to prod `master`, quality floor held, `/ui-ux-pro-max` BLOCKING on UI phases. STOP only for product ambiguity / unresolvable quality-gate failure / cross-tenant leak / CRC lock-out.

---

*Handoff created: 2026-06-12*
