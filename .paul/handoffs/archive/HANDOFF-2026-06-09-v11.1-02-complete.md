# PAUL Handoff

**Date:** 2026-06-09 (session 6 — v11.1 opened; phase v11.1-02 COMPLETE + shipped)
**Status:** paused (clean — phase committed + pushed, tree clean, in sync with origin/master)

---

## READ THIS FIRST

You have no prior context. This document + STATE.md tell you everything.

**Project:** sheet-music-app (centralreform.live) — band charts/recordings on iPads; setlists authored via Claude + MCP. MULTI-TENANT (2nd live tenant: Brothers Lazaroff on brotherslazaroff.live).
**Core value:** The band gets the right charts + recordings on their iPads each week, and Daniel authors setlists conversationally via Claude + MCP.

---

## Current State

**Milestone:** v11.1 Brothers Lazaroff Post-Launch Fixes — 🚧 IN PROGRESS (opened 2026-06-09). **1 of 4 phases complete.**
**Phase:** v11.1-02 (Multi-org membership + authoring) — ✅ COMPLETE (2/2 plans shipped). **Next phase: v11.1-01 (branding, P0) — needs PLAN.**
**Version:** package.json `11.0.0` (no bump this phase). origin/master tip `648c0a3278`.

```
PLAN ──▶ APPLY ──▶ UNIFY        [v11.1-02 — PHASE COMPLETE; loop idle, ready to plan v11.1-01]
  ✓        ✓        ✓
```

**Tenancy model (locked 2026-06-09 — the spec everything derives from):**
- **Consumers (musicians/members):** NOT per-org-gated; anyone uses either site; the **landing-page host** sets the experience (branding/setlists/library) via `x-org-id` / `<html data-org>`. (err-public.)
- **Band leaders:** explicit `orgIds` membership (CRC / broslaz / both), set via the admin toggle (shipped this phase). The authoring tier.

---

## What Was Done (this session)

1. `/paul:resume` (v11.0 was complete) → Daniel surfaced post-launch issues on brotherslazaroff.live. Triaged 4 against deployed code (Explore agents) + prod Firestore (firebase MCP):
   - **Branding leak** (authed nav still "CRC Music" + /logo.jpg on broslaz) — confirmed root cause; → phase v11.1-01.
   - **Library shared in generic tab** — confirmed unscoped HTTP/SSR reads; → phase v11.1-03 (display-only filter + Shared flag + All-sites toggle).
   - **MCP-authored setlist invisible on broslaz** — ROOT-CAUSED: it landed `orgId:'crc'` (crc-pinned bearer). DELETED the throwaway setlist `bd3b549c` (verified gone) per Daniel.
   - **Liturgical vocab remnants** ("Plan Service/Show", "Upcoming Services") — → phase v11.1-04.
2. Locked the two-tier tenancy model → `/paul:discuss-milestone` → `/paul:milestone` created **v11.1** (4 phases).
3. **Shipped phase v11.1-02 (both plans, full quality floor):**
   - **v11.1-02-01** (`941e6856d1`) host-derived MCP authoring — `resolveMintOrg(uid, requestedOrg)`; the OAuth + self-service mint paths stamp the bearer's org from the connection DOMAIN's `x-org-id` (validated ∈ caller's orgIds, else primary-org fallback). Org pinned at mint by host, NOT a tool arg → **v11-06-02 no-arg-injection invariant preserved** (test unchanged). Verified broslaz MCP routing live. emulator 9/9.
   - **v11.1-02-02** (`d466160601`) admin org-membership toggle — tri-state "Band access" (CRC/BL/Both) control on band_leader/admin rows in `/manage → People` (admin-only) + membership badge; `/api/admin/set-role` now writes `orgIds` to BOTH the Auth claim AND the user doc (claim+doc lockstep). unit 4/4.
   - Phase transition (`648c0a3278`): PROJECT.md evolved, ROADMAP phase→complete (1/4), STATE transitioned.
   - Gates: tsc 0 · full suite **3327/0** · `SKIP_ENV_VALIDATION=1 next build` clean throughout.

---

## What's In Progress

Nothing mid-flight. Phase v11.1-02 is closed + pushed; tree clean.

---

## What's Next

**Immediate:** `/paul:plan` for **v11.1-01 (Org-aware authed branding, P0)** — make `src/components/nav/DesktopHeader.tsx` (`:106-107`) + `MobileHeader.tsx` (`:34-41`) resolve the wordmark + logo from the host org instead of hardcoding `/logo.jpg` + the CRC congregation. The broslaz congregation doc (`config/congregation__brotherslazaroff`) is already correct (name "Brothers Lazaroff", `logoUrl:""`) — decide the per-org logo asset (text wordmark vs a broslaz asset). CRC byte-identical. **/ui-ux-pro-max BLOCKING.**

**After that:** v11.1-03 (library generic-tab visibility, P1) → v11.1-04 (broslaz vocab remnants, P1). v11.1-04 may fold into 03's UI pass.

**Standing UAT-PENDING (non-blocking, Daniel's hands-on):**
- Connect Claude Desktop to **`https://www.brotherslazaroff.live/api/mcp`** (www direct; apex 308-redirects) → OAuth login → author a test setlist → confirm it lands broslaz (not CRC).
- In `/manage → People`, set a leader's "Band access" → confirm the badge updates + persists.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live state — v11.1, phase v11.1-02 complete, next v11.1-01 |
| `.paul/ROADMAP.md` | v11.1 Current Milestone (4 phases, focus, constraints) |
| `.paul/phases/v11.1-02-multiorg-authoring/` | Shipped phase (2 PLANs + 2 SUMMARYs) |
| `src/lib/org/membership-server.ts` | `resolveMintOrg` (host-derived authoring org) |
| `src/components/admin/UserRow.tsx` | Band-access control + membership badge |
| `src/app/api/admin/set-role/route.ts` | orgIds → claim + doc lockstep |

---

## Reusable gotchas (carry forward)

- **Run `SKIP_ENV_VALIDATION=1 npx next build` before declaring any shared-lib/client phase deployable** — tsc + vitest miss client/server bundle-boundary breaks (v11-05 lesson). v11.1-01 touches nav components (client) → applies.
- **/ui-ux-pro-max is BLOCKING on UI phases** (01/03/04). Invoke it during APPLY before the UI task.
- Canonical broslaz MCP URL is the **www.** host (apex 308-redirects, can drop auth). Same pattern as CRC's www.
- v11.x AUTONOMY directive still in force: run autonomously, auto-commit+push per phase, quality floor held; STOP only for product ambiguity / unresolvable gate failure / cross-tenant leak.
- `git pull` first next session (multi-computer); push `origin master` (NOT `master:main`).

---

## Resume Instructions

1. `git pull` (multi-computer).
2. Read `.paul/STATE.md` (phase v11.1-02 complete; next v11.1-01).
3. `/paul:resume` (or directly `/paul:plan` for v11.1-01).

---
*Handoff created: 2026-06-09 (session 6 — v11.1-02 complete + shipped)*
