# PAUL Handoff

**Date:** 2026-06-22
**Status:** paused (clean phase boundary — v11.7-06 COMPLETE 3/3, all pushed; Daniel requested pause + context clear)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (centralreform.live) — multi-tenant (CRC + Brothers Lazaroff on brotherslazaroff.live).
**Core value:** The band gets the right charts + recordings on their iPads each week; Daniel authors setlists conversationally via Claude + MCP (the MCP server is the PRIMARY authoring surface; the browser app is the band/consumer surface).

---

## Current State

**Version:** 11.7.0 (in progress)
**Milestone:** 🚧 v11.7 — Discoverability & Deferred Backlog — **6 of 7 phases complete**
**Phase:** v11.7-06 (Infra adjacents) ✅ COMPLETE (3/3) → next is **v11.7-07 (broslaz design pass, the LAST phase)**

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY     [v11.7-06 COMPLETE — phase boundary]
  ✓        ✓        ✓
```

All work committed + pushed to `origin master`. Working tree clean of this session's changes (one unrelated pre-existing edit: `.paul/research/STRESS-TEST-PROMPT-2026-06.md`, not mine).

---

## What Was Done (this session, 2026-06-22)

Resumed at the v11.7-05-complete boundary and **completed all of phase v11.7-06 (Infra adjacents) — the three cross-tenant org-scoping gaps + the v7.0 fold-forward tail.** Three plans, each PLAN→APPLY→UNIFY, committed + pushed:

1. **v11.7-06-01** `feat(v11.7-06-01)` `b0920118e4` — **recordings org-scoping end-to-end.** `/api/recordings/upload` stamps `orgId = coerceOrgId(x-org-id)` (crc fallback; was hardcoded DEFAULT_ORG_ID). `subscribeRecordingsForSong(songId, orgId, cb)` + `where("orgId","==")`; `RecordingBindPopover` passes `useOrg()`. NEW composite index `recordings(orgId,songId,createdAt)` **deployed** via `firebase deploy --only firestore:indexes`. Query-level scoping (firestore.rules untouched). **No backfill** — prod recordings collection verified empty (0 docs). Qualify caught a memory drift: the real OrgId is **`brotherslazaroff`**, not the `broslaz` project shorthand. upload-route 3 + RecordingBindPopover 4 tests.
2. **v11.7-06-02** `feat(v11.7-06-02)` `500fec79f5` — **finalize org-stamp + anon chord-cache guard.** `finalizeChartUpload(uid, args, org?)` — optional caller org; stamps the NEW chart only (`if(org) stampOrg(...)` on the new-chart return; heal mode returns earlier → an existing chart is never re-tenanted; optional/no-default so the chunked-commit path's external stampOrg isn't doubled). Handler passes `orgFrom(extra)`. chord-cache POST+PATCH gain `assertChordCacheTenant` (module-local, non-exported): resolves `library_index/{fileId}.orgId` via `rowOrg` vs host `coerceOrgId(x-org-id)` → **403 on a definite cross-tenant mismatch**; same-tenant + row-absent write through. upload-session emulator 23/23 + chord-cache 13/13.
3. **v11.7-06-03** `feat(v11.7-06-03)` `511aa3cc45` — **v7.0 fold-forward tail + phase close.** Removed dead `Recording.durationSeconds` (zero readers/writers). Verify-first retired the rest of the v7.0 list: `extractApiError` STALE (0 matches); **no** dead TanStack block (only live `react-table`; no `react-query`); upload caps already exist (25MB); 40→44px targets + ImporterModal = UI → **deferred to v11.7-07**. ROADMAP/PROJECT/paul.json evolved to phase-complete.

Plus two `docs(...)` bookkeeping commits (`15ad03f391`, `2af61f8678`) recording self-referential commit hashes in STATE (master is prod → no force-push). Quality floor held everywhere: tsc 0 / tests green / `next build --webpack` 0.

---

## What's In Progress

Nothing mid-flight. Clean phase boundary. Session paused for context clear.

---

## What's Next

**Immediate:** `/paul:plan` for **Phase v11.7-07 — Authed-broslaz design pass + cross-org leader-wall UI check.**
- This is a **UI phase → `/ui-ux-pro-max` is BLOCKING** (per `.paul/SPECIAL-FLOWS.md`; APPLY won't proceed until it's invoked).
- Scope: design polish of the authed broslaz surface + a UI check that the cross-org leader wall reads correctly (a both-org leader's authoring scope is clear; no CRC leak on BL). CRC byte-identical where shared.
- **Fold in (deferred from 06's verify-first):** the UI v7.0 fold-forward items — 40→44px in-app tap targets, ImporterModal P3 polish.

**After v11.7-07:** `/paul:complete-milestone` → tag `v11.7.0`, bump package.json, archive the ROADMAP.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live state (Session Continuity has the next-action detail) |
| `.paul/ROADMAP.md` | v11.7 phase table + § v11.7-07 detail |
| `.paul/phases/v11.7-06-infra-adjacents/` | The 3 PLANs + SUMMARYs just shipped |
| `src/lib/mcp/org-context.ts` | `coerceOrgId` is in `org/registry`; `rowOrg`/`stampOrg` idioms |

---

## Constraints / Posture (carried)

- **Autonomy directive binding:** run autonomously, auto-commit + push per phase to `master`; chain PLAN→APPLY→UNIFY; STOP only at phase/milestone boundaries, STOP-gates (publish/notify, destructive migration, auth changes), or genuine 2+-option decisions. Firebase deploys/backfills are AUTO tasks.
- **Quality floor (non-negotiable):** tsc + tests + AC proof; `SKIP_ENV_VALIDATION=1 npx next build --webpack` before deployable; emulator tests where rules/queries change (run via `firebase emulators:exec --only firestore,auth "npx vitest run --config vitest.emulator.config.ts <file>"`); **/ui-ux-pro-max BLOCKING on UI phases (07 is one).**
- **err-public cross-tenant HARD WALL:** err-public WITHIN a tenant; hard wall ACROSS. CRC byte-identical where shared.
- **No publish/notify, no assignment-scheduling, no photo-import** (Daniel).
- **Real OrgId is `brotherslazaroff`** (not `broslaz`). David STAYS admin.
- Push `origin master` (NOT `master:main`). Multi-computer — `git pull` first next session.

---

## Resume Instructions

1. `git pull` (multi-computer; master also moves via parallel `.coord` cowork).
2. Read `.paul/STATE.md` (Current Position + Session Continuity).
3. Run `/paul:resume` (or go straight to `/paul:plan` for v11.7-07 — remember `/ui-ux-pro-max` is BLOCKING).

---

*Handoff created: 2026-06-22*
