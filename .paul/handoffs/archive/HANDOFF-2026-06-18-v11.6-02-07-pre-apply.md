# PAUL Handoff

**Date:** 2026-06-18
**Status:** paused — Plan v11.6-02-07 CREATED, awaiting APPLY (clean PLAN boundary)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — multi-tenant worship-band setlist/chart app. CRC + Brothers Lazaroff (brotherslazaroff.live) both live.
**Core value:** the band gets the right charts on their iPads each week; Daniel authors setlists via Claude + MCP (the browser app is the band/consumer surface — Perform mode on 11" iPads).

---

## Current State

**Milestone:** v11.6 — Airtight (Weekend Stress & Usability). Narrow hardening anchored to the 3 Camp Sabra weekend sets.
**Phase:** 2 of 4 — **v11.6-02 Perform reading airtight — IN PROGRESS (6 of 7 plans shipped).**
**Plan:** `v11.6-02-07` (drawer-nav) — **CREATED, NOT yet applied. This is the FINAL plan of phase 02.**

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ○        ○     [Plan 07 created, awaiting approval/apply]
```

Last shipped commit: `5f15209a0b` (Plan 06, image viewer) + `dbad845fec` (its close-loop docs). All pushed to `origin master`.

---

## What Was Done (this session)

Resumed at Plan 05, then ran **three full PLAN→APPLY→UNIFY loops**, each committed + pushed to prod `master`:

1. **v11.6-02-05 Reading controls** `241e6e04e8` — WS-18 (zoom-% always visible on iPad), WS-22 (PROVEN reachable — was an e2e selector false-alarm, fixed `:visible` + live prod sweep), WS-26 (honest "Reset zoom to 100%" label), WS-14 (PDF fit-page mode: pure `computeFitPageWidth` + ephemeral `fitMode` store slice + PDF-only toolbar toggle).
2. **v11.6-02-06 Image viewer** `5f15209a0b` — WS-06 (`ImageScoreViewer` honors store zoom via CSS `zoom`, pannable), WS-15 (`role="alert"` + ≥44px Retry remounting `<img>`).
3. **v11.6-02-07 PLANNED** (this is where we paused — plan written, not applied).

All quality gates green each loop (tsc 0 · `next build --webpack` 0 · tests). Real-iPad UAT items appended to `.paul/UAT-PENDING.md`.

---

## What's Next

**Immediate:** `/paul:apply .paul/phases/v11.6-02-perform-reading-airtight/v11.6-02-07-PLAN.md`

Plan 07 = 2 tasks (load **/ui-ux-pro-max FIRST** — BLOCKING):
- **T1 WS-08 (P1):** in `SetlistDrawer.tsx`, the in-chart QueueRow `onOpen` does `router.push('/perform/<fileId>')` → single-chart dead-end (Next/Prev die). Change it to `jumpToSong(globalIndex)` (add `jumpToSong` to the `useMusicStore()` destructure) + `setOpen(false)`. **Confirmed:** the drawer builds rows from the store `playbackQueue`, so `globalIndex` IS the playbackQueue index (the existing `isCurrent = globalIndex === queueIndex` proves it). Add `setlist-drawer.test.tsx` mocking `@tanstack/react-virtual`'s `useVirtualizer` (render all rows) + store + next/navigation; assert click → `jumpToSong(idx)`, NOT `router.push`; no-fileId row → no-op.
- **T2 WS-28:** delete the unmounted `src/components/performance/SwipeOverlay.tsx` (re-confirm zero render sites first — only globals.css `swipe-anim` keyframe + the file itself reference it) + remove the orphaned `@keyframes swipe-anim` from `src/app/globals.css`.

**DECISION baked in (don't re-litigate):** WS-17 (dead `currentTrackIndex`/`setCurrentPosition` "current position" highlight) is **DEFERRED → the active v7.1 cycle-13a leader-broadcast workstream**, which owns that plumbing and whose leader-broadcast feature IS the real fix. PAUL must NOT touch `use-setlist-performance.ts` currentTrackIndex/setCurrentPosition (collision). *If Daniel says cycle-13a is NOT touching it, fold WS-17 back into Plan 07.*

**After APPLY+UNIFY:** Plan 07 is the LAST plan of phase 02 → **UNIFY triggers the phase-02 TRANSITION** (evolve PROJECT.md/ROADMAP.md, phase-close commit `feat(v11.6-02): …`, clean handoffs, route to phase v11.6-03).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live state — Session Continuity has the exact next command |
| `.paul/phases/v11.6-02-perform-reading-airtight/v11.6-02-07-PLAN.md` | The plan to apply |
| `.paul/research/v11-6-01-stress-triage-REPORT-2026-06-17.md` | 31-finding triage (WS-08/28 cites) |
| `src/components/performance/SetlistDrawer.tsx` | WS-08 (QueueRow onOpen ~line 402-406) |
| `src/components/performance/SwipeOverlay.tsx` | WS-28 (delete) |
| `src/lib/store.ts` | `jumpToSong` (already exists + tested) |

---

## Working Notes / Posture

- **Autonomy:** auto-commit + push per phase to prod `master`; bake decisions into PLANs; Daniel paces with "go" per PAUL step. STOP only at phase boundaries / STOP-gates (phase 04 live send) / real 2+-option decisions.
- **Quality floor:** tsc 0 + tests green + AC proof; `SKIP_ENV_VALIDATION=1 npx next build --webpack` before declaring deployable; /ui-ux-pro-max BLOCKING on UI phases.
- **Git:** master (prod). `git pull` first next session (multi-computer); push `origin master` (NOT `master:main`). Per-plan commits, then a small `docs(...)` close-loop commit (STATE + paul.json) after each UNIFY.
- **Carry:** the live stress sweep had INTERMITTENT headless-WebKit `open-chart` timeouts on text sets — NOT a regression (headless+Firestore-streaming fidelity); confirm real-device on camp wifi in phase 03. David STAYS admin. F4 BL key badges still await authoring.
- **Bash cwd quirk this session:** the shell cwd intermittently reset to repo root — use absolute paths (`/c/Users/dsbog/centralreform.live/sheet-music-app/...`) in Bash. The Write tool refused a brand-new test file once ("not read yet") — fall back to a Bash heredoc with an absolute path.

---

## Resume Instructions

1. `git pull` (multi-computer).
2. Read `.paul/STATE.md` (Session Continuity = exact next command).
3. Run `/paul:resume` (or `/paul:apply` directly on the Plan 07 path) — load **/ui-ux-pro-max** before applying.

---
*Handoff created: 2026-06-18*
