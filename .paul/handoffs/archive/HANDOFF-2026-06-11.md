# PAUL Handoff

**Date:** 2026-06-11 (session 11)
**Status:** paused (clean — everything committed + pushed to `master`)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** CRC Music (sheet-music-app) — multi-tenant (CRC + Brothers Lazaroff). Next.js + Firebase (`crcmusiccharts`); git repo at the `sheet-music-app/` subdirectory; `.paul/` is inside it.
**Core value:** The band gets the right charts + recordings on their iPads each week; Daniel authors setlists conversationally via Claude + MCP (the browser app is the band/consumer surface only).

---

## Current State

**Milestone:** 🚧 v11.2 MCP Stress-Test Fixes (OPEN 2026-06-09) — fixing 9 findings from the Brothers Lazaroff MCP/Perform stress-test report. **3 of 5 phases complete; phase 04 is 1 of its 2 plans done.**
**Phase:** v11.2-04 publish + test-data hygiene [P2] — 🚧 in progress.
**Plan:** none active (loop idle). Next is **v11.2-04-02 (BUG-5)**.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [04-01 loop COMPLETE; phase 04 open for 04-02]
```

**Autonomy posture (binding):** run autonomously — waive approval gates, auto-commit + push per plan to prod `master`, bake decisions into PLANs. QUALITY FLOOR HELD: tsc clean + tests green + AC proof + `SKIP_ENV_VALIDATION=1 npx next build` before declaring deployable + emulator-backed tests where rules/queries change. STOP only for product ambiguity / unresolvable gate failure / cross-tenant leak. Daniel paces with "go" per PAUL step.

---

## What Was Done (this session, all shipped to `master`)

- **v11.2-03-02 (BUG-3, P2)** `ebb520164d` — `bulk_add_tracks` per-row `BulkAddResult.error` was a bare string while single-row tools return `{code, machine_code, message}`. Converted to the shared `RichErrorBody` at 4 sites (3 pre-validation + 1 best-effort `addTrack` catch surfaced by tsc). ERROR_CODE_MAP += `title_required:400` (also corrects single add_track) + `batch_rolled_back:409`. Per-row codes: song_not_found(404)/title_required(400)/batch_rolled_back(409)/server_error(500). **Phase v11.2-03 closed** (both BUG-2 + BUG-3 shipped). Emulator mcp-setlist-write 78/78.
- **v11.2-04-01 (BUG-4, P2)** `90774a7e76` — `preview_publish` was blind to `type:"song"` rows with no chart bond (fileId AND songId both null) — they render blank in Perform but the gate said "publish". Now counts them via `isSongType` + `getTracksForSetlist`, surfaces `unbondedSongCount` + `unbondedSongs[]`, escalates recommendation to `review_first` (hard_block keeps precedence). Emulator mcp-w01-preview-and-bonds 14/14.

---

## What's In Progress

Nothing mid-edit. Working tree clean, HEAD = `e00038dced` (the 04-01 sha-record doc commit), in sync with `origin/master`.

---

## What's Next

**Immediate:** `/paul:plan` for **v11.2-04-02 (BUG-5)** — test-data hygiene. Two parts:
1. `cleanup_all_test_data` only cascades `test-*`-owned data (via `mcpTestUsers` + Auth `test-*`). Extend it to ALSO sweep `setlists`/`library_index` where `isTest==true` independent of owner (same admin/band_leader gate) OR document the manual-delete requirement.
2. Decide whether the authed `(main)` dashboard should reuse the `/perform` `isTest!=true` filter (isTest setlists under a real admin uid currently show on the dashboard, correctly hidden from `/perform`).
**Self-inclusion regression-test rule applies** ([[feedback_self_inclusion_test_fixtures]] — a tool whose caller can be in the operand set needs a test modeling that path; `cleanup_all_test_data` is exactly such a tool).

**After that:** v11.2-04 phase transition (it's the phase's last plan → ROADMAP mark ✅ + transition commit), then **v11.2-05** (BUG-6 "CRC MUSIC" authed-header brand leak / BUG-7 text/plain chord-over-lyric renderer / BUG-8 ISO timestamp serialization). Then milestone close.

**Open action on Daniel (live UAT, all safe / emulator-proven):** reconnect Claude Desktop BL connector to `https://www.brotherslazaroff.live/api/mcp` (current bearer is crc-pinned), then run UAT-PENDING: (1) BUG-1 create→propose→commit on BL; (2) BUG-9 `preview_publish` on a BL setlist shows BL roster size not 17 (dryRun, safe). See `.paul/UAT-PENDING.md`.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (read first) |
| `.paul/ROADMAP.md` § Active Milestone | v11.2 phase specs (v11.2-04 BUG-4/BUG-5 detail) + locked constraints |
| `.paul/phases/v11.2-04-publish-testdata-hygiene/v11.2-04-01-SUMMARY.md` | just-closed loop (BUG-4) |
| `src/lib/mcp/tools/cleanup-test-data.ts` (or similar) | the BUG-5 `cleanup_all_test_data` target — locate via grep |
| `.paul/UAT-PENDING.md` | live retest checklist for Daniel |

---

## Resume Instructions

1. `git pull` first (multi-computer box).
2. Read `.paul/STATE.md` for latest position.
3. Run `/paul:resume` (or `/paul:plan` directly for v11.2-04-02).
4. Push `origin master` (NOT `master:main`).

---
*Handoff created: 2026-06-11 (session 11)*
