# PAUL Handoff

**Date:** 2026-06-13 03:08 UTC
**Status:** paused (Plan v11.5-02-01 created + ready for APPLY — autonomous)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — multi-tenant worship/band sheet-music app. Browser = band/consumer surface (Perform mode on iPads); Daniel authors via Claude + MCP. 2 live tenants: CRC (centralreform.live) + Brothers Lazaroff (brotherslazaroff.live).
**Core value:** The band gets the right charts + recordings on their iPads each week; the web app's job is **performance + quick edits** (doctrine: bulletproof > novel).

---

## Current State

**Milestone:** v11.5 — Bulletproof Performance (🚧 5 phases; oracle `docs/ACCESS-POLICY.md` v0.4).
**Phase v11.5-01 ✅ COMPLETE 3/3** (shipped): H4 Perform-nav branding leak (`180c9b666e`) · H5 anon chord-cache PATCH (`cd97ab21a3`) · H9 band_leader library-edit + cross-tenant wall (`d7cbb1a4e0`). Phase-close `b3821bc26c`.
**Now in Phase v11.5-02 — The performance surface (headline, P1).**

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ○        ○     (Plan v11.5-02-01 created, ready for APPLY)
```

---

## What Was Done (this session)

- Shipped + closed **all of phase v11.5-01** (H4, then H9 [STOP-gate, Daniel approved], then H5 [STOP-gate, approved]); ran the phase transition (PROJECT/ROADMAP/STATE/paul.json evolved, phase commit, pushed).
- **Planned phase v11.5-02** as a 4-plan split (complex, UI-heavy). Recon (Explore agent) grounded each concern. **Daniel picked H3 as Plan 01.**
- **Wrote Plan v11.5-02-01 (H3 seekable audio)** — uncommitted until this pause's commit.

---

## What's In Progress

- **Plan v11.5-02-01 (H3) — created, NOT yet applied.** Autonomous (pure serving-layer enhancement; not a STOP-gate). Ready for `/paul:apply`.

---

## What's Next

**Immediate:** `/paul:apply .paul/phases/v11.5-02-performance-surface/v11.5-02-01-PLAN.md` (H3 — seekable audio).
- The fix: a pure `byteRangeResponse` helper (`src/lib/http/byte-range.ts`) + wire BOTH audio routes — `/api/drive/file/[fileId]` (what `AudioViewer.tsx:60` actually plays) and `/api/recordings/file/[id]` — to return `206`/`Accept-Ranges`/`Content-Range`/`416`. No-Range GETs byte-identical; all auth/404/502 gates untouched; native `<audio>` (no /ui-ux-pro-max). 3 tasks (helper+unit · wire routes · route regression test).
- Gates: tsc · vitest (helper + recordings route) · `next build --webpack`. Autonomous → applies+unifies straight through unless Daniel wants per-step pauses.

**The rest of phase v11.5-02** (plan one at a time, in order):
- **02 = H7/F1** — `/perform` cold-open TTFB **verify-first** (re-run `node scripts/v11-3-04-webvitals-slice.mjs`; likely confirms infra-bound → Vercel ticket, NOT app churn — see Deferred Issue) + build the F1 "jump to tonight's set" entry (no such routing today; list only).
- **03 = F2** — in-Perform leader-only shared key change. `tracks/{id}.key` already broadcasts via `changeTrackKey` (`live-director.ts`); F2 = a leader-gated in-Perform control (+ optional chart swap) without leaving Perform. /ui-ux-pro-max + UAT. (Key is label-only — chords/graphics don't transpose, per prior decision.)
- **04 = H1** — landscape auto-fit + **persisted per-chart calibration** (zoom/crop override). Biggest: PDFViewer uses session-global `zoom` (store.ts), no per-chart persistence today. /ui-ux-pro-max + heaviest 7-tablet UAT.

**Operational (Daniel's discretion):** revert David admin → band_leader (temp-bumped for the June-13 gig; H9 is live so the proper band_leader path works). He said "David is fine" — not urgent.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (current) |
| `.paul/ROADMAP.md` | § Phase v11.5-02 |
| `.paul/phases/v11.5-02-performance-surface/v11.5-02-01-PLAN.md` | Plan 01 (H3) to apply |
| `src/app/api/drive/file/[fileId]/route.ts` · `src/app/api/recordings/file/[id]/route.ts` | The two audio routes (H3 target) |
| `src/components/music/AudioViewer.tsx` | Player — plays `/api/drive/file/<id>` |
| `.paul/research/MILESTONE-BRIEF-v11.5-bulletproof-performance.md` | Ratified v11.5 spec |

---

## Resume Instructions

1. Read `.paul/STATE.md` (Session Continuity) for latest position.
2. `git pull` first (multi-computer); push `origin master` (NOT master:main).
3. `/paul:resume` (or go straight to `/paul:apply` Plan v11.5-02-01).
4. Autonomy posture binding: quality floor held (tsc · tests · `next build --webpack` before deployable); auto-commit+push per phase to prod master. STOP-gates only for prod-side-effect/auth/destructive — H3 is none, so it's autonomous. Daniel paces with "go" per PAUL step.

---

*Handoff created: 2026-06-13 03:08 UTC*
