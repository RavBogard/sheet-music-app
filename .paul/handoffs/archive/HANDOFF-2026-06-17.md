# PAUL Handoff

**Date:** 2026-06-17
**Status:** paused — clean phase boundary (v11.6-01 complete, committed + pushed)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — multi-tenant worship-band setlist/chart app. CRC + Brothers Lazaroff (brotherslazaroff.live) both live.
**Core value:** the band gets the right charts on their iPads each week; Daniel authors setlists via Claude + MCP (the browser app is the band/consumer surface only — Perform mode on 11" iPads).

---

## Current State

**Version:** v11.6 Airtight (Weekend Stress & Usability) — `package.json` will bump to 11.6.0 at milestone close.
**Phase:** 1 of 4 — **v11.6-01 Stress sweep & triage ✅ COMPLETE**.
**Plan:** `v11.6-01-01` (research) closed. No plan active.
**Last commit:** `c4f6aba72d` (`test(v11.6-01): live Perform stress sweep + triaged findings report; open v11.6`) — pushed to `origin master`.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [phase 01 loop closed]
```

---

## What This Milestone Is

A deliberately **narrow** hardening milestone: stress-test the band's entire path against the **three real upcoming Camp Sabra weekend sets** and fix whatever isn't airtight. NOT a feature milestone. The three sets are the canonical fixtures (realism anchor — NOT a deadline/service-gate triage axis; paper is the implicit fallback). Broad backlog (photo-import, library density, comms layer) deferred to v11.7.

- **Triage bar:** usability-AND-access (a real usability defect counts even if it doesn't contradict an access-policy cell). `err-public` still governs access-shaped findings. Oracle `docs/ACCESS-POLICY.md` v0.4.
- **Anchor fixtures (all chart-healthy, none published):** Shir Shabbat—Juneteenth `a84f8cce-176e-4b5e-9653-4df71db6f5ba` (Fri, all PDF) · Camp Sabra Havdalah `7e005452-7c42-4cdc-b27d-ff0c78b6667b` (Sat, mostly text/plain) · Camp Sabra Staff Concert `7c640a8a-358e-48ee-8523-6b8a0eca9d05` (Sat, all text/plain).
- **4 phases:** 01 ✅ stress sweep & triage (discovery) → 02 Perform reading airtight → 03 off-site flaky-wifi resilience → 04 authoring + publish round-trip (live send = STOP-gate).

---

## What Was Done (this session)

1. `/paul:discuss-milestone` → `/paul:milestone`: opened **v11.6**, scoped 4 phases, wrote ROADMAP/STATE/PROJECT/paul.json, created phase dirs, pushed the broad backlog to v11.7.
2. `/paul:plan` → `/paul:apply` → `/paul:unify` on **v11.6-01** (DISCOVERY, no prod code):
   - Built `e2e/v11-6-perform-stress.spec.ts` — a re-runnable live Perform sweep (prod, iPad-WebKit 820×1180 portrait + landscape, the 3 sets, a wifi-drop cell). Run with `PLAYWRIGHT_BASE_URL=https://www.centralreform.live`.
   - Ran a 5-surface read-only multi-agent code audit (text-render / PDF-image-zoom / perform-nav / offline / publish).
   - Produced **`.paul/research/v11-6-01-stress-triage-REPORT-2026-06-17.md`** — 31 findings (WS-01..31), P0–P3, each tagged to fix-phase 02/03/04, with a coverage section.
   - Closed the loop: SUMMARY written, transition done, committed `c4f6aba72d`, pushed.

---

## Findings That Drive 02/03/04 (from the report)

- **WS-01 (P0, suspected — confirm live):** opening a bonded **non-song** row (prayer/reading with a chart) bounces the musician to song 1 (`PDFOverlay` queue filter excludes `type!=='song'`). Hits the Friday Juneteenth set's bonded prayer rows (Shema, Mi Chamocha, Adonai sfatai). → **02**.
- **WS-02 (P1, live + config-confirmed):** CSP `img-src` (`src/proxy.ts:47`) omits `https://www.google.com` → Firestore long-poll beacon `cleardot.gif` blocked → setlist rows can fail to hydrate on iPad Safari over long-poll/flaky wifi. **One-line fix.** → **02** (quick win).
- **WS-03 / WS-04 (P1):** text Fit-mode font shrinks below legibility / clips long chorded lines with no scroll (TextScoreViewer.tsx:255-258,289); transposed chords drift off syllables (171-176,299-308). The camp text sets' dominant path. → **02**.
- **WS-05 (P1):** PDF render-stage stuck-spinner (no watchdog/Retry once bytes fetched). → **02**.
- **WS-11 (P1):** in-app "published" bell notification deep-links to dead `/setlist/{id}` (push/SMS are correct). → **04**.
- **Offline = CONFIRMED WORKING LIVE** (open chart stays visible + offline next-nav on PDF and text; "Saved offline" indicator live). ⇒ **Phase 03 is reframed from "re-introduce offline" to "verify + close 2 gaps": WS-10 (MusicXML still uses iPad-flaky `fetch(blob:)`) + WS-12 (incognito/memory-cache can blank the set).** The `checkpoint:decision` in 03 shrinks accordingly.

Full P0–P3 table + phase routing + coverage in the report.

---

## What's Next

**Immediate:** `/paul:plan` for **phase v11.6-02 (Perform reading airtight)**, driven by the report's WS-NN punch list. Suggested lead-ins: **WS-02** (CSP one-line quick win) → **WS-01** (P0 — first confirm live by opening a bonded prayer row on the Juneteenth set, then fix the `PDFOverlay` queue filter) → **WS-03/04** (text Fit-mode legibility floor + transposed-chord alignment). **/ui-ux-pro-max is BLOCKING on 02** (it touches UI).

**After that:** 03 (off-site resilience — verify + close WS-10/WS-12) → 04 (authoring + publish round-trip; live send STOP-gated).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (read first) |
| `.paul/research/v11-6-01-stress-triage-REPORT-2026-06-17.md` | The 31-finding triaged punch list driving 02/03/04 |
| `.paul/phases/v11.6-01-stress-sweep-triage/v11.6-01-01-SUMMARY.md` | Phase 01 outcome |
| `e2e/v11-6-perform-stress.spec.ts` | Re-runnable live Perform sweep (regression harness for 02) |
| `.paul/ROADMAP.md` | v11.6 phase detail + constraints |

---

## Working Notes / Posture

- **Autonomy:** run autonomously — auto-commit + push per phase to prod `master`; bake decisions into PLANs; deploys/backfills are AUTO tasks. Daniel paces with "go" per PAUL step. STOP only at phase boundaries, STOP-gates (phase 04 live publish/notify send), or a real 2+-option `checkpoint:decision`.
- **Quality floor:** tsc clean + tests green + AC proof; `SKIP_ENV_VALIDATION=1 npx next build --webpack` before declaring any route/shared-lib/client phase deployable; /ui-ux-pro-max BLOCKING on UI phases.
- **Method (browser probes):** live Playwright from THIS Windows box — NOT cowork (cowork can't launch WebKit). Real-device iPad confirmations → `.paul/UAT-PENDING.md`.
- **Harness caveat:** headless WebKit + Firestore long-poll is flaky (this is WS-02 itself); landscape gave full interaction data, portrait runs partially didn't hydrate. Confirm WS-01/WS-02/transpose/legibility on a real iPad.
- **Git:** master (prod). `git pull` first next session (multi-computer); push `origin master` (NOT `master:main`). MCP endpoint: `https://www.centralreform.live/api/mcp` (www direct).

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position.
2. Read `.paul/research/v11-6-01-stress-triage-REPORT-2026-06-17.md` (the punch list).
3. `git pull` (multi-computer).
4. Run `/paul:plan` for phase v11.6-02 (or `/paul:resume` / `/paul:progress`).

---
*Handoff created: 2026-06-17*
