# PAUL Handoff

**Date:** 2026-06-14 (late / 2026-06-15 UTC)
**Status:** PAUSED at a clean phase boundary — v11.5-02 COMPLETE, ready to PLAN v11.5-03.

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — multi-tenant worship/band sheet-music app. Browser = band/consumer surface (Perform mode on iPads); Daniel authors via Claude + MCP. 2 live tenants: CRC + Brothers Lazaroff (brotherslazaroff.live).
**Core value:** the band gets the right charts + recordings on their iPads each week; the web app's job is performance + quick edits.

---

## Current State

**Milestone:** 🚧 v11.5 — Bulletproof Performance (2 of 5 phases complete). Oracle `docs/ACCESS-POLICY.md` v0.4.
**Phase:** v11.5-02 (performance surface) ✅ COMPLETE 4/4 (2026-06-14). Next = v11.5-03 (photo-of-paper-chart import).
**Plan:** none open.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY     [phase v11.5-02 closed; loop idle]
  ✓        ✓        ✓
```

Everything committed + pushed to `origin master`. Working tree clean except unrelated untracked research artifacts.

---

## What Was Done (this session)

- **Resumed** from HANDOFF-2026-06-14 (archived) and completed the LAST plan of phase v11.5-02.
- **v11.5-02-04 (H1) — PLAN → APPLY → UNIFY → phase transition**, all in one session:
  - Per-device per-chart zoom: `chartZoom: Record<fileId, number>` in `useMusicStore` (`src/lib/store.ts`), written through by `setZoom`, restored per chart on every queue transition (mirrors `transposition`), persisted to localStorage (global `zoom` dropped from `partialize`).
  - Tappable **Fit** reset: the toolbar zoom-% readout (`PerformanceToolbar.tsx`) is now an accessible ≥44px button → `setZoom(1)` (auto-fit baseline).
  - Tests: `src/lib/__tests__/chart-zoom-calibration.test.ts` (5) + 1 new toolbar test. **22/22 green**, tsc clean (changed files), `next build --webpack` exit 0, `/ui-ux-pro-max` invoked before the toolbar edit.
- **Phase close:** PROJECT.md / ROADMAP.md / STATE.md / paul.json evolved; commits `fac9db7fd4` (`feat(v11.5-02)`) + `2af81c31d8` (STATE align), pushed `2c5360077d..2af81c31d8`.

## Key Decision (this session)

- **H1 per-chart zoom = PER-DEVICE (Daniel-ratified 2026-06-14):** localStorage, NOT shared via Firestore. Zoom is a per-eyesight/screen-size viewing preference that must not override another musician's view — deliberately diverges from the recon's "mirror transposition / shared applyEdit" Option B. NO write seam / rules / follower-propagation. Also baked: NO viewer changes (verified PDFViewer + OSMD already fit-to-width + reflow on rotate). Crop + image-chart calibration + standalone `/perform/[fileId]` route deferred.

## What's Next

**Immediate:** `/paul:plan` Phase **v11.5-03 — Photo-of-paper-chart import** (P1, funded L). MCP path: photo → deskew/crop/normalize → org-stamped library row (provenance "photo import") → bonded. **Image normalization first, NOT OCR.** **VERIFY-FIRST** what `scrape_chart_from_url` / `salvage_chart_bytes` infra is reusable. /ui-ux-pro-max BLOCKING; iPad UAT before close.

**Operational (Daniel's discretion):** revert David admin→band_leader (H9 stopgap; H9 shipped 2026-06-12 so the lockout is gone).

**Flagged, not actioned:** `/perform` cold·mobile CLS 0.250 (n=8) + Vercel TTFB infra follow-up (both INFRA, not app code).

**UAT-PENDING (non-blocking, live):** H1 7-tablet iPad fleet block just appended (per-chart persist across nav/reload, rotate preservation, two-device per-device proof, PDF+MusicXML honor / image unaffected, Fit reset). Plus earlier v11.5-01/02/03/04 items in `.paul/UAT-PENDING.md`.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview (§ Phase v11.5-03 = next) |
| `.paul/phases/v11.5-02-performance-surface/v11.5-02-04-SUMMARY.md` | H1 outcome |
| `src/lib/store.ts` | `chartZoom` per-device map (H1) |
| `src/components/performance/PerformanceToolbar.tsx` | Fit reset control (H1) |

## Resume Instructions

1. `git pull` first (multi-computer). master is prod; push `origin master` (NOT master:main).
2. `/paul:resume` → it will route to `/paul:plan` for Phase v11.5-03.

---
*Handoff created: 2026-06-14 (pause at v11.5-02 → v11.5-03 phase boundary)*
