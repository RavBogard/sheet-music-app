# PAUL Handoff

**Date:** 2026-06-16
**Status:** PAUSED at a clean phase boundary — Phase v11.5-04 COMPLETE 4/4 + transitioned to v11.5-05. Working tree clean; all committed + pushed.

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — multi-tenant worship/band sheet-music app. Browser = band/consumer surface (Perform mode on iPads); Daniel authors via Claude + MCP. 2 live tenants: CRC + Brothers Lazaroff (brotherslazaroff.live).
**Core value:** the band gets the right charts + recordings on their iPads each week; the web app's job is performance + quick edits.

---

## Current State

**Milestone:** 🚧 v11.5 — Bulletproof Performance. Oracle `docs/ACCESS-POLICY.md` v0.4. **3 of 4 active phases complete** (v11.5-03 photo-import DROPPED → v11.6).
**Phase:** v11.5-04 (Hygiene & harness) — **✅ COMPLETE 4/4.** Next phase v11.5-05 (Consumer polish) — **NOT STARTED.**
**Plan:** none open.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY     [Phase v11.5-04 closed + transitioned — loop idle, ready to PLAN v11.5-05]
  ✓        ✓        ✓
```

HEAD `02ecf0ce52`, pushed to `origin master`. Working tree clean (except unrelated untracked research artifacts from other workstreams).

---

## What Was Done (this session, 2026-06-16)

Resumed at v11.5-04 2/4, then closed the phase end-to-end:

- **Plan 03 — harness code** (`7bffbca1a3`): H8 `releaseMonitorBusAssignments` in the test-account revoke/cleanup cascade (walks `config/monitor.busAssignments`); F-8 `orgIds` option on `create_test_account` (doc+claims+token; crc-default byte-equivalent; unknown-org refused); M-12 60-min `CHUNKED_SESSION_TTL_MS` for `beginChunkedChartUpload` (signed-URL stays 10m) + begin/append/commit descriptions name `import_chart_from_drive` as primary path; **chord-cache tsc fix** (`mockReturnValueOnce`→`mockResolvedValueOnce`) → **`tsc --noEmit` clean on master**. 7 new emulator cases.
- **Plan 04 — ops+docs** (STOP-gate, part of phase-close `2c0abcc3b4`): verified `delete_chart`'s author guard (already correct + 66 emulator tests, no code); **VERIFY-FIRST prod junk-row deletion** — probe found only `.DS_Store` (`12if9gHg88ZqNMZjmLG1UH57GSriTHzqF`) remained (the two `[role-*]` rows already gone; orphan sweep 0/734), bond-probed 0 tracks, Daniel-approved, deleted, re-probe clean; recorded `.docx` import fixture (Drive `1-RxHu0bDzG6dkuHPbj8L5BBBzXX8OowO`); wrote `docs/BL-MCP-BEARER.md`.
- **Phase transition:** PROJECT.md + ROADMAP.md evolved, STATE → v11.5-05, paul.json synced (phase 5, not_started), phase-close commit `2c0abcc3b4` + 2 align commits, state-consistency verified, all pushed.
- **Decision recorded:** David Lazaroff STAYS admin permanently (Daniel) — do NOT suggest reverting to band_leader.

## What's In Progress

Nothing — clean phase boundary.

## What's Next

**Immediate:** `/paul:plan` **Phase v11.5-05 — Consumer polish quick wins** (the LAST v11.5 phase):
- **Q3** — branded, QR-aware error copy on both hosts ("code expired or was used — ask for a fresh QR").
- **Q4** — anon `/setlists`: hide write controls (invariant-6) + suppress junk drafts from the anon archive.
- **Q5** — chart titles never show raw filenames (".pdf"/".docx") on consumer surfaces.
- **Q6** — "Public sets" vocab.
- **F4** — key badges on broslaz setlist rows.
- **/ui-ux-pro-max is BLOCKING** (UI phase).

**After that:** v11.5-05's close = the **v11.5 milestone close** → `/paul:complete-milestone`.

## Flagged / Backlog (not blocking)

- **INFRA (not app code):** `/perform` cold·mobile CLS 0.250; Vercel TTFB keep-warm (fluid-compute/region).
- **v11.6 candidates:** library folder/audio non-chart clutter cleanup (observed in Plan 04 probe; consumer-hidden); photo-of-paper-chart import (dropped from v11.5); M-12 fold-opportunistic adjacents (recordings org-scoping + `finalize_chart_upload` signed-URL org-stamp) — NOT pulled into v11.5-04.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (Phase v11.5-05 NEXT) |
| `.paul/ROADMAP.md` | § Phase v11.5-05 = next-phase scope |
| `.paul/phases/v11.5-04-hygiene-harness/*-SUMMARY.md` | Closed-phase detail (4 plans) |
| `docs/BL-MCP-BEARER.md` | Brothers Lazaroff MCP bearer setup (Plan 04) |

## Resume Instructions

1. `git pull` first (multi-computer). master is prod; push `origin master` (NOT master:main).
2. `/paul:resume` → it will route to `/paul:plan` for Phase v11.5-05.
3. Autonomy posture binding: auto-commit+push per plan to master; quality floor held; /ui-ux-pro-max BLOCKING on this UI phase; Daniel paces with "go"/"1" per PAUL step.

---
*Handoff created: 2026-06-16 (pause at phase boundary, v11.5-04 complete)*
