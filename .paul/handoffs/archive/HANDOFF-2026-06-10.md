# PAUL Handoff

**Date:** 2026-06-10
**Status:** paused (clean — phase boundary, pushed to prod)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — multi-tenant worship-band app (CRC + Brothers Lazaroff). Daniel authors setlists via Claude + MCP; the browser app is the band/consumer surface (Perform mode on iPads).
**Core value:** The band gets the right charts + recordings on their iPads each week; Daniel authors conversationally via Claude + MCP.

---

## Current State

**Milestone:** 🚧 v11.3 — Worthiness & Access (OPEN 2026-06-10; 5 phases). Oracle: `docs/ACCESS-POLICY.md` v0.3 (a finding is a bug only if it contradicts a cell).
**Phase:** 1 of 5 COMPLETE → **next is Phase v11.3-02 (Agent chart-upload path, P1)**.
**Plan:** none active.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY     [v11.3-01 ✅ complete + committed + pushed — ready to PLAN v11.3-02]
  ○        ○        ○
```

**Git:** `master`, in sync with `origin/master`, tip **`bc8f935aa2`** (v11.3-01 phase commit, Vercel auto-deploying). Push `origin master` (NOT `master:main`). Multi-computer → `git pull` before next session.

---

## What Was Done (this session)

- **`/paul:discuss-milestone` → `/paul:milestone`:** created **v11.3 Worthiness & Access** (5 phases) verbatim from the ratified brief `.paul/research/MILESTONE-BRIEF-2026-06-10-worthiness-access.md`. Phase dirs + ROADMAP/STATE/MILESTONES/PROJECT/paul.json all set up.
- **Phase v11.3-01 Anon access correctness — COMPLETE (2 plans, PLAN→APPLY→UNIFY→transition):**
  - **BUG-5 (Plan 01):** `/api/library/file/[id]` was a default-auth, `db-*`-only route → anon 401 `missing_bearer` for every id. Reworked into a **public chart proxy mirroring `/api/drive/file/[fileId]`** (`requireAuth:false` + `isTrusted` Sec-Fetch/bearer gate + `chart` rate-limit tier + `fetchFileById` for `upload-*`/Storage; `db-*` digitized_charts now anon-public). **Verify-first re-graded BUG-5 → P2** (Perform renders via `/api/drive/file`, anon-OK; the "blank cold-device render" P1 escalation was unfounded). No client edit (`parseFileId` already correct). 6/6 route tests.
  - **BUG-4 (Plan 02, D-Q2):** anon transpose dead-ended on "Waiting for scan…". Opened `/api/library/chord-cache` GET+POST and `/api/ai/transposer/scan` POST to anon (`requireAuth:false`); scan got an **anon-only** `ai`-tier rate-limit (authed unchanged — it had no limiter before → zero regression); anon scan results persist (cost control). PATCH/DELETE stay role-gated. No client edit (`apiFetch` sends anon; dead-end clears on 200). 7/7 route tests.
  - Committed `feat(v11.3-01): anon access correctness (BUG-5 + BUG-4)` (`bc8f935aa2`) + pushed. Gates green: tsc · 13 route tests · `SKIP_ENV_VALIDATION=1 next build`. CRC byte-identical.

---

## What's In Progress

- Nothing mid-flight. Clean phase boundary. v11.3-01 shipped to prod.

---

## What's Next

**Immediate:** `/paul:plan` for **Phase v11.3-02 — Agent chart-upload path** (P1, David's report `.paul/research/BUG-cowork-chart-upload-2026-06-10.md`):
- Primary: `import_chart_from_drive` accepts `.docx` + Google Docs, converts to PDF **server-side** (Drive API export / convert-on-copy). Agent passes references, never bytes.
- Secondary: chunked inline `upload_chart` (init/append/commit) for non-Drive sources.
- Out of scope: the Cowork sandbox proxy (Anthropic-side; reported separately).

**After that:** Phases v11.3-03 (harness & hygiene — BUG-9/7/1), v11.3-04 (/perform perf — BUG-2), v11.3-05 (P3 polish — BUG-6/F-6). D8 publish/notify is v11.4 (NOT this milestone).

---

## Binding Context (do not re-litigate)

- **Oracle:** `docs/ACCESS-POLICY.md` v0.3 is the bug arbiter; err-public prime directive (err toward letting someone see a chart; writes/admin gated).
- **Scope walls:** D8 → v11.4; BUG-3 (RUM)/BUG-8 (member library)/Policy-Q1 closed by policy (no code); F-4 dup setlist + Cowork proxy out; D2 anon **recordings** stays the ⚠️ veto cell (charts only — don't widen into recordings).
- **Autonomy posture (binding):** run autonomously, auto-commit + push per phase to `master`, bake decisions into PLANs. Quality floor: tsc + tests + AC proof per task; `SKIP_ENV_VALIDATION=1 npx next build` before declaring deployable; emulator tests where rules change; /ui-ux-pro-max BLOCKING on UI-touching phases. STOP only for ambiguity / unresolvable gate failure / cross-tenant leak.
- Every fixed BUG gets a regression test citing the stress-report coverage cell.
- Daniel paces with "go" per PAUL step.
- **Tooling gotcha (this session):** the Bash tool is bash, NOT PowerShell — don't use `@'…'@` here-strings (leaks a stray `@` into commit subjects); use `<<'EOF'` heredocs.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (read first) |
| `.paul/ROADMAP.md` | v11.3 milestone + phase detail (§ Active Milestone) |
| `.paul/research/MILESTONE-BRIEF-2026-06-10-worthiness-access.md` | Ratified milestone brief |
| `.paul/research/BUG-cowork-chart-upload-2026-06-10.md` | v11.3-02 source (David's upload dead-end) |
| `docs/ACCESS-POLICY.md` | The oracle (v0.3) |
| `.paul/phases/v11.3-01-anon-access-correctness/` | Completed phase 1 (PLAN + SUMMARY ×2) |
| `.paul/UAT-PENDING.md` | Non-blocking live checks (incl. v11.3-01 anon chart/transpose) |

---

## Resume Instructions

1. `git pull` (multi-computer).
2. Read `.paul/STATE.md` for latest position.
3. Run `/paul:resume` or `/paul:plan` for Phase v11.3-02.

---

*Handoff created: 2026-06-10 — paused at the v11.3-01 → v11.3-02 phase boundary (v11.3-01 shipped to prod).*
