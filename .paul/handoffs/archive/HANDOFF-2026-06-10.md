# PAUL Handoff

**Date:** 2026-06-10 (session 10)
**Status:** paused (clean — everything committed + pushed to `master`)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** CRC Music (sheet-music-app) — multi-tenant (CRC + Brothers Lazaroff). Next.js + Firebase (`crcmusiccharts`); git repo at the `sheet-music-app/` subdirectory; `.paul/` is inside it.
**Core value:** The band gets the right charts + recordings on their iPads each week; Daniel authors setlists conversationally via Claude + MCP (the browser app is the band/consumer surface only).

---

## Current State

**Milestone:** 🚧 v11.2 MCP Stress-Test Fixes (OPEN 2026-06-09) — fixing 9 findings from the Brothers Lazaroff MCP/Perform stress-test report. **2 of 5 phases fully done; phase 03 is 1 of its 2 plans done.**
**Phase:** v11.2-03 MCP error contract [P1/P2] — 🚧 in progress.
**Plan:** none active (loop idle). Next is **v11.2-03-02 (BUG-3)**.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [03-01 loop COMPLETE; phase open for 03-02]
```

**Autonomy posture (binding):** run autonomously — waive approval gates, auto-commit + push per phase to prod `master`, bake decisions into PLANs. QUALITY FLOOR HELD: tsc clean + tests green + AC proof + `SKIP_ENV_VALIDATION=1 npx next build` before declaring deployable + emulator-backed tests where rules/queries change. STOP only for product ambiguity / unresolvable gate failure / cross-tenant leak. Daniel has been pacing with "go" per PAUL step.

---

## What Was Done (this milestone, all shipped to `master`)

- **v11.2-01 (BUG-1, P0)** `6920d61668` — `propose_setlist_changes` + `commit_staged_changes` were the only by-id setlist write tools not threading `orgFrom(extra)`, so the org wall defaulted to crc and 404'd BL setlists. Threaded caller org into both + added a commit-time `rowOrg!==org` wall. Emulator 20/20.
- **v11.2-02 (BUG-9, P1)** `6079d4e3cf` — CONFIRMED cross-tenant leak: `publish_setlist`/`preview_publish` derived recipients from the ENTIRE users collection. Threaded caller org, added caller-org wall, filtered recipients to the setlist's org via `rowOrgIds(...).includes(setlistOrg)` (the v11-05-02 roster pattern). Emulator 25/25. No real publish ever triggered.
- **v11.2-03-01 (BUG-2, P1)** `54cd7ba3bc` — deterministic client errors were 500. Added `song_not_found:404` + `reorder_failed:400` to `ERROR_CODE_MAP`; new shared `uploadFailureEnvelope` maps `processChartUpload` code/status (dedup→409, fault→500) across all 4 upload tools. Unit 12/12 + emulator mcp-chart-upload 55/55.

---

## What's In Progress

Nothing mid-edit. Working tree clean, HEAD = `89a80e9236` (the 03-01 sha-record doc commit), in sync with `origin/master`.

---

## What's Next

**Immediate:** `/paul:plan` for **v11.2-03-02 (BUG-3)** — `bulk_add_tracks` returns **bare-string** per-row errors (`BulkAddResult.error?: string`) while single-row tools return `{code, machine_code, message}`. Fix: change `BulkAddResult.error` to the structured envelope in `src/lib/mcp/server-tracks-write.ts` (`bulkAddTracks`, ~line 1452 interface + ~1536-1639 per-row `planned.push({kind:"invalid", error})` sites), assign a machine_code per cause (song not found → `song_not_found` [now 404 thanks to 03-01], title required → `invalid_argument`, rolled-back collateral → a rolled-back code, resolver-missing → server_error), then update assertions in `mcp-setlist-write.emulator.test.ts` (asserts bare-string `error`) + any bulk tests. Derive code via `codeFor(machine_code)`.

**After that:** v11.2-04 (BUG-4 preview_publish unbonded-song flag + BUG-5 isTest cleanup/dashboard), then v11.2-05 (BUG-6 "CRC MUSIC" authed-header brand leak / BUG-7 text/plain chord-over-lyric renderer / BUG-8 ISO timestamp serialization). Then milestone close.

**Open action on Daniel (live UAT, all safe / emulator-proven):** reconnect Claude Desktop BL connector to `https://www.brotherslazaroff.live/api/mcp` (current bearer is crc-pinned), then run UAT-PENDING: (1) BUG-1 create→propose→commit on BL; (2) BUG-9 `preview_publish` on a BL setlist shows BL roster size not 17 (dryRun, safe). See `.paul/UAT-PENDING.md`.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (read first) |
| `.paul/ROADMAP.md` § Active Milestone | v11.2 phase specs + locked constraints |
| `.paul/phases/v11.2-03-mcp-error-contract/v11.2-03-01-SUMMARY.md` | just-closed loop (BUG-2); BUG-3 notes in Next-Phase-Readiness |
| `src/lib/mcp/server-tracks-write.ts` | `bulkAddTracks` + `BulkAddResult` (the BUG-3 target) |
| `src/lib/mcp/errors.ts` | `ERROR_CODE_MAP` + `codeFor` (per-row code derivation) |
| `.paul/UAT-PENDING.md` | live retest checklist for Daniel |

---

## Resume Instructions

1. `git pull` first (multi-computer box).
2. Read `.paul/STATE.md` for latest position.
3. Run `/paul:resume` (or `/paul:plan` directly for v11.2-03-02).
4. Push `origin master` (NOT `master:main`).

---
*Handoff created: 2026-06-10 (session 10)*
