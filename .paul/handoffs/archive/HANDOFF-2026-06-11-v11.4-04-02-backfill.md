# PAUL Handoff

**Date:** 2026-06-11
**Status:** paused — clean. v11.4-04-02 (default-both rollout) PLAN created, awaiting APPLY. The plan is `autonomous: false` (it ends in a human-action prod backfill).

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** CRC Music (sheet-music-app) — multi-tenant (CRC + Brothers Lazaroff). Next.js + Firebase (`crcmusiccharts`); the git repo is the `sheet-music-app/` subdirectory; `.paul/` is inside it. Production branch is `master`; push `origin master` (NOT `master:main`). Multi-computer box — `git pull` before the next session.
**Core value:** The band gets the right charts + recordings on their iPads each week; Daniel authors setlists conversationally via Claude + MCP (the browser app is the band/consumer surface).

---

## Current State

**Milestone:** 🚧 v11.4 — Publish & Notify (D8). **3 of 4 phases shipped; v11.4-04 is the LAST phase, mid-way (plan 01 ✅, plan 02 planned).**
**Phase:** v11.4-04 — Musician org-membership toggle + default-both backfill (D8 item 5).
**Plan:** `v11.4-04-02` — created, awaiting APPLY (autonomous: false — human-action prod apply).

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ○        ○     [plan 02 created; APPLY builds T1+T2, then STOPS at the T3 human-action]
```

**Git:** `master`, pushed through this WIP commit (the v11.4-04-02 PLAN + STATE/ROADMAP). Prior shipped phase commits: v11.4-04-01 `ba826730d7` (toggle) · v11.4-03 `fb055b4b5d` (MCP contacts) · v11.4-02 `f03b48db88` (org-branded emails) · v11.4-01 `80ea721508` (no-auto-blast picker). Working tree clean after the WIP commit (only the long-standing untracked sweep/research dirs at repo root remain — not ours).

---

## What Was Done (this session — all shipped to `master`)

The whole v11.4 milestone except the final backfill:

- **v11.4-01 `80ea721508`** — killed implicit auto-blast: MCP `publish_setlist` refuses a real publish with undefined `recipients` (`recipients_required`; dryRun/preview still previews); browser `PublishDialog` selection governs all channels (it's orphaned UI but code is correct/tested). Closed the v11.2 BUG-9 blast class.
- **v11.4-02 `f03b48db88`** — org-branded publish/gig-packet/resend emails (BL from-name/header/wordmark/footer); CRC byte-identical. `EmailBranding` registry in `branding.ts`; branding follows the setlist's org. BL from-address env-gated (`RESEND_FROM_EMAIL_BROSLAZ`) with verified-sender fallback.
- **v11.4-03 `fb055b4b5d`** — MCP **contacts** (remembered ad-hoc recipients): org-scoped `contacts` collection + rules (DEPLOYED to prod) + `list/create/delete_contact` tools + `preview_publish.savedContacts[]`. (Surface ratified MCP-not-browser because PublishDialog is orphaned.)
- **v11.4-04-01 `ba826730d7`** — opened the `/manage` People "Band access" tri-state (CRC/BL/Both) from leader-only to ALL non-pending rows (admin-only). Pure UI-gating change.

Each phase: PLAN→APPLY→UNIFY, gates green (tsc · vitest/emulator · `SKIP_ENV_VALIDATION=1 next build`), committed + pushed.

---

## What's In Progress

`v11.4-04-02` PLAN is written but NOT yet applied. Nothing half-coded — APPLY hasn't run. The plan has 3 tasks:
- **T1 (auto):** `ensureUserProfile` (src/lib/users-firebase.ts, the `role:'pending'` create path) defaults new docs to `orgIds: ['crc','brotherslazaroff']`.
- **T2 (auto):** `scripts/v11-4-04-orgids-backfill.mjs` — mirrors `scripts/v11-3-03-library-orphan-sweep.mjs`'s firebase-CLI refresh-token → temp ADC bootstrap. Modes: `--diagnose`, dry-run (default), `--apply`, `--rollback <snapshot>`. Stamps every user `['crc','brotherslazaroff']` on doc + Auth claim (lockstep, like set-role), idempotent (skip already-both), writes a per-user prior-state snapshot under `scripts/.backfill-snapshots/` (absent prior → `FieldValue.delete()` on rollback). Pure helpers (`alreadyBoth`/`targetOrgIds`/`snapshotEntry`/`rollbackTargets`) unit-tested in `scripts/v11-4-04-orgids-backfill.test.ts`.
- **T3 (checkpoint:human-action, BLOCKING):** Daniel runs the prod `--apply`.

---

## What's Next

**Immediate:** `/paul:apply .paul/phases/v11.4-04-membership-toggle-backfill/v11.4-04-02-PLAN.md`
- Builds T1+T2 autonomously, runs gates (tsc · `vitest run scripts/v11-4-04-orgids-backfill.test.ts` · `vitest run src/lib` · `next build`) + a read-only `--diagnose`/dry-run against prod (proves connection, no writes), commits + pushes.
- Then STOPS at the T3 human-action gate.

**Human-action (Daniel runs):** after reviewing the dry-run:
`node scripts/v11-4-04-orgids-backfill.mjs --apply --stamp "$(date -u +%Y%m%dT%H%M%SZ)"`
→ reply "applied" with the changed-count. Rollback if needed: `--rollback scripts/.backfill-snapshots/<file>.json`.

**After that:** UNIFY plan 02 → `/paul:complete-milestone` (v11.4 closes; bump `docs/ACCESS-POLICY.md` oracle → v0.4; tag a release per the v11.x pattern).

**RATIFIED scope (do not re-litigate):** default-both applies to EVERYONE incl. band_leaders/admins → grants cross-tenant authoring (Daniel chose this with the warning shown). `rowOrgIds`/`getOrgIdsFromClaims` default STAYS `['crc']` (safety net) — "both" is explicit data.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (read first) |
| `.paul/phases/v11.4-04-membership-toggle-backfill/v11.4-04-02-PLAN.md` | The plan to apply |
| `.paul/phases/v11.4-04-membership-toggle-backfill/v11.4-04-01-SUMMARY.md` | The toggle (prior plan) |
| `src/lib/users-firebase.ts` | `ensureUserProfile` (T1 new-account default seam) |
| `scripts/v11-3-03-library-orphan-sweep.mjs` | The prod-script ADC + flag pattern to mirror (T2) |
| `src/app/api/admin/set-role/route.ts` | The claim+doc lockstep write pattern (T2) |
| `.paul/UAT-PENDING.md` | Live checks (incl. v11.4-01/02/03 + the toggle) |

---

## Resume Instructions

1. `git pull` first (multi-computer box).
2. Read `.paul/STATE.md`.
3. Run `/paul:resume` (or go straight to `/paul:apply` the plan above).
4. Push `origin master` (NOT `master:main`).

---
*Handoff created: 2026-06-11 (v11.4-04-02 backfill pause)*
