# PAUL Handoff

**Date:** 2026-05-12 (afternoon pause)
**Status:** paused — milestone reconciliation complete, awaiting /paul:resume

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — tablet-first web app for Central Reform Congregation's worship band. Musicians load it on iPads/tablets/phones to manage setlists, view and live-transpose charts, and adjust monitor mixes during services, rehearsals, and jam events.

**Core value:** Musicians can instantly access setlists, transpose charts, and control monitor mixes — all live on any device, no paper or prep needed.

**Single user in production today:** Rabbi Daniel (sole admin, owner). Band onboarding pending. Deploys go straight to master on Vercel; no preview branches.

---

## Current State

**Version:** v5.4 🟡 IN PROGRESS — closing imminently
**Phase:** v54-01 ✅ LOOP COMPLETE (PENDING-UAT); v54-02-01 LOOP COMPLETE pending push; v54-02-02 / v54-03 / Mobile AddBar DEFERRED (will fold-forward into v6.0)
**Plan:** No active plan — between milestones at pause time.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     [between milestones — v5.4 closing, v6.0 not yet opened]
```

**Master HEAD at pause:** `4ee6e70` (pushed). Today shipped 10 commits total — 8 P0 patches + 2 cleanups — listed below.

---

## What Was Done This Session

### Morning — T2.6 dead-code sweep (cleanup commits)
- `146b40b` — Delete unused `isMobile` + `useMediaQuery` import in SetlistGrid.tsx (T2.6.a from the old FIX-PLAN-V2 backlog)
- `6b83ec4` — Drop deprecated `clearFailedOutboxRows` alias in src/lib/sync/cleanup.ts (T2.6.d)

### Mid-day — P0 bandage sprint (5 fixes + 1 modal kill + 1 library fix + tests)
User reported in production: deleted tracks resurrecting, library adds invisible, aggressive merge popups. Shipped:
- `6cd2c4e` — Stop clearing tombstones on delete-commit success (engine.ts)
- `c9e92a5` — SSR reads top-level `tracks/{id}` when `setlists/{S}.hydrated === true` (page.tsx)
- `ed63efc` — Reconciliation modal filters to version-mismatch rows only (later superseded)
- `5601726` — Fold `trackCount` into the cascade's `hydrated:true` setlist update (SetlistGridHydrator.tsx)
- `b0e7033` — Engine auto-retries VersionMismatchError when `remote=undefined` + attempts==0 (engine.ts)
- `7421c51` — Test expectations updated to match the new behavior
- `a0c61cc` — Modal force-disabled (`hasConflict = false` unconditional)
- `4ee6e70` — Library "Add to setlist" mirrors writes into top-level `tracks/{id}` (use-add-to-setlist.ts)

### Afternoon — User halted patching; deep architectural audit
User explicit instruction: "stop with the bandaids. I want a real plan and a real fix. with real research." Launched four parallel scope-narrowed audit agents + one synthesizer:
- `RESEARCH/audit-writes.md` — 27 production writers mapped (W1-W27)
- `RESEARCH/audit-reads.md` — 26 production read sites + 5 subscriptions (R1-R26 + R-Sub1-Sub5)
- `RESEARCH/audit-sync.md` — sync mechanics, state machine, 4 hot-path traces
- `RESEARCH/audit-hotpaths.md` — 6 end-to-end user scenarios walked through current code
- `RESEARCH/TRACKS-MIGRATION-AUDIT-2026-05-12.md` — synthesized design doc recommending Option A (finish migration) with first-24h commit plan

### Late afternoon — Milestone reconciliation
User invoked `/paul:discuss-milestone`. Reconciled v5.4 vs the migration scope; user locked 12 decisions; produced `.paul/MILESTONE-CONTEXT.md` defining **v6.0 — Tracks Single-Source-of-Truth** (10 phases in 4 waves, with v5.4 deferrals folded forward).

---

## What's In Progress

- **v5.4 closure** — pending. v54-01 + v54-01-02 + v54-02-01 work shipped; v54-02-02 + v54-03 + Mobile AddBar fold-forward into v6.0. Awaiting `/paul:complete-milestone` invocation.
- **v6.0 scaffolding** — `.paul/MILESTONE-CONTEXT.md` is written and locked, but the milestone itself is not yet created. Awaiting `/paul:milestone` (after v5.4 closes).
- **PENDING-UAT carry-over** — v5.0, v5.2, v5.3, v54-01 all still in PENDING-UAT against deployed commits over the upcoming worship cycle. The 8 P0 patches today are also PENDING-UAT (Daniel reported partial recovery mid-session but symptoms reappeared, triggering the audit pivot).

---

## What's Next

**Immediate (in order):**
1. `/paul:complete-milestone` — Close v5.4 cleanly. Fold-forward labels: v54-02-02 → v60-03, v54-03 → v60-09, Mobile AddBar → v60-10.
2. `/paul:milestone` — Create v6.0 scaffolding from `.paul/MILESTONE-CONTEXT.md`. No re-litigation of decisions; they're all locked.
3. `/paul:apply` v60-01 — SyncIndicator conflict click rewire + silent last-write-wins-on-retry. 2-line UI + 1-line engine policy. Target start: Monday 2026-05-13 so Daniel has a full week before the next worship cycle.

**Wave plan (10 phases / 4 waves):**

| Wave | Phases | Purpose |
|------|--------|---------|
| 1 (parallel-safe) | v60-01 (conflict click rewire) + v60-02 (pagehide blur) | Orthogonal UX fixes; ship first |
| 2 (BLOCKING for Wave 3+ engine phases) | v60-03 (Java JDK 21 install + emulator canary) | Harness Fidelity Gate closure |
| 3 (migration spine, sequential) | v60-04 (server reads) → v60-05 (editor/perform reads) → v60-06 (dashboard + backfill) → v60-07 (writer removal + strip) → v60-08 (cleanup) | Finish v50-05 |
| 4 (parallel-safe after Wave 3) | v60-09 (library sync) + v60-10 (Mobile AddBar) | Folded-in v5.4 deferrals |

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/PROJECT.md` | Requirements + constraints |
| `.paul/MILESTONES.md` | Completed milestone log |
| `.paul/ROADMAP.md` | Phase overview |
| `.paul/MILESTONE-CONTEXT.md` | **v6.0 plan with 12 locked decisions and 10-phase wave structure (consumed by /paul:milestone)** |
| `RESEARCH/TRACKS-MIGRATION-AUDIT-2026-05-12.md` | Synthesized design doc; defends Option A; first-24h commit plan |
| `RESEARCH/audit-writes.md` | 27 writer entries with file:line + payload + side effects |
| `RESEARCH/audit-reads.md` | 26 reader entries + 5 subscriptions |
| `RESEARCH/audit-sync.md` | Sync mechanics + 4 traces (edit, legacy unstamped, real conflict, SW reload) |
| `RESEARCH/audit-hotpaths.md` | 6 end-to-end user scenarios traced through current code |
| `RESEARCH/P0-SAVE-DELETE-MODAL-2026-05-12.md` | Earlier P0 triage doc (somewhat superseded by the full audit but useful for historical context) |
| `HANDOFF.md` (root, untracked) | Old leftover from earlier in the day — NOT this handoff; can be ignored or deleted |

---

## Locked Decisions (from MILESTONE-CONTEXT.md — do NOT re-litigate on resume)

1. Mid-edit text protection (pagehide blur) ships in parallel with migration (Wave 1)
2. Dashboard search-by-track-title acceptably degrades during migration window (returns via Dexie scan later)
3. Backfill scope = 15 most-recent setlists only
4. Conflict retry → silent last-write-wins + Sentry capture (no user-facing modal)
5. Embedded `tracks[]` field stripped immediately (no freeze period)
6. Option A (finish migration), NOT Option B (rollback)
7. Top-level `tracks/{id}` is single source of truth
8. Browser-smoke per phase mandatory (not just vitest green)
9. v5.4 closes cleanly via /paul:complete-milestone first
10. Install Java JDK 21 + ship v54-02-02 emulator canary before Wave 3 engine phases
11. v54-03 library sync + Mobile AddBar variant fold INTO v6.0 (not deferred to v6.1)
12. Milestone name is v6.0 — Tracks Single-Source-of-Truth (major version bump)

---

## Working Tree at Pause

Untracked (uncommitted but valuable):
- `.paul/MILESTONE-CONTEXT.md` — v6.0 plan; persists across /clear per workflow spec
- `RESEARCH/TRACKS-MIGRATION-AUDIT-2026-05-12.md` + four audit scratch docs + earlier P0 triage doc
- `HANDOFF.md` (root) — stale leftover from morning; not related to this handoff

No tracked-file modifications. Master is clean against `4ee6e70` which is pushed to origin.

---

## Resume Instructions

1. `/paul:resume` — restore context from this handoff
2. Verify working tree is in expected state (`git status` — should show the untracked research + .paul/MILESTONE-CONTEXT.md)
3. Run `/paul:complete-milestone` to close v5.4
4. Run `/paul:milestone` to open v6.0 from MILESTONE-CONTEXT.md
5. Begin Wave 1 with v60-01 — SyncIndicator conflict click rewire (Monday start recommended)

---

## Important Context for Resume Claude

- **The user is fatigued from band-aid fixes.** They explicitly halted patching mid-day. The architectural audit was their requested antidote. Do NOT propose tactical patches when /paul:resume runs — execute the planned waves under standard PAUL discipline.
- **Browser-smoke before phase close is mandatory** per locked decision #8. Vitest green is necessary but not sufficient.
- **HFG counter is at 1/3** (clause-(b) waiver from v53-02). v60-03 (Wave 2) resets it to 0/3 before Wave 3 engine phases begin. If Wave 3 phases ship without HFG closure they burn waivers and the gate auto-escalates at 3/3.
- **Daniel publishes Friday afternoons for Friday evening services.** No risky deploys Thursday afternoon → Sunday. v60-01/02 (orthogonal UX fixes) are low-risk and safe anytime; engine-touching phases respect the cadence.
- **Java install for v60-03** is a fully-attended human-action step (Windows `winget install Microsoft.OpenJDK.21` with admin rights). Plan v60-03 around the install or treat it as out-of-band prerequisite per /paul:milestone's preference.

---

*Handoff created: 2026-05-12 afternoon by /paul:pause.*
