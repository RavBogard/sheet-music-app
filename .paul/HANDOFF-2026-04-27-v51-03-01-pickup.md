# PAUL Handoff — v51-03-01 PLAN created; paused before APPLY

**Date:** 2026-04-27
**Status:** paused (mid-loop, awaiting APPLY approval)
**Branch:** master
**Last commit:** `05ddafb` (v51-02 phase-close, pushed)

---

## READ THIS FIRST

You have no prior context. This document tells you everything you need.

**Project:** sheet-music-app (CentralReform.live) — web app for Central Reform Congregation's worship band. Tablet-first; iPads on music stands during live services.

**Core value:** Musicians can instantly access setlists, transpose charts, and control their monitor mix — live on any device, no paper or prep needed.

**CRITICAL CULTURAL CONTEXT:** CRC is a **Reform Jewish synagogue**. Worship cadence is **Friday evening (Erev Shabbat) + Saturday morning (Shabbat)**. NOT Sunday. See `~/.claude/projects/C--Users-dsbog-CentralReform-live/memory/project_shul_cadence.md`.

---

## Current State

| Field | Value |
|-------|-------|
| package.json version | 0.1.0 (pre-existing dirty state — not v51-03 work; auto-touched by dev script) |
| PAUL milestone | v5.1 — Editor UX Polish (band-onboarding gate) |
| Milestone progress | 2 of 4 phases complete |
| Phase | 🚧 v51-03 — Smart create-setlist wizard (date-aware via Hebcal) |
| Plan | v51-03-01 PLAN created, awaiting approval |
| Loop | PLAN ✓ → APPLY ○ → UNIFY ○ |

Other open milestones (no action required):
- **v5.0** Bulletproof Editor 🟡 PENDING UAT — close path is v5.1 ships → Daniel UAT → `/paul:audit-milestone v5.0`
- **v5.0-hotfix** ✅ COMPLETE 2026-04-27, archived

---

## What Was Done This Session

1. `/paul:resume` → restored from `HANDOFF-2026-04-27-v51-01-complete.md` (v51-01 picker rework was last shipped); archived that handoff
2. `/paul:plan` v51-02-01 → wrote PLAN with /ui-ux-pro-max BLOCKING gate + 5 tasks + 8 ACs; STATE.md + ROADMAP.md updated to "v51-02 in PLAN"
3. `/paul:apply v51-02-01` →
   - Loaded `/ui-ux-pro-max`; queried design DB for dashboard/data-table dense + dark-mode hierarchy + shadcn data-table
   - Task 1: wrote `v51-02-01-DESIGN-CONTRACT.md` with 3 option sets (A Tight Compact / B Comfortable Dense / C Hierarchical Spacious)
   - Task 2 decision-checkpoint: Daniel locked **Option B "Comfortable Dense"**
   - Task 3: single-file edit to `SetlistGrid.tsx` (tier classes + section-row framing + tightened TD padding + column sizes); all 8 cell components already accept `className` so no cell-internal churn
   - Task 4: extended `SetlistGrid.a11y.test.tsx` with 3 new cases; suite 1495/1495 (was 1492 + 3 new); `next build` clean; boundary diff empty for mobile/perf-view/picker/sync/firestore-rules
   - Task 5 HUMAN-VERIFY: Daniel approved ("go"); pushed `c40d880` to origin master
4. `/paul:unify v51-02-01` →
   - Wrote `v51-02-01-SUMMARY.md` (full pass/fail table, decisions, deviations including the realism-adjustment lesson on `py-1.5/2.5` → `py-0.5/0.5`)
   - Phase v51-02 ✅ COMPLETE (1/1 plans). Triggered transition workflow:
     - Evolved PROJECT.md (added v51-02 entry under "Validated this cycle"; bumped v5.1 to 2/4; added 2 decisions to Key Decisions table)
     - Updated ROADMAP.md (v51-02 row → ✅ Complete; milestone → 2 of 4)
     - Phase-close commit `05ddafb` + push (range `c40d880..05ddafb`)
     - State consistency verified ✓
5. `/paul:plan` v51-03-01 →
   - Researched existing infra: `liturgical-calendar.ts` (`getServiceContext` + `getFullServiceContext` already wired with Hebcal HTTP API + offline graceful fallback); `liturgical-templates.ts` (TEMPLATE_LABELS + getAllTemplateKeys + buildSetlistFromTemplate); `CreationWizard.tsx` (single-step Dialog: name + template select + date + rabbi); `setlist-firebase.ts` `cloneForNextWeek(source)` (clones with date+7); `EmptyState.tsx` already has `onMakeNextWeeks` + `onUseTemplate` hooks
   - Wrote `v51-03-01-PLAN.md` with 5 tasks (helper → 3-offer UI with /ui-ux-pro-max consult → clone wiring + sticky-memory verify → suite/build verify → HUMAN-VERIFY)
   - 9 ACs covering helper match/fallback/null + UI surface/disable + clone uses chosen date + sticky-memory preserved + telemetry tag + UAT
6. `/paul:pause` → this handoff

---

## What's In Progress

**Nothing actively in progress** — clean pause point.

Local-only changes (not pushed; uncommitted):
- `.paul/STATE.md` (session continuity update — being written now via this pause workflow)
- `.paul/ROADMAP.md` (v51-03 row → 🚧 Planning)
- `.paul/phases/v51-03-create-setlist-wizard/v51-03-01-PLAN.md` (new file)

Pre-existing dirty (NOT v51-03 work — predates this session, auto-touched by dev script):
- `package.json` (version 2.11.19 → 0.1.0)
- `src/build-info.json` (version + commit hash)

---

## What's Next

**Immediate:** `/paul:apply .paul/phases/v51-03-create-setlist-wizard/v51-03-01-PLAN.md` to execute v51-03-01.

**The plan in one paragraph:** Add a `findLastMatchingService(serviceType, beforeDate?)` helper to setlist-firebase.ts that finds the most recent setlist matching an inferred service-type (using either `templateType` field OR `getServiceContext(eventDate).type` fallback for legacy setlists). Add a three-offer pre-form strip to CreationWizard.tsx: when user picks a date, show "Clone last {service-name} ({date})" as primary CTA with secondary "Use a template" + tertiary "Start from scratch". Refactor `cloneForNextWeek` as a wrapper around new `cloneSetlist(source, targetDate)` so the EmptyState's "Make next week's" CTA still works. Verify (don't modify) that v50-04 sticky song memory propagates through the clone path: cloned tracks are byte-identical copies of source tracks (verbatim copy preserves user intent); fresh sticky values arrive via seedTrackFromSong on next ChartBindPopover use.

**autonomous=false:** /ui-ux-pro-max BLOCKING at APPLY entry per SPECIAL-FLOWS.md (must load BEFORE Task 2 designs the offer strip); 1 HUMAN-VERIFY checkpoint at end (Daniel UAT on desktop + iPad).

**After v51-03 closes:** v51-04 (Vocal Lead label rename + Daniel-loop UAT codification + gig-packet print smoke check) → Daniel UAT for next Erev Shabbat → invite band → first-week smoke → `/paul:audit-milestone v5.0` closes the v5.0 milestone.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (read first on resume) |
| `.paul/ROADMAP.md` | v5.1 milestone + 4 phases (v51-01 ✅ + v51-02 ✅ + v51-03 🚧 Planning + v51-04 Not started) |
| `.paul/PROJECT.md` | Requirements; v51-02 entry added under Validated this cycle; 2 new decisions in Key Decisions table |
| `.paul/phases/v51-03-create-setlist-wizard/v51-03-01-PLAN.md` | The plan to execute next |
| `.paul/phases/v51-02-editor-readability/v51-02-01-PLAN.md` | Just-shipped plan (reference for similar shape) |
| `.paul/phases/v51-02-editor-readability/v51-02-01-DESIGN-CONTRACT.md` | Locked Option B design ground-truth (referenced by v51-04 if it touches column widths) |
| `.paul/phases/v51-02-editor-readability/v51-02-01-SUMMARY.md` | What got built in v51-02 (incl. realism-adjustment lesson) |

Source code surfaces v51-03-01 will touch:
- `src/lib/setlist-firebase.ts` — add `findLastMatchingService` + new `cloneSetlist(source, targetDate)`; refactor `cloneForNextWeek` as wrapper
- `src/lib/__tests__/setlist-firebase.test.ts` — extend
- `src/hooks/use-creation-wizard.ts` — extend with mode + cloneSource state; wire clone branch in create()
- `src/hooks/__tests__/use-creation-wizard.test.ts` — extend with 4 cases
- `src/components/setlist/wizard/CreationWizard.tsx` — add three-offer pre-form strip

Source code surfaces v51-03 must NOT touch (boundary lock):
- `src/components/setlist/grid/**` — v51-02 just shipped; pixel-frozen
- `src/lib/songs/defaults.ts` — v50-04 sticky-memory infrastructure
- `src/lib/local/**` + `src/lib/sync/**` — substrate
- `src/components/performance/SetlistView.tsx` — v5h-01-03 architectural refactor
- `firestore.rules` — v5h-01-02 fix
- `cloneForNextWeek` public surface (refactored to wrapper, public signature preserved)

---

## Commits This Session

| Commit | Description | Pushed? |
|--------|-------------|---------|
| `c40d880` | feat(v51-02-01) editor readability + visual hierarchy (Option B Comfortable Dense) | ✅ pushed |
| `05ddafb` | feat(v51-02) close phase | ✅ pushed |
| (uncommitted) | v51-03-01 PLAN + STATE.md + ROADMAP.md updates | local |

Earlier same-day local-only commits also pushed in the c40d880 push range:
- `70abfff` (v51-01 close from prior session) — pushed in `304e940..c40d880` range

**For the resumer:** when you do the v51-03-01 task feature commit, explicitly stage the v51-03 plan dir alongside the source files (per memory `feedback_paul_phase_commits.md` — explicitly stage `.paul/phases/{phase}/` to avoid orphaning PLAN/SUMMARY artifacts). The pre-existing `package.json` + `src/build-info.json` dirty state should NOT be staged.

---

## Resume Instructions

1. Run `/paul:resume` — it'll detect this handoff (`HANDOFF-2026-04-27-v51-03-01-pickup.md`), load STATE.md, and route to `/paul:apply .paul/phases/v51-03-create-setlist-wizard/v51-03-01-PLAN.md`
2. Or explicitly: `/paul:apply .paul/phases/v51-03-create-setlist-wizard/v51-03-01-PLAN.md` to skip the resume routing
3. `/ui-ux-pro-max` will be BLOCKING at APPLY entry — invoke before Task 2 (UI work)

---

## Standing User Preferences (from auto-memory)

- Push to `origin master` (not `master:main`); deploy via Vercel; no preview branches
- Tablet-first application; band on iPads on music stands
- Reform Jewish synagogue — Friday night + Shabbat morning; not Sunday
- "Vocal Lead" not "Lead"/"Leader" (per-song role); rabbi "Led by:" on print is distinct
- App must be "bulletproof and easy and intuitive" before band onboarding
- Explicitly stage `.paul/phases/{phase}/` dir on PAUL commits (don't orphan PLAN/SUMMARY files)
- Run `next build` not just `tsc` for verification (catches Next.js App Router export violations)

---

*Handoff created: 2026-04-27 at end of v51-02 close + v51-03-01 PLAN session.*
*Single next action on resume: `/paul:apply .paul/phases/v51-03-create-setlist-wizard/v51-03-01-PLAN.md`.*
