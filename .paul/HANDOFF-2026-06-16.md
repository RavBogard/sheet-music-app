# PAUL Handoff

**Date:** 2026-06-16
**Status:** PAUSED mid-phase — v11.5-04 at 2 of 3 plans complete; loop idle (ready to PLAN 03).

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** sheet-music-app (CentralReform.live) — multi-tenant worship/band sheet-music app. Browser = band/consumer surface (Perform mode on iPads); Daniel authors via Claude + MCP. 2 live tenants: CRC + Brothers Lazaroff (brotherslazaroff.live).
**Core value:** the band gets the right charts + recordings on their iPads each week; the web app's job is performance + quick edits.

---

## Current State

**Milestone:** 🚧 v11.5 — Bulletproof Performance. Oracle `docs/ACCESS-POLICY.md` v0.4. **2 of 4 active phases complete** (v11.5-03 photo-import DROPPED → v11.6 backlog).
**Phase:** v11.5-04 (Hygiene & harness) — **IN PROGRESS, 2 of 3 plans complete.**
**Plan:** none open. 01 ✅ + 02 ✅ closed; 03 not started.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY     [Plan 02 closed; loop idle — ready to PLAN 03]
  ✓        ✓        ✓
```

All committed + pushed to `origin master` (HEAD `98c39c5c17`). Working tree clean except unrelated untracked research artifacts.

---

## What Was Done (this session, 2026-06-15→16)

- **Resumed** from HANDOFF-2026-06-14 (v11.5-02 complete; archived).
- **DROPPED v11.5-03 photo-import** — Daniel reversed the funding call before any code was written. VERIFY-FIRST recon (what's reusable) preserved in ROADMAP § Phase v11.5-03 → v11.6 backlog. Number retired, NOT renumbered (04/05 stable).
- **Opened v11.5-04 (Hygiene & harness)**, split into 3 plans:
  - **Plan 01 — error contracts ✅ CLOSED** (`d3ee8907fc`): M-11 `contact_not_found`→404 in `ERROR_CODE_MAP` + emulator test (7/7); M-10 `publish_setlist` D8 schema doc (doc-only).
  - **Plan 02 — library hygiene ✅ CLOSED** (`fa3cfd2a8a`): pure `src/lib/library/junk-filter.ts` (`isJunkLibraryRow` + extracted `isNonChartArtifactShape`; `library.ts` delegates → no MCP regression); consumer browse (`getServerLibrary`) + bind-picker (`ChartBindPopover`) hide junk; Drive-sync ingestion guard (`sync-engine.ts` `ingestFiles` + `stats.skippedNonChart`). Gates: junk-filter 7/7, sync 9/9, `next build --webpack` exit 0, tsc no new errors. /ui-ux-pro-max invoked.
- STATE git-state aligned (`918dbb8c9c`, `98c39c5c17`).

## What's Next

**Immediate:** `/paul:plan` Phase **v11.5-04 Plan 03 — harness & ops** (the LAST plan of the phase → its UNIFY triggers the phase transition: PROJECT/ROADMAP evolve + phase-close commit). Scope:
- H8 — `cleanup_all_test_data` cascade releases monitor-bus assignments (orphaned uid on a real bus).
- F-8 — `orgIds` option on `create_test_account`.
- M-12 — chunked-upload session TTL → ~60 min + tool descriptions document Drive-staging as the primary agent path.
- Permanent test `.docx` fixture in the app Drive folder.
- BL-tier bearer doc for Daniel's Claude Code MCP config.
- **chord-cache `tsc` fix** (see Carried Concern — makes `tsc` clean before phase close).
- **VERIFY-FIRST prod junk-row deletion** — the two `[role-*] tiny` rows + the `.DS_Store` row. `delete_chart` has a live-bond guard (refuses `chart_in_use` 409) but **no dryRun** → bond-probe each fileId (`tracks where fileId==`) BEFORE deleting. Real data op — handle as a single-owner ops step.
- **delete_chart-author verification** (recon: uploader-or-admin; core/supplemental admin-only).

Plan 03 is a grab-bag — consider recon + possibly splitting once the cascade/harness pieces are scoped.

## Carried Concern (NOT a blocker)

- **Pre-existing `tsc` error** at `src/app/api/library/chord-cache/route.test.ts:164` (`NextResponse<{error}>` where `Promise<NextResponse|null>` expected). Stash-verified present at baseline HEAD `e7d9773017` — pre-dates this phase (chord-cache = H5/v11.5-01-02 test). Does NOT affect `next build --webpack`. **Fix folded into Plan 03** so `tsc` is clean before phase close.

## Operational (Daniel's discretion)

- Revert David admin→band_leader (H9 shipped `d7cbb1a4e0` 2026-06-12; the temp admin bump is no longer needed). Use `/api/admin/set-role` (orgIds-preserving; NEVER `scripts/set-role.js`).

## Flagged (INFRA, not app code)

- `/perform` cold·mobile CLS 0.250 + Vercel TTFB keep-warm (both infra follow-ups, not app churn).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview (§ Phase v11.5-04 = current; v11.5-03 dropped) |
| `.paul/phases/v11.5-04-hygiene-harness/v11.5-04-02-SUMMARY.md` | Plan 02 outcome |
| `src/lib/library/junk-filter.ts` | Shared pure junk predicate (Plan 02) |
| `.paul/research/MILESTONE-BRIEF-v11.5-bulletproof-performance.md` | v11.5 scope (§ Phase 4 lists Plan 03 items) |

## Resume Instructions

1. `git pull` first (multi-computer). master is prod; push `origin master` (NOT master:main).
2. `/paul:resume` → it will route to `/paul:plan` for Phase v11.5-04 Plan 03.

---
*Handoff created: 2026-06-16 (pause mid-phase, v11.5-04 at 2/3)*
