---
phase: 03-setlist-performance-view
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/perform/page.tsx
  - src/app/(main)/DashboardClient.tsx
  - src/app/(main)/page.tsx
  - src/components/home/NextServiceCard.tsx
  - src/components/performance/__tests__/public-view.test.tsx
  - src/components/home/__tests__/next-service-card.test.tsx
  - package.json
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03, HOME-01, HOME-02]

must_haves:
  truths:
    - "Non-signed-in visitor at centralreform.live/perform sees a list of public setlists"
    - "Visitor taps a public setlist and sees the same performance view -- read-only, no transposition, no monitor, no editing"
    - "No sign-in prompt shown to public visitors"
    - "Signed-in musician sees one focused card: service date, setlist name, who's playing, and a Perform button"
    - "Home screen is not a dashboard -- one card, one action"
    - "Empty state shows the most recent past setlist for practice reference"
  artifacts:
    - path: "src/app/perform/page.tsx"
      provides: "Public setlist listing page for unauthenticated visitors"
    - path: "src/components/home/NextServiceCard.tsx"
      provides: "Single focused card for home screen"
      exports: ["NextServiceCard"]
    - path: "src/app/(main)/DashboardClient.tsx"
      provides: "Simplified dashboard rendering single card"
  key_links:
    - from: "src/app/perform/page.tsx"
      to: "src/lib/setlist-firebase.ts"
      via: "subscribeToPublicSetlists for listing"
      pattern: "subscribeToPublicSetlists"
    - from: "src/app/perform/setlist/[id]/page.tsx"
      to: "useAuth"
      via: "isPublicView = !user conditional rendering"
      pattern: "isPublicView.*!user"
    - from: "src/components/home/NextServiceCard.tsx"
      to: "src/app/(main)/DashboardClient.tsx"
      via: "replaces HeroCard + surrounding complexity"
      pattern: "NextServiceCard"
---

<objective>
Build public access for community jam sessions and redesign the home screen as a single focused card. Non-signed-in visitors browse public setlists and view PDFs without authentication. Signed-in musicians see one card with this week's service and a prominent Perform button.

Purpose: Public access enables community engagement without friction. The focused home screen ensures musicians get to their music in one tap, not through a busy dashboard.
Output: Public setlist landing page, conditional public/authenticated rendering, simplified home screen.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-setlist-performance-view/03-CONTEXT.md
@.planning/phases/03-setlist-performance-view/03-RESEARCH.md
@.planning/phases/03-setlist-performance-view/03-01-SUMMARY.md

@src/app/(main)/DashboardClient.tsx
@src/app/(main)/page.tsx
@src/app/perform/layout.tsx
@src/lib/setlist-firebase.ts
@src/lib/auth-context.tsx
@src/middleware.ts
@src/components/dashboard/HeroCard.tsx

<interfaces>
<!-- Existing code the executor needs -->

From src/lib/setlist-firebase.ts:
```typescript
export function createSetlistService(uid?: string | null, displayName?: string | null): {
    subscribeToPublicSetlists: (callback: (setlists: Setlist[]) => void) => () => void
    subscribeToPersonalSetlists: (callback: (setlists: Setlist[]) => void) => () => void
    // ... other methods
}
```

From src/lib/auth-context.tsx:
```typescript
export function useAuth(): {
    user: User | null
    profile: UserProfile | null
    isMember: boolean
    isBandLeader: boolean
    loading: boolean
    signIn: () => void
    // ...
}
```

From src/types/models.ts:
```typescript
export interface Setlist {
    id: string
    name: string
    eventDate?: FirestoreDate
    tracks: SetlistTrack[]
    isPublic?: boolean
    musicians?: SetlistMusician[]
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Public setlist landing page and conditional rendering</name>
  <files>src/app/perform/page.tsx, src/components/performance/__tests__/public-view.test.tsx</files>
  <behavior>
    - Test 1: Public listing page renders setlists without requiring auth
    - Test 2: Public setlist links point to /perform/setlist/{id}
    - Test 3: Public performance view hides monitor button, transposition, edit controls
    - Test 4: No sign-in prompt shown in public view
  </behavior>
  <action>
  **Create src/app/perform/page.tsx** -- Public setlist listing:
  - This route is already allowed for unauthenticated users (middleware allows `/perform/*`).
  - Use `createSetlistService()` (no uid) and `subscribeToPublicSetlists` to list public setlists.
  - Display each setlist as a card/row: setlist name, event date (formatted with date-fns), track count.
  - Link each to `/perform/setlist/{id}`.
  - Sort by event date descending (most recent first).
  - Clean, minimal design -- no sign-in prompt, no auth-gated UI.
  - Header: "CRC Music" or congregation name, simple branding.
  - Empty state: "No public setlists available" message.
  - Loading state: simple skeleton cards.

  **Conditional rendering in performance page:**
  - The performance page (from Plan 01) already accepts `isPublicView` prop through SetlistView.
  - Ensure the page.tsx derives `isPublicView = !user` from `useAuth()`.
  - When public: hide edit button (pencil), hide print button, hide monitor-related UI, show concert key (no transposition -- defaultTransposition = 0).
  - When public: header back link goes to `/perform` (public listing) not `/setlists` (authenticated).
  - Do NOT call `useMusicianTransposition()` for public view -- pass `defaultTransposition: 0`.

  **Test file:**
  - Test that public listing renders setlist cards.
  - Test that public performance view does not render monitor or transposition controls.
  - Mock auth context to return null user for public tests.
  </action>
  <verify>
    <automated>npx vitest run src/components/performance/__tests__/public-view.test.tsx -x</automated>
  </verify>
  <done>Public visitors can browse setlists at /perform and view any public setlist. No sign-in required, no monitor/transposition/edit UI shown.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Home screen redesign to single focused card</name>
  <files>src/components/home/NextServiceCard.tsx, src/app/(main)/DashboardClient.tsx, src/components/home/__tests__/next-service-card.test.tsx</files>
  <behavior>
    - Test 1: NextServiceCard renders service date, setlist name, and Perform button
    - Test 2: NextServiceCard renders musician names when available
    - Test 3: Empty state shows most recent past setlist with "Practice" label
    - Test 4: Card has a single prominent action button
  </behavior>
  <action>
  **Create NextServiceCard.tsx:**
  - Single focused card per CONTEXT.md: "Signed-in musicians see one focused card: this week's service date, setlist name, who's playing, and a prominent 'Perform' button."
  - Props: `setlist: Setlist`, `onPerform: () => void`, `isPastSetlist?: boolean`.
  - Layout: date at top (formatted like "This Saturday, March 14"), setlist name large and bold, musicians listed as small tags/chips, prominent "Perform" button full-width at bottom.
  - If `isPastSetlist`, label changes to "Recent" and button says "Practice" instead of "Perform".
  - Design: glass-card styling, rounded-2xl, subtle border, dark mode compatible.
  - No competing sections, no additional actions beyond the single button.

  **Simplify DashboardClient.tsx:**
  - For signed-in members: replace the current HeroCard + CommandRow + UpcomingTimeline + WhatsChangedBanner + PrepRecommendations + TaskCards with just NextServiceCard.
  - Keep: greeting section (atmospheric hero zone), branding, pending/onboarding states.
  - Remove/hide for members: CommandRow, UpcomingTimeline, WhatsChangedBanner, PrepRecommendations, TaskCards, CompactSetlistRow, additionalUpcoming.
  - The "one card, one action" philosophy per CONTEXT.md.
  - For non-members / public: keep the existing sign-in flow (QRSignIn + Google button) and public setlist links.
  - Empty state (no upcoming service): find the most recent past setlist from the subscribed list and render NextServiceCard with `isPastSetlist: true`.
  - NOTE: Do not delete the existing dashboard component imports -- just change what renders. The removed components may be useful in Phase 4 or Phase 6. Comment them out or conditionally render.

  **Also: Remove recharts if applicable.**
  - Per STATE.md: "recharts dependency orphaned after analytics deletion -- remove when TimelineChart.tsx is deleted in Phase 3."
  - Check if `src/components/analytics/TimelineChart.tsx` still exists. If so, delete it.
  - Run `npm uninstall recharts` to remove the orphaned dependency.
  - If TimelineChart.tsx does NOT exist (already deleted), just uninstall recharts if it's still in package.json.

  **Test file:**
  - Test NextServiceCard renders date, name, musicians, and button.
  - Test empty/past state rendering.
  </action>
  <verify>
    <automated>npx vitest run src/components/home/__tests__/next-service-card.test.tsx -x</automated>
  </verify>
  <done>Home screen shows single focused card with service date, setlist name, musicians, and one Perform button. Dashboard complexity removed. recharts cleaned up.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/components/performance/__tests__/public-view.test.tsx -x` -- all tests pass
2. `npx vitest run src/components/home/__tests__/next-service-card.test.tsx -x` -- all tests pass
3. `npx vitest run --reporter=verbose` -- no regressions
4. Visit `/perform` without auth -- see public setlists
5. Tap a public setlist -- see performance view with no monitor/transposition/edit UI
6. Sign in and visit home -- see single card with next service and Perform button
</verification>

<success_criteria>
- /perform lists public setlists for unauthenticated visitors
- Public setlist view is read-only: no monitor, no transposition, no editing
- No sign-in prompt for public visitors
- Home screen shows one focused card: date, setlist name, musicians, Perform button
- Empty state shows most recent past setlist for practice
- recharts dependency removed
- Not a dashboard -- one card, one action
</success_criteria>

<output>
After completion, create `.planning/phases/03-setlist-performance-view/03-03-SUMMARY.md`
</output>
