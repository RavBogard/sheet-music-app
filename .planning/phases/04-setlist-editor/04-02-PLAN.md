---
phase: 04-setlist-editor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/liturgical-templates.ts
  - src/lib/liturgical-templates.test.ts
  - src/lib/setlist-firebase.ts
  - src/lib/setlist-firebase.test.ts
  - src/components/setlist/wizard/CreationWizard.tsx
  - src/hooks/use-setlist-dashboard.ts
  - src/components/setlist/DuplicateSetlistDialog.tsx
autonomous: true
requirements:
  - EDIT-01
  - EDIT-02

must_haves:
  truths:
    - "Band leader can select from 16 service templates (7 regular, 9 holiday stubs) and get a pre-filled liturgical skeleton"
    - "Band leader can one-tap 'Duplicate for next week' and get a copy with date advanced 7 days, auto-generated name, editor opens immediately"
    - "Template auto-fill matches songs from library using Fuse.js fuzzy search"
    - "Rabbi variants (Daniel/Karen vs Randy) produce different slot structures via onlyFor/skipFor conditionals"
  artifacts:
    - path: "src/lib/liturgical-templates.ts"
      provides: "16 template definitions (7 regular, 9 holiday stubs)"
      contains: "TEMPLATES"
    - path: "src/lib/liturgical-templates.test.ts"
      provides: "Tests for all template definitions and buildSetlistFromTemplate"
      contains: "describe"
  key_links:
    - from: "src/lib/liturgical-templates.ts"
      to: "src/components/setlist/wizard/CreationWizard.tsx"
      via: "getTemplate and buildSetlistFromTemplate called during creation"
      pattern: "getTemplate|buildSetlistFromTemplate"
    - from: "src/hooks/use-setlist-dashboard.ts"
      to: "src/lib/setlist-firebase.ts"
      via: "cloneForNextWeek for duplicate workflow"
      pattern: "cloneForNextWeek"
---

<objective>
Complete all 16 liturgical templates and enhance the duplicate-and-tweak workflow to be the primary weekly creation path.

Purpose: Templates provide the liturgical skeleton so Daniel doesn't start from scratch. Duplicate-and-tweak is the most common weekly workflow (copy last week, swap 2-3 songs). Together, these make setlist creation "faster than a spreadsheet."
Output: 16 templates in liturgical-templates.ts (7 regular with full slot structures, 9 holiday stubs), enhanced duplicate with "Duplicate for next week" prominent on dashboard.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-setlist-editor/04-CONTEXT.md
@.planning/phases/04-setlist-editor/04-RESEARCH.md

@src/lib/liturgical-templates.ts
@src/lib/liturgical-calendar.ts
@src/lib/setlist-firebase.ts
@src/hooks/use-setlist-dashboard.ts
@src/components/setlist/wizard/CreationWizard.tsx
@src/components/setlist/DuplicateSetlistDialog.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/liturgical-templates.ts:
```typescript
export interface TemplateSlot {
    label: string
    type?: TrackType
    isHeader?: boolean  // deprecated, use type: 'header'
    queries: string[]
    topics?: string[]
    parashaSpecific?: boolean
    onlyFor?: string[]
    skipOnHoliday?: boolean
    defaultPerformer?: string
    estimatedMinutes?: number
    description?: string
}

// Existing templates (extend, don't rebuild):
export const FRIDAY_NIGHT_TEMPLATE: TemplateSlot[]      // 19 slots
export const SHABBAT_MORNING_TEMPLATE: TemplateSlot[]    // 24 slots

export function getTemplate(serviceType: string): TemplateSlot[] | null
export function buildSetlistFromTemplate(template, library, context): SetlistTrack[]
export function generateSetlistName(context: ServiceContext): string
```

From src/lib/setlist-firebase.ts:
```typescript
// cloneForNextWeek already exists:
async cloneForNextWeek(source: Setlist): Promise<string>
// Advances date by 7 days, generates liturgical name, creates new Firestore doc
```

From src/hooks/use-setlist-dashboard.ts:
```typescript
// Dashboard state with duplicate/transfer actions
// Provides: handleDuplicate, handleTransfer, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete all 16 liturgical templates</name>
  <files>
    src/lib/liturgical-templates.ts
    src/lib/liturgical-templates.test.ts
  </files>
  <behavior>
    - Test: getTemplate returns a valid template for all 16 service types
    - Test: FRIDAY_NIGHT_TEMPLATE slots include rabbi-variant conditionals (onlyFor: ['daniel_karen'] and onlyFor: ['randy'])
    - Test: buildSetlistFromTemplate with FRIDAY_NIGHT_TEMPLATE produces tracks with correct types (songs, headers, readings, etc.)
    - Test: Rabbi filtering -- when context.rabbi='randy', slots with onlyFor=['daniel_karen'] are excluded
    - Test: Each regular template has at least 10 slots
    - Test: Each holiday stub has at least 5 slots
    - Test: All templates have unique service type keys in TEMPLATES registry
  </behavior>
  <action>
    **1. Extend FRIDAY_NIGHT_TEMPLATE** with rabbi variant slots:
    - Add `onlyFor: ['daniel_karen']` to slots specific to Daniel/Karen services (e.g., meditation moment, specific psalm selections)
    - Add `onlyFor: ['randy']` to Randy-specific slots (e.g., different opening, different closing)
    - Most slots are shared (no onlyFor) -- only 1-3 slots differ per rabbi

    **2. Create 5 new regular templates:**
    - `SHIR_SHABBAT_TEMPLATE`: Simpler Friday evening format, music-focused, fewer liturgical slots (~12 slots)
    - `SATURDAY_MORNING_DANIEL_KAREN_TEMPLATE`: Extend SHABBAT_MORNING_TEMPLATE with Daniel/Karen-specific slots (use onlyFor, mostly same as base)
    - `SATURDAY_MORNING_RANDY_TEMPLATE`: Extend SHABBAT_MORNING_TEMPLATE with Randy-specific slots
    - `BNEI_MITZVAH_SATURDAY_TEMPLATE`: Based on SHABBAT_MORNING but with added B'nei Mitzvah ceremony slots (Torah reading, Haftarah, d'var Torah, parent blessing) (~28 slots)
    - `HAVDALAH_BNEI_MITZVAH_TEMPLATE`: Afternoon service with Havdalah + B'nei Mitzvah elements (~15 slots)

    Use base template + variant overlay pattern to avoid copy-paste. Share common slot sequences where possible (e.g., Torah service slots are reusable across Saturday templates).

    NOTE: Since Daniel/Karen Saturday Morning and Randy Saturday Morning are variants of the same base template, consider using the existing SHABBAT_MORNING_TEMPLATE with onlyFor/skipFor conditionals rather than creating separate arrays. Same approach as Friday Night.

    **3. Stub 9 holiday templates** with basic structures:
    - Rosh Hashanah Evening, Rosh Hashanah Morning
    - Yom Kippur Kol Nidre, Yom Kippur Morning, Yom Kippur Afternoon/Neilah
    - Sukkot, Simchat Torah
    - Passover (Seder service), Shavuot
    - Each stub: 5-8 slots with holiday-specific headers and placeholder song queries
    - Mark as `isStub: true` or add a comment so they can be refined later

    **4. Update TEMPLATES registry** (the object/map that getTemplate reads from):
    - Map all 16 service type keys to their template arrays
    - Ensure getTemplate returns the correct template for each key
    - Template keys should be descriptive: 'friday_night', 'shir_shabbat', 'shabbat_morning', 'bnei_mitzvah_saturday', 'havdalah_bnei_mitzvah', 'rosh_hashanah_evening', etc.

    **5. Update buildSetlistFromTemplate** if needed:
    - Ensure rabbi filtering works: when `context.rabbi` is set, filter out slots where `onlyFor` exists and doesn't include the rabbi
    - Verify Fuse.js matching still works with new template queries

    **6. Write/extend tests:**
    - Test each template type is returned by getTemplate
    - Test rabbi variant filtering
    - Test buildSetlistFromTemplate produces correct track count and types
    - Test that holiday stubs produce valid (if minimal) setlists

    Keep total template code under 500 lines per research pitfall guidance -- use shared slot sequences and the onlyFor/skipFor conditional pattern rather than copy-pasting entire templates.
  </action>
  <verify>
    <automated>npx vitest run src/lib/liturgical-templates.test.ts -x</automated>
  </verify>
  <done>
    - getTemplate returns a valid template for all 16 service types
    - 7 regular templates have full liturgical slot structures (10+ slots each)
    - 9 holiday templates have stub structures (5+ slots each)
    - Rabbi variants work via onlyFor/skipFor conditionals
    - buildSetlistFromTemplate produces correct tracks with proper types
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance duplicate workflow and simplify creation</name>
  <files>
    src/hooks/use-setlist-dashboard.ts
    src/components/setlist/DuplicateSetlistDialog.tsx
    src/components/setlist/wizard/CreationWizard.tsx
    src/lib/setlist-firebase.ts
    src/lib/setlist-firebase.test.ts
  </files>
  <action>
    **1. Enhance "Duplicate for next week"** (EDIT-02):
    - In `DuplicateSetlistDialog.tsx` or `use-setlist-dashboard.ts`: the `cloneForNextWeek` function already exists in setlist-firebase.ts and handles date advance + liturgical name generation. Verify it works correctly.
    - Make "Duplicate for next week" a prominent one-tap action: ensure it is a primary button (not buried in a menu) on the setlist dashboard and on the editor's action bar.
    - After duplication, automatically navigate to the new setlist editor (router.push to `/setlists/{newId}`).
    - Ensure the duplicated setlist opens at the top, ready to scan.

    **2. Simplify CreationWizard** (Claude's Discretion per CONTEXT.md):
    - Current 3-step wizard: Step 1 (name/date), Step 2 (template), Step 3 (review/confirm).
    - Simplify to 2 steps for phone-first UX: Step 1 picks template (with preview of slot count), Step 2 sets name/date (auto-generated from template + date, editable). One-tap "Create" at the end.
    - When template is selected, auto-fill the name using `generateSetlistName` from liturgical-templates.ts with the selected date and template type.
    - After creation, navigate directly to the editor with the new setlist.

    **3. Extend setlist-firebase.test.ts:**
    - Add test for cloneForNextWeek: verify date is advanced by 7 days, name is auto-generated, tracks are copied.
    - Test that duplicated setlist has a new ID (not same as source).

    **4. Dashboard prominence:**
    - In the setlist dashboard (wherever setlists are listed), ensure the most recent setlist has a prominent "Duplicate for next week" button.
    - This should be visually distinct -- a primary/accent button, not just a menu item.
  </action>
  <verify>
    <automated>npx vitest run src/lib/setlist-firebase.test.ts src/lib/liturgical-templates.test.ts -x</automated>
  </verify>
  <done>
    - "Duplicate for next week" is a prominent one-tap action on dashboard
    - Duplicating advances date by 7 days, auto-generates name, opens editor immediately
    - CreationWizard simplified to 2 steps (template picker + name/date)
    - Template selection auto-fills name via generateSetlistName
    - All 16 templates appear in the template picker
    - Tests pass
  </done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- full test suite passes
- `getTemplate('friday_night')` returns a template with 19+ slots
- `getTemplate('rosh_hashanah_evening')` returns a stub template with 5+ slots
- CreationWizard shows 16 template options
- "Duplicate for next week" visible on dashboard without opening a menu
</verification>

<success_criteria>
- 16 templates defined and accessible via getTemplate (7 regular, 9 holiday stubs)
- Rabbi variants work via onlyFor/skipFor conditionals on shared templates
- "Duplicate for next week" is one-tap from dashboard, advances date, navigates to editor
- CreationWizard simplified for phone-first flow
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-setlist-editor/04-02-SUMMARY.md`
</output>
