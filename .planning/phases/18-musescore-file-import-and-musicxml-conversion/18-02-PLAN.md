---
phase: 18-musescore-file-import-and-musicxml-conversion
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/app/api/library/upload/route.ts
  - src/components/library/UploadDialog.tsx
  - src/app/api/library/__tests__/upload-musescore.test.ts
  - src/components/library/__tests__/upload-dialog-musescore.test.ts
autonomous: false
requirements:
  - MS-03
  - MS-04
  - MS-06

must_haves:
  truths:
    - "Upload dialog file picker accepts .mscz and .mscx files"
    - "Upload API validates and accepts .mscz/.mscx files without rejecting them as unsupported"
    - "Uploaded .mscz/.mscx files are converted to MusicXML server-side before storage"
    - "Both original file and converted MusicXML are stored in Firebase Storage"
    - "Library index entry uses application/xml mimeType so OSMD renders the converted file"
    - "Converted MusicXML renders correctly in the existing OSMD viewer"
  artifacts:
    - path: "src/app/api/library/upload/route.ts"
      provides: "Extended upload handler with MuseScore conversion"
      contains: "processMuseScoreFile"
    - path: "src/components/library/UploadDialog.tsx"
      provides: "Updated file picker accepting .mscz,.mscx"
      contains: ".mscz"
    - path: "src/app/api/library/__tests__/upload-musescore.test.ts"
      provides: "Integration tests for MuseScore upload flow"
      min_lines: 40
    - path: "src/components/library/__tests__/upload-dialog-musescore.test.ts"
      provides: "Unit tests for UploadDialog .mscz/.mscx file type acceptance"
      min_lines: 20
  key_links:
    - from: "src/app/api/library/upload/route.ts"
      to: "src/lib/musescore-converter.ts"
      via: "import { processMuseScoreFile }"
      pattern: "processMuseScoreFile"
    - from: "src/app/api/library/upload/route.ts"
      to: "src/lib/firebase-storage.ts"
      via: "uploadToStorage for both original and converted files"
      pattern: "uploadToStorage.*original"
---

<objective>
Wire the MuseScore converter module (from Plan 01) into the existing upload pipeline. Update the upload API route to detect .mscz/.mscx files, convert them to MusicXML, and store both original + converted files. Update the client UploadDialog to accept .mscz/.mscx file types.

Purpose: Users can now upload MuseScore files through the same upload flow they already use for PDFs and MusicXML files, and the app will automatically convert and render them.

Output: Fully integrated upload flow handling MuseScore files end-to-end.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-musescore-file-import-and-musicxml-conversion/18-RESEARCH.md
@.planning/phases/18-musescore-file-import-and-musicxml-conversion/18-01-SUMMARY.md

@src/app/api/library/upload/route.ts
@src/components/library/UploadDialog.tsx
@src/lib/firebase-storage.ts
</context>

<interfaces>
<!-- From Plan 01 output -->
From src/lib/musescore-converter.ts (created in Plan 01):
```typescript
export async function extractMscx(buffer: Buffer): Promise<string>
export async function convertMscxToMusicXml(mscxContent: string): Promise<string>
export async function processMuseScoreFile(buffer: Buffer, extension: 'mscz' | 'mscx'): Promise<{
    musicXml: string
    originalContent: Buffer
}>
```

From src/app/api/library/upload/route.ts (existing):
```typescript
const ALLOWED_TYPES: Record<string, string> = {
    'application/pdf': '.pdf',
    'application/xml': '.xml',
    'text/xml': '.xml',
    'application/vnd.recordare.musicxml+xml': '.musicxml',
    'application/vnd.recordare.musicxml': '.musicxml',
}
// Line 64 extension validation:
if (!ALLOWED_TYPES[mimeType] && !file.name.match(/\.(pdf|xml|musicxml|mxl)$/i))
```

From src/components/library/UploadDialog.tsx (existing):
```typescript
const ACCEPTED_TYPES = ".pdf,.xml,.musicxml,.mxl"
// Line 58 validation:
const validExt = /\.(pdf|xml|musicxml|mxl)$/i.test(selectedFile.name)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate MuseScore conversion into upload route and update client</name>
  <files>src/app/api/library/upload/route.ts, src/components/library/UploadDialog.tsx, src/app/api/library/__tests__/upload-musescore.test.ts, src/components/library/__tests__/upload-dialog-musescore.test.ts</files>
  <behavior>
    - Test: Upload route accepts .mscz file and calls processMuseScoreFile
    - Test: Upload route accepts .mscx file and calls processMuseScoreFile
    - Test: Upload route stores original .mscz file at `library/originals/{fileId}.mscz`
    - Test: Upload route stores original .mscx file at `library/originals/{fileId}.mscx`
    - Test: Upload route stores converted MusicXML with application/xml mimeType
    - Test: Upload route still rejects unsupported file types (.doc, .txt)
    - Test: Library index entry has mimeType 'application/xml' and originalStorageUrl for MuseScore uploads
    - Test: UploadDialog ACCEPTED_TYPES includes .mscz and .mscx
    - Test: UploadDialog validExt regex accepts .mscz files
    - Test: UploadDialog validExt regex accepts .mscx files
    - Test: UploadDialog still rejects unsupported extensions (.doc, .txt)
  </behavior>
  <action>
**Write tests first:**

1. Create `src/app/api/library/__tests__/upload-musescore.test.ts`:
   - Mock `@/lib/musescore-converter` to return a known MusicXML string
   - Mock `@/lib/firebase-storage` uploadToStorage
   - Mock `@/lib/firebase-admin` (follow pattern from existing `list.test.ts`)
   - Test that POST with a .mscz file triggers conversion and dual storage (original stored at `library/originals/{fileId}.mscz`)
   - Test that POST with a .mscx file triggers conversion and dual storage (original stored at `library/originals/{fileId}.mscx`)
   - Test that non-MuseScore files are unaffected (existing behavior)

2. Create `src/components/library/__tests__/upload-dialog-musescore.test.ts`:
   - Test that ACCEPTED_TYPES constant includes `.mscz` and `.mscx`
   - Test that the validation regex accepts filenames ending in `.mscz` and `.mscx`
   - Test that the validation regex still rejects unsupported extensions like `.doc` and `.txt`
   - These can be simple unit tests importing the constants/regex or rendering the component and checking the file input's `accept` attribute

Run tests -- should FAIL.

**Then implement changes:**

1. **`src/app/api/library/upload/route.ts`:**
   - Add import: `import { processMuseScoreFile } from '@/lib/musescore-converter'`
   - Add to `ALLOWED_TYPES`: `'application/x-musescore': '.mscz'`, `'application/octet-stream': '.mscz'` (browsers send generic MIME for .mscz)
   - Update the extension regex on line 64 to include mscz and mscx: `/\.(pdf|xml|musicxml|mxl|mscz|mscx)$/i`
   - After reading the buffer (line 80), detect MuseScore files by extension:
     ```typescript
     const msExt = file.name.match(/\.(mscz|mscx)$/i)?.[1]?.toLowerCase() as 'mscz' | 'mscx' | undefined
     ```
   - If `msExt` is set, call `processMuseScoreFile(buffer, msExt)` to get `{ musicXml, originalContent }`
   - Store the original: `await uploadToStorage(`${fileId}-original`, originalContent, 'application/octet-stream')` at path `library/originals/${fileId}.${msExt}`
   - Override the buffer with the converted MusicXML: `const xmlBuffer = Buffer.from(musicXml, 'utf-8')`
   - Set `contentType = 'application/xml'` so the main storage and library_index entry treat it as MusicXML
   - Add `originalStorageUrl: `library/originals/${fileId}.${msExt}`` to the indexEntry for MuseScore uploads
   - Add `sourceFormat: msExt` to the indexEntry so we know this was converted from MuseScore
   - Wrap the conversion in try/catch: if conversion fails, return 422 with error message "Failed to convert MuseScore file: {error}". Do NOT store a broken file.
   - Update the error message on line 66 from "Only PDF and MusicXML files are supported" to "Only PDF, MusicXML, and MuseScore files are supported"
   - Log conversion: `logger.info(\`[Upload] Converting ${file.name} from ${msExt} to MusicXML\`)`

2. **`src/components/library/UploadDialog.tsx`:**
   - Change `ACCEPTED_TYPES` to `".pdf,.xml,.musicxml,.mxl,.mscz,.mscx"`
   - Change the `validExt` regex to `/\.(pdf|xml|musicxml|mxl|mscz|mscx)$/i`
   - Change the drop zone help text from "PDF or MusicXML" to "PDF, MusicXML, or MuseScore"
   - Change the error toast from "Only PDF and MusicXML files are supported." to "Only PDF, MusicXML, and MuseScore files are supported."
   - Change the `DialogDescription` text to include MuseScore

Run tests -- should PASS.
  </action>
  <verify>
    <automated>cd C:/Users/dsbog/CentralReform.live/sheet-music-app && npx vitest run src/app/api/library/__tests__/upload-musescore.test.ts src/components/library/__tests__/upload-dialog-musescore.test.ts --reporter=verbose && npx vitest run src/components/library/__tests__/song-charts-library.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Upload route accepts .mscz/.mscx, converts via processMuseScoreFile, stores both original and converted files with correct extension-specific paths. Client UI accepts MuseScore file types. All tests pass including new upload-dialog-musescore tests and existing song-charts-library tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify MuseScore upload and rendering end-to-end</name>
  <files>n/a</files>
  <action>Push to main and verify the full MuseScore upload flow on the deployed Vercel site. This is a human-verify checkpoint -- no code changes, only visual/functional verification.

What was built: Full MuseScore upload and conversion pipeline -- users can upload .mscz/.mscx files through the UploadDialog, the server converts them to MusicXML, and the converted scores render in the OSMD viewer.

Steps to verify:
1. Push to main and wait for Vercel deployment
2. Navigate to the Library page
3. Click "Upload" button
4. Verify the file picker shows .mscz and .mscx as accepted types (dialog text says "PDF, MusicXML, or MuseScore")
5. Upload a .mscz file from MuseScore 4
6. Verify upload succeeds with a success toast
7. Find the uploaded song in the library
8. Click to open it -- verify the score renders in the OSMD viewer
9. Check that notes, time signature, and key signature appear correctly
10. (Optional) Upload a .mscx file and verify the same flow works

Resume signal: Type "approved" if the score renders correctly, or describe any issues with the conversion output.</action>
  <verify>Manual verification by user -- visual confirmation that converted MusicXML renders correctly in OSMD viewer</verify>
  <done>User has confirmed that uploaded MuseScore files render correctly in the OSMD viewer</done>
</task>

</tasks>

<verification>
- `npx vitest run` -- full test suite passes (no regressions)
- Upload dialog accepts .mscz/.mscx files
- Upload route converts MuseScore files to MusicXML
- Both original and converted files stored in Firebase Storage
- Library index entry has correct mimeType for OSMD rendering
</verification>

<success_criteria>
- User can upload a .mscz or .mscx file through the UploadDialog
- Server converts the file to MusicXML and stores both original + converted
- Converted MusicXML renders in OSMD viewer
- Existing PDF and MusicXML upload flows are unaffected
- All tests pass (new + existing)
</success_criteria>

<output>
After completion, create `.planning/phases/18-musescore-file-import-and-musicxml-conversion/18-02-SUMMARY.md`
</output>
