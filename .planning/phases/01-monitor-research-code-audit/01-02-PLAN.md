---
phase: 01-monitor-research-code-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/codebase-audit.md
  - src/lib/setlist-store.ts
  - src/app/(main)/tasks/page.tsx
  - src/app/api/tasks/create/route.ts
  - src/app/api/tasks/delete/route.ts
  - src/app/api/tasks/update/route.ts
  - src/components/setlist/tasks/TaskSheet.tsx
  - src/components/admin/UsageAnalyticsSection.tsx
  - src/app/(main)/leader/page.tsx
  - src/types/models.ts
  - src/middleware.ts
  - src/components/library/SongChartsLibrary.tsx
  - src/components/library/LibraryFileRow.tsx
  - src/components/setlist/v2/SetlistEditorV2.tsx
autonomous: true
requirements: [CODE-01, CODE-02]

must_haves:
  truths:
    - "All confirmed dead code (tasks page, tasks API, TaskSheet, UsageAnalytics, leader redirect, legacy setlist-store) is deleted from the codebase"
    - "The app builds successfully after all deletions (npm run build passes)"
    - "All existing tests pass after deletions (vitest run passes)"
    - "A codebase audit document catalogs every cleanup item with clear recommendations"
    - "The ideal Zustand store architecture is planned and documented (not implemented)"
    - "Admin features are categorized as essential vs duct-tape with recommendations for Phase 5"
  artifacts:
    - path: "docs/codebase-audit.md"
      provides: "Complete audit document with dead code inventory, store consolidation plan, admin simplification recommendations"
      min_lines: 100
  key_links:
    - from: "src/components/library/SongChartsLibrary.tsx"
      to: "src/lib/setlist-store.ts"
      via: "import { useSetlistStore }"
      pattern: "useSetlistStore"
    - from: "src/components/library/LibraryFileRow.tsx"
      to: "src/lib/setlist-store.ts"
      via: "import { useSetlistStore }"
      pattern: "useSetlistStore"
    - from: "src/components/setlist/v2/SetlistEditorV2.tsx"
      to: "src/lib/setlist-store.ts"
      via: "import { useSetlistStore }"
      pattern: "useSetlistStore"
---

<objective>
Audit the entire codebase to identify cleanup opportunities, then delete confirmed dead code and produce a comprehensive audit document that guides future phases.

Purpose: Clean the codebase now while there are no users and no risk. The audit document becomes the roadmap for store consolidation (Phase 2-5) and admin simplification (Phase 5). Dead code removal reduces the surface area for bugs and confusion during upcoming feature work.

Output: Clean codebase (dead code removed, build passing), comprehensive audit document.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monitor-research-code-audit/01-CONTEXT.md
@.planning/phases/01-monitor-research-code-audit/01-RESEARCH.md

<interfaces>
<!-- Dead code import chains that must be traced before deletion -->

From src/lib/setlist-store.ts (DEAD — replaced by setlist-firebase.ts):
- Exported: useSetlistStore hook
- Imported by: SetlistEditorV2.tsx, SongChartsLibrary.tsx, LibraryFileRow.tsx
- These imports may be dead code paths within those files — verify before deletion

From src/types/models.ts (task types to remove):
- TaskStatus type
- SetlistTask interface
- Only used by task-related files

From src/components/admin/UsageAnalyticsSection.tsx:
- Imported by admin page — need to remove import and usage

Current Zustand stores (document architecture, do NOT refactor):
1. store.ts (279 lines) — music player, transposition, zoom, AI chord scanner
2. monitor-store.ts (162 lines) — mixer state
3. library-store.ts (90 lines) — file library search/filtering
4. annotation-store.ts (131 lines) — per-file drawing annotations
5. chat-store.ts (89 lines) — AI chat window state
6. alert-store.ts (45 lines) — global system alert/banner
7. congregation-store.ts (97 lines) — org config and feature flags
8. setlist-store.ts (63 lines) — **DEAD** legacy, replaced by setlist-firebase.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete confirmed dead code with build verification after each step</name>
  <files>src/lib/setlist-store.ts, src/app/(main)/tasks/page.tsx, src/app/api/tasks/create/route.ts, src/app/api/tasks/delete/route.ts, src/app/api/tasks/update/route.ts, src/components/setlist/tasks/TaskSheet.tsx, src/components/admin/UsageAnalyticsSection.tsx, src/app/(main)/leader/page.tsx, src/types/models.ts, src/middleware.ts, src/components/library/SongChartsLibrary.tsx, src/components/library/LibraryFileRow.tsx, src/components/setlist/v2/SetlistEditorV2.tsx</files>
  <action>
Delete dead code in this order, running `npm run build` after each group to catch issues early. Per CONTEXT.md: "Aggressive. If code is clearly unused or half-baked, recommend removing it. No users yet -- no risk."

**Group 1: Tasks system (completely cut from v2 scope)**
1. Delete `src/app/(main)/tasks/page.tsx` (tasks page — 290 lines)
2. Delete `src/app/api/tasks/create/route.ts` (tasks API create)
3. Delete `src/app/api/tasks/delete/route.ts` (tasks API delete)
4. Delete `src/app/api/tasks/update/route.ts` (tasks API update)
5. Delete `src/components/setlist/tasks/TaskSheet.tsx` (tasks component — ~200 lines)
6. Delete the parent directories if they become empty: `src/app/api/tasks/`, `src/components/setlist/tasks/`
7. In `src/types/models.ts`: Remove the `TaskStatus` type and `SetlistTask` interface (only used by task code). Leave all other types intact.
8. Run `npm run build` — fix any import errors that surface. Grep for "TaskSheet", "TaskStatus", "SetlistTask", "/tasks" imports and remove any remaining references.

**Group 2: UsageAnalyticsSection (cut from v2 scope)**
1. Delete `src/components/admin/UsageAnalyticsSection.tsx` (377 lines)
2. Find and remove the import/usage in the admin page: grep for "UsageAnalyticsSection" in `src/app/(main)/admin/` or `src/app/(main)/manage/`. Remove the import line and the `<UsageAnalyticsSection />` component usage.
3. Run `npm run build` — verify clean.

**Group 3: Legacy /leader route**
1. Delete `src/app/(main)/leader/page.tsx` (redirect page)
2. In `src/middleware.ts`: find references to '/leader' route (lines ~39, ~66 per RESEARCH.md) and remove them. Be careful to only remove the leader-specific lines, not break other route checks.
3. Run `npm run build` — verify clean.

**Group 4: Legacy setlist-store.ts (most delicate — has 3 importers)**
1. First, examine each importing file to understand how `useSetlistStore` is used:
   - `src/components/library/SongChartsLibrary.tsx` — look for `useSetlistStore` usage. It likely reads/writes setlist state that is now handled by setlist-firebase.ts. Remove the import and any code paths that use it. The component should still work for its library browsing purpose.
   - `src/components/library/LibraryFileRow.tsx` — same analysis. Remove `useSetlistStore` import and any code using it.
   - `src/components/setlist/v2/SetlistEditorV2.tsx` — same analysis. This file may have more extensive usage. If the useSetlistStore usage is deeply entangled, leave the import with a `// TODO: Remove setlist-store dependency (Phase 4)` comment instead.
2. For each file, after removing the import, check if the file still compiles. If removing the store usage breaks functionality, add a `// TODO` comment explaining what needs replacing and leave the import.
3. Delete `src/lib/setlist-store.ts` only after all importers are cleaned up or TODO'd.
4. Run `npm run build` — must pass.
5. Run `npx vitest run` — all existing tests must pass.
  </action>
  <verify>
    <automated>cd C:/Users/dsbog/CentralReform.live/sheet-music-app && npm run build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>All confirmed dead code is deleted. Build passes. Tests pass. No broken imports remain. Any code that could not be cleanly removed has a TODO comment explaining why.</done>
</task>

<task type="auto">
  <name>Task 2: Write comprehensive codebase audit document</name>
  <files>docs/codebase-audit.md</files>
  <action>
Produce a comprehensive codebase audit document at `docs/codebase-audit.md`. This document guides cleanup work in Phases 2-5. Per CONTEXT.md: "Plan now, execute later" for store consolidation.

Analyze the full codebase — all 155 components, 69 API routes, 8 stores (now 7 after deleting setlist-store), 18 hooks, 90+ lib files. Use grep, file counts, and import analysis.

Structure:

**1. Dead Code Removed (Phase 1)**
- Table of everything deleted in Task 1 with line counts
- Any items that could NOT be removed and why (with TODOs)

**2. Additional Dead Code Candidates**
- Scan for components that are never imported (orphaned)
- Scan for API routes that are never called from the frontend
- Scan for hooks that are never used
- Scan for lib files that are never imported
- For each candidate: file path, line count, evidence it's dead, risk assessment
- Categorize: "safe to delete" vs "needs investigation"

**3. Zustand Store Architecture Plan**
- Current state: 7 stores, their responsibilities, line counts, and consumers
- Problems: what's fragmented? What's in the wrong store? What's coupled?
- Target state: define the ideal store architecture for v2.0
  - Which stores merge? Which stay separate?
  - What is each store's single responsibility?
  - Per CONTEXT.md: "Define the ideal Zustand store architecture. Actual refactoring happens during the phases that build features on those stores."
- Migration plan: for each store change, which phase does it happen in?

**4. Component Audit**
- Count components by directory
- Identify oversized components (>300 lines) — candidates for splitting
- Identify components with poor separation of concerns
- Note components that will be rebuilt in Phases 3-4 (setlist editor, performance view)

**5. Admin Feature Triage**
- List every admin feature/page/component
- Categorize each as: essential (keep), duct-tape (remove when backend is fixed), simplify (Phase 5)
- Per CONTEXT.md: "Audit should identify which admin features are duct tape vs. essential. Recommend what to keep, cut, and simplify (feeds Phase 5)."

**6. API Route Audit**
- List all 69 API routes with their purpose
- Identify routes that are dead, redundant, or should be consolidated
- Note routes that need hardening (error handling, validation, rate limiting)

**7. Dependency Health**
- Check package.json for unused dependencies (use depcheck or manual analysis)
- Flag any deprecated packages
- Note packages with known security issues

**8. Recommended Cleanup by Phase**
- Phase 2 cleanup tasks (monitor-related)
- Phase 3 cleanup tasks (setlist/performance-related)
- Phase 4 cleanup tasks (editor-related)
- Phase 5 cleanup tasks (backend/library-related)
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const doc = fs.readFileSync('docs/codebase-audit.md', 'utf8'); const sections = ['Dead Code Removed', 'Store Architecture', 'Component Audit', 'Admin Feature', 'API Route', 'Cleanup by Phase']; const missing = sections.filter(s => !doc.includes(s)); console.log('Sections:', sections.length - missing.length, '/', sections.length); if (missing.length > 0) { console.log('Missing:', missing); process.exit(1); } const lines = doc.split('\\n').length; console.log('Lines:', lines); if (lines < 100) { console.log('ERROR: too short'); process.exit(1); }"</automated>
  </verify>
  <done>Codebase audit document exists with all 8 sections. Dead code inventory is complete. Zustand store target architecture is defined with per-phase migration plan. Admin features are triaged. API routes are cataloged. Each recommendation includes which phase it belongs to.</done>
</task>

</tasks>

<verification>
- `npm run build` passes after all dead code deletion
- `npx vitest run` passes (no test breakage)
- `docs/codebase-audit.md` exists with all required sections
- No orphaned imports remain (grep for deleted file paths returns nothing)
- Store architecture plan covers all 7 remaining stores with target state
</verification>

<success_criteria>
- CODE-01 satisfied: Dead code identified and removed, additional candidates documented with evidence
- CODE-02 satisfied: Zustand store architecture planned with target state and per-phase migration schedule (no refactoring in Phase 1)
- Build and tests pass after all changes
- Audit document provides actionable guidance for Phases 2-5
</success_criteria>

<output>
After completion, create `.planning/phases/01-monitor-research-code-audit/01-02-SUMMARY.md`
</output>
