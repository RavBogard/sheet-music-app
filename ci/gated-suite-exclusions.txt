# Gated Unit Suite — the exclusion list. R-0904-live-cw-21 §2, adopted by
# R-0904-live-cw-23 on Daniel's word ("whatever you recommend").
#
# WHAT THIS IS. `Gated Unit Suite` is the required status check on `master`.
# It runs the whole vitest suite MINUS the files listed here. Every line is a
# test file whose failure does not block a promotion — i.e. this file is the
# repo's counted test debt, written down as a list of names instead of a
# number nobody can see.
#
# THE LIST IS REMOVE-ONLY (R-0904-live-cw-21 §2(c)). A line may be deleted by
# any lane the moment CI reports that file green in the gated job. A line may
# NOT be added: adding one is the same act as hiding a bonded row — the number
# improves and the thing the number was for is gone. An addition needs a
# RULING from Daniel, and `ci/check-exclusions-remove-only.sh` fails the build
# if one appears without it.
#
# WHEN THIS FILE IS EMPTY, the gate moves: `Unit & Integration Tests` becomes
# the required check and the `Gated Unit Suite` job is deleted
# (R-0904-live-cw-21 §2(c)). Until then both jobs run, so the FULL suite
# number stays visible on every push rather than hiding behind the subset.
#
# Format: one repo-relative test path per line. `#` comments and blank lines
# are ignored. `wc -l` is not the debt — the guard prints the real count.
#
# INSTALLED AT SEVEN, NOW AT TWO (R-0904-live-cw-23 §2, then G6). The list
# went in carrying the seven red files Daniel adopted the shape against, and
# five came off in the same wave on CI evidence — `Unit & Integration Tests`
# (the whole suite, unrequired) reported exactly 3 failures at `1bb4c775f2`
# and again at `e2084e32dc`, all three inside the two files left below, which
# is the only CI evidence that CAN exist for an excluded file: the gated job
# cannot report a file it is excluding. The gated run on the commit that
# removes them is the confirmation.
#
# WHAT IS LEFT IS THE REAL DEBT. Both remaining files are red for a reason
# that is NOT a bug in the test, and each leaves this list in its own wave:
#
#   registry.test.ts — REMOVED 2026-09-15 (live-integration). The two tests
#   caught a real class-2 defect: `seasonal.shehecheyanu@shabbat-shacharit`
#   on folio 145 against a declared `pages: 144`. R-0904-live-cw-27 held the
#   line open pending "a PRINTED page", but Ruling 8 (2026-09-15) settles that
#   there is no printed page to wait for: `shabbat-shacharit` is an ALPHA
#   DRAFT and its folios are never published. The number was answered from the
#   build instead — the snapshot came from `dist/` @8ab7be6, whose screen PDF
#   is 153 physical pages at a constant folio offset of 7, so the last folio is
#   146, and 144 was the figure belonging to a DIFFERENT build (dist-app
#   7d2cbcc+dirty, 59 of 94 unit rows apart). registry.json now records 146
#   with that provenance, and the file is green in the gated run.
#
#   sync-engine-songs-mirror.test.ts — Scenario 1, left red under the
#   GREEN-REPAIR order's own STOP clause: every Drive shortcut is dropped at
#   ingestion (`junk-filter.ts:35` rejects the whole
#   `application/vnd.google-apps.` prefix, and the filter reads `f.mimeType`,
#   not `shortcutDetails.targetMimeType`), so no chart-shaped shortcut fixture
#   exists and turning it green would test a shape Drive cannot return.
#   Leaves when R-0904-live-cw-28 rules whether a shortcut-bonded row should
#   reach `songs/*` at all.
#
# NOTE: `src/lib/org/__tests__/registry.test.ts` is a DIFFERENT, green,
# out-of-scope file and is deliberately NOT on this list (R-0904-live-cw-10 —
# an order names its file in full; so does an exclusion).

src/lib/__tests__/sync-engine-songs-mirror.test.ts
