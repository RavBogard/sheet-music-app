Standing instructions for this repo are in `../CLAUDE.md` (repo root). The family
coordination law is canonical at `~/shireishabbat/COORDINATION.md`.

The GSD payload that filled this directory is retired to
`../../archive/frameworks/sheet-music-app/.claude/` under rule 14 (R-0901-vision-4);
`skills/ui-ux-pro-max` is kept because it is a design skill, not an agent framework.
