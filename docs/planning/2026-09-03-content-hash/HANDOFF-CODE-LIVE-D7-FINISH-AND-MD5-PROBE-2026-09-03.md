# ORDER → `live` (Code): finish D7's two rows, write the program's first new mark, and probe the metadata the md5 guard needs

Lane: **live-cw (Opus Cowork)** · **Executor: `live`**, host-side in `~/CentralReform.live`
Authority: **R-0903-live-cw-7** (the two held rows go to `active`; the status is WRITTEN, not RECOVERED, and the
record says which) · **R-0903-live-cw-8** (Daniel at the keyboard 21:4xZ: the Mizmor mark, and mark-never-delete as
the standing precedent) · **R-0903-live-cw-9** (probe before backfilling; a guard that can only pass vacuously is
retired, never left green) · R-0903-live-cw-5 · R-0903-live-cw-2 §2a · §5 · R-0903-live-cw-3 §2 · rules 1, 4, 5,
8 addendum, 11, 18 · R-0803-168 · R-0831-guards-2 · **R-0903-live-cw-10** (the guard form, this amendment) ·
**R-0903-live-cw-11** (what F3's answer decided).
**AMENDED (1) 2026-09-03 23:4xZ by `live-cw`, under `R-0903-live-cw-10`.** `live` stopped this order at G2 and was
right to: G2/G3/G4 pinned ABSOLUTE CATALOG TOTALS, and Daniel uploaded a chart at 21:32:01Z, which made the stated
post-states arithmetically unreachable. **The three guards are restated below as DELTAS on the named rows — not
as `+1`**, which would be correct for one wave and rebuild the same trap for the next. F1, F2 and their
discipline are unchanged. **F3 is COMPLETE** and is not re-run; its answer is ruled in `R-0903-live-cw-11`.
**AMENDED (2) 2026-09-04 00:2xZ by `live-cw`, under `R-0904-live-cw-1` and `R-0904-live-cw-2`.** F1 LANDED and
is verified; **F2 IS REMOVED FROM THIS ORDER** — `R-0903-live-cw-8`'s mark is a byte-decided SINGLE-ROW mark and
no tool on this server can write one reversibly, so F2 waits for an instrument, not for a reconsideration
(`R-0904-live-cw-2` §2 fixes that tool's shape; it ships in the batched deploy and the mark is its own wave
after). **G5 is re-cut to the property it protects** — `priorStatus` carries no VALUE; the tool's `null` tombstone
satisfies it, and both rows PASS (`R-0904-live-cw-1` §1). **G4 and G6 leave with F2.** **This order now closes on
F1 + F4.**
Status: DONE — closed by `RETURN-CODE-LIVE-D7-FINISH-2026-09-03.md` rev-2 (F1 done and verified, F4 returned, F2 left the order, F3 closed by `R-0903-live-cw-11`). **This order WRITES THREE STATUS FLIPS to the production
catalog and nothing else.** **No deploy, no `src/` change** — every tool it uses shipped at 20:27:30Z.
Verified-against: `5e52e29ff0` [inherited: your 20:4xZ return, which reports production serving it since
2026-09-03T20:27:30Z] — the tree is not the subject here; **the CATALOG is**, and G2 is its measurement.
Tier: CLOSED — it states a measured pre-state and an exact post-state after each wave.
NEXT: F4 return, and close. F1 is DONE, F3 is DONE, F2 has LEFT this order — re-run nothing.

---

## 1 · What this finishes

Your D7 restored `Adon Olam` and held two, because their `toStatus` would have been a status you chose rather than
one you read. **That stop was right and it is not overridden.** What changed is the question, not the tolerance:
**`R-0903-live-cw-5` had already ruled the mark on these rows WRONG**, and a mark that should never have been
written has no prior state to restore — only a correct status to be given, which R-0903-live-cw-3 §2's asymmetry
decides. `R-0903-live-cw-7` records the corroboration you offered (the pre-L1-W2 scan could not see an `archived`
row) as corroboration and not as the reason.

**Your correction to G5 is accepted and is in the ruling:** `fileId` + `toStatus` consults no run record, so all
three rows were always reachable. G5 named the wrong mechanism for a hazard that was real.

## 2 · F1–F4 · The wave

- [ ] **F1 · Restore the two held rows, explicitly, one call each.** `1HmJ7mu9qYx6eGVaJcg88Hklsei7bnjcAR-ZYmjkgfbU`
      (Hashiveinu) and `1PYjUUqxH12ip7Uz5aFP7q1wRKwi-pKbvVNJB8G-wV6k` (Mi shebeirach), through
      `undo_dedupe_group` in row mode with **`toStatus: "active"` passed explicitly**, never defaulted.
      **`R-0903-live-cw-7`'s discipline is the load-bearing part and it is yours to honour in the return: do NOT
      write, synthesise or backfill a `priorStatus` for either row**, and state per row that `active` was
      **DERIVED under R-0903-live-cw-7**, not read from any artifact. A value that launders itself into the record
      as a recovered fact is the defect this order exists to avoid.
- [~] **F2 · REMOVED from this order by amendment 2 — do not run it.** The mark stands as Daniel decided it
      (`R-0903-live-cw-8`, unchanged and unspent); what it lacks is a tool that can write one row reversibly, which
      `R-0904-live-cw-2` §2 specifies and the batched deploy carries. The original text follows for the record
      only: ~~**F2 · Mark `1VuMq83_0W8ya9SCeBaQ0vCvuFeHgGHeC` (`Mizmor Shiru L'adonai .mp3`) `duplicate`**, canonical
      `1d-aXA4WzVjKYvCxAaVyezGgzRKlXs9y_` (`Mizmor Shiru Ladonai.mp3`). **This is the first new mark the program
      has authorized, Daniel's own at 21:4xZ, and it OVERRIDES the canonical picker's default**, which kept the
      other row on earliest-upload. Write it through the marking path that records a run and a `priorStatus`, so
      the mark is reversible by `undo_dedupe_group` the moment it exists — G6 is that requirement as a guard.
      **`1czN_ywRWm2bnjIrnNVg7ad4RyynM_GcR` (`Mizmor Shiru Ladonai.pdf`) IS NOT IN THIS.** It is the chart of the
      same song, correctly ungrouped by the format rule, and G7 exists because a wave that reaches it has
      misunderstood `R-0903-live-cw-5`.~~
- [x] **F3 · DONE 23:1xZ — do not re-run.** Metadata is present and universal: 1,201 of 1,201 `library/` objects
      carry `md5Hash` and `crc32c`; 863 of 892 rows have such an object [inherited: `RETURN-CODE-LIVE-D7-FINISH-2026-09-03.md` R4].
      **`R-0903-live-cw-11` rules the outcome: the cross-check is BUILT, not retired**, it certifies the READ and
      says so in the source, and it reports its own denominator. That is a `src/` change and it rides the batched
      deploy, not this order. The original text of this track follows, struck, for the record only:
      ~~**F3 · Probe for an object md5. A READ, and nothing else.**~~ Take a sample of at least ten rows spanning
      **both sources** (Storage-backed `upload-*` rows and Drive-backed rows) and **both populations** (`chart`
      and `nonChart`), and report, per row, whether the object's own metadata exposes an md5 — Storage
      `md5Hash`, Drive `md5Checksum` — and what it is where present. **Do not backfill anything and do not touch
      `src/`.** `R-0903-live-cw-9` splits on this measurement: metadata present → a backfill is buildable and the
      cross-check can fire; absent → `md5CrossCheck` is retired in the source with its reason. **Both outcomes are
      a `src/` change and neither is this order** — they ride the next deploy together with the four items §4
      lists. Your probe is what decides which.
- [ ] **F4 · Return** `RETURN-CODE-LIVE-D7-FINISH-2026-09-03.md` + a CLOSED board row: the three flips with
      before/after status per row, the derived-basis sentence for the two restores, the reversibility proof for
      the mark, the probe's per-row table, and the closing census. **Under amendment 2 this is the CLOSING return: F1's two
      flips with before/after and the derived-basis sentence per row, G5's re-cut result, the recorded pre- and
      post-partitions as observations, and the `forceScore` dry-run plan as evidence for `R-0904-live-cw-3`. F2's
      absence is stated, not apologised for.**

## 3 · Guards that can fail

**G1 · The standing Rosh Hashanah read, before F1 and after F2.** `list_books` → `shirei-tshuvah` **184 / `feed`**.
[inherited: green at both your 20:0xZ and 20:4xZ reads.] FAIL: any other page count or tier.

**G2 · The pre-state — RECORDED, not predicted (amended, `R-0903-live-cw-10`).** Take the full `fileId → status`
map before F1 and **record the partition in the return as an observation with its provenance. There is no expected
total and a total cannot fail this guard.** What CAN fail it is the five rows this order names, which must read:
`1HmJ7mu9qYx6eG…` **`duplicate`** · `1PYjUUqxH12ip7…` **`duplicate`** · `1VuMq83_0W8ya9…` **`active`** ·
`1d-aXA4WzVjKYv…` **`active`** · `1czN_ywRWm2bnj…` **`active`**. FAIL: any named row in any other status —
then something moved that this order does not understand, and that is still a stop.

**G3 · After F1: the DELTA is exactly the two named rows.** Re-read the map. Over the `fileId`s **present in both
reads**, the set whose status DIFFERS must be **exactly** `{1HmJ7mu9qYx6eG…, 1PYjUUqxH12ip7…}`, each
`duplicate → active`. **A `fileId` present only in the later read is new authoring: RECORD it in the return —
who, when, what — and carry on. It is not a failure** unless it is one of the five named rows. FAIL: any other
row's status differing. This is still the guard that catches a row-mode call that took a group; it just cannot be
tripped by Daniel uploading a chart mid-wave.

**G4 · WITHDRAWN with F2 (amendment 2).** It travels with the mark to the wave that can write it.

**G5 · No `priorStatus` VALUE was written for either restored row (re-cut, amendment 2, `R-0904-live-cw-1` §1).**
Read both docs after F1 and confirm `priorStatus` carries **no value**. **`null` PASSES** — that is
`undo_dedupe_group`'s own tombstone (`undo-dedupe.ts:211-220` clears the three dedupe stamps by writing `null`, so
a restored row cannot report itself as belonging to a run that no longer holds it), and what is present is the
absence of an invention. FAIL: a `priorStatus` carrying an actual STATUS on either — that is the invention
`R-0903-live-cw-7` forbids arriving through the back door of a helpful tool. **The original wording — "the field
is still absent" — could not have passed for any restore, ever; it was written from the design, not the
implementation.**

**G6 · WITHDRAWN with F2 (amendment 2)** — and it did its job on the way out: it is the guard that refused a
mark nothing could reverse, which is how the missing tool was found. It returns with F2. Original text:
~~**G6 · The new mark is reversible the moment it is written.** After F2, confirm a run record exists carrying that
row's `priorStatus: "active"`, and that `undo_dedupe_group`'s dry run for it plans a clean restore. **Do not
execute the undo** — plan it. FAIL: no record, or a plan that cannot restore. **A mark that is not reversible is
not authorized by `R-0903-live-cw-8`**, whose whole precedent is that hiding is the recoverable act; if the record
does not appear, STOP and the mark comes back off.~~

**G7 · The PDF is untouched.** `1czN_ywRWm2bnjIrnNVg7ad4RyynM_GcR` reads `active` before and after. FAIL: anything
else.

## 4 · Stop conditions

- **STOP if any of the five named rows is not in the status G2 lists.** Not on a total — `R-0903-live-cw-10`
  retires absolute totals as stop conditions on this catalog, because Daniel authors into it continuously and a
  pinned total is one upload away from a false stop. **Record the totals; fail on the rows.**
- **STOP if any flip moves a row this order does not name.** Three rows change status here. Three.
- **Do not write the mark at all** (amendment 2). No composition of tools substitutes for the one that does not
  exist — `R-0904-live-cw-2` §1 refuses the `undo_dedupe_group` + `seed_legacy_dedupe_run` path by name.
- **Do not call `dedupe_library` with `forceScore` other than `dryRun: true`** (`R-0904-live-cw-3` §2), on this
  wave or any other, until the format-class gate ships.
- **F3 is closed. Do not re-run it and do not backfill from it.** `R-0903-live-cw-11` holds its outcome.
- **No deploy and no `src/` change.** The four items waiting on a deploy are named so they cannot creep in: the
  dead Google-Apps demotion, the two emulator tests asserting the forbidden shape, `get_chart_status`'s false
  green, and `delete_chart`'s hint pointing at readers that cannot see the gap. **They ride one deploy together
  with F3's outcome, in an order this desk writes when F3 answers.**
- **The `songs`-without-`library_index` census is a DIFFERENT ORDER** you also hold
  (`HANDOFF-CODE-LIVE-SONGS-DIVERGENCE-CENSUS-2026-09-03.md`). Do not start it inside this one.

## 5 · Reversibility, stated plainly

**Every write in this order is reversible, and that is not an accident of the wave — it is the first time it has
been true.** W2 and W3 shipped at 20:27:30Z; before that a mark was recoverable only from a hand-written JSON file
in another repository. F1's two restores can be re-marked; F2's mark is undone by the record G6 insists on. **F3
writes nothing at all.** If any of that stops being true mid-wave, that is the stop condition, not a detail.

Claude records; Daniel decides.
