# COORDINATION.md — pointer stub

The family coordination policy is canonical at `~/shireishabbat/COORDINATION.md` on this machine
(`C:/Users/dsbog/shireishabbat/COORDINATION.md`). Read it at session open.

Since R-0919-audit-8 it says one thing: **each Claude Code session is executor and producer for
its own repo.** There is no baton board — `~/shireishabbat/STATUS.md` is archived — and no row to
open before work here touches liturgy data. Commits do not carry a `Lane:` trailer.

What still holds across repos, and is not a process rule but a content one:

- The licensing posture. `shireishabbat/build/tools/export-scan.sh` is the public/private
  boundary there; shirei-tshuvah-web stays private; the deployed reader stays open.
- The pinning law. A PRINTED volume's registry rows generate from its press commit, never HEAD.
- Hebrew is sacred text: verify, never improvise.
- No liturgical text enters this repo, and chart bytes leave it only through the approved grant.
